# Abzu 项目宪法（AGENTS.md）

> 本文件是**入口与路由**：项目一句、红线、去哪读、常用命令。**规则条文不在这里**——命名与格式在标准里，协作流程在施工机制里。
>
> **两个环境，别混**：**开发**＝本仓库（`AGENTS.md`／`docs/`／`scripts/`），读者是开发者与开发用的 AI 工具——这些内容**不随产品分发**，与运行时无关；**运行**＝`skills/` 下的 skill 组（**尚未创建**），唯一运行目标是 **Claude Code**（装进用户项目后以 `/{skill 名}` 显式调用）。开发**不要求**在 Claude Code 里进行——开发工具可换，产品宿主不换。
>
> **业务层在哪**：本项目业务（网文写作的产品设计）在 `docs/product/`——读哪份由规格的「必读输入」指定，本文件不做路由。

**项目一句**：Abzu——面向长篇网文创作的 AI 写作 skill 套件（运行宿主唯一：Claude Code）；业务设计见 `docs/product/`；**当前仓库只有开发侧基座——`skills/` 与产品层各 skill 均尚未创建**。

## 一、红线（违反即停）

1. **git 默认只读**：commit／push 等一切 git 写操作，每次单独取得作者明确确认；作者对其他操作的授权不外延到 git 提交推送。
2. **临时产物不入库**：临时脚本与输出一律放 `.tmp/<任务>/`，用完即删；不以 `test_*` 前缀伪装正式测试。
3. **外部参考只读**：引用的外部项目与参考目录绝对只读，不在其中安装依赖、跑构建、跑联网命令。
4. **失败先判因**：任何测试／校验失败，先定位原因再改；**禁止改断言、改数字、删检查来变绿**。
5. **密钥与凭据不入库**：token、key、密码、`.env` 类文件不进 git；凭据一律用环境变量。
6. **禁带病开工**：动工前先确认 `bash scripts/check.sh` 全绿；有红先呈报裁决，不在红基线上叠改动。基线定义见 [`测试与验收标准`](docs/standards/测试与验收标准.md) §1.2（建设期内未建设能力的失败不计入红基线）。

## 二、路由（什么内容去哪份文档）

**动手改任何非平凡内容前**：先读 [`docs/specs/施工机制.md`](docs/specs/施工机制.md) §二 定路径（轻／标准／重），再按该路径执行。

| 你要做什么 | 读哪份 |
| --- | --- |
| 写代码／文档前的「怎样才合规」（命名、格式、skill 形态、产物契约、触发、测试、版本、规格写作） | [`docs/standards/`](docs/standards/)（**一个标准一份文件**——命名／文档写作／skill 形态／产物契约／触发与交互／版本与分发／测试与验收／规格写作／术语／运行环境／安全与合规） |
| 怎么协作施工（角色、规格流程、核验、审查、止损、模板） | [`docs/specs/施工机制.md`](docs/specs/施工机制.md) |
| 在制与历史规格（某个决定当年为什么这么定） | [`docs/specs/`](docs/specs/) 与 [`docs/specs/archive/`](docs/specs/archive/) |
| 某个决定／某次变更是怎么回事（沿革） | [`CHANGELOG.md`](CHANGELOG.md) 与 git 历史 |

## 三、常用命令

- 一键检查（提交前必跑）：`bash scripts/check.sh`——提交门禁启用后由 `pre-commit` 自动执行（红了即拦截提交）
- 提交门禁启用（一次性，每个克隆各做一次）：`git config core.hooksPath scripts/hooks`；hook 件见 `scripts/hooks/pre-commit`，未设此配置的克隆不会自动拦截
- Markdown 体检：`npx markdownlint-cli2 "**/*.md"`

## 四、不做清单（真源在此；各标准引用本节，不复述）

**继承（多源验证过）**：

- RAG／向量检索——复杂度与收益不成比例
- 数据库后端（任何形式）——文件即真相已够用
- 常驻服务／Dashboard 产品化——无真实需求
- 多宿主适配——唯一宿主 Claude Code，不做第二宿主通用化
- CI 里跑 LLM 或联网——守卫零外部依赖
- 拦截式宿主 hook——宿主操作拦截 hook 只提醒不拦截，写作流程不能被卡死（须「宁可漏拦不可误伤」）；**git 本地提交门禁不在此列**（pre-commit 已启用）

**追加（事故驱动）**：

- 平行台账（施工日志／审核记录）——规格 + git 历史即台账
- 注册表类守卫（行为契约／能力接线／引用闭包）——引入任何机制须先登记事故出身
- shared-assets 多副本同步——单一真源 + 引用，从根源避免副本
- CI workflow——单人阶段，本地校验脚本够用
- doc-budget 字数预算体系——有条件缓建（**解冻触发**：任一热路径文件连续两批触碰行数上限，或单文件实测 > 2 万字符，届时以字符实测立法；热路径＝触发即全量加载的文件，行数上限见 `skill形态标准` §5）
- 多人协作治理件（CONTRIBUTING／CODE_OF_CONDUCT／OWNERS）——单人开发；收到外部 PR 时再评估

引入任何新机制前先过**内生必要性自问**（能否不新增 → 能否并入既有文件 → 必须新增时的验收标准与索引同步）；触碰本节即停，先修订本节并取得作者确认。

## 五、三条分工原则

1. **脚本做确定性、AI 做语义、作者做品味**：新增能力先问该哪层做。
2. **候选永不拦截**：机器检出的候选类发现只呈报，不阻断流程、不影响退出码。
3. **文件账本律**：工作流每一步必须落盘或修改某个文件；没有文件落点的步骤不存在。
