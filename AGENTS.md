# Abzu 项目宪法（AGENTS.md）

> 本文件是**入口与路由**：项目一句、红线、去哪读、常用命令。**具体标准与操作细则不在这里**——命名与格式在标准里、协作流程在施工机制里；**宪法级约束（红线／不做清单／分工原则）在本文件**；本文件的修改与权威顺序见 §六；本文件在起作用时的信号见 §七。
>
> **两个环境，别混**：**开发**＝本仓库（`AGENTS.md`／`docs/`／`scripts/`），读者是开发者与开发用的 AI 工具——这些内容**不随产品分发**，与运行时无关；**运行**＝`skills/` 下的 skill 组（**尚未创建**），唯一运行目标是 **Claude Code**（装进用户项目后以 `/{skill 名}` 显式调用）。开发**不要求**在 Claude Code 里进行——开发工具可换，产品宿主不换。
>
> **业务层在哪**：本项目业务（网文写作的产品设计）在 `docs/product/`——读哪份由规格的「必读输入」指定，本文件不做路由。

**项目一句**：Abzu——面向长篇网文创作的 AI 写作 skill 套件（运行宿主唯一：Claude Code）；业务设计见 `docs/product/`；**当前状态：运行侧（`skills/`）与产品层各 skill 均尚未创建——本仓库只有开发侧基座**。

## 一、红线（违反即停）

1. **git 默认只读**：**一切 git 写操作**（包括但不限于 `add`／`commit`／`push`／`tag`／`reset`／`checkout -b`），每次单独取得作者明确确认；作者对其他操作的授权不外延到 git 提交推送。（自测：这条命令是读还是写？写＝本次确认拿到了吗？）
2. **临时产物不入库**：临时脚本与输出一律放 `.tmp/<任务>/`，用完即删；不以 `test_*` 前缀伪装正式测试。（自测：这是产物还是临时件？后者＝在 `.tmp/` 且收工即删吗？）
3. **外部参考只读**：引用的外部项目与参考目录绝对只读，不在其中安装依赖、跑构建、跑联网命令。（自测：会让外部目录发生变化吗？会联网跑东西吗？）
4. **失败先判因**：任何测试／校验失败，先定位原因再改；**禁止改断言、改数字、删检查来变绿**。（自测：我改的是原因，还是断言／数字／检查？）
5. **密钥与凭据不入库**：token、key、密码、`.env` 类文件不进 git；凭据一律用环境变量。（自测：里面有没有 token／key／密码／连接串之类字面？）
6. **禁带病开工**：动工前先确认 `bash scripts/check.sh` 全绿；有红先呈报裁决，不在红基线上叠改动。基线定义见 [`测试与验收标准`](docs/standards/测试与验收标准.md) §1.2（建设期内未建设能力的失败不计入红基线——**何谓「未建设」见该节判据句**）。（自测：我是跑了 `check.sh`，还是记得它绿？）

## 二、路由（什么内容去哪份文档）

**动手改任何非平凡内容前**：先读 [`docs/specs/施工机制.md`](docs/specs/施工机制.md) §二 定路径（轻／标准／重），再按该路径执行（裁量分级与送审纪律见 `施工机制` §二／§五）。

| 你要做什么 | 读哪份 |
| --- | --- |
| 写代码／文档前的「怎样才合规」（命名、格式、skill 形态、产物契约、触发、测试、版本、规格写作） | [`docs/standards/`](docs/standards/)（**一个标准一份文件**——命名／文档写作／skill 形态／产物契约／触发与交互／版本与分发／测试与验收／规格写作／术语／运行环境／安全与合规）（**[文件清单见 `命名标准` §1 目录结构](docs/standards/命名标准.md)**） |
| 怎么协作施工（角色、规格流程、核验、审查、止损、模板） | [`docs/specs/施工机制.md`](docs/specs/施工机制.md) |
| 在制与历史规格（某个决定当年为什么这么定） | [`docs/specs/`](docs/specs/) 与 [`docs/specs/archive/`](docs/specs/archive/) |
| 某个决定／某次变更是怎么回事（沿革） | [`CHANGELOG.md`](CHANGELOG.md) 与 git 历史 |
| 某些词在这里是什么意思（作者／候选类发现／红基线／负向流程守卫……） | [`docs/standards/术语标准.md`](docs/standards/术语标准.md) |

## 三、常用命令

- 一键检查（提交前必跑）：`bash scripts/check.sh`——提交门禁启用后由 `pre-commit` 自动执行（红了即拦截提交）
- 提交门禁启用（一次性，每个克隆各做一次）：`git config core.hooksPath scripts/hooks`；hook 件见 `scripts/hooks/pre-commit`，未设此配置的克隆不会自动拦截
- Markdown 体检：`npx markdownlint-cli2 "**/*.md"`

## 四、不做清单（真源在此；各标准引用本节，不复述）

条目格式：不做 ＋ 理由 ＋ 已有逃生口 ＋ 解冻触发条件——逃生口＝不引入该机制时用什么既有做法达到同一目的（确无替代写「无」）；解冻触发条件＝出现何种情况才重新评估。

**继承（多源验证过）**：

- RAG／向量检索——复杂度与收益不成比例；逃生口：文件系统直读 ＋ 全仓搜索（`grep`）与目录清单定位，书稿规模下单机内可定位；解冻触发条件：单书体量（文件数或总字数）超出人工与 `grep` 可定位的范围，且实测证明检索成为瓶颈
- 数据库后端（任何形式）——文件即真相已够用；逃生口：以文件为唯一真源，版本与差异交给 git；解冻触发条件：出现真源必须支持并发多写或多端同时编辑的需求（单人写作前提下不存在）
- 常驻服务／Dashboard 产品化——无真实需求；逃生口：一次性 CLI／脚本命令 ＋ Markdown 报告（看内容进编辑器，看结论读文档）；解冻触发条件：出现必须实时刷新或多人在线查看的状态需求
- 多宿主适配——唯一宿主 Claude Code，不做第二宿主通用化；逃生口：宿主相关内容只写在 `README.md` 安装说明，域资产用宿主无关的 Markdown ＋ 脚本表达；解冻触发条件：作者裁定新增第二宿主（须先修订本节）
- CI 里跑 LLM 或联网——守卫零外部依赖；逃生口：本地 `bash scripts/check.sh` ＋ 人工场景走查（可机械化的上断言，判断留人）；解冻触发条件：出现必须无人值守自动执行、且仍然零外部依赖的检查需求（**例外**：`check.sh` [1] 段的 lint 工具链经 `npx` 调 markdownlint，属**环境前提**，见 [`运行环境标准`](docs/standards/运行环境标准.md) §一.3）
- 拦截式宿主 hook——宿主操作拦截 hook 只提醒不拦截，写作流程不能被卡死（须「宁可漏拦不可误伤」）；**git 本地提交门禁不在此列**（pre-commit 已启用）；逃生口：要拦的走负向流程守卫与 `check.sh` 段，提交门禁走 pre-commit；解冻触发条件：出现不拦即造成不可逆损失（误删书稿、误发布之类）的事故，且实测确认非拦截不可

**追加（事故驱动）**：

- 平行台账（施工日志／审核记录）——规格 + git 历史即台账；逃生口：当批规格的本批核销（相关时）、开放项与审查记录节 ＋ `CHANGELOG` 一行 ＋ git 历史；解冻触发条件：单批记录量确实无法由规格承载、且查证成本已成实际瓶颈
- 无事故出身的注册表类守卫（行为契约／能力接线／引用闭包）——不做：守卫的价值取决于事故出身，凭空建表既无判据也无维护人（**引入任何此类机制须先登记事故出身**）；逃生口：人工审查 ＋ 当批规格的核验清单（内容轨检查器按需扩展）；解冻触发条件：同类事故复发，且该检查可机械化为零外部依赖的断言
- shared-assets 多副本同步——单一真源 + 引用，从根源避免副本；逃生口：共享内容放单一真源文件，他处只引用；解冻触发条件：出现无法用引用表达的共享资产（如必须随包分发的二进制）
- CI workflow——单人阶段，本地校验脚本够用；逃生口：本地 `bash scripts/check.sh` ＋ pre-commit hook；解冻触发条件：进入多人协作，或出现必须在远端环境（如 Linux）才能复现的检查
- doc-budget 字数预算体系——有条件缓建；逃生口：`skill形态标准` §5 行数上限 ＋ 先压缩／下沉冷路径／调上限的处理序（体积由既有条目约束）；解冻触发条件：任一热路径文件连续两批触碰行数上限，或单文件实测 > 2 万字符，届时以字符实测立法；热路径＝触发即全量加载的文件，行数上限见 `skill形态标准` §5
- 多人协作治理件（CONTRIBUTING／CODE_OF_CONDUCT／OWNERS）——单人开发；逃生口：[`docs/specs/施工机制.md`](docs/specs/施工机制.md) §一 三角色与规格流程即协作约定；解冻触发条件：收到外部 PR 时再评估
- 门禁启用的机械自检（`setup.sh` 或 `check.sh` 检测 `core.hooksPath`）——不做：启用命令与「未设不拦截」已明文（§三 ＋ `施工机制` §七 的「提交前必跑」），再建自检即第二载体；逃生口：**提交前必跑 `bash scripts/check.sh` 的纪律**（hook 只是自动化、不增检测面——克隆未设 hooksPath 时由该纪律覆盖同一风险）；解冻触发条件：出现「未启用 hooksPath 而漏拦截」的实际事故

引入任何新机制前先过**内生必要性自问**（能否不新增 → 能否并入既有文件 → 必须新增时的验收标准与索引同步）；触碰本节即停，先修订本节并取得作者确认；**解冻一律由作者裁决并记入 `CHANGELOG`**。

## 五、三条分工原则

1. **脚本做确定性、AI 做语义、作者做品味**：新增能力先问该哪层做。
2. **候选永不拦截**：机器检出的候选类发现只呈报，不阻断流程、不影响退出码。（自测：这条发现会挡住谁吗？会＝它到底是不是候选？）
3. **文件账本律**：工作流每一步必须落盘或修改某个文件；没有文件落点的步骤不存在。（自测：这一步落在哪个文件上了？答不出＝该步不存在。）

## 六、本文件的修改与权威顺序

1. **修改本文件须作者确认**；触碰 §四 不做清单先按该节末句修订本节。
2. **权威顺序**：本文件的宪法级约束 ＞ `docs/standards/` ＞ `docs/specs/`（规格与施工机制）＞ 其他文档；**两处规定相抵时以本序定取舍；规划方／施工方对同一 finding 的分歧按 [`施工机制`](docs/specs/施工机制.md) §六 升级**。
3. **例外**：**具体真源的指定不在此序**——某事项**或某一类规则**以哪份为准若已有明文指定（**如协作与流程类以 `施工机制` 为准**、术语叫法以 [`术语表`](docs/product/术语表.md) 为准、域清单以 [`架构设计`](docs/product/架构设计.md) 为准），从其指定——此时本序的层级**不适用**。

## 七、本文件在起作用时（治理文档自检信号）

本节信号用于治理文档（读者＝开发 AI／开发者）；与 `文档写作标准` §一.9.2 的产品层信号（作者不打开 skill 即可自查）判据不同，两者不得互引为据。

- 规格里的数字与锚点能被独立复算——重跑一下就与规格所写一致（判据见 `规格写作标准` §一.4／§5）
- 拦下问题的是机器与审查，不是自觉——提交前的变动里没有清单外文件（判据见 `施工机制` §三 核验三步）；链接若被改动，断链按当批规格的链接命令现场核（机检段 `[3]` 待建，尚无常驻闸）
- 工具报得出的与应当报出的一致——`[4]` 段的每条预警都已逐条判为「属预期」或「待处置」、无未核项；若某个检查连续几批只报「跳过」而无人核，先查它是不是空转
- 每批改动可追溯——规格、`CHANGELOG` 一行与 git 历史三者能还原「为什么这么改」（判据见 `施工机制` §三 过程产物两项）
