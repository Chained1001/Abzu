# Abzu 项目宪法（AGENTS.md）

> 本文件是**入口与路由**：红线、路由、常用命令、不做清单、分工原则、权威顺序、模型执行通用准则。**具体标准与细则不在这里**——命名在标准里、协作在 `施工机制` 里、业务在 `docs/product/`。
>
> **开发 ≠ 运行**：本仓库（`AGENTS.md`/`docs/`/`scripts/`）是开发侧，不随产品分发；`skills/` 下是运行侧（唯一宿主 Claude Code）。

**项目一句**：Abzu——面向长篇网文创作的 AI 写作 skill 套件（运行宿主唯一：Claude Code）；业务设计见 `docs/product/`；当前以开发侧基座为主。

## 一、红线（违反即停）

| # | 规则 | 自测 |
| --- | --- | --- |
| 1 | **git 默认只读**——凡改变仓库状态的 git 命令，每次单独取得作者明确确认（`fetch` 等只动 `.git/` 者在内；对其他操作的授权不外延到 git） | 这条 git 命令会改仓库吗？会＝确认拿到了吗？ |
| 2 | **临时产物不入库**——临时脚本与输出放 `.tmp/<任务>/`，用完即删 | 这是临时件吗？＝在 `.tmp/` 且收工即删吗？ |
| 3 | **外部参考只读**——不在外部项目/参考目录中安装依赖、跑构建、跑联网命令 | 会让外部目录变化吗？ |
| 4 | **失败先判因**——禁止改断言/改数字/删检查来变绿；合法路径=呈报→裁定→修订规格→再改（见 [`施工机制`](docs/specs/施工机制.md) §六） | 我改的是原因，还是断言？ |
| 5 | **密钥不入库**——token/key/密码/`.env` 用环境变量 | 里面有密钥字面吗？ |
| 6 | **禁带病开工**——动工前 `bash scripts/check.sh` 全绿；红起点定义见 [`测试与验收标准`](docs/standards/测试与验收标准.md) §1.2 | 我跑了 check.sh，还是记得它绿？ |

## 二、路由

**动手改非平凡内容前**：先读 [`施工机制`](docs/specs/施工机制.md) §二 定路径。

| 任务 | 去哪份 |
| --- | --- |
| 写代码/文档前查合规 | [`docs/standards/`](docs/standards/)（文件清单见 [`命名标准` §1](docs/standards/命名标准.md)） |
| 协作施工（角色/流程/核验/审查/停手） | [`docs/specs/施工机制.md`](docs/specs/施工机制.md) |
| 在制与历史规格 | [`docs/specs/`](docs/specs/) 与 [`docs/specs/archive/`](docs/specs/archive/) |
| 沿革（为什么这么改） | [`CHANGELOG.md`](CHANGELOG.md) 与 git 历史 |
| 术语叫法 | [`术语表`](docs/product/术语表.md)；治理类词条见 [`术语标准`](docs/standards/术语标准.md) |
| 发布／版本／`CHANGELOG` 条目格式 | [`版本与分发标准`](docs/standards/版本与分发标准.md) |
| 文档写法与用词（含禁用清单） | [`文档写作标准`](docs/standards/文档写作标准.md)／[`用词标准`](docs/standards/用词标准.md) |
| 安全红线／运行环境与工具链 | [`安全与合规标准`](docs/standards/安全与合规标准.md)／[`运行环境标准`](docs/standards/运行环境标准.md) |
| 产物字段契约／规格写法 | [`产物契约标准`](docs/standards/产物契约标准.md)／[`规格写作标准`](docs/standards/规格写作标准.md) |

**改某区域前先读什么**（开工说明／自查按此表点名，不再逐批手写）：

| 区域 | 先读 |
| --- | --- |
| 协作流程（角色／流程／核验／审查／停手） | `施工机制` §一–§五 |
| 加／改守卫判定标准 | `施工机制` §四 ＋ `测试与验收标准` §1.1／§三／§4（**清单全集**含 `施工机制-附录.md`） |
| 写规格 | `规格写作标准` ＋ `施工机制` §二／§三；模板与逐条确认节计算方式见 `施工机制-附录.md` §三 |
| 事故记录／事故追溯 | `施工机制` §六 一行式索引 → 全文见 `施工机制-附录.md` §一 |
| 命名／术语 | `命名标准` ＋ `术语标准`（产品词见 `术语表`） |
| skill 运行资产 | `skill形态标准` §6／§13 ＋ `触发与交互标准` |
| 产品层设计 | `docs/product/` 对应域设计 |
| 历史沿革 | `CHANGELOG` ＋ `docs/specs/archive/` |

## 三、常用命令

- 一键检查（提交前必跑）：`bash scripts/check.sh`
- 提交前自动检查启用（一次性）：`git config core.hooksPath scripts/hooks`
- Markdown 体检：`npx markdownlint-cli2 "**/*.md"`

## 四、不做清单（唯一出处在此）

> 格式：不做 + 理由 + 解冻条件 + 解冻条件。增删改本节须作者确认；解冻由作者裁决记入 CHANGELOG。

**继承**：RAG/向量检索（单书体量超人工与 grep 可定位、且实测成瓶颈）｜ 数据库后端（唯一出处须并发多写或多端同编）｜ 常驻服务/Dashboard（须实时刷新或多人在线查看）｜ 多宿主适配（作者裁定增第二宿主）｜ CI 跑 LLM/联网（须无人值守且仍零外部依赖）｜ 拦截式宿主 hook（不拦即不可逆损失且实测非拦不可）

**追加**：平行清单（单批记录规格载不下、且查证成本成实际瓶颈）｜ 无事故出身注册表守卫（同类事故复发且可零依赖机械化）｜ shared-assets 多副本（出现无法用引用表达的共享资产）｜ CI workflow（多人协作、或须远端环境才能复现的检查）｜ doc-budget 字数预算（每批必读文件连两批触行数上限、或单文件 >2 万字符）｜ 多人协作治理件（收到外部 PR）｜ 提交前自动检查启用机械自检（未设 hooksPath 而漏拦截的实际事故）

（**各条括注＝解冻触发关键词**；完整理由与解冻条件见 git 历史原版本〔`578815a` 及此前的本节〕——速查索引即现行法，详注供追溯。）

引入新机制前过**内生必要性自问**：能否不新增 → 能否并入既有文件 → 必须新增时的验收标准与索引同步。

## 五、三条分工原则

1. **脚本做确定性、AI 做语义、作者做品味**
2. **候选永不拦截**——机器检出的候选类只呈报，不阻断、不影响退出码
3. **文件账本律**——工作流每一步必须写入文件或修改某个文件

## 六、修改与权威顺序

1. 修改本文件须作者确认。
2. **权威顺序**：宪法级约束 ＞ `docs/standards/` ＞ `docs/specs/` ＞ 其他。
3. **具体唯一出处指定优先于层级**（如协作以 `施工机制` 为准、术语以术语表为准）；指定不得抵触红线/不做清单/分工原则；指定冲突按 `施工机制` §六升级。

## 七、自检信号

本节为治理文档信号；产品层信号见 `文档写作标准` §一.9.2——两者判定标准不同，不得互引为据。

| 信号 | 判定标准 | 唯一出处 |
| --- | --- | --- |
| 数字可复算 | 重跑与规格一致 | `规格写作标准` §一.4/§5 |
| 拦下问题的是机器与审查 | 提交无清单外文件 | `施工机制` §三 |
| 工具报得出与应报出一致 | [4] 段 A–J 无未核项 | `check.sh` 头注 |
| 每批可追溯 | 规格+CHANGELOG+git 三者还原 | `施工机制` §三 |

## 八、模型执行通用准则

> 本节是保底做法：标准与《施工机制》有规定的从其规定；没规定的按本节执行。

1. **动手前**：按 §二 路由读对应文件；读完仍不确定怎么做，停下向作者呈报，不凭猜测动工。
2. **改动范围**：只改任务点名的文件；发现必须同时改其他文件时，先呈报、征得同意再改。
3. **检查失败**：先找原因再改；禁止改断言、改数字、删检查来变绿（红线 4）；找不到原因即停下呈报。
4. **规则冲突**：按 §六 权威顺序取高者；同层级两处规则冲突时停下呈报，不自行选边。
5. **默认不做**：不新增机制、不另建记录清单、不引外部依赖、不跑联网命令（先过 §四 不做清单与内生必要性自问）。
6. **收尾必做**：`bash scripts/check.sh` 全绿；`CHANGELOG` 落一行（改了什么＋核验结论）。
