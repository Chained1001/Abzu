# 087-2026-09-15-最小壳与真机试验

> 定位：**首个真机资产批（拆批后的前半）**——只建**一个最小壳** `skills/abzu-outline/SKILL.md`（四节骨架齐、内容从简），目的是把三件事**先问清楚再铺开**：① 调用形态（`/` 菜单可见性）② 模型自主触发是否真被 `disable-model-invocation` 抑制 ③ 该键是否被生态接受（**成稿审查实测 `agentskills validate` 拒绝它**——见 §一②）。同时启用 `check.sh [2]` 段（**纯本地断言、零外部依赖**）、同步各处「未建」状态真源，并交作者**真机走查**（含本地安装路径——见 §一⑨）。**阶段一四件资产移入 088**。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**重路径**——动 `skills/` 域资产（架构级）→ **成稿审查 ＋ 核验三步 ＋ 双维产物审查 ＋ 真机走查**（本批改标准条文 → 产物审查取**双维**）。scope：**skill 资产＝skill**、**产品层＝product**、**标准件＝standards**、**开发守卫＝scripts**。
> 自由度分布：[A]×4 [B]×8（F1／F2／F3／F5）
> 必读输入：本规格 §一；（壳）`docs/standards/skill形态标准.md` §2／§3／§4（H1 格式、frontmatter 五键、description 三段式）＋ §1（目录形态）＋ §6（088 用）；（交互）`docs/standards/触发与交互标准.md` §1／§2／§3；（状态真源）`docs/product/架构设计.md` §一／§二、`产品定义:10`、`大纲域设计` 头部与 §一、`docs/standards/命名标准.md` §1／§2／§三、`docs/standards/测试与验收标准.md` §1.1／§1.2／§3
> 施工形态：subagent（规划方 spawn 的施工 subagent；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `skills/` 尚未建立；`check.sh` 段现状 | `ls -d skills` → 不存在；头注三条 TODO（`# TODO: [2] skill 格式校验`／`[3] 内容轨`／`[5] 脚本语法`，`:8`／`:9`／`:11`）；`[0]` 跳过、`[1]` lint **55 件**（**含在制规格**——lint 忽略面仅 `.zcode/**`／`node_modules/**`／`.tmp/**`）0 issue、`[4]` 实跑（候选非阻断；**绝对数随在制规格变化、不写入本规格**——窄域复审 R6）✓ |
| ② | **本机已装 `skills-ref 0.1.1`，实测拒绝 `disable-model-invocation`** | `command -v agentskills` → 存在；含键壳 `rc=1`（`Unexpected fields in frontmatter: disable-model-invocation. Only ['allowed-tools','compatibility','description','license','metadata','name'] are allowed.`）；去键壳 `rc=0`；目录名≠`name` 时 rc=1；**同名目录＋错 H1＋无四节仍 `rc=0`**（该工具只校 frontmatter 与目录名同名，**不查 H1／不查四节名**）✓ |
| ③ | 壳的硬要求（`skill形态标准`） | §2 H1「{产品名} · {中文 skill 名}（{英文 skill 名}）」＋四节名；§3 frontmatter 五键（含 `disable-model-invocation: true`，该条标「**未实测·待核**」并明文要求首个域落地实测两项＋**回写四要素**）；§4 三段式（**禁写工作流摘要**）；§1 目录形态（`SKILL.md` 必需；`references/`／`assets/`／`scripts/` 按需建，**无 `{skill 名}` 子目录层**）✓ |
| ④ | 各处「未建」状态陈述（**本批须同步**——逐处列出，**不标总数**：口径歧义（按「处」或按「行」、表格若干行算 1 或 N）易致不可复算，窄域复审 R4） | **产品层 6 处**：`大纲域设计:4`／`:22`／`:26`（占位·待建设等）＋`:197`／`:200`（过时文字）、`大纲域设计-附录:3`（分流路径）→ F8／F9。**外部 3 处**：`产品定义:10`、`架构设计:10`（状态列）／`:19`（联订句）→ F6／F7。**标准件 5 处**：`命名标准:39`（现状段）／§1 目录树（**新增 `skills/` 子树**——现树内**无**该行，成稿审查 F-10）／§2 表六行（`（预留）` **14→8**）／§三 机检锚点（`:119`「已实现（**目录未建时跳过**）」）→ F10；`测试与验收标准:24`（§1.1 `[2]` 行——**F5① 的落点**，同 R4）／`:38`（§1.2 已实现段枚举）／**`:102`（§3 机检锚点 `[2]` 行「待建」——与 §1.1 行**同件互斥**，成稿审查 F-04）** → F5；`skill形态标准:14`（「现状预留」）／`:21`（「`skills/` 尚未建立，本条为规则先行」）／`:47`（`[2]` 段表行「待建」）／`:140`（§13 矩阵「待接入」）／`:170`（机检锚点「待建」）→ F11。**`AGENTS.md:5`／`:9` 须作者确认**（§八 1）✓ |
| ⑤ | 交互纪律（`触发与交互标准`） | §1 入口唯一／禁互调／域外休眠；§2 门控＝阶段级硬停（三选）／停靠三级／须落产物状态表行／中断契约标准句／**未建「域」如实告知**（只覆盖域，未覆盖**阶段**——本批新立，§二 口径要点③）；§3 交互模态（每问句标模态、禁宿主 UI 词）✓ |
| ⑥ | description 逐字稿（F-14 收窄后） | 见 §二 F2——**不点四阶段序**（§4.2 禁工作流摘要）；施工时 python 实计字符数须 ≤1024 并记入报告 ✓ |
| ⑦ | 计数口径 | `grep -c` 计**行**、`grep -o … \| wc -l` 计**出现次数**——本规格计数断言**逐条标注口径**；实测 `命名标准` 的 `（预留）`＝**11 行／14 处**（去 6 行标注后 **5 行／8 处**）✓ |
| ⑧ | 基线 | `bash scripts/check.sh` exit **0**；链接一行式 **175／断链 0**；`[4]` 段对在制规格实跑（候选非阻断、逐条属预期）✓ |
| ⑨ | **远端 `main` 上躺着重建前的同名旧壳**（成稿审查 F-2 查出） | `git ls-tree --name-only origin/main` → 含 `skills`／`.claude-plugin`；`git ls-tree -r origin/main -- skills` → **31 文件**（旧六域：`abzu-outline`／`volume`／`write`／`style`／`analyze`／`scan`——窄域复审 R5 订正：原写「26 文件」且漏列 `write`）；`git show origin/main:skills/abzu-outline/SKILL.md` → H1＝`# Abzu · 大纲（未建设）`、**无 `disable-model-invocation`**、无四节骨架；**本分支 `rebuild` 无 upstream** ⇒ 仓内**多处**记载的安装命令（`README:8`／`验收场景:19`／`版本与分发标准:52`／`:74`——窄域复审 R5 订正：原写「唯一」，实为 ≥4 处）装的是**旧资产**，直接走查会测错对象、污染 §八 3 的结论 ✓ |

**不变量（施工前后须同）**：**改动面与禁改面逐件对账**——本批改动**全部落点**：`skills/abzu-outline/SKILL.md`（新建）／`scripts/check.sh`（`[2]` 段＋头注一行）／`docs/standards/测试与验收标准.md`（§1.1 `[2]` 行＋§1.2 已实现段句＋§3 机检锚点 `[2]` 行）／`docs/standards/命名标准.md`（§1 目录树新增子树＋§1 现状段＋§2 表六行＋§三 机检锚点句）／`docs/standards/skill形态标准.md`（§1 标题与现状句＋§3 走查回写＋§13 矩阵列＋机检锚点 `[2]` 行）／`docs/product/产品定义.md:10`／`docs/product/架构设计.md`（`:10`＋`:19`）／`docs/product/outline/大纲域设计.md`（`:4`／`:22`／`:26`／`:197`／`:200`）／`docs/product/outline/大纲域设计-附录.md:3`／`CHANGELOG.md`；**除此之外零改动**（`AGENTS.md` 不动；`docs/specs/` 含 `archive/` 零改动；产品层**设计判据**零改动）。链接一行式 **175／断链 0**（新建资产内零 markdown 链接）。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `skills/abzu-outline/SKILL.md`（新建） | **最小壳**：H1 逐字 `# Abzu · 大纲域（abzu-outline）`；frontmatter 五键（`name: abzu-outline`／`description`（逐字见 F2）／`license: MIT`／`disable-model-invocation: true`／`metadata.version: "0.1.0"`）；四节骨架逐字节名——①**入口判定**（查 `创作进度.md` 有无 → 无＝全新开书；有＝读产物状态表报「你在哪」；**按文件证据、不凭对话记忆**）②**工作流**（四阶段流程随 088–091 落地；本节暂一句指认）③**门控与交互原则**（门控＝阶段级硬停（确认／要改／先停）／停靠三级／每问句标模态／门控与停靠须落产物状态表行——细则引 `触发与交互标准` §2／§3）④**语言**（对作者话术零黑话） | ③｜`触发与交互标准` §2／§3 | [A] |
| F2 | 同件（`description` 逐字） | 「Abzu 套件的大纲域——把作者的初步想法压成一页可执行的创作约束集，并沿阶段推进长成全书大纲，管到定稿门为止。触发方式：仅 /abzu-outline 显式调用；自然语言提及不触发。本域不产出卷纲级与章级内容（卷纲归 abzu-volume、章纲与正文归 abzu-write）；已退役域不参与。」（字符数 ≤1024，施工时实计记入报告） | ③（§4 三段式） | [A] |
| F3 | 同件（**建设状态段**，置于壳末） | **如实告知段**（逐字）：「**本域建设状态**：最小壳（087）——仅壳骨架与调用形态；规格收集等阶段细则尚未落地（随 088–091）。**未建阶段不模拟、不顶替、不产出占位内容**。」（该行为**无标准锚**——本批新立，§八 2 登记；**其行为验收落点**随 088 走查批，见 §八 6） | ⑤（口径要点③） | [A] |
| F4 | `scripts/check.sh`（`[2]` 段实现 ＋ 头注一行） | **`[2]` 段＝纯本地断言（零外部依赖）**——逐 `skills/*/` 校**逐壳六项**＋段行一条（⑦ 头注，段级非逐壳判据——施工期按补修 4 定口径；规格原写「七项」已同步）（成稿审查独立判断②：**不引入 `agentskills`**——其白名单无本仓硬约束键（长期空转）、不查 H1／四节名（覆盖面反窄）、需解析其输出（易碎））：①`SKILL.md` 存在；②H1 匹配 `^# .+ · .+（.+）$`；③frontmatter 含五键（`name`／`description`／`license`／`disable-model-invocation`／`metadata`）；④四节名齐（入口判定／工作流／门控与交互原则／语言）；⑤**官方三条约束**（`name` 与目录同名；`name` ≤64 字符；`description` 非空且 ≤1024 字符）；⑥**目录形态**（`skills/{skill 名}/` 下不得出现 `{skill 名}` 同名子目录层；出现的子目录名须在 `references`／`assets`／`scripts` 三名单内）；⑦**头注** `# TODO: [2] skill 格式校验…` → `# [2] skill 格式校验…`（**列 0**，`_seg_summary` 按头注取段状态）。**输出形态（成稿审查 F-11）**：**仅段标题行以 `[2]` 加一空格起首**，其余行按既有体例（`x` 空格／两空格缩进）；跳过须打**手动路径**行。**零外部工具**——`agentskills validate` 的结论改由**走查期人工跑一次**取得（§三 断言 11） | ①②⑦｜086 §八 2（目录形态机检归本批）｜成稿审查 F-02／F-05／F-11／F-13＋独立判断② | [B] |
| F5 | `docs/standards/测试与验收标准.md`（**3 处**） | ①§1.1 的 `[2]` 行：「管什么」列改「skill 壳结构与 frontmatter 校验（**本地断言**：H1／五键／四节名／官方三条约束／目录形态）」、现状列「待建（脚本头注已留 TODO 位）」→「**已实现**」②§1.2「已实现段（现为 [0]／[1]／[4]）」→ 含 `[2]`③**§3 机检锚点的 `[2]` 行**（`:102`「各壳 frontmatter 与 description 合规｜`agentskills validate`（接入 check.sh [2]）｜**待建**」——成稿审查 F-04：与 §1.1 行**同件互斥**）→「本地断言（`check.sh [2]`）」／「**已实现**」 | §3（条目失效同批更新）｜成稿审查 F-04／F-05 | [A] |
| F6 | `docs/product/产品定义.md:10` | 「三域均未建设，`skills/` 尚未创建——套件范围即此三域，无第四个域。」→「**大纲域最小壳已建（087，试验性）**，卷纲域与正文域未建；`skills/` 已建——套件范围即此三域，无第四个域。」 | ④ | [B] |
| F7 | `docs/product/架构设计.md`（`:10` ＋ `:19`） | ①`:10` 大纲域行「未建」→「**最小壳（087）**」②`:19` 的联订句（「首个域落地时…两处实测口径同批订正（本处与 `docs/standards/skill形态标准.md` §3）」）→ 走查后**与 F11 同批回写**（成稿审查 F-12：原写「与 F10 同批」指认错位；施工期先不改，待走查结论） | ④（成稿审查 F-05 f／F-12） | [B] |
| F8 | `docs/product/outline/大纲域设计.md`（5 处） | ①`:4`「本域仍为**占位·待建设**——运行资产…归后续域建设批」→「本域**最小壳已建（087）**，各阶段细则随 088–091 落地——运行资产（域壳、`references/`、`assets/`、脚本）随各批落地」②`:22`「（`abzu-outline`；占位·待建设）」→「（`abzu-outline`；**最小壳已建，阶段细则构建中**）」③`:26`「`skills/abzu-outline/` 的域壳、references 与脚本尚未创建，归域建设批」→「`skills/abzu-outline/` 最小壳已建（087）；`references/`／`assets/` 与脚本随各批落地」④`:197`「部分进 `skills/abzu-outline/references/`」→「模板进 `assets/`、方法论与词表进 `references/`」⑤`:200`「运行资产（域壳、references、脚本）」→「运行资产（域壳、`references/`、`assets/`、脚本）」（**④⑤ 目标文本不带行内代码**——与现文体例一致，成稿审查 F-03） | ④（成稿审查 F-05 a–c） | [B] |
| F9 | `docs/product/outline/大纲域设计-附录.md:3` | 「随 `skills/abzu-outline/references/` 分流」→ **与 F8④ 同句逐字**（成稿审查 F-06：初稿此处词序与 F8④ 相反——「词表与方法论」，按 F8④ 统一为「方法论与词表」） | 086 §八 4 | [B] |
| F10 | `docs/standards/命名标准.md`（4 处） | ①§1 目录树**新增 `skills/` 子树**（该树现无此行，成稿审查 F-10）——形态：`skills/abzu-outline/` ＋ `SKILL.md`（`references/`／`assets/`／`scripts/` 标注「随各批落地」）②§1「现状与预留」段「`skills/`（skill 资产）与 `.claude-plugin/`（分发清单）尚未建立」→「`skills/` 已建（首个域＝`abzu-outline` 最小壳）；`.claude-plugin/`（分发清单）尚未建立」③§2 表**六行去「（预留）」**（域名行／skill 壳／域工作流／阶段方法论／域内参考文件／产物模板）——**11 行／14 处 → 5 行／8 处**④§三 机检锚点「`skills/` 为真目录非符号链接…已实现（**目录未建时跳过**）」→ 改述（目录已建，不再跳过） | ④（成稿审查 F-04／F-05 d／F-10） | [B] |
| F11 | `docs/standards/skill形态标准.md`（**6 处**） | ①§1 标题的「（`skills/{skill 名}/`，**现状预留**）」→ 去「现状预留」②§1 现状句「现状：`skills/` 尚未建立，本条为规则先行」→「现状：`skills/` 已建（`abzu-outline` 最小壳）」③§3 调用控制键条：**走查回写**（删「未实测·待核」标记＋回写四要素「宿主版本／实测日期／结论／发现的不一致」——内容由作者走查提供）④§13 内容规范矩阵的 `[2]` 相关列（`:140`）→「本地断言（`check.sh [2]`）」⑤**机检锚点 `:170`（同一行的两格）**——「条文」格改为「**本地断言**（`check.sh [2]`）」、「强制方式／状态」格改为「**已实现**」（窄域复审 R9：原列 ⑤⑦ 两项目同指此行）⑥`:47` 的 **§3 校验工具条** → 「校验：**本地断言**（`check.sh [2]`）；**外部校验器仅走查期可选**（其白名单不含本仓硬约束键）」——**目标文本刻意不含那四个词的字面串**（断言 8d 要求该串在该件归零，若目标文本自带即自相抵，窄域复审 R1） | ③｜成稿审查 F-04＋窄域复审 R1／R9 | [B] |
| F12 | `CHANGELOG.md`（`## [Unreleased]` → `### Added` 段首） | 一行：『最小壳与真机试验（087）：…』——最小壳／`[2]` 段启用（纯本地：逐壳六项＋段行一条）／状态真源同步 14 处／真机走查（本地安装路径）；**≤400 字符**、末尾「核验：」句 | 过程产物 | [B] |

**口径要点（易错）**：① **F1／F2／F3／F5 逐字**（H1／description／建设状态段／§1.1 与 §3 行改写）；② **计数口径显式**（行 vs 出现次数）；③ 「未建**阶段**如实告知」是**本批新立**（无标准锚，§八 2）；④ `[2]` 段**纯本地、零外部依赖**（不引入 `agentskills`——其结论走查期人工得；§八 5 已销）；⑤ **禁改面逐件对账**（§一 不变量与 §五 已全列）；⑥ `AGENTS.md` 不动（§八 1）；⑦ 新建资产零 markdown 链接。

## 三、验收断言（一条命令一期望；计数口径逐条标注）

### 断言 1｜最小壳结构与五键

```bash
grep -c "^# Abzu · 大纲域（abzu-outline）$" skills/abzu-outline/SKILL.md
```

期望 **1**（**计行**）。**预验基线 0**。

```bash
grep -c "^## 入口判定$\|^## 工作流$\|^## 门控与交互原则$\|^## 语言$" skills/abzu-outline/SKILL.md
```

期望 **4**（计行）。**预验基线 0**。

```bash
grep -c "^name: abzu-outline$\|^license: MIT$\|^disable-model-invocation: true$\|^  version: \"0.1.0\"$" skills/abzu-outline/SKILL.md
```

期望 **4**（计行）。**预验基线 0**。

### 断言 2｜description 逐字与字符数

```bash
grep -o "触发方式：仅 /abzu-outline 显式调用；自然语言提及不触发。" skills/abzu-outline/SKILL.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
python -c "import io,re;t=io.open('skills/abzu-outline/SKILL.md',encoding='utf-8').read();m=re.search(r'^description:\s*(.+)$',t,re.M);print(len(m.group(1)))"
```

期望 **≤1024 且 >0**（施工后实核，实测值记入报告）。**预验基线**：文件不存在。

### 断言 3｜建设状态段

```bash
grep -o "未建阶段不模拟、不顶替、不产出占位内容" skills/abzu-outline/SKILL.md | wc -l
```

期望 **1**（计出现次数）。**预验基线 0**。

### 断言 4｜`[2]` 段启用与输出形态

```bash
bash scripts/check.sh 2>&1 | grep -c "^\[2\] "
```

期望 **1**（**恰 1 行**＝段标题行；其余行按既有体例缩进，成稿审查 F-11）。**预验基线 0**。

```bash
grep -c "^# \[2\] skill 格式校验" scripts/check.sh
```

期望 **1**（**计行**——头注行**以 `#` 加一空格起首**，锚点须含之；先例＝075 的 `grep -c "^# \[4\] 规格自动自检"`。施工期偏差 a：初稿漏掉那个反引号加空格的锚点前缀致恒 0，施工方判因后未构造绕过、呈报裁定 ✓）。**预验基线 0**。

```bash
bash scripts/check.sh 2>&1 | grep "^共 [0-9]* 段" | grep -c "\[2\] 待建"
```

期望 **0**（**判据限汇总行**——`[3]`／`[5]` 的「待建」字样仍在，但不得再有 `[2] 待建`）。**预验基线 1**。

### 断言 5｜`[2]` 段非空转（探针落 `skills/`，正负控双面）

> **探针期 `check.sh` 可能 exit 1 属预期**（负控会置红）——勿按红线 6 误停；两条命令均用 `;` 串联，故 `rm` 恒执行。

**负控**（探针落 `skills/` 内——**跑完即删**）：

```bash
mkdir -p skills/__probe-087 && printf '%s
' '---' 'name: x' 'description: d' 'license: MIT' '---' '# 错 H1' > skills/__probe-087/SKILL.md && bash scripts/check.sh 2>&1 | sed -n '/^\[2\] /,/^\[4\] /p' | grep -c "skills/__probe-087" ; rm -rf skills/__probe-087
```

期望 **≥1**（**计行**——探针被 `[2]` 段报出失败；判据**锚路径**而非报错措辞（窄域复审 R3），且**限 `[2]` 段内**（施工期偏差 b：不限面则 `[4]` 段对本规格产出「跳过」候选行、含同路径串 → 污染判据——082／086 同族第三次））。**预验基线 0**。

**正控**（探针名取**纯 kebab**——`name` 只允许小写字母／数字／连字符，含下划线的目录名会撞官方约束，窄域复审 R2；内容**写死**并留节间空行以过 lint）：

```bash
mkdir -p skills/zz-probe-ok && printf '%s
' '---' 'name: zz-probe-ok' 'description: 探针：合规壳的最小样例（087，勿入库）' 'license: MIT' 'disable-model-invocation: true' 'metadata:' '  version: "0.1.0"' '---' '' '# Abzu · 探针域（zz-probe-ok）' '' '## 入口判定' '' '（探针）' '' '## 工作流' '' '（探针）' '' '## 门控与交互原则' '' '（探针）' '' '## 语言' '' '（探针）' > skills/zz-probe-ok/SKILL.md && bash scripts/check.sh 2>&1 | sed -n '/^\[2\] /,/^\[4\] /p' | grep -c "skills/zz-probe-ok" ; rm -rf skills/zz-probe-ok
```

期望 **0**（**计行**——合规探针不被 `[2]` 段报为失败；同上**限 `[2]` 段内**）。**预验基线 0**。

**两面合证非空转**（负控报出／正控不报）。**探针跑完删净**（红线 2；断言 10 末项须确认）。

### 断言 6｜状态真源同步（产品层）

```bash
grep -o "大纲域最小壳已建（087，试验性）" docs/product/产品定义.md | wc -l
```

期望 **1**。**预验基线 0**。

```bash
grep -o "最小壳（087）" docs/product/架构设计.md | wc -l
```

期望 **1**。**预验基线 0**。

```bash
grep -c "占位·待建设" docs/product/outline/大纲域设计.md
```

期望 **0**（**计行**）。**预验基线 2**。

```bash
grep -o "模板进 \`assets/\`、方法论与词表进 \`references/\`" docs/product/outline/大纲域设计.md docs/product/outline/大纲域设计-附录.md | wc -l
```

期望 **2**（两件各 1——**词序统一**，成稿审查 F-06）。**预验基线 0**。

```bash
grep -o "最小壳已建，阶段细则构建中" docs/product/outline/大纲域设计.md | wc -l
```

期望 **1**（`:22` 新句，成稿审查 F-13）。**预验基线 0**。

```bash
grep -o "最小壳已建（087）；\`references/\`／\`assets/\` 与脚本随各批落地" docs/product/outline/大纲域设计.md | wc -l
```

期望 **1**（`:26` 新句，成稿审查 F-13）。**预验基线 0**。

```bash
grep -o "运行资产（域壳、references、assets、脚本）" docs/product/outline/大纲域设计.md | wc -l
```

期望 **1**（`:200` 新句，窄域复审 R7 补）。**预验基线 0**。

### 断言 7｜`命名标准` 五处

```bash
grep -c "skills/\\`（skill 资产）与" docs/standards/命名标准.md
```

期望 **0**（**计行**——原句已改）。**预验基线 1**。

```bash
grep -o "skills/\\` 已建（首个域＝\\`abzu-outline\\` 最小壳）" docs/standards/命名标准.md | wc -l
```

期望 **1**。**预验基线 0**。

```bash
grep -o "（预留）" docs/standards/命名标准.md | wc -l
```

期望 **8**（**计出现次数**——改前 **14**）。**预验基线 14**。

```bash
grep -c "abzu-outline/" docs/standards/命名标准.md
```

期望 **≥1**（§1 目录树新增的 `skills/abzu-outline/` 子树行——**计行**）。**预验基线 0**。

```bash
grep -o "已实现（目录未建时跳过）" docs/standards/命名标准.md | wc -l
```

期望 **0**（§三 机检锚点句已改述）。**预验基线 1**。

```bash
grep -o "现状预留" docs/standards/命名标准.md docs/standards/skill形态标准.md | wc -l
```

期望 **0**（**计出现次数**——`skill形态标准:14` 那处由 F11① 去掉；`命名标准:39` 写的是「现状**与**预留」不含该串，故一并归零；窄域复审 R7 补）。**预验基线 1**（实测仅 `skill形态标准:14` 一处）。

### 断言 8｜`测试与验收标准` 与 `skill形态标准`

```bash
grep -o "本地断言（\`check.sh \[2\]\`）" docs/standards/测试与验收标准.md | wc -l
```

期望 **≥1**（§3 机检锚点 `[2]` 行——成稿审查 F-04 的同件互斥修除）。**预验基线 0**。

```bash
grep -c "现为 \[0\]／\[1\]／\[4\]" docs/standards/测试与验收标准.md
```

期望 **0**（§1.2 已实现段枚举须含 `[2]`——**计行**）。**预验基线 1**。

```bash
grep -c "待建（脚本头注已留 TODO 位）" docs/standards/测试与验收标准.md
```

期望 **0**（§1.1 的 `[2]` 行现状列已改「已实现」——窄域复审 R7 补）。**预验基线 1**。

```bash
grep -c "尚未建立，本条为规则先行" docs/standards/skill形态标准.md
```

期望 **0**（§1 现状句已改——**计行**）。**预验基线 1**。

```bash
grep -o "agentskills validate" docs/standards/skill形态标准.md | wc -l
```

期望 **0**（**计出现次数**——§3 校验工具条／§13 矩阵／机检锚点三处均改为本地断言口径；改前 **3 处**（`:47`／`:140`／`:170`）；`:144` 运行时脚本行的「`node --check`／`py_compile`（待接入 check.sh）」属 `[5]` 段、**不在本批范围**，故不以其为判据）。**预验基线 3**。

### 断言 9｜零外部依赖与链接

```bash
grep -rc "](" skills/abzu-outline/ | grep -v ":0$"; echo "扫毕"
```

期望：仅「扫毕」（**前提＝目录已存在**）。**预验基线**：目录不存在。

```bash
grep -c "agentskills" scripts/check.sh
```

期望 **0**（**计行**——`[2]` 段纯本地，不引入外部工具；基线含头注 `# TODO: [2] skill 格式校验（agentskills validate）——待落首个 skill 壳后加入` 一处，F4⑦ 改头注后归零）。**预验基线 1**。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**。**预验基线 175‧0**。

### 断言 10｜门禁与改动面

```bash
bash scripts/check.sh
```

期望 exit **0**。**预验基线 0**。

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**（件数不设期望——**含在制规格**，随批内状态浮动）。**预验基线 0 issue**。

```bash
git status --porcelain | wc -l
```

期望 **11**（**9 件 tracked M**：`scripts/check.sh`／`测试与验收标准`／`命名标准`／`skill形态标准`／`产品定义`／`架构设计`／`大纲域设计`／`大纲域设计-附录`／`CHANGELOG` ＋ **2 未跟踪**：`skills/` 目录行与 087 规格——成稿审查 F-1 订正：新建壳不另计 M 行）。**预验基线 1**；施工后**逐行列核验记录**。

```bash
ls skills/__probe-087 2>&1 | grep -c "No such file"
```

期望 **1**（探针已删净）。**预验基线 1**。

### 断言 11｜真机走查（作者执行；判据与陷阱）

**安装路径（本地，禁远端）**：把 `skills/abzu-outline/` 复制或链接到宿主的 skill 发现目录（用户项目内 `.claude/skills/abzu-outline/` 或用户级 `~/.claude/skills/abzu-outline/`，以宿主实际规则为准）——**明文禁止**走 `npx skills add …`（§一⑨：它装的是 `origin/main` 上的**旧壳**，会测错对象、污染结论）；仓库内亦**禁跑安装器**（`check.sh [0]` 真目录守卫）。

**三判据**：① `/` 菜单**可见** `/abzu-outline`；② **用一句强诱导提示测 2–3 次**（记原话），观察是否触发——单次提及无法区分「键生效」与「模型本就不触发」（成稿审查观察③）；③ 宿主对 `disable-model-invocation` 的**接受情况**（接受／忽略／报错）有明确结论。

**第 4 项（成稿审查独立判断＋窄域复审 R8）**：跑一次 `agentskills validate skills/abzu-outline`，记 rc 与首行输出——这是「生态是否接受该键」的第二个证据源（第一个是 §一② 的既有实测），也复核 F11⑥「外部校验器仅走查期可选」的定位。

**走查记录模板**（落 §九；成稿审查独立判断③：四要素须由作者报告转写，无模板即靠自觉）：宿主版本｜走查日期｜三判据结论（含②的原话与次数）｜发现的不一致｜安装落点确切路径。

```bash
grep -c "未实测·待核" docs/standards/skill形态标准.md
```

期望 **0**（回写后标记已删——**计行**；该行现含 **2 处**，`grep -c` 计行故改前为 1）。**预验基线 1**。

**负结果预案**：若③为「不接受」→ 按 §八 3 回产品层评估 frontmatter 形态（去掉该键／换机制／保留记差异），结论决定 088 的四件资产形态。

### 断言 12｜过程产物

```bash
grep -cF '最小壳与真机试验（087）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 九条实跑取证（② `agentskills` 实测含正控／去键／目录名／H1 四组；⑦ 双口径 11 行／14 处；⑨ 旧壳六项实测）；④ 的 14 处逐处读原文（成稿审查 F-04 补齐 6 处）。
2. **断言实跑过**：断言 1–12 改前侧实跑（新文件 0／状态句 0／`（预留）` 14 处／`占位·待建设` 2 行／汇总行 `[2] 待建` 1 行／`未实测·待核` 1 行／`尚未建立，本条为规则先行` 1 行／`待接入 check.sh` 1 行／`已实现（目录未建时跳过）` 1 行／`skills/`（skill 资产）与 1 行／`现为 [0]／[1]／[4]` 1 行）；断言 2 第二命令、5 两控、7 第四命令、10 第三命令属施工后实核项。**互斥性**：新锚均不在现文内 ✓；**计数口径逐条标注** ✓。
3. **禁止事项已写明**：见 §五。**[4] 段预警**：本规格在制时实跑得候选（新文件未建所致），逐条属预期（施工后多数转自噬／中性）。

## 五、禁止事项

- 禁止触碰：`.git/`、`docs/specs/` 全部（含 `archive/`）、**`AGENTS.md`**（须作者确认）、`README.md`、`scripts/hooks/`、`scripts/check_spec.py`、`docs/product/` 除 F6–F9 点名处外的内容、`docs/standards/` 除 F5／F10／F11 点名面外的内容、产品层的**设计判据**。
- **不得**把产品层设计文档的「设计语言」（心流锚、设计理由、旧仓教训）搬进 skill 资产。
- 禁止动作：任何 git 写命令；联网；安装依赖；**不得**在仓库内跑安装器或复制 `skills/` 到宿主（走查由作者在**仓库外**做）。
- 禁止越权：`[2]` 段**不得引入外部校验工具**（纯本地七项）；skill 资产内零 markdown 链接；探针须在断言后删净；计数断言按 §三 标注口径书写与判读；F11 四要素不得代填。
- 禁止改断言、改数字、删检查来变绿。

## 六、被否决的替代方案

- **本批一次建阶段一四件资产**（原 087 设计）→ 否决（成稿审查 F-02 独立判断＋作者裁定「拆」）：四件资产建立在 frontmatter 形态之上，而该形态核心键被生态校验器拒绝；先问清楚，代价 1 个文件。
- **`[2]` 段接入 `agentskills validate`** → 否决（成稿审查 F-02 实测＋独立判断②）：其白名单无本仓硬约束键（长期「已知差异」空转）、不查 H1／四节名（覆盖面反窄于本地断言）、需解析其输出文本（易碎）、且需动 `AGENTS.md` §四 例外句（须作者确认）。改**纯本地七项**，该工具的结论改由**走查期人工跑一次**取得。
- **顺带改 `AGENTS.md` 状态句** → 否决（§六.1 须作者确认；§八 1 登记）。
- **「未建阶段如实告知」挂 `触发与交互标准` §2.6 名下** → 否决（F-08／F-15）：该条只覆盖未建**域**；本批新立并登记（§八 2）。
- **走查用仓内记载的 `npx skills add` 路径** → 否决（§一⑨）：装的是 `origin/main` 的旧壳；改**本地安装路径**并明文禁远端。
- **词表／模板的「头部两行声明」在本批补** → 移入 088（§八 4）。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**本批改动直接触及或可能变状态者**（必列面 ②）。**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 086 §八 2 | `[2]` 段的**目录形态**校验（`agentskills` 是否覆盖待实测——若只校 frontmatter 须补本地断言） | **本批处置** | 实测确认其**不覆盖** → **F4⑥** 落本地目录形态断言（成稿审查 F-05 查出本行原缺） |
| 086 §八 4 | 产品层三处过时文字同步 | **本批处置** | **F8④⑤＋F9** |
| 086 §八 3 | `references` 平铺后的单目录容量 | 仍待 | 本批只建壳，`references/` 未建（088 起） |
| 086 §八 5 | 变体表头使检查 A／C 空转（处置＝写法回归模板表头） | **本批处置** | 本规格 §二 用模板表头（`#｜文件:位置｜改动｜依据｜自由度`） |
| 086 §八 6／7 | 机检锚点措辞收口／断言 4 只锁下限 | 仍待 | 随 `[3]` 内容轨检查器建设 |
| 083 §八 3 | `check_spec.py` 自带测试件 | 仍待 | 本批不动该件 |
| 大纲域设计 §十一 | 运行资产随域建设批落地 | **本批处置（部分）** | 最小壳落地（F1）；`references/`／`assets/`／脚本随 088–091 |

**汇总**：共 **7 行／按条 7**——本批处置 4（086 §八 2／4／5、大纲域 §十一 部分）｜已履行 0｜仍待 3 ＝ 7 ✓。

## 八、开放项

1. **`AGENTS.md` 的两处状态句**（`:5`「运行＝`skills/` 下的 skill 组（**尚未创建**）」／`:9`「当前状态：运行侧（`skills/`）与产品层各 skill 均尚未创建」）——**须作者确认**，本批不动；触发＝作者裁定。
2. **「未建阶段如实告知」缺标准锚**：本批以 F3 新立；触发＝下一批触碰 `触发与交互标准` §2 时评估补条。
3. **真机走查的负结果路径**：若该键不被宿主接受 → 回产品层评估 frontmatter 形态（`skill形态标准` §3 与 `架构设计` §二.2 同批订正），结论决定 088 资产形态。
4. **088 的四件资产要求**（F-06 转记，成稿审查 F-06 补充）：`outline-workflow.md`（§6 四要素＋中断契约＋建设状态通报**只落此处**、另两处引用）／`outline-stage-规格与资料.md`／`outline-emotion-words.md`（**头部两行声明**＋五处消费点**只写 1.1 基线、其余随 088–091 补**）／`assets/outline-基本规格-template.md`（§6 四段式＋三要素＋头部两行声明）。
5. ~~`运行环境标准` §4 与 `AGENTS.md` §四 的例外句~~——**已销**（F4 改纯本地后，`[2]` 段零外部依赖，无新例外需登记；成稿审查独立判断②）。
6. **走查安装路径的仓内真源仍装旧壳**（产物审查 P1-3）：`README.md:8`／`docs/product/验收场景.md:19`／`docs/standards/版本与分发标准.md:52`／`:74` 四处**仍教 `npx skills add Chained1001/Abzu -y`**（装 `origin/main` 的**重建前旧壳**）——本批把禁令只写进规格断言 11 的措辞，仓内真源未改（`README.md` 属禁改面，须作者确认）。触发＝`rebuild` 推远端后同批订正四处，或作者裁定解禁 `README.md` 即本批末补。
7. **`skill形态标准` §2.1「跨域意图告知」要素未落壳**（产物审查 P2-7）：该节要求壳的入口判定含「跨域意图告知对应 skill 命令与状态」，而本批壳只写文件证据定位。触发＝088 铺阶段二时评估补入（或明确登记从简）。
8. **F3 行为要求的验收落点**（成稿审查 F-13）：「未建阶段如实告知」目前只有文本断言；其**行为**验收（壳报「能走到哪」）随 **088 走查批**核（届时并入走查判据）。

## 九、审查记录

- 2026-09-15｜**成稿审查·第一轮**（独立上下文只读 subagent）｜findings 共 **15**：**P0:4** P1:4 P2:6 ＋观察 1｜结论 **不通过**。关键处置：**F-02（生态级发现）**——本机 `skills-ref 0.1.1` **拒绝** `disable-model-invocation`（白名单六键无它）→ 据其独立判断＋**作者裁定「拆」**，本批缩为「最小壳＋`[2]` 段＋状态同步＋真机走查」，四件资产移入 088；其余（计数口径／token 互斥／禁改面漏项／连带面 6 处／断言覆盖／走查三判据与负预案）逐条处置。
- 2026-09-15｜**成稿审查·第二轮**（拆批重写稿）｜findings 共 **14**：**P0:2** P1:4 P2:8 ＋观察 6｜结论 **修订后通过**。审查方**独立实跑** `agentskills validate` 四组（含**同名＋错 H1＋无四节仍 rc=0**，确证不查 H1 与四节）、逐条验前轮 15 条处置、复算全部计数基线、**查出远端 `main` 上的重建前旧壳**。处置（全部照准）：
  - **F-2（P0，最致命）**：仓内唯一记载的安装命令装的是 `origin/main` 的**旧壳**（无该键、H1 与四节均不合新形）→ 走查会**测错对象并污染 088 的形态结论** → **断言 11 改本地安装路径 ＋ 明文禁远端**；**§一 增第 ⑨ 行**记该事实（防下批再踩）。
  - **F-1（P0）**：断言 10 第三命令期望 12 → 实为 **11**（9 M ＋ 2 未跟踪）→ 已订正。
  - **F-3（P1）**：断言 5／7 的探针落 `.tmp/` 而 `[2]` 扫 `skills/*/` → **恒真** → **探针改落 `skills/__probe-087/`**（正负控双面＋跑完删净）。
  - **F-4（P1）**：连带面**仍漏 6 处**（`测试与验收标准:102` 与 §1.1 行**同件互斥**；`skill形态标准:14／:21／:47／:140／:170`；`命名标准:119`）→ **全部纳入**（F5③／F10④／F11①②④⑤⑥）。
  - **F-5（P1）**：§七 **漏 086 §八 2**（目录形态机检明文归本批）＋F4 未含该断言 → **补 §七 行 ＋ F4⑥**。
  - **F-6（P1）**：断言 6 的 token 与 F9 示例**词序相反** → F9 与 F8④ 同句逐字。
  - **P2 八条**：F-7（lint **55 件含在制规格**，原写反）／F-8（`[4]` 候选数标口径）／**F-9（头注自由度分布 → `[A]×4 [B]×8`）**／F-10（§1 目录树**无** `skills/` 行可「展开」→ 改「**新增**子树」）／F-11（F4 输出形态钉死＋跳过补手动路径）／F-12（「与 F10 同批」→「与 F11 同批」）／F-13（覆盖残余：`:22`／`:26` 补断言；F3 行为验收落点记 §八 6）／F-14（定位行改「成稿审查＋核验三步＋**双维**产物审查＋真机走查」）——**全部照改**。
  - **审查方独立判断（采纳）**：① 缩批后**仍需补三处**才成可测闭环（安装路径／判据强化（强诱导测 2–3 次）／探针落位）——均已落；② `[2]` 段**去掉外挂校验、改纯本地**并**并进目录形态与官方三条约束断言**（覆盖面反超外挂），外挂改走查期人工跑一次 → **§八 5 随之销**；③ 走查补**记录模板**。
- 2026-09-15｜**施工**（subagent）｜**完成**：`skills/abzu-outline/SKILL.md`（34 行）＋`check.sh`（`[2]` 段＋头注）＋状态真源同步 20 处（9 件 tracked M）＋`CHANGELOG`（390 字符）。**两处后置未做**（F11③ §3 走查回写／F7② 架构设计 `:19` 联订句）。**施工期两条偏差申报（均照准，且都是我规格的错）**：**a** 断言 4② 锚点漏掉 `#` 加一空格的前缀（头注行以 `#` 加一空格起首）→ 规格补锚（先例 075 同型）；**b** 断言 5 两控被 `[4]` 段对本规格产出的「跳过」候选行污染（探针路径写在断言里）→ 判据**限 `[2]` 段内**（`sed -n '/^\[2\] /,/^\[4\] /p'`）——082／086 同族**第三次**；施工方两次均**未构造绕过、呈报裁定** ✓。
- 2026-09-15｜**规划方核验（三步）**｜**通过**。① 改动面：恰 **11 行** porcelain（9 M＋`skills/`＋规格），逐行在点名面内；零改面（`AGENTS.md`／`README.md`／`hooks/`／`check_spec.py`／`docs/specs/`）无改动。② 断言独立重跑：1–12 全中（H1 1／四节 4／五键 4／description **149** 字符／建设状态段 1／段标题行 1／头注锚 1／汇总行 `[2] 待建` 0／两控 8 与 0／状态真源 7 项／`（预留）` **8 处 5 行**／`agentskills` 0／一行式 **175 0**／porcelain 11／exit 0／lint 0 issue／CHANGELOG 1 行）。③ 内容抽查：壳四节与回显句、`check.sh` 的 `[2]` 段六项＋锚定＋locale、`skill形态标准:169/171` 状态。
- 2026-09-15｜**双维产物审查**（独立上下文只读 subagent；重路径＋改标准条文）｜findings 共 **10**：**P0:0 P1:3** P2:7 ＋观察 7｜结论 **修订后通过**。审查方**独立重跑断言 1–12 全过**、以**副本**做四项**反向构造**（H1 破坏→断言 1a 红／非法子目录与同名层→`[2]` 红／**四节全改 H3→`[2]` 静默通过＝假绿通道**／`references/{skill 名}/`→未报＝覆盖缺口）、逐处核状态真源 20 处、复核 `[4]` 段由 18 降至 13 条并逐条判属。处置（**七项补修**＋两条登记）：
  - **P1-1**：`skill形态标准:169`（§三 §1 目录形态行）状态仍「待建」而与 `测试与验收标准` 判「已实现」互抵 → 改「已实现（本地断言）」；同件 `:171`（§3 调用控制键行）→「**部分实现**（键存在性已落 `[2]`；宿主实测待走查）」。
  - **P1-2（守卫真缺陷）**：`[2]` 的四节名判据用 `grep -qF "## ${_s}"` **未行首锚定** → 四节改 H3 或写 `## 工作流细则` 均能通过（**常驻守卫永久漏检**）→ 改 `grep -qFx`。
  - **P2-4**：`[2]` ⑥ 只查顶层白名单 → **扩二级遍历**（禁 `references/{skill 名}/` 同名层与三层深度）。
  - **P2-5**：`${#}` 计长在 `LC_ALL=C`（Git Bash 缺省）按**字节**计 → >341 汉字 description 假红挡提交 → 段落首 `export LC_ALL=C.UTF-8`（并改准注释出处）。
  - **P2-6**：「七项」措辞与实现不符（⑦ 是段级）→ 改「**逐壳六项 ＋ 段行一条**」；**规格 F4／F12 已同步**。
  - **P2-3**：`CHANGELOG` 末句「两处待裁定」与规格实况时态相抵 → 改「已呈报并据裁定订正规格」（394→**359** 字符）。
  - **产物审查独立判断①（采纳）**：壳**缺「壳被加载了」的可观测句**——走查①③靠作者观察，而原壳无一句可供区分 → **入口判定末补一行建设状态回显**（引 F3 原话前半，指回壳末；不复制判据串以免断言 3 计 2）。
  - **登记两条**：**P1-3**（四处安装命令仍教装旧壳 → §八 6）／**P2-7**（§2.1「跨域意图告知」要素未落 → §八 7）。
- 2026-09-15｜**补修**（施工方）｜七项全落、门禁绿、porcelain 仍 11 行；**判别力自证**（副本上四夹具）：四节改 H3 **报出**／`references/{skill 名}/` **报出**／三层深度 **报出**／合规壳不报；**locale 反证**（400 汉字＝1200 字节，`LC_ALL=C` 下不误报）。补修期门禁另抓到其一处真错（CHANGELOG 末句行内代码含边缘空格 → MD038）已修。
- **真机走查（作者执行）：待回填**——走查结论回来后：① 回写 **F11③**（`skill形态标准` §3 四要素）与 **F7②**（`架构设计:19` 联订句）；② 据其结论裁定 §八 3（该键被接受否 → 决定 088 的资产形态）；③ 本规格归档、呈作者提交。
