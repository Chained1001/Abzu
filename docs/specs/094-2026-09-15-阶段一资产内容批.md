# 094-2026-09-15-阶段一资产内容批

> 定位：把 `skills/abzu-outline/` 正文的三笔账收掉——① **1.2 门控的落行式样未自包含**（`092` §八 1b：式样真源全在 `docs/`，部署副本内不存在，正是 `命名标准` §5 所谓「模型执行时必须知道的事实」）；② `outline-stage-spec.md:7` 的**裸具名包外指针**（《大纲域设计·规格与资料步》§一，无标注）；③ **批次号型标记的前提已失效**（8 行写「随 089–091 未建」，而 089–093 实为开发侧批次、并未建 Stage 2–4）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**重路径**——**动 `skills/` 域资产**。→ 标准路径 ＋ **双维产物审查** ＋ **真机走查（必跑）**：本批把状态表行的**落行式样**写进资产，模型此后产出的 `创作进度.md` 状态表行会带该式样＝**用户可观测行为**，故按重路径行的判据不享豁免；**并与 `088` §八 16 的走查欠账合并复验**（同一路径：1.0→1.1→1.2 四步），一次走查销两笔。
> 自由度分布：[A]×2 [B]×3（**逐行实测得出**，与 §二 清单逐行一致；本行数值经成稿审查 P1-3 订正——同型在 `093` 成稿审查再现，**G22 族复发**，本批起由检查 G 常驻兜住；**括注不复述旧值**，否则检查 G 会把括注内的数字当作头注声明）
> 必读输入：本规格 §一；`skills/abzu-outline/SKILL.md`（全文，尤其 `:31`）；`skills/abzu-outline/references/outline-stage-spec.md`（全文，尤其 `:3`–`:7`／`:16`–`:23`／`:55`–`:57`）；`skills/abzu-outline/references/outline-workflow.md`（`:18`／`:23`／`:27`／`:31`／`:48`）；`skills/abzu-outline/references/outline-emotion-words.md`（`:3`／`:21`–`:24`）；`docs/product/书目录契约.md` §2（`:26`–`:37`）；`docs/standards/命名标准.md` §5（`:94`–`:105`）；`docs/standards/测试与验收标准.md` §3（`:48`–`:61`）
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `outline-stage-spec.md` 的 **1.2 只有两行**：`:55` 标题 `### 1.2 规格门（门控）`、`:57` 一句（过关条件／交互／「通过→`创作进度.md` 翻「大纲-资料收集中」」）——**零落行式样** | `awk` 读 `:53`–`:57`（该件共 57 行，1.2 为末节） |
| ② | 「门控与停靠须落产物状态表行」的**义务句不在 1.2**，在 `SKILL.md:31`；同义句另见 `outline-workflow.md:18`（「确认落产物状态表行」） | 逐行读两件 |
| ③ | 式样真源 **全在 `docs/`**（部署副本内不存在）：`书目录契约.md:37`「落行式样如「世界观底盘-待确认」」／`大纲域设计.md:46`／`触发与交互标准.md:20`「（落行式样如「{产物名}-待确认」）」；具名例另见 `书目录契约.md:73` | `grep -rn '待确认' skills/ docs/ AGENTS.md`（`docs/` 命中 **7 件／8 行**，`skills/` **0 行**） |
| ④ | `skills/` 内 `待确认` **零命中**；unique 的 `产物名｜状态｜备注` 骨架在 `outline-stage-spec.md:18`–`:23`（围栏内），**无数据行**；`assets/` 内**无**状态表示例块 | `grep -rn '状态表' skills/abzu-outline/`（6 行，均为义务句／结构名）／读 `:18`–`:23` |
| ⑤ | **包外旧措辞「见 `X` §N」存量 = 0**——`skills/` 内 7 处「见 `X`」**全部指向包内件**（`references/`／`assets/`），合规；`092` 的 F3–F5 已把三处「见 `书目录契约` §1」改成「维护出处：」形态 | `grep -rn '见 \`' skills/`（8 行逐条分类） |
| ⑥ | 「维护出处：」标注现有 **4 处**（`SKILL.md:33`／`outline-workflow.md:48`／`outline-stage-spec.md:7`／`:16`），与 `092` §八 2 所述 ×4 一致 | `grep -ro '维护出处：[^；]*；运行时不必加载' skills/abzu-outline/ \| wc -l` → 4 |
| ⑦ | **裸具名包外指针 1 处**：`outline-stage-spec.md:7` 的「出处：内容逐字迁自产品层《大纲域设计·规格与资料步》§一（1.0–1.2）」——件名裸写、无标注（同**行尾**另有 `命名标准` §5 的标注，但那是另一指针） | 逐字读 `:7` |
| ⑧ | **批次号型标记 8 行**，均以「随 089–091」为到期依据：`outline-workflow.md:23`／`:27`／`:31`、`outline-stage-spec.md:4`、`outline-emotion-words.md:21`／`:22`／`:23`／`:24` | `grep -rn '随 089–091' skills/` → 8 行 |
| ⑨ | **该依据已失效**：089 之后的**六次提交**（089／一批未编号轻路径／090／091／092／093）均已在库，全部为**开发侧**批次（施工机制／守卫／规格收口），**未建任何 Stage 2–4 资产**（`git log --numstat … -- skills/` 仅有标注改动、无新增件）——标记所指的「随某批未建」前提不成立 | `git log --oneline -8`（089–093 主题均为开发侧） |
| ⑩ | 行数预算：`SKILL.md` **37**（≤500）／reference 最长 **57**（<300）／`assets/` 模板 **99**（不计预算） | `wc -l` 五件 |
| ⑪ | `088` 的施工记录把 `outline-workflow.md` 记作 **52 行**，实测 **56**——**088 误记**（该件创建时即 ＋56），本批引用须按 56 | `wc -l` ＋ `git log --numstat` |
| ⑫ | `092` §八 1b **未进 `092` §七 核销表**，但**已经 `093` §七 转承**（该行现标「仍待（触发修订）」、触发改为「阶段一资产内容批」）——本批按跨批索引纪律引 `093` §七；`092` 原触发明写「内容轨批同批内联进 `outline-stage-spec.md` 1.2 的落行句（**一行**）」——本批勘察发现 **1.2 内并无「落行句」**（落行义务在 `SKILL.md:31`／`outline-workflow.md:18`），故落点须本批重定（见 §六） | `grep -n '1b\|F-14' archive/092-…md`（`:213`／`:227`） |
| ⑬ | 基线：`check.sh` **exit 0**（`共 6 段：已跑 6｜跳过 0`）／lint **67 件 0 issue**（tracked 66 ＋ 未跟踪本规格 1）／链接 **176‧断链 0**／工作区 **1 行**（仅本规格未跟踪） | 三命令实跑 |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `skills/abzu-outline/references/outline-stage-spec.md:57`（1.2 门控句） | 句末追加（**引号块为逐字目标**）：「（**门控落行式样：`{产物名}-待确认`**——例：`基本规格-待确认`。）」——**自包含**（式样串 ＋ 一例，部署副本内即可判） | [A] | ①③④⑫；`命名标准` §5 条款（模型执行时必须知道的事实须自包含） |
| F2 | `skills/abzu-outline/references/outline-stage-spec.md:7`（出处行） | 「内容逐字迁自产品层《大纲域设计·规格与资料步》§一（1.0–1.2）」→「内容逐字迁自**产品层设计件**（**维护出处：`大纲域设计-规格与资料步` §一；运行时不必加载**）」 | [A] | ⑦；`命名标准` §5 条款（裸具名包外指针＝`docs/` 下各件之一） |
| F3 | 批次号型标记 **8 行**：`skills/abzu-outline/references/outline-workflow.md`（`:23`／`:27`／`:31`）／`skills/abzu-outline/references/outline-stage-spec.md`（`:4`）／`skills/abzu-outline/references/outline-emotion-words.md`（`:21`–`:24`） | 「（随 089–091）」类表述 → **按到期制改述为阶段指向**：`outline-workflow.md:23`／`:27`／`:31` 与 `outline-stage-spec.md:4`／`outline-emotion-words.md:21`–`:24` 的「随 089–091」→「**随对应阶段批**」（若该行同时含「本批」等其他批次号，一并按其语义改述或删） | [B] | ⑧⑨；`092` §八 8 裁定「批次号型标记按各批上线时删除的到期制处理」——**本批即该到期时刻**（依据已失效）；**只改标记表述，不改任何「未建」的事实陈述** |
| F4 | `CHANGELOG.md`（`### Changed` 段首） | 一行：『阶段一资产内容（094）：…』——1.2 门控落行式样自包含／出处行裸具名收口／批次号标记按到期制改述；≤400 字符、末尾「核验：」句 | [B] | 过程产物 |
| F5 | 本规格 §九（走查记录） | 真机走查由**作者**执行后回填：宿主版本｜**模型与会话形态**（含是否触发 `测试与验收标准` §3 前提① 的换模型重试）｜走查日期｜1.0→1.1→1.2 四步逐项结论｜式样的实际落行观察｜发现的不一致｜安装落点（体例见 `087` 的走查记录） | [B] | ⑫；`测试与验收标准` §3（做法／前提／记录落点）；**本批走查同时销 `088` §八 16 欠账** |

**口径要点（易错）**：① **F1 只能落一处**——`SKILL.md:31` 与 `outline-workflow.md:18` 是**义务句**，式样串**不得**再复制一份（反模式 #4 单一真源）；② F2 的替换**只动该半句**，该行行尾原有的 `命名标准` §5 标注**保留不动**；③ F3 **不得改**「未建」的事实（Stage 2–4 确实未建），只改「随 089–091」这一**到期依据的表述**；④ 三件的行尾与编码按现状保持（`outline-emotion-words.md` 等为 LF）。

## 三、验收断言

### 断言 1｜落行式样自包含

```bash
grep -c '待确认' skills/abzu-outline/references/outline-stage-spec.md
```

期望 **1**（**计行**——式样串与具名例同在一行）；**预验基线 0**。

```bash
grep -c '待确认' skills/abzu-outline/SKILL.md skills/abzu-outline/references/outline-workflow.md | awk -F: '{s+=$2} END{print s+0}'
```

期望 **0**（**计行**——式样只落一处，义务句不复制）；**预验基线 0**。

### 断言 2｜裸具名收口

```bash
grep -c '维护出处：`大纲域设计-规格与资料步` §一' skills/abzu-outline/references/outline-stage-spec.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '迁自产品层《' skills/abzu-outline/references/outline-stage-spec.md
```

期望 **0**（裸具名形态离场）；**预验基线 1**。

### 断言 3｜批次号标记改述

```bash
grep -rc '随 089–091' skills/ | grep -v ':0$' | wc -l
```

期望 **0**（**计件数**——改述后无残留件）；**预验基线 3**（`outline-workflow.md`／`outline-stage-spec.md`／`outline-emotion-words.md` 各含）。

```bash
grep -c '随对应阶段批' skills/abzu-outline/references/outline-emotion-words.md
```

期望 **4**（**计行**——该件四行同类标记逐行改述）；**预验基线 0**。

### 断言 4｜不动面（不变量）

```bash
grep -ro '维护出处：[^；]*；运行时不必加载' skills/abzu-outline/ | wc -l
```

期望 **5**（**计出现次数**——既有 4 处 ＋ **F2 新增 1 处**；成稿审查 P0-1 订正：原写 4 与本批改动相抵）；**预验基线 4**。

```bash
grep -c '见 `' skills/abzu-outline/references/outline-workflow.md
```

期望 **2**（**计行**——包内「见 `references/…`」两处不动）；**预验基线 2**。

### 断言 5｜门禁、链接与改动面

```bash
bash scripts/check.sh > /dev/null 2>&1; echo "exit=$?"
```

期望 **exit=0**；**预验基线 `exit=0`**。

```bash
npx markdownlint-cli2 "**/*.md" 2>&1 | tail -1
```

期望 **0 issues**；**预验基线 0 issue**（66 件）。

```bash
git status --porcelain | wc -l
```

期望 **5**（**逐类列举**：`M` **4 件**＝`CHANGELOG.md`／`skills/abzu-outline/references/outline-stage-spec.md`／`skills/abzu-outline/references/outline-workflow.md`／`skills/abzu-outline/references/outline-emotion-words.md`；**未跟踪 1 件**＝本规格）。**预验基线 1**（仅本规格未跟踪）。**施工方须现场逐行列举**并与上列 5 件逐名核对；**若不符即停摆申报**（不得自行改数字）。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **断链 0**（总数应与基线 176 同；本批不新增链接）。**预验基线 176‧0**。

### 断言 6｜过程产物与走查

```bash
grep -cF '阶段一资产内容（094）' CHANGELOG.md
```

期望 **≥1**；**预验基线 0**；行长 ≤400 字符（`python len` 实测并记入核验结论）。

走查型：**真机走查（作者执行）**——按 `测试与验收标准` §3 做法（仓库内禁跑安装器；装入目标项目 → 交互面板新开会话 → 跑 1.0→1.1→1.2 四步）→ 通过判据：① 停靠／门控确认后 `创作进度.md` 的状态表行**带「-待确认」式样**；② `088` §八 16 的四步结论逐项有答案；③ 记录回填本规格 §九（体例见 `087` 的走查记录）。未执行前**不得归档**。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 十三条全部实跑取证（① 逐行读 `:53`–`:57`／②③④⑤⑥⑦⑧ 六条 `grep` 逐行／⑨ `git log`／⑩⑪ `wc -l` ＋ `git log --numstat`／⑫ 两处 `grep`／⑬ 三命令）。
2. **断言实跑过（改前侧）**：断言 1（0／0）／2（0／1）／3（3 件／0）／4（4／2）／5（`exit=0`／0 issue／porcelain 0／断链 0·总 176）／6（0）逐条实跑（见 §四 附）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：断言 3 的「基线 3 件」由 `grep -rc` 逐件实测得出（3 件非 0）；断言 3 第二条的「期望 4」由该件四行同类标记**逐行列举**得出；断言 5 的 porcelain 期望由**逐类列举**得出（4 M ＋ 1 未跟踪）；断言 1 第二条的「期望 0」由「式样只落一处」的设计裁定＋基线的 0 得出。

**预验摘要（改前侧实测）**：断言 1-1＝**0**｜1-2＝**0**｜2-1＝**0**｜2-2＝**1**｜3-1＝**3 件**｜3-2＝**0**｜4-1＝**4**｜4-2＝**2**｜5-1＝**exit 0**｜5-2＝**0 issue**（66 件）｜5-3＝**porcelain 1**（仅本规格）｜5-4＝**176‧0**｜6-1＝**0**。

**[4] 段预警（施工前 6 条）逐条判定**：检查 G 自由度不符＝**待处置**（本修订已改头注）；对账① 禁改面未豁免＝**待处置**（本修订已在 §五 明示豁免）；`[A]` 未落实＝**属预期**（施工前常态）；零命中待复核＝**属预期**；断言 2-2 的自噬预警＝**属预期**（近似误报，成稿审查已独立判正）；「无尺寸声称」＝非发现。**无未核项**。

## 五、禁止事项

- 禁止触碰：**除 F1–F3 列明的四件外的**任何 `skills/` 资产（**豁免**：`references/outline-stage-spec.md`／`references/outline-workflow.md`／`references/outline-emotion-words.md`——本批改动面见 §二）；`skills/` 以外的任何**资产**（本批只动列明三件）；`docs/`（含 `docs/product/`／`docs/standards/`／`docs/specs/` 除本规格）；`scripts/`；`AGENTS.md`／`README.md`。
- **F1 不得在 `SKILL.md`／`outline-workflow.md` 复制式样**（单一真源；反模式 #4）。
- **F3 不得改「未建」的事实**，只改「随 089–091」这一到期依据的表述；**不得**顺手删除任何「未建」告知句（降级纪律要求如实告知）。
- 不得改断言、改数字、删检查来变绿。
- 禁止动作：任何 git 写命令；联网；安装依赖（**仓库内禁跑安装器**——`check.sh` `[0]` 段守卫所辖）；真机走查由作者在**目标项目**内做。

## 六、被否决的替代方案

- **式样落 `SKILL.md:31`（义务句）** → 否决：义务句是**全域原则**（门控与停靠），而式样是**产物格式细节**，其消费时刻是执行 Stage 1 时（阶段文件已加载）；落 `1.2` 使「义务—判据—落点」同处一屏，且 `092` §八 1b 原定落点即此。**勘察订正**：1.2 内并无「落行句」（落行义务在别件），故本批的 F1 是在 1.2 的**门控末句**上补式样（形态与原定一致：一行）。
- **把「{产物名}-待确认」写进骨架围栏（`:18`–`:23`）作示例数据行** → 否决：该骨架是**最小骨架**（空表头），示例数据行会把「示例」误当「初始状态」；式样串＋一例在 1.2 句内即够自包含。
- **`SKILL.md:31` 与 `outline-workflow.md:18` 各补一句式样** → 否决：两处即第二副本（反模式 #4）；且义务句不必承载格式细节。
- **F3 改为删除「随 089–091」标记** → 否决：删除会使该行只剩「未建」，而「未建」的**告知义务**（降级纪律）仍在；「随对应阶段批」既保留告知、又去掉失真的批次指向。

## 七、本批核销（093 §八 全部 ＋ 092 §八 1b／2 ＋ 088 §八 16／2）

**必列面**：① **前一批（`093`）§八 全部开放项**；② 本批改动面直接触及或使其状态变化者（含经 `093` §七 转承的 `092` §八 1b／§八 2、`088` §八 16／§八 2）。来源列取**跨批索引落点**（各批核销表）——`092` 两条经 `093` §七 转承，此处并列引其原批以便追溯。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 093 §八 1 | `[3]` 加粗密度项未实现（触发＝阈值扩展立法） | **仍待** | 触发未到（阈值与「入口型文档」定义均未立法；已并入 `测试与验收标准` §4 G1） |
| 093 §八 2 | 「维护出处」标注检查的判据近似（`G23`） | **仍待** | 触发＝出现漏报实例（`G23` 已登记） |
| 093 §八 3 | 1.2 落行式样内联 ＋ 既有旧措辞收口（触发＝阶段一资产内容批） | **本批处置** | **F1／F2／F3**（本批即该批量） |
| 093 §八 4 | 叙事型文档出现后加粗密度与 TOC 适用面须重核 | **仍待** | 触发＝出现叙事型治理文档 |
| 093 §八 5 | `docs/specs/施工机制.md` 272 行的模板是否下沉 | **仍待** | 触发＝触及体积预算讨论（`G12`） |
| 092 §八 1b（经 093 §七 转承） | 1.2 的落行式样未内联也未指向（成稿审查 F-14） | **本批处置** | **F1**（勘察订正落点：1.2 内无「落行句」，落 1.2 门控末句） |
| 092 §八 2（经 093 §七 转承） | 既有「见 `X` §N」旧措辞的收口触发 | **已履行** | 本批实测：**包外旧措辞存量 = 0**（`skills/` 内 7 处「见 `X`」全指包内件）——该条**自然满足**；残余两项（裸具名 ⑦／批次号 ⑧）由 **F2／F3** 处置 |
| 088 §八 16 | 真机走查未执行（欠账不得超过下一个域建成） | **本批处置** | **F5**：本批真机走查与 `088` 欠账**合并复验**（同路径 1.0→1.1→1.2 四步），走查记录落本规格 §九 |
| 088 §八 2 | 词表另四处消费点的补入（各批上线时，并删标注） | **仍待** | 本批只改**标记表述**（F3），**消费点本身未上线**（Stage 2–4 仍零资产）——成稿审查 P1-5 订正原「本批处置」 |

- 汇总（恒列五档、无该档计 0）：`已履行 1｜作废 0｜本批处置 3｜仍待 5｜待作者 0 ＝ 9` ✓

## 八、开放项

1. **`assets/` 无状态表示例**：本批只让 1.2 自包含式样；若实测发现「无完整示例行」仍致模型写错形态，则补一例（触发＝真机走查观察到形态偏差）。
2. **`SKILL.md:31` 与 `outline-workflow.md:18` 的义务句重复**（两处同义）：本批不动（属既有多载体，非本批引入）；触发＝任一义务句变更时一并收口。
3. **`088` 施工记录的行数误记（52 实为 56）**：归档件只读，不回填；本批 §一 ⑪ 已如实登记。
4. **残余的批次号型标记**（成稿审查 P2-10）：`skills/abzu-outline/references/outline-emotion-words.md:3`「本批唯一上线消费点」与 `skills/abzu-outline/SKILL.md:24`「本批 Stage 1」等**非「随 089–091」形态**的批次号标记不在 F3 的 8 行内（F3 面窄，属已知）——触发＝下一次触及 `skills/` 正文时一并处置。
5. **`:7` 的差异清单未含本批增补**（产物审查 P2-1）：该行的括注自称「源件自带 3 条 markdown 链接已改写…（`088` §九 差异有记）」，而本批 F1 在 1.2 末句**增补**了源件没有的落行式样括注（`git diff` 可证）——**该差异只记在本规格内**。触发＝下一次触碰该件时把差异括注扩为「（链接改写与增补差异分见 `088` §九／各批规格）」。
6. **`命名标准` §5 末句的类目名与实物脱节**（产物审查 P2-2）：该括注写「**批次号型**包外标记——如「随某批未建」」，而本批后 `skills/` 内**批次号型实物已归零**（`随 089–091` 无命中）、存量 8 处新形态「随对应阶段批」**既无批次号也无对应类目名**（它不指向任何 `docs/` 件，本不落入该条款本体）。触发＝下次触碰 `命名标准` §5 时把类目名放宽为「批次／阶段**指向型**」或另立表述。
7. **「随对应阶段批」与「随各自阶段批」两措辞并存**（产物审查 P2-3）：本批新写 ×8 为「随**对应**阶段批」，而既存 ×2（`outline-workflow.md:48`／`outline-stage-spec.md:16`）为「随**各自**阶段批」——同指两形。触发＝下一次触碰 `skills/` 正文时统一择一（本批 F3 面为 8 行，既存两处不在面内，属已知面宽）。

## 九、审查记录

- **核验结论（规划方，2026-09-15）**：**第 1 步·改动面核对**——`git status --short` 5 行（4 `M`＝`CHANGELOG.md`／三件 `skills/abzu-outline/references/` 资产；1 未跟踪＝本规格），与断言 5 的逐类列举**逐名一致**、清单外零文件；`git diff --stat` 恰 `4 files changed, 11 insertions(+), 10 deletions(-)`；`SKILL.md`／`assets/`／`docs/`／`scripts/` **零改动**。**第 2 步·断言独立重跑**——断言 1–6 机检部分逐条命中（1-1＝1／1-2＝0／2-1＝1／2-2＝0／3-1＝0 件／3-2＝4／4-1＝**5**／4-2＝2／5 `exit=0`‧lint 0 issue‧porcelain 5‧链接 176‧断链 0／6＝1 行 263 字符）；退出码不经管道。**第 3 步·内容抽查**——触发产物审查条件（动 `skills/` 资产＝重路径）→ 派双维产物审查（见下条）。**[4] 段预警逐条判定（施工后 6 条）**：4 条自噬预警＝**属预期**（本批新增文本已到位，「目标文本内」计数含规格自身引文）；`目标文本未提及且现行为 0: 待确认 @ outline-workflow.md`＝**属预期**（该件刻意不落式样——单一真源，**断言 1-2 即此判据**）；`现行有值／目标文本未提及: 1 个`＝**属预期**（归零型／引文形态）。**无未核项**。
- **施工期判因申报与裁定（规划方）**：① **P1-1（三行同型锚点改用 Edit）→ 判定合规**——`施工机制` `:80` 修订工具律末句原文：「触发条件：**以脚本／命令行方式做批量或长串匹配的仓内文本改动**时本条适用；**宿主自带的一次一处、要求唯一匹配的编辑工具（Edit）不受本条约束**，但改前须 `grep` 取原文（#18）」——`outline-workflow.md:23`／`:27`／`:31` 为**逐字相同的整行**，`patch_file.py` 的「锚片段须唯一」设计使其无法一次命中一行；施工方改用 Edit 带多行上下文（逐处唯一、未用 replace-all）并先 `grep` 取证，**路径合规**（B 面：同类三行以上同型锚点若成常态，可评估为 `patch_file.py` 增「按行号 ＋ 上下文双键」模式——本批不扩）。② **P1-2（`outline-stage-spec.md:4` 删「本批」二字）→ 符合 F3 预期**：「（本批未建，随 089–091）」→「（未建，随对应阶段批）」——**「未建」事实陈述逐字保留**、无一告知句被删 ✓。③ **P1-3（`[4]` 段施工前 7 条而非规格所记 6 条）**：规格 §四 记的是**预验时点**的值（该时点两条「零命中待复核」尚未随规格引入而出现）；差异属检查器口径与时点，非改动引起，记录在案。④ **P1-4（`CHANGELOG.md` 的 CRLF 警告）**：该件施工前即 CRLF（本批沿用其行尾、未改），警告为既存状态。
- **产物审查·双维**（独立上下文只读 subagent）｜findings 共 **5**：**P0:0 P1:1 P2:4**｜结论 **有 finding（无 P0，均不阻断）**。**P1-1（读法分岔）**：本批把落行**式样**写进 1.2，而 `outline-workflow.md:56` 写着「规格门确认后**只落阶段值**，不进入资料收集」——模型若照 `:56` 字面只写阶段值、不落行，则走查判据① 失败；照判据① 落行又与 `:56` 字面不合 → **处置：先钉口径、后走查**（见下条「走查口径预钉」）；若走查实测确因 `:56` 未落行，则**下一批**把 `:56` 改为「只落阶段值与门控落行（不产出 Stage 2 内容）」。**P2 四条**：P2-1（`:7` 差异清单未含本批增补）／P2-2（`命名标准` §5 末句类目名与实物脱节）／P2-3（「对应／各自阶段批」两措辞并存）——**三条登记入 §八 5／6／7**；P2-4（走查记录字段未覆盖「换模型并记录」）——**已补进 F5 字段表**（「模型与会话形态」）。**审查方独立实证**（未采信规格自述）：式样串与 `触发与交互标准:20` **逐字同源**、包内**只此一份**（另两件 `待确认` 计数 0）；裸具名**归零**（`《大纲域设计` 在 `skills/` 无命中，`迁自产品层《` 0）；两处「维护出处：」并置**读不出歧义**；8 行改述后**「未建」事实全部保留**；`CHANGELOG` 条目 263 字符且所声称三项逐项在 diff；真源结构**未被取代**（`书目录契约:37`／`触发与交互标准:20` 仍是规范级真源，资产内为部署副本）；`088` §八 2 的到期制**仍成立**；**真机走查「必跑」独立成立**（F1 改变用户书目录内可见的 `创作进度.md` 状态表行式样）。
- **走查口径预钉（规划方裁定，P1-1 的处置；落 §九 供走查时判读）**：`outline-workflow.md:56` 的「**规格门确认后只落阶段值**」——**指「不产出 Stage 2 内容」**；**门控呈现时须落 `基本规格-待确认` 行，确认后翻阶段值**。走查判据①（状态表行带「-待确认」式样）**按此口径判**：若模型在 1.2 门控**呈现**阶段写出了带式样的行、并在确认后翻阶段值 → **判据① 成立**；若模型全程只写阶段值、从未落行 → **判据① 不成立**，**且该结果直接触发下一批改 `:56`**（把该句补为「只落阶段值与门控落行」）。此口径为**预钉**（不修改资产），走查记录须如实记「模型的实际行为属哪一支」。
