# 099-2026-09-15-字段移植与形态收口批

> 定位：处置**真机走查第二轮**（作者执行，2026-09-15，宿主 Claude Code 2.1.272）的六条发现——① **书名从 1.1 移出**（基本规格阶段故事尚空白、定不出书名；落到大纲侧）＋ 连带修正占位符歧义（`资料/参考-{书名}.md` 的 `{书名}` 实为**参考作品**名）；② **「避雷」措辞**去掉（该项名与已立术语相抵：「避雷」本批已明确归**读者侧**采风数据）；③ **`触发与交互标准` §3 第 7 条收窄**（原示例「一问（对话式）＋三问（选项列表）」正是实机「结构化问句抢答、自由问句被推迟一轮」的成因）；④ **「约束」节形态**（实机把模板该节的四项规则 ＋ 处置话术 ＋ 三态注记**替换成了本次校验结论**，下游要用的东西丢了）；⑤ **门控呈现须在对话内贴出产物全文**（原「全文可展开」被实机理解成「文件可打开」，把翻阅负担推给作者）；⑥ **「底色交织」采集口径**（规则写「作者主动说时记录」，实机按段一推导补并在回读中确认——口径与行为须择一）。
> 路径判定（`规格写作标准` §1／`施工机制` §二）：**重路径**——动 `skills/` 运行资产（作者可观测行为）＋ 改产品层设计件 ＋ 改规范条文 → 标准 ＋ **双维产物审查** ＋ **真机走查**（本轮走查即其发现来源；下一轮走查随批尾合并）。
> 自由度分布：[A]×7 [B]×10
> 必读输入：本规格 §一（全部锚点）；`docs/product/outline/大纲域设计-规格与资料步.md` §一／§二；`docs/product/outline/大纲域设计-草稿步.md`（3.1 故事核节字段）；`docs/product/outline/大纲域设计-附录.md` B-1／B-2；`docs/product/书目录契约.md` §1／§2；`docs/standards/触发与交互标准.md` §3；`skills/abzu-outline/assets/outline-basic-spec-template.md`；`skills/abzu-outline/references/outline-stage-spec.md`；`skills/abzu-outline/references/outline-emotion-words.md`
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `skills/abzu-outline/assets/outline-basic-spec-template.md:17`＝`书名` 定义表行（「可暂定但必须有值」）／`:58`＝模板正文 `书名` 行 | 逐行 read |
| ② | `docs/product/outline/大纲域设计-规格与资料步.md:39` 完备校验句含「书名可暂定但必须有值」／`:45` 段三句首「书名选项列表[{AI 拟三候选}/自拟]」／`:128` 验收基准首项「书名」；`skills/abzu-outline/references/outline-stage-spec.md:44`／`:50` 同两处 | 逐行 read |
| ③ | `docs/product/书目录契约.md:28` 括注内「书名可暂定但必须有值」；`:14`／`:30`＝`资料/参考-{书名}.md`（文件名内嵌书名） | 逐行 read |
| ④ | `docs/product/outline/大纲域设计-附录.md:31`＝B-1 摘要（首项「书名」）／`:33`＝B-2 标题行含 `资料/参考-{书名}.md` | 逐行 read |
| ⑤ | `docs/product/outline/大纲域设计-规格与资料步.md:45` 句尾「避雷对话「有什么绝对不写的？越具体越好。」」／`:96` 2.2④ 产物 `资料/参考-{书名}.md`／`:137` 验收基准含 `资料/参考-{书名}.md`；`skills/abzu-outline/references/outline-stage-spec.md:50` 句尾「避雷对话（对话式）…」；`skills/abzu-outline/assets/outline-basic-spec-template.md:24` 说明列「**避雷清单**，越具体越好（对话式采集）」 | 逐行 read |
| ⑥ | `docs/standards/触发与交互标准.md:34`＝§3 第 7 条（含示例「可并列不同模态（如一问（对话式）＋三问（选项列表））」） | 逐行 read |
| ⑦ | `skills/abzu-outline/assets/outline-basic-spec-template.md:87`＝「### 约束（联动校验四规则＋区间外处置话术）」／`:89`／`:90`／`:91`（区间外处置话术）／`:92`（生命周期三态注记「草稿（1.1）→ 实测（2.3 回填「参考标尺」段）→ 定格（4.3）」） | 逐行 read |
| ⑧ | `docs/product/outline/大纲域设计-规格与资料步.md:52`＝1.2 交互句「摘要 ≤3 条+全文可展开，选项列表…」；`skills/abzu-outline/references/outline-stage-spec.md:57` 同句（副本形态） | 逐行 read |
| ⑨ | `skills/abzu-outline/assets/outline-basic-spec-template.md:27`＝`底色交织` 定义表行（说明列「**作者主动说时记录**，写补充句；不进完备校验、不做偏离判定」）／`:71`＝模板正文行；`skills/abzu-outline/references/outline-emotion-words.md:17` 取值规则末句含「其余底色走『底色交织』自由补」 | 逐行 read |
| ⑩ | `docs/product/outline/大纲域设计-草稿步.md:70`＝故事核节字段列表（一句话故事／类型内核／金手指十字段／主角轮廓／支柱场面／阻力三层／灵感来源——**无书名项**） | 逐行 read |
| ⑪ | 走查第二轮现场产物（`C:\Users\1\Desktop\test2-3\`）：`创意进度`＝`创作进度.md` 含「基本规格-待确认」行、阶段值仍「大纲-规格中」；`基本规格.md` 字段面为新版（`作者绝对不写`／`底色交织` 在位，`读者避雷点`／两倾向字段零残留），**但**其「约束」节写的是本次校验结论（全绿／相符／完备律过），**模板 `:89`–`:92` 的四项规则与三态注记未落**；且 `# 基本规格` 下多出一行 `> 状态：草稿（…待规格门确认）`（模板无此行） | 两份产物逐行 read |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `skills/abzu-outline/assets/outline-basic-spec-template.md:17`（定义表整行）、`:58`（模板正文行） | **删** `书名` 两行——书名**不再是 1.1 的必答字段**（作者裁定：基本规格阶段故事尚空白、定不出书名） | [A] | 走查发现① |
| F2 | `docs/product/outline/大纲域设计-规格与资料步.md:45` 段三句首半句 ＋ 其**头注计数**；`skills/abzu-outline/references/outline-stage-spec.md:50` 同两处；`docs/product/outline/大纲域设计.md:124`／`:180`（两处「段三选项列表 ≤3 ＋对话式 ≤3」计数句）；`docs/product/outline/大纲域设计-规格与资料步.md:46`／`skills/abzu-outline/references/outline-stage-spec.md:51`（收拢示例「还剩三个小项」） | 删「**书名选项列表[{AI 拟三候选}/自拟]；**」——段三不再问书名。**连带（成稿审查 P1-1：删后段三零个选项列表问句）**：① 头注——真源 `:45`「（选项列表≤3+对话≤3）」→「（**对话式 ≤3**）」；副本 `:50`「（**逐项单模态**：选项列表≤3、对话式≤3）」→「（**逐项单模态：对话式≤3**）」（**窄域复核 P1-1**：副本头注形态与真源不同，须**各自写死**；「逐项单模态：」前缀系 `088` 差异 V-04 立的口径，**保留**）；② `大纲域设计.md:124`（**半角 `+`**）与 `:180`（**全角 `＋`**）的「段三选项列表 ≤3 ±对话式 ≤3」**按各自现文实串**同改为「段三对话式 ≤3」；③ 收拢示例「**还剩三个小项**」→「**还剩两个小项**」（段三只剩基调＋作者绝对不写） | [A] | 走查发现①；**成稿审查 P1-1**（原只删句首半句，致 F2 与 F7 互斥、三处计数失准） |
| F3 | `docs/product/outline/大纲域设计-规格与资料步.md:39`、`:128`；`skills/abzu-outline/references/outline-stage-spec.md:44`；`docs/product/书目录契约.md:28`；`docs/product/outline/大纲域设计-附录.md:31` | 删各处「书名」相关半句与项——**逐处按现文实串落**（成稿审查 P2-1：原引号与两处现文不符）：`:39`／`:44` 完备校验句内「**书名可暂定但必须有值；**」；`:128` 验收基准首项「书名／」；`书目录契约.md:28` 括注内「**书名可暂定但必须有值；**」（现文**无尾「；」**，按实串落）；`附录.md:31` B-1 摘要首项「**书名/**」（现文为 **ASCII 斜杠**，非全角「／」） | [A] | 走查发现①；**成稿审查 P2-1** |
| F4 | `docs/product/outline/大纲域设计-草稿步.md:70`（故事核节字段列表）；`docs/product/outline/大纲域设计-附录.md:89`（B-4 `①故事核` 模板行——该件自称「模板类内容的**唯一落点**」，**同源镜像须同改**）；`docs/product/outline/大纲域设计-草稿步.md` 的 **3.1⑤ 硬检查清单**与 **B-9 草稿验收单**（`附录`） | **新增**字段项「**书名（定名——本书的正式名，此处定案）**」——落点由 1.1 移至大纲侧。**成稿审查 P1-4 两处补正**：① 落点**扩至 `附录:89`**（原只改文档面；`附录:3` 自称唯一落点，属同源分叉）；② **声明必填性与判据**——书名在 3.1 为**必填**，**入 3.1⑤ 硬检查清单与 B-9 完备项**（判据：定案后须为具体值；理由：书名是本书对外的名字，故事核成形即当有值）。**不新建大纲模板件**（`skills/` 侧 asset 属 Stage 3 建设批） | [B] | 走查发现①；**成稿审查 P1-4**（原把「`skills/` 侧未建」与「`附录` B-4 已在」混为一事） |
| F5 | `docs/product/书目录契约.md:14`（**同一行两处**：占位符 `{书名}` ＋ 注释「文件名内嵌**书名**」）、`:30`；`docs/product/outline/大纲域设计-附录.md:33`、`:74`、`:75`；`docs/product/outline/大纲域设计-规格与资料步.md:96`、`:137`；`skills/abzu-outline/assets/outline-basic-spec-template.md:54`、`:76`、`:77` | **占位符歧义修正（同一歧义的全仓面）**：凡**指参考作品**的表述一律改带「参考作品」限定——**占位符形** `{书名}` → `{参考作品名}`（`书目录契约:14`／`:30`、`附录:33`、`规格与资料步:96`／`:137`、`assets:54`／`:76`／`:77`）；**无花括号形**「带**书名**指针」（`附录:74`／`:75`）→「带**参考作品名**指针」；**注释形**「文件名内嵌**书名**」（`书目录契约:14` 行尾）→「文件名内嵌**参考作品名**」。理由：该 `{书名}` 实指**参考作品**名（2.2 是参考作品档案），与「本书书名」同形致误读（规划方首读即误） | [A] | 走查发现① 连带；**预跑断言 1 时扩面**；**成稿审查 P0-2**（`书目录契约:14` 行尾注释原无归属，致断言 1 不可达）＋ **P2-2**（口径须标注：该件共 **19 行／21 次出现**，「19」为**计行**口径）＋ **P2-3**（无花括号形的替换式） |
| F6 | `docs/product/outline/大纲域设计-规格与资料步.md:45` 句尾（现文「**避雷对话**「有什么绝对不写的？越具体越好。」」——**无模态括注**）；`skills/abzu-outline/references/outline-stage-spec.md:50` 句尾（现文「**避雷对话（对话式）**「…」」——**已带括注**）；`skills/abzu-outline/assets/outline-basic-spec-template.md:24` 说明列 | 「**避雷**对话」→「**作者绝对不写**（对话式）」；说明列「**避雷清单**，越具体越好」→「**作者主观禁区清单**，越具体越好」——「避雷」本批已归**读者侧**采风数据（`资料/参考-{参考作品名}.md` 的「争议与雷点节」），此处是作者主观禁区，须分开。**成稿审查 P1-2：两处现文形态不同，须拆两处写死**——副本 `stage-spec:50` 处**删去原有「（对话式）」**（否则替换后成「作者绝对不写（对话式）（对话式）」），真源 `:45` 处**新增**「（对话式）」 | [A] | 走查发现②；作者裁定；**成稿审查 P1-2** |
| F7 | `docs/standards/触发与交互标准.md:34`（§3 第 7 条）＋ §3 的正反例段；**产品层四处「同屏」句**：`docs/product/outline/大纲域设计-规格与资料步.md:43`、`docs/product/outline/大纲域设计.md:124`／`:180`、`skills/abzu-outline/references/outline-stage-spec.md:48` | **收窄为「同一屏只出一种模态」**（作者／规划方裁定）：① 标准侧——`§3 第 7 条` 的「一条消息内可并列多个问句」改为「**同一屏只出一种模态**」（可并列若干**同模态**问句；**不混模态批**），并**替换示例**（原「一问（对话式）＋三问（选项列表）」**正是实机「结构化问句抢答、自由问句被推迟一轮」的成因**，系规划方所写）；② **产品层四处「同屏」句同改**（成稿审查 P1-3：原 F7 只改标准 → 施工后**标准禁、产品层要求**，两相抵）——「段一＝邀请（对话式）＋**同屏**并列三个（选项列表）问句」按裁定改述为**分模态批**；③ **段序落产品层件**（非标准件——`触发与交互标准` §4 明写「域内准入律属产品层…本件只作指认，不复制细则」）：段一＝邀请（对话式）批在先、选项列表批在其后；段三＝**纯对话式批**（经 F2 删书名后段三已零选项列表问句） | [B] | 走查发现③（第三轮观察）；**成稿审查 P1-1／P1-3**；规划方自查订正（该示例系其所写） |
| F8 | `docs/product/outline/大纲域设计-规格与资料步.md:52`；`skills/abzu-outline/references/outline-stage-spec.md:57` | **门控呈现形态**：「摘要 ≤3 条+全文可展开」→「**摘要 ≤3 条 ＋ 在对话内贴出产物全文**（并说明落盘路径）」——原措辞被实机理解成「文件可打开」，把翻阅负担推给作者；作者明示「别让用户自己翻」 | [B] | 走查发现⑤；作者裁定 |
| F9 | `skills/abzu-outline/assets/outline-basic-spec-template.md:87`（节标题）与 `:89`–`:92` | **「约束」节＝规则声明、产物须原样保留**：节内补一句「**本节为长期规则与话术声明，产物中须原样保留**；本次校验结论另起一行写（可选）」。**该节四项内容一字不动**（承诺性／参考性／区间外处置话术／生命周期三态注记） | [B] | 走查发现④（实机用校验结论顶掉了该节） |
| F10 | 同件 `:27`（说明列）、`:71`（模板正文行） | 「**作者主动说时记录**」→「**段二可按段一推导补，回读时请作者确认**」——口径与实机行为对齐（实机即如此，作者未反对）；模板正文行括注「（可选）」保留 | [B] | 走查发现⑥；规划方裁定（择「可推导补」，理由：该栏可选且推导补能提高一次到位率，回读即确认口） |
| F11 | `CHANGELOG.md`（`[Unreleased]` 段） | **过程产物**：新增本批条目一行（格式按 `docs/standards/版本与分发标准.md` §3——列 0、以 `-` 加空格起首、≤400 字符；**条首串写死为「字段移植与形态收口（099）」**） | [B] | `施工机制` §三 过程产物两项；成稿审查 P2-⑤ |
| F12 | `docs/product/outline/大纲域设计-规格与资料步.md:40`（⑤过关）；`skills/abzu-outline/references/outline-stage-spec.md:45`（同句） | **补第三条过关判据并消歧**：「联动校验全绿＋完备律过」→「联动校验全绿 ＋ 完备律过 ＋ **产物对规过**」；「（本步无评委）」→「（本步无评委；**产物对规属硬检查，不是评委**）」；并新增**对规定义子条**：「**产物对规** ＝ 拿 `assets/outline-basic-spec-template.md` 的**模板正文**当清单逐段逐字段对照——节数一致／字段数一致／**呈列形态一致**／**无模板外增行**／该留的占位符（④标尺）留着」 | [B] | 走查二轮**新增发现⑨**（作者提出：本阶段缺「对最终产物的审查」）；依据＝四处形态偏离全部漏过（见 §九 ⑦）；**边界**：对规**不是** `施工机制` §五 的「审查」（无需独立上下文——有外部参照物、机械可比），也**不是**产品层「三评委」（`术语表:26`） |
| F13 | `docs/standards/触发与交互标准.md:20`（§2.3）；`docs/product/书目录契约.md:37` | **落行式样落在哪一列**补明——两处现文各按实串（**窄域复核 P2-1**）：`触发与交互标准:20`「落行式样如「{产物名}-待确认」）」与 `书目录契约:37`「落行式样如「世界观底盘-待确认」）」（**两处的例名本就不同**），各在其后补「（**产物名列写产物名、状态列写状态值**；式样串是读法，不是单元格内容）」 | [A] | 走查二轮**新增发现⑧**：两轮走查落法不一致（094 轮＝产物名「基本规格」／状态「待确认」；二轮＝产物名「基本规格-待确认」／状态「待确认」——同一式样两种落法） |
| F14 | `skills/abzu-outline/assets/outline-basic-spec-template.md:22`（卷数定义表行）、`:41`（推导句）、`:48`（示例行）、`:63`（模板正文行）；`docs/product/outline/大纲域设计-规格与资料步.md:36`（推导句）、`:39`（完备校验句）、`:114`（标尺同步句）；`skills/abzu-outline/references/outline-stage-spec.md:41`／`:44` | **卷数改为「实测回填的派生栏」**（作者裁定：卷数不是作者意志、是品类常态）：① 卷数 **1.1 只留位**（作者主动说了就记其值；未说则标「待资料收集后按标尺回填」）——**不计入完备校验**（比照读者避雷点旧例的处置方式）；② **标尺实测后按派生卷数区间回填**（甲：作者已给值 → 保留作者值、标尺值留痕为参照；乙：作者未给 → 直接按标尺填）；③ **规格侧不再判冲突**（来源唯一或作者优先，**不弹窗问作者二选一**——撤回原「判定＋四选」方案）；④ **跨批口径（窄域复核 P1-4 收窄）**：本批**不改标尺组数**——⑬⑭（体量／单章字数）及「每章字数按标尺⑭实测取」**以 `100` 批为准**（两批同在 `规格与资料步:36`／`stage-spec:41` 落笔，**施工次序先 099 后 100**，`100` 后落不覆盖本批语义） | [B] | 作者裁定（2026-09-15 会话）；**窄域复核 P1-4**（原 ④「写明每章字数与体量不进标尺」与 `100` F2/F4 **正面相抵**——收窄为跨批口径） |
| F15 | `docs/product/outline/大纲域设计.md:114`（「生成期不装」句）；`docs/product/outline/大纲域设计-草稿步.md:47`（「上游产物」行刻意不装栏，**窄域复核 P1-2 补**）、`:48`／`:86`／`:115`／`:142`（四处装载栏，即 `098` F19 的落面） | **标尺进草稿步——当「心里的秤」用，不当「要凑的数」**（作者裁定）：「实测标尺**不装**」→「实测标尺**装**，作**量级感参照**——**不得为凑区间注水**（护栏引 `docs/product/outline/大纲域设计.md:116` 四选之『补』的原话）」；**装的是量级十组**（作秤）；**⑬⑭（体量／单章字数）不进草稿**——它们的消费点是**开书定位**（即规格阶段，见 `100` 批）；**对照与三态判定仍留收尾**（085 的另一条理由——「生成期对象未成形、无从对照」——不受影响）；**`:47` 补改**——该行「④标尺段（实测态——实测区间不进生成，对照归 3.5）」改「④标尺段（实测态——作量级感参照装载，对照与判定归 3.5）」（**窄域复核 P1-2**：该栏与四处装载栏同表同义，漏改即两说）。**沿革落痕**：本项**翻** `098` F19 的「十组不装、⑪⑫装」判法，须在 §九 记明 098 F19 → 099 F15 | [B] | 作者裁定（2026-09-15 会话）：「草稿步骤其实要读规格＋参考标尺」；085 批「防对着区间写」的顾虑以「参照≠配额＋禁止注水」化解 |
| F16 | `docs/product/outline/大纲域设计-草稿步.md:165`（3.5 四查之「**一致**」行——**既有检查，本条为扩写而非新立**，窄域复核 P1-3 订正）、`:162`（3.5② 收集清单）、`:166`（量级查句）；`docs/product/outline/大纲域设计.md:114`（判定统一落 3.5 句）；`docs/product/outline/大纲域设计-附录.md:182`（B-9「一致」行，同源镜像） | **收尾对表扩为「双对表」**（作者裁定：「草稿结束再读一次这两个文件，保证不跑偏」）——**认领并扩既有「一致」检查**（`:165`／B-9:182 已有「对规格与底盘零矛盾」，本条**点明具体方向**）：① **对标尺**：量级偏离 → 三态判定 ＋ 四选处置（既有，不动）；② **对规格**：违背约束（**基调跑偏／踩作者绝对不写／参考侧重没照办**——原「一致」未点明的三向）→ **回改产物，不改规格**。两条路的**处置方向相反**，须写明谁改谁；③ **补装载**（窄域复核 P1-3）：`:162` 收集清单补 `基本规格.md（约束集）`——「对规格」须先装得进；④ **「参考侧重」判据与 `:47`「③参考偏好段 2.x 消费、刻意不装」相抵**——**择一**：装载纪律不动，判据收窄为「基调与作者绝对不写」（参考侧重已由 2.x 的分派与拍二对照承载，3.5 不重复查） | [B] | 作者裁定（2026-09-15 会话）；**窄域复核 P1-3**（原写「现设计收尾不查是否违背规格」与 `:165`／B-9:182 相抵——实为「未点明方向」，非「无检查」） |
| F17 | `skills/abzu-outline/assets/outline-basic-spec-template.md:26`（基调行「生产来源 Stage」列）；**`:4`（「> 边界：」行）之后新增一行注记**（窄域复核 P1-6 写死落点——原「`:3`／`:5` 附近形态注记处」在实物中不存在，`:5` 为空行） | **同件两小项收口**（成稿审查 P2-7／P2-6）：① **`098` 涟漪**——基调行「生产来源 Stage＝**1.1 段三（选项列表）**」而 `098` 已把基调改为**草案先行（对话式）**，本批正改该件故**同批收口**，改「1.1 段三（对话式·草案先行）」；② **产物不得增模板外行**——在 `:4` 边界行后新增一行（**逐字**）：「> 产物实例化纪律：按模板正文逐段逐字段产出，**不得增模板外行**（含状态行——状态由『创作进度.md』承载）。」（出处＝走查二轮发现⑦的现场产物多出状态行；**窄域复核 P2-2**：删原「比照 `#12`」悬空引用） | [A] | 成稿审查 **P2-6／P2-7**；**窄域复核 P1-6／P2-2** |

## 三、验收断言

### 断言 1a｜「书名」在五件归零（F1–F5）

```bash
grep -rc '书名' skills/abzu-outline/assets/outline-basic-spec-template.md docs/product/outline/大纲域设计-规格与资料步.md skills/abzu-outline/references/outline-stage-spec.md docs/product/书目录契约.md docs/product/outline/大纲域设计-附录.md
```

期望 **各 0**（**计行**口径）。**预验基线（改动前实测）**：`assets` **5 行**／`规格与资料步` **5 行**／`书目录契约` **3 行**／`附录` **4 行**／`stage-spec` **2 行** ＝ **19 行**（**21 次出现**——`assets:58` 与 `书目录契约:14` 各含两次）。**逐行归属**：F1→`assets:17`／`:58`；F2→`规格与资料步:45`／`stage-spec:50`；F3→`规格与资料步:39`／`:128`＋`stage-spec:44`＋`书目录契约:28`＋`附录:31`；F5→`assets:54`／`:76`／`:77`＋`书目录契约:14`（**该行两处：占位符 ＋ 行尾注释**）／`:30`＋`附录:33`／`:74`／`:75`＋`规格与资料步:96`／`:137`。**无残留、无归属错**（成稿审查 P0-2：`书目录契约:14` 行尾注释原无归属，致本该断言不可达——已归 F5）。

### 断言 1b｜书名的**新落点**（F4）

```bash
grep -c '书名（定名' docs/product/outline/大纲域设计-草稿步.md docs/product/outline/大纲域设计-附录.md
```

期望 **各 1**。**预验基线 0**（已实跑——两件均为 0）。

### 断言 2｜占位符歧义（F5）

```bash
grep -rc '参考-{书名}' docs/product skills
```

期望 **全 0**；**预验基线**：`书目录契约.md` **2**／`附录.md` **1**／`规格与资料步.md` **2**（实测合计 5）。

```bash
grep -rc '参考-{参考作品名}' docs/product skills
```

期望 **≥5**。

### 断言 3｜措辞（F6）

```bash
grep -rc '避雷对话\|避雷清单' docs/product skills
```

期望 **全 0**；**预验基线**：`规格与资料步.md` **1**／`outline-stage-spec.md` **1**／`outline-basic-spec-template.md` **1**（实测 3）。

### 断言 4｜§3 第 7 条收窄（F7）

```bash
grep -c '同一屏只出一种模态' docs/standards/触发与交互标准.md
```

期望 **1**。**预验基线 0**。

### 断言 5｜门控全文呈现（F8）

```bash
grep -rc '在对话内贴出产物全文' docs/product/outline/大纲域设计-规格与资料步.md skills/abzu-outline/references/outline-stage-spec.md
```

期望 **各 1**。**预验基线各 0**。

### 断言 6｜过程产物与门禁

```bash
bash scripts/check.sh
```

期望 **退出 0**（lint 0 issue、检查 G 不符 0 项）。

```bash
grep -c '字段移植与形态收口（099）' CHANGELOG.md
```

期望 **1**。

## 四、规格自审（四条）

1. **锚点现场核过**——§一 十一行逐条 read／grep 取得（本会话实测）；「某文档 §N」引用逐个核对：`触发与交互标准` §3（`:34`）、`书目录契约` §1／§2（`:14`／`:28`–`:30`）为现场读得的节；首现术语「参考作品名」在本规格内自带释义（F5 行）。
2. **断言实跑过**——断言 **1–5 的基线全部实跑**（见各条括注）；**预跑断言 1 时查出一处规格硬伤并已订正**：原只按 F1–F3 推「assets 归零」，实测五件共 **19 处**「书名」，其中模板主／辅参考行与标尺「带书名指针」属**同一占位符歧义**、须归 F5 面，否则断言不可达——此为「范围与期望同源」条（本条 4）在编写期的第一次生效；断言 6 两条属施工时点取值。
3. **禁止事项写明**——见 §五。
4. **范围与期望同源**——断言 2／3 的基线与期望取自**同一批目标件**的同一条 `grep`。

**附加项**：清单完备（F1–F17 与走查两轮十项发现、成稿审查 14 条 findings 逐条对齐——一轮：发现①→F1–F5、②→F6、③→F7、④→F9、⑤→F8、⑥→F10；二轮补：⑦→**作 F12 的依据**（自身不需改动——模板本已写对，是产物没照做）、⑧→F13、⑨→F12、⑩→F14（**已按作者后续裁定改写**：卷数改为「实测回填」而非「判定＋四选」）；另 **F15／F16 为作者 2026-09-15 会话内新裁**（标尺进草稿步＝当秤／收尾双对表）；F11 为过程产物）；**禁改面与改动面对账**——§五 列举的件若同时出现在 §二，已在该处明示豁免。

## 五、禁止事项

- **禁改**：`docs/specs/施工机制.md`、`AGENTS.md`、`scripts/` 下任何文件、`docs/specs/archive/` 内任何件。
- **禁改**：`skills/abzu-outline/SKILL.md` 与 `skills/abzu-outline/references/outline-workflow.md` 一概不动。
- **禁**：删或改 `docs/product/outline/大纲域设计-草稿步.md:189` 的「**避雷机制**」——那是**读者侧**概念（读者本能雷点反应），与 F6 的主观禁区不同族。**该件的改动面仅 `:70`（F4 落点）与 3.1⑤／B-9 的完备项入列处**，其余逐字不动（**成稿审查 P2-8**：原措辞只有禁止、未明示允许面，而**文件级豁免不在检查 E 面内**，全靠人工对账）。
- **禁**：改 `docs/product/outline/大纲域设计-打磨定稿步.md`（本批不触及）。
- **禁**：联网、采集、跑安装器。
- **禁**：改任何断言的期望值求绿（红线 4）。

## 六、被否决的替代方案

- **新建「大纲产物模板」件（B-4）承载书名** → 否决（本批）：模板件属 Stage 3 建设批的产物面，本批只落**字段列表**一处，避免两批搅在一起（若作者另有裁定则改）。
- **保留 §3 第 7 条原示例、只补一句「作者只答选项时走收拢式重问」** → 否决：收拢式是**兜底**，不该用来常态化「抢答」；且我写的示例正引导模型同屏混模态。
- **把「约束」节从产物模板中删除**（视其为一次性校验） → 否决：该节含**区间外处置话术**与**生命周期三态注记**，是跨阶段长期使用的约束声明，不是一次性结论。
- **「底色交织」维持「作者主动说时记录」并禁止推导补** → 否决：实机推导补 ＋ 回读确认提高了可用性且作者未反对；改口径比改行为更合本批目的。

## 七、本批核销（来源：走查两轮发现①–⑩ ＋ `098` §八 全部 ＋ `094` §八 1／4／8）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 走查发现① | 书名移出 1.1 ＋ 占位符歧义 | **本批处置** | F1–F5 |
| 走查发现② | 「避雷」措辞 | **本批处置** | F6 |
| 走查发现③ | 混合模态抢答（§3 第 7 条示例致） | **本批处置** | F7 |
| 走查发现④ | 「约束」节被校验结论顶掉 | **本批处置** | F9 |
| 走查发现⑤ | 门控呈现未在对话内展开全文 | **本批处置** | F8 |
| 走查发现⑥ | 「底色交织」口径与行为不一致 | **本批处置** | F10 |
| 走查发现⑦ | 产物形态对模板的四处偏离 | **本批处置** | 作 **F12** 的依据（不另立 F 项——模板本已写对，修法是补对规） |
| 走查发现⑧ | 落行式样落在哪一列未明 | **本批处置** | F13 |
| 走查发现⑨ | 本阶段缺「对最终产物的审查」 | **本批处置** | F12 |
| 走查发现⑩ | 卷数校准（措辞单向／无落点／冲突听谁） | **本批处置** | F14（已按作者后续裁定**改写**为「实测回填」） |
| `098` §八 1 | 「底色交织」栏的字段名与落位待实测校准（触发＝下一批真机走查观察） | **本批处置** | 触发**本批已到达**（二轮走查即该走查）→ 发现⑥ ＋ F10 |
| `098` §八 2 | 标尺十二组的守卫仍缺 | **仍待** | 触发＝内容轨检查器扩面；`100` 批改组数（十二→十四）但守卫仍未建 |
| `098` §八 3 | 清账项：`094` §八 6（`命名标准` §5 末句类目名放宽） | **仍待** | 本批未触及 `命名标准` |
| `098` §八 4 | 本批（098）改动面大 | **作废** | 该条系 `098` 的**自指提示**（非开放项），无处置面——按窄域复核 P0-1 裁定归五档之「作废」 |
| `098` §八 5 | 真机走查欠账（**明文「不得再顺延」**） | **本批处置** | **本批已执行**＝二轮真机走查（记录见 §九） |
| `098` §八 6 | ~~真源／副本分歧~~ | **已履行** | `098` 批内已消解——按窄域复核 P0-1 裁定归五档之「已履行」 |
| `094` §八 1 | `assets/` 无状态表示例（触发＝真机走查观察到形态偏差） | **仍待** | 走查**确已观察到**形态偏差（发现⑦），但本批的解法是**补对规**（F12）而非补示例——示例面未动，触发未真正满足 |
| `094` §八 4 | 残余批次号型标记（触发＝下一次触碰 `skills/` 正文） | **仍待** | **窄域复核 P0-2 订正**：两处实物在 `skills/abzu-outline/SKILL.md:24` 与 `references/outline-emotion-words.md:3`——**前者被 §五 冻结、后者不在 §二 清单**，触发虽命中、实物面本批不可及（原判「本批处置」系假销账）；触发＝触及该两件的批次 |
| `094` §八 5 | `outline-stage-spec.md:7` 差异清单未含增补（触发＝下一次触碰该件） | **仍待** | **窄域复核 P1-5 补列**：触发已到达（本批 F2／F3／F6／F12／F14 五处落点在该件）；但 `100` 批 F4 与本批 F14 同改 `stage-spec:41` 一行——**归 `100` 批处置**（其改动面已含），避免两批同址竞写 |
| `094` §八 7 | 「随对应阶段批」与「随各自阶段批」两措辞并存（触发＝下一次触碰 `skills/` 正文） | **仍待** | **窄域复核 P1-5 补列**：两处实物在 `stage-spec.md:16` 与 `outline-workflow.md:48`——前者不在本批任何 F 项落点、后者被 §五 冻结，实物面本批不可及；触发＝触及该两行的批次 |
| `094` §八 8 | 走查未采集两项（宿主版本／模型与会话形态） | **仍待** | 二轮走查**已采宿主版本**（2.1.272，见 §九）；**「模型与会话形态」仍未采集** |

- 汇总（恒列五档、无该档计 0）：`已履行 1｜作废 1｜本批处置 12｜仍待 8｜待作者 0 ＝ 22` ✓（**窄域复核 P0-1 订正**：原汇总「＝14／处置 10」与表体 19 行不自洽、且含五档外状态——现表体 22 行逐格可复核）

## 八、开放项

1. **大纲产物模板件（B-4）是否建立**：触发＝Stage 3（草稿步）建设批。
2. **`skill形态标准:113` 的占位符示例 `{书名}`**：本批未触及该件；书名不再是字段后，该示例宜换（触发＝下一次触碰该件的占位符语法节）。
3. **本批改动面（17 项／9 件）**：若施工期出现 P0 族 ≥2，按 `施工机制` §五 止损条收窄复核面。
4. **「产物对规」的通则面未落**：本批只落 1.1 的实例（F12）；通则（「每个产物产出后须对其模板做机械对规」，拟落 `docs/standards/产物契约标准.md`）**同批未做**。触发＝下一批触及该标准件，或作者指定同批补。**关联实例判据**：`产物契约标准` 内出现「产物对规」串。
5. **`每章字数`／`体量`（总字数）的定位待裁**（作者 2026-09-15 评：「没啥实际意义，存在与否并不重要，起码大纲阶段用不太到」）：**规划方核证**——`体量` 原有两条用途（①推卷数 ②推节点量级）**现在均可由标尺接管**（标尺有派生「卷数／节点数」），故「大纲阶段用不到」是**本几轮改动的结果**，非原始设计；`每章字数` 的真实用户是**正文域／章纲**（大纲阶段几乎不用）。**规划方建议**：**留在规格当「商品标签」**（规格的定义即「商品标签，不是内容」；且正文域要查每章字数），但**卸掉推导与校验职责**（不再由体量推卷数；「字数↔卷数量级」这条参考性校验随之可撤或改述）。触发＝作者裁定。
6. **大纲阶段的 agent 分工（架构级，跨批）**：**采风检索**＝orchestrator-workers（设计里已有「2.2 每书一档，subagent 产出」＋检索预算）；**草稿生成**＝按模块切 worker（模块**可枚举** → 用并行分片，不用动态分解；每个 worker 只装自己的方法论＋上游产物——**这天然解决上下文膨胀**）；**打磨三评委**＝evaluator-optimizer（设计里已有，且「只看产物不看过程」＝评估者独立）；**规格阶段不加 agent**（三查是硬检查、参照物在外部机械可比；加 agent 会引入「批评幻觉」——外部实证：在本来就写对的输出上自评，准确率从 98% 掉到 57%）。**三条判定**：能枚举 → 用固定流程；子任务随输入而异 → 编排者＋工人；生成与评估是两种认知任务 → 分离。触发＝架构批（**关联实例判据**：该批规格 §二 出现「agent 分工」或「编排者」串）。
7. **跨批界面：`099` ↔ `100`（两批同时在制，面有交叠）**：`100` 给标尺加两组（⑬体量／⑭单章字数），本批 F15 又改草稿步装载栏——**界面口径**＝草稿步装的是**量级十组子集**（作秤），⑬⑭ 属**开书定位**（规格阶段）**不进草稿**；故两批不撞（本批 F15 已按此写明）。**另**：`100` F3 的命名面（十二组→十四组）**不含 `草稿步`**（该件用「十组」＝量级子集，按 `098` 定规保留）——亦不撞。**窄域复核 P1-4 补第三面**：本批 F14 ④ 已**收窄**为「本批不改标尺组数，⑬⑭ 及每章字数的实测来源**以 `100` 为准**」（原写「写明每章字数与体量不进标尺」与 `100` F2/F4 正面相抵）。**施工次序建议**：先 `099` 后 `100`（`100` 的命名面与 F4 以 `099` 落地后的实况为准）。

## 九、审查记录

- **真机走查·第二轮记录（作者执行，2026-09-15）**｜宿主 **Claude Code 2.1.272**｜安装落点 `C:\Users\1\Desktop\test2-3\.claude\skills\abzu-outline\`（5 件齐；项目根 `skills-lock.json`）｜模型与会话形态：**未采集**（触发＝下一轮走查补记）｜**1.0→1.1→1.2 结论**：①入口判定按文件证据判「全新开书」✓②建设状态行如实报出 ✓③1.0 骨架（`创作进度.md` 头部两行＋三列表）✓④1.1 三段式走通：段一＝**邀请（对话式）＋同屏三个（选项列表）问句、每句带「拿不准的跳过」**✓、段二＝**一次静默推导＋一次回读**（含联动校验结论）✓、段三＝**基调草案先行**（四词草案请作者点头或改）✓＋**收拢式重问**（只答选项后合并不逐项追问）✓⑤1.2 规格门提出，作者选「先停下」✓。**判据①（落行式样）＝出现**：`创作进度.md` 含「**基本规格-待确认**」行；阶段值**未翻**（仍「大纲-规格中」）✓。**盘上产物实证**：`基本规格.md` 字段面为新版（`作者绝对不写`／`底色交织` 在位、`读者避雷点`／两倾向零残留、`读者定位` 三件值齐）✓。
  - **发现六条**（处置见 §二）：①书名不该在 1.1 定（作者裁定：移到大纲侧）＋ 连带查出占位符歧义 `参考-{书名}.md`；②「避雷」措辞（作者裁定改）；③混合模态同屏时结构化问句**抢答**、自由问句被推迟一轮（成因＝§3 第 7 条的示例系规划方所写）；④产物的「约束」节被**本次校验结论顶掉**，模板该节的规则／话术／三态注记未落；⑤门控「全文可展开」被理解成「文件可打开」——作者明示须**在对话内贴出全文**；⑥「底色交织」规则写「作者主动说时记录」而实机**按段一推导补**（回读确认），口径与行为须择一。
- **成稿审查·第一轮**（独立上下文只读 subagent，2026-09-15）｜findings 共 **14：P0:2 P1:4 P2:8**｜结论 **须修订**（审查方未采信本规格自述，全部结论自带实跑证据；锚点 11/11 与「书名」19 行归属**逐条复现通过**）。
  - **P0-1｜§七 核销表两条来源系虚构，且核销面漏列**——实测 `098` §八 实物为 1「底色交织」栏待校准／2 标尺十二组守卫／3 清账项／4 改动面大／5 真机走查欠账／6 真源副本分歧（已消解）；本规格挂的「`098` §八 5＝新术语未入术语表」「§八 6＝无在制批时走查记录无落点」**两条文本与编号都对不上**（前者实为 `098` §九 P2⑤，后者实为 `测试与验收标准` §3 的口径）。漏列面含 `098` §八 1–5 与 `094` §八 1／4／8，其中**五条触发条件本批已到达**（§八 1／5 触发＝下一批真机走查——本轮即该走查；`094` §八 1／4／8 同）。**裁定：按 `施工机制` §八「必列①前一批全部开放项 ②本批触及或状态可变者」重写 §七**，两条错挂条目来源改指真源，汇总行重算。
  - **P0-2｜断言 1 不可达**——`docs/product/书目录契约.md:14` **同一行内有两处「书名」**：占位符 `{书名}`（F5 面）与注释「每书一档，文件名内嵌**书名**」（**无 F 项归属**）。`grep -rc` 计**行**，施工后该件仍余 1 行 → 断言必红。属「范围与期望同源」失守（`规格写作标准` §4.4／§5.13，`088` F-02 同型）。**裁定：F5 增列该注释的目标形态**（「文件名内嵌**参考作品名**」），并在断言 1 附言点名该行两处。
  - **P1-1｜F2 与 F7 互斥**——F2 删掉段三唯一的「选项列表」问句（书名）后，段三**零个选项列表问句**，而 F7 写「段三＝先对话式批、后选项列表批」→ 不可同时成立；连带 `规格与资料步:45`／`stage-spec:50` 头注「选项列表≤3」、`大纲域设计.md:124`／`:180` 的计数句、`:46`／`:51` 的「还剩三个小项」示例均失准。**裁定：段三改为纯对话式**（基调 ＋ 作者绝对不写；头注改「对话式 ≤3」），`大纲域设计.md:124`／`:180` 与两处收拢示例同批同步；F7 的段序句删去段三的选项列表分句。
  - **P1-2｜F6 的统一替换串会在副本产出重复模态标注**——`规格与资料步:45` 现文为「避雷对话「…」」（无括注），而 `stage-spec:50` 为「避雷对话**（对话式）**「…」」；照 F6 单串替换，后者会成「作者绝对不写（对话式）（对话式）」。**裁定：F6 拆两处写死目标文本**（副本处删原「（对话式）」）。
  - **P1-3｜F7 漏了「抢答」的载荷面（产品层）**——实机抢答发生在**产品层 1.1⑥**「段一＝邀请（对话式）＋**同屏**并列三个（选项列表）问句」（四处 `同屏` 句：`规格与资料步:43`、`大纲域设计.md:124`／`:180`、`stage-spec:48`）；F7 只改标准 → 施工后**标准禁、产品层要求**，两相抵。且 F7 的「段序」子句**无落点**（写进 §3 与 `触发与交互标准` §4「域本件只作指认、不复制细则」相抵）。**裁定：F7 扩面**——四处产品层句按「同屏＝同模态批」改述（或删「同屏」限定），段序落产品层件；标准侧只留模态批约束。
  - **P1-4｜F4 与 `附录` B-4 分叉**——`docs/product/outline/大纲域设计-附录.md:3` 自称「模板类内容的**唯一落点**」，且 `:89` 已有 `①故事核` 模板行（**无书名**）；F4 只改 `草稿步:70` → 同字段清单两处不同源。F4 的「不新建大纲模板件」理由把「`skills/` 侧 asset 未建」与「附录 B-4 已在」混为一事。另 F4 未声明书名在 3.1 的**必填性与过关判据**（3.1⑤ 硬检查清单与 B-9 完备项均未含书名，而 `大纲域设计.md:79` 完备律以「必填字段全为具体值」为口径）。**裁定：F4 落点扩至 `附录:89`（同源镜像）**，并明写书名在 3.1 的必填性（拟：**必填**，入 3.1⑤ 与 B-9 完备项——书名是本书对外的名字，草稿步定案后即当有值）。
  - **P2 八条**（记录不阻塞，随修订一并落或登记）：① F3 引号与两处现文不符（`附录:31` 实为 ASCII `书名/`，`书目录契约:28` 括注**无尾「；」**）——[A] 逐字串须按现文改写；② 「19 处」口径未标注（实为 **19 行／21 次出现**，`规格写作标准` §5.8 要求标注计行或计次）；③ F5 对无花括号的「带书名指针」（`附录:74`／`:75`）替换式不适用，须写明目标文本；④ 断言 1／2／6 各含两条命令两个期望，须按 §5.9 拆为 `1a/1b` 等；⑤ F11 未约定 CHANGELOG 条目起首串（断言 6 的 token 依赖它）；⑥ **锚点⑪ 第三项事实（产物多出模板外状态行）无处置**——或立 F 项（模板补状态行／产物不得增行）或入 §八；⑦ **`098` 涟漪残留**：`assets:26` 基调「生产来源 Stage＝1.1 段三（**选项列表**）」而 098 已把基调改为草案先行（**对话式**），本批正改该件而未收口；⑧ §五 对 `草稿步` 的豁免措辞与 §四 自述不符（只有「禁删 `:189`」而无「允许改 `:70`」），且**文件级豁免不在检查 E 面内**。
- **作者裁定与后续发现（2026-09-15 会话内）**：**⑦ 产物形态偏离**（四处：③辅参考该两行却合成一条＋子项／⑤故事直觉该一行却拆两行／作品名丢书名号／「约束」节被顶掉）＝**不立独立 F 项**，作 **F12 的依据**——模板本已写对、是产物没照做，故修法不是改模板而是**补一道对规**；**⑧ 落行式样落哪一列**（两轮走查落法不一致）→ **F13**；**⑨ 本阶段缺「对最终产物的审查」**（作者提出）→ **F12**；**⑩ 卷数校准**（作者提问「为何收窄而非放宽／与标尺冲突听谁」）→ **F14**。**裁定**：⑦⑧⑨⑩ 按作者「落进去」一并入本批（F12–F14）。
  - **作者本轮新裁（2026-09-15 会话内，第二组）**：① **卷数改为「实测回填的派生栏」**——原 F14 的「判定＋四选」方案**撤回**（作者指出：卷数不是作者意志、是品类常态）。**查证结果**：标尺十二组里**只有卷数可进**（它是「换图规划」的派生项）；`每章字数`（来源＝**平台默认**）与 `体量`（来源＝**作者意志**）**无处可放**——故只改卷数，并**显式声明另两项不进标尺**（防日后误并）→ **F14 改写**。② **标尺进草稿步**，作「**心里的秤**」（量级感参照、不得为凑区间注水）、不当配额；「对照与三态判定」仍留收尾（085 的另一条理由——生成期对象未成形、无从对照——不受影响）→ **F15**。③ **收尾对表扩为「双对表」**：对标尺判量级／对规格判服从，两条路**处置方向相反**（前者可改标尺／后者只改产物）→ **F16**。**沿革落痕**：② **翻** `098` F19 的「十组不装、⑪⑫装」判法（`098` F19 → `099` F15）——非矛盾，系设计推进。
- **术语归位（作者提问触发）**：**「审查」与「评委」是两个角色**——**审查**＝开发流程角色（`施工机制` §五；由**独立上下文只读 subagent** 承担；审**开发产物**＝规格与施工产物；判据＝流程纪律与规范符合性）；**评委**＝产品层角色（`docs/product/术语表.md:26` 三评委＝读者／编辑／同行三视角，审**作品内容**＝大纲；判据＝市场与手艺），住在 4.1 打磨定稿步。二者**只共享「隔离装载」纪律**（`docs/product/outline/大纲域设计-打磨定稿步.md:4` 明写「同源」）。**而「产物对规」两者都不是**——它是**硬检查**的一种（有外部参照物＝模板、机械可比，故**不需要**独立上下文）；`1.1⑤` 的「本步无评委」本身没错，但它易被读成「什么都不查」，F12 即为此消歧。
- **第一轮 findings 的修订（规划方，2026-09-16）｜14 条全部落定**：**P0-1**（§七 核销表按 `施工机制` §八「必列前一批全部开放项＋本批触及者」**重写**：列 `098` §八 1–6 与 `094` §八 1／4／8，两条错挂条目改指真源，`098` §八 4 判「不适用」（自指提示非开放项），汇总重算为 **14 行／本批处置 10／仍待 4**）｜**P0-2**（F5 增列 `书目录契约:14` 行尾注释的目标形态，断言 1a 点名该行两处归属，并标**计行口径**——**19 行／21 次出现**）｜**P1-1**（F2 扩至段三头注计数、`大纲域设计.md:124`／`:180` 计数句、两处收拢示例「三个小项→两个小项」，消 F2↔F7 互斥）｜**P1-2**（F6 **拆两处写死**：副本处删原有「（对话式）」）｜**P1-3**（F7 **扩至产品层四处「同屏」句**，段序改落产品层件——`触发与交互标准` §4 明写「域内准入律属产品层、本件只指认不复制」）｜**P1-4**（F4 落点扩至 `附录:89` B-4 同源镜像，并声明书名在 3.1 为**必填**、入 3.1⑤ 与 B-9）｜**P2 八条**（① F3 按现文实串落／② 计行口径标注／③ F5 无花括号形替换式／④ 断言 1 拆为 1a／1b／⑤ F11 条首串已于前轮写死／⑥**锚点⑪第三项无处置** → **F17**／⑦ **`098` 涟漪**（`assets:26` 基调生产来源仍写「选项列表」）→ **F17**／⑧ §五 豁免措辞补明允许面）。
- **新增 F12–F17**（走查二轮发现⑦⑧⑨⑩ ＋ 作者后续三裁 ＋ 成稿审查 P2-6／P2-7）：对规进过关（F12）／落行式样落哪一列（F13）／卷数改实测回填（F14）／标尺进草稿步＝当秤（F15）／收尾双对表（F16）／同件两小项（F17）。**本批现为 17 项／9 件**。
- **窄域复核·第二轮**（独立上下文只读 subagent，2026-09-16）｜findings 共 **11：P0:2 P1:6 P2:3**｜结论 **部分修复——第一轮 P0-2／P1-1～P1-4 四项确认到位；P0-1 只修了虚构来源与漏列面，汇总行与档位未修**。**审查方独立确认面**：断言 1a 可达（19 行归属全覆盖、`书目录契约:14` 两处已收）、F2↔F7 互斥已解（段三＝2 问句零选项列表）、「同屏」恰四处、F4 落点属实（`附录:89`／`:3`）、F15 与 `100` F2/F3 **不相抵**、头注自由度自洽、断言基线逐条复现。
  - **P0-1**｜§七 汇总行与表体**不自洽**（表体 19 行 vs 汇总写 14；「本批处置 10」实为 13），且出现**两个五档外状态**（「不适用」「已销账」——`施工机制` §八 明写「状态取五档之一」）。**裁定**：`098` §八 4 判**作废**、§八 6 判**已履行**，汇总重算＝**19 行（已履行 1｜作废 1｜本批处置 13｜仍待 4｜待作者 0）**。
  - **P0-2**｜`094` §八 4 判「本批处置」系**假销账复发**（`098` 成稿审查 P0-3 同型）：两处实物一在 `SKILL.md:24`（§五 明文冻结）、一在 `outline-emotion-words.md:3`（不在 §二 清单）——触发虽命中、**实物面本批不可及**。**裁定**：改判「仍待」。
  - **P1-1**｜F2 对**副本头注**的引文与现文不符（副本是「逐项单模态：选项列表≤3、对话式≤3」，非真源形态），且未约定是否保留「逐项单模态：」前缀（`088` 差异 V-04 立的口径）。**裁定**：比照 F6 拆两处写死。
  - **P1-2**｜F15 漏 `草稿步:47`（3.1 装载表「上游产物」行的刻意不装栏仍写「实测区间不进生成」）。**裁定**：补落点并写死目标形态。
  - **P1-3**｜F16 的现状依据**不实**——3.5 的四查里早有「**一致**＝对规格与底盘零矛盾」（`草稿步:165`＋`附录` B-9:182）；真缺口只是「一致」未点明**基调跑偏／踩作者绝对不写／参考侧重没照办**的具体方向，且 `:162` 收集清单**未含 `基本规格.md`**、「参考侧重」判据与 `:47`「③参考偏好段 2.x 消费、刻意不装」相抵。**裁定**：F16 改为**认领并扩既有「一致」检查**（非「新立」），`:162` 补装载，「参考侧重」判据与装载纪律择一。
  - **P1-4**｜**F14 与在制 `100` 批正面相抵**（F14 ④「写明每章字数与体量不进标尺」vs `100` F2「标尺新增⑬⑭」＋F4「每章字数按标尺⑭实测取」），且两批同在 `规格与资料步:36`／`stage-spec:41` 落笔；§八 7 的界面只覆盖了 F15 ↔ 100，**漏 F14 ↔ 100**。**裁定**：F14 ④ 收窄为「本批不改标尺组数；⑬⑭ 及每章字数的实测来源**以 `100` 为准**」。
  - **P1-5**｜§七 按口径②仍漏列 `094` §八 5／7（触发已到达：本批五处落点在 `stage-spec.md`）。**裁定**：补列并判「仍待」（§八 7 的实物在 `outline-workflow.md:48`，本批禁改）。
  - **P1-6**｜F17② 的落点「模板头注形态注记处」**在实物中不存在**（`:5` 是空行；全仓无「产物不得增行」类条文）。**裁定**：写死落点＝「`:4` 边界行后新增第 5 行注记」＋目标文本。
  - **P2 三条**｜① F2／F13／F12 的引文须按现文逐处写死（`大纲域设计.md:124` 半角 `+`／`:180` 全角 `＋`；`触发与交互标准:20` 与 `书目录契约:37` 的式样例各不相同）；② F17「比照 `#12`」是**悬空引用**（三个候选目标都不对）——删括注、直接标走查二轮发现；③ `098` §八 1 判「本批处置」而其实测触发（**混用／误当必答**）未被观察到，与 `094` §八 1 判「仍待」的严格度不一致——理由里补「实质同族、从宽」或改档。
- **第二轮 11 条的修订（规划方，2026-09-16）｜全部落定**：**P0-1**（§七 汇总重算＝**22 行**（已履行 1｜作废 1｜本批处置 12｜仍待 8｜待作者 0）；`098` §八 4 判**作废**、§八 6 判**已履行**——五档外状态清除）｜**P0-2**（`094` §八 4 改判**仍待**——实物在 `SKILL.md:24`／`outline-emotion-words.md:3`，前者被 §五 冻结、后者不在清单）｜**P1-1**（F2 副本头注写死「（**逐项单模态：对话式≤3**）」，`088` V-04 前缀保留）｜**P1-2**（F15 补 `草稿步:47` 落点与目标形态）｜**P1-3**（F16 改为**认领并扩既有「一致」检查**（`:165`／B-9:182），`:162` 补装载；「参考侧重」判据收窄——与 `:47` 装载纪律择一，参考侧重已由 2.x 承载）｜**P1-4**（F14 ④ 收窄为跨批口径「以 `100` 为准」；§八 7 补第三面）｜**P1-5**（§七 补列 `094` §八 5／7 判「仍待」——§八 5 归 `100` 批处置、§八 7 实物不可及）｜**P1-6**（F17 落点写死「`:4` 边界行后新增一行」，逐字块改引号形态——原反引号嵌套致 MD038）｜**P2 三条**（① F2 的 `大纲域设计.md:124`（半角 `+`）／`:180`（全角 `＋`）按现文各自写死；② F17 删「比照 `#12`」悬空引用、出处改标走查二轮发现⑦；③ `098` §八 1 理由补「实质同族、从宽处置」并注明判据差异）。
- **两轮审查上限已用尽**（1 全文审 ＋ 1 窄域复核）——**本规格转入呈作者确认**。
