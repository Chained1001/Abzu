# 100-2026-09-15-标尺体量参照批

> 定位：把「**同类书多长、一章多少字**」从**手写平台常量**改为**实测参照**——作者 2026-09-15 三次追问「字数能不能挪进参考标尺」，核证结论＝**能，且有一处真收益**：`每章字数` 现以硬编码平台常量给出（`起点 3000-4000／番茄 2000-3000`，4 处），而这个数**本来就是从实践统计出来的**——挪进标尺（从参考作品数）后，不再手写维护平台表，且平台演变时自然跟随。**同时划清主体**：标尺采的是**同类画像**（参照），规格记的是**本书承诺**（作者意志）。
> 路径判定（`规格写作标准` §1／`施工机制` §二）：**重路径**——动 `skills/` 运行资产 ＋ 改产品层设计件（档案与标尺两模板的结构）＋ 改规范口径 → 标准 ＋ **双维产物审查** ＋ **真机走查**（本轮不单独走查，随批尾合并）。
> 自由度分布：[A]×5 [B]×5
> 必读输入：本规格 §一（全部锚点）；`docs/product/outline/大纲域设计-规格与资料步.md` §二；`docs/product/outline/大纲域设计-附录.md` B-1／B-2／B-3；`docs/product/outline/大纲域设计.md` §四 写法纪律 13；`docs/product/书目录契约.md` §1／§2；`docs/product/术语表.md`；`skills/abzu-outline/assets/outline-basic-spec-template.md`；`skills/abzu-outline/references/outline-stage-spec.md`
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `docs/product/outline/大纲域设计-附录.md:37`–`:52`＝B-2 参考档案字段表（14 行）——**无「作品体量」，无「单章字数」**；`:52`–`:56`＝枚举与默认值段 | 逐行 read |
| ② | 同件 `:58`–`:79`＝B-3 参考标尺表（十二组：①–⑫）／`:77`＝标尺分类句（可数组／不可数组／决策参照两组）／`:79`＝联动校验 | 逐行 read |
| ③ | `docs/product/outline/大纲域设计-规格与资料步.md:96`＝2.2④ 产物字段串（含本批前已加的「结局形态」「感情线处理」）／`:109`／`:111`／`:112`／`:114`／`:116`＝2.3 标尺五句 | 逐行 read |
| ④ | `skills/abzu-outline/assets/outline-basic-spec-template.md:23`（每章字数定义表行，说明列＝「按平台默认（起点 3000-4000／番茄 2000-3000）」）／`:42`（推导句「每章字数＝平台默认（…）」）／`:64`（模板正文行） | 逐行 read |
| ⑤ | `docs/product/outline/大纲域设计-规格与资料步.md:36`（默认值推导句，句尾「每章字数按平台默认（起点 3000-4000/番茄 2000-3000）」）／`:128`（作者验收基准含「每章字数」）／`docs/product/outline/大纲域设计-附录.md:31`（B-1 摘要含「每章字数」）；`skills/abzu-outline/references/outline-stage-spec.md:41`（同 `:36`） | 逐行 read |
| ⑥ | **平台常量**（串 `3000-4000`）落点 = **4 处**：`规格与资料步.md:36`／`assets:23`／`assets:42`／`stage-spec:41`（实测） | `grep -rc` 实跑 |
| ⑦ | **「十二组」落点 = 10 行**（实测）：`docs/product/书目录契约.md` **4**／`docs/product/术语表.md` **1**／`docs/product/outline/大纲域设计.md` **1**／`docs/product/outline/大纲域设计-附录.md` **1**／`docs/product/outline/大纲域设计-规格与资料步.md` **2**／`skills/abzu-outline/assets/outline-basic-spec-template.md` **1** | `grep -rc` 实跑 |
| ⑧ | 决策沿革：作者三次追问「字数能不能挪进标尺」；规划方前两次只答「标尺里现在没有」（属实但未答「该不该有」），第三次核证后改为「能，且每章字数的平台常量应由实测替代」——**本批即该结论的落地** | 本会话记录 |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `docs/product/outline/大纲域设计-附录.md:37`–`:52`（B-2 字段表）＋ `:52`–`:56`（枚举段）；`docs/product/outline/大纲域设计-规格与资料步.md:96`（2.2④ 字段串） | 档案**补采两字段**：「**作品体量**」（该参考作品的总字数量级——采集中注明「截至采集时／已完结」）与「**单章字数**」（该作品的典型单章字数）；均必填、来源 2.2④；枚举段按需补（单章字数＝区间／体量＝量级） | [B] | 走查后设计续：本批立「同类的字数画像」为可采项 |
| F2 | `docs/product/outline/大纲域设计-附录.md:58`–`:79`（B-3）；`docs/product/outline/大纲域设计-规格与资料步.md:109`／`:111`／`:112`／`:114`／`:116` | 标尺**新增两组（体量参照组）**：**⑬作品体量**（同类总字数量级分布）／**⑭单章字数**（同类典型单章字数）；**归参照类**（只给视角、**不做偏离判定**——「本书比同类长」是选择不是错误）；`:77` 分类句补该两组；`:112` 消费时点句补「⑬⑭ 供**开书定位参照**（作者定本书体量与每章字数时看一眼同类）」；`:109` 方法论默认表补两行默认值；`:111` 统计口径句的组数随之改 | [B] | 本批依据；对照 098 的「决策参照两组」同型 |
| F3 | `docs/product/书目录契约.md`（4 行）／`docs/product/术语表.md:45`／`docs/product/outline/大纲域设计.md`（1 行）／`docs/product/outline/大纲域设计-附录.md`（1 行）／`docs/product/outline/大纲域设计-规格与资料步.md`（2 行）／`skills/abzu-outline/assets/outline-basic-spec-template.md`（1 行） | 命名面：**「十二组」→「十四组」**（10 行，见锚点⑦；施工方须现场逐行列举并与锚点⑦对账）。**子集处的「十组」不动**（量级／判定口径，与 098 同规） | [A] | 锚点⑦；与 098 F21 同规 |
| F4 | `skills/abzu-outline/assets/outline-basic-spec-template.md:23`（说明列）／`:42`（推导句）；`docs/product/outline/大纲域设计-规格与资料步.md:36`；`skills/abzu-outline/references/outline-stage-spec.md:41` | **每章字数的来源改由实测**：现文两处「每章字数按平台默认（起点 3000-4000／番茄 2000-3000）」→「每章字数**按标尺⑭实测取**；作者主动说了就以作者值为准；**无参考时回退平台默认常量**（起点 3000-4000／番茄 2000-3000——**降级为兜底，不再是主来源**）」；`:23` 说明列同步（「1.1 默认推导」→「1.1 留位／资料收集后按标尺⑭回填」） | [B] | 本批核心收益：替掉手写平台表；与 099 F14（卷数改实测回填）**同规** |
| F5 | `CHANGELOG.md`（`[Unreleased]` 段） | **过程产物**：新增本批条目一行（列 0、以 `-` 加空格起首、≤400 字符；条首串写死为「标尺体量参照（100）」） | [B] | `施工机制` §三 过程产物两项 |
| F6 | `scripts/check.sh:4`（依赖与前置行） | **依赖声明补漏段**：现文（**逐字，无反引号**）「Python 3（**[3]／[4] 段需** python3／python／py，三级链均缺即置红）」→「Python 3（**[3]／[4]／[5] 段需** python3／python／py，三级链均缺即置红）」。依据：`[5]` 段用 `$PY` 跑 `ast.parse`（`scripts/check.sh:176`／`:180`），且 `scripts/*.py` 恒存在 → Python 3 为该段实际前置；093 批把 `[4]` 改述为「`[3]`／`[4]`」时，**同批新加的 `[5]` 加漏**。**注**：该行段号在现文中**不带反引号**（规划方初稿误加反引号致断言 token 恒零——预跑时订正） | [A] | 作者 2026-09-16 提问触发；规划方逐段实测（`$PY` 使用点 `:135`／`:151`／`:176`／`:180`） |
| F7 | `scripts/check_content.py:10`（文档头·用途段） | 去批次时态：「**加粗密度本批不实现**——阈值未立法且仓内零适用目标件」→「**加粗密度不实现**——阈值未立法且仓内零适用目标件」（与 `scripts/check.sh:9` 头注「加粗密度不实现」**同口径**；「本批」指 093，非状态声明） | [A] | G20 同族；规划方实测（该件「本批」仅 `:10` 一处为状态型，`:14` 的「本批改动面」是**工具无此概念的说明**、不属此列） |
| F8 | `scripts/check.sh`／`scripts/check_content.py`／`scripts/check_spec.py`／`scripts/patch_file.py` 四件的文档头 | **批次注释的两分处置**（作者裁定，本批范围）：① **状态型**（把某批的运行态写成恒常陈述者）**逐处改述为状态无关**；② **出处型**（批次号用作「该判据的事故来由」者）**保留原文**，改为在**四件文档头各补一句口径**：「内联注释中的批次号＝该判据的**事故出处**（provenance），**非状态声明**」。**范围判据**：以施工方现场 `grep` 逐处列举并分类为准（**不得沿用本规格未列的具体处数**——规划方以正则粗测「状态型」时大量命中系运行时状态〔如「当前命中 0」〕与示例串〔如「增量式如「本批给该件加 16 处加粗」不判」〕，**不可作范围依据**） | [B] | G20 族（**台账记「残余不存在」与实况不符**，见 F9）；**范围收窄理由**：出处型占多数且承载**代码之所以如此**的理由，全量改写会损技术承载，而其「会过时」的风险**由口径句消解**（声明为历史出处即不可能失真） |
| F9 | `docs/standards/测试与验收标准.md:83`（§4 `G20` 行） | **台账状态订正**（原状态与实况不符）：`G20` 现写「**本批处置（090 F1／F2①；091 复核判残余不存在）**：下次触发＝同类自指时态再现时」→ 改述为「**部分处置（090 F1／F2① 已收敛 docstring 与 `[2]` 段注释）**；**残余在册**——`check.sh`／`check_spec.py`／`patch_file.py`／`check_content.py` 内联注释仍有批次引用（**091 的「残余不存在」判定与实况不符**，本批订正）；本批以 F8 的两分处置收口（状态型改述／出处型加口径句）：下次触发＝**新增守卫件时同口径书写**」 | [A] | 作者 2026-09-16 提问触发；规划方实测（090 只动 `check_spec.py` docstring 与 `check.sh [2]` 段） |
| F10 | `docs/standards/测试与验收标准.md` §4 缺口表（`G25` 行之后追加两行） | **补登两条真工具缺陷**（规划方欠账——**此前多轮口头称「已登记」，实测四处台账均无**，本条为补登）：**`G26`**＝「`check_spec.py` 的 `BAN_EXCUSE_MARKS` 豁免词表**无「豁免」**，而判一的报错文案作「禁改面**未豁免**」——判据与文案不一致（写「豁免」不被识别；实测：`098`／`099` 两批各白折腾一轮）」；**`G27`**＝「检查 A 对**转义竖线 token**（如 `结局形态\|感情线处理`）按**字面**取数 → 「现行」计数**恒 0**（工具伪零：真值与 0 不可分；`098` 施工期复现）」。**并声明第三条不登**：曾拟登「计数期望易被新引入同串顶红」，经核该情形**已由 `规格写作标准` §5.4「断言与替换文本不得互斥」立法**，属**未执行既有规则**（`施工机制` §六 `#21` 族），**不构成工具缺口、故不登记** | [A] | 作者 2026-09-16「scripts 都看看」触发；`AGENTS.md` §四「引入任何此类机制须先登记事故出身」 |

## 三、验收断言

### 断言 1｜档案与标尺新增到位（F1／F2）

```bash
grep -c '作品体量\|单章字数' docs/product/outline/大纲域设计-附录.md
```

期望 **≥4**（B-2 两行 ＋ B-3 两组）。**预验基线 0**（已实跑）。

```bash
grep -c '作品体量\|单章字数' docs/product/outline/大纲域设计-规格与资料步.md
```

期望 **≥2**（2.2④ 字段串 ＋ 2.3 句）。**预验基线 0**（已实跑）。

### 断言 2｜命名面（F3）

```bash
grep -rc '十四组' docs/product/书目录契约.md docs/product/术语表.md docs/product/outline/大纲域设计.md docs/product/outline/大纲域设计-附录.md docs/product/outline/大纲域设计-规格与资料步.md skills/abzu-outline/assets/outline-basic-spec-template.md
```

期望 **各 ≥1**。**预验基线全 0**（已实跑）。

```bash
grep -rc '十二组' docs/product skills
```

期望 **全 0**。**预验基线（改动前实测）**：`书目录契约` **4**／`术语表` **1**／`大纲域设计` **1**／`附录` **1**／`规格与资料步` **2**／`assets` **1** ＝ **10 行**（锚点⑦）。

### 断言 3｜平台常量降级（F4）

```bash
grep -rc '3000-4000' docs/product skills
```

期望：仍 **4 处**（降级为兜底**并未删除**），但**每处的前缀须为「无参考时回退平台默认」**——施工方须逐处列举该处上下文并附在核验请求中。**预验基线 4 处**（锚点⑥）。

### 断言 4｜过程产物与门禁

```bash
grep -c '标尺体量参照（100）' CHANGELOG.md
```

期望 **1**。**预验基线 0**（已实跑）。

### 断言 5｜工具面收口（F6–F10）

```bash
grep -cF '[3]／[4]／[5] 段需' scripts/check.sh
```

期望 **1**。**预验基线 0**（现行文为「[3]／[4] 段需」——**无反引号**）。**注（预跑订正）**：该 token 含**方括号**，在 BRE 下 `[3]` 是**字符类**（匹配单个 `3`）——不加 `-F` 时恒零；故本条**以 `-F` 固定串核**（`grep -cF '[3]／[4] 段需'` 实测 **1** 已复现）。**通则**：断言 token 含正则元字符（`[`／`\|`／`.` 等）时，命令须声明匹配模式（`-F` 或转义），**不得默认读者按字面理解**。

```bash
grep -c '加粗密度本批不实现' scripts/check_content.py
```

期望 **0**。**预验基线 1**（`:10`，已实跑）。

```bash
grep -c '非状态声明' scripts/check.sh scripts/check_content.py scripts/check_spec.py scripts/patch_file.py
```

期望 **各 1**（F8 的口径句）。**预验基线各 0**（已实跑）。

```bash
grep -c '残余在册' docs/standards/测试与验收标准.md
```

期望 **1**（F9 的改述串）。**预验基线 0**（已实跑）。**并**：`grep -c '091 复核判残余不存在' docs/standards/测试与验收标准.md` 期望 **0**（**预验基线 1**）。

```bash
grep -c '^| G2[67] ' docs/standards/测试与验收标准.md
```

期望 **2**（F10 新增 `G26`／`G27`）。**预验基线 0**（已实跑——现表止于 `G25`）。

```bash
bash scripts/check.sh
```

期望 **退出 0**（lint 0 issue、检查 G 不符 0 项）。

## 四、规格自审（四条）

1. **锚点现场核过**——§一 八行逐条 read／`grep -rc` 取得（锚点⑥⑦为实跑计数）；「某文档 §N」引用逐个核对：`大纲域设计` §四 写法纪律 13（`docs/product/outline/大纲域设计.md:108`–`:116`）为现场读得的节。
2. **断言实跑过**——断言 **1／2／3／4／5 的全部基线已实跑**（见各条括注）：`作品体量｜单章字数` 两件各 **0**／`十四组` 六件 **全 0**／`十二组` **10 行**（逐件 4・1・1・1・2・1）／`3000-4000` **4 处**／`标尺体量参照（100）` **0**／`[3]／[4]／[5] 段需` **0**／`加粗密度本批不实现` **1**／`非状态声明` 四件各 **0**／`残余在册` **0**／`091 复核判残余不存在` **1**／`G26｜G27` **0**——**本批无未预验项**。
3. **禁止事项写明**——见 §五。
4. **范围与期望同源**——断言 2 第二命令的期望（全 0）与基线（10 行）取自**同一件同一命令**；断言 3 的期望与基线同源（4 处）；**断言 5 第一命令的匹配模式经预跑订正**（方括号 token 须 `-F`，见该条注）。

**附加项**：清单完备（F1–F5 与「字数的同类画像」这一设计意图逐条对齐；**F6–F10 为作者 2026-09-16「scripts 都看看」触发的一轮工具面收口**——F6 依赖声明补漏／F7 批次时态／F8 批次注释两分／F9 台账订正／F10 补登欠账）；**禁改面与改动面对账**——§五 列举的件若同时出现在 §二，已在该处明示豁免（**本批新触 `scripts/` 四件与 `docs/standards/测试与验收标准.md`，§五 已列明豁免**）。

## 五、禁止事项

- **禁改**：`docs/specs/施工机制.md`、`AGENTS.md`、`docs/specs/archive/` 内任何件。
- **禁改**（豁免外内容）：`scripts/` 下**仅** F6（`check.sh:4`）／F7（`check_content.py:10`）／F8（四件文档头）所指处——**其余脚本内容一概不动**（尤其 `check.sh` 的段逻辑、`check_spec.py` 的判据实现）；`docs/standards/测试与验收标准.md` **仅** F9（`G20` 行）／F10（§4 表追加两行）所指处，其余逐字不动。
- **禁改**：`skills/abzu-outline/SKILL.md` 与 `skills/abzu-outline/references/outline-workflow.md` 一概不动。
- **禁**：**删除**那 4 处平台常量（本批是**降级为兜底**，不是删除——无参考时仍要用）。
- **禁**：把新两组（⑬⑭）**改为判定对象**（它们是参照类——「本书比同类长」不是错误）。
- **禁**：动子集处的「十组」（量级／判定口径，与 098 同规）。
- **禁**：把 F8 的「出处型」批次引用**逐处改写**（只加口径句，原文保留——见 §六 否决项）。
- **禁**：联网、采集、跑安装器；禁改任何断言的期望值求绿（红线 4）。

## 六、被否决的替代方案

- **把 `体量`（本书总字数）也搬进标尺** → 否决：标尺讲**同类**，本书的体量是**作者的商业承诺**，主体不同（此为规划方前两轮误答的根因——只核「有没有」，未分「主体是谁」）。
- **只采「单章字数」不采「作品体量」**（最小可行版）→ 本批**不采**：两组同源同机械（都从档案统计），分两次做要多动一轮命名面（十二组→十三组→十四组），一次到位更省。
- **保留手写平台常量作主来源、标尺仅作旁证** → 否决：手写表会过时（本轮检索已见「起点 3000-4000」与实践口径不一致，见 §八），实测能自然跟随。
- **把新两组并入「决策参照两组」** → 否决：⑪⑫ 是**形态选项**、⑬⑭ 是**体量数值**，语义不同族，各立一组更清楚。
- **F8 改为「全量改写 55 处批次引用为指向式」** → 否决：规划方实测该计数**不可靠**（正则粗测的「状态型」大量命中系运行时状态与示例串），且**出处型占多数、承载「代码之所以如此」的理由**——全量改写会损技术承载，而「会过时」的风险**由口径句消解**（声明为历史出处即不可能失真）。**只改状态型 ＋ 加一句口径**。

## 七、本批核销（来源：`099` §八 5／6）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `099` §八 5 | `每章字数`／`体量` 的定位待裁 | **本批处置** | F4（每章字数改实测取；体量仍留规格作承诺与推导锚）；`099` 的建议「卸掉推导职责」经**实践核证后修正**——体量**仍是锚**（真实工序＝总字数反推卷数/章数），故不卸 |
| `099` §八 6 | 大纲阶段的 agent 分工（架构级） | **仍待** | 触发＝架构批（本批不涉 agent 面） |

- 汇总（恒列五档、无该档计 0）：`已履行 0｜作废 0｜本批处置 1｜仍待 1｜待作者 0 ＝ 2` ✓

## 八、开放项

1. **平台常量数据的时效性**：`起点 3000-4000`（4 处）与检索到的实践口径（起点下限 2000、日更 4000 拆 2 章）**不一致**；本批只**改其地位**（降级为兜底），**未核其数值**——触发＝本次改动后由作者或后续批次以平台官方规则复核（**本仓「数字须有依据强度」纪律的适用**）。**注**：该检索未返回可引链接，故不据以改数。
2. **「每卷字数」字段缺失**：真实实践里卷纲层的核心量（每卷 25–35 万字），本仓**无此字段**；触发＝卷纲域建设批。
3. **本批改动面（10 项／11 件）**：若施工期出现 P0 族 ≥2，按 `施工机制` §五 止损条收窄复核面。
4. **`check_spec.py` 的内联批次引用未逐处列举**：F8 的范围判据交给施工方现场列举分类（规划方**未**提供处数——粗测不可靠）。若施工期发现「状态型」数量远超预期（>10 处），**停摆申报**再定是否扩面。

## 九、审查记录

- **决策沿革（作者与规划方，2026-09-15 会话内）**：作者三次追问「字数能不能挪进参考标尺」——① 第一次（卷数与字数是否都放标尺）→ 规划方答「只有卷数能进」（属实）；② 第二次（每章字数／总字数要不要按标尺校准）→ 规划方答「标尺里没有这两项、无处可放」（属实但**未答「该不该有」**）；③ 第三次（人类作家会不会提前想好字数）→ 核证**真实工序**：总字数在**总纲层**定并**反推**卷数/章数（体量**是锚**，非闲字段）；每章字数是**平台规约＋更新节奏**的产物（日更 4000＝2×2000；单章 2000 ≈ 7 分钟阅读）；字数规划**分三层**（总纲／分卷／章纲）。→ 结论：**能进标尺，且每章字数的平台常量本应由实测替代**；本批即其落地。
- **规划方自查（记账）**：前两轮答复只核「标尺里现在有没有」，未先分「**主体是谁**」（标尺＝同类画像／规格＝本书承诺）——导致连续两轮答非所问；第三轮补上主体判别后结论翻转。此型（**答「有没有」而不答「该不该有」**）登记为自查记录。
- **待填**：成稿审查（独立上下文只读 subagent）／施工期偏差申报／核验三步／双维产物审查。
