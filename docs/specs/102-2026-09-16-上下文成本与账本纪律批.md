# 102-2026-09-16-上下文成本与账本纪律批

> 定位：处置流程成本实测报告的**零防错损失档**——把「点名装载」与「只回吐摘要」两条纪律从「施工」扩到覆盖全位，把「批式实跑」改为三列可引，收口审查记录写法（节标题／档位词／N 口径）并新增机械闸（检查 J），并按 DASH 补 `#28` 第四半句。
> 路径判定：**标准路径**——跨 `施工机制`／两份标准件／`check_spec.py`／`check.sh`／`CHANGELOG`，动规则条文与守卫实现。真机走查：本批不动 `skills/` 资产、无用户可观测行为改动，**豁免**（理由见 §五）。
> 自由度分布：[A]×4 [B]×8 共 12 行
> 必读输入：本规格 §一；`docs/specs/施工机制.md` §三（`:56`–`:68`）／§五（`:95`–`:104`）／§六（`:116`–`:160`）／§八（`:257`–`:277`）；`docs/standards/测试与验收标准.md` §1.1／§三／§4；`scripts/check_spec.py` 的 `NEVER_SCANNED`／`_section()`／`check_anchor()` 附近；`scripts/check.sh` 头注与 `[4]` 段 echo；`AGENTS.md` §七
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-16）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 成本实测：每批**强制角色交互 8 次**（标准路径；其中 4 次为作者确认，零 token），而 088 实测 **205 次工具往返／~23.5 M token** ⇒ 单次往返 ≈114,634 token（**≈ 整段常驻面重发**）。测试面：审查轮次 **1.95 轮/件**（设计最小 2）＝流程被严格执行、无超发 | 报告实测，两路独立收敛 |
| ② | 常驻面＝**79,601 字符／13 件**（`AGENTS.md` 2,708 ＋ `施工机制` 17,114 ＋ 11 标准件 59,779）；`施工机制` 内 **§六 5,546 字符＝32.4%**（其中反模式清单 **4,789＝28.0%**），而 **§五 审查仅 1,685＝9.8%** | `python -c` 围栏感知分节，字符口径 |
| ③ | `施工机制:64` 「实跑纪律（批式）」现文为**一段**，含三条内容（**规则**＋**理由**＋**适用范围**）；适用面已含施工方／规划方／审查方三处 | 逐行 read |
| ④ | `施工机制:240` 立有「必读输入按『文件:节』点名（不得只写整件）」与「交底与施工默认**只读点名节**，未点名的节不读」——**射程只写"交底与施工"**，不含核验与审查 | 逐行 read |
| ⑤ | `施工机制:260`–`:266` 的【核验请求】模板**五项**，无「本次工具往返数」；规划方取回报时无成本可见面 | 逐行 read |
| ⑥ | `施工机制:99` 的报告纪律只辖「计数／行数／条数类发现须附实测命令与输出」，**不辖审查记录节自身的写法**（节标题／档位词／N 口径） | 逐行 read |
| ⑦ | 归档 41 件有审查记录节者：**节标题归一为 §九者 33 件**，另 **9 件不在 §九**（`061`–`065` 五件编 §八、`059`／`060` 两件编 §七、`071` 编 §十、`060` 附件无该节） | 正则逐件实测 |
| ⑧ | **4 件审查记录实质未填**：`091`／`095`／`096`／`097` 该节内无任何 `findings 共` 数字（另有 7 件的「待填」出现在非审查记录处，**不属此列**） | 节内正则实测 |
| ⑨ | **P3 是无立法档位**：`施工机制:118`–`:120` 分级表只定义 P0／P1／P2，而归档 **6 件**用过 P3（`075`×11、`076`、`077`、`078`、`082`、`083`） | 逐件计数实测 |
| ⑩ | `archive/087` 同一节三行 N 口径自相矛盾：首行 `N=15` **把「观察 1」计入**，另两行 `N=14`／`N=10` **不计入**（观察 6／7 另列） | 逐行 read |
| ⑪ | `施工机制:160` 的 `#28` 现为**三半句**（实测命令＋单位／同源同口径／换工具换基线先判口径），**无"面判据"半句** | 逐行 read |
| ⑫ | 报告体量口径错位实例：同一「常驻面」在不同量法中件集不同——本次核算中一度把 `docs/specs/archive/` 42 件计入常驻面，得 187,385 字符，而口径正确的常驻面 13 件为 79,601 字符 | 两次实测对照 |

## 二、文件级改动清单

### 面一·`docs/specs/施工机制.md`（F1–F7）

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md:64` | **拆段**：现有整段拆为三行——① **规则行**只留「断言与门禁的实跑一律**批式**——断言用一个脚本一次跑完并一次输出（每行三列：命令／实测／期望）；门禁命令取尾（`bash scripts/check.sh > .tmp/<任务>/chk.out 2>&1; tail -5`）；大输出先 `head`／`tail` 收敛。」② **理由行**（独立列表项）：「**理由**：每次工具往返都要重发整段累积上下文，逐条单跑是批内成本的主要来源（088 实测 205 次往返／~23.5 M token）。」③ **适用范围行**照原文保留，末补一句：「**本节三条同辖覆盖率面与回报面**——审查方与核验方的抽样实跑、以及任何『列出所读文件与所跑命令』的回报，同按本纪律。」 | [B] | 锚点①；成本报告杠杆 2（往返数是唯一与常驻面规模无关的乘数） |
| F2 | `施工机制:240` | 末句「装载量是每个 subagent 的成本主项」**之后**补：「**本条同辖审查与核验位**——成稿审查／产物审查／单维核对的交底同按『文件:节』点名，未点名的节不读；审查方产出须含『**所读文件清单**』（读了哪些件与节），供规划方核装载面与成本。」 | [B] | 锚点④ |
| F3 | `施工机制` §八【核验请求】模板（`:260`–`:266`） | 模板项**五 → 六**，新增一项（置于「自核验结果」之后、偏差记录之前）：「- 本次工具往返数：{N}（自述；核验期由规划方核对，**非置红项**）」；并在 `:275` 交底必带七项第 5 项「输出要求」末补「含本次工具往返数」 | [A] | 锚点⑤；成本报告杠杆 2 的"可测化"落法 |
| F4 | `施工机制` §五（`:104` 后，`:105`「报告落点」之前） | 新增一条**审查记录写法三则**：「- **审查记录写法三则**：① 节标题固定为『九、审查记录』；② 档位词取值只认 §六 分级表的 P0／P1／P2（表外档位词如 P3 不得使用；确需新档位须先经作者裁定并同批回写分级表）；③ 同时给出 findings 总数 N 与各档位数时，**N ＝ 各档位之和**，观察项**不计入 N**、另列。」 | [B] | 锚点⑦⑧⑨⑩（三则分别对应三种实测偏差；**不新增 P3 档位**——不加级，只收口写法） |
| F5 | `施工机制` §三 过程产物两项（`:66`–`:69`） | 在「2. `CHANGELOG`…」之后补第三句：「**审查记录须同批落当批规格的『九、审查记录』节**——不得留占位符（`091`／`095`／`096`／`097` 四件实测未填，属已发生偏差）。」 | [B] | 锚点⑧；`#9` 族（纪律靠自觉不靠机制）的当日实例 |
| F6 | `施工机制` §六 反模式 `#28`（`:160`） | **事故出处列**补记本批两个实例：「**＋ 2026-09-16 外审三轮报告复核**：④『常驻面』件集口径未声明（13 件 79,601 字符 vs 误含归档 42 件的 187,385 字符）；⑤ `git add -N` 基线用 `grep -c` 行数口径记作 1、实为出现 2 次。」**正确方式列**补第四半句：「**凡称『常驻面／归档件／守卫面』者，须同时给件集判据（glob 或清单），否则量出的数不可比。**」 | [A] | 锚点⑪⑫；**第四半句属规则扩写，正文新增须作者确认**（本规格即确认载体） |
| F7 | `施工机制` §六 已知缺口（`:163` 表） | 新增一行：「\| 协2 \| 归档件的审查记录节标题编号不统一（**9/41 件**不在 §九：`059`／`060` 编 §七、`061`–`065` 编 §八、`071` 编 §十、`060` 附件无该节）——**归档只读，不回溯改**；F4① 已立规，新批按规落 \| 出现第三处编号形态时评估是否加机械闸 \|」 | [B] | 锚点⑦；`施工机制` §七 归档件只读纪律 |

### 面二·机械闸（F8–F10）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F8 | `docs/standards/测试与验收标准.md` §4 缺口表 | 新增 **G28**：「在制规格的『九、审查记录』节存在性（缺失或留占位符即报候选）｜触发＝检查 J 上线后首个漏检实例」 | [A] | 锚点⑧；`施工机制` §四 落点分工（机检类缺口落 §4） |
| F9 | `scripts/check_spec.py` | 新增**检查 J**（`check_archive_record()`）：对每个在制规格，定位审查记录节（`^## [一二三四五六七八九十]+、[^\n]*审查记录`）——① 无该节 → 报候选；② 该节内无任何 `findings 共` 匹配 → 报候选「留占位符」；③ 该节内同时含总数 N 与 P0／P1／P2 计数时做 N ＝ 之和对账，不符报候选。**口径同 E／F／G／H／I：候选只呈报、不置退出码**；同步更新模块 docstring 的用途句与「检查 A–J 十类」枚举、维护入口行 | [B] | 锚点⑧⑨⑩；`施工机制` §四 守卫变更回写清单 |
| F10 | `scripts/check.sh` 头注（`:10`）＋ `[4]` 段 echo（`:138`） | 两处 `[4]` 段枚举**末补**「／检查J 审查记录核对」，与 `check_spec.py` 头注同口径；**段序与段数不变** | [A] | `施工机制` §四 段序真源 |

### 面三·回写面（F11）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F11 | `docs/standards/测试与验收标准.md` §1.1 `[4]` 行与 §三 机检锚点 `[4]` 行 | 两处枚举末补「／审查记录核对（检查 J）」 | [B] | `施工机制` §四 守卫变更回写清单 ①⑤ |

### 面四·过程产物（F12）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F12 | `CHANGELOG.md` | 一行过程产物：条首串「上下文成本与账本纪律（102）」，列 0、以连字符加空格起首、≤400 字符 | [B] | `施工机制` §三 |

## 三、验收断言（基线全部 2026-09-16 实跑）

### 断言 1｜批式纪律拆段与可引（F1）

```bash
grep -c '本节三条同辖覆盖率面与回报面' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '^\*\*理由\*\*：每次工具往返都要重发整段累积上下文' docs/specs/施工机制.md
```

期望 **1**（**计行**：理由行行首为加粗「理由」）。基线 **0**（现文为「**理由**：…」嵌在同一段中部，非行首）。

```bash
grep -c '逐条单跑是批内成本的主要来源' docs/specs/施工机制.md
```

期望 **1**（**计行**：拆段后理由容仍在原段内出现一次）。基线 **1**——**本断言为负控**（该行原有一次，拆段后计数不变，用于确认「理由」内容未被拆丢）。

### 断言 2｜点名装载扩面与装卸面（F2/F3）

```bash
grep -c '本条同辖审查与核验位' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '本次工具往返数' docs/specs/施工机制.md
```

期望 **2**（模板新增项 ＋ 交底第 5 项各 1；**计行**）。基线 **0**。

```bash
grep -c '所读文件清单' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 3｜审查记录三则与同批落（F4/F5）

```bash
grep -c '审查记录写法三则' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '不得留占位符' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '档位词取值只认' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 4｜#28 第四半句（F6）

```bash
python -c "print(open('docs/specs/施工机制.md',encoding='utf-8').read().count('须同时给件集判据'))"
```

期望 **1**（**计出现次数**，与 F6 语义同源）。基线 **0**。

```bash
grep -c '^| 28 |' docs/specs/施工机制.md
```

期望 **1**（**计行**；`#28` 行形态实测为无粗体 `| 28 |`）。基线 **1**（负控：不得因补记而新开一行）。

### 断言 5｜检查 J 与回写面（F8–F11）

```bash
grep -c 'def check_archive' scripts/check_spec.py
```

期望 **1**。基线 **0**。

```bash
grep -c '审查记录核对' scripts/check_spec.py scripts/check.sh docs/standards/测试与验收标准.md
```

本命令为**多文件合扫**，`grep -c` **逐件输出**、须以**总命中**判读：期望 **6**（`check_spec.py`：docstring 用途句 ＋ 「检查 A–J」枚举 ＝ 2｜`check.sh`：头注 `:10` ＋ `[4]` 段 echo ＝ 2｜`测试与验收标准`：§1.1 `[4]` 行 ＋ §三 机注 `[4]` 行 ＝ 2）。基线 **3 件均 0（总 0）**。

```bash
grep -c 'G28' docs/standards/测试与验收标准.md
```

期望 **≥1**。基线 **0**。

```bash
grep -c '协2' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 6｜门禁与过程产物（F12）

```bash
bash scripts/check.sh > .tmp/v.txt 2>&1; echo $?; tail -3 .tmp/v.txt; rm -f .tmp/v.txt
```

期望 **退出 0**，末三行含「检查全绿」。基线 **退出 0**（改动前）。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 十二条逐条实跑——① 往返/轮次数取自成本报告实测（并已用两条独立数据点交叉核对）；② 常驻面 79,601 字符与 `施工机制` 17,114 字符用围栏感知 `python -c` 实测；③④⑤⑥⑪ 逐行 read 取原文；⑦ 41 件节标题编号用正则逐件实测；⑧ 4 件未填用「节内无 `findings 共`」判据实测（与另 7 件的非审查记录占位符区分）；⑨ P3 逐件计数实测（6 件）；⑩ 逐行 read；⑫ 两次实测对照。**无未预验项**。
2. **断言实跑过**：§三 六组断言的**改前基线**已逐条实跑（含 `| 28 |` 形态实测为无粗体、`grep -c` 与 `count()` 两种口径的分别声明）。**断言 5 第二条为多项合扫，施工期按逐件实测订正期望**（该条已标「以实测基线为准」，防 `#28` 的口径错位）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：§三 各断言的期望值均取自本规格 §二 点名文件的范围；断言 4 第二条的基线 1 由 `grep -c '^| 28 |' docs/specs/施工机制.md` 在**同一文件同一范围**实跑得出。

## 五、禁止事项

- 禁止触碰：`docs/specs/archive/` **全部**（归档只读——本批 D 三条偏差**不回溯改归档**，只立规＋记缺口）；`docs/product/` 全部；`skills/` 全部；`scripts/check_content.py`；`scripts/patch_file.py`；`AGENTS.md`；`docs/standards/` 中除 `测试与验收标准` 外的十件。
- 禁止动作：联网采集；新增**第四档** P3 档位（本批只收口写法，不加级）；改 `[4]` 段段序或段数；改任何既有检查（A–I）的判据；把新检查 J 接成置红。
- 真机走查**豁免**：本批改动面全为开发侧（流程条文、守卫实现、缺口表），**无用户可观测行为改动**，按 `施工机制` §二 重路径行豁免判据不跑。

## 六、被否决的替代方案

- **拆 `施工机制` §六／§八 为冷路径子件**（可省 ~44% 热路径字符） → 暂不采用：会把"协作单点权威"变成两件，涉权威结构变更，需作者单独立项裁定。**记 §八 开放项 1**。
- **反模式表中 8 条"已自述机械化"项改指针**（可省 ~1,370 字符） → 暂不采用：反模式表是**事故账本**，删条＝丢事故出处；需作者逐条裁定。**记 §八 开放项 2**。
- **新增 P3 档位**（使 `:118`–`:120` 表与 6 件实况一致） → 否决：扩分级体系属新机制，撞 `AGENTS.md` §四 内生必要性自问；改用 F4② 收口写法（表外档位词不得使用）即可消歧。
- **取消或降级独立产物审查**（成本报告杠杆 3） → 否决：`施工机制:91` 有自家实证「规划方三轮自审未发现、独立上下文首跑即报」，砍它＝回到自审失守（`#25`）。**记 §八 开放项 3**（列为待作者裁定的高风险档）。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `101` §八 | 检查 I 截断计数潜伏缺陷（`cands[:15]` 后取 `len`） | 已履行 | `9394a15` 已修（批 101 施工期处置） |
| `101` §八 | `_anchor_norm` 不剥 CJK 引号（目标件带「」时落空） | 仍待 | 触发＝下个实例；本批不动 `check_spec.py` 的锚点判据（F9 只新增 J，不改 A–I） |
| `101` §八 | 裸点名 16 处清单 | 已履行 | 批 101 已落（F15） |
| `100` §八 | `BAN_EXCUSE_MARKS` 豁免词无「豁免」 | 仍待 | 触发＝下次触碰该判据面（本批不触） |
| `099` §八 | `读者避雷点`／两倾向字段收口 | 已履行 | 批 099／098 已落 |
| `085` §八 | 结构组提前照等两条 | 仍待 | 触发＝首批实机走查发现 3.5 返工成本过高 |

- 汇总：`按条计 6 条：已履行 3｜作废 0｜本批处置 0｜仍待 3｜待作者 0 ＝ 6`

## 八、开放项

1. **`施工机制` 冷热路径拆分**（§六 反模式清单 4,789 字符＝全文 28.0%；§八 模板区 2,731＝16.0%）——落点＝作者裁定后单开一批；扣此两条收益：热路径 17,114 → 约 9,596 字符（**-44%**，估）。触发＝作者裁定。
2. **反模式表 8 条已机械化项改指针**——落点＝作者逐条裁定；可省 ~1,370 字符（估）。触发＝同上。
3. **审查面成本策略**（P2 占 findings 53.7%、P0 仅 10.3%；是否把纯 P2 类并入下一批处置）——落点＝作者裁定；**风险高**（涉唯一有效外部性来源）。触发＝作者裁定。
4. **`施工机制:240` 自述"088 实测定名十件共 190 KB"未核**——该数与本批常驻面口径（13 件 79,601 字符）件集不同、不可直接比。触发＝下次触碰该行时校准。
5. **`patch_file.py` 形态闸读取线程解码错**（**本批实测**）：以 `--batch` 落盘时 `stderr` 抛 `UnicodeDecodeError: 'gbk' codec can't decode byte 0xac`（`subprocess._readerthread` → `fh.read()`），**不影响四段式判定**（本批 rc=0、汇总「全部通过」），但**形态闸的 lint 输出被吞、诊断信息丢失**——`patch_file.py` 的「跳过不静默」体例在此失效。触发＝下次触碰 `_shape_gate()` 时改 `encoding='utf-8', errors='replace'`（与 `check_spec.py` 的 `_read_target()` 同口径）。**落点候选＝`测试与验收标准` §4 新 G 行**（本批不认领）。

## 九、审查记录

- 2026-09-16｜**外审预核（批式实跑）**：§一 十二条锚点与 §三 六组断言基线全部实跑；其中 8 条与外部成本报告的原始表述**不一致**，经复测后按实测订正（详见下条）。
- 2026-09-16｜**预核订正记录**：① 「4 件审查记录未填」——外部报告对（`091`／`095`／`096`／`097`），但其表述「`待填` 出现于 3 件」不完整（实测 `待填` 出现在 11 件，其中 7 件为非审查记录用途）；② 「P3 使用 5 处」→ 实测 **6 件**（外部报告漏 `077`）；③ 「8 件编到别处」→ 实测 **9 件不在 §九**（另含 `060` 附件无该节）；④ `check_spec.py` 报告 1622 行 vs 规格撰写前 1612 行——差 10 行系 `fc11fa1` 净增（报告值为准）；⑤ 「常驻面」件集口径错位由本规格撰写期自身踩中（锚点⑫），已据 `#28` 补第四半句（F6）。
