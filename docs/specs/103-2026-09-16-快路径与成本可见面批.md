# 103-2026-09-16-快路径与成本可见面批

> 定位：**给流程开一条快通道并装上成本账**（15 项；含施工期实测后**撤销用词闸** 的 F14）——新增第四条分流行「快路径」（判据封闭的改动免两道审查）；把每批的工具往返数与周期时长记入规格审查记录与 `CHANGELOG`；并把 `术语标准` §1 既有的「通用词优先／生造词须就地定义」两条由「待建」落成候选机械闸。
> 路径判定：**标准路径**——跨 `施工机制`／两份标准件／`check_content.py`／`CHANGELOG`，改规则条文与守卫实现。真机走查：本批改动面全为开发侧（流程条文、标准条文、守卫实现），**无用户可观测行为改动**，按 `施工机制` §二 重路径行豁免判据**豁免**。
> 自由度分布：[A]×8 [B]×7 共 15 行
> 必读输入：`docs/specs/施工机制.md` 的「二、规格流程」／「三、施工与验收」／「五、审查」／「七、提交与登记」四节；`docs/standards/术语标准.md` §1／§3／§三；`docs/standards/规格写作标准.md` §1 判定表；`docs/standards/测试与验收标准.md` §1.1／§三／§4；`scripts/check_content.py` 的 `_checks()`／常量区／模块 docstring
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-16）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `施工机制` §二 现为**三条路径**分流表（轻／标准／重）：轻＝单文件 <5 行错别字或治理微调 ≤3 文件（**不写规格**）；标准＝跨文件／增删文件／改规则（**写规格 ＋ 成稿审查 ＋ 核验 ＋ 条件产物审查**）；重＝动 `skills/` 等（标准 ＋ 产物审查 ＋ 真机走查） | 逐行 read（`:33`–`:46`） |
| ② | **无「有规格但免审查」的通道**：任何写规格的批次一律走成稿审查（§五「一律送审，无逃逸」） | 逐行 read（§二 表 `:39`–`:41`／§五 `:106`） |
| ③ | 成本可见面现仅**一项且不在账本**：`102` 批已把「本次工具往返数」写进【核验请求】模板（施工方自述），但**规格审查记录与 `CHANGELOG` 均无此数**，跨批不可比 | 逐行 read（`施工机制` §八 模板区） |
| ④ | `施工机制` 现 **286 行 / 18,035 字符**，每批四个上下文各读一遍；其中 §六 反模式区与 §八 模板区为冷路径 | `python -c` 字符口径实测 |
| ⑤ | 反模式表 **28 条**中，正文引用守卫/工具的仅 **8 条**（逐行字符合计 1,926）；改指针净省约 **640 字符＝全文 3.6%**——**收益低于动事故账本的风险，故本批不改**（见 §六） | 逐条实测（正则判「引用 check\*.py／check.sh／patch_file／检查 A–J／MD0／守卫／形态闸」） |
| ⑥ | `术语标准:12` 已立「术语优先取既有的通用词（预训练里已有的说法能免费招募先验）……生造词须在同一处给出定义……落笔前先问一句「通用词能否替代」」 | 逐行 read |
| ⑦ | 同件 §3 词条表**9 条**（`subagent`／skill 资产／仓库治理文档／调用双层／作者／候选类发现／红基线／负向流程守卫／维护出处）；§1 另有指向 `术语表` 的表 | 逐行 read（`:21`–`:31`） |
| ⑧ | 同件 **§三 机检锚点表 5 行全为「待建」**，其中第一、二行分别是「§1 术语一致（无第二种叫法）」「§1 首现术语有定义或指向本标准」——**本批正是给这两条装上闸** | 逐行 read（`:48`–`:49`） |
| ⑨ | `check_content.py` 现有**五项**检查（`_checks()` 返回五项：①引用闭合 ②TOC ③行数与条目限长 ④`CHANGELOG` 条目限长 ⑤「维护出处」标注）；模块 docstring 与 `--help` 文案均写「五项」；`GOV_MENTION`／`MAINT_MARK`／`_gov_names()` 等常量与辅助函数可直接复用 | 逐行 read |
| ⑩ | 规格模板「九、审查记录」行现为：`- {日期}｜{成稿审查／产物审查}｜findings 共 {N}：P0:{a} P1:{b} P2:{c}｜结论 {通过／修订后通过／裁决建议}`——**无成本字段** | 逐行 read（`施工机制` §八 模板围栏块） |

## 二、文件级改动清单

### 面一·新增快路径（F1–F3）

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md` §二 分流表（`:37`–`:41`） | 表内**新增一行**，置于「轻」行之后、「标准」行之前；**路径名＝「快」**（与既有单字命名同族）。**触发条件**（三条须同时满足，缺一即回退）：① 改动面**全部是逐字替换**（每条改动都能写成「逐字：旧→新」，无「按方向自定」项）；② 验收断言**全部可机械判定**——判据形态＝**每条断言＝单条命令 ＋ 单个期望值**，不含「走查型」「判据为…」「语义通顺」类表述（**反例**：「真机走一遍该域主流程，判据＝走通」不满足本条）；③ **不涉 `skills/` 资产正文、不动产物契约字段与状态值枚举**。**流程**：写规格（现状锚点＋文件级改动清单＋验收断言）→ 按清单逐字替换 → `bash scripts/check.sh` 全绿 → 断言逐条复跑 → `CHANGELOG` 一行（**免成稿审查与产物审查**） | [B] | 锚点②；本批即增量实例（`102` 批 13 项中绝大多数满足三条） |
| F2 | 同件 §二 表下方说明（`:43` 判据来源行之后） | 补一条回退与守门规则：「**快路径的三条触发条件任一不成立即回退标准路径**——判据仅『像不像逐字』存疑时，取较重者（§二 前导语既有规则）。**混装批次按最重项定级**：一批内只要有一项不是逐字替换，全批走标准路径，不得拆项规避。**本句是 §二 前导语的批内细化、不取代之**（前导语管「整批属哪条路径」的不确定；本句管「批内混装」）。」 | [B] | 锚点①；`施工机制` §二 前导语「不确定属哪条时取较重的一条」 |
| F3 | 同件 §五「审查」表（成稿审查行）与 §二「标准」行流程列 | **同步口径**：§五 成稿审查行的「何时派」列补「（**快路径批次除外**）」；§二「标准」行流程列保持原样（标准路径仍须成稿审查）。**不得改动 §五 其他行**（产物审查行已于 `102` 批含轻路径例外，本批不动）。**另须同批补 §五「无裁量」条**（`:107`）：其「标准与重路径**一律送审**」后补「（**快路径除外**，见 §二）」——否则同节两处口径不同步 | [A] | 锚点②；防两处口径不一（`施工机制` §四 反模式 #4／#22 族） |

### 面二·成本可见面（F4–F5）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F4 | `docs/specs/施工机制.md` §八 规格模板「九、审查记录」行（锚点⑩） | 该行末补成本字段：`｜工具往返数 {R}｜周期时长 {T}`。**口径**：`R`＝本批规划方会话的工具调用总次数（含施工与审查 subagent 自述数之和，核验期由规划方汇总）；`T`＝本批首条改动至提交的墙钟时长（人工填写，量级即可：`<1h`／`1-4h`／`>4h`）。**快路径批次**该行写「（免审查）」＋同样的两个字段 | [A] | 锚点③；成本报告杠杆 2 的「可测化」落法 |
| F5 | `docs/specs/施工机制.md` §三 过程产物两项（`CHANGELOG` 项） | 补一句：「**`CHANGELOG` 条目末尾附成本註记**：`（往返 {R}／{T}）`——**`R` 与 `T` 须落实际数值**（如 `（往返 12／1-4h）`），不得照抄花括号占位；供跨批比较；不占条目限长豁免（仍受 ≤400 字符约束）。」 | [B] | 锚点③；`版本与分发标准` §3 条目限长 |

### 面三·用词收口（F6–F9）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F6 | `docs/standards/术语标准.md` §三 机检锚点表（`:48`–`:49`） | 两行**状态列**由「待建」改「**已实现**（`check_content.py` 候选 ⑥）」；「强制方式」列分别改述为「内容轨检查器（候选 ⑥：全仓仅 1–2 次的 2–4 字中文词组 ↔ 术语表／本标准 §3 词条比对）」 | [A] | 锚点⑧；守卫变更回写纪律（§四） |
| F7 | `scripts/check_content.py`：新增 `_check_terms()` ＋ 常量 | 新增**候选检查 ⑥「疑似自造词」**：① 收集词条集＝`术语表` 全表首列 ＋ `术语标准` §3 词条表首列；② 候选面＝`docs/` 下 `.md`（**不含 `skills/`、不含 `docs/specs/archive/`、不含在制规格本身**——在制规格高频出现新词与自指 token，纳入必自噬）；③ 生成法＝逐行取**连续 2–4 个汉字**的词组，**须落在行内散文处**（跳过行内代码跨度与围栏内文本，复用 ① 的跨度／围栏判据形态）；④ 频次＝该词组**在与候选面同一范围**（`docs/` 活件面 ＋ 根 `*.md`，**排除 `archive/` 与在制规格**）的出现次数——**取词面与计数面同口径**（两口径不同源会同时抬高噪声与假绿）；⑤ 报候选＝频次 **≤2** 且**不在词条集**者；⑥ **去种子**：若某候选是另一候选的子串且两者频次相同，只报较长者（降噪）。**口径同 ①／②／⑤：候选只呈报、不置退出码** | [B] | 锚点⑥⑦⑨；`术语标准` §1 两条既有条文 |
| F8 | `scripts/check_content.py` 模块 docstring（`:6`–`:11`）与 `_checks()` docstring（`:284`）＋ `--help` 描述（`:300`） | 三处「五项检查」改「**六项检查**」，并在 ⑨ 项列 ⑥ 的判据与分档（候选类） | [A] | 锚点⑨；`施工机制` §四 守卫变更回写纪律（含工具自身 docstring） |
| F9 | `docs/standards/测试与验收标准.md` §1.1 `[3]` 行与 §三 机检锚点 `[3]` 行 | 两处枚举末补「／疑似自造词（候选 ⑥）」；**§4 缺口表不动**（本批不新增 G 行——⑥ 一上线即为候选类，无待建缺口） | [B] | `施工机制` §四 守卫变更回写清单 ①⑤ |

### 面四·连带面（F12–F13）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F12 | `docs/specs/施工机制.md` **三处**「三条路径」载体 | 三处同步改「**四条路径**」——`:10`（目录行）、`:33`（节标题）、`:35`（前导语首句「三条路径分流表」）。**仅改「三」为「四」，该三行其余文字一字不动** | [A] | 审查 P1-3；§四 反模式 `#4`／`#22` 族（同载体的枚举面须同批并改） |
| F13 | `scripts/check.sh:9`（`[3]` 段行枚举）＋ `scripts/check_content.py:284`（`_checks()` docstring 的「顺序＝检查 ①–⑤」） | 两处**各补 ⑥**：`check.sh:9` 段行枚举末补「／疑似自造词」；`_checks()` docstring 改「顺序＝检查 ①–⑥」。**段序与段数不变** | [A] | 审查 P2-8；§四 守卫变更回写清单 ①／④ |

### 面五·施工期订正（F14–F15）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F14 | `scripts/check_content.py`（摘除第六项检查全部代码面）＋ `scripts/check.sh:9` ＋ `docs/standards/术语标准.md:48`／`:49` ＋ `docs/standards/测试与验收标准.md:25`／`:118` ＋ `CHANGELOG.md:9` | **撤销用词闸（内容轨第六项「疑似自造词」）**：① 摘除 `check_content.py` 内该检查的全部代码（8 块共 170 行：7 个常量、9 个函数、`_checks()` 内调用与签名参数、`main()` 内「缺席即 return 2」块），docstring 与 `--help` 回「五项」；② 摘除四处悬挂引用（`check.sh:9` 段行、`术语标准` §三 两行回「待建」并记因、`测试与验收标准` §1.1／§三 枚举）；③ 删除运行期入参件 `scripts/check_content.extra`；④ `CHANGELOG` 条目据实重述 | [A] | **施工期实测**：判据「全仓频次＝1」在中文上近恒真（2–4 字滑窗词组全仓 75,055 个、频次 1 者 **54,316**；三种生成法最小量级仍 2,631），输出为「开发时／发时用／时用的」类重叠碎片，人工不可用；且其运行期入参件缺席会使**任何干净检出**的门禁恒红（红线 6 反例）。根因＝用确定性判据判需语义判断之事，违 `AGENTS.md` §五.1 |
| F15 | 本规格 §三 断言 1 第三项与断言 3 第五项 | 两项期望**按实测订正**：① 断言 1 第三项（`快路径批次除外` 的 `grep -c`）**期望 1 改 2**（P1-1 裁定「两处统一为同一字面」后必然两处：§五 成稿审查行 ＋ §五 无裁量条）；② 断言 3 第五项的期望**撤销并改归零型**（该件 §三 那一行枚举的是**守卫构成**、与 §1.1 的**检查明细**不同源，该 token 在该件实无处可补；且第六项检查已随 F14 撤销） | [A] | 施工方 P1-1／P1-2（均附实测命令与输出）；`规格写作标准` §5.3「计数必须实跑」 |

### 面六·过程产物（F10–F11）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F10 | `CHANGELOG.md` | 一行过程产物：条首串「快路径与成本可见面（103）」，列 0、以连字符加空格起首、≤400 字符，**末尾附成本註记**（本批 `（往返 {R}／{T}）`） | [B] | `施工机制` §三；F5 |
| F11 | 本规格 §九 审查记录 | 本批自己的审查记录行**按 F4 新格式**写（含工具往返数与周期时长），作为新格式的首个实例 | [B] | 锚点⑩；F4 |

## 三、验收断言（基线 2026-09-16 实跑）

### 断言 1｜快路径三处落文（F1–F3）

```bash
grep -c '^| 快 |' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '改动面\*\*全部是逐字替换\*\*' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '快路径批次除外' docs/specs/施工机制.md   # P1-1 裁定两处统一为同一字面 ⇒ 必然两处（§五 成稿审查行 ＋ §五 无裁量条）
```

期望 **1**。基线 **0**。

```bash
grep -c '混装批次按最重项定级' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 2｜成本可见面（F4/F5）

```bash
grep -c '工具往返数 {R}' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '往返 {R}／{T}' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 3｜用词闸（F6–F9）

```bash
grep -c 'def _check_terms' scripts/check_content.py
```

期望 **1**。基线 **0**。

```bash
grep -c '疑似自造词' scripts/check_content.py
```

期望 **≥2**（docstring 用途句 ＋ 函数 docstring；**计行**）。基线 **0**。

```bash
grep -c '六项检查' scripts/check_content.py
```

期望 **≥2**（模块 docstring `:6`「六项内容轨检查」＋ `--help` 描述 `:300`）。基线 **0**（现文为「五项内容轨检查」／「五项检查」——**判据 token 用「六项」而非「六项检查」**，因现文两处形态不同且 `:300` 不含「内容轨」三字）。

```bash
grep -c '术语表' scripts/check_content.py
```

期望 **≥1**（词条集采集处）。基线 **0**。

```bash
grep -c '疑似自造词' docs/standards/测试与验收标准.md   # 本项已撤销（F15）：第六项检查随 F14 撤销，期望 0（归零型）
```

期望 **≥2**（§1.1 `[3]` 行 ＋ §三 `[3]` 行）。基线 **0**。

```bash
grep -c '已实现（`check_content.py` 候选 ⑥）' docs/standards/术语标准.md
```

期望 **≥1**。基线 **0**。

### 断言 4｜闸自身有效性（**关键·防空转**）

```bash
python scripts/check_content.py --static > .tmp/103/terms.out 2>&1; grep -c '疑似自造词' .tmp/103/terms.out
```

期望 **≥1**（闸**必须报出至少一条**候选，证明判据非空转；纯"不报"可通过空实现蒙过）。基线 **不适用**（本批新增）。

```bash
printf '# 探针\n\n本地生造术语瘞蘡仅此一见。\n' > zz-terms-probe.md; python scripts/check_content.py --static > .tmp/103/terms.out 2>&1; rm -f zz-terms-probe.md; grep -c '瘞蘡' .tmp/103/terms.out
```

期望 **≥1**（**夹具正控**：`zz-terms-probe.md` 为仓根临时件、其中的「瘞蘡」全仓仅此一见且不在词条集内，故必报候选；跑完即删，**不留件**）。基线 **0**（无闸）。**不用既有语料做正控**——实测既有四例的频次与去子串判据均**不保证**报出（`面判据` 全仓 4 处/3 文件，且其同位置的「词面判据」为同频次子串，会被判据⑥去子串滤掉）。

### 断言 5｜门禁与过程产物（F10/F11）

```bash
bash scripts/check.sh > .tmp/103/chk.out 2>&1; echo $?; tail -3 .tmp/103/chk.out
```

期望 **退出 0**，末三行含「检查全绿」。基线 **退出 0**（改动前）。

```bash
grep -c '快路径与成本可见面（103）' CHANGELOG.md
```

期望 **1**。基线 **0**。

```bash
grep -c '往返 [0-9]' CHANGELOG.md
```

期望 **≥1**（F5 成本註记**落实际数值**后的形态，如 `往返 12`）。基线 **0**。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 十条逐条实跑——①② 逐行 read 分流表与 §五；③ 逐行 read 核验请求模板；④ `python -c` 字符口径；⑤ 反模式 28 条逐条正则判「引用守卫/工具」（8 条、1,926 字符）；⑥⑦⑧ 逐行 read `术语标准`；⑨ 逐行 read `check_content.py` 的 `_checks()`／常量区／docstring；⑩ 逐行 read 规格模板围栏块。**无未预验项**。
2. **断言实跑过**：§三 五组断言的**改前基线**已逐条实跑（断言 1–3 与本规格所写一致；断言 4 为**新增闸的有效性正控**，其基线标注「不适用」并给期望 ≥1 的理由——防"空实现通过"）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：§三 各断言的期望值均取自本规格 §二 点名文件与节的范围；断言 3 的 `grep -c '术语表'` 与断言 4 的 `.tmp/103/terms.out` 均在同一文件同一范围实跑。

## 五、禁止事项

- 禁止触碰：`docs/specs/archive/` **全部**（归档只读）；`docs/product/` 全部；`skills/` 全部；`AGENTS.md`；`scripts/patch_file.py`；`scripts/check_spec.py`；`docs/standards/` 中除 `术语标准` 与 `测试与验收标准` 外的九件（`skill形态标准`／`命名标准`／`文档写作标准`／`规格写作标准`／`产品契约标准`〔原 `产物契约标准`〕／`安全与合规标准`／`触发与交互标准`／`运行环境标准`／`版本与分发标准`）。**本批可改件＝恰三件：`docs/specs/施工机制.md`／`docs/standards/术语标准.md`／`docs/standards/测试与验收标准.md`（＋`scripts/check_content.py`／`CHANGELOG.md`）**。
- 禁止动作：联网采集；**改动 `施工机制` §六 反模式清单的任何一行**（本批明确不动，理由见 §六）；改 `[4]` 段或 `[2]` 段段序段数；把 ⑥ 检查接成置红；改既有 ①–⑤ 检查的判据；新增任何「台账／索引件」（`AGENTS.md` §四 平行台账不做）。
- 真机走查**豁免**：改动面全为开发侧，无用户可观测行为。

## 六、被否决的替代方案

- **反模式表 8 条「已机械化」项改指针**（原批准项） → **否决（本批不改）**：实测该 8 条逐行合计 1,926 字符，改指针后净省约 **640 字符＝全文 3.6%**，而代价是动**事故账本正文**（每条带真实事故出处，删条＝同类事故复发风险）。**记 §八 开放项 1**。
- **全面砍审查轮次**（原批准项） → **改以本批 F1 的快路径替代**：快路径是「按判据封闭精确免审」，比「一刀降 P2 轮次」可控且可回退；而全面降级有自家反证（`施工机制:91` 实测「规划方三轮自审未发现、独立上下文 subagent 首跑即报」）。**记 §八 开放项 2**（保留为待作者裁定的高风险档）。
- **⑥ 检查改用「未在 `术语表` 出现的所有中文短语」** → 否决：伪阳爆炸（普通名词全中），违反「候选类须可人工判读」的可用性底线。改用「频次 ≤2 ＋ 2–4 字 ＋ 去子串」三条件收窄。
- **⑥ 检查扫 `skills/` 与 `archive/`** → 否决：`archive/` 是历史件（归档只读、不回溯改），扫它只会产噪声；`skills/` 是运行侧、其用词受 `命名标准` §5 自包含约束管，另有口径。**两者均不在候选面**。
- **本批顺带拆 `施工机制` 冷热路径** → 否决（非否决其价值，而是**不进本批**）：那是结构性变更（把"协作单点权威"拆两件），须单独立批并给独立审查面。**记 §八 开放项 3**（收益最大：热路径可 -44%）。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `102` §八 1 | `施工机制` 冷热路径拆分 | 仍待 | 本批 §六 记不进批理由；触发＝作者裁定后单独立批 |
| `102` §八 2 | 反模式表 8 条改指针 | 仍待 | 本批实测净省 3.6%、判定不划算；触发＝作者裁定 |
| `102` §八 3 | 审查面成本策略 | **本批处置** | 以 F1 快路径替代（精确免审优于一刀降级）；全面降级仍待裁定 |
| `102` §八 4 | `施工机制:240` 「190 KB」口径未核 | 仍待 | 触发＝下次触碰该行 |
| `102` §八 5 | `patch_file.py` 形态闸解码错 | 已履行 | `496052e` 已修并验证 |
| `102` §八 6 | `git commit -- <pathspec>` 对 untracked 无效 | 仍待 | 触发＝下次触碰 §七 时补一行 |
| `102` §八 7 | `patch_file.py` 三条使用面观察 | 仍待 | 触发＝下次触碰该工具 |

- 汇总：`按条计 7 条：已履行 1｜作废 0｜本批处置 1｜仍待 5｜待作者 0 ＝ 7`

## 八、开放项

1. **反模式表 8 条改指针**——本批实测净省 3.6%，判定不划算。触发＝作者裁定。
2. **审查轮次全面降级**（P2 类并入下一批／只保 P0-P1）——**风险高**（涉唯一有效外部性）；本批已用快路径做精确替代。触发＝作者裁定。
3. **`施工机制` 冷热路径拆分**——§六 反模式区（约 5,500 字符）与 §八 模板区（约 2,700 字符）沉为子件；热路径 18,035 → 约 10,000 字符（**-44%**，估）。触发＝作者裁定后单独立批。
4. **`git commit -- <pathspec>` 对 untracked 无效**——触发＝下次触碰 §七 时补一行。
5. **`patch_file.py` 三条使用面观察**（`--set` 片段语义／`--insert-before` 黏连／回读明细截断 120 字符）——触发＝下次触碰该工具。
6. **`检查 G` 不认 `[C]` 档**（已在 `测试与验收标准` §4 登记为 **G29**）——**本批处置（`105` F1 已补 `[C]` 计数与不符判据；`105` §七 核销表同步记）**。
7. **（已撤销）内容轨第六项检查的候选量与阈校准**——该检查已随 F14 **整体撤销**（判据在中文上近恒真、输出不可用），本条随之作废；下列三组实测数为**留证**，供将来重考「用词收口」时参考。原实测：本批实测：全仓 2–4 字词组 75,055、频次＝1 者 **54,316**，故原「≤20 候选」判据**不可满足（方向相反）**：判据为「频次 ≤ 阈值」、阈值越低候选越少，而最小可能阈值（≤1）已是 54,316 条。**已改判据面**为「本批点名件内、全仓频次＝1 的 2–4 字词组；按件分组、每件上限 50 条；不设频次阈值、不设总量阈值」。**三组实测数（留证）**：① 全仓面（`docs/` ＋ 根 `*.md`，含归档 43 件，排在制规格 2 件）——唯一词组 209,458，不在词条集者 75,017；频次＝1 54,316（← 最小可能阈值）／＝2 64,951（原「≤2」值）／＝3 68,952／＝4 70,719／＝5 71,779／＝8 73,291／＝15 74,251；达 ≤20 需阈值 ≈300+。② 非重叠窗口切分——唯一词组 13,869，频次 ≤1 候选 10,894、≤2 → 12,537。③ 只取完整 2–4 字词块——唯一词组 3,747，频次 ≤1 候选 2,631、≤2 → 3,155。触发＝该闸跑过三批后评估每文件候选量是否可人工过目（目标：每件 ≤50 条）。

## 九、审查记录

- 2026-09-16｜**成稿审查**（独立上下文只读 subagent）｜findings 共 **10**：P0:1 P1:4 P2:5｜结论 **修订后通过**。处置：**P0-1 照准**——断言 3 第三项判据 token「六项检查」与现文不可同真（现文为「五项内容轨检查」／「五项检查」），已改判据为「六项」并写明两处落点形态；**P1 四条全采**（断言 4 正控改为**夹具法**——原判据「面判据全仓 1 次」实测为 4 处/3 文件故不成立；「三条路径」三处载体补为 **F12**；`规格写作标准` §1 判据来源面补句；断言 1 落盘字面钉死）；**P2 采四条**（F4 行号订正／F7 候选面与频次面**同口径**＋阈值改为「施工期实测取 ≤20 候选」／F8 补 `check.sh:9` 与 `_checks()` docstring 两处枚举为 **F13**／F5 钉死成本註记须落实际数值），**P2-9 不采**（`术语标准` §1 两处枚举——F6 所改正是其机检面，§1 条文本身无须改）。原「面判据真阳性」一项经复核**撤销**（不成立）。
- 2026-09-16｜**施工期订正（规划方裁定后落）**：**F14**——用词闸经施工期实测**撤销**（判据在中文上近恒真、输出为重叠碎片、运行期入参件致门禁恒红）；**同步修复一处阻断级缺陷**：`main()` 内「点名件清单缺席即 `return 2`」会使**任何干净检出**的门禁恒红，已随该检查一并摘除。**F15**——两条断言期望按实测订正。**另：F6–F9 四项均属 ⑥ 的落文面（术语闸状态／工具枚举／两件标准枚举），已随 F14 一并还原，故不另设撤销项**——F14 是本批唯一「做了什么又撤掉什么」的完整记录。施工方两轮共报 P0×1（判据无解，停摆申报、未自行定值）＋P1×2，**处置全部采纳，零断言被擅改**。
- 2026-09-16｜**施工期复核**（规划方裁定 ＋ 施工方 P0 停摆申报）｜findings 共 **4**：P0:1 P1:1 P2:2｜结论 **裁决后施工**。**P0-1（施工方报）**：F7 原判据「全仓面 ＋ 频次阈值」不可实现——校准规则要求「候选总数 ≤20」，而判据方向为「频次 ≤ 阈值」（阈值越低候选越少），实测最小可能阈值（≤1）已 54,316 条（实测数与三组口径见 §八 开放项 7）。**规划方裁定＝正确**，判据面改为「本批点名件内、全仓频次＝1」；**P1-1（施工方报）**：「快路径除外」两处措辞不同源，裁定统一为「（**快路径批次除外，见 §二**）」；**P2-1**：⑥ 的候选面入参落为临时件 `scripts/check_content.extra`（不入库、缺席即退出 2）；**P2-2**：F6 强制方式列不写「频次 1–2 次」（父裁定），本批落为「本批点名件内全仓仅此一见」。**审查期发现**：断言 1 第三项期望 1 与 P1-1 统一措辞后实得 2 不符（同一形态两处载体）、断言 3 第五项期望 ≥2 与 F9 事实一处载体不符——两项**如实登记、未据以改断言**，建议下批订正期望值。
- 2026-09-16｜工具往返数 106｜周期时长 1-4h（施工方自述；核验期由规划方核对）
