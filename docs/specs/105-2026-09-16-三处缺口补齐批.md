# 105-2026-09-16-三处缺口补齐批

> 定位：补上三处**已在册、触发已到**的缺口——检查 G 认 `[C]` 档（`G29`）、反模式 `#29` 挂机械判据、以及**新常态**（反模式 `#30`：把「只读一小段代码就断言此处缺某检查」记入台账）。**不含**任何结构搬动（原 `104` 的「移出两块」已移出本批——理由见 §八 开放项 1）。
> 路径判定：**应走标准路径**（原按「快」起手，属**定级失守**——详见下）。三条触发条件逐条核：①「改动面全部是逐字替换、无『按方向自定』项」**不成立**——§二 **F1 标 `[B]`**（按方向自定实现）；②「断言全部可机械判定」成立；③「不涉 `skills/` 资产正文与产物契约字段」成立。按 §二「**混装批次按最重项定级**」，整批应走标准路径。实际执行＝「写规格 → 改动 → `check.sh` 全绿 → 断言逐条复跑 → `CHANGELOG` 一行」（**免成稿审查与产物审查**——该豁免本批无资格取得）。**「快路径」自 `103` 立规以来的第一次实跑，首跑即暴露定级失守**：依据、后果与挂账见 §八 开放项 4、§九。
> 自由度分布：[A]×4 [B]×2 共 6 行
> 必读输入：`scripts/check_spec.py` 的 `check_freedom()` 全函数（含 docstring）与该函数内 `FREEDOM_*` 常量处；`docs/specs/施工机制.md` §六 反模式表末（`#29`／`#30` 行）；`docs/standards/测试与验收标准.md` §4 的 G29 行；`docs/specs/103-2026-09-16-快路径与成本可见面批.md` §二 F7（`G29` 的来历）
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-16）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `check_spec.py` 的 `FREEDOM_COUNT` 正则**已认 `[C]`**（字符类为 `[ABC]`），但 `check_freedom()` 的清单侧**只有 `c_a`／`c_b`／`c_mixed` 三个计数器**、`fmt()` 只输出 A／B／混合——故头注 `[C]×n` **解析已收录（进 `declared`）却未进 `fmt()` 与不符判据——即不参与对账**；该函数 docstring 明写「`[C]` 行不属 A／B 两侧，故由 `共 N 行` 兜住」（**明知而不判**） | 读码实测：正则所在行字符类含 C；三计数器处；docstring 该句 |
| ② | `测试与验收标准` §4 **G29 行**已在册（`104` 前身批登记），触发列写「下次触碰 `check_freedom()`／`FREEDOM_*` 常量时同批补 `[C]` 计数与不符判据」——**本批即该触发** | 逐行 read |
| ③ | `施工机制` §六 反模式 `#29` 的「正确方式」列**已写**「先跑 `check_spec.py --static` 的检查 G，再据其『清单』侧计数填头注」，「事故出处」列已写「三次均由检查 G 抓出」——**即判据早已存在且已在跑**；缺的只是**把它在行内显式标为「已机械化」**（表内其他已机化行有此标注体例） | 逐行 read `#29` 行全文 |
| ④ | `#30` 行**已落盘**（本次会话前置落，含 4 列同构）：错误做法＝只读一小段代码就断言「此处缺某检查」；事故出处＝`104` 批成稿审查 P0-3 | 逐行 read |
| ⑤ | 反模式表现 **30 行**（`#1`–`#30`）；表头注记已补「补实例前先自问是否应新开一行」与「理想终态＝每行写得出机械判据」两句 | 逐行 read |
| ⑥ | `CHANGELOG.md` 的 `## [Unreleased]` 段**无「105」条目**（改动前） | 改动前 §三 4b 基线 ＝ 0 |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py` 的 `check_freedom()` | **新增 `[C]` 档对账**：① 清单侧加计数器 `c_c`（行内含 `[C]` 且**不同时**含 `[A]`／`[B]` 者计入；**同时含 `[A]`／`[B]` 者按分支序只计前者，组合标记行不进 `[C]` 计数**，见锚点①）；② `fmt()` 输出串加一格 `[C]×c`；③ 不符判据把 `[C]` 一并比（头注缺写者按 `未写`、只比已给项，口径同现有四值）；④ 该函数 docstring 的「四种结构」与「由『共 N 行』兜住」两句**同步改述**（`[C]` 已进对账，不再是"由总行数兜住"）。**不新增任何常量**（正则已含 `C`） | [B] | 锚点①②；`测试与验收标准` §4 G29 |
| F2 | `docs/standards/测试与验收标准.md` §4 **G29 行的触发条件列** | 「触发＝下次触碰 `check_freedom()`／`FREEDOM_*` 常量时同批补 `[C]` 计数与不符判据」→「**本批处置**（`105` F1 已补 `[C]` 计数与不符判据）」——照同表 **G16 行**的「本批处置（…）」形态 | [A] | 锚点②；同表体例 |
| F3 | `docs/specs/施工机制.md` §六 反模式 `#29` 行的「正确方式」列 | 列末补半句：「**（已机械化：跑 `bash scripts/check.sh`，`检查 G 汇总` 报「不符」即命中本行）**」——与表内其他已机化行同构 | [A] | 锚点③ |
| F4 | `CHANGELOG.md`（`## [Unreleased]` 段） | 无「105」条目 → **新增过程产物一行**（列 0、以 `-` 加空格起首、≤400 字符；**条首串写死为「三处缺口补齐（105）」**；**子段写死为 `### Added`**——本批主体是加能力） | [B] | `施工机制` §三 过程产物两项；`100` F4（子段须写死，其成稿审查 P2-8）；`版本与分发标准` §3.2 |
| F5 | `docs/specs/103-2026-09-16-快路径与成本可见面批.md` §八 6 | 「触发＝下次触碰 `check_freedom()` 时补判据」→ 「**本批处置（`105` F1 已补 `[C]` 计数与不符判据；`105` §七 核销表同步记）**」——本批已消耗该触发，按 `施工机制` §三「本批内被处置的开放项须同批改标」（**产物审查 P1-2 追加**） | [A] | 产物审查 P1-2；`施工机制` §三；反模式 `#23` |
| F6 | `docs/standards/测试与验收标准.md` §4 表末（`G29` 行之后） | 新增一行 `G31`（4 格）：登记**组合标记行**的 `[C]` 计数不对称 ＋ 头注缺写 `[C]` 时不参与对账（**产物审查观察项 3／4 追加**） | [A] | 产物审查观察项 3／4；同表体例 |

## 三、验收断言（基线 2026-09-16 实跑）

### 断言 1｜`[C]` 进对账（F1）

```bash
grep -c 'c_c' scripts/check_spec.py
```

期望 **≥3**（计数器初始化 ＋ 递增 ＋ 入账各一；**计出现次数**）。基线 **0**。**注意不用 `grep -c 'FREEDOM_C'`**——它是 `FREEDOM_COUNT` 的前缀子串，改前即 3 命中，测不出本项。

```bash
grep -c '\[C\]×' scripts/check_spec.py
```

期望 **≥1**（`fmt()` 输出串新增的 `[C]×k` 一格）。基线 **0**。

### 断言 2｜判据真的会报（F1·**夹具正控，防"存在即可"**）

```bash
mkdir -p .tmp/105 && printf '# T\n\n> 自由度分布：[A]×1 [C]×9 共 2 行\n\n## 二、文件级改动清单\n\n| # | 文件:位置 | 改动 | 自由度 | 依据 |\n| --- | --- | --- | --- | --- |\n| F1 | a.md | x | [A] | y |\n| F2 | b.md | x | [C] | y |\n' > .tmp/105/fx.md && python scripts/check_spec.py --static .tmp/105/fx.md 2>&1 | grep -c '检查 G 头注「自由度分布」与改动清单不符'
```

期望 **1**（夹具：头注声明 `[C]×9` 而清单实为 1 行 `[C]` ⇒ 必须报不符）。**判据取候选行全文**：若用 `grep -c 不符`，会把汇总行「不符 1 项」一并计入（实测 2），测不出「候选行是否报出」（成稿审查 P2-1 抓出）。基线 **不适用**（本项新增）。**说明**：本断言是**有效性正控**——只证"存在 `c_c`"会被"写了但没接进判据"蒙过，故须证**判据会报**。夹具用完即删（红线 2）。

### 断言 3｜G29 状态与 `#29` 判据（F2/F3）

```bash
grep -c '本批处置（`105` F1 已补' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '已机械化：跑 `bash scripts/check.sh`，`检查 G 汇总`' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 4｜门禁与过程产物

```bash
bash scripts/check.sh > .tmp/105/chk.out 2>&1; echo $?; tail -3 .tmp/105/chk.out
```

期望 **退出 0**，末三行含「检查全绿」。基线 **退出 0**（改动前）。

```bash
grep -c '三处缺口补齐（105）' CHANGELOG.md
```

期望 **1**。基线 **0**。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 **六条**逐条实跑——① 读码（正则字符类含 C；三计数器处；docstring 该句）；② 逐行 read G29 行；③ 逐行 read `#29` 行；④ 逐行 read `#30` 行；⑤ 逐行 read 表头注记与末行号；⑥ read 改动前 `CHANGELOG` 无「105」条目。**无未预验项**。（锚点⑥ 系补列 F4 时为 §一 新增，本条初写「五条」未随之同步——成稿审查 **P1-2** 抓出，已改。）
2. **断言实跑过**：§三 各断言的**改前基线**已逐条实跑（`c_c`＝0、`[C]×`＝0、G29 串＝0、`#29` 机械串＝0、CHANGELOG 串＝0）。断言 2 为**夹具正控**，其夹具文本已在本规格内逐字给出（可照抄实跑）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：§三 各断言的期望值均取自 §二 点名文件与节的范围；断言 2 的夹具与判据在同一命令内自洽（声明 `[C]×9` vs 清单 1 行）。

## 五、禁止事项

- 禁止触碰：`docs/specs/archive/` **全部**；`docs/product/` 全部；`skills/` 全部；`AGENTS.md`；`docs/specs/施工机制.md` 的**除 `#29` 行外的任何文字**（尤其 §二 四条路径分流表与 §六 分级表）；`docs/standards/` 中除 `测试与验收标准` 外的十件；`scripts/patch_file.py`；`scripts/check_content.py`。
- 禁止动作：联网采集；把 `[C]` 判据接成**置红**（检查 G 属 `[4]` 段，静态发现一律只呈报）；新增任何常量（正则会话已含 `C`）；改 `[4]` 段段序或段数；改既有检查 A–J 的**其他**判据。
- 真机走查**豁免**：改动面全为开发侧，无用户可观测行为。
- **本批非快路径**（见头注路径判定）：**不免**成稿审查与产物审查。**成稿审查已补做**（结论见 §九）；**产物审查**按 `施工机制` §五 判据——本批动标准条文 ⇒ **须做**，已派**另一个**独立只读 subagent 补做（结论待回填 §九）。核验三步照走（改动面核对／断言独立重跑／内容抽查）。

## 六、被否决的替代方案

- **与本批同做「移出两块」**（原 `104` 的内容） → 否决：该动作的连带面（行号引用／指针／目录／编号／标题层级）在 `104` 成稿审查里**一次报出 5 条 P0、其中 4 条为规划方自身漏列**——说明**搬动结构的连带面扫描方法尚未成形**，不该在同一批里重做第二次。**记 §八 开放项 1**。
- **只断言 `c_c` 存在、不设夹具正控** → 否决：「写了但没接进判据」会被蒙过（这正是 `check_spec.py` 里 `:1526` 那处"收集了但差点没并入"的同族风险）。
- **给 `[C]` 单立常量 `FREEDOM_C`** → 否决：`FREEDOM_COUNT` 的正则字符类已含 `C`，新增常量只会再造一个"前缀子串"式的判据陷阱（`104` 成稿审查 P0-4 即此）。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `102` §八 5 残余 | `测试与验收标准` §4 新 G 行仍未登记，触发＝下次触碰 §4 | **本批处置** | F2 改该表 G29 行；另本批**不新增 G 行**（F1 属检查 G 的判据扩充，非新检查） |
| `103` §八 6 | `检查 G` 不认 `[C]` 档（已在 §4 登记为 `G29`） | **本批处置** | F1 ＋ F2 |
| `103` §八 5 | `patch_file.py` 三条使用面观察（触发＝下次触碰该工具） | 仍待 | 本批不触该工具实体（只在规格里引用其用法） |
| `103` §八 4 | `git commit -- <pathspec>` 对 untracked 无效 | 仍待 | 触发＝下次触碰 §七 |
| `103` §八 2 | 审查轮次全面降级 | 仍待 | 高风险；触发＝作者裁定 |
| `102` §八 4 | `施工机制:240`「190 KB」口径未核 | 仍待 | 原拟随"移出两块"一并核，该动作已移出本批（见开放项 1） |
| `archive/101` §八 2 与 9 | §4 表行号引用全扫／混合格残余（触发＝下次触碰该表） | 仍待 | 本批只改 G29 **一行**，不动全表；触发未满 |

- 汇总：`按条计 7 条：已履行 0｜作废 0｜本批处置 2｜仍待 5｜待作者 0 ＝ 7`

## 八、开放项

1. **「移出两块」的连带面扫描方法**——`104` 成稿审查实测：搬动 §六／§八 需改的件内指针**至少 7 处**（含 `:93`）、件外**至少 4 处**（含 `测试与验收标准:65`）、另需处理目录行／节标题／新增件回写 `命名标准` §2／新件 H1 结构（违 `文档写作标准:16` 即 lint 置红）。**在把这类扫描做成机械检查（或一份"搬动前必查清单"）之前，不再开搬动批**。触发＝该清单成形，或作者裁定直接做。
2. **反模式表的「机械判据」列**——30 行中仅约 9 行写得出可跑判据；表头注记已立「理想终态＝每行写得出判据」，但**未逐行补齐**。触发＝下次回填时按行评估。
3. **已归档规格里的教训是否已回填台账**——已派独立只读审计在查（42 件归档），结论待收。触发＝审计回报。
4. **「快／标准」路径定级的机械守卫**——本批实测：定级只认头注一句**自述**，**无任何检查核它**，故 `[B]` 项混在自称「快」的批里可一路绿灯（本批即此，见 §九）。**数据面一半在手、一半须加一次取数**：§二 各行自由度计数（`c_a`／`c_b`／`c_c`／`c_mixed`）检查 G 已有；但**「路径判定」行它并不读**——`FREEDOM_HEAD = re.compile(r'自由度分布')`（`scripts/check_spec.py:250`）只取「自由度分布」那一行。**本条初稿曾误称「检查 G 已读到头注整行」**，核 `:250` 常量后改正（同族＝`#30`：未经全件核对即断言某处「已有某能力」）。守卫＝**新增一次取数**（取含「路径判定」的行）＋一条判据：**该行第一个加粗路径词为「快」而清单含 `[B]` 或 `[C]` 行 ⇒ 报候选**（候选非阻断，口径同 G 现状）。触发＝下次触碰 `check_freedom()` 或 `check_spec.py`；登记落点＝`测试与验收标准` §4 新 G 行。
5. **「自述即无效」——规格头注里的一切自我声明都须有守卫，否则不得据以免除任何步骤**（本批定级失守的一般化教训，见 §九）。本批暴露的不是某人标错了，而是**一条靠自述生效的流程豁免**：`施工机制` §二 快行以头注一句「改动面全是逐字替换」为闸，**而这句话由规划方自己写、无人核**，于是「标 `[B]` 的行混进自称『快』的批」可以一路绿灯。同族自述句在本仓还有若干（如「真机走查豁免」理由、「不涉 `skills/` 资产正文」、「断言全部可机械判定」）。**作者已裁定：立条文**。条文＝**凡规格头注的自我声明，须逐句指认核对它的机械检查；指认不出者，该声明不生效、不得据以免除任何步骤**；落点＝`规格写作标准` §4 自审四条升为五条（真源）＋ `施工机制` §二 前导语与 §四 模板。**已转 `106` 批执行**（见该批 F3／F4／F5／F6／F7）。
6. **检查 J 不核「成本字段」**——`check_archive_record()` 只判①审查记录节存在性、②`findings 共 N` 与各档位之和、③占位符，对「工具往返数／周期时长」两字段**无任何判据**，故成本漏记永不会被 `check.sh` 的 `[4]` 段报出（本批即漏：只记了施工段）。触发＝下次触碰 `check_archive_record()` 时评估是否补一条「§九 含成本字段」判据（`施工机制` §八 该行已明写两字段为必填）。
7. **`AGENTS.md` §七 自检信号行仍写「`[4]` 段 A-I 无未核项」**，而 `[4]` 段现为 A–J（`102` 批加检查 J 时漏回写；非本批改动面）——`105` 产物审查观察项 6 报出。改 `AGENTS.md` 须**作者确认**（宪法 §六.1），故只登记、不擅改。触发＝作者确认。

## 九、审查记录

- 2026-09-16｜**路径定级失守（规划方自查，属本批最重发现）**｜本批按「快」起手，但 §二 **F1 标 `[B]`**，与快行触发条件①「无『按方向自定』项」相抵 ⇒ 按 §二「**混装批次按最重项定级**」应走**标准路径**。**后果**＝免成稿审查与产物审查各少一轮，而**该豁免本批无资格取得**。**成因**＝定级只靠规划方一句自述、无机械守卫核对（本批是「快路径」自 `103` 立规后的第一次实跑，**首跑即失守**）。**处置**＝①改 §一 头注「路径判定」与 §二 F4 标级，如实记为应走标准路径；②**补做一次独立成稿审查**（定稿后补，结论见下下条）；③守卫设计与通用条文已由作者裁定转 `106` 批执行（见 §八 开放项 4／5）。
- 2026-09-16｜**成本记账（分段）**｜**施工段** 工具往返数 31｜**审查段** 工具往返数 27（成稿审查自述）｜**全批合计 58**（§八 口径＝含施工与审查 subagent 自述数之和）｜周期时长 <1h（规格末次落盘 20:42 → 提交 `8c45093`）。
- 2026-09-16｜**成稿审查（定稿后补做；独立只读 subagent，因本批豁免无资格取得）**｜findings 共 7（P0:1 P1:2 P2:4）｜结论**修订后通过**。**逐条处置**：**P0-1**（未取得豁免却未走成稿审查、审查结论亦未入本 §九）→ 即本行＋上行成本分段；**P1-1**（成本只记施工段、`CHANGELOG` 缺成本註记，且检查 J **不核**成本字段）→ 上行改分段记账、`CHANGELOG` 条目补成本註记，检查 J 不核成本字段一事留 §八 开放项 6；**P1-2**（§四.1 写「五条」而 §一 实为 6 行）→ 已改「六条」并补 ⑥；**P2-1**（断言 2 的判据把汇总行一并计入）→ 判据改取候选行全文；**P2-2**（`[C]×k` 与代码 `[C]×c` 不一致）→ 规格与 `CHANGELOG` 统一为 `[C]×c`；**P2-3**（F1 括注易读成组合标记行进 `[C]` 计数、代码实为不进）→ 括注改为「组合标记行不进 `[C]` 计数」；**P2-4**（锚点①「不参与对账」与 `G29` 行「既不计数」易互读为自相抵）→ 锚点① 改述为「解析已收录、仅未进 `fmt()`／不符判据」（`G29` 行留作缺口登记，不追改）。**审查报告另附两条观察项（未立案、不计入 N）**：① 本批施工方偏差 P1-1（`--set` 残留）只写了「正确方式」侧，未入反模式表任何行的「事故出处」列；② `#22`／`#23` 两族仅间接触及，不另开行。
- 2026-09-16｜**产物审查（补做；另一个独立只读 subagent）**｜findings 共 6（P0:0 P1:2 P2:4）｜结论**修订后通过**。**逐条处置**：**P1-1**（落地 `CHANGELOG` 条目缺 §三 强制的成本註记，且补上会超 400 字符）→ 已在提交 `eee1b76` 落地：条目压到 307 字符并补 `（往返 58／<1h）`；**P1-2**（被处置的开放项未同批改标——`103` §八 6 仍写旧触发；且本规格 §一 必读输入把 `103` 写成 `docs/specs/archive/…`，实为断链）→ §二 追加 **F5**（改标 `103` §八 6）＋已改正必读输入路径；**P2-1**（`check_freedom()` docstring 替换边界留「两者」悬空指代，枚举已改三者）→ 该句改写为「含 `[A]` 与 `[B]` 者计入混合……三者皆无者只进总行数」；**P2-2**（`G29` 的「本批处置」落进**触发条件**列、状态/处置列留空，与自述「形态照 `G16`」不符）→ 该格移入第 3 列；**P2-3**（`#29` 新挂的机械判据不能区分命中与未命中：「不符 0 项」也含「不符」二字；且未标「只对在制规格打印」）→ 改述为「报『不符 N 项』且 N≥1 即命中（该汇总只对在制规格打印，口径同 `#27`）」；**P2-4**（同一格三个字母：条目 `[C]×k`／docstring `[C]×j`／实现 `[C]×c`）→ docstring 统一为 `[C]×c`（条目侧 `eee1b76` 已重写掉该短语）。**按观察项 3／4 新开 `G31` 行**（§二 F6）；**观察项 5**（`scripts/check_spec.py:248` 内联注释「三种可数结构」与 docstring「五种结构」不一致）→ 同批改为「五种」；**观察项 6**（`AGENTS.md` §七 A-I 未回写，须作者确认才可改）→ 记 §八 开放项 7；**观察项 1／2／9** 为实跑确认与并发说明，无需动作。
- 2026-09-16｜**前身批（`104`）成稿审查结论存入**（该批已缩范围为 `105`，其审查报告仍具参考价值）：findings 共 23（P0:5 P1:10 P2:8）｜结论**修订后通过**。其中 **P0-3 直接促成反模式 `#30`**（锚点⑧失实、F8 无标的）；P0-5（新件无 H1 即违 `文档写作标准:16`）与 P0-1／P0-2（件内／件外指针漏列）**均为"搬动结构"类，随该动作一并移出本批**（见 §八 开放项 1）。
- 2026-09-16｜**施工方偏差登记 5 条（P1×3 P2×2；非审查 findings，不计入 N）**——均**登记不改断言**（红线 4）：**P1-1** `--set` 锚片段未含到行尾致 5 行残留（写入后全文重读察觉、已定点修净；回读测不出该残留）；**P1-2** `fmt()` 按五值参数化——规划方裁定**保留显式五参**，不收默认参（默认值会让漏传者在输出里静默显示 `[C]×0`，正是本批要关的「收集了但没接进判据」同族风险）；**P1-3** `CHANGELOG` 条目为 §三 要求但未列 §二，且原落点在 `### Added` 之外（悬空列表项）——规划方已移至 `### Added` 之下并置该段首条，并在 §二 补记 **F4** ＋头注、§一 锚点⑥同步（F4 标级按 099–103 同项先例取 `[B]`，非 `[C]`）；**P2-1** 断言 1a 期望 `≥3` 实测 **4**（两处入账：`fmt()` 调用与不符判据元组），断言本身通过、不修；**P2-2** 该函数 docstring 示例串含非 `FREEDOM_COUNT` 类目——经核为**预期**（`混合×k` 由 `FREEDOM_MIXED` 识别，docstring 原文即写 `FREEDOM_*` 常量复数）。
