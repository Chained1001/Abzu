# 106-2026-09-16-定级守卫与自述即无效批

> 定位：`105` 批自查出一处**流程豁免失守**——头注自称「快路径」（据此免成稿审查与产物审查），而 §二 里明明留着 `[B]` 项；豁免条件写的是「改动面全部是逐字替换」，**这句话由规划方自己写、无人核**。本批做两件事：① 把这条自述交给**机械守卫**（检查 G 扩判据）；② 立**通用条文**「自述须有守卫，否则不生效」——不只治这一句，治这一族。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**（跨多文件、改规范条目、动守卫实现）＋ **双维产物审查**（改标准条文）。真机走查**不适用**：本批 §二 逐行文件的改动面无用户可观测行为（该结论可由清单直接读出，非自述）。
> 自由度分布：[A]×9 [B]×4 共 13 行
> 必读输入：`scripts/check_spec.py` 的 `FREEDOM_*` 常量区（`:248`–`:254`）与 `check_freedom()` 全函数（`:1400`–`:1458`）；`docs/specs/施工机制.md` §二 前导语（`:35`）、快行（`:40`）、「快路径回退与守门」bullet、`:52` 自审指针、`:219` 模板 §四、§六 反模式表末（`:170` `#30` 行）；`docs/standards/规格写作标准.md` §4（`:46`–`:50`）与 §三 表（`:123`）；`docs/standards/测试与验收标准.md` §4 末行（`:93` `G29`）；`docs/specs/105-2026-09-16-三处缺口补齐批.md` §八 开放项 4／5 与 §九
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-16）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `check_freedom()` **只取「自由度分布」那一行**，**不读「路径判定」行**——`FREEDOM_HEAD = re.compile(r'自由度分布')`，取数语句为 `head = next((l for l in text.split('\n') if FREEDOM_HEAD.search(l)), '')` | `grep -n 'FREEDOM_HEAD' scripts/check_spec.py` 实测 **4 处**：`:250`（定义）与 `:1414`（取数）两处为**使用**，另 `:48`（模块 docstring 常量枚举）与 `:1405`（本函数 docstring 判据段）两处为**非使用提及**（施工方 **D-3** 订正：本条原写「仅两处」） |
| ② | 同一函数**已握有 §二 各行自由度计数** `c_a`／`c_b`／`c_c`／`c_mixed`（`105` F1 补 `c_c` 后） | 逐行 read `:1423`–`:1434` |
| ③ | `施工机制` §二 快行触发条件①原文：「改动面**全部是逐字替换**（每条改动都能写成「逐字：旧→新」，**无「按方向自定」项**）」；表下「快路径回退与守门」bullet 已写「**混装批次按最重项定级**：一批内只要有一项不是逐字替换，全批走标准路径」——**规则齐备，但无任何检查核头注那句自述** | 逐行 read `:40` 与该 bullet |
| ④ | `105` 批实测：头注自称「快路径」而 §二 F1 标 `[B]`（按方向自定实现），**免成稿审查与产物审查一路绿灯到提交**，最终由规划方自查才发现 | read `105` §九；`git show 8c45093 --stat` |
| ⑤ | `规格写作标准` §4 标题现为「规格自审（**四条**…）」；「四条」另有三处载体：`施工机制:52` 指针、`施工机制:219` 模板节标题、`规格写作标准:123` §三 表行「§4 自审**四条**齐备」 | `grep -rn '自审四条\|规格自审（四条' docs/standards/规格写作标准.md docs/specs/施工机制.md` 实测 4 处（**不含**各批规格自身的历史 §四 标题——历史记录不追改，见 §五） |
| ⑥ | `测试与验收标准` §4 末行现为 `G29`；`施工机制` §六 反模式表末行为 `#30` | 逐行 read `测试与验收标准:93`；`grep -c` 按「行首＋竖线＋空格＋数字」模式（此处不写字面竖线，避 MD056）实测 **30** |
| ⑦ | 各批规格「路径判定」行的主形态为「路径判定（…）：加粗路径词」——路径词是该行**第一个加粗片段**；`105` 改正后写「应走标准路径（原按「快」起手…）」 | `grep -h '路径判定' docs/specs/*.md docs/specs/archive/*.md` 全量后经 `sed`／`sort`／`uniq` 去重统计，实测 32 行**声明式**（以 `>` 起首）全部符合「路径词＝首个加粗片段」；另 16 行含「路径判定」的**正文／代码围栏引文**不属声明式——故 F1 ① 的常量只认 `>` 起首行（成稿审查 P1-1）。同一「自审四条」形态另存于 `docs/specs/103-…` `:185`、`docs/specs/105-…` `:81` 与 `:121`，**均属历史记录、§五 已声明不追改**，且**不在**断言 6 的扫描面内（成稿审查 P0-1 的解法） |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py` 的 `FREEDOM_*` 常量区（`:253` `FREEDOM_TOTAL` 之后）、模块 docstring `:48` 的常量枚举、`check_freedom()`（`:1400`–`:1458`） | **新增「路径定级」判据（八小项）**：① 新增常量 `FREEDOM_PATH = re.compile(r'^\s*>\s*路径判定')`——**只认头注块内以 `>` 起首的声明行**，不收正文与代码围栏内的引文（成稿审查 P1-1；紧接 `FREEDOM_TOTAL` 行后另起一行）；② 函数内取该行：`pline = next((l for l in text.split('\n') if FREEDOM_PATH.search(l)), '')`；③ 取该行**第一个加粗片段**：`m_bold = re.search(r'\*\*(.+?)\*\*', pline)`，`declared_fast = bool(m_bold) and '快' in m_bold.group(1)`；④ `declared_fast and (c_b + c_c + c_mixed) > 0` 时出候选（**混合行也须触发**——`[A]`／`[B]` 并存行按分支序只进 `c_mixed`，规则上同样不是逐字替换，成稿审查 P1-2），候选文案以「路径定级失守」五字起（断言 1 即匹配此串）；⑤ **②③④ 的取数与判定须置于 `head` 早退（`:1415` `if not head: return [], []`）之前**——否则头注缺「自由度分布」行时整套检查 G 静默，守卫可被一行之差整段关掉（成稿审查 P0-2）；此时**仍出定级候选、但不打汇总行**（「静默条件（硬）」只管自由度对账）；⑥ **只改 `summary` 那一条 f-string（`:1456`）**：末尾追加 `｜定级 {快／非快／未写}`（全角竖线，与既有两处分隔符同形；`pline` 为空串＝`未写`，`declared_fast`＝`快`，其余＝`非快`）——**`fmt()`、`head_s`、`list_s` 一律不动**（后两者同时被嵌进候选行文案，若塞进 `fmt()` 则候选行也会出现「定级」格——成稿审查观察项）；⑦ docstring 判据段补一句，**逐字**：**路径定级（106 加）**：另取头注块内以 `>` 起首的「路径判定」行，若其**首个加粗片段**含「快」而清单含 `[B]`／`[C]`／混合行即出候选——「快」行的三条触发条件全靠规划方自述，本条是它的守卫（`施工机制` §六 `#31`）；该判定在 `head` 早退之前执行，故无「自由度分布」行时仍生效；无「路径判定」行时不判，只记 `定级 未写`。；⑧ 模块 docstring `:48` 的常量枚举补 `FREEDOM_PATH`。**既有** `[A]`／`[B]`／`[C]`／混合／总行数五值的判据**一律不动** | [B] | 锚点①②③⑦；`施工机制` §六 `#31`；成稿审查 P0-2／P1-1／P1-2 |
| F2 | `docs/specs/施工机制.md` §二「快路径回退与守门」bullet 末 | 句末**逐字追加**：**该回退已部分机械化**：头注声明「快」而 §二 含 `[B]`／`[C]` 行时，`检查 G 汇总` 出「路径定级失守」候选（`106` F1；候选非阻断，退出码不受影响）。 | [A] | 锚点③；F1 |
| F3 | `docs/specs/施工机制.md` §二 前导语（`:35`）末 | 句末**逐字追加**：**本表各条豁免所依赖的自述句（路径判定／真机走查豁免／「不涉某面」等）须逐句指认核对它的机械检查；指认不出者，该自述不生效、不得据以免除任何步骤——但本句只管「本该有机械检查而没写」的自述；真源明文交给作者或人判轨的豁免（「轻」行的作者会话内逐项确认、真机走查的语义判断）不在此列，照原文执行**（真源＝`规格写作标准` §4 第 5 条；射程界定见成稿审查 P1-3）。 | [A] | `105` §九；F4 |
| F4 | `docs/standards/规格写作标准.md` §4（标题 `:46`、正文 item 4 之后、§三 表行 `:123`） | **三处**：① `:46` 标题「规格自审（**四条**；各规格的「规格自审」节按此写）」→「规格自审（**五条**；各规格的「规格自审」节按此写）」；② 在 item 4 与「附加项：」之间插入新 item 5，**逐字**：5. **自述有守卫**——规格头注里的每一句自我声明（路径判定／真机走查豁免／「不涉某面」／「断言全部可机械判定」等）须**逐句指认核对它的机械检查**（点名检查号或命令）；**指认不出者，该自述不生效、不得据以免除任何步骤**——该步「不生效」指的是**照最重路径执行**，不是把自述删掉。**射程限定**：本句只管「本该有机械检查而没写」的自述；真源明文交给作者或人判轨的豁免（「轻」行的作者会话内逐项确认、真机走查的语义判断）不在此列，照原文执行。事故出身：`105` 批头注自称「快路径」而 §二 有 `[B]` 项，两轮审查被免、一路到提交才由规划方自查发现（见 `施工机制` §六 `#31`）。；③ `:123` 表行「§4 自审**四条**齐备」→「§4 自审**五条**齐备」 | [A] | 锚点⑤；`105` §八 开放项 5（作者裁定：立条文） |
| F5 | `docs/specs/施工机制.md` `:52` 自审指针与 `:219` 模板节（含模板 item 4 之后） | **三处**：① `:52`「规格自审**四条**（真源＝`规格写作标准` §4；缺任一条不得产出施工交底）：锚点／断言／禁止／范围与期望同源——细则见真源。」→「规格自审**五条**（真源＝`规格写作标准` §4；缺任一条不得产出施工交底）：锚点／断言／禁止／范围与期望同源／**自述有守卫**——细则见真源。」；② `:219`「## 四、规格自审（**四条**）」→「## 四、规格自审（**五条**）」；③ 同节 item 4 之后插入模板 item 5，**逐字**：5. **自述有守卫**——头注每句自述指认核对它的机械检查；指认不出者不生效、照最重路径执行（**射程**：只管本该有机械检查而没写的自述；真源明文交给作者或人判轨的豁免不在此列）。{逐句指认结果} | [A] | 锚点⑤；F4 同源 |
| F6 | `docs/standards/测试与验收标准.md` §4 表末（`G29` 行之后） | 新增一行（4 格，照 `G29` 形态），**逐字**（行内表体竖线写作转义形式）：G30 行**四格依次为：第 1 格＝编号 `G30`；第 2 格＝缺口文本**「检查 G **不核头注的路径自述**：`check_freedom()` 只取「自由度分布」行（`FREEDOM_HEAD`，`scripts/check_spec.py:250`），头注自称「快路径」而 §二 含 `[B]`／`[C]` 行时**无任何检查报**——「快」行的三条触发条件全靠规划方自述（`105` 批实测：自称「快」而 F1 标 `[B]`，两轮审查被免、一路绿灯至提交才由自查发现）」；**第 3 格（状态/处置）＝「本批处置（`106` F1 已加路径定级判据）」；第 4 格（触发条件）留空**——形态照同表 `G16`／`G21`／`G22`（`105` 产物审查 P2-2 已订正 `G29` 行列位，本条照订正后体例）。**另**：`测试与验收标准` §1.1 `[4]` 行的「管什么」列逐个枚举 A–J 判据面、§三 机检锚点表亦列检查 G 判据面——**两处须同批补「路径定级」一面**（`施工机制` §四 守卫变更回写清单①⑤；成稿审查 P2-1①） | [A] | 锚点①④⑥；F1；成稿审查 P2-1① |
| F7 | `docs/specs/施工机制.md` §六 反模式表末（`#30` 行之后） | 新增 `#31` 行（4 列同构），**逐字**：第 1 列＝`31`；第 2 列＝「**靠头注自述取得流程豁免**——把「本批走快路径（免成稿审查与产物审查）」写在规格头注，而 §二 里明明留着 `[B]`（按方向自定实现）项；豁免条件写的是「改动面全部是逐字替换」，**这句话由规划方自己写、无人核**」；第 3 列＝「头注每句自述**须指认核对它的机械检查**；路径自述由检查 G 核（声明「快」而 §二 含 `[B]`／`[C]` 行即出「路径定级失守」候选，`106` F1）；指认不出守卫的自述，一律照最重路径执行」；第 4 列＝「2026-09-16 `105` 批（自称「快路径」而 F1 标 `[B]`，两轮审查被免、一路绿灯至提交，规划方自查时才发现；见该批 §九）；同族＝`#9`（纪律靠自觉不靠机制）／`#19`（放宽判据下结论）」 | [A] | `105` §九；`施工机制` §六 分级表 |
| F8 | `CHANGELOG.md`（`## [Unreleased]` 段） | **新增过程产物一行**（列 0、以 `-` 加空格起首、≤400 字符；**条首串写死为「定级守卫与自述即无效（106）」**；**子段写死为 `### Added`**——本批主体是加守卫与加条文；**条目末尾附成本註记 `（往返 R／T）`**——`R` 含施工与审查 subagent 自述数之和、由规划方汇总，`T` 按量级，**连同註记仍须 ≤400 字符**） | [B] | `施工机制` §三 过程产物两项（含成本註记）；`100` F4（子段须写死）；成稿审查 P2-1③ |
| F9 | `scripts/check.sh` `:10`（头注段行）与 `:138`（`echo` 行） | 两处 `[4]` 段枚举中检查G 的段名改为「检查G 自由度分布／路径定级核对」——**段序与段数不变**；系 `施工机制` §四 回写清单 ③ 的漏项（**产物审查 P1-1 追加**） | [A] | `施工机制` §四 回写清单 ③；产物审查 P1-1 |
| F10 | `scripts/check_spec.py:10`（模块 docstring 的检查 G 描述） | 该括注末补「＋**路径定级核对**（头注「路径判定」自述 ↔ §二 清单行：自称「快」而清单含 `[B]`／`[C]`／混合行即出候选，`106` 加）」——回写清单 ④ 的漏项（**产物审查 P1-2 追加**） | [A] | `施工机制` §四 回写清单 ④；产物审查 P1-2 |
| F11 | `scripts/check_spec.py` 的 `check_freedom()` 取数（`pline`）与函数 docstring | **取数限定在头注块内**（`text.split('\n## ', 1)[0]`，即首个二级标题之前）——原实现取全文首个 `^\s*>\s*路径判定` 行，**正文引文会致误报**（产物审查 P1-3 的 fx12 夹具实证：头注无路径判定行、§一 引文块含 `> 路径判定：**快路径**` ⇒ 误报 1 条）；docstring 同步改述为「头注块（首个 `##` 二级标题之前）内」 | [B] | 产物审查 P1-3；`施工机制` §六 `#24` |
| F12 | `docs/specs/施工机制.md` `:35`／`:45`／`:171` 与 §六 表末 | **四处**：① `:35` 末注的射程指代补批号，改为「射程界定见 `106` 批成稿审查 P1-3」；② `:45` 旧述把候选说成出自汇总行，订正为「检查 G 出**候选行**（汇总行只多一格「｜定级 X」）」并补「／混合行」；③ `:171` 同补「／混合行」与「候选行」；④ §六 表末新增 `#32`（**把「回写面」写进禁止面**）（**产物审查 P2-1／P2-2／观察项新族追加**） | [A] | 产物审查 P2-1／P2-2；`施工机制` §六 表头注 |
| F13 | 本规格 §五（禁止面）与 §七（核销表） | **两处**：① §五 解除对 `scripts/check.sh` 的禁改并写明豁免范围（只 `:10`／`:138` 两处枚举串）；② §七 补 `105` §八 开放项 7 一行、汇总改 7 条（**产物审查 P1-1 根因／P1-4 追加**；本条改的是**本规格自身**、无逐字目标文本可引，故记 `[B]`） | [B] | 产物审查 P1-1／P1-4；`施工机制` §四 回写清单／§八 模板 |

## 三、验收断言（基线 2026-09-16 实跑）

### 断言 1｜守卫**真会报**（F1·夹具正控，防「存在即可」）

```bash
mkdir -p .tmp/106 && printf '# T\n\n> 路径判定：**快路径**\n> 自由度分布：[A]×1 [B]×1 共 2 行\n\n## 二、文件级改动清单\n\n| # | 文件:位置 | 改动 | 自由度 | 依据 |\n| --- | --- | --- | --- | --- |\n| F1 | a.md | x | [A] | y |\n| F2 | b.md | x | [B] | y |\n' > .tmp/106/fx_fast.md && python scripts/check_spec.py --static .tmp/106/fx_fast.md 2>&1 | grep -c '路径定级失守'
```

期望 **1**。基线 **0**。**说明**：夹具＝「自称快路径 ＋ 清单里有 `[B]` 行」，正是 `105` 批的真实形态。只断言「常量存在」会被「写了但没接进判据」蒙过（同 `105` 断言 2）。夹具用完即删（红线 2）。

### 断言 2｜不误报（F1·负控一：全 `[A]` 的快路径批）

```bash
mkdir -p .tmp/106 && printf '# T\n\n> 路径判定：**快路径**\n> 自由度分布：[A]×2 共 2 行\n\n## 二、文件级改动清单\n\n| # | 文件:位置 | 改动 | 自由度 | 依据 |\n| --- | --- | --- | --- | --- |\n| F1 | a.md | x | [A] | y |\n| F2 | b.md | x | [A] | y |\n' > .tmp/106/fx_ok.md && python scripts/check_spec.py --static .tmp/106/fx_ok.md 2>&1 | grep -c '路径定级失守'
```

期望 **0**（合规的快路径批**不得**被报）。

### 断言 3｜不误报（F1·负控二：本仓现行件回归）

```bash
python scripts/check_spec.py --static docs/specs/103-2026-09-16-快路径与成本可见面批.md 2>&1 | grep -c '路径定级失守'
```

期望 **0**（`103` 为**标准路径**且含 7 行 `[B]`，路径词是「标准路径」⇒ 不触发）。

### 断言 4｜定级格可见（F1·汇总行新格）

```bash
python scripts/check_spec.py --static docs/specs/103-2026-09-16-快路径与成本可见面批.md 2>&1 | grep -c '检查 G 汇总:.*定级 非快'
```

期望 **1**。基线 **0**。

### 断言 5｜条文与台账落盘（F2–F7）

```bash
grep -c '规格自审（五条' docs/standards/规格写作标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '规格自审（五条' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '自述有守卫' docs/standards/规格写作标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '自述有守卫' docs/specs/施工机制.md
```

期望 **≥2**（`:52` 指针项名一处 ＋ 模板 item 5 标题一处）。基线 **0**。

```bash
grep -c '^| G30 |' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '^| 31 |' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 6｜「四条」无残留（F4/F5·连带面）

```bash
grep -c '自审四条\|规格自审（四条' docs/standards/规格写作标准.md
```

期望 **0**。基线 **2**。

```bash
grep -c '自审四条\|规格自审（四条' docs/specs/施工机制.md
```

期望 **0**。基线 **2**。

**断言 6 的扫描面说明**（成稿审查 P0-1 经复核为**误报**）：本条自始即为「两个具体件分列」的路径白名单写法（`规格写作标准`／`施工机制`），未见裸 grep；`103:185`／`105:81`／`105:121` 的同形串不在扫描面内，属 §五 声明的「历史记录不追改」。

### 断言 8｜守卫不被「缺自由度分布行」关掉（F1·落点正控）

```bash
mkdir -p .tmp/106 && printf '# T\n\n> 路径判定：**快路径**\n\n## 二、文件级改动清单\n\n| # | 文件:位置 | 改动 | 自由度 | 依据 |\n| --- | --- | --- | --- | --- |\n| F1 | a.md | x | [A] | y |\n| F2 | b.md | x | [B] | y |\n' > .tmp/106/fx_nohead.md && python scripts/check_spec.py --static .tmp/106/fx_nohead.md 2>&1 | grep -c '路径定级失守'
```

期望 **1**（**头注无「自由度分布」行时守卫仍须生效**——成稿审查 P0-2 的正控；改动前基线 **0**，且改动前该夹具的 `检查 G 汇总` 命中数为 **0**＝整套静默）。夹具用完即删（红线 2）。

### 断言 7｜门禁与过程产物

```bash
bash scripts/check.sh > .tmp/106/chk.out 2>&1; echo $?; tail -3 .tmp/106/chk.out
```

期望 **退出 0**，末三行含「检查全绿」。基线 **退出 0**（改动前；在制规格 `106` 自身亦须 lint 全绿）。

```bash
grep -c '定级守卫与自述即无效（106）' CHANGELOG.md
```

期望 **1**。基线 **0**。

## 四、规格自审（五条）

1. **锚点现场核过**：§一 七条逐条实跑——① `grep -n 'FREEDOM_HEAD'`；② 逐行 read `:1423`–`:1434`；③ 逐行 read `:40` 与守门 bullet；④ read `105` §九 ＋ `git show 8c45093 --stat`；⑤ `grep -rn '自审四条\|规格自审（四条'` 得 4 处载体；⑥ 逐行 read `G29` 行与 `#30` 行；⑦ `grep -h '路径判定'` 全量形态统计。**无未预验项**。
2. **断言实跑过**：§三 各断言的**改前基线**已逐条实跑（「路径定级失守」＝0、「定级 非快」＝0、`规格自审（五条`＝0／0、`自述有守卫`＝0／0、`^| G30 |`＝0、`^| 31 |`＝0、`自审四条`＝2／2、CHANGELOG 串＝0；**断言 8** 的基线＝候选 0 **且** `检查 G 汇总` 命中 0——整套静默，即 F1 ⑤ 要关掉的那个洞）。断言 1／2／3／8 的夹具文本均在本规格内逐字给出（可照抄实跑）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：§三 各断言的期望值均取自 §二 点名文件与节的范围；断言 1／2／3 的夹具与判据在同一命令内自洽（正控：自称「快」＋ 有 `[B]` 行 ⇒ 必报；负控一：全 `[A]` ⇒ 不得报；负控二：路径词为「标准路径」⇒ 不得报）。
5. **自述有守卫**（本批新立的第 5 条，本规格自己先照做）：头注四句自述逐句指认——①「标准路径」⇒ 由本批 F1 的判据直接核（该判据只在声明「快」且含 `[B]`／`[C]` 时触发；本件非「快」，属**判据覆盖外**，故另以 §三 断言 3 做现行件回归）；②「双维产物审查」⇒ 由 `施工机制` §五「改规则／结构／契约的批次」送审条件覆盖，**无机械检查**，按第 5 条应视为**不生效**——本批据此**照最重路径执行**（真做双维，不放宽）；③「真机走查不适用」⇒ **不是自述**：它是 §二 逐行文件清单的直接推论（清单内无 `skills/` 资产、无用户可观测行为），判据＝清单本身；④「自由度分布 8 行」⇒ 由检查 G 核（本件为在制规格，`bash scripts/check.sh` 的 `[4]` 段逐批核）。

## 五、禁止事项

- 禁止触碰：`docs/specs/archive/` **全部**；`docs/product/` 全部；`skills/` 全部；`AGENTS.md`；`docs/specs/103-2026-09-16-快路径与成本可见面批.md` 与 `docs/specs/105-2026-09-16-三处缺口补齐批.md`（**各批规格自身的历史 §四 标题属已落盘记录，一律不追改**）；`docs/standards/` 中除 `规格写作标准`／`测试与验收标准` 外的九件（**该两件为本批改动面、已在 §二 点名，于本禁改面明示豁免**）；`scripts/patch_file.py`；`scripts/check_content.py`。**`scripts/check.sh` 原列禁改面，经产物审查 P1-1 裁定解除**——`施工机制` §四 回写清单 ③ 要求同批回写其 `[4]` 段枚举；**豁免范围仅限 `:10` 与 `:138` 两处枚举串，段序与段数不得动**（**新反模式 `#32`**：禁止面把回写面一并禁掉）。
- 禁止动作：联网采集；**新增检查号**（不新增 K，不改 `[4]` 段段序与段数）；把新判据接成**置红**（检查 G 属 `[4]` 段，静态发现一律只呈报）；改既有检查 A–J 的**其他**判据；把「快」以外的路径自述（「轻」行、「真机走查豁免」）纳入本批判据射程（见 §六 与 §八 开放项 1）。
- 真机走查**不适用**（非自述：本批 §二 逐行文件的改动面无用户可观测行为，判据＝清单本身）。

## 六、被否决的替代方案

- **新开一个检查号 K** → 否决：会把「A–J 十类」的每一处回写牵动起来（`check.sh` 头注与 `[4]` 段 echo、`测试与验收标准` §1.1、`AGENTS.md` §七、`check_spec.py` 的 `static_report()` 三态判据），连带面远超收益——`104` 批 P0-1／P0-2 正是栽在「改动连带面漏列」上；检查 G 已握有两侧数据，**扩其判据最小**。
- **判据取「路径判定行里出现『快』字」** → 否决：`105` 改正后的头注写「**应走标准路径**（原按「快」起手…）」，字面含「快」会被**误报**；取该行**第一个加粗片段**（＝路径词，见锚点⑦）可避开，并由 §三 断言 3 做回归。
- **把条文只写进 `施工机制` §二 前导语，不动 `规格写作标准` §4** → 否决：自审的**真源唯一**在 `规格写作标准` §4（`施工机制:52` 明写「真源＝…§4」、`:219` 只是模板）；在非真源处立条文会造第二载体，触 `施工机制` §六 反模式表「同一规范复制多份维护」同族。
- **守卫同时覆盖「轻」行** → 否决：轻行的豁免走的是另一套条件（单文件 <5 行／≤3 文件 ＋ 作者会话内逐项确认 ＋ `CHANGELOG` 留痕不计入），与「快」行的三条触发条件不同构；本批只处置实测失守的「快」行，扩射程留 §八 开放项 1。
- **`105` 批重做（撤回已提交内容）** → 否决：`105` 的三处缺口补齐（检查 G 认 `[C]`／`#29` 挂判据／CHANGELOG 子段写死）本身正确且已验证；失守的是**路径定级**，已在 `105` §九 如实登记并补做独立成稿审查。撤回会把正确改动一并丢弃。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `105` §八 开放项 4 | 「快／标准」路径定级的机械守卫（触发＝下次触碰 `check_freedom()`） | **本批处置** | F1 ＋ F2 ＋ F6（登记 `G30`） |
| `105` §八 开放项 5 | 「自述即无效」条文的立与落点（触发＝作者裁定） | **本批处置** | 作者裁定＝立条文；落点＝F4（真源）＋ F5（指针与模板）＋ F7（台账 `#31`） |
| `105` §八 开放项 6 | 检查 J 不核「成本字段」（触发＝下次触碰 `check_archive_record()`） | 仍待 | 本批不触 `check_archive_record()`；触发未到 |
| `105` §八 开放项 1 | 「移出两块」的连带面扫描方法 | 仍待 | 本批**实测**了一次小规模连带面扫描（锚点⑤：`grep -rn` 得 4 处载体，已逐处列进 F4／F5），但**尚未成形为通用方法**——触发仍为该清单成形或作者裁定 |
| `105` §八 开放项 2 | 反模式表的「机械判据」列逐行补齐 | 仍待 | 本批只新增 `#31` 一行且该行**自带机械判据**（检查 G）；全表回填触发未到 |
| `105` §八 开放项 3 | 已归档规格里的教训是否已回填台账 | 仍待 | 只读审计在查（42 件归档），结论待收 |
| `105` §八 开放项 7 | `AGENTS.md` §七 仍写「`[4]` 段 A-I」而实为 A–J（`102` 批漏回写） | 仍待 | 改 `AGENTS.md` 须作者确认（宪法 §六.1）——**本条系产物审查 P1-4 抓出的漏列**（原表只列 6 行、汇总行按全列计 6，检查 H 只核表内自洽故不可见） |

- 汇总：`按条计 7 条：已履行 0｜作废 0｜本批处置 2｜仍待 5｜待作者 0 ＝ 7`

## 八、开放项

1. **守卫射程的其余自述句**——本批只机械化「快路径声明」与「§二 含 `[B]`／`[C]`」这一对。同族尚未有守卫的自述句至少还有：「轻」行声明、「真机走查豁免」、「不涉 `skills/` 资产正文」、「断言全部可机械判定」。触发＝其中任一再造出实际失守，或作者裁定补做。
2. **「轻」行的定级守卫**——轻行条件含「单文件 <5 行」「≤3 文件」「作者会话内逐项确认」三项，前两项**可机械核**（§二 点名文件数、改动行数），第三项不可。触发＝同 §八 开放项 1。
3. **`103` 规格的归档**——该批已提交且在制；归档动作（移入 `docs/specs/archive/` ＋ 回写指针）触发＝下次归档批，且须先有 §七 所列「搬动连带面扫描方法」。
4. **本批「照最重路径执行」的实例留痕**——§四 第 5 条的「不生效⇒照最重路径执行」本批首次实操（自述②「双维产物审查」无机械检查 ⇒ 真做双维，不放宽）。触发＝该判定在后续批次被援引或争议时，回看本批 §四.5。
5. **新建 `.md` 不经 `patch_file.py` 的形态闸**——本规格首稿由宿主 `write` 工具直落，**未经形态闸**，结果是 lint 置红 18 处（MD056 表格竖线未转义 ＋ MD038 因嵌套反引号跨度配对错位）——直到 `bash scripts/check.sh` 才被拦下（好在提交门禁拦住，未落红提交）。`施工机制` §六 `#13` 的形态闸措辞是「经 `patch_file.py` 落盘的 `.md`」，**新建件不在其内**。触发＝作者裁定是否把纪律改为「新建 `.md` 落盘后立即单件跑 lint」并写入 §六 `#13`。
6. **仓内 `.md` 行尾漂移（LF→CRLF）无机械守卫**——本批实测：`docs/standards/规格写作标准.md` 被改成 **CRLF**（同批另 4 件与全仓其余件均 LF；核 `HEAD` blob 为 LF 133 行 ⇒ 系统系本批引入），`git diff` 只给一行警告（`CRLF will be replaced by LF the next time Git touches it`），而门禁 `check.sh` 六段**全绿、不报**。成因：`patch_file.py` 的「保持目标件原有行尾」只保证**经它落盘**的件，走宿主编辑工具或其它路径改仓内 `.md` 时无任何拦截。**可机械化的判据现成**：`[1]` 段（或新增一段）对仓内 `.md`／`.py` 逐件统计 CRLF 行数，>0 即报候选（全仓现为纯 LF）。触发＝作者裁定，或下次触碰 `scripts/check.sh` 的 `[1]` 段。
7. **`[A]` 行里的引号块只能放「目标侧」文本**——本批规划方两次把**现状侧**旧文写成引号块（`106` F9 引旧段名、F12① 引旧指代），`检查 A` 当即按「[A] 逐字核对」把它当目标文本、报「未落实或已漂移」（旧文已不存在 ⇒ 恒报）。**可机械化的判据现成**：`检查 A` 已能定位该形态，缺的只是**规格写作侧的一句律**——`规格写作标准` §5 应补一条「`[A]` 行的引号块为**目标侧**；现状侧一律用文字转述，不得用引号块」，写入后此形态即可由「检查 A 报漂移」升级为「写作期即知不能这么写」。触发＝作者裁定或下次触碰 `规格写作标准` §5。

## 九、审查记录

- 2026-09-16｜**成稿审查（独立只读 subagent）**｜findings 共 6（P0:2 P1:3 P2:1）｜结论**修订后通过**｜工具往返数 25｜周期时长 <1h。**逐条处置**：**P0-1**（断言 6 被指「扫描面含 `105:121` 致必红」）→ **经复核为误报并留证**：断言 6 自始即为「两件分列」的路径白名单写法（已在 §三 与 §一 ⑤ 写明）；**P0-2**（`check_freedom()` 在缺「自由度分布」行时整段早退 ⇒ 守卫可被一行之差关掉；F1 未交代新代码落点）→ F1 增 ⑤「取数与判定须置于 `head` 早退之前」，并新增 **§三 断言 8** 作落点正控；**P1-1**（判据「取第一个含『路径判定』的行」会落到正文与代码围栏引文）→ F1 ① 常量改为只认头注声明行；**P1-2**（触发条件漏「混合」行）→ F1 ④ 改为 `(c_b + c_c + c_mixed) > 0`；**P1-3**（F3 条文射程会波及「轻」行与真机走查两处**人判豁免**）→ F3／F4／F5 三处逐字条文各补射程限定句；**P2-1**（合并条：F6 漏 §1.1／§三 回写、F6 列位错、F8 漏成本註记、F1 ⑤ 未写死落盘串）→ F6 改为「第 3 格＝本批处置、第 4 格留空」并补两处回写；F8 补成本註记句与 400 字符约束；F1 ⑥ 写死落盘串且明写「只改 `summary` 一行」。**观察项**已逐条采纳或复核。**本轮为该规格第 1 轮全文审**（`施工机制` §五 止损：全文审硬上限 2 轮）。
- 2026-09-16｜**施工（后台 subagent；改动 8 项／5 件）**｜断言 1–8 全绿；**规划方独立复跑**另加两条计划外控制——**标准路径不误报**（路径词为「标准路径」＋有 `[B]` 行）＝0、**混合行须触发**（`[A][B]` 行＋自称「快」）＝1；门禁 `bash scripts/check.sh` 退出 0。**施工方偏差 4 条**：**D-1（P1，规划方裁定）** §二 F6 格号自相抵——同句既写「4 格」又写「第 2 格留空；第 3 格＝处置；第 4 格留空」，而该表实为「编号｜缺口｜状态/处置｜触发条件」四列，首稿照字面写成 5 格被形态闸拦下（MD056，**零写盘**）；**裁定＝按四列落**（`| G30 | <缺口> | 本批处置 | |`，与 `G29` 订正后同构），并已订正 F6 的格号描述；**D-2（P2）** `CHANGELOG` 成本註记先写 R＝82，已按实际段数订正（见下条）；**同一口径回头订正了 `105` 的 R**（58→100：原只含成稿审查段、漏了产物审查段 42——该批产物审查 P1-1 的「成本记账不完整」据此才真正闭账），两处改动随本批一并提交；**D-3（P2，已订正）** §一 锚点① 原写「仅两处」，实为 4 处（2 使用＋2 提及），锚点所指事实不变；**D-4（P2）** 临时件已按红线 2 清理。**规划方另查出施工方未报的一处（P1）**：`docs/standards/规格写作标准.md` 被**由 LF 改成 CRLF**（同批其余件与全仓均为 LF）——已按「只归一化行尾、不动内容」改回 LF，并复核内容 diff 仍为预期 3 处；**新开 §八 开放项 6** 记其机械判据。
- 2026-09-16｜**产物审查（独立只读 subagent；与成稿审查为两个不同上下文）**｜findings 共 7（P0:0 P1:4 P2:3）｜结论**修订后通过**。**逐条处置**：**P1-1**（`check.sh` 头注 `:10` 与 `echo` `:138` 的 `[4]` 枚举未补「路径定级」＝回写清单 ③ 漏项；且本规格 §五 把该件列入禁改面致回写不可能）→ §二 追加 **F9**（补两处枚举）＋ **F13①**（§五 解除禁改并限定豁免范围）；**P1-2**（`check_spec.py:10` 模块 docstring 的检查 G 描述未回写＝清单 ④ 漏项）→ **F10**；**P1-3**（新判据取全文首个 `> 路径判定` 行而非「头注块内」，**正文引文可致误报**——审查方 fx12 夹具实证 1 条假候选）→ **F11**（取数限定首个 `##` 二级标题之前）＋ docstring 同步；**P1-4**（§七 核销表漏列 `105` §八 开放项 7，汇总行却按全列计 6）→ **F13②**（补行、汇总改 7）；**P2-1**（`:45`／`:171` 说「`检查 G 汇总` 出候选」实为独立候选行，且两处漏「混合行」）→ **F12②③**；**P2-2**（`:35` 末注「见成稿审查 P1-3」无批号＝悬空指代）→ **F12①**；**P2-3**（`CHANGELOG` 成本註记 R＝82 未含产物审查段、且施工段无往返数不可分解）→ 见下条三段成本，`CHANGELOG` 同步改 **129**。**观察项落地**：①审查方提报**候选新族**「把回写面写进禁止面」→ **F12④** 新开反模式 **`#32`**（本批 `#22`／`#24` 各重犯一次，已在该行注明）；②`106` 自身 `[4]` 段 33 条预警＋2 条「对账① 禁改面未豁免」逐条判属＝**均预期**：预警为施工前基线与候选类呈报，「禁改面未豁免」系 §五「除…外的九件」表述未被检查 E 的目录前缀判据识别 → **F13①** 顺手在 §五 写明「该两件为本批改动面、明示豁免」；③审查方行尾首测（`grep -c $'\r$'`）全量假阳、已改 Python 字节级复测——属反模式 `#28`（换工具复量不一致先判口径），非本批缺陷。
- 2026-09-16｜**成本记账（三段，`施工机制` §八 口径）**｜**施工段** 59｜**成稿审查段** 25｜**产物审查段** 45｜**全批合计 129**（`CHANGELOG` 条目末尾成本註记同步由 82 改为 129）。本条与 `105` §九 同口径——**产物审查段随其结论补记**。
