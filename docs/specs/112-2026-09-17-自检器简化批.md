# 112-2026-09-17-自检器简化批

> 模式：全量
> 必读输入：`scripts/check_spec.py`（全件）、`docs/specs/开发侧评审意见.md` §二／§三／§五、`docs/specs/施工机制.md` §九 模板

## 一、现状位置（现场核过）

- `scripts/check_spec.py`：实测 **2089 行**（`wc -l`＝换行符出现次数；**此为施工前基线**，施工后由断言 1 复核）。
- **动态阶段永不触达**：`check.sh` 的 `[4]` 段只以 `--static` 调用；`report()` 只在无 `--static`／`--verify`／`--reconcile` 时跑（`check_spec.py:2083`），本仓无此调用点。
- **死函数**：`_git_changes`（`:1925`）、`_archive_equiv`（`:1945`）全件仅定义处出现（`grep -n '_git_changes(' scripts/check_spec.py` → 4 处命中，3 处是 docstring 提及）。
- **低价值检查**：
  - 检查 F（禁改文件禁词）取词面为顿号分隔中文通用词，误报多。
  - 检查 G（可自定程度分布）所核的列**已不在现行模板里**（`施工机制` §九 模板改动清单列为「文件:位置｜改动｜依据／裁定」）；实跑 `--static` 本规格时它自报「清单 [A]×0 [B]×0 共 0 行」，即守护对象已消失。
- **但 `freedom` 字段必须留**：`_change_rows()` 产出的 `freedom` 字段被**检查 A**（`:765`）与**检查 C**（`:1023`）消费（判该行是 `[A]` 还是 `[B]`）；`free_i`（`:530`）只是它的定位手段。故本批**不删** `HDR_FREEDOM_ALIAS` 与 `free_i`，只把定位改**双读**（旧规格表头写「自由度」、新规格无该列）。
- **docstring 实测 86 行**（`:1-86`），其中约 40 行描述动态阶段与已删检查。
- `check.sh:10` 的 `[4]` 段头注为「检查项与各项判定标准见 `scripts/check_spec.py` 模块 docstring」——**无检查项枚举**；`AGENTS.md:113` 却写「[4] 段 A–J 无未核项」。

## 二、改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 依据／裁定 |
| --- | --- | --- | --- |
| 1 | `scripts/check_spec.py:92-134` | 删动态阶段常量组（`ALLOWED`／`MUTATING`／`_GIT_DIFF_LIKE`／`GIT_READONLY_OPTS`／`INJECT_ENV_EXACT`／`INJECT_ENV_PREFIX`） | 动态阶段专用 |
| 2 | `scripts/check_spec.py:136-145` | 删 `_child_env()`（含其上方注释行） | 只服务动态阶段与死函数 `_git_changes`——两者本次俱删 |
| 3 | `scripts/check_spec.py:181-192` | 删检查 F 注释块与 `BAN_WORD_CLAUSE`／`BAN_WORD_TICK`／`BAN_WORD_RUN` | 检查 F 专用 |
| 4 | `scripts/check_spec.py:225`、`:530-531` | `HDR_FREEDOM_ALIAS` 由单值改**双读**（新名与旧名并列），`free_i` 的定位同步改双读，`_change_rows` docstring 同步 | `freedom` 字段被检查 A／C 消费——**不可删，只可双读**（成稿审查 P0） |
| 5 | `scripts/check_spec.py:254-259` | 删检查 G 注释块与 `FREEDOM_HEAD`／`FREEDOM_COUNT`／`FREEDOM_MIXED`／`FREEDOM_TOTAL` | 检查 G 专用（含其注释行） |
| 6 | `scripts/check_spec.py:262-442` | 删动态阶段函数组：`extract`／`_outside_quotes`／`unauditable`／`_git_readonly`／`readonly`／`_emit`／`report` | 动态阶段专用 |
| 7 | `scripts/check_spec.py:1681-1734` | 删 `_ban_words()`／`check_ban_words()` | 检查 F（误报多、守护对象已收窄） |
| 8 | `scripts/check_spec.py:1735-1797` | 删 `check_freedom()` | 检查 G（守护的列已不在模板里） |
| 9 | `scripts/check_spec.py:1925-1949` | 删 `_git_changes()`／`_archive_equiv()` | 死函数 |
| 10 | `check_spec.py` `static_report()` | 删对 `check_ban_words`／`check_freedom` 的调用，及其 docstring 里「A／B／…／J 十类」的表述（`:1800`／`:1802`／`:1803` 改为八类） | 随第 7／8 项 |
| 11 | `check_spec.py` `__main__` | 删 `report(spec)` 调用（`:2083`）与「两者都不给→先跑动态阶段」分支——无参数时只跑静态 | 随第 6 项 |
| 12 | `scripts/check_spec.py:1-86` | docstring 精简：删动态阶段、执行面允许清单、不可审计六类、子进程宿主语义、检查 F／G 的描述（86 行 → 约 45 行） | 随上述删除 |
| 13 | `scripts/check_spec.py:234-237` | 删「`FENCE_BLOCK` 与 `extract()` 须同计算方式」的不变量注释（`extract` 本次删） | 成稿审查 P2 |
| 14 | `scripts/check_spec.py:444` | `repo_root()` docstring 里的「循 `report()` 的推导做法」改为「循脚本自身路径推导」 | 成稿审查 P2 |
| 15 | `scripts/check_spec.py:1319-1322` | 围栏态改为**按标记字符配对**（现为朴素翻转，孤立标记使后续判据静默失效） | 评审意见 P2-12 |
| 16 | `scripts/check_spec.py:1819`、`:1960`、`:1984` | 三处 `open(...)` 补 `errors='replace'`（现为非 UTF-8 即抛栈） | 评审意见 P2（容错口径） |
| 17 | `scripts/check.sh:10` | `[4]` 段头注**落检查项枚举**「检查 A–E、H–J」（原为纯指针，无枚举可读） | `施工机制` §六「改检查项同步两处」① |
| 18 | `AGENTS.md:113` | 「[4] 段 A–J 无未核项」改为「[4] 段 A–E、H–J 无未核项」 | 自洽回扫（`施工机制` §五）——指针须随被引事实更新 |

**不动项**（明确保留）：检查 A（`check_swallow`）、B（`check_counts`）、C（`check_pairs`）、D（`check_writing`）、E（`check_reconcile` 含模式判定）、H（`check_nuclear`）、I（`check_anchor`）、J（`check_archive_record`）、`--verify`、`--reconcile`。

## 三、验收断言

1. 行数：`wc -l scripts/check_spec.py` → 期望 ≤1700。
2. 动态阶段零残留：`grep -c "def report\|def extract\|def unauditable\|def readonly\|def _emit\|_child_env" scripts/check_spec.py` → 期望 0。
3. 死函数零残留：`grep -c "_git_changes\|_archive_equiv" scripts/check_spec.py` → 期望 0。
4. 检查 F／G 零残留：`grep -c "check_ban_words\|check_freedom\|BAN_WORD_CLAUSE\|BAN_WORD_TICK\|BAN_WORD_RUN\|FREEDOM_HEAD\|FREEDOM_COUNT\|FREEDOM_MIXED\|FREEDOM_TOTAL" scripts/check_spec.py` → 期望 0。

   （**模式收窄说明**：初稿用宽模式 `FREEDOM_`，会命中本批**声明保留**的 `HDR_FREEDOM_ALIAS`——它是 `freedom` 字段的定位手段，见 §一。收窄后只覆盖检查 G 的四个专属常量。**这是断言写法的订正，不是为转绿改期望**。）
5. 语法：`python -c "import ast,io; ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read())"` → 期望退出 0。
6. 静态检查仍在跑（拿归档件实测，不崩且有输出）：`python scripts/check_spec.py --static docs/specs/archive/111-2026-09-17-用词规范与禁用清单批.md` → 期望退出 0。
7. 检查 A／C 未被打断：接断言 6 的输出含方括号字母标记（证明 `freedom` 字段通路仍在）→ 期望 ≥1 处。
8. 无效参数仍退出 2：`python scripts/check_spec.py --static --verify` → 期望退出 2。
9. 零参数仍明示跳过：`python scripts/check_spec.py --static` → 期望退出 0 且输出「无在制规格，跳过」。
10. 门禁全绿：`bash scripts/check.sh` → 期望退出 0。

## 四、规格自审（五条）

1. **位置现场核过**：行数（`wc -l`＝2089）、各段行界（脚本实测函数边界）、死函数命中（`grep -n` 实测）、`free_i` 的消费点（`:765`／`:1023` 实测）、`check.sh:10` 无枚举、`AGENTS.md:113` 写「A–J」——均已现场核过；成稿审查指出的三处行号偏差（`_child_env` 界、`open` 行号、`check.sh` 段号）已订正。
2. **断言实跑过**：断言 1–4 改前基线实测＝2089／10／5／25；断言 8／9 实测退出 2／输出「无在制规格，跳过」退出 0；断言 5／6／7／10 为改后目标值，**未预验**，交产物审查核验。
3. **禁改文件已写明**：本批**只改** `scripts/check_spec.py`、`scripts/check.sh`、`AGENTS.md`（各一处）；禁止涉及 `docs/standards/**`、`skills/**`、`docs/product/**`、`docs/specs/archive/**`。
4. **范围与期望同源**：断言 1 的期望 ≤1700 由「2089 − 删量 431（逐段实算：43＋10＋12＋1＋6＋181＋54＋63＋25＋41）＝1658」加余量得出；断言 2–4 的期望 0 与本批删除项同源。
5. **自述有守卫**：头注「模式：全量」由 `check_spec.py` 的模式判定核对核（件数 18 ＞10 → 全量，自算一致）；「不动项保留」由断言 7 佐证；「未预验」四字交产物审查逐条核。

## 五、禁止事项

- 禁止涉及：`docs/standards/**`、`skills/**`、`docs/product/**`、`docs/specs/archive/**`。
- 禁止动作：联网、装依赖、改断言的期望值求绿、顺手改本批清单外的函数、删「不动项」所列的任何检查。

## 六、被否决的替代方案

- **删 `HDR_FREEDOM_ALIAS` 与 `freedom` 字段**（本规格初稿）→ 否决：该字段被检查 A／C 消费，删了会让这两项静默失效或直接崩（成稿审查 P0 实测指认）。
- **整件重写（2089 → 约 500 行）** → 否决：检查 A／C／E／H／I／J 有真实事故出身（断言自噬、`[A]` 漂移、核销对账、位置对源、审查记录未填），删了等于把守卫失明风险换行数。
- **只删死代码（不动检查 F／G）** → 否决：F 误报多、G 的守护列已不存在，留着是持续噪声。
- **把动态阶段保留为「按需启用」** → 否决：它零调用点，且 `--static` 已覆盖门禁需求；留着是 238 行代码＋40 行注释的维护面。

## 七、开放项

- 检查 B（计数重算）在归档语料上误报偏高——本批保留；若后续仍无真阳性，建议下次评估删除。
- 检查 D 的 ①②③ 与 `check.sh [1]` 段 markdownlint 重复——本批保留，理由同上。
- `--verify`／`--reconcile` 未进 `check.sh`（工具自陈）；是否纳入门禁另议。

## 八、审查记录

- 2026-09-17｜成稿审查｜发现的问题 共 7：P0:1 P1:3 P2:3｜结论 修订后通过｜工具往返数 6 轮（11 次调用）｜周期时长 <1h
  - P0-1：删 `HDR_FREEDOM_ALIAS` 会打断检查 A／C（二者消费 `freedom` 字段）→ 改为**双读**，并回填 §一 与 §六。
  - P1 三项：`_child_env` 行界多算 1 行｜三处 `open` 行号差 1 且漏列一处｜`check.sh` 段号与「无枚举可改」两处不实 → 均已订正。
  - P2 三项：`extract` 删除后不变量注释悬空｜`repo_root` docstring 指向已删函数｜`static_report` docstring 的「十类」未列入清单 → 已补为改动项 13／14／10。
- 2026-09-17｜产物审查｜发现的问题 共 4：P0:0 P1:1 P2:3｜结论 修订后通过｜工具往返数 13｜周期时长 <1h
  - 核对结论：18 项改动落实 16 项（漏 2），10 条断言全过，保留项 A／B／C／D／E／H／I／J 与 `--verify`／`--reconcile` 全活。
  - **意外收获**：双读把旧表头规格的检查 A／C **从全量静默救回**（归档 111 件：改前 A 命中 0／C 命中 0，改后 A 16／C 4）——即评审意见 P0-3 的实证修复。
  - P1-①：改动项 13 未落实（`extract()` 悬空引用仍在）→ 已删该子句。
  - P2 三项：检查 F／G 的残留引用 4 处（docstring×3＋注释×1）｜新引入未用常量 `LINE_LEAD`｜`_change_rows` docstring 未记双读 → 均已修。

### 核验期追加（第 19 项改动）

- **核验第 1 步（`--reconcile`）跑出真问题**：初跑报「本件声明 0 件」，追因发现**模板与工具节名不一致**——`施工机制` §九 模板写「二、改动清单」，而 `check_spec.py` 的 `CHANGE_SECTION` 只认「**文件级**改动清单」；同时本规格 §二 的路径列写的是裸文件名（非仓根相对全路径），两因叠加使声明面为空。
- **处置**：① `CHANGE_SECTION` 放宽为 `(?:文件级)?改动清单`（兼容两种写法，纳入模板节名对齐面——评审意见 P0-7 同族）；② 本规格 §二 路径列补全为 `scripts/check_spec.py:…` 全路径。
- **结果**：复跑 `--reconcile` → 「实盘改动 4 件｜本件声明 4 件｜**清单外 0 件**」，核验第 1 步通过。
