# 113-2026-09-17-评审余项收尾批

> 模式：全量
> 必读输入：`docs/specs/开发侧评审意见.md` §三／§四、`AGENTS.md`、`docs/standards/*.md` 四件、`docs/specs/施工机制.md`、`scripts/*.py`、`scripts/check.sh`、`scripts/hooks/pre-commit`、`README.md`、`.markdownlint-cli2.jsonc`

## 一、现状位置（现场核过）

- **悬空引用（3 处）**：`运行环境标准.md:34`「（事故记录 #28 同族）」｜`patch_file.py:11`「事故记录 #12／#18／#20 的」｜`check_spec.py:700`「／事故记录 `#33`」——**那张事故记录表已退役**（教训改走「直接立法」），编号无处可查。
- **规则互斥（1 处）**：`AGENTS.md:97` 账本律「每一步必须写入文件或修改某个文件」↔ `施工机制.md:98`「规格可在会话内呈报，也可落 `docs/specs/` 文件」。
- **口径不一（4 处）**：`文字与命名标准.md:194／197`「存量已清零」↔ `check_content.py:417` 输出「存量分批收尾」｜`运行环境标准.md:18`「已落 `check.sh` [4] 段」↔ 同件 `:119`「[3]／[4] 共用」｜`产物契约标准.md:56` 标「待建」↔ `skill资产标准.md:217` 标「已实现」（同为「引用闭合」）｜`skill资产标准.md:210`「（先例：6/6 主文件句式统一）」而现存主文件只有 1 个。
- **指针失效（3 处）**：`.markdownlint-cli2.jsonc:2` 指向**已删除**的 `文档写作标准.md`｜`check.sh:143` 注释指 `施工机制` §八（该件仅七节）｜`文字与命名标准.md:296` 引「事故记录清单的『正确方式』列」（清单已删）。
- **机架标记缺失（3 处）**：`AGENTS.md:5`「唯一宿主 Claude Code」、`:72`「单书体量」、`skill资产标准.md:21`「`abzu-outline`（阶段一资产已建）」——均未带本仓自立的「（本项目＝…）」标记。
- **脚本口径（6 处）**：`check_content.py:257` CHANGELOG 缺件静默跳过｜`check.sh:11`／`:157` 头注称「仓级 `.py`」而实际 glob 仅 `scripts/*.py`｜`check_content.py:155`／`:222` 扫描面含 `archive/`（与 §7「归档不改写」相抵）且对 CHANGELOG 的 TOC 与用词两处口径不一｜`patch_file.py:283` docstring 段号错（称第四段，实为第二段）｜`patch_file.py:22` 尾随换行通例与实际不符｜`patch_file.py:179` 非 UTF-8 抛栈未包成 `Fail`。
- **重复与杂项（5 处）**：`施工机制.md` 「`check.sh` 全绿」重复 5 处｜`施工问题记录.md` 的严重度与 §七 偏差等级同名未互引｜`check_content.py:451` `gov` 死变量＋重复算三遍｜`check_content.py:297` `HOT_BUDGET` 条目的注释文字（「上限线，目标 ≤12000」）与现件体量脱节｜`pre-commit:2` 注释含有禁用词**「门禁」**（应为「提交前自动检查」）——注意：禁用词是「门禁」而非替换词，本规格初稿描述反向。
- **门面（4 处）**：`README.md` 列 3 条命令而实际交付 1 条（评审意见 P0-6）｜安装说明缺「装后重载窗口」｜文档索引无 `CHANGELOG.md`｜安装命令未取人工试跑实测形态。

## 二、改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 依据／裁定 |
| --- | --- | --- | --- |
| 1 | `docs/standards/运行环境标准.md:34` | 删「（事故记录 #28 同族）」——改述为「同类：`grep -c` 计行与 `wc -c` 计字节均属计算方式误判」 | 评审意见 P1-2；事故记录表已退役 |
| 2 | `scripts/patch_file.py:11` | 删「事故记录 #12／#18／#20 的」句，改述为不带编号的机制沿革 | 同上 |
| 3 | `scripts/check_spec.py:412`、`:700` | 删「事故记录 `#33`」**与遗留批号「109 F3」**（同件两处）——违「不写批号」纪律 | 同上＋`文字与命名标准` §17 |
| 4 | `docs/standards/文字与命名标准.md:296` | 「事故记录清单的「正确方式」列」改为「`施工机制` §五 自洽回扫 的事故族列」 | 同上 |
| 5 | `AGENTS.md:97` | 账本律补例外：「（规格在会话内呈报时不计；落文件才算一步）」 | 评审意见 P1-4 |
| 6 | `docs/standards/运行环境标准.md` §2 | 补第 13 条：内联脚本包 heredoc（`>` 被 shell 当重定向）——与 `AGENTS.md:64` 速查第 6 条对齐 | 评审意见 P1-5 |
| 7 | `docs/standards/文字与命名标准.md:204` | §8 产品领域词行首补括注「（§6 清单内去箭头后同形的四个产品词，由本段豁免）」 | 评审意见 P1-6 |
| 8 | `docs/standards/产物契约标准.md:56` | 「引用闭合」状态「待建」→「已实现」（`check.sh [3]` 已覆盖） | 评审意见 P1-7 |
| 9 | `docs/standards/运行环境标准.md:18`、`:119` | 两处同改：`:18`「已落 [4] 段」→「[3]／[4]／[5] 段共用前置」；`:119`「[3]／[4]」同步补 `[5]`（口径以 `check.sh` 头注为准） | 评审意见 P1-25；成稿审查 P1-3 |
| 10 | `docs/standards/运行环境标准.md:36` | §2.11 补适用面注：「本条指**命令调用**用 `python`；探测链以 `python3` 起」 | 评审意见 P1-8 |
| 11 | `docs/standards/文字与命名标准.md:194`、`:197` | 「存量已清零」→「存量已清零，守卫末行的『分批收尾』措辞随之停用」；同步改 `check_content.py:417` 的输出措辞 | 评审意见 P1-24 |
| 12 | `scripts/check_content.py:417` | 输出末行「存量分批收尾」→「存量已清零」 | 随第 11 项 |
| 13 | `docs/standards/skill资产标准.md:210` | 「（旧仓先例：6/6 主文件句式统一）」改为「（旧仓多域时期先例）」 | 评审意见 P2-5 |
| 14 | `docs/standards/文字与命名标准.md:362` | 加粗密度行：删重复的「阈值未立法，阈值未立法」半句；状态「待建」→「**不设守卫**」（阈值未立法；**不引 `守卫缺口清单`**——该件已退役） | 评审意见 P2-6／P2-8 |
| 15 | `docs/standards/skill资产标准.md:3`、`docs/standards/运行环境标准.md:3` | 头注补「依据」行，或改为一行式并标注豁免 | 评审意见 P2-7 |
| 16 | `scripts/check_content.py:258-259` | `CHANGELOG.md` 缺件时补「原因行 ＋ 手动路径」两行（现为静默 `return`） | 评审意见 P1-10 |
| 17 | `scripts/check.sh:11`、`:157` | 头注「仓级 `.py`」改述为「`scripts/*.py`」；并明示 `skills/{名}/scripts/*.py` **不在本段扫描面** | 评审意见 P1-11 |
| 18 | `scripts/check_content.py:155`；`CHANGELOG.md` | ① 引用闭合扫描面排除 `docs/specs/archive/**`（与 §7「归档不改写」一致）；② **不给 135 行的 `CHANGELOG.md` 加目录豁免**（那会与 `文字与命名标准` §10.3 相抵）——改为**给 `CHANGELOG.md` 补节目录** | 评审意见 P1-12；成稿审查 P1-4 |
| 19 | `scripts/patch_file.py:283` | docstring「四段式第四段之**前置检查关**」→「四段式**第二段**之**前置检查关**」（与同件头部定义②＝形态检查一致） | 评审意见 P1-21 |
| 20 | `scripts/patch_file.py:22` | 尾随换行说明按模式分写：`--append`／`--insert-before` 剥一次；`--set` 不剥 | 评审意见 P1-22 |
| 21 | `scripts/patch_file.py:179` | 非 UTF-8 目标件包成 `Fail`（现抛 `UnicodeDecodeError`，退出码 1 而非 2） | 评审意见 P2-20 |
| 22 | `scripts/hooks/pre-commit:2` | 注释「git 提交门禁」→「git 提交前自动检查」 | 评审意见 P1-20 |
| 23 | `.markdownlint-cli2.jsonc:2` | 权威指针改指 `docs/standards/文字与命名标准.md` §12（原指向已删除件） | 评审意见 P2-9 |
| 24 | `scripts/check.sh:143` | 注释「其 §八 模板内嵌本节标题」→「其 §九 模板内嵌本节标题」 | 评审意见 P2-10 |
| 25 | `AGENTS.md` §二 路由表 | 补一行：加／改规格自检器 → `scripts/check_spec.py` | 评审意见 P2-17 |
| 26 | `scripts/check_content.py:451` | 删 `gov` 死变量；同一面改为算一次 | 评审意见 P2-18 |
| 27 | `scripts/check_content.py:297` | `HOT_BUDGET` 的「`施工机制` 13000／曾 11988」注释与现件体量脱节 → 复算 | 评审意见 P2-22 |
| 28 | `docs/specs/施工机制.md:98` | **只收敛归档句**：`:98` 的「（验收后移入 `archive/`）」改为「（归档见 §八）」——与 `:228` 重复。**「`check.sh` 全绿」6 处不改**：实证各司其职（三张流程图各一＋闭环规则＋核验定义＋提交门禁），非冗余 | 评审意见 P2-3（**部分成立**，见 §九） |
| 30 | `AGENTS.md:5`、`:72` | 「唯一宿主 Claude Code」「单书体量」补「（本项目＝…）」标记 | 评审意见 P2-1 |
| 31 | `AGENTS.md:56` | `git config core.hooksPath` 行补括注「（改 `.git/config`，须你确认——红线 1）」 | 评审意见 P2-2 |
| 32 | `docs/standards/skill资产标准.md:21` | 「`abzu-outline` 阶段一资产已建」补「（本项目＝…）」标记 | 评审意见 P1-26 |
| 33 | `README.md:16-18` | 三条命令各补建设状态（只 `abzu-outline` 已建，另两条标「未建」） | 评审意见 P0-6 |
| 34 | `README.md:7-9` | 安装说明补「装后须重载宿主窗口，`/` 菜单才列出新装的 skill」 | 评审意见 P2-23 |
| 35 | `README.md:21-26` | 文档索引补 `CHANGELOG.md` 一行（沿革入口） | 评审意见 P2-25 |
| 36 | `docs/standards/文字与命名标准.md:153` | 禁用清单「反模式 → 事故记录」→「反模式 → 施工问题记录」（那张「事故记录」表已退役） | 成稿审查 P2-13 |
| 39 | `scripts/check_spec.py:200` | 修 `PATH_TOKEN` 正则：分支改**长扩展名优先**（`jsonc\|json\|md\|js\|sh\|py`）＋补词边界——原序把 `.jsonc` 截成 `.js`，`--reconcile` 据此误报「清单外」 | 核验第 1 步实测 |
| 37 | `docs/specs/施工机制.md:197` | §五 事故族补一行：**「删符号未查消费链」**（删常量／函数前未追其消费方，导致守卫崩或静默失效）——源自 112 批成稿审查 P0 | `施工机制` §三 移动前沉淀闸 |
| 38 | `docs/specs/112-2026-09-17-自检器简化批.md` → `docs/specs/archive/112-2026-09-17-自检器简化批.md` | 移入 `docs/specs/archive/`；移入前 §七 开放项逐条标沉淀落点 | `施工机制` §三 归档闸 |

**不动项**：`README.md:8` 的安装命令形态（是否补 `-a claude-code -g`）——属对外口径，须作者单独裁定；四项保留（P1-18 检查 D 重复、P2-13／P2-15／P2-16）随 112 批归档留档，本批不处置。

## 三、验收断言

1. 悬空编号零残留：`grep -cE "事故记录.*#" docs/standards/运行环境标准.md scripts/patch_file.py scripts/check_spec.py` → 期望三件全为 0。（**模式订正**：初稿写「事故记录 #」，漏了反引号形态 `事故记录 \`#33\`` 一处——改前基线实为三件各 1 处，非两件；**这是断言写法的订正，不是改期望求绿**。）
2. 配置指针实存：`grep -c "文档写作标准" .markdownlint-cli2.jsonc` → 期望 0。
3. 用词检查仍零：`python scripts/check_content.py --static 2>&1 | grep -c "用词待改"` → 期望 0。
4. 段号注释订正：`grep -c "第二段" scripts/patch_file.py` → 期望 1。（**模式订正**：目标文本含粗体 `**第二段**`，带粗体的字面模式命不中——改为只取去粗体后仍唯一的「第二段」三字。）
5. 钩子用词：`grep -c "门禁" scripts/hooks/pre-commit` → 期望 0。
6. 账本律例外已补：`grep -c "会话内呈报时不计" AGENTS.md` → 期望 1。
7. heredoc 条已入运行环境标准：`grep -c "heredoc" docs/standards/运行环境标准.md` → 期望 ≥1。
8. README 建设状态已标：`grep -c "未建" README.md` → 期望 ≥1。
9. 语法：`python -c "import ast,io;[ast.parse(io.open(f,encoding='utf-8').read()) for f in ['scripts/check_content.py','scripts/check_spec.py','scripts/patch_file.py']]"` → 期望退出 0。
10. 门禁全绿：`bash scripts/check.sh` → 期望退出 0。

## 四、规格自审（五条）

1. **位置现场核过**：第 1／2／3 项的悬空编号（`grep -n` 实测三处）、第 5 项两件互斥条文、第 8 项两表状态、第 13 项「6/6」与现存主文件数、第 16–27 项各脚本行号——均已现场核过。
2. **断言实跑过**：断言 1–5 与 9 改前实测基线（悬空编号 3 处／配置指针 1 处／用词候选 0／段号 0／门禁词 1 处／语法 OK）；断言 6–8／10 为改后目标值，**未预验**，交产物审查核验。
3. **禁改文件已写明**：本批不改 `docs/product/**`、`skills/**`、`docs/specs/archive/**`、`docs/specs/112-*.md`（已归档批）。
4. **范围与期望同源**：断言 1 的期望 0 由「删除三处编号引用」得出；断言 6／7／8 的期望 ≥1 由本批新增内容得出——均与改动清单同源。
5. **自述有守卫**：头注「模式：全量」由 `check_spec.py` 的模式判定核对核（件数 35 ＞10，自算一致）；「不动项」由不列入改动清单保证；「未预验」四字交产物审查逐条核。

## 五、需同步更新的文件核对

> 本批触检查脚本（`check.sh`／`check_content.py`／`check_spec.py`／`patch_file.py` 四件），按 `施工机制` §六 逐面表态。

| # | 面 | 结论 |
| --- | --- | --- |
| ① | `测试与验收标准` §1.1（已溶解） | **无需**——该件已随重构溶解，段序/段现状唯一出处＝`check.sh` 头注 |
| ② | `AGENTS.md` §七 自检信号 | **已同步**（第 25 项补 `check_spec` 路由行） |
| ③ | `check.sh` 头注（段序唯一出处） | **已同步**（第 17 项改述 `[5]` 段扫描面） |
| ④ | 工具模块 docstring | **已同步**（第 16／18／19／20／26 项，三件各自 docstring） |
| ⑤ | 各标准件 §三 脚本检查点表 | **已同步**（第 8 项 `产物契约标准` 状态订正） |
| ＋ | 缺口表 | **无需**——`守卫缺口清单` 已退役，该面不存在 |

## 六、禁止事项

- 禁止涉及：`docs/product/**`、`skills/**`、`docs/specs/archive/**`、`docs/specs/112-2026-09-17-自检器简化批.md`。
- 禁止动作：联网、装依赖、改断言期望值求绿、改 `README.md:8` 的安装命令形态（须作者单独裁定）、改 `check_spec.py` 的四项保留项。

## 七、被否决的替代方案

- **补回编号制事故记录表** → 否决：该表已退役（教训改走直接立法），重建等于把刚拆掉的第三态加回来。
- **`README.md:8` 一并改成实测形态**（补 `-a claude-code -g`）→ 否决：改为对外安装口径，须作者裁定（旧仓曾两处登记待定）。
- **把 P1-18／P2-13／P2-15／P2-16 一并做掉** → 否决：规格 112 §七 已登记为本批保留，理由在其处；跨批改会失去「当批欠账当批记」的账本纪律。

## 八、开放项

- `README.md:8` 安装命令形态待作者裁定。
- `check_content.py` 对 `skills/{名}/scripts/*.py` 的扫描缺口（第 17 项只收窄措辞、未扩 glob）——扩 glob 须同步 `check.sh [5]` 的零目标件判定，属独立小批。
- **承 112 批 §七 转入的三条**（112 归档时点名转入本批 §八）：① 检查 B（计数重算）在归档语料上误报偏高——继续观察，仍无真阳性则评估删除；② 检查 D 的 ①②③ 与 `check.sh [1]` 段 markdownlint 重复——同上；③ `--verify`／`--reconcile` 未进 `check.sh`——是否纳入提交前自动检查另议。

## 九、审查记录

- 2026-09-17｜成稿审查｜发现的问题 共 14：P0:1 P1:6 P2:7｜结论 修订后通过｜工具往返数 14 轮（27 次调用）｜周期时长 <1h
  - **P0-1**：#29 前提失实（严重度已改名为「严重／一般／轻微」，互引句亦已存在）→ **撤该项**。
  - P1 六项：`check_spec.py:412` 同病批号未清｜`运行环境标准:119` 会成新口径不一｜CHANGELOG 移出 TOC 面＝放松守卫（改为补目录）｜加粗密度行号错且新引已退役件｜断言 4 与目标粗体不匹配｜**缺「需同步更新的文件核对」表**（已补 §五）→ 均已订正。
  - P2 七项：引文失真 4 处（已按原文改）｜两处行号差 1｜「check.sh 全绿」实为 6 处非 5｜§一 对 `pre-commit` 的禁用词描述方向反向｜禁用清单「反模式 → 事故记录」需改指 → 均已订正（含新增第 36 项）。
- 2026-09-17｜产物审查｜发现的问题 共 5：P0:0 P1:3 P2:2｜结论 修订后通过｜工具往返数 19 轮（约 37 次调用）｜周期时长 <1h
  - 核对结论：39 项落实 37 项（漏 2），§三 断言 1–10 全过，守卫六段全绿。
  - P1-①：第 17 项漏改 `check.sh:154` 的第二处「仓级」，且未明示 skills 下 `.py` 不在面内 → 已补。
  - P1-②：第 27 项复算不实（写 6.0K，实测 7116）→ 改为 7.1K，并把旧史话数与现值分层表述。
  - P1-③：112 归档件点名「转入本批 §八」的三条未落 → 已补入 §八（三项）；另本规格与评审意见对「四项保留登记在 112 §七」的说法不实 → 已改述。
  - P2-④：第 39 项超出清单（`PATH_TOKEN` 写死 `scripts/hooks/pre-commit`）→ 改为**通则** `scripts/hooks/[\w.-]+`。
  - P2-⑤：`文字与命名标准:194` 的「见下 §7」是自指（该行即在 §七 内）→ 改为「见 §7 迁移做法」。
