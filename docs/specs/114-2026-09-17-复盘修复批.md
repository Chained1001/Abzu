# 114-2026-09-17-复盘修复批

> 模式：全量
> 必读输入：`docs/specs/archive/112-*.md` §七（四处保留）／`docs/specs/archive/113-*.md` §八（承转三项）、本件 §一 所列目标件

## 一、现状位置（现场核过）

- **缺口落点断裂（本次复盘新发现）**：`守卫缺口清单` 退役时立的口径是「已知限制写在各守卫脚本自身注释」，实测——`grep -c "已知限制" scripts/check_spec.py scripts/check_content.py scripts/patch_file.py scripts/check.sh` → **0／0／0／0**（四件全无该段）；`grep -rn "守卫缺口清单" scripts/` → **0 行**。而 112／113 两批的保留项（112 §七 4 条 ＋ 113 §八 承转 3 条）**全随规格归档进了只读区**，守卫脚本里无任何承接。
- **一致性无机械检查**：`施工机制` §五 立「自洽回扫」，但零守卫；本轮抓出的「模板节名 `改动清单` ↔ 工具 `文件级改动清单`」不一致即靠人撞出。
- **三处鲁棒性缺口**：`check_spec.py:1518` 的 `_git_changed(root) or []` 在 git 不可用时**静默按空改动继续**（核验模式会给出「清单外 0 件」的假安心）｜`:1611` 对目录输入报「文件不存在」（目录实存，措辞不准）｜`:991` 的「引文未在目标件出现」不区分**预期陈旧**（目标件已被后续批改动）与真异常。
- **成本不可见**：`施工机制` §二 的定模式弹窗只给相对倍数（`1×`／`2–3×`／`5–8×`）；本轮观测单次产物审查达 **1.14M token**（**本会话观测、仓内无存档**——批 C 审查记录只记工具往返数 19 轮／37 次调用，不含 token；仓内可引口径是第 10 项写入 `施工机制` 的「单次审查位 0.4–1.1M、全量模式合计 80–120 次调用／三百万级」）——作者无法据弹窗判断「这批值不值得走全量」。`§四` 交底虽点名「文件:节」，但**未给必读清单的体量上限**。

## 二、改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 依据／裁定 |
| --- | --- | --- | --- |
| 1 | `scripts/check_spec.py:27` 后 | docstring 新增**「已知限制」段**，回填 8 条：① 检查 B 在归档语料上误报偏高（无真阳性则评估删）② 检查 D ①②③ 与 `check.sh [1]` markdownlint 重复 ③ `--verify`／`--reconcile` 未进 `check.sh` ④ git 不可用时 `--verify` 仍静默按空改动（`--reconcile` 路已明示报错，见第 5 项）⑤ 相对路径按 cwd 归一，非仓根 cwd 调用时「本件」排除失配 ⑥ 每规格全读 `docs/specs`（O(N×1.9MB)）⑦ **正则硬编码节名**——机架复用时须改代码（`SECTION`／`CHANGE_SECTION`／`BAN_SECTION`／`ANCHOR_SECTION`／`NUCLEAR_SECTION`／`OPEN_SECTION`／`ARCHIVE_RECORD_SECTION`）⑧ `skills/{skill 名}/scripts/*.py` 不在检查面 | 复盘第七维；`守卫缺口清单` 退役时立的口径 |
| 2 | `scripts/check_content.py:31` 后 | docstring 新增「已知限制」段，回填 3 条：① `skills/{skill 名}/scripts/*.py` 不在扫描面 ② 加粗密度不实现（阈值未立法）③ ⑤ 的判据含正则近似（无标注的等价写法漏报） | 同上 |
| 3 | `scripts/patch_file.py:37` 后 | docstring 新增「已知限制」段，回填 1 条：临时件落目标同目录（非 `.tmp/`），信号中断（如 Ctrl-C）时无保底清理 | 同上 |
| 4 | `scripts/check.sh:12` 后（`set -u` 前） | 头注新增「已知限制」段，回填 2 条：① `skills/{skill 名}/scripts/*.py` 不在 `[5]` 段面 ② 收尾汇总的段计数对头注段行形态破坏无容错 | 同上 |
| 5 | `scripts/check_spec.py:1518` | 核验模式：`_git_changed` 返回 None 时**明示一行**（「git 不可用，改动面未核——结果不可信」）而非静默按空改动继续 | 复盘第四维 |
| 6 | `scripts/check_spec.py:1611` | 参数校验分两种措辞：`os.path.isdir(a)` 时报「是目录不是文件」；否则「文件不存在」 | 复盘第四维 |
| 7 | `scripts/check_spec.py:991` | 「引文未在目标件出现」的文案追加**陈旧提示**：「（若目标件已被本批或后续批改动，属预期陈旧，非缺陷）」 | 复盘第五维 |
| 8 | `scripts/check_spec.py`（`_checks()` 挂新检查） | 新增**检查 K**：**直读** `施工机制` §九 规格模板的节名，核**有模板对应**的正则是否仍命中——对应面＝`SECTION`（验收断言）／`CHANGE_SECTION`（改动清单）／`ANCHOR_SECTION`（现状位置）；**`BAN_SECTION`／`NUCLEAR_SECTION`／`ARCHIVE_RECORD_SECTION`／`OPEN_SECTION` 入白名单**（模板无对应节，属规格道可选节，不核）。命中失败即出候选 | 复盘第六维；事故出身＝本轮 `CHANGE_SECTION` 实撞；**成稿审查 P0：初稿核全部七个正则，而模板只有四节，BAN／NUCLEAR／ARCHIVE／OPEN 必不命中** |
| 9 | `scripts/check_spec.py`（`_checks()`） | 检查 K 的节名**从 `docs/specs/施工机制.md` §九 原文现场读取**（不抄进工具）——抄进工具即「比对自指」，模板改了照样漂。读不到该件时**明示跳过**（不静默） | 随第 8 项；成稿审查 P1-7 |
| 10 | `docs/specs/施工机制.md` §二 弹窗 | 第 4 项补**成本量级**：三选项 description 追加「约 N 次工具调用／M token 量级」（按件数与目标件体量估） | 复盘第三维 |
| 11 | `docs/specs/施工机制.md` §四 交底 | 审查位版第 1 项与施工位版第 1 项各补**必读清单体量上限**：「点名面 ≤ 10 万字符（口径引 `check_content.py` 既有的 `109` 批实测：点名面上限 ≈10.7 万字符/实例）；超限须先收窄点名节」 | 复盘第三维；**成稿审查 P1-9：初稿的「5 万字符」无出处且与仓内唯一实测互斥** |
| 12 | `docs/specs/施工机制.md` §六 | 「改检查项同步两处」扩为**三处**：补「该脚本自身的『已知限制』段」 | 随第 1–4 项；本条为**新增面**（原 §六 只列 `check.sh` 头注与工具 docstring 两处，未含「已知限制」段） |
| 14 | `scripts/check_spec.py` 的「八类」枚举 8 处（`:1`／`:5`／`:16`／`:29`／`:33`／`:1382`／`:1384`／`:1385`）＋`scripts/check.sh:10`＋`AGENTS.md:113` | 加检查 K 后各类枚举同步为**九类**（A–E、H–K） | 随第 8 项；**成稿审查 P1-6：初稿漏此连带面** |
| 13 | `scripts/check_spec.py`（`check_reconcile()` 的模式判定） | **件数口径修正**：`n_t = len(targets)`（去重文件数）→ `len(rows)`（改动清单**条目数**）——与 `施工机制` §二 的「件数」定义一致。本规格自测：改前实算「标准」（去重文件 5 件）与头注「全量」不符，改后按清单条数实算「全量」相符（实测「模式判定核对不符 0 件」） | 写规格期实测；复盘第六维 |

**不动项**（明确不做，理由见 §六）：正则节名改为可配置（无事故出处的预立法）；`skills/{名}/scripts/*.py` 扩 glob（须同步零目标件判定，属独立小批）；README 安装命令形态（待作者裁定）。

## 三、验收断言

1. 四脚本均有「已知限制」段：`grep -c "已知限制" scripts/check_spec.py scripts/check_content.py scripts/patch_file.py scripts/check.sh` → 期望四件均 ≥1。
2. 检查 K 存在且能跑：`grep -c "检查 K" scripts/check_spec.py` → 期望 ≥1。
3. 检查 K 在模板对齐时静默：`python scripts/check_spec.py --static docs/specs/archive/111-2026-09-17-用词规范与禁用清单批.md 2>&1 | grep -c "检查 K"` → 期望 0。（**范围**：只核 `SECTION`／`CHANGE_SECTION`／`ANCHOR_SECTION` 三个**有模板对应**的正则——实测模板节名＝现状位置／改动清单／验收断言／规格自审，三者均可命中。）
4. git 不可用不静默：`grep -c "改动面未核" scripts/check_spec.py` → 期望 ≥1。
5. 目录当规格措辞：`grep -c "是目录不是文件" scripts/check_spec.py` → 期望 ≥1。
6. 检查 I 陈旧提示：`grep -c "预期陈旧" scripts/check_spec.py` → 期望 ≥1。
7. 弹窗成本量级：`grep -c "token 量级" docs/specs/施工机制.md` → 期望 ≥1。
8. 交底体量上限：`grep -c "10 万字符" docs/specs/施工机制.md` → 期望 ≥1。
9. 语法：`python -c "import ast,io;[ast.parse(io.open(f,encoding='utf-8').read()) for f in ['scripts/check_spec.py','scripts/check_content.py','scripts/patch_file.py']]"` → 期望退出 0。
10. 门禁全绿：`bash scripts/check.sh` → 期望退出 0。

## 四、规格自审（五条）

1. **位置／计数／写入位置三件现场核过**：第 1 项的四个脚本「已知限制」实测计数（1／0／0／0）、第 5 项的 `:1518` 行、第 6 项的 `:1611`、第 7 项的 `:991`、第 10／11 项的插入位置——均已现场核过。
2. **断言实跑过**：断言 1／2／4／5／6／7／8 改前基线实测（1／0／0／0／0／0／0）；断言 3 改前实测 0（检查 K 尚不存在）；断言 9／10 改前为已知值（语法 OK／门禁绿）。断言 3 为改后目标值，**未预验**。
3. **禁改文件已写明**：本批不改 `docs/product/**`、`skills/**`、`docs/standards/**`、`docs/specs/archive/**`、`README.md`、`CHANGELOG.md`。
4. **范围与期望同源**：断言 1 的期望 ≥1 由「四件各新增一段」得出；断言 3 的期望 0 由「当前模板节名与工具正则逐条对齐」得出——均已逐条核过模板节名与正则的对应关系。
5. **自述有守卫**：头注「模式：全量」由 `check_spec.py` 的模式判定核对核（件数 **14** ＞10，实测「模式判定核对不符 0 件」）；「不动项」由不列入改动清单保证；「未预验」三字交产物审查核。

## 五、需同步更新的文件核对

| # | 面 | 结论 |
| --- | --- | --- |
| ① | `测试与验收标准` §1.1（已溶解） | **无需**——该件已溶解，段序唯一出处＝`check.sh` 头注 |
| ② | `AGENTS.md` §七 自检信号 | **无需**——本批不改既有检查项，只新增检查 K（八类→九类，但 `§七` 只写「[4] 段 A–E、H–J 无未核项」的粗粒度指认） |
| ③ | `check.sh` 头注（段序唯一出处） | **已同步**（第 4 项补「已知限制」段） |
| ④ | 工具模块 docstring | **已同步**（第 1／2／3 项） |
| ⑤ | 各标准件 §三 脚本检查点表 | **无需**——本批不改既有检查的判定标准；检查 K 是对规格自检器自身的元检查，不落标准件锚点表 |
| ＋ | 缺口表 | **无需**——该面已由第 1–4 项的「已知限制」段承接（这正是本批要修的目标） |

## 六、禁止事项

- 禁止涉及：`docs/product/**`、`skills/**`、`docs/standards/**`、`docs/specs/archive/**`、`README.md`、`CHANGELOG.md`。
- 禁止动作：联网、改断言期望值求绿、把正则节名改为可配置（无事故出处的预立法）、扩 `skills/{名}/scripts/*.py` 的 glob（须同步零目标件判定）。

## 七、被否决的替代方案

- **把 `*_SECTION` 正则改为从 `施工机制` 读节名（可配置）** → 否决：机架复用尚未发生，属无事故出处的预立法（`AGENTS.md` §四 内生必要性自问第一条）；改为**记入「已知限制」段**，机架复用那个批次再动。
- **重建一张缺口清单** → 否决：那正是本轮拆掉的第三态；「已知限制写在各脚本自身」才是债跟主题走。
- **扩 `skills/{名}/scripts/*.py` 进 `[5]` 段** → 否决：须同步改动零目标件判定与收尾汇总，属独立小批，本批只明示。

## 八、开放项

- 归 `运行环境标准` §脚本契约：`skills/{skill 名}/scripts/*.py` 的扫描缺口（`check.sh [5]` 与 `check_content`）——扩面须同步零目标件判定，另批。
- 归 `运行环境标准` §宿主与安装：`README.md:8` 安装命令形态——待作者裁定。
- 归 `施工机制` §六 机器检查：检查 K 目前只核「工具正则 ↔ 模板节名」一面；`施工机制` 其它节的引用面未纳——先观察是否够用。

## 九、审查记录

- 2026-09-17｜成稿审查｜发现的问题 共 13：P0:1 P1:8 P2:4｜结论 修订后通过｜工具往返数 19 轮（29 次调用）｜周期时长 <1h
  - **P0-1**：检查 K 初稿核**全部七个** `*_SECTION` 正则，而 `施工机制` §九 模板只有四节——`BAN`／`NUCLEAR`／`ARCHIVE`／`OPEN` **必不命中**，断言 3 的「期望 0」不可能成立 → 改为**只核有模板对应的三个**＋其余入白名单。
  - P1 八项：§一 的缺口计数不实（实为四件全 0，初稿写「check_spec 1 处」「7 条」）｜§四.1 同错｜件数 12→13（后随第 14 项→14）｜第 13 项缺「§一 现状位置」行且未标「判因期已改」｜**第 8 项漏连带面**（「八类」枚举 8 处＋`check.sh:10`＋`AGENTS.md:113`）→ 补为第 14 项｜第 9 项「清单抄进工具」＝比对自指 → 改为**现场读 §九 原文**｜第 12 项依据不实 → 改述为新增面｜**「≤5 万字符」无出处且与仓内唯一实测（10.7 万）互斥** → 改引既有口径。
  - P2 四项：第 1 项 ④ 与第 5 项互斥（`--reconcile` 已明示）→ 改述｜「1.14M token」无仓内出处 → 改述｜第 2 项条目序号错（①②③⑤）→ 订正｜三处插入点落在段落中 → 行号订正。

- 2026-09-17｜产物审查｜发现的问题 共 4：P0:0 P1:2 P2:2｜结论 修订后通过｜工具往返数 12 轮（约 20 次调用）｜周期时长 <1h
  - 核对结论：§三 断言 1–10 全过，`--reconcile` **清单外 0 件**，守卫六段全绿，检查 K 实跑 `check_template_names('.') → ([], [])`。
  - **P1-①**：第 14 项漏两处「八类」——`check_spec.py:1` 与 `:5`；且 `:7–14` 的 A–J 枚举**缺 K 条目**（而 docstring 自称「各项判定标准见 docstring」）→ 已补「九类」两处 ＋ 补 `**检查 K**` 条目。
  - **P1-②**：断言 8 与第 11 项互斥（断言写 `5 万字符`≥1，第 11 项已改为 `10 万字符`；实跑 `5 万字符`→0）→ 断言 8 改 `10 万字符`，实跑 2 ≥1。
  - **P2-③**：`check_spec.py:39` 已知限制 ④ 仍写「`--verify` 仍静默按空改动继续」——与第 5 项已改后的实况相反 → 改述为「已 stderr 明示 ＋ 置退出 1（残留：声明面不可核）」，并新增 ⑩ 记检查 K 单向核。
  - **P2-④**：`:29` 退出契约只记「过程产物缺失」一条阻塞源，漏第 5 项新增的「git 不可用」→ 已补。
  - 附带订正（审查未列、自查发现）：§一 的「1.14M token／批 C 实测」**仓内无存档** → 改标「本会话观测、仓内无存档」并指既有口径。
