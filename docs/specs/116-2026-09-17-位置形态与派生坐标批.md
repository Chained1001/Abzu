# 116-2026-09-17-位置形态与派生坐标批

> 模式：全量
> 必读输入：`docs/specs/施工机制.md` §九（模板与断言写法）；`scripts/check_spec.py` 模块 docstring 与 `check_anchor()`／`_anchor_probe()`／`_change_position_probe()`；`AGENTS.md` §五（分工原则）／§七（自检信号）。

## 一、现状位置（现场核过）

> **本规格的 §一／§二 位置一律按「文件 ＋ 节或函数 ＋ 唯一字符串」书写，不写行号**——这正是本批要立的形态，先在本规格上试行（同时也是检查 L 的「新形态静默」控制）。下方每条引文均经 `grep -c` 现场核过。

- `docs/specs/施工机制.md` §九 规格模板｜「- {文件}:{行}：现文「{原文}」」：§一 位置行现形态——`grep -c '{文件}:{行}' docs/specs/施工机制.md` → 1。
- `docs/specs/施工机制.md` §二｜「形如「文件:位置」」：**「件数」定义**引用了该列形态——`grep -c '文件:位置' docs/specs/施工机制.md` → 2（本行与下行 表头 两处）。
- `docs/specs/施工机制.md` §九 规格模板｜「| # | 文件:位置 | 改动（现状 → 目标） | 依据／裁定 |」：§二 表头现形态——同上命令的第 2 处。
- `docs/specs/施工机制.md` §九 规格模板｜「| 1 | {路径}:{行} | {现状} → {目标} | {理由} |」：§二 位置格现形态——`grep -c '{路径}:{行}' docs/specs/施工机制.md` → 1。
- `scripts/check_spec.py` 检查 I｜「for m in ANCHOR_FILELINE.finditer(raw):」：§一 腿的探针驱动处——`grep -c 'ANCHOR_FILELINE.finditer' scripts/check_spec.py` → 1。
- `scripts/check_spec.py` 检查 I｜「refs += _change_position_probe(text, root, seen, p2_cands)」：§二 腿调用处——`grep -c '_change_position_probe(text, root, seen, p2_cands)' scripts/check_spec.py` → 1。
- `scripts/check_spec.py` 模块 docstring｜「- **检查 M** §四 规格自审」：检查项清单末条——`grep -c '检查 M\*\* §四 规格自审' scripts/check_spec.py` → 1。
- `scripts/check.sh` 头注｜「# [4] 规格静态自检（检查 A–E、H–M 十类」：`grep -c 'A–E、H–M 十类' scripts/check.sh` → 1。
- `AGENTS.md` §七｜「[4] 段 A–E、H–M 无未核项」：`grep -c 'H–M 无未核项' AGENTS.md` → 1（**该行的区间记法已覆盖检查 L，本批不需改**）。
- `docs/specs/施工问题记录.md` §二 已总结区｜「形态侧（行号改唯一字符串）另立批 116」：本批即该行所指——`grep -c '另立批 116' docs/specs/施工问题记录.md` → 1。
- **另有 6 处散文描述该列／该腿的形态**（第 8 项的连带落点，成稿审查 P2-⑤ 指出），全在 `scripts/check_spec.py`：模块 docstring 的检查 I 条目、检查 I 的分节注释、`check_anchor` docstring 首句、同 docstring 的 §二 腿句、§二 腿的分节注释、`_declared_paths()` docstring 的位置列描述——命令：`grep -c '文件:位置\|路径:行号\|文件:行号' scripts/check_spec.py` → 6。（**本规格不写这 6 处的行号**——正是本批要立的形态。）

**派生坐标的实测面**（各件 §一／§二 内的 `文件:行号` 处数，命令＝本批检查 L 的判据形态）：

| 件 | 处数 |
| --- | --- |
| `docs/specs/archive/113-2026-09-17-评审余项收尾批.md` | 62 |
| `docs/specs/archive/112-2026-09-17-自检器简化批.md` | 19 |
| `docs/specs/114-2026-09-17-复盘修复批.md` | 10 |
| `docs/specs/archive/111-2026-09-17-用词规范与禁用清单批.md` | 7 |
| `docs/specs/archive/110-2026-09-17-重构产物审核整改批.md` | 0 |

（命令：逐件取 §一／§二 两节、围栏外，按 `文件:数字` 计数——本批 §三 断言 6／7 以 115 与 116 两件作控制。存量面即上表；新形态下应为 0。）

**检查 I 在新形态下会空转**（本批须双读的理由）：§一 腿的探针由 `ANCHOR_FILELINE`（`` `文件.md:数字` ``）与 `ANCHOR_BARELINE`（`` `:数字` ``）驱动——新形态不含行号 ⇒ 两者皆不命中 ⇒ 该腿对每件 `refs` 计 0、零核。实测 `ANCHOR_FILEONLY`（`` `文件.md` ``）**只认 `.md` 扩展名**，`.py`／`.sh` 件在新形态下连文件上下文都建不起来——`grep -c "ANCHOR_FILEONLY = " scripts/check_spec.py` → 1（该行为 `.md` 专用模式）。

**枚举落点逐行清点**（115 产物审查 P1-② 的教训：枚举漏改是同批再犯的高发面）：

- 「十类」：`scripts/check_spec.py` 8 行（模块头注首句／用途句／非阻断句／退出契约句／呈报守恒句（该行两处）／`static_report` docstring 三处）＋ `scripts/check.sh` 头注 1 行 ＝ **9 行 10 次**。
- 斜杠枚举需补 `L` 者 5 行：呈报守恒的候选面、`check_audit` docstring 的「同 A／B／C／D 做法」、`static_report` docstring 的两处全枚举、`extra` 注行。
- **不动的两处**：`static_report` docstring 的「E／H／I／J／K 的汇总行不计入」（检查 L **无汇总行**，同 M）与中性行注的「检查 B／C／D／E 输出」（与 L 无关）。
- `AGENTS.md` 的 `A–E、H–M` **已覆盖 L**（H–M 含 L），不需改。

## 二、改动清单

| # | 文件｜位置 | 改动（现状 → 目标） | 依据／裁定 |
| --- | --- | --- | --- |
| 1 | `docs/specs/施工机制.md` §九 规格模板｜「- {文件}:{行}：现文「{原文}」」 | §一 位置行 →「- `{文件}` §{节或函数}｜「{唯一字符串}」：现文「{原文}」；实测计数 {N}（命令：{命令}）。」**文件路径须写进反引号跨度**（产物审查 P1-①：检查 I 的文件上下文由反引号建立，不写则新腿 0 命中、打印「无发现」——「静默空转」换个形态又回来） | 位置是**派生坐标**：规格按定义要改目标件，行号在改动落地后失效 |
| 2 | `docs/specs/施工机制.md` §九 规格模板｜「文件:位置」与「{路径}:{行}」 ＋ §二｜「形如「文件:位置」」 | §二 表头 →「文件｜位置」；位置格 →「{路径} §{节}｜「{唯一字符串}」」；**§二「件数」定义句里的「形如「文件:位置」」同步改为「形如「文件｜位置」」**（成稿审查 P1-①：该句是同一形态的第二处，漏改即令定义与模板相抵） | 同 1；列角色按表头别名定位，含「文件」二字即仍可解析 |
| 3 | `docs/specs/施工机制.md` §九｜「**断言写法七条**」之前 | 新增「**位置形态**」句：位置一律「文件 ＋ 节或函数 ＋ 唯一字符串」、**不写行号**，并写明判定标准（`检查 L` 报残留行号；唯一字符串须在该件内 `grep -c` 恰 1 次） | 条文立法须给理由与守卫（`施工机制` §五 条文立法条） |
| 4 | `scripts/check_spec.py` 模块 docstring｜「- **检查 M** §四 规格自审**留痕表**核对」 | 其后补「**检查 L**」条目 | 新增检查须入清单 |
| 5 | `scripts/check_spec.py`｜`CHANGE_POS` 常量的注释块 | 补 `check_derived_pos()`（检查 L）：**命中模式复用 `CHANGE_POS`**（其形态＝可选反引号 ＋ 路径 ＋ 可选反引号 ＋ 冒号 ＋ 数字，扩展名白名单为 `md`／`py`／`sh`／`js`／`jsonc`／`json`；不在规格内抄全文，抄即比对自指）；**节定位复用 `ANCHOR_SECTION`／`CHANGE_SECTION`**；逐行去重、15 条截断、**无汇总行**；**不扫 §三**（断言可执行、自纠错）与 **§九**（引述外部发现的行号是事实）。实测该模式：115→17／116→0／113→62（成稿审查 P1-③ 指出未定式则断言 6 不可达） | 第 3 项的守卫；判据＝§九「位置形态」句 |
| 6 | `scripts/check_spec.py`｜`static_report()` 的「extra += …」行 | 接入检查 L（逐件，候选并入 `extra`） | 新检查须入编排 |
| 7 | `scripts/check_spec.py`｜检查 I 的 §一 腿 | **双读**：原行号探针保留（旧形态）；新增「文件 ＋ 引文」腿——无行号而有引文时，核该引文是否在目标件内出现；**并加「位置无文件上下文」明示支**（位置条目有引文却无可解析件名时**报候选**、不静默通过——把静默变成可见，产物审查 P1-①）。并**放宽 `ANCHOR_FILEONLY` 的扩展名**到 `.md／py／sh／js／jsonc／json`（现只认 `.md`，新形态下 `.py`／`.sh` 件建不起文件上下文） | **甲不能裸改**：不改这处，新形态下 §一 腿静默空转＝废掉既有守卫（「删符号未查消费链」族） |
| 8 | `scripts/check_spec.py` 8 行 ＋ `scripts/check.sh` 头注｜「十类」 | 「十类」→「十一类」（9 行 10 次）；斜杠枚举需补 `L` 的 5 行补上（汇总面与中性行面**不动**）；`:29` 维护入口补「检查 L 改 check_derived_pos()」；**另补 6 处描述该列／该腿形态的散文**（**按内容定位、不写行号**：模块 docstring 的检查 I 条目、检查 I 的分节注释、`check_anchor` docstring 首句、同 docstring 的 §二 腿句、§二 腿的分节注释、`_declared_paths()` docstring 的位置列描述；见 §一）——双读后其中 4 处须同时述旧形态与新形态（另 2 处只涉 §二 腿，不变） | 连带面；落点已逐行清点（见 §一，成稿审查 P2-⑤ 补 6 处） |
| 9 | `scripts/check_spec.py` 模块 docstring「已知限制」段｜「⑫ 检查 I 的 §二 腿」 | 补 ⑬：检查 L 只扫 §一／§二；**存量规格候选量大**（113 实测 62 处）——判据是「新写的不许有」，不是要清存量；**漏报形态已知三类**（成稿审查 P2-⑦）：行号前置（`259：路径`）、路径含空格或 `~`、扩展名不在白名单者，按 `CHANGE_POS` 均不命中 | `施工机制` §六 改检查项须同步三处之③ |
| 10 | `docs/specs/施工问题记录.md` §二 已总结区｜「形态侧（行号改唯一字符串）另立批 116」 | 去向句更新为「形态侧已于批 116 落地（`施工机制` §九 位置形态 ＋ 检查 L）」 | 去向指针须可检索（该表 §四 去向写法） |
| 11 | `scripts/check_spec.py`｜`COST_PLACEHOLDER` 常量 | 占位词表增一个「待回填」（现 6 词） | 成稿审查 P1-④：本规格 §四 曾填「待回填」而检查 M **放行**——词表漏词即留痕表可放假值；判定标准与实例须同批（`施工机制` §五 条文立法条） |
| 12 | `docs/specs/116-2026-09-17-位置形态与派生坐标批.md` | 新增本规格 | — |

## 三、验收断言

1. 模板 §一 已改形态：`grep -c '§{节或函数}' docs/specs/施工机制.md` → 期望 ≥1。
2. 模板不再要求行号：`grep -c '{文件}:{行}' docs/specs/施工机制.md` → 期望 0。
3. 模板 §二 位置列已改：`grep -c '文件｜位置' docs/specs/施工机制.md` → 期望 ≥1。
4. 位置形态句已落：`grep -c '位置形态' docs/specs/施工机制.md` → 期望 ≥1。
5. 检查 L 已入清单：`grep -c '检查 L' scripts/check_spec.py` → 期望 ≥1。
6. 检查 L 对旧形态出候选：`python scripts/check_spec.py --static docs/specs/115-2026-09-17-自审留痕与位置核批.md 2>&1 | grep -c "· 检查 L"` → 期望 ≥1。
7. 检查 L 对本规格静默（新形态）：`python scripts/check_spec.py --static docs/specs/116-2026-09-17-位置形态与派生坐标批.md 2>&1 | grep -c "· 检查 L"` → 期望 0。
8. 检查 I 双读已落：`grep -c '新形态' scripts/check_spec.py` → 期望 ≥2。
9. `ANCHOR_FILEONLY` 已放宽：`grep -c 'ANCHOR_FILEONLY = .*py' scripts/check_spec.py` → 期望 1。
10. 枚举同步（「十类」归零）：`grep -rc '十类' scripts/check_spec.py scripts/check.sh | grep -c ':0'` → 期望 2。
11. 斜杠枚举已含 L：`grep -c 'J／K／L／M' scripts/check_spec.py` → 期望 ≥2。
12. `AGENTS.md` 不需改（回归控制）：`grep -c 'A–E、H–M' AGENTS.md` → 期望 1。
13. 检查 L 截断线可触发：`python scripts/check_spec.py --static docs/specs/archive/113-2026-09-17-评审余项收尾批.md 2>&1 | grep -c "· 检查 L 另有"` → 期望 1。
14. 语法：`python -c 'import ast,io;ast.parse(io.open("scripts/check_spec.py",encoding="utf-8").read())'` → 期望退出 0。
15. 检查 L 无汇总行（与 M 一致）：`python scripts/check_spec.py --static docs/specs/115-2026-09-17-自审留痕与位置核批.md 2>&1 | grep -c "· 检查 L 汇总"` → 期望 0。
16. 新形态腿能抓坏引文（夹具法，第 7 项正向控制）：`mkdir -p .tmp/116 && python -c "import io;B=chr(96);N=chr(10);H=chr(35);io.open('.tmp/116/q_bad.md','w',encoding='utf-8').write('> 模式：全量'+N+N+H+H+'一、现状位置（现场核过）'+N+N+'- '+B+'scripts/check.sh'+B+' §头注｜「这一段在目标件里绝不存在」。'+N)" && python scripts/check_spec.py --static .tmp/116/q_bad.md 2>&1 | grep -c "引文未在目标件出现（新形态）"` → 期望 1。
17. 新形态腿对真引文静默（夹具法，第 7 项负向控制）：同法以 `q_good.md`（引文换为 `scripts/check.sh` 头注里真实存在的「一键检查（提交前必跑）」）→ `grep -c "引文未在目标件出现（新形态）"` → 期望 0；跑完删 `.tmp/116/`。
18. 一键检查全绿：`bash scripts/check.sh` → 期望退出 0。

**改前预验实测**（断言写法①）：1→0｜2→**1**｜3→0｜4→0｜5→0｜6→0｜7→0｜8→0｜9→0｜10→0｜11→0｜12→1｜13→0｜14→0｜15→0｜16→0（该腿尚不存在）｜17→0｜18→0。

## 四、规格自审（五条·留痕表）

| 项 | 核法与命令 | 实测输出摘要 | 判定 |
| --- | --- | --- | --- |
| 1 位置／计数／写入位置三件现场核过 | 逐条 `grep -c` 取证（§一 八条）｜派生坐标面按件计数 | 八条引文各得 1；派生坐标面 113→62／112→19／114→10／111→7／110→0；枚举落点 9 行 10 次 ＋ 斜杠 5 行已逐行清点 | 通过 |
| 2 断言实跑过 | 逐条跑 §三 十八条（改前预验，见 §三 末行）｜两向夹具（坏引文／真引文）按断言字面整条跑 | 改前：1–6、8–11、13、16 全红；7／12／14／15／17／18 本为绿。改后复跑：**18／18 通过** | 通过 |
| 3 禁改文件已写明 | 逐件指认 §六 | §六 列 6 项禁改面 | 通过 |
| 4 范围与期望同源 | 逐条指认 §三 ← §二 | 断言 1–4 源第 1–3 项；5–7、13、15 源第 4–6 项；8–9、16、17 源第 7 项；10–12 源第 8 项；**第 9 项（已知限制 ⑬）由断言 5／8 间接覆盖、第 10 项（记录表去向句）无专用断言（文本类，交产物审查读）**；14／18 为回归 | 通过 |
| 5 自述有守卫 | 逐句指认 | 「模式：全量」由检查 E 模式判定核对核（件数 **12** ＞10，机械可达）；「不写行号」由检查 L 兜；「检查 I 不空转」由断言 8／9 兜；「唯一字符串须恰 1 次」由 `grep -c` 判定标准写进条文、守卫待另批（记 §八） | 通过 |

## 五、需同步更新的文件核对

| # | 面 | 结论 |
| --- | --- | --- |
| ① | `check.sh` 头注（段序唯一出处） | **已同步**（第 8 项：十类→十一类） |
| ② | `AGENTS.md` §七 自检信号 | **无需**——该行记法是 `A–E、H–M`，区间已覆盖检查 L（实测不改） |
| ③ | 工具模块 docstring（含维护入口行与「已知限制」段） | **已同步**（第 4／8／9 项） |
| ④ | `施工机制` §九 断言写法七条 | **无需**——本批新增的是「位置形态」句，两条并列不合并（写法七条本身不改） |
| ⑤ | 各标准件 §三 脚本检查点表 | **无需**——检查 L 是规格自审器对规格自身形态的元检查 |
| ⑥ | `施工问题记录` §二 去向句 | **已同步**（第 10 项） |

## 六、禁止事项

- 禁止涉及：`docs/product/**`、`skills/**`、`docs/standards/**`、`docs/specs/archive/**`（只读引用可，不得回改存量行号）、`README.md`、`CHANGELOG.md`。
- 禁止动作：联网；改断言期望值求绿；**回改存量规格的行号**（判据是「新写的不许有」，不是清存量）；让检查 L 扫 §三／§九；给检查 L 加汇总行（会牵动汇总面枚举，与 M 保持一致的做法是不加）；把「数字随附命令」立成守卫（见 §七）。

## 七、被否决的替代方案

- **甲 裸改模板、不同批改检查 I** → 否决：新形态不含行号 ⇒ 检查 I §一 腿的探针（`ANCHOR_FILELINE`／`ANCHOR_BARELINE`）一次也不触发，等于静默废掉一个既有守卫——正是「删符号未查消费链」族的形态。故第 7 项同批做双读。
- **把检查 I §一 腿直接改成新形态（废弃旧探针）** → 否决：存量 5 件（111／112／113／114／115）仍是旧形态，废弃即对它们零核；**双读**是仓内既有做法（`文字与命名标准` §7 双读窗口）。
- **「数字随附命令」立成守卫（本批原计划的丙）** → **否决，实测证伪**：一般式（「§一 行含阿拉伯数字但无『命令：』」）在 115／113／112 三件的命中率是 19/19、8/8、3/4——它实际测的是「§一 有没有引用位置」，判别力为零；而模板对 §一 计数**已**要求「实测计数 {N}（命令：…）」，无需新守卫。撤出本批，记 §八。
- **要求唯一字符串「恰 1 次」并本批建守卫** → 否决（本批）：须按列角色定位位置格并新增解析面，属独立改动面；且要求面在无行号时才有对象，须待存量消化。先写进条文，守卫记 §八。
- **回改 111／112／113／114／115 的行号** → 否决：归档是只读历史，「当时的行号在当时是对的」是事实；回改反抹掉沿革。检查 L 对它们的候选即存量面，只呈报。

## 八、开放项

- 归 `施工机制` §九 位置形态：**「唯一字符串恰 1 次」目前无守卫**——须按列角色定位位置格并新增解析面，另批；本批只把该要求写进条文。
- 归 `施工机制` §九 位置形态：「数字随附命令」撤出本批（实测判别力为零，见 §七）——若要立，须先想出有判别力的形态（例如只核「实测计数 {N}」这一具体声称形态）。
- 归 `scripts/check_spec.py` 已知限制：检查 L 只扫 §一／§二；§三 断言里的行号（夹具与命令）不报——断言可执行，写了行号一跑即红。
- 归 `施工机制` §九 模板：检查 I 的 §二 腿在本批后仍是「行号越界／空行」判据——新形态下它无对象（新规格 §二 不含行号）；是否改核「唯一字符串唯一性」随上一条另批。
- 归 `scripts/check_spec.py` 检查 J：实测**缺 §九 审查记录节时完全静默**（承 115 §八，未动）。
- 归 `scripts/check_spec.py` `static_report()` 的「有预警 N 条｜分类：」尾：**分类只统计五类**（`warn`／`zero`／`undecided`／`uncountable`／`sentinel`），**不含 `extra` 与 `counts`**——故发现全来自 `extra` 时（如检查 M／J 行）条数有而分类为空（本批实测 3 条 / 分类空）。明细行本身照常打印，属**分类尾的口径不全**，非计数错；修它要动 `_cls` 的构成，另批。

## 九、审查记录

- 2026-09-17｜成稿审查｜发现的问题 共 7：P0:0 P1:4 P2:3｜结论 修订后通过｜工具往返数 20｜周期时长 <1h
  - **P1-①**：`施工机制` §二「件数」定义那句「形如「文件:位置」」是同一形态的**第二处**，漏改即令定义与模板相抵 —— 实测 `grep -c '文件:位置'` 真值 **2**（:43 与表头），规格原写 1 → 已补入第 2 项落点。
  - **P1-②**：§一 第 3 条的取证命令用单字符 `.` 通配 `*`，实测 0 命中 ⇒ 「八条引文各得 1」不实 → 改用 `检查 M\*\* §四` 并回填实测值。
  - **P1-③**：检查 L 的**判据未定式**——只写「扫 §一／§二 的 `文件:行号`」而未给正则；实测若按 `ANCHOR_FILELINE` 形态则 115 命中 **0** ⇒ 断言 6 不可能成立，按 `CHANGE_POS` 才 115→17／116→0／113→62 → 已写定为「复用 `CHANGE_POS` ＋ 复用 `ANCHOR_SECTION`／`CHANGE_SECTION`」。
  - **P1-④**：§四 项 2 填「待回填」却判「通过」，而 `COST_PLACEHOLDER` **不含**该词 ⇒ 留痕表可放假值 → 已填实测值，并立为**第 11 项**（占位词表补「待回填」）。
  - P2 三项：⑤ 另漏 **6 处描述该列／该腿形态的散文**（`check_spec.py`）→ 补入第 8 项落点；⑥ §四 项 4 未覆盖第 9／10 项 → 补；⑦ 检查 L 的**漏报形态**（行号前置／路径含空格或 `~`／非白名单扩展名）→ 记已知限制。
  - 观察项：①B 面全实（八条引文、五个数、枚举 9 行 10 次与斜杠 5 行、两处「不动」皆复现）②改表头为「文件｜位置」未损 C／E／K 与 `_declared_paths` ③双读两向均无回归 ④两条 `--static` 预期属实 ⑤§四 件数 11 与实测 11 行同源。
- 2026-09-17｜产物审查｜发现的问题 共 7：P0:0 P1:2 P2:5｜结论 修订后通过｜工具往返数 19｜周期时长 <1h
  - **P1-①（本批最重）**：新模板的 `{文件}` **没加反引号**，而检查 I 的文件上下文由 `ANCHOR_FILEONLY`（要求反引号）建立 ⇒ **照模板字面写，新腿 0 命中、打印「无发现」**——本批要修的「静默空转」在模板字面形态下**依然存在**。复现：无反引号 → `位置引用 0 处｜异常 0 项`；加反引号 → `1 处`。→ 两处对策：模板把 `{文件}`／`{路径}` 写进反引号跨度并写明理由；工具**加「位置无文件上下文」明示支**（有引文却无可解析件名时报候选，不静默通过）。改后复测：无反引号 → `1 处／异常 1`（可见）。
  - **P1-②**：§二 第 8 项自身写了 **6 个裸行号**，与 §一「本规格不写这 6 处的行号」相抵，且落地后 5/6 已失效 → 改为按内容定位。
  - P2 五项：③ 已知限制缺第四类漏报（同行已给件名、行号裸写 `:N`）→ 补；④ **新腿＋放宽改了存量旧形态规格的检查 I 结果**（115 由 6 引用／0 异常 → 19／12、113 由 4→5；多为 `.py`／`.sh` 件此前建不起上下文）→ 记入已知限制 ⑬；⑤ 检查 L 的围栏识别照抄 `FENCE_BLOCK`（info-string 只认纯字母），```` ``` python ```` 内行号被误报 → 另取宽松形态 `FENCE_LOOSE`，夹具复测得 0；⑥ 新腿静默面（引文 <6 字、`###` 级标题不命中「`^##` ＋ 一个空格」、路径不实存只计引用）→ 记入已知限制 ⑬；⑦ 已知限制里「113 实测 62 处」是**出现次数**口径，检查 L 实际按 token 去重报 16 → 补两种口径说明。
  - 观察项：断言 1–18 逐条字面实跑 **18/18 通过**；§一 八条引文与「十类 9 行 10 次」按 HEAD 复算全对、两处「不动」确未动；116 的 6 条 stale **全属预期陈旧**（4 条源第 2 项／1 条第 8 项／1 条第 10 项）；表头改形态未损 C／E／K；占位词表 6→7 ✓。
  - **产物审查后自查又逮一条同类**：`ANCHOR_FILELINE` 的扩展名也写死 `.md`（我只放宽了 `ANCHOR_FILEONLY`）——故 `.py`／`.sh` 件的**旧形态**位置此前一律不核，且我新加的「无文件上下文」明示支会在 114 那类「单行打包三处位置」的行上**误报**。对称放宽后：114 由 6／0 → 12／1（那条 1 为真实失效位置）、113 由 4／1 → 64／6、115 19／13，误报归零。
  - 自查另补：本批过程中我三次踩同一坑——**在 Markdown 表格单元格里写裸竖线的正则**（两次把表格切坏）与**heredoc 把 `
` 吞成真换行**（一次写出断裂的字符串字面量）。前者靠 `check_spec` 的「格数与表头不符」当场逮住，后者靠 `ast.parse` 逮住。
