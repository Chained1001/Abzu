# 118-2026-09-17-A信号净化与沉淀补账批

> 模式：全量（件数 6；触发风险升级信号⑥「改守卫脚本自身」——check_swallow 行为变更）

## 一、现状位置（现场核过）

- `scripts/check_spec.py` check_swallow｜「目标文本未提及且现行为 0」：现文分支「`elif n == 0: … lines.append(f'· 目标文本未提及且现行为 0: …（可能是改动行目标文本漏写，也可能是归零型已达成）')`」——`n==0 且 cur==0` 时**无条件打印**，零目标断言的达成态与真异常同屏；实测 117 单扫 8 行（实体 6＋「现行有值／目标文本未提及」中性行 1＋汇总分类行 1）。
- `scripts/check_spec.py` check_swallow｜「自我匹配预警」：现文分支「`elif cur > 0: lines.append(f'· 自我匹配预警: …近似预测 {cur + n}…')`」——`n>0 且 cur>0` 时无条件打印，≥1 目标断言的达成态（token 已在目标件、期望 ≥1、现值达标）同样上屏；实测 117 单扫 6 行（实体 5——{判定}／无则写明／任务书三要素／检查 K 改／mimosa 全为达成态——＋汇总分类行 1）。
- `scripts/check_spec.py` check_swallow｜「expect_re」：期望解析（五步取数）现只在 `if exists and cur == 0 and literal_token.match(norm)` 的 F2 块内执行——达成态静默化所需的期望值在 n/cur 分支处**不可得**。
- `scripts/check_spec.py` 已知限制段｜「⑭ 检查 L 只扫 §一／§二」为现末条——**检查 A 的归档件手动扫描面**无条目（hit 门 117 放宽后，手动跑归档规格会出 A 类预警：实测 `archive/115` 修前 A 类实体 8 行＝未提及 2＋预警 5＋待复核 1，另 2 条为汇总行）。
- `AGENTS.md` §三速查｜「内联脚本包 heredoc」：现速查六条止于「多用 `<<'EOF'` 写法」——**多层转义塌陷**（JSON→shell→Python 两层坍缩把 `\n`／`\u0000` 写成真实换行／NUL 字节进源码）两起事故（116 断言 16、117 产物审查 P0 修复过程）无条目、无立法。
- `docs/specs/施工问题记录.md` §二已总结区｜「代码跨度尾带空格触发 MD038」：现末行去向止于检查器——转义家族 2 起未记行、未总结。
- `docs/specs/施工机制.md` §九｜「规格模板（三要素，标准／全量模式用）」：模板本体四节（§一–§四）与批件实际节集（可至 §九）之差异无一句话说明——检查 K 白名单的「模板无对应节」判定缺读者侧出处。
- `docs/specs/施工机制.md` §三审查位｜「审查任务书三要素」：现文三要素（点名文件清单／实测命令与输出／输出上限）——117 产物审查 41 次往返（其 §九 所记）、约 2.2M tokens（会话实测，无文件出处）——无往返上限约束，第四要素缺位。
- 一键检查候选基线（修前，118 落盘后实测）：67 行/次（[4] 段扫 117＋118 两件）；其中 A 类两可态 19 行（未提及 12＋预警 6＋中性汇总 1，含 2 条汇总行；成稿审查纠初稿「47」——那是 118 落盘前的旧基线）。

## 二、改动清单

| # | 文件｜位置 | 改动（现状 → 目标） | 依据／裁定 |
| --- | --- | --- | --- |
| 1 | `scripts/check_spec.py` check_swallow｜「目标文本未提及且现行为 0」与「自我匹配预警」两分支 | **达成态静默**：把「窗口内定位首个期望子句＋五步取数」提为小函数（约 6 行，探测与 F2 **共用**——继承分配型不判护栏、消双解析漂移；成稿审查 P2-⑪ 采纳的更小改法），n/cur 分支前以它做**保守探测**——① `exp==0 且 cur==0`（零目标已达成）→ 不打「未提及」行；② `exp≥1 且 cur>0`（存在目标已达成）→ 不打「自我匹配预警」；③ `exp≥1 且 cur==0 且字面且目标件实存` → 不打「未提及」行（F2 待复核是更锐报法；**exists 门**——目标件不存在时该行是「路径写错」的唯一信号，不得压）；④ 分配型句（复用 `distrib_re`）／多值／无期望＝**不采信**，行为与修前逐字节同 | A 类两可态与真异常同屏（全仓 17 实体行／归档件 8 行），狼来了效应；成稿审查 P1 补两洞：分配型单值句会被误采信、缺 exists 门丢路径写错信号 |
| 2 | `AGENTS.md` §三速查 与 `docs/standards/运行环境标准.md` §2｜「内联脚本包 heredoc」 | 速查追加第 7 条：**多层转义**——写文件／生成补丁脚本不经 bash heredoc 内嵌反斜杠转义（JSON→shell→Python 两层会把 `\n`／`\u0000` 塌成真实换行／NUL 字节）；改用专用写文件工具或 `chr()` 构造，落盘前语法预检。**两处同步**（成稿审查 P1-④：初稿漏）：AGENTS「跑命令前先过这六行」→「七行」；运行环境标准 §2 增同内容条文（速查的完整清单正灶，初稿误列禁改） | 转义家族 2 起事故立法（116／117），现无任何防线 |
| 3 | `docs/specs/施工问题记录.md` §二已总结区｜「代码跨度尾带空格触发 MD038」行后 | 新增已总结行：多层转义塌陷（2 起：116 断言 16 `\n`、117 `\u0000`）｜去向：`AGENTS.md` §三 速查第 7 条（随第 2 项立法） | 文件账本律＋已总结区「修完当场已立法直接进」规则 |
| 4 | `scripts/check_spec.py` 已知限制⑨｜「不区分**预期陈旧**」 | 补一句：**归档件手动扫描面**——hit 门放宽（117）后手动跑 `--static` 于归档规格会出 A 类预警（达成态随第 1 项静默后实测 N 条——施工后回填实值），门禁不扫归档件 | 上轮检视 P2；边界自认 |
| 5 | `docs/specs/施工机制.md` §九｜「规格模板（三要素，标准／全量模式用）」 | 标题下补一句：模板本体四节是**最小集**——批件按需增「需同步更新的文件核对／禁止事项／被否决的替代方案／开放项／审查记录」等节；检查 K 只核模板四节对应的节名正则，余者白名单 | 上轮检视 P2（模板四节 vs 批件九节无解释） |
| 6 | `docs/specs/施工机制.md` §三审查位｜「审查任务书三要素」 | 三要素 → **四要素**：追加「往返数上限（≤25 次）」；实证反例＝117 产物审查 41 次往返、约 2.2M tokens（产物审查 P1-④：初稿「46／2.17M」与 §一 所记 41 并存，订正） | 上轮检视 P3；成本预算缺位 |
| 7 | 本规格 | 新建（本件） | 文件账本律 |

## 三、验收断言

1. 零目标达成静默（夹具法）：夹具规格（改动行**不含** token、目标件**无** token、断言「`grep -c "静默探针零" AGENTS.md` → 期望 0。」）→ `python scripts/check_spec.py --static <夹具> 2>&1 | grep -c '未提及且现行'` → 期望 0（修前该夹具必出 1 条）。
2. 存在目标达成静默（夹具法）：夹具（token 取 `AGENTS.md` 现值 ≥1 的真实词「多层转义」、改动行含该词、断言「`grep -c "多层转义" AGENTS.md` → 期望 ≥1。」）→ `grep -cE '^· 自我匹配预警'` → 期望 0（产物审查 P1-③：初稿 token「路由」现值实为 0、「修前必出」不实——规则②当时未被覆盖；审查位已以同型夹具实测静默成立）。
3. 待复核不双报（夹具法）：夹具（token 不在目标件、期望 ≥1、字面 token）→ `grep -c '零命中待复核'` → 期望 1 且 `grep -c '未提及且现行'` → 期望 0。
4. 保守性回归（夹具法）：夹具断言**无**「期望」数字（或期望子句多值）→ 「未提及且现行」与「自我匹配预警」旧行为保留（`grep -c '未提及且现行'` → 期望 ≥1）。
5. 旧格式回归（夹具法）：自建 `[A]`／`[B]` 夹具（改动行含 `[A]` 标记列、断言「`grep -c "旧格式探针" AGENTS.md` → 期望 ≥1。」、token 不在 AGENTS.md）→ 「零命中待复核」出 1 条（成稿审查实测该形态修前即出 1——exp≥1 且 cur==0 走 F2，静默不改变它）。
6. `grep -c '多层转义' AGENTS.md` → 期望 1；且 `grep -c '七行' AGENTS.md` → 期望 ≥1（「六行」改「七行」）。
7. `grep -c 'chr()' AGENTS.md` → 期望 ≥1（速查第 7 条含构造法）。
8. `grep -c '转义家族\|多层转义塌陷' docs/specs/施工问题记录.md` → 期望 ≥1（已总结新行）。
9. `grep -c '归档件手动' scripts/check_spec.py` → 期望 1（已知限制⑮新句，含施工后实测值）；且 `grep -c '多层转义' docs/standards/运行环境标准.md` → 期望 1（§2 增条文）。
10. `grep -c '最小集' docs/specs/施工机制.md` → 期望 1。
11. `grep -c '四要素' docs/specs/施工机制.md` → 期望 ≥1（三要素行改四要素）。
12. 门禁与候选收缩（双判）：`bash scripts/check.sh` → 期望退出 0；且 `bash scripts/check.sh 2>&1 | grep -c '^· '` → 期望 ≤60（修前实测 **67**——成稿审查 P0 纠初稿 47：118 落盘后 [4] 段扫两件；静默预计消实体 ≥13，上限留不采信形态余量）；锐断言：`bash scripts/check.sh 2>&1 | grep -cE '^· (目标文本未提及|自我匹配预警)'` → 期望 ≤8（修前 17 实体；余量＝多值／分配型／无期望的不采信行）。
13. 归档件手动面复测：`python scripts/check_spec.py --static docs/specs/archive/115-2026-09-17-自审留痕与位置核批.md 2>&1 | grep -cE '^· (目标文本未提及|自我匹配预警)'` → 期望 ≤4（修前**实体 7**＝未提及 2＋预警 5；精确行首锚排除汇总行——成稿审查纠初稿口径混入 2 条汇总）。
14. 语法：`python -c "import ast,io; ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read())"` → 期望退出 0。
15. 用词扫描：`bash scripts/check.sh 2>&1 | grep '用词扫描'` → 期望含「命中 0 件／0 处」。

## 四、规格自审（五条·留痕表）

| 项 | 核法与命令 | 实测输出摘要 | 判定 |
| --- | --- | --- | --- |
| 1 位置／计数／写入位置三件现场核过 | §一 9 条逐条 grep／读源实跑＋成稿审查纠偏后复核 | 初稿三组基线失真已订正（47→67、A 类 14→19、归档件 10→实体 7——详见 §九 P0/P1-①/P2-⑦）；锚点引文 9 条全真 | 通过 |
| 2 断言实跑过 | 夹具类（1–5/13）待施工后首跑并先留修前基线（断言内已注「修前必出」）；grep 类（6–12/14/15）命令可复跑 | 夹具构造沿用 chr 码法；基线值随条注 | 通过 |
| 3 禁改文件已写明 | 逐件指认 | `docs/product/**`、`skills/**`、`README.md`、`CHANGELOG.md`、`.mimosa/**`、`docs/specs/archive/**`、`docs/standards/**`、117 规格及其余在制件 | 通过 |
| 4 范围与期望同源 | 逐条指认 | §二 7 项 ↔ §三 15 条：1↔1–5/12/13/14（行为）、2↔6/7、3↔8、4↔9/13、5↔10、6↔11 | 通过 |
| 5 自述有守卫 | 逐句指认 | 行为类兜＝夹具断言五向（静默两向＋不双报＋保守回归＋旧格式回归）；文档类兜＝grep 断言；门禁/用词语法兜＝12/14/15 | 通过 |

## 五、需同步更新的文件核对

- `scripts/check_spec.py` check_swallow docstring：达成态静默的判定标准须写入（n/cur 分支前的保守单值探测、四条规则）——随第 1 项同步。
- `AGENTS.md` §三 首句「跑命令前先过这六行」→「七行」（随第 2 项）。
- `docs/standards/运行环境标准.md` §2 增一条同内容条文（速查完整清单的正灶——成稿审查 P1-④，初稿误列禁改）。

## 六、禁止事项

- 禁改：`docs/product/**`、`skills/**`、`README.md`、`CHANGELOG.md`、`.mimosa/**`、`docs/specs/archive/**`、`docs/specs/117-2026-09-17-审计修复与脚本瘦身批.md`。
- `docs/standards/运行环境标准.md` 仅 §二 第 2 项点名的新增条文行可改，余名禁改；`AGENTS.md`／`施工机制.md`／`施工问题记录.md` 同此点名行粒度。
- check_swallow 改动**只增静默门、不动 F2 五步取数与既有分支语义**——探测不采信时行为必须与修前逐字节同。
- 不新增检查项编号、不新增脚本文件、不引外部依赖、不跑联网命令。

## 七、被否决的替代方案（过度设计门槛）

1. 把 F2 五步取数**整体**提为公共函数——重构面大、F2 是 109 立法的精细逻辑；但成稿审查指出**定位＋取数入口**的小函数（约 6 行）是更小改法且消双解析漂移——**已采纳进第 1 项**（否决的只是整体重构）。
2. 「中性/哨兵汇总行」也按期望静默——哨兵型与归零型同形是 docstring 明载的设计取舍，动它超出本批「两可态降噪」范围。
3. 已知限制 ①–⑧⑩–⑭ 全面重写——仅 ⑨ 有新增事实（归档件手动面），其余经核仍准确，重写属纯搬动。
4. 转义家族建检查器（拦 heredoc 生成脚本）——坑在生成侧（AI 写补丁脚本阶段），仓内检查器够不着上游；立法入速查＋`ast.parse` 预检已是可达防线。

## 八、开放项

- 第 1 项静默后，若某批规格的断言**故意**以「未提及」行为提示（如断言写法故意不给期望值），保守性回归（断言 4）兜住不静默——若实测发现误静默形态，停手呈报。
- 归档件手动面（第 4 项）实值随施工后回填；若静默后归档件 A 面仍 >10 条/件，另立批治理。

## 九、审查记录

- 2026-09-17｜成稿审查｜发现的问题 共 10：P0:1 P1:5 P2:4｜结论 修订后通过｜工具往返数 16｜周期时长 <1h
  - **P0**：断言 12 必挂——修前基线实为 **67** 非 47（118 自身落盘后 [4] 段扫 117＋118 两件），≤42 不可达 → 基线改 67、上限 ≤60＋锐断言（两可态实体 ≤8）。
  - **P1 五条**：①基线三组数字失真混源（47/A 类 14 系 118 落盘前旧值；「117 扫描 6 条」实 8）→ 全部按单扫/全仓分列实值；②「46 次往返」无出处（117 §九 记 41）→ 改 41＋token 数标注会话实测出处；③AGENTS 增第 7 条漏两同步面（「六行」写死、运行环境标准 §2 是完整清单正灶且在初稿禁改清单）→ 第 2 项扩双同步、§五/§六 补；④保守单值探测缺分配型护栏（分配型句首子句恰一数字会误采信→压行且 F2 不判＝零输出）→ 探测排除分配型（复用 `distrib_re`）；⑤rule ③ 缺 exists 门（目标件不存在＋exp≥1＋cur==0 → 全静默，丢路径写错信号）→ 加「目标件实存」门。
  - P2 四条：⑥已知限制⑨ 错档（那是检查 I 的条目）→ 改为新增 ⑮；⑦归档件「10 条」混入 2 条汇总行（实体 8）→ 断言 13 改精确行首锚、修前实体 7；⑧§六 缺点名行粒度 → 补；⑨否决 1 呈假二择——审查位提出更小改法（定位＋取数入口 6 行小函数共用，继承分配型护栏）→ **采纳进第 1 项**，否决的改标「仅整体重构」。
  - 观察项：§一 9 条中 6 条初审即真（两分支引文逐字符核真、expect_re 仅 F2 块、四个文档锚点真、归档件实体基线真）；夹具 1–5 五形态经审查位**实际构造并跑修前基线全中**（含「期望 ≥1 → 未提及＋待复核双报」的修前实态）；hits 计数与汇总不受静默影响；exp 撞名无虞（F2 块内先置 None）；断言 5 前提失实（117 无 [A]/[B] 夹具）→ 改自建。四要素任务书首轮实效：16 往返／545K tokens（对照 117 产物审查 41 往返／2.2M）。
- 2026-09-17｜产物审查｜发现的问题 共 6：P0:0 P1:4 P2:2｜结论 修订后通过｜工具往返数 14｜周期时长 <1h
  - **P1-①**：已知限制⑮ 回填值失实——「archive/115 实体 7→0」实为 **7→1**（余 1 条多值不采信形态「M|M 的留痕表」；「全仓 17→0」为真）→ 按实测改。施工自查：我在刷新⑮时以全仓门禁的 0 推写了归档件同值——**推测代实测**，产物审查逮住。
  - **P1-②**：§五 同步义务未履行——check_swallow docstring 未写静默判定，且尾段「cur==0 打中性行／预测行只在 cur>0 且 n>0 输出」与现行为相抵 → docstring 补静默四规则全文。
  - **P1-③**：断言 2 前提不可满足——初稿夹具 token「路由」在 AGENTS.md 现值 0，「修前必出 1 条」不实，**规则②当时未被自测覆盖** → 断言 2 改用现值 1 的真实词「多层转义」并重验（实体 0 ✓）。
  - **P1-④**：§二 第 6 项残留「46 次往返 2.17M」与 §一 的 41 并存 → 订正为 41／约 2.2M。
  - P2-⑤：§四 留痕表行 1 仍记初稿基线（47/14/10）与 §九 裁定相抵 → 改 67/19/实体 7 并注裁定出处。P2-⑥：A 全静默时「｜分类：」空尾可达 → 一行守卫（空分类不打印尾缀）。
  - 已核无问题面：审查位以 `git show HEAD` 旧版与新版**对拍 16 对象**（14 夹具＋118＋archive/115＋K=M 补测）——差异仅预期静默行、不采信形态逐字节同；六反例全过（期望 0 现值 >0 出预警、非字面／exists 门保留、真多值双报保留、「≥1；预验 0。」止符判 1、分配型保留、K=M 双向各用自身序号）；断言 15 条复跑全过；「有预警 N」分类计数与实体同步（118：14→10 删 4、115：31→25 删 6）；文档四件落位与格式一致。
