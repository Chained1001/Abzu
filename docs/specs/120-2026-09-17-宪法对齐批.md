# 120-2026-09-17-宪法对齐批

> 模式：标准（件数 8；纯文档面，不触守卫脚本）

## 一、现状位置（现场核过）

- `AGENTS.md` §一红线表｜「全绿」：红线 6 自测写「我跑了 check.sh，还是记得它绿？」——而「全绿」不含「各段跳过已读懂、候选面已过目」之意（[3] 缺件判失败系 119 才立，候选恒不阻断）；同表红线 2「临时产物不入库」未给 `patch_file.py` 同目录临时件留例外说明（其临时件须与目标同目录才能原子替换，中断残留属其已知限制①）。
- `AGENTS.md` §二路由表｜「写规格」行：现文「规格模板＋自审五条＋断言写法七条」——未点明自审五条须落成 §四 留痕表形态（检查 M 的核对对象）；同表「加／改守卫」行未提「触检查脚本时规格须含需同步更新的文件核对表」（检查 E 的核对③会出候选）。
- `AGENTS.md` §三｜「本机用 `python`」：速查第 5 条与 `check.sh` 探测链（`python3 → python → py`，`运行环境标准` §一.4 明文顺序不可换）表述相抵；第 6 条「`python -c` 里的 `>` 会被 shell 当重定向」不精确（加引号即不重定向，真风险是引号缺失或提前闭合）；常用命令未列 `--verify`／`--reconcile` 的调用时机；未说明 `patch_file.py` 写文件属工作流内动作、不触发红线 1 的 git 确认。
- `AGENTS.md` §四不做清单｜「完整理由与解冻条件见 git 历史」：格式说明写「不做＋理由＋解冻条件」，实现是「不做（括注＝解冻触发关键词）」——格式说明与实现对不上。
- `AGENTS.md` §五分工原则 3｜「例外：规格在会话内呈报时不计；落文件才算一步」：「呈报不计」与「必须写入文件」两句的关系读不通。
- `AGENTS.md` §七自检信号表｜「[4] 段 A–E、H–M 无未核项」：「未核项」无判定标准（跳过行？候选行？全部跳过形态？）。
- `docs/standards/文字与命名标准.md` §8｜「产品领域词」：与 `docs/product/术语表.md` 的关系（两表是否同源、豁免面以何为准）无一句话说明。
- 本批逐条确认记录：评审另 3 项（断言写法七/九条数、`--verify` 与 CHANGELOG 出口、`_ledger` 清单补员）已由批 119 顺带修复（`grep -c '第 9 条' scripts/check_spec.py` → 0、verify 出口已立、LEDGER 已含施工问题记录），不在本批范围。

## 二、改动清单

| # | 文件｜位置 | 改动（现状 → 目标） | 依据／裁定 |
| --- | --- | --- | --- |
| 1 | `AGENTS.md` §一红线表｜「红线 2 与红线 6」 | 红线 2 补括注：工具内部临时件除外（`patch_file.py` 同目录临时件为原子替换所必需，中断残留见其已知限制①）；红线 6 自测改为「跑过且过目各段跳过与候选（全绿≠候选面为零）」 | 外审 P1×2；119 起候选面可读性已备 |
| 2 | `AGENTS.md` §二路由表｜「写规格／加改守卫」两行 | 写规格行补「自审五条按 §四 留痕表落（检查 M 核对对象）」；加改守卫行补「触检查脚本时规格须含需同步更新的文件核对表（检查 E 核对③）」 | 外审 P1/P2 |
| 3 | `AGENTS.md` §三｜「第 5／6 条＋常用命令」 | 第 5 条改「命令行手写调用用 `python`；工具内探测以 `运行环境标准` §一.4 三级链为准」；第 6 条改「`>` 未加引号或引号提前闭合时被 shell 当重定向」；常用命令补一行 `--verify`／`--reconcile` 调用时机；补一句「`patch_file.py` 写文件属工作流内动作，不触发红线 1（其只管 git 命令），仍守红线 4／6」 | 外审 P0/P2/P3 三处同节 |
| 4 | `AGENTS.md` §四｜「格式说明」 | 改「不做 ＋ 解冻触发关键词（完整理由见 git 历史原版本）」——与条目实现对齐 | 外审 P2 |
| 5 | `AGENTS.md` §五｜「分工原则 3」 | 改「工作流每一步须落文件；仅『规格在会话内呈报』这一动作不单独计为一步，落文件后才计」 | 外审 P2 |
| 6 | `AGENTS.md` §七｜「工具报得出与应报出一致」 | 判定标准补：「无未核项＝[4] 段十一类均实际执行、无『全部跳过』行、跳过行逐条有因」 | 外审 P2 |
| 7 | `docs/standards/文字与命名标准.md` §8｜「产品领域词」 | 补一句：产品领域词豁免面以本件 §8 为准，`docs/product/术语表.md` 是产品侧正名记录——两表分工（豁免清单 vs 正名），不同源不冲突 | 外审 P2 |
| 8 | 本规格 | 新建（本件） | 文件账本律 |

## 三、验收断言

1. `grep -c '原子替换' AGENTS.md` → 期望 ≥1（红线 2 例外括注）。
2. `grep -c '候选面为零' AGENTS.md` → 期望 ≥1（红线 6 新自测句）。
3. `grep -c '需同步更新的文件核对' AGENTS.md` → 期望 ≥1（路由补句）。
4. `grep -c '留痕表' AGENTS.md` → 期望 ≥1（自审五条落法）。
5. `grep -c '三级链为准' AGENTS.md` → 期望 ≥1（python 双标准归一）；且 `grep -c '本机用 \`python\`（不是' AGENTS.md` → 期望 0（旧抵牾句清除）。
6. `grep -c '提前闭合' AGENTS.md` → 期望 ≥1（第 6 条精确化）。
7. `grep -c 'reconcile' AGENTS.md` → 期望 ≥1（调用时机入常用命令）。
8. `grep -c '解冻触发关键词' AGENTS.md` → 期望 ≥1（§四 格式说明对齐）。
9. `grep -c '落文件后才计' AGENTS.md` → 期望 ≥1（§五.3 收紧）。
10. `grep -c '无未核项' AGENTS.md` → 期望 ≥1 且同格含判定标准（十一类均实际执行）。
11. `grep -c '正名' docs/standards/文字与命名标准.md` → 期望 ≥1（两表分工句）。
12. `bash scripts/check.sh` → 期望退出 0；用词扫描「命中 0 件／0 处」；`npx markdownlint-cli2 "AGENTS.md" "docs/standards/文字与命名标准.md"` → 期望 0 issue。

## 四、规格自审（五条·留痕表）

| 项 | 核法与命令 | 实测输出摘要 | 判定 |
| --- | --- | --- | --- |
| 1 位置／计数／写入位置三件现场核过 | §一 7 条逐条读文核对（本轮验证输出留痕） | 7 条现状引文与判定全真；3 项逐条确认记录附 grep 证据 | 通过 |
| 2 断言实跑过 | 断言 1–11 起点随写随核（多数现值 0、目标 ≥1） | 命令可复跑 | 通过 |
| 3 禁改文件已写明 | 逐件指认 | `docs/product/**`、`skills/**`、`README.md`、`CHANGELOG.md`、`.mimosa/**`、`docs/specs/archive/**`、`scripts/**`（本批零脚本改动） | 通过 |
| 4 范围与期望同源 | 逐条指认 | §二 8 项 ↔ §三 12 条断言：1↔1/2、2↔3/4、3↔5/6/7、4↔8、5↔9、6↔10、7↔11 | 通过 |
| 5 自述有守卫 | 逐句指认 | 全部为文字改动，保底做法＝断言 12（提交前自动检查＋lint＋用词三绿） | 通过 |

## 五、需同步更新的文件核对

- 无（AGENTS 各节改文互不联动；文字与命名标准单点增句）。

## 六、禁止事项

- 禁改：`docs/product/**`、`skills/**`、`README.md`、`CHANGELOG.md`、`.mimosa/**`、`docs/specs/archive/**`、`scripts/**`（零脚本改动——涉脚本的评审项已由 119 修毕或逐条确认）。
- 不新增机制、不引外部依赖、不跑联网命令。

## 七、被否决的替代方案（过度设计门槛）

1. 探测链翻序（`python` 优先）——`运行环境标准` §一.4 明文「顺序不可换」，翻序须先查立法出处与事故史；本批只改宪法侧表述归一（第 3 项）。
2. 「未核项」建机械检查——无事故出身（现行输出已可人工判读），先立法判定标准一句。

## 八、开放项

- 第 1 项红线 6 新自测句落地后，若「过目候选」在实操中无人执行（下批复盘核），再议收紧。

## 九、审查记录

- 2026-09-17｜事后审计｜发现的问题 共 3：P0:0 P1:0 P2:3｜结论 修订后通过｜工具往返数 10｜周期时长 <1h
  - P2 三条全处置：①§三.5 括注「顺序不可换」四字在运行环境标准 §一.4 无字面（原文只「依次探测」）——AGENTS 改按原文引（规格 §一同病，以本记录更正：该四字系我转述失实）；②红线 6 增句「≠守卫都在——119 起 [3] 缺件才判失败」超出 §二.1 目标文本——超目标夹带，删除（事实留 §一 现状描述）；③红线 2 自测句未随例外同步（ 按原自测会误判）——自测补括注豁免。
  - 已核无问题面：工作区恰为规格三件无夹带；8 项单义落地；断言 12 条复跑全过；交叉引用逐个落实（检查 M＝§四留痕表、检查 E 核对③、十一类与 check.sh 头注逐字一致、patch_file 已知限制①、check_content 实时读 §8、术语表定位相符）。
