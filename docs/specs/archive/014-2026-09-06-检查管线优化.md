# 规格：014 · 检查管线优化（性能加固 + hooksPath + 增量模式）

> 本规格自包含。施工前先读仓库根 AGENTS.md。施工方只动本规格「文件级改动清单」列出的文件；docs/ 下任何文件不得触碰。

## 背景与目标

性能外审（2026-09-06）发现三个可优化点：①npx -y 在缓存缺失时静默联网下载 ②validate 六壳循环六次 Python 进程启动 ③hook 副本漂移无同步机制。本批一并修复：npx 改 --no-install 防联网、validate 合并单次调用、core.hooksPath 取代手动 cp 安装 hook、新增 --staged 增量模式。

## 现状事实（2026-09-06 实测锚点）

- `scripts/check.sh`：当前 33 行（六壳循环 + npx markdownlint + 聚合判定）
- `.git/hooks/pre-commit`：4 行 exec 调 check.sh（已安装，与 `scripts/hooks/pre-commit` 源文件一致）
- `scripts/hooks/pre-commit`：不存在（需新建）
- `scripts/check_content.py`：内容轨检查（013 评审新增，不在本批范围）
- agentskills CLI：实测 `agentskills validate --help` 不支持多路径一次调用；`npx markdownlint-cli2 --help` 不支持 --no-install（npx 自身参数）
- 本机 Node v25.8.0、Git Bash

## 文件级改动清单

### 1. `scripts/check.sh` 整文件替换 [A]（逐字）

```bash
#!/usr/bin/env bash
# 一键检查（宪法 §0）：六壳 skill 格式校验 + Markdown 体检 + 引用闭合 + 加粗密度 + TOC
# 用法：bash scripts/check.sh [--staged]
#   --staged  仅检查 git 暂存区涉及的 .md 文件（增量模式，日常快速反馈）
#   默认全量（六壳 validate + 全仓 markdownlint + 引用闭合 + 加粗密度 + TOC）
set -u
cd "$(dirname "$0")/.." || exit 1
fail=0
STAGED=false
[[ "${1:-}" == "--staged" ]] && STAGED=true

echo "[1] agentskills 六壳 validate..."
AS=""
command -v agentskills >/dev/null 2>&1 && AS=agentskills
if [ -z "$AS" ]; then
  AS=$(ls "${LOCALAPPDATA:-}/Programs/Python/"*/Scripts/agentskills.exe 2>/dev/null | head -1)
fi
if [ -z "$AS" ]; then
  echo "x agentskills 未找到（pip install skills-ref）"
  fail=1
fi

for d in skills/abzu-*/; do
  if [ -n "$AS" ]; then
    "$AS" validate "$d" || fail=1
  else
    fail=1
  fi
done

echo "[2] markdownlint..."
if $STAGED; then
  STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM -- '*.md' 2>/dev/null)
  if [ -n "$STAGED_FILES" ]; then
    npx -y markdownlint-cli2 $STAGED_FILES || fail=1
  else
    echo "  无暂存 .md，跳过"
  fi
else
  npx -y markdownlint-cli2 || fail=1
fi

echo "[3] 引用闭合 + 加粗密度 + TOC..."
python scripts/check_content.py || fail=1

if [ "$fail" -ne 0 ]; then
  echo "== 检查未全绿，禁止提交 =="
  exit 1
fi
echo "== 检查全绿 =="
```

### 2. 新建 `scripts/hooks/pre-commit` [A]（逐字替换现有内容）

```bash
#!/usr/bin/env bash
# git 提交门禁（L3）：提交前自动跑一键检查，红了即拦截
# 使用 core.hooksPath 指向本目录，clone 后自动生效，无需手动安装
exec bash "$(git rev-parse --show-toplevel)/scripts/check.sh"
```

### 3. 新建 `.claude-plugin/marketplace.json` [A]（逐字，已存在则跳过）

```json
{
  "name": "abzu",
  "owner": {
    "name": "Chained1001"
  },
  "metadata": {
    "description": "长篇网文 AI 辅助写作工作流 skill——从扫榜调研到逐章起草与修订，阶段门控式人机协同。"
  },
  "plugins": [
    {
      "name": "abzu",
      "description": "扫榜调研——网文市场题材调研与选题决策（六域套件首域，其余随建设发布）",
      "source": "./",
      "strict": false,
      "version": "0.3.0",
      "category": "novel-writing",
      "keywords": ["扫榜", "网文", "题材调研", "选题", "novel", "writing"],
      "skills": ["./skills/abzu-scan"]
    }
  ]
}
```

### 4. `scripts/check_content.py` 修复 TOC 变量遮蔽 bug [A]

当前 TOC 循环内 `content` 被赋值为 `.split()` 结果（列表），后续 `'## 目录' not in content` 对列表做 substring 检查——逻辑错误。改为对字符串检查。

### 5. git 配置（一次性，不由文件改动）

```bash
git config core.hooksPath scripts/hooks
```

在仓库根执行一次即可。此后 hooks 自动从 `scripts/hooks/` 读取（入库即生效，免手动安装，免漂移）。

## 禁止事项

- 禁止修改 skills/abzu-scan/ 下任何文件内容（本批只动 scripts/ 和 .claude-plugin/）
- 禁止修改 docs/ 下任何文件
- 禁止修改 CHANGELOG.md
- 禁止 git commit / push
- 禁止运行联网采集

## 验收标准（施工方自核验 + 规划方独立复跑）

- [ ] `bash -n scripts/check.sh` 语法通过
- [ ] `bash scripts/check.sh` 全绿退出 0（六壳 validate + markdownlint + check_content.py）
- [ ] `bash scripts/check.sh --staged` 语法通过（无暂存 .md 时应报"无暂存"但不报错）
- [ ] `scripts/hooks/pre-commit` 存在且内容与规格逐字一致
- [ ] `.claude-plugin/marketplace.json` 存在且 JSON 可解析
- [ ] `git config core.hooksPath` 返回 `scripts/hooks`
- [ ] `python scripts/check_content.py` 退出 0（TOC 变量遮蔽 bug 已修）
- [ ] `npx markdownlint-cli2` 0 违例

## 提交

施工方不提交。完成后输出【核验请求】——整段包在单个代码块里（格式见 AGENTS.md §3.8 与 docs/specs/collab-log.md）。

> 验收完成：2026-09-07 核验通过（collab-log 行 3）。
