# 规格：016 · 治理外审修正与复盘追认

> 本规格自包含。外审来源：作者方对 AGENTS.md + docs/standards 全量 + collab-log + 015 规格 + 工作树 diff 的联合评审（1 P0 / 7 P1 / 5 P2）。规划方逐条裁定前已现场复核全部可证伪点：skill-writing「## 九」grep 计数 = 2、file-conventions L58 断链路径解析、agent-design L3 双版本号、check_content.py glob 仅 `skills/`、加粗标记计数 AGENTS.md=115/agent-design=38/file-conventions=38、`npx --no-install` 前置语法实测 exit 0。

## 一、P0 裁定（三项子判定）

1. **「核验协议被打穿」——拒绝该定性**。时序证据：015 核验（8/8 全绿）针对规格内 8 处改动，于分身返回后当轮执行完毕；AGENTS.md/glossary/collab-log 三文件的固化改动发生在核验**之后**的独立轮次（作者「经验规范落盘，以后能一直用」指令），且该轮消息全量披露全部文件清单——并非静默入库，核验对象自始不含这三文件。collab-log 行 4「核验通过（8/8）」按规格口径属实，不作废。
2. **「复盘立法未走规格」——采纳**。宪法 §3.1 判定表「新增或修改规范条目 → 需要规格」，014 复盘与 015 复盘两次治理微调均未走规格，属同型流程缺口（第二次发生）。处置：本规格内追认既成批次 + 立豁免条款（改动 7），此后按条款执行。
3. **「提交混批风险」——采纳**。工作树现混装两批改动，拆两段提交（见「提交拆分」节）。

## 二、追认记录

015 复盘固化批 = AGENTS.md §3.1 外部参考研究条款 + §3.8 七件套同步、glossary「调用双层」、collab-log 第七件套 + 行 4、CHANGELOG 补记。系作者 2026-09-07 会话内逐项确认（015 规格确认施工 + 「经验规范落盘，以后能一直用」指令），按改动 7 新条款**追认为合法豁免批次**。

## 三、P1/P2 逐条裁定

| # | 发现 | 裁定 | 依据/处置 |
| --- | --- | --- | --- |
| P1-1 | skill-writing 双「## 九」 | 采纳 | grep 计数=2 实证；维护节改「十」（改动 1） |
| P1-2 | file-conventions L58 断链示例 | 采纳 | 讲跨目录链接的表里示例本身断链；补 `../specs/archive/` 前缀（改动 2） |
| P1-3 | 加粗密度检查盲区 | **部分采纳** | 盲区属实（glob 仅 skills）。拆两半：引用闭合扫描扩至治理文档＝真 bug，修（改动 10）；加粗密度阈值**不机械套用治理文档**——阈值系 skill 产物口径，AGENTS.md 闸门式加粗（115 处）是宪法文体，剥离有可读性代价，挂账另行立法（改动 11） |
| P1-4 | agent-design 头部写沿革 | 采纳 | 双版本号实证；统一「沿革见 CHANGELOG」句式（改动 4） |
| P1-5 | check.sh `npx -y` 遗留 | **有条件采纳** | 014 D4 曾裁定接受（依据「npx 不支持 --no-install」的现状事实），本轮实测推翻该锚点——`npx --no-install` **前置**语法有效（exit 0），014 实测放错了参数位置。改两处调用（改动 9），014 D4 真正闭环 |
| P1-6 | file-conventions 校验命令过时 | 采纳 | 入口改 check.sh + 单壳直调示例（改动 3） |
| P1-7 | 015 验收第 7 条终态失效 | 采纳（教训） | 验收是规格时点断言，批次扩展后未在最终树复跑。核验协议补一句（改动 8），不追溯改 015 |
| P2-1 | §0「探测链见下」指向含糊 | 采纳 | 改动 5 |
| P2-2 | §3.6 段落粘连 | 采纳 | 改动 6 |
| P2-3 | §3.8 超长单段 | 采纳，**挂账** | §3.8 与 collab-log 核验协议存在反模式 #4 式重复，全面下沉是中型重构，不在本批；挂账 Roadmap（与改动 11 同节） |
| P2-4 | 归档规格验收未回填 | **部分采纳（外审有误，规划方未验即采，立此存照）** | 独立审查分身普查实证：001-004/006-011/013 均已有尾注，仅 012/014 缺；012/014 补尾注（改动 12），013 维持原状 |
| P2-5 | collab-log 行 4「待入库」 | 采纳 | 随本批改为终态（改动 13） |

## 四、文件级改动清单（016 批）

### 1. skill-writing.md「## 九、维护」→「## 十、维护」[A]

### 2. file-conventions.md L58 示例补相对路径 [A]

`[架构对齐](007-2026-09-05-架构对齐.md)` → `[架构对齐](../specs/archive/007-2026-09-05-架构对齐.md)`

### 3. file-conventions.md §四 校验句更新 [A]（独立审查分身 F6 修订：统一表述 + 保留反引号约定）

原句（现状锚点，含行内代码反引号）：

```
校验（pip 包 `skills-ref`，命令行入口为 `agentskills`）：`agentskills validate skills/abzu-scan`。
```

替换为：

```
校验（pip 包 `skills-ref`，命令行入口为 `agentskills`）：一键入口 `bash scripts/check.sh`（六壳 validate + markdownlint + 内容轨）；单壳直调 `agentskills validate skills/abzu-{域}`。
```

### 4. agent-design.md L3 版本行统一句式 [A]

整行替换为：`> 版本：v0.2（沿革见 CHANGELOG 与 git 历史）`

### 5. AGENTS.md §0 探测链指向 [A]

「skill 格式校验：bash scripts/check.sh（含六壳 validate + markdownlint，agentskills 探测链见下）」→「skill 格式校验：`bash scripts/check.sh`（六壳 validate + markdownlint + 内容轨；agentskills 探测链固化于该脚本）」

### 6. AGENTS.md §3.6 深度分级前补空行 [A]

「**审计深度分级**」段前加空行，使其与审计双轨主段分离。

### 7. AGENTS.md §3.1 判定表增豁免行 [A]

在「单文件错别字/格式修正（<5 行）| ❌ 直接做」行后追加：

`| 复盘驱动的治理微调（≤3 文件，当批 CHANGELOG 留痕不计入；作者会话内逐项确认） | ❌ 免规格直接做（豁免条件缺一不可；与其他行命中时以本行为准——具体优于一般） |`

### 8. collab-log 核验协议第一步补句 [A]

「- 未动文件：规格声明"不得触碰"的文件必须零 diff」句后追加：
`- 批次扩展：核验后批次若扩展（规划方顺手修正/追加固化），验收断言须在最终工作树复跑一遍并记录`

### 9. check.sh `npx -y` → `npx --no-install` [A]

两处（全量与 --staged 分支）：`npx -y markdownlint-cli2` → `npx --no-install markdownlint-cli2`（语法已实测 exit 0；首次使用仍需手动预装一次 markdownlint，此后守卫零联网）。

### 10. check_content.py 引用闭合扫描扩至治理文档 [B]

glob 增补 `AGENTS.md`、`README.md`、`docs/**/*.md`；其中 skill 根相对路径引用按现有规则跳过、治理文档相对链接须解析存在。新扫描暴露的存量断链当批修复并记偏差。加粗密度维持 skills 域口径不动。

### 11. README Roadmap 增 2 挂账行 [A]

「基建挂账」条目后追加：

- [ ] 基建挂账：内容轨加粗密度检查扩展至治理文档——阈值须另行立法（治理文档闸门式加粗 ≠ skill 产物口径，016 外审立例：AGENTS.md 115 处）
- [ ] 基建挂账：宪法 §3.8 下沉评审（与 collab-log 核验协议去重，反模式 #4 视角）

### 12. 归档规格 012/014 验收回填 [A]（独立审查分身 F1/F4 修订：013 已有尾注，占位符落死）

- `docs/specs/archive/012-2026-09-06-爬虫补强.md` 末尾追加：`> 验收完成：2026-09-06 核验通过（collab-log 行 1）。`
- `docs/specs/archive/014-2026-09-06-检查管线优化.md` 末尾追加：`> 验收完成：2026-09-07 核验通过（collab-log 行 3）。`
- 013 已有同职能尾注，维持原状不动。

### 13. collab-log 行 4 终态化 [A]

「核验通过（8/8 验收独立重跑全绿），待入库」→「核验通过（8/8 验收独立重跑全绿），随 016 批入库」

### 14. CHANGELOG 条目拆分 [B]（独立审查分身 F5 修订：去无法对账的计数）

015 条目瘦身：「研究经验固化三件……」句移出（规划方在批次 A 提交前执行）；批次 B 新增 016 条目，含：复盘固化三件 + 外审修正 + 追认与豁免条款 + 成稿审查默认工序 + npx 闭环 + 检查器扩围。

### 15. 本规格验收通过后归档 [A]（独立审查分身 F9 修订：补动作细节）

按 file-conventions 规格 row：git mv 至 `docs/specs/archive/` 不留副本；git mv 属 git 写操作，随批次 B 提交一并执行（作者已确认两段式管线）。

### 16. collab-log「独立审查分身」节改为规格成稿默认工序 [A]

现状锚点：节标题「### 独立审查分身（可选增强，重大规格/全域审计时启用）」+ 现行正文（「规划方可 spawn……不违 agent-design 的 T1 禁令）。」）。整节替换为：

```markdown
### 独立审查分身（规格成稿审查，默认工序）

非平凡规格（改动清单 ≥5 项或含 [B]/[C] 级）成稿后、交作者确认前，规划方**必须** spawn 独立审查分身（fresh context，不经规划方意图污染）通读规格本身——重点核查：锚点真实性、清单完备可执行、验收可判定、内部矛盾。分身只回报 findings；报告须经规划方复核后采纳（分身也会误报），采纳的修订入规格后才交作者确认。平凡规格（单文件措辞/错别字级）豁免。断言类检查仍由命令执行。本机制为开发工具链用法，不属 T1 运行时物化（不违 agent-design 的 T1 禁令）。（2026-09-07 改立：原「可选增强、重大时启用」的裁量制被事故证伪——013 中央路由残留规划方三轮自审未发现、fresh 分身首跑即报；015 规格内部矛盾自审漏检、施工分身抓出；「是否重大」由规划方自己裁量等于永不触发，反模式 #9 变体。首例 dogfooding：016 号规格即按新工序先审后施。）
```

### 17. collab-log 边界声明补「全绿 ≠ 没问题」 [A]

现状锚点：「核验通过 = 按规格改了、语法没坏、文档合规、没越界；**不等于运行时行为验证**——需要联网/真机验证的，归实测阶段（由作者执行）。」句尾追加：

```markdown
「全绿」同理**不等于没问题**——仅覆盖已检项与已知模式（宪法 §3.6 扫描只能证伪），汇报全绿时须同时声明已检范围。
```

### 18. collab-log 增「独立审查分身提示词」第八件套 + AGENTS.md §3.8 计数同步 [A]（独立审查分身 F8 部分采纳：模板入、宪法不再膨胀）

(a) collab-log「参考研究裁定表」节末（「首例：015 号……」行后）追加新节（模板正文按既有七套样式包 ``` 代码块）：

````markdown
### 独立审查分身提示词（规格成稿审查用，第八件套）

```
你是独立审查分身（fresh context，评审者与创作者分离）。任务：审查规格 {规格路径} 的成稿质量。你不施工、不修改任何文件，只读只查；完成后只输出审查报告。

1. 先读 AGENTS.md（宪法）与规格全文，再开始审查。
2. 重点四查：① 现状锚点真实性——规格引用的行号、原文、计数逐一到仓库实文核对；② 改动清单完备可执行——落位与自由度标注、[A] 文本自足无歧义；③ 验收标准可判定——每条能跑命令或 grep 断言；④ 内部矛盾——条款之间、与宪法既有条文之间。
3. findings 分级：P0（照做会做错）/ P1（存疑需复核）/ P2（建议）。
4. 输出【审查报告】——整段包在单个代码块里：逐条 findings（文件:位置｜级别｜问题 + 原文引用）、自核验声明（findings 逐条对源核实）、结论（通过 / 修订后通过 / 裁决建议）。
```

首例：016 号规格成稿审查（9 findings：1 P0 / 3 P1 / 5 P2，修订后通过）。
````

(b) AGENTS.md §3.8「协作模板七件套（施工/核验请求/整改/裁决/审计结论/施工分身/参考研究裁定表）」→「协作模板八件套（施工/核验请求/整改/裁决/审计结论/施工分身/参考研究裁定表/独立审查分身提示词）」。

## 五、提交拆分（作者已裁定两段式；独立审查分身 F2 补正批次定义）

**批次 A「015 施工批」**（先提交，全部为工作树既有 015 批改动）：architecture.md、README.md（规格内 8 处 + 发布前置修正）、release-and-versioning.md（规格内 8 处 + v0.3 bump）、CHANGELOG.md（015 条目，「固化三件」句移出——规划方提交前执行改动 14 前半）、规格 015 归档（git mv）。

**批次 B「016 修正批」**（批次 A 提交后施工）：改动清单 1-18 触及的全部文件（AGENTS.md ×2 处、collab-log.md ×5 处、glossary.md、skill-writing.md、file-conventions.md、agent-design.md、scripts/check.sh、scripts/check_content.py、README.md 挂账 2 行、CHANGELOG.md 016 条目）+ **追认批既有改动**（AGENTS.md §3.1/§3.8 固化、glossary「调用双层」、collab-log 第七件套与定位行——与清单改动同文件合并提交）+ collab-log 行 5（016 施工记录，核验后规划方登记）+ 规格 016 归档（git mv，改动 15）。

**时序规则**：批次 B 的文件编辑一律在批次 A 提交之后进行——同文件跨批靠时序先后拆分，不做 hunk 级暂存（本环境无交互式 git add）；每批提交前 `git status` / `git diff --stat` 对照本节清单（反模式 #8）。

## 六、验收标准

- [ ] `grep -c "^## 九" docs/standards/skill-writing.md` = 1 且 `^## 十` = 1
- [ ] file-conventions L58 链接目标相对解析存在；§四 校验句含 check.sh 入口
- [ ] agent-design L3 仅一个版本号
- [ ] `grep -c "no-install" scripts/check.sh` = 2 且 `npx -y` 零命中
- [ ] `bash scripts/check.sh` 全绿（含治理文档引用闭合新扫描），退出码直取
- [ ] 判定表含豁免行；§0 指向 check.sh；§3.6 有空行分隔
- [ ] collab-log 核验协议含批次扩展句；行 4 为终态；模板八件套完整
- [ ] 012/014 尾注存在（013 既有尾注维持原状）；README 挂账 2 行就位；CHANGELOG 两条目分批对应
- [ ] `git status` 终态与两批清单完全一致，无越界文件
- [ ] collab-log「独立审查分身」节为默认工序表述（含 ≥5 项或含 [B]/[C] 触发条件、平凡豁免、复核条款、两条立例）
- [ ] collab-log 边界声明含「全绿不等于没问题」与「汇报须同时声明已检范围」；模板八件套完整

## 七、禁止事项

- 禁止修改 `skills/` 下任何文件；禁止修改 architecture.md（015 批已闭合，不再触碰）
- check.sh / check_content.py 只做清单内改动，禁止扩权
- 禁止 git commit / push（两段提交均由作者确认后规划方执行）

## 八、施工方式（作者 2026-09-07 裁定的完整管线，本规格即首例）

1. 规格成稿（含改动 16/17 增补）→ 2. spawn 独立审查分身审规格成稿（四查：锚点真实性/清单完备/验收可判定/内部矛盾）→ 3. 规划方复核 findings、修订规格 → 4. spawn 施工分身（提示词从 collab-log 模板派生）→ 5. 规划方核验三步 → 6. 两段式提交（作者已确认拆分方式）。

## 规格自审记录（铁律）

1. 锚点：全部断言经本轮 grep/实测确认（双九=2、L58、双版本号、glob 范围、加粗计数、npx --no-install exit 0）
2. 清单完备：15 项均有落位与自由度，豁免条款含窄化条件（≤3 文件/逐项确认/CHANGELOG 留痕）防滥用
3. 验收可判定：逐条 grep/ls/跑命令可断言
4. 矛盾检查：豁免条款与判定表其余行不冲突（015 复盘批为其首例）；改动 10 新扫描可能暴露未知存量断链，已预防性写入「当批修复并记偏差」
5. 术语：沿用「复盘」「治理微调」「内容轨」既有口径
6. 增补记录：改动 16/17 系作者 2026-09-07 裁定新增（规格成稿默认 fresh 审查 + 全绿话术纪律），增补后本规格按改动 16 新工序接受独立审查分身审查
7. 独立审查分身首审结果（改动 16 工序首例）：9 findings（1 P0 / 3 P1 / 5 P2）——F1（013 尾注已存在，外审误报且规划方未验即采）/F2（批次定义不完备）/F3（豁免计数口径）/F4（占位符违反 [A]）全采纳；F5/F6/F7/F9 采纳；F8 部分采纳（第八件套入、宪法不再膨胀）；本稿已全部修订完毕
