# 规格：017 · 协作流程外审修正（collab-log 三份外审合并裁定 + 施工模板合并）

> 本规格自包含。外审来源：collab-log 外部评审三份（总评、效率与性能版、更新版），合并去重后 20 项发现。按宪法 §3.1 转规格逐条裁定。现状锚点均已现场 grep/read 确认（2026-09-07）：`.gitignore` L12-13 已含 `*.tmp` 与 `.tmp/`；collab-log 定位行 L3「八件套」、纪律行 L4、施工提示词 L8、施工分身提示词 L20、核验请求 L32、第八件套 L103、白名单 L122/未动文件 L125、findings 处置表 L166-168（L167/168 存在「规划分方」笔误——外审未发现，本规格顺带修正）、默认工序节 L178、施工记录行 3 L186；AGENTS.md §3.8 L81「八件套」引用。

## 一、逐条裁定（20 项，三份外审去重合并）

### 采纳（9 项）

| # | 发现 | 裁定理由 | 落位 |
| --- | --- | --- | --- |
| F1 | 施工/施工分身两模板 90% 同文，重复维护有漂移风险 | 两模板统一禁令与输出格式后仅剩首句身份差异，合并显然；外部窗口模式保留为备选形态（宪法 §3.8「与外部分工并行可选」不删） | 改动 1/2 |
| F2 | 「自由度分布」统计口径不统一（行 3 括号说明增理解成本） | 表注统一口径（只统计文件级改动清单条目），行 3 括号随之瘦身 | 改动 3 |
| F3 | agent-design 判据被依赖但无摘要，读者无法判适用边界 | 治理文档允许链接；补一句话摘要 + 指向 | 改动 4 |
| F4 | 文件白名单可能误报副作用文件（编辑器缓存等） | `.tmp/` 已被 ignore；补口径句——被忽略文件不计 P0，untracked 新文件须对应规格预期 | 改动 5 |
| F5 | P0/P1/P2 判定可操作性不足 | 不建独立案例库（反模式 #4）；处置表内联三个真实实例（行 1-5 与 016 审查 findings 均在册） | 改动 6 |
| F6 | 平凡规格豁免定义模糊（「单文件措辞/错别字级」） | 不造第二标准，援引宪法 §3.1 判定表平凡行（<5 行）——单一真源 | 改动 7 |
| F7 | 审查依赖道德风险（「反正有分身兜底」导致初稿放水） | 补首要责任句 + P0≥2 重审门槛 | 改动 8 |
| F8 | 回传文本无摘要行，findings 多时解析成本高 | 审查报告与核验请求模板各加首行摘要（016 分身实际输出已自发带类似结构，顺势成文） | 改动 9 |
| F9 | P1 成因聚类无回顾机制 | 每满 10 条施工记录回顾一次成因分布；同源过半则上调规格编写强度（自动锚点校验届时按机制事故驱动另立项） | 改动 10 |

### 已覆盖（6 项，不改文件）

| # | 发现 | 我方既有对应 |
| --- | --- | --- |
| F10 | P1 频发源于现状锚点过时（建议规格模板加「现状基线」节/施工前核验） | 宪法 §3.8 锚点现场 grep 纪律（014 立例后）+ 成稿审查四查①；行 5 实证锚点类 P1 已清零（三条 P1 均为新类型） |
| F11 | 独立审查分身应设为重大规格必选项 | 更新版已成默认工序（本外审更新版自己确认） |
| F12 | 审查上下文精简/复用 | 第八件套提示词只给规格路径，分身按需读文件（天然最小上下文）；「复用宪法加载缓存」不符合 Agent spawn 一次性语义 |
| F13 | 成本收益指标入施工记录 | 行 5 已内联审查 findings 总数（首审 9 findings），分项计数见默认工序节首例行；数据可从行追溯，不另加列 |
| F14 | 参考研究「规格留痕」与施工记录脱节 | 施工记录「规格」列即研究规格本体（015 行），已闭环 |
| F15 | 施工方低风险 P1 自行修正免整改 | 现行 P1 处置本就是「按最合理理解执行 + 申报」，无整改轮产生 |

### 拒绝（5 项，留痕防重提）

| # | 发现 | 拒绝理由 |
| --- | --- | --- |
| F16 | 验收命令分级信任/抽样重跑/输出缓存 | 重跑对象是秒级确定性命令（grep/ls/check.sh 约 30-60s），全量重跑成本可忽略；缓存比对机制复杂度＞收益，且违守卫零外部依赖方针 |
| F17 | 审查深度按风险三级分档（全 [A] 单文件轻量审） | 全 [A] 规格恰是锚点过时最高发型（014 实证全 [A] 三处锚点过时）；四查是最小充分集，拆级必然漏内部矛盾类（015 痛点）；宪法 §3.6 已有核验深度分级，不重复立法 |
| F18 | 审查/施工并行化、部分结果异步交付 | 施工必须始于作者确认后（裁决位不可绕过）；批次分钟级，异步协调开销＞收益且破坏批次原子性 |
| F19 | 施工记录 git hook 自动生成 + 必填字段瘦身 | 新机制无事故出身（§6 方针）；git 提交本有作者确认关口；「整改轮数」是对话纪律数据，非 git 可推导 |
| F20 | 删除「本行后补」「顺手修正」等叙事词 | 系事实性溯源记录（防账实不符被误读为实时登记），删除反损日志可信度；根因（漏记）已由反模式 #8 立例收口 |

## 二、文件级改动清单

### 1. collab-log 两施工模板合并为一 [A]

删除「### 施工提示词」与「### 施工分身提示词（规划方 spawn 施工分身时使用）」两节（L8-L30），原位替换为单节：

````markdown
### 施工提示词（分身为默认形态；外部窗口为备选）

```
你是施工分身。任务：按规格施工 {规格路径}。你没有 git 写权限，也无需尝试提交；完成后只输出核验请求。

1. 先读 AGENTS.md（宪法）与规格全文，再动手。
2. 严格按规格「文件级改动清单」施工——每条带自由度标记：[A] 逐字执行、[B] 按方向自定实现（不得改变行为）、[C] 按验收标准执行。
3. 偏差分级：P0 阻塞型（规格与现状冲突，继续会做错）→ 立即停下，输出全部改为偏差报告；P1 观察型（不影响施工）→ 继续施工，记入核验请求偏差记录。
4. 规格「禁止事项」为硬边界：禁止触碰清单外文件、禁止运行联网采集。
5. 全部完成后输出【核验请求】——首行摘要（{改动 N 项｜验收 a/b 绿｜P1×n}），整段包在单个代码块里：改动清单逐文件对照规格、验收标准逐条自跑结果、偏差记录（P1）、未决问题。

外部窗口备选形态：作者在另一窗口施工时，首句身份改为「你是施工方」，其余逐字不变。
```
````

（合并依据 F1：两模板统一禁令与输出格式后仅剩首句身份差异；外部窗口模式依宪法 §3.8「与外部分工并行可选」保留为备选。顺带升级二处：第 2 条补 [C] 级对齐宪法三级自由度定义；占位符统一为 {规格路径}。）

### 2. 件套计数八→七同步 + 序号词退役（审查分身 F3 根治式修订） [A]

序号词（第 N 件套）随模板增删反复重编号（七→八→七，48 小时两 churn）——根治：**节标题退役序号后缀，指称一律用模板名**；总称计数只留两处。

- collab-log L3 定位行：「模板八件套」→「模板七件套」
- collab-log L64 节标题：「### 审计结论（规划方核验输出，第六件套）」→「### 审计结论（规划方核验输出）」
- collab-log L86 节标题：「### 参考研究裁定表（外部参考/外审研究的核心产出，第七件套）」→「### 参考研究裁定表（外部参考/外审研究的核心产出）」
- collab-log L103 节标题：「### 独立审查分身提示词（规格成稿审查用，第八件套）」→「### 独立审查分身提示词（规格成稿审查用）」
- AGENTS.md §3.8 L72：「研究程序与裁定表用 collab-log 第七件套」→「研究程序与裁定表用 collab-log「参考研究裁定表」模板」
- AGENTS.md §3.8 L81：「协作模板八件套（施工/核验请求/整改/裁决/审计结论/施工分身/参考研究裁定表/独立审查分身提示词）」→「协作模板七件套（施工/核验请求/整改/裁决/审计结论/参考研究裁定表/独立审查分身提示词；施工模板已合并分身版，外部窗口为备选形态）」

（CHANGELOG 与归档规格中的历史序号属沿革记录，不改——宪法头部沿革纪律。）

### 3. 施工记录表注 + 行 3 瘦身 [A]

「## 施工记录」标题与表头之间插入表注行（**前后各空一行**，防表头被吸进引用块）：

```markdown
> 表注：「自由度分布」只统计规格「文件级改动清单」条目；非文件改动（git 配置等）不计入，如有须在偏差列标注。
```

行 3「[A]×4（第 5 项为 git 配置，非文件改动）」→「[A]×4」（信息由 014 归档规格承载，表注统一口径）。

### 4. 默认工序节补 agent-design 判据摘要 [A]（审查分身 F5 修订：插入点为整句之后）

L178 段首句「规划方**必须** spawn 独立审查分身（fresh context，不经规划方意图污染）通读规格本身——重点核查：锚点真实性、清单完备可执行、验收可判定、内部矛盾。」整句之后、紧接的「分身只回报 findings」之前，插入独立一句：

```markdown
判据核心：评审者与创作者必须为不同上下文，防意图污染与自审偏袒（详见 [agent-design](../standards/agent-design.md) §二）。
```

### 5. 核验协议第一步补副作用文件口径 [A]

「- 文件白名单：`git status --short` 必须且仅显示规格清单允许的文件——多出任何一个 = P0，整份打回」条目后追加一条：

```markdown
- 副作用文件口径：被 `.gitignore` 忽略的临时产物（`.tmp/` 等，红线 2 已辖）不计 P0；untracked 且未忽略的新文件须对应规格预期的新增产物，否则仍 P0
```

### 6. findings 处置表增「实例」列 + 修「规划分方」笔误 [A]（审查分身 F4 修订：整表含表头替换）

L164-168 **整表**（含 4 列表头与分隔行）替换为 5 列新表：

```markdown
| 级别 | 判定 | 处置 | 谁决 | 实例 |
| --- | --- | --- | --- | --- |
| P0 违规 | 违反规格/宪法红线，继续会做错 | 规划方立即修，commit 时作者确认 | 规划方 | 016 审查 F1：锚点前提与现状不符（013 尾注已存在，外审误报被分身抓出） |
| P1 存疑 | 规格含糊/边界模糊/不影响施工 | 按最合理理解执行 + 标注假设 + 核验请求中申报 | 规划方裁定，作者可推翻 | 015 D1：验收预期 CHANGELOG 命中但清单无此条（规格内部矛盾） |
| P2 建议 | 风格/优化/不影响正确性 | 记录，不阻塞 commit | 规划方 | 016 审查 F6：两处替换文本对同一对象表述不一致 |
```

（顺带修正 L167/168 既有笔误「规划分方」→「规划方」——2026-09-07 现场核对发现，外审未报。）

### 7. 平凡豁免定义收窄 [A]（审查分身 F6 修订：以触发线补集定义，不引第二标准）

默认工序节「平凡规格（单文件措辞/错别字级）豁免。」替换为：

```markdown
触发线以下的规格（改动清单 <5 项且全部 [A]）豁免送审，随批次照常核验；触发线以上（≥5 项或含 [B]/[C]）一律必须送审，无裁量。
```

### 8. 初稿质量门槛（道德风险对策） [A]

默认工序节段尾括号「（……首例 dogfooding：016 号规格即按新工序先审后施。）」**右括号之后**另起，追加：

```markdown
审查是兜底不是替代——规划方对初稿质量负首要责任（铁律自审）；审查 findings 中 P0 ≥2 时，修订后须重新送审一次再交作者确认。（2026-09-07 外审立例：防「反正有分身审查」的初稿放水。）
```

### 9. 两模板首行摘要 [A]

(a) 第八件套（标题按改动 2 更新后）第 4 条：「4. 输出【审查报告】——整段包在单个代码块里：」→「4. 输出【审查报告】——首行摘要（{对象}｜findings 共 N：P0:a P1:b P2:c｜结论），整段包在单个代码块里：」

(b) 核验请求模板首条「- 规格文件：{规格路径}」前插入：「- 摘要：{改动 N 项｜验收 a/b 绿｜P1×n}」

### 10. 纪律行补聚类回顾 [A]

L4 纪律行句尾追加：

```markdown
每满 10 条施工记录回顾一次 P1 成因分布——同源占比过半（如锚点过时）时上调规格编写核验强度或按机制事故驱动立项自动校验。
```

### 11. CHANGELOG 条目 [B]

017 条目：三份外审 20 项合并裁定（9 采纳/6 已覆盖/5 拒绝）+ 施工模板合并（八件套→七件套，外部窗口降备选）+ 处置表实例列与「规划分方」笔误修正 + 副作用文件口径 + 平凡豁免收窄 + 初稿质量门槛 + 首行摘要 + 聚类回顾。

### 12. 本规格验收通过后归档 [A]（审查分身 F10 修订：执行者为规划方）

规划方于核验通过后、随本批提交执行：git mv 至 `docs/specs/archive/`（施工分身无 git 权限，此项不属施工清单）。

## 三、验收标准

- [ ] `grep -c "### 施工提示词" docs/specs/collab-log.md` = 1 且「### 施工分身提示词」零命中；合并模板含外部窗口备选句
- [ ] collab-log.md 与 AGENTS.md 内 grep「八件套」零命中（CHANGELOG/归档规格的历史沿革与本规格自身不计）；两文件均含「七件套」总称；两文件内「第.件套」序号词零命中
- [ ] 施工记录表注存在（前后空行）；行 3 自由度列为裸 `[A]×4`
- [ ] 默认工序节含 agent-design 判据摘要句（独立成句）、触发线补集式豁免句、P0 ≥2 重审句
- [ ] 核验协议第一步含副作用文件口径条
- [ ] findings 处置表为 5 列（含「实例」）且三行实例齐全；docs/specs/collab-log.md 内 grep「规划分方」零命中
- [ ] 审查报告与核验请求模板均含首行摘要要求
- [ ] 纪律行含「每满 10 条」聚类回顾句
- [ ] `bash scripts/check.sh` 全绿（退出码直取）

## 四、禁止事项

- 禁止修改 `skills/`、architecture.md、standards 五文件（本批只动 collab-log、AGENTS.md 一处、CHANGELOG、本规格）
- 禁止 git commit / push；禁止联网采集
- 拒绝项 F16-F20 不得借本批夹带落地（如需翻案须另立规格）

## 五、施工方式

规格含 [B] 项且清单 ≥5 项——按 collab-log 成稿审查工序：独立审查分身先审（本规格成稿后执行），修订后交作者确认，再 spawn 施工分身执行，规划方核验三步，单批提交。

## 规格自审记录（铁律）

1. 锚点：L3/L4/L8/L20/L32/L103/L122/L125/L166-168/L178/L186 与 .gitignore L12-13、AGENTS.md L81 均现场 grep/read 确认；「规划分方」笔误为现场核对新发现
2. 清单完备：9 采纳项全部有落位与自由度；已覆盖/拒绝项按定义不需文件改动
3. 验收可判定：逐条 grep/跑命令可断言（含「规划分方」「八件套」清零断言）
4. 矛盾检查：模板合并与宪法 §3.8「与外部分工并行可选」不冲突（外部窗口保留为备选形态）；件套计数七件套与改动 2 三处同步无游离；F7 重审门槛与作者确认位次序无冲突（重审在交作者确认之前）
5. 术语：沿用「成稿审查/施工分身/外部窗口/件套」既有口径
6. 独立审查分身首审结果（成稿审查工序第二例）：13 findings（2 P0 / 4 P1 / 7 P2）——F1/F2（「全仓零命中」断言把沿革与自指都算入，照做必红）改限定文件内断言；F3（件套序号三处漏同步）升级为序号词退役根治；F4/F5/F6/F9（整表范围/插入点/平凡口径/括号位置）全部修订；F7/F8/F10/F11/F12 顺带采纳；F13（外审原文未落盘）裁定：裁定表逐条含原意概述即宪法 §3.1 之「附件」形态，全文不另存（翻案依据充分）。本稿已全部修订完毕。
7. 施工注意：改动按序执行时行号会漂移，锚点一律以原文锚文本为准（行号仅辅助）
