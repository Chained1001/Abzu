# 规格：019 · 宪法重构（§3.8 去重下沉 + §3.6 术语补全）

> 本规格自包含。外审来源：AGENTS.md 宪法外部评审（内容 P1×3 / P2×2、性能 8 项）。按宪法 §3.1 转规格逐条裁定。现状锚点现场确认（2026-09-07）：AGENTS.md 137 行；§3.6 深度分级段（L79）、§3.8 整段（L81，017 批后含七件套句）、README L79 挂账行——均逐字核对。外审所见为 017 批前版本，个别发现已被 016/017 修复，如实标注。

## 一、逐条裁定（12 项去重合并）

### 采纳（4 项）

| # | 发现 | 裁定理由 | 落位 |
| --- | --- | --- | --- |
| F1 | §3.6 出现「D3 模式扫描」但 D1/D2/D3 全未定义，违宪法头部「流程术语首次出现处内联定义」声明 | 属实——D 级孤儿术语，且 collab-log 审计结论模板已在用 `深度：{D1/D2/D3}` 字段，必须成文定义 | 改动 1 |
| F2 | §3.8「仅**大型**非平凡改动适用本分工」之「大型」无客观界定 | 属实，且已被现实超越：014-017 四批全部非平凡规格走分身管线，「大型」门槛已名存实亡——修订为按判定表划线，门槛退役 | 改动 2 |
| F3 | §3.8 单段约 800 字、6 类规则分号串联，解析成本高（016 外审 P2-3 同发现，已挂账「§3.8 下沉评审」） | 两次外审同点名的结构性问题；重构为带标签子列表 + 细则下沉 collab-log（其核验协议本就是细则正本——宪法现行文本是重复陈述，反模式 #4 视角） | 改动 2 |
| F4 | 宪法硬编码模板件数与清单枚举，每次模板增删需多点同步 | 属实（七→八→七 48 小时两 churn 实证）；宪法只留指向，collab-log 定位行为件数唯一真源 | 改动 2（并入重构） |

### 已覆盖（2 项，不改文件）

| # | 发现 | 我方既有对应 |
| --- | --- | --- |
| F5 | check.sh 建议增加宪法链接有效性检查 | 016 批已实现：check_content.py 引用闭合扫描覆盖 AGENTS.md/README/docs/**/*.md 相对链接——外审基于旧版仓库 |
| F6 | 模板枚举清单同步风险 | 017 批已收窄（节标题序号词退役 + 计数仅剩两处）；本批 F4 进一步收至一处 |

### 拒绝（6 项，留痕）

| # | 发现 | 拒绝理由 |
| --- | --- | --- |
| F7 | 判定表「<5 行」门槛改为「非语义改动」 | 亮线纪律保留：「非语义改动」是判断题，会重开扯皮空间——2026-09-06 立例正是连续 5 批「感觉很小」式判断未走规格；偶发 6 行纯错别字写轻规格的成本是分钟级，可接受代价 |
| F8 | 反模式清单加「超 15 条强制下沉」条款 | §7 体积纪律已辖（本文件上限 300 行 + 超限处理序），重复立法；无实例属预立法 |
| F9 | 反模式按风险域分组/关键词索引 | 9 条无分组需求（预立法）；增长之忧由 §7 处理序覆盖 |
| F10 | 分身宪法预热加载/宪法精简版 | 与 017 外审同类建议同裁定：Agent spawn 语义即一次性 fresh，无预热概念；「宪法精简版」＝第二真源（反模式 #4） |
| F11 | 锚点索引/增量核实（免重复 grep） | 索引自身会过时——用可能过时的索引去核实锚点是套娃式风险（018 批 L46 事故实证：连现场 grep 都会漏，索引更险）；现场 grep 秒级，成本不成问题 |
| F12 | 判定表配文字决策流 | 表格即决策表，文字流程是第二真源（反模式 #4）；豁免行已标「缺一不可」+「以本行为准」 |
| F13 | 宪法热/冷路径拆分（远期） | 现行 137 行 < 300 上限，§7 处理序已辖；无实例不预建 |

## 二、文件级改动清单

### 1. AGENTS.md §3.6 深度分级段补 D 级定义 [A]

L79 整段替换为：

```markdown
**审计深度分级**：核验深度映射施工自由度，深度三级内联定义——D1＝逐字核验（与规格文本 diff 为零）、D2＝通读核验（语义通顺、行为不变）、D3＝模式扫描（残留词/格式/路径闭合）；映射：[A] 级改动→D1、[B] 级→D2、[C] 级→按验收标准核验（不占 D 级）；存量/知识文件最低 D3。**扫描只能证伪不能证真**——零命中仅表示"已知模式无违例"；扫出问题即升级该文件深度，待交付产物模板一律逐字。（2026-09-06 立例：矩阵 9/9 CLEAN 后仍需逐文件通读补审——深度此前无成文标准；2026-09-07 补 D 级内联定义——外审指出 D1/D2 从未定义，而 collab-log 审计结论模板已在用该字段。）
```

### 1b. AGENTS.md 头部内联定义声明扩展 [A]（审查分身 P1-4 补列）

头部声明句「流程术语（candidate、文件账本律、P0/P1、自由度分级等）均在首次出现处内联定义。」替换为：

```markdown
流程术语（candidate、文件账本律、P0/P1、自由度分级等）均在首次出现处内联定义，或指向单点权威文件（协议细则类见 docs/specs/collab-log.md）。
```

（§3.8 下沉后 P0/P1、结论三态等细则定义以 collab-log 为单点权威，声明须同步——否则修 F1 的孤儿术语却制造新孤儿引用。）

### 2. AGENTS.md §3.8 整段重构 [A]（审查分身 P0-3 修订：子弹点 4 空格缩进，实测过 lint）

L81 整段替换为（结构：总起句 + 空行 + 四个子弹点，**缩进 4 空格**——3 空格实测触发 MD007/MD029/MD032 共 7 错）。**有意语义变更二处**（审查分身 P1-6 如实标注）：①「并行可选」→「默认形态 + 外部窗口备选」（017 批 collab-log 已先行对齐口径）；②「适用于迭代速度优先的场景」动机句删除（分身已为唯一默认，动机句失义）：

```markdown
8. **分工协作（规划方/施工方/作者）**：非平凡改动（判定表 ✅ 行）适用本条协作协议；平凡改动按本节 1-3 直接执行（原「大型」前置门槛退役——施工分身已使协作成本与规格成本重合，2026-09-07 外审裁定）。

    - **三角色**：规划方（持全局上下文）——勘察、写规格、派生施工提示词、核验、统一提交；施工方/施工分身——只按规格施工，无 git 写权限；作者——确认规格、确认提交、重大分歧拍板（纯裁决位，仅在两确认与拍板时出手）。
    - **施工分身为默认形态**：规格获作者确认后规划方**主动 spawn**（Agent 工具传规格路径与约束），不退回作者粘贴中转（2026-09-07 立例：014 首轮产出粘贴式提示词，作者纠正后切换）；外部窗口双开分工为备选形态。
    - **铁律**：①施工规格写完必须先自审一遍（现状锚点与实文一致/清单完备可执行/验收可判定/内部无矛盾），自审通过才准产出施工提示词；②现状锚点必须现场 grep/read 确认，不得沿用对话记忆（2026-09-07 立例：014 号规格 3 处现状事实过时，施工时才暴露）；③施工提示词一律从 collab-log 模板派生，不得现场自由撰写（2026-09-07 立例：014 提示词漏带输出格式要求，分身返回未包代码块）。
    - **协议细则单点权威在 collab-log**：全套提示词与回传模板、P0/P1 偏差分级与 findings 处置、规划方核验三步与结论三态、成稿审查工序、施工记录表——见 docs/specs/collab-log.md；协作记录逐次登记（规格/施工方模型/自由度分布/偏差次数/整改轮数/结果）。（2026-09-06 作者审定；2026-09-07 重构去重——原细则系 collab-log 核验协议的重复陈述，反模式 #4 视角，016/019 两轮外审推动。）
```

（去重不丢规则核对：施工提示词必含要件/P0-P1 分级定义/核验请求格式→collab-log 施工提示词模板与 findings 处置表在册；核验三步与结论三态→collab-log 核验协议在册；模板件数枚举→collab-log 定位行唯一真源。）

### 3. README.md L79 挂账行销账 [A]

删除整行（016 挂账的「宪法 §3.8 下沉评审」由本批执行完毕）：

```markdown
- [ ] 基建挂账：宪法 §3.8 下沉评审（与 collab-log 核验协议去重，反模式 #4 视角）
```

### 4. CHANGELOG 条目 [B]

019 条目：宪法外审 12 项裁定（4 采纳/2 已覆盖/6 拒绝留痕）；§3.8 重构为子列表结构——「大型」门槛退役（按判定表划线）、细则下沉 collab-log 去重（016 挂账销账）、模板件数枚举去除（collab-log 定位行唯一真源）；§3.6 补 D1/D2/D3 内联定义（孤儿术语成文，对齐 collab-log 审计结论模板字段）；拒绝项亮线留存（<5 行门槛/预热/锚点索引/决策流等各附理由）。

### 5. 本规格验收通过后归档 [A]

规划方于核验通过后随本批提交执行 git mv 至 `docs/specs/archive/`。

## 三、验收标准

- [ ] AGENTS.md §3.6 段含「D1＝」「D2＝」「D3＝」内联定义与 [A]/[B]/[C] 映射句；头部声明含「或指向单点权威文件」
- [ ] AGENTS.md §3.8 为「总起句 + 四子弹点」结构（三角色/施工分身/铁律/协议细则指向，4 空格缩进），`grep -F "仅**大型**非平凡改动" AGENTS.md` 零命中（固定串匹配，星号系正则元字符），无「件套」字样
- [ ] §3.8 新段（总起句 + 四子弹点）合计字符数 ≤ 900（原段实测 1140；`LC_ALL=C.UTF-8` 前导下 `sed -n 'Lp' AGENTS.md | wc -m` 逐行累加断言——UTF-8 字符口径，防 locale 退化为字节数误挂，二审 F3 补注）
- [ ] `grep -c "① git diff 对照改动清单" AGENTS.md` = 0（细则复述下沉，宪法不再含核验三步展开句——指向句中的「核验三步」字样不属复述）
- [ ] README Roadmap 无「§3.8 下沉」挂账行；「基建挂账」其余两行仍在
- [ ] collab-log 与其余文件零改动（`git status` 对照：仅 AGENTS.md、README.md、CHANGELOG.md、本规格归档）
- [ ] `bash scripts/check.sh` 全绿（退出码直取）

## 四、禁止事项

- 禁止修改 collab-log（本批纯下沉指向，不反向搬运内容）、skills/、standards 五文件、architecture.md
- 拒绝项 F7-F13 不得借本批夹带落地
- 禁止 git commit / push；禁止联网采集

## 五、施工方式

清单 6 项含 [B]——按 collab-log 成稿审查工序：独立审查分身先审（**首轮 P0≥2，已按初稿质量门槛修订，须重审一次后**）交作者确认，再 spawn 施工分身执行，规划方核验三步，单批提交。与规格 018 的文件交集仅 CHANGELOG.md（各写独立条目）；AGENTS/README 归 019，standards/architecture/file-conventions 归 018，互不越界，顺序施工 018 先、019 后。

## 规格自审记录（铁律）

1. 锚点：§3.6 L79、§3.8 L81（017 批后最新态）、README L79、AGENTS.md 行数 137——均现场 read/grep 确认；外审所指「八件套」为 017 批前旧态，已按现势裁定
2. 清单完备：4 采纳项全部有逐字替换文本；去重核对表逐条确认下沉目标在册（collab-log 模板/findings 表/核验协议）
3. 验收可判定：逐条 grep/字符断言（§3.8 字符数粗校验 1140→≤900；行数因单行拆列表净增 5 行属预期，不设行数断言——二审 F1 更正）
4. 矛盾检查：「大型」退役与 collab-log 成稿审查触发线（≥5 项或含 [B]/[C]）不冲突——触发线管审查工序，判定表管规格必要性，各管一段；铁律三条保留完整（自审/锚点/派生）；§3.6 的 [C] 级不占 D 级表述与原意一致（原文本就未给 [C] 配 D 级）
5. 术语：D1/D2/D3 沿用既有口径；「亮线」系本规格新引入词（指可机械判定的硬阈值，与「判断题标准」相对）；「退役」用法同 017 序号词退役
6. 独立审查分身首审结果：9 findings（3 P0 / 3 P1 / 3 P2）——P0-1（净减行数算术反号，原段实为单行 1140 字符）改字符数断言；P0-2（grep「核验三步」吞掉指向句）改对复述句断言；P0-3（3 空格缩进沙盘实测 7 错）改 4 空格；P1-4（头部内联定义声明被打破）补改动 1b；P1-5（018 交集陈述失实）施工方式节改写；P1-6（两处语义变更未标注）改动 2 前言如实标注；P2-7（「亮线」非既有口径）自审 5 更正；P2-8（grep -F）验收 2 注明；P2-9（CHANGELOG 历史条目「大型」）按沿革豁免备案。全部修订完毕；P0≥2 触发初稿质量门槛——本稿送重审（见五）
