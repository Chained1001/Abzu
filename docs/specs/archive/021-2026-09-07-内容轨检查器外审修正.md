# 规格：021 · 内容轨检查器外审修正（check_content.py 三处）

> 本规格自包含。外审来源：scripts/check_content.py 双轨评审（内容 P1×3 / P2×2、性能 6 项），合并裁定 9 项。现场核实（2026-09-07，锚点纪律）：① 五占位壳 `references/|scripts/` 提及 = 0 → L17 缺陷系**潜伏 bug**（第二域上内容即触发，机制经 python 实测复现：他壳引用会错查 abzu-scan）；② 治理文档 `(skills/` 链接 = 0；③ 代码块内 `**` 实测仅存在于 scan-output-format.md（fenced 块内 9 行；清洗后计数 32→14）。现状锚点：check_content.py 全文 61 行（L17 / L42-47 / L50-54 为落点，纯代码段行号）。

## 一、逐条裁定（9 项）

### 采纳（3 项）

| # | 发现 | 裁定理由 | 落位 |
| --- | --- | --- | --- |
| F1 | L17 skill 根解析硬编码 `abzu-scan`——扫描全部六壳但引用一律错查 scan 目录（误报+漏报双风险） | 机制实测复现属实；现仓五壳零引用故为潜伏 bug，第二域上内容即爆——按「防患于已排期的确定缺陷」修复 | 改动 1 |
| F3 | 加粗密度未清洗代码块/行内代码，示例中的 `**` 虚增计数 | 与检查意图不符（只应计真加粗语义）；scan 域代码块 `**` 实测存在 | 改动 2 |
| F4 | TOC 检查受代码块内 `## 目录` 字样干扰（漏报） | 与 F3 同一清洗，顺带修复 | 改动 3 |

### 拒绝（5 项，留痕）

| # | 发现 | 拒绝理由 |
| --- | --- | --- |
| F2 | 治理文档 `skills/` 开头链接会被误报断链 | 前提不成立：Markdown 相对链接按**当前文件位置**解析是标准语义——从 docs/ 发出 `(skills/...)` 链接在 GitHub 渲染同样 404，报断链是正确行为；file-conventions §三 已规定相对路径写法；现仓零实例 |
| F6 | 合并循环消除重复 I/O（skill 文件读三遍） | 约 50 文件毫秒级（check.sh 全程 4.2s，瓶颈在 npx 启动）；整脚本重构无事故驱动，[A] 可验性反而变差 |
| F7 | 正则预编译 re.compile | Python re 自带缓存，微优化无实测收益 |
| F8 | `with open` 上下文管理器 | CPython 引用计数下 `read()` 返回即回收句柄；守卫进程毫秒级生命周期无句柄累积场景；4 处读取点缩进扩散零收益 |
| F9 | issues 分组输出 | 全绿时零 issues、红时个位数，分组无需求 |

### 已覆盖（1 项）

| # | 发现 | 现状 |
| --- | --- | --- |
| F5 | 引用闭合正则匹配「文本提及」非仅链接 | **有意设计**：skill-writing §六 规定 skill 资产引用形式即路径文字（非链接），检查全部路径文字恰是本仓约定；现仓全绿证明无误报 |

## 二、文件级改动清单

### 1. check_content.py L17 skill 根解析修复 [A]

原行：

```python
        full = os.path.join('skills', 'abzu-scan', ref) if not ref.startswith('skills/') else ref
```

替换为：

```python
        skill_root = '/'.join(f.replace(os.sep, '/').split('/')[:2])
        full = os.path.join(skill_root, ref) if not ref.startswith('skills/') else ref
```

（skill 根从被扫文件自身路径提取——`skills/abzu-scan/references/scan/x.md` 取前两段；`os.sep` 归一化防 Windows 反斜杠路径。）

### 2. check_content.py 加粗密度段清洗代码区 [A]

原段（L42-47）：

```python
for f in sorted(glob.glob('skills/abzu-*/**/*.md', recursive=True)):
    s = open(f, encoding='utf-8').read()
    b = len(re.findall(r'\*\*', s))
    l = s.count('\n') + 1
    if b > l // 3:
        issues.append(f'加粗超限: {f} ({b} > {l//3})')
```

替换为：

```python
for f in sorted(glob.glob('skills/abzu-*/**/*.md', recursive=True)):
    s = open(f, encoding='utf-8').read()
    # 清洗代码区后统计：fenced 代码块与行内代码中的 ** 非加粗语义，不计（021）
    s2 = re.sub(r'```.*?```', '', s, flags=re.S)
    s2 = re.sub(r'`[^`]*`', '', s2)
    b = len(re.findall(r'\*\*', s2))
    l = s.count('\n') + 1
    if b > l // 3:
        issues.append(f'加粗超限: {f} ({b} > {l//3})')
```

### 3. check_content.py TOC 段清洗代码区 [A]

原段（L50-54）：

```python
for f in sorted(glob.glob('skills/abzu-*/references/**/*.md', recursive=True)):
    s = open(f, encoding='utf-8').read()
    lc = s.count('\n') + 1
    if lc > 100 and '## 目录' not in s:
        issues.append(f'缺目录: {f} ({lc} 行)')
```

替换为：

```python
for f in sorted(glob.glob('skills/abzu-*/references/**/*.md', recursive=True)):
    s = open(f, encoding='utf-8').read()
    lc = s.count('\n') + 1
    # 目录判定排除代码块内字样（021）
    sc = re.sub(r'```.*?```', '', s, flags=re.S)
    if lc > 100 and '## 目录' not in sc:
        issues.append(f'缺目录: {f} ({lc} 行)')
```

### 4. CHANGELOG 条目 [B]

021 条目：check_content.py 外审 9 项裁定（3 采纳/5 拒绝/1 已覆盖）——skill 根解析从硬编码 abzu-scan 改为按被扫文件路径提取（潜伏 bug：第二域上内容即爆，机制实测复现）、加粗密度与 TOC 判定清洗代码区（示例 `**` 与代码块内标题字样不再虚增/漏报）；拒绝合并循环（50 文件毫秒级）等各附理由。

### 5. collab-log 施工记录登记 [A]（规划方自执，二审 P2 补标注）

施工核验通过后、提交前，规划方在施工记录表追加本规格行。

### 6. 本规格验收通过后归档 [A]

规划方于核验通过后随本批提交执行：`mv` 至 `docs/specs/archive/` 后随批 `git add`。

## 三、验收标准

- [ ] `python -m py_compile scripts/check_content.py` 退出 0
- [ ] `bash scripts/check.sh` 全绿退出 0（**回归断言**：三处修复对现仓零行为变化——五壳零引用故 L17 不触发、现加粗计数均在阈值内）
- [ ] L17 沙盘：造 `skills/abzu-t1/SKILL.md`（正文含一行 `references/only-t1.md`）+ `skills/abzu-t1/references/only-t1.md` 实文件，直跑 `python scripts/check_content.py`——**修复后**无 t1 断链（修复前该引用会错查 abzu-scan 误报）；验完即删 `skills/abzu-t1/` 整目录
- [ ] 清洗沙盘：python 单行对 `skills/abzu-scan/references/scan/scan-output-format.md` 断言「清洗后 `**` 计数 < 原始计数」（实测 32→14——代码块 `**` 实证文件，一审 P0-1 更正：scan-workflow.md 代码块为零，清洗前后均 34 不可用）；TOC 清洗（改动 3）无专项沙盘，由回归断言与本条同款清洗正则覆盖（一审 P2-5 注明）
- [ ] `grep -c "skill_root" scripts/check_content.py` = 2（定义行 + 使用行，一审 P0-2 更正）；加粗段与 TOC 段清洗行各一（`grep -c '```.*?```' scripts/check_content.py` = 2，二审 P2 补文件参数）
- [ ] `git status` 变更为四项：scripts/check_content.py、CHANGELOG.md、collab-log.md（登记行）、本规格归档；沙盘 t1 目录零残留

## 四、禁止事项

- 禁止改动 check.sh（020 批辖）、hooks/、skills/ 正式内容（沙盘 t1 用后即删）、docs/ 其余内容（本规格自身的 collab-log 登记与归档动作除外——一审 P1-1 收口）
- 拒绝项 F2/F6-F9 不得借本批夹带落地
- 禁止 git commit / push；禁止联网采集

## 五、施工方式

清单 6 项含 [B]——按成稿审查工序送审后交作者确认，再 spawn 施工分身执行，规划方核验三步，单批提交。与规格 020 无守卫脚本交集（check.sh vs check_content.py），共用追加型登记文件（CHANGELOG/collab-log，顺序施工无冲突，一审 P2-4 更正），建议 020 先、021 后相邻提交。

## 规格自审记录（铁律）

1. 锚点：check_content.py 61 行全文现场核对（L17/L42-47/L50-54 原文逐字）；三个前提核实命令实测（五壳零引用/治理零 skills 链接/代码块 ** 存在）；L17 误判机制 python 单行复现
2. 清单完备：3 采纳项均行级 [A] 替换文本；F1 修复含 Windows 路径归一化；过程产物三件（CHANGELOG/collab-log/归档）全部入清单（020 P1-5 教训直用）
3. 验收可判定：语法/回归/双沙盘/grep 六条断言，沙盘目录用后即删入断言
4. 矛盾检查：清洗正则 `` ```.*?``` `` 非贪婪配对与文件内 fenced 块结构一致；加粗段行数 `l` 仍按原文计（代码行是真实行，只豁免计数不豁免行数）；TOC 段 lc 同理；F5「文本提及有意检查」与 F1 修复正交（修的是解析根，不是匹配范围）
5. 术语：潜伏 bug/沙盘/回归断言沿用既有口径
6. 独立审查分身一审结果：8 findings（2 P0 / 1 P1 / 5 P2）全采纳——P0-1（清洗断言对象错误：scan-workflow.md 代码块为零，实测可用对象为 scan-output-format.md 32→14）改断言对象；P0-2（skill_root 按行计数实为 2）改断言数字；P1-1（禁止事项 docs/ 全禁与登记/归档冲突）收口措辞；P2-1（头部行号粒度）统一为纯代码段行号；P2-2（前提③措辞失准，P0-1 根因）改实测表述；P2-3（4 处非 6 处）订正；P2-4（「无文件交集」字面不成立）改「无守卫脚本交集」；P2-5（TOC 清洗无专项断言）验收注明回归覆盖。P0=2 触发初稿质量门槛——本稿送二审
