# 规格：022 · 宪法二轮外审修正（D 级重排 + 三处微调）

> 本规格自包含。外审来源：AGENTS.md 二轮外部评审（内容 P1×2 / P2×2、性能 7 项），合并裁定 12 项。现场核实（2026-09-07）：① 全仓活文档「预立法」标注实例 = 0（仅 §3.9 定义句自身）——P2-4 属为空集立法；② 深度级 D3 活文档涟漪恰 2 处（AGENTS.md L79 + collab-log L59 审计结论模板字段）；③ 现场自发现（外审未见）：**偏差记录的 D 序号（deviation，现至 D5 且随批次递增）与深度级 D1/D2（depth）字母撞名**——历史施工记录行不可改，靠字段语境消歧，呈作者知悉。现状锚点：AGENTS.md 142 行（L69 判定表行 / L79 §3.6 / L91 §4 引言——一审 F2 订正，L90 为空行 / L137 §7）；collab-log L59。

## 一、逐条裁定（12 项）

### 采纳（3）/ 部分采纳（1）

| # | 发现 | 裁定 | 落位 |
| --- | --- | --- | --- |
| F1 | D3（模式扫描，最轻）编号最大，与「编号越大越深」直觉相反 | 采纳：改 **D0＝模式扫描（最低基线）**，D1＝逐字、D2＝通读，编号单调递增；涟漪恰 2 处活文档 | 改动 1/2 |
| F2 | §7 缺「冷路径下沉」范式示例 | 采纳：§3.8 协议细则下沉 collab-log 即现成范式，一句注记；「下沉到 docs/standards/」一般化为「docs/ 下单点权威文件」（collab-log 在 docs/specs/，原句字面不含） | 改动 5 |
| F3 | §4 决策树与 §3.1 判定表功能相邻，缺交叉引用 | 采纳：§4 引言一句分流（规格与否查判定表；内生必要性查本节） | 改动 4 |
| F4 | 「<5 行」门槛与平凡定义不一致（两轮外审同点） | **部分采纳**：阈值**不改**（亮线防「感觉很小」式逃逸，2026-09-06 五批未走规格立例；模糊化重开判断题）；但在判定表行内**补亮线理由注记**，终结重复争议 | 改动 3 |

### 拒绝（8 项，留痕）

| # | 发现 | 拒绝理由 |
| --- | --- | --- |
| F5 | 「施工分身为默认形态」加「具备 spawn 能力时」条件化 | 唯宿主 Claude Code 恒有 Agent 工具（宪法既定前提）；为不存在的「无 spawn 环境」预留条件分支是投机性泛化（§6 精神）；「默认 + 备选」已表达优先级与兜底 |
| F6 | 预立法条文补校准触发机制（首批实例回填/三批无实例降级删） | 全仓预立法实例 = 0（现场核实）——为空集立法校准器本身即预立法（§3.9 反例）；首批真实预立法出现时随实例校准本机制 |
| F7 | §3.8 铁律改「铁律1/2/3」标签 + 细则清单 bullet 化 | ①②③ 已具可提取性；细则 bullet 化会**回填刚下沉的枚举**——019 去重刚完成，反向操作自相矛盾；膨胀之忧由 §7 处理序管辖 |
| F8 | §5 反模式「超 15 条分组或下沉」维护规则 | 二轮外审同点，维持拒绝：§7 体积纪律（300 行上限+超限处理序）已辖，行内重复立法；无实例预立法 |
| F9 | 必读文档表加「加载模式」列（常驻/按需） | 已覆盖——「何时必读」列即按需触发器，加列是同义重复（反模式 #4 邻近） |
| F10 | Python 探测链指向具体实现（check.sh/common.sh） | 前提不成立：探测链系会话内约定，check.sh 仅直调 `python`，无已实现真源可指；为它建 common.sh 属机制无事故驱动 |
| F11 | 反模式 #9 补「已机械化/未机械化纪律清表」 | 注册表类清单（§6 不做：引入须登记事故出身）；各纪律的工具化状态由其所在条文自表达 |
| F12 | D 级映射逻辑（已由 F1 采纳覆盖其主体） | — |

## 二、文件级改动清单

### 1. AGENTS.md §3.6（L79）D 级重排 [A]

整段替换为：

```markdown
**审计深度分级**：核验深度映射施工自由度，深度三级内联定义——D0＝模式扫描（残留词/格式/路径闭合，最低基线）、D1＝逐字核验（与规格文本 diff 为零）、D2＝通读核验（语义通顺、行为不变），**编号越大核得越深**；映射：[A] 级改动→D1、[B] 级→D2、[C] 级→按验收标准核验（不占深度级）；存量/知识文件最低 D0。**扫描只能证伪不能证真**——零命中仅表示"已知模式无违例"；扫出问题即升级该文件深度，待交付产物模板一律逐字。（2026-09-06 立例：矩阵 9/9 CLEAN 后仍需逐文件通读补审——深度此前无成文标准；2026-09-07 补 D 级内联定义、二轮外审改 D3 为 D0——扫描系基线而非最深档，编号越大越深才合直觉。）
```

### 2. collab-log L59 审计结论模板深度字段 [A]

`深度：{D1/D2/D3}` → `深度：{D0/D1/D2}`

### 3. AGENTS.md L69 判定表行补注记 [A]（一审 F7 注：行首 3 空格缩进保留，做子串替换不整行替换）

`| 单文件错别字/格式修正（<5 行） | ❌ 直接做 |` → `| 单文件错别字/格式修正（<5 行） | ❌ 直接做（亮线阈值防「感觉很小」式逃逸——两轮外审建议放宽均裁定维持；超限纯错别字走轻规格成本分钟级） |`

### 4. AGENTS.md §4 引言（L91）交叉引用 [A]

`新增任何文件 / 脚本 / 机制前，按序自问：` → `新增任何文件 / 脚本 / 机制前，按序自问（是否需要规格先查 §3.1 判定表；本节管内生必要性与索引同步）：`

### 5. AGENTS.md §7（L137）下沉句一般化 + 范式示例 [A]（一审 F3 修订：旧文本以代码块逐字引出）

旧文本（逐字，含行内代码反引号）：

```markdown
- **本文件上限 300 行**：超限先压缩；内容确实放不下时下沉到 `docs/standards/` 并在此留一行链接。
```

替换为：

```markdown
- **本文件上限 300 行**：超限先压缩；内容确实放不下时下沉到 docs/ 下单点权威文件并在此留一行链接（范式示例：§3.8 协议细则下沉 collab-log，宪法只留触发条件与铁律）。
```

### 6. CHANGELOG 条目 [B]

022 条目：宪法二轮外审 12 项裁定（3 采纳/1 部分采纳/8 拒绝）——深度级 D3 改 D0（扫描系最低基线，编号越大越深；涟漪 AGENTS/collab-log 两处）；判定表 <5 行行补亮线理由注记（阈值维持）；§4 补判定表交叉引用；§7 下沉句一般化 + §3.8 范式示例；拒绝施工分身条件化（唯宿主恒有 Agent）/预立法校准器（零实例空集立法）/反模式 15 条规则（§7 已辖，二轮维持）等各附理由；现场发现偏差 D 序号与深度 D 级字母撞名（历史行不可改，语境消歧，呈报）。

### 7. collab-log 施工记录登记 [C]（按表头既有格式自拟，一审 F5 更正自由度标注）

### 8. 本规格验收通过后归档 [A]（规划方自执：mv 至 `docs/specs/archive/` + 随批 add，一审 F5 补目标路径）

## 三、验收标准

- [ ] `grep -c "D0＝" AGENTS.md` = 1 且 `grep -n "D3" AGENTS.md` 仅命中 L79 立例句 1 处（「改 D3 为 D0」沿革句自带字样——一审 F1 更正：零命中断言会吞掉自家文本，此类陷阱第三次复发，断言预验立法呈作者）；collab-log 审计结论模板行为 `深度：{D0/D1/D2}`
- [ ] 活文档（AGENTS/collab-log 正文，施工记录历史行豁免）`grep "D1/D2/D3"` 零命中
- [ ] 判定表行含「亮线阈值」注记；§4 引言含「先查 §3.1 判定表」；§7 含「范式示例：§3.8」
- [ ] `bash scripts/check.sh` 全绿退出 0
- [ ] `git status` 四项逻辑变更：M×3（AGENTS.md、collab-log.md、CHANGELOG.md）+ 1 次规格归档 mv（暂存后呈 R 或 D+??，一审 F6 注明口径）

## 四、禁止事项

- 禁止改动施工记录行 1-8（历史 D 序号系偏差编号，非深度级——撞名问题呈报不擅改）
- 禁止改动 skills/、scripts/、standards 五文件、architecture.md、README.md
- 拒绝项 F5-F11 不得借本批夹带
- 禁止 git commit / push；禁止联网采集

## 五、施工方式

清单 8 项含 [B]——按成稿审查工序送审后交作者确认，再 spawn 施工分身执行，规划方核验三步，单批提交。与规格 020/021 无守卫交集、无宪法交集（它们不动 AGENTS.md），仅共用追加型登记文件，建议 020 → 021 → 022 顺序施工。

## 规格自审记录（铁律）

1. 锚点：L69/L79/L91/L137 与 collab-log L59 现场逐字核对（L91 系一审 F2 订正——初稿误写 L90）；预立法零实例、D3 涟漪两处、撞名问题均现场 grep 发现
2. 清单完备：3 采纳 + 1 部分采纳全部行级 [A] 文本；过程产物三件入清单
3. 验收可判定：grep 计数断言逐条预验（D0＝ 恰 1、D3 限立例句 1 处、斜杠形态施工后零命中）；历史行豁免口径经预验实际不需特殊处理
4. 矛盾检查：D0 重排与 collab-log 模板同步无游离；判定表注记不改变行语义（仍是 ❌ 直接做）；§7 一般化不与 file-conventions 的 standards 定位冲突（下沉目标泛化为 docs/ 单点权威）
5. 术语：亮线/范式/基线沿用既有口径；D 级撞名系呈报项不属本批改动
6. 独立审查分身一审结果：7 findings（1 P0 / 2 P1 / 4 P2）全采纳——F1（验收零命中断言吞自家立例句「D3」，**断言自噬类第三次复发**：020 断言吞替换文本、021 对象与计数错、022 本条）改「仅命中立例句 1 处」；F2（L90 系空行，实为 L91——锚点行号错+自审声明失真）订正；F3（旧文本反引号转义失真致逐字匹配必败）改代码块逐字引出；F4（偏差号实至 D5）口径订正；F5（登记项无逐字之物应标 [C]、归档补路径）更正；F6（git status 计数口径）注明；F7（判定表行首缩进）补子串替换注。P0=1 未达重审阈值
