# 规格：023 · 文件规范外部对照修正（命名总表 py 口径 + 对照裁定留痕）

> 本规格自包含。外审来源：外部 AI 问答材料（通用文件/Markdown/SKILL/references/scripts 内容规范），按参考研究同款处理——逐条对照我方既有规范裁定。现场核实（2026-09-07）：`scripts/` 实际文件 = check.sh（kebab）、check_content.py（**snake**）、hooks/pre-commit（kebab）；file-conventions 命名总表 L20/21 的 py 示例 `check-frontmatter.py` 系**虚拟示例，从未与实件对齐**——表与实件不一致（外部材料 Python snake_case 惯例照出自查缺口）。

## 一、逐条裁定（17 项）

### 采纳（1 项）

| # | 外部主张 | 裁定 | 落位 |
| --- | --- | --- | --- |
| F11 | Python 脚本 snake_case、Shell kebab_case | **采纳（自查缺口）**：实际 check_content.py 已是 snake_case 且正确，错的是命名总表的虚拟示例——修表不修文件（021 规格锚点引用 `scripts/check_content.py` 路径，改名会破坏待施工规格） | 改动 1 |

### 已覆盖（9 项，不改）

| # | 外部主张 | 我方既有对应 |
| --- | --- | --- |
| F1 | 标题逐级不跳级 | markdown-style §结构（H1-H3、逐级递进） |
| F2 | 列表缩进空格非 tab | markdownlint MD010 默认启用（配置仅豁免 MD013/MD040/MD024 参数化） |
| F4 | 中英文混排空格 | markdown-style §中文排版 |
| F6 | frontmatter name/description 规则与触发描述 | file-conventions §四 + skill-writing §一（触发方式 + 边界声明——官方 strong description 三要素，我方领先） |
| F7 | 渐进披露、主文件精简 | skill-writing §二体积分级（SKILL <500、references <300） |
| F8 | references 按主题拆分、结构化组织 | skill-writing §三四类节序 + 头注（消费点/边界）制度 |
| F10 | 关键指令必须在必读层 | 域壳 + 域工作流双层必读设计（SKILL.md 入口判定→workflow 固定第一读），方法论层才可选——头注消费点制度明示 |
| F14 | 退出码 0/1/2 | file-conventions 脚本编写规范首行（完全一致） |
| F15 | stdout/stderr 分离 | 同表「错误输出」行 |

### 有意分歧（3 项——我方有据更强，留痕防重提）

| # | 外部主张 | 我方裁定与依据 |
| --- | --- | --- |
| F3 | 代码块必须指定语言 | MD040 已禁用——选择性标注（markdown-style 规则取舍节：中文正文块无语言可标） |
| F5 | 行宽 80-100 字符 | MD013 已禁用——中文行宽与 diff 可读性权衡，已立法记录 |
| F9 | references 以 Markdown 链接引用 | **反向**：skill 资产禁链接（部署副本断链），用 skill 根相对路径文字 +「节名」（file-conventions §三、出身 mo-shu 教训） |

### 拒绝（2 项，留痕）

| # | 外部主张 | 拒绝理由 |
| --- | --- | --- |
| F12 | 脚本必须提供 `--help` | 用法说明已由错误路径覆盖（脚本编写规范「输入校验」行：缺参/非法→用法+退出 2）；强制 --help 属 7 脚本行为变更，无实例驱动 |
| F13 | 改动状态的脚本应实现 `--dry-run` | 采集脚本写文件即其产出，dry-run 语义不明；无需求实例 |

### 其余（2 项）

| # | 外部主张 | 裁定 |
| --- | --- | --- |
| F16 | 脚本独立性、不依赖项目环境 | 已覆盖（变体）：降级声明制度（CDP 不可用→SSR）+ agent-browser 依赖显式文档化——采集域的依赖是业务必要且已声明 |
| F17 | logging 模块/函数封装/主流程清晰 | **部分覆盖、不逐条立法**：现行脚本实践遵循；规范层由 I/O 契约行覆盖 stdout/stderr 与输出格式，代码风格类不立法（守卫简单性——一审 P2-3 收窄定性） |

## 二、文件级改动清单

### 1. file-conventions.md 命名总表补扩展名口径表注 [A]（一审 P0-1 更正插入点：表格**末行**之后）

命名总表末行「| 协作日志 | …」（L31）之后、空行（L32）与「### 脚本编写规范（运行时脚本必守）」标题（L33）之前插入表注（一审 P0-1 更正：初稿误锚 L21「开发测试」行——该行非表尾，其后尚有规格/工程规范/项目参照/根目录治理/工程配置/命令薄壳/架构/agent/协作日志共 10 行；按初稿锚点插入会把表注打进表格中部、下半表失去表头）：

```markdown
> 表注（扩展名口径）：`.py` 文件用 snake_case（下划线，Python 惯例），`.sh` / `.js` 用 kebab-case（连字符）——上表运行时脚本/开发守卫/开发测试三行规则列之 `.py` 后缀以本表注为准（`{动词}-{对象}` 连字符式仅适用于 sh/js；立法时虚拟示例 `check-frontmatter.py` 未与实件对齐，实件对照为 `check_content.py`）。
```

（不改 L19/20/21 原行文字——表注显式声明对三行 py 后缀的**覆盖修正**关系（一审 P1-2：表注与行文字字面互斥，须明示位阶而非自称兼容）；虚拟示例失真由表注自认。）

### 2. CHANGELOG 条目 [B]

023 条目：文件规范外部对照 17 项裁定（1 采纳/11 已覆盖含变体/3 有意分歧留痕/2 拒绝）——命名总表补扩展名口径表注（py snake_case，实件 check_content.py 对齐，表内虚拟示例失真自认，表注显式覆盖三行 py 后缀）；有意分歧三项固化留痕（MD040 选择性标注/MD013 行宽/资产禁链接）；拒绝 --help 与 --dry-run（无实例驱动）。

### 3. collab-log 施工记录登记 [C]（按表头既有格式自拟）

### 4. 本规格验收通过后归档 [A]（规划方自执：mv 至 `docs/specs/archive/` + 随批 add）

## 三、验收标准

- [ ] file-conventions 命名总表（末行「协作日志」之后）与「### 脚本编写规范」之间存在表注行，含「snake_case」「check_content.py」「以本表注为准」字样，前后空行
- [ ] `grep -c "check-frontmatter" docs/standards/file-conventions.md` = 2（不变型断言，基准值实测 2——一审 P2-1 锚入验收行）
- [ ] `bash scripts/check.sh` 全绿退出 0
- [ ] 本批逻辑变更：M×2（file-conventions.md、CHANGELOG.md）+ collab-log.md 登记 + 规格归档 mv——核验以 `git diff` 对照清单判定（共批脏工作区：022/立法批改动同时在树，status 计数不可直接用——一审 P2-2 注明语境）

## 四、禁止事项

- 禁止改动 scripts/ 实件文件名（021 规格锚点保全）、skills/、AGENTS.md、其余 standards
- 拒绝项 F12/F13 与分歧项 F3/F5/F9 不得借本批夹带
- 禁止 git commit / push；禁止联网采集

## 五、施工方式

清单 4 项含 [B]/[C]——按成稿审查工序送审后交作者确认，再 spawn 施工分身执行，规划方核验三步，单批提交。与 020/021/022 无文件交集（file-conventions 独占），共用追加型登记文件，序 020→021→022→023。

## 规格自审记录（铁律）

1. 锚点：scripts/ 实件清单与 file-conventions 表格全文现场核对——初稿误将 L21 当表尾（其后尚有 10 行至 L31 协作日志行），系一审 P0-1 抓出后现场复核更正；snake/kebab 不一致系本次对照自查发现（非外部材料直接指出——其仅给惯例，对表是我方动作）
2. 清单完备：唯一采纳项有逐字表注文本；过程产物三件入清单（铁律④对照 ✓：CHANGELOG/collab-log/归档齐）
3. 验收可判定：断言逐条预验（铁律④）——表注 grep 三关键词、基准值 2 实测锚入验收行、check.sh 实跑绿
4. 矛盾检查：表注与 L19-21 规则列的字面互斥已由表注「以本表注为准」显式覆盖修正（一审 P1-2：不自称兼容）；与 file-conventions §二双语命名纪律不冲突（那节管中英文，不管扩展名）
5. 术语：snake_case 系首次入规范文档（表注自带释义，符合首次出现处内联定义声明）；kebab-case 已有存量用例（§二/§8），不属新引入——一审 P2-4 更正初稿表述
6. 独立审查分身一审结果：7 findings（1 P0 / 2 P1 / 4 P2）全采纳——P0-1（插入点 L21 系表格中部非表尾，照插打断总表）锚点更正至 L31 后；P1-1（CHANGELOG 草案计数 1+12+3+2=18≠17）改 11；P1-2（表注与行文字字面互斥却自称「兼容」）改显式覆盖修正表述；P2-1（基准值锚入验收）/P2-2（共批脏工作区核验语境）/P2-3（F17 定性收窄）/P2-4（kebab 非新引入）一并修订。P0=1 未达重审阈值
