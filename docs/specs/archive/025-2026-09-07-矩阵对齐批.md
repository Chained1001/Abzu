# 规格：025 · 矩阵对齐批（存量文件对照内容规范矩阵审计与 retrofit）

> 本规格自包含。立法依据：file-conventions.md §五 内容规范矩阵（024 批）+ README 挂账行。审计于 2026-09-07 现场实测完成；**头注键形态计数经一审审查分身复核更正**（初稿「7/9 裸键」误计——file-conventions 自身头注即加粗键被漏数，真实为裸键 5 / 加粗 3，分母 8 个带版本头注的治理文档；键形态核数应脚本化，本规格断言即此形态）。本批 = 审计结论落档 + 缺口 retrofit（注释/头注级，**行为零变化**）。

## 一、审计结论（四类对象）

### A. 六壳 SKILL.md —— **PASS，零改动**

五占位壳五要素全齐（frontmatter/H1 未建设/定位句/行为规则×2/语言——逐壳 grep 实测 1/1/1/2/1）；scan 壳五节与 skill-writing §三 域壳模板节序一致。

### B. 脚本文档头（7 js + 2 守卫 = 9 文件）—— 四抓取器齐，5 文件缺项（retrofit）

| 脚本 | 用途 | 用法与参数 | 依赖与前置 | 维护入口 | 缺口 |
| --- | --- | --- | --- | --- | --- |
| qidian/fanqie/qimao/jjwxc-rank-scraper.js | ✓ | ✓ | ✓ | ✓（012 维护入口块，qidian L29 实证） | 无 |
| setup-cdp-chrome.js | ✓ | ✓ | △（隐含于用途行，无独立前置节） | ✗ | 补前置节与维护入口 |
| scan-analyze.js | ✓ | ✓（参数表） | ✗ | ✗ | 补依赖前置与维护入口 |
| cdp-utils.js | ✓ | ✓（使用方式） | ✓（前置节） | ✗ | 补维护入口 |
| scripts/check.sh | ✓ | ✓（用法块） | △（探测提示在代码内非头注） | ✗ | 补前置与维护入口 |
| scripts/check_content.py | ✓ | △（docstring 已含独立运行命令，无参数说明） | ✗ | ✗ | 补依赖/维护入口、扩用法 |

（retrofit 原则：**补缺不重排**——四抓取器既有结构保留；纯注释/docstring 改动，不改任何可执行行。）

### C. 治理文档头注（8 个带版本行文档）—— 加粗键 3 文件去粗 + 版本行 3 处修正 + 矩阵更正

实测（`grep -c "^> \*\*版本\*\*"`）：**裸键 5**（architecture/agent-design/markdown-style/release-and-versioning/skill-writing）vs **加粗键 3**（glossary、test-prompts、**file-conventions**——024 立法括注与 025 初稿均误计，一审更正）。处置：**全仓统一裸键**（多数派 5/8），三文件头注块去粗：

1. **去粗 3 文件**（头注块内键名去 `**`，正文不涉）：glossary（头注 3 行：版本/定位/用法）；test-prompts（头注块 6 行：版本/定位/评估闭环/断言分级/状态分组/前置）；file-conventions（头注 3 行：版本/定位/与术语表分工——正文 blockquote 中**仅域前缀适用判据块（L47）不动**；§五.2 示例三行恰被改动 1(a) 去粗，不在「不动」之列——二审 F-2 更正）。目标态：**在役治理文档**统一裸键（archive/ 归档规格系历史件豁免——二审 F-7）。
2. **版本行修正 5 处**（3 追溯 + 2 当批，二审 F-3 统一口径）：glossary「v0.1（2026-09-04 初版）」→ **v0.2 沿革式**（追溯：016 批「调用双层」术语增补未 bump）；test-prompts 同款 → **v0.2**（追溯：「审计与验收体系三补」批（4a08aa2）场景 6/7 修订未 bump）；architecture「v1.0（2026-09-05 作者拍板）」→ **v1.1 沿革式**（追溯：013/015/018 多轮修订）；file-conventions → **v0.4**（当批：矩阵更正与头注修复）；skill-writing → **v0.4**（当批：豁免条款；另数字上吸收 024 批占位壳段未 bump 欠账——二审 F-4）。
3. **矩阵 §五.2 更正三则**（024 审查误报的善后）：示例加粗改裸键；括注句更正为「键名不加粗——2026-09-07 实测存量 5/8 为裸键，024 立法时审查误报，025 更正并全仓统一」；**补两款**：③第三行变体——参照型文档（术语表/评估场景）第三行可用「用法」等变体行，与「依据」并列合法；④治理文档 bump 语义——实质内容修订 bump minor、纯措辞修正 bump patch（本批 4 处追溯 bump 即首批实例）。

### D. references 抽查 —— **PASS，零改动**（豁免落规范）

10 份头注两行式全齐；scan-workflow 中断契约在册（grep=1）；topic-decision-template 四段式在册；genre-trends 抽样节序合规（目录→主题分节）。「每节问句/操作/反例」结构系**新建文件**标准——存量移植知识文件不强改（落规范：skill-writing §三 方法论模板行尾补豁免句，防后续按矩阵审计重复发现此「缺口」）。

### 裁决项（呈作者，随规格确认一并拍板）

**AGENTS/README 头注不纳入三行式（推荐豁免）**：AGENTS 头部四段系内容型定位声明，压三行损信息；README 系门面无版本概念。矩阵该行同步改「豁免（AGENTS 内容型头部与 README 门面文档不适用三行式，2026-09-07 裁定）」。

## 二、文件级改动清单

### 1. file-conventions.md 矩阵 §五 三处微修 + 自身头注去粗与 bump [A]

(a) §五.2 三行示例去粗（`> **版本**：` → `> 版本：`三行同）；括注句整句替换为：「（键名不加粗——2026-09-07 实测存量 5/8 为裸键，024 立法时审查误报为加粗，025 更正并在役文档统一裸键（归档件豁免）；个别文档可加分工/配套类附加行（如本文件与术语表分工、architecture 配套行）；参照型文档第三行可用「用法」等变体行；治理文档 bump 语义：实质内容修订 minor、纯措辞 patch。）」（附加行措辞系二审 F-5 更正——原「第四行分工声明」与实文第三行分工不符）
(b) §五.1 末尾补一句：「四要素为目标清单，**顺序不强制**（既有头部结构按补缺处理，不重排）。」
(c) 矩阵表「治理与门面文档」行：「头注三行式（§五.2；AGENTS/README 现头注为内容型多段，是否纳入对齐由 025 立项时裁定）」→「头注三行式（§五.2；AGENTS 内容型头部与 README 门面文档豁免，2026-09-07 裁定）」。
(d) 自身头注 3 行键名去粗；版本行 → 「v0.4（沿革见 CHANGELOG 与 git 历史）」。

### 2. glossary.md 头注 [A]

头注 3 行键名去粗；版本行 → 「v0.2（沿革见 CHANGELOG 与 git 历史）」（「调用双层」016 增补追溯）。

### 3. test-prompts.md 头注块 [A]

头注块 6 行（版本/定位/评估闭环/断言分级/状态分组/前置）键名全部去粗；版本行 → 「v0.2（沿革见 CHANGELOG 与 git 历史）」（「审计与验收体系三补」批场景 6/7 修订追溯）。正文其余不动。

### 4. architecture.md 首行 [A]

「> 版本：v1.0（2026-09-05 作者拍板）」→「> 版本：v1.1（沿革见 CHANGELOG 与 git 历史）」。

### 5. skill-writing.md §三 方法论模板行补豁免句 + bump [A]

「**方法论参考**：①头部两行声明 ②按主题分节（每节：问句 / 操作 / 反例）③超 100 行加目录」行尾追加：「——问句式小节辖**新建**文件；存量移植文件以头注/目录/主题分节为合规底线，不强改（2026-09-07 025 裁定）」。版本号 v0.3 → v0.4（理由：本句为实质修订条款；另吸收 024 批占位壳段未 bump 欠账）。

### 6. 脚本文档头补缺 [B]（5 文件，行为零变化）

按 §一.B 缺口表逐脚本补齐（约束：注释/docstring 级；不改可执行行；`node --check`（setup-cdp-chrome/scan-analyze/cdp-utils 三文件）、`bash -n scripts/check.sh`、`python -m py_compile scripts/check_content.py` 全过，pycache 用后即删；四抓取器零改动）：

- setup-cdp-chrome.js：补前置节（Chrome 路径要求）与维护入口（平台/环境变更时改哪里）。
- scan-analyze.js：补依赖前置（Node 20+，零外部依赖）与维护入口（榜单 md 格式变更时改解析段位置）。
- cdp-utils.js：补维护入口（CDP 协议端点变更时改哪些函数）。
- scripts/check.sh：头部块补依赖前置（agentskills/markdownlint-cli2/Python 及缺失提示去向）与维护入口（新增检查项接入位置）。
- scripts/check_content.py：docstring 补参数说明（无参数，全量扫描）、依赖（Python 3 标准库，零外部依赖）与维护入口（新增检查维度接入位置）。

### 7. CHANGELOG 条目 [B]

025 条目：矩阵对齐批——审计四类结论（六壳 PASS/references 抽查 PASS 落豁免条款/5 脚本补缺/头注治理）；键形态实测更正（5/8 裸键，024 审查误报善后）与在役文档统一裸键；版本 bump 5 处（3 追溯〔glossary v0.2/test-prompts v0.2/architecture v1.1〕+ 2 当批〔file-conventions v0.4/skill-writing v0.4〕——治理文档 bump 语义首批实例）；AGENTS/README 豁免裁定；方法论存量豁免落规范；README 挂账勾销。

### 8. collab-log 施工记录登记 [C]（规划方自执）

### 9. 本规格验收通过后归档 [A]（规划方自执：mv 至 `docs/specs/archive/` + 随批 add）

### 10. README 挂账行销账 [A]（规划方随批执行——二审 F-6 补落点）

README「- [ ] 025 对齐批：…」行改「- [x]」（文件账本律：批次完成勾销挂账）。

### 11. file-conventions §六 维护节补「对齐批原则」条 [A]（作者 2026-09-07 会话授权修订——本批复盘立法）

「## 六、本规范的维护」节末追加一条：

```markdown
- **对齐类改动先审标准后审文件**：对齐批第一步核对标准条文自身的实文依据，再审文件符合度——2026-09-07 立例：命名总表虚拟 py 示例（023 抓出）、矩阵加粗键标杆系审查误报（025 抓出），两次均是标准错而非文件错。
```

## 三、验收标准（断言已预验，含计数基线）

- [ ] 去粗断言：`head -8 docs/glossary.md docs/test-prompts.md docs/standards/file-conventions.md | grep -c "^> \*\*"` 逐文件 = 0（预验基线：glossary 3 / test-prompts 6 / file-conventions 头注 3——`head -8` 限定头注域，正文 blockquote 不误伤）；skill-writing/architecture 头注无加粗键（本就裸键）
- [ ] 版本行断言：五文件（glossary/test-prompts/architecture/file-conventions/skill-writing）首块各含「版本：vX.Y（沿革见 CHANGELOG 与 git 历史）」且版本号为 v0.2/v0.2/v1.1/v0.4/v0.4
- [ ] file-conventions 全文件含「5/8」「025 更正」（§五.2 括注句）、「顺序不强制」（§五.1 末句）、「豁免，2026-09-07 裁定」（矩阵治理行）、「先审标准后审文件」（§六 维护节）四字样（落点分列——二审 F-1 更正）；skill-writing §三 方法论行含「不强改（2026-09-07 025 裁定）」
- [ ] file-conventions 终态 `grep -c "^> \*\*"` = 1（仅剩 L47 域前缀块——二审 F-2 补：§五.2 示例去粗的专项兜底断言）
- [ ] 脚本行为核验：`node --check` ×3（setup-cdp-chrome/scan-analyze/cdp-utils）过、`bash -n scripts/check.sh` 过、`python -m py_compile scripts/check_content.py` 过（pycache 即删）；`git diff --stat skills/abzu-scan/scripts/` 仅 3 个 js；`git diff --stat scripts/` 仅 check.sh 与 check_content.py；补缺 5 文件各含「维护」字样 ≥1
- [ ] `bash scripts/check.sh` 全绿退出 0
- [ ] 终态 git diff 对照清单：M×13（file-conventions/glossary/test-prompts/architecture/skill-writing/CHANGELOG/collab-log/README + 3 js + check.sh + check_content.py）+ 规格归档 mv（untracked → archive/，不入 diff 统计）

## 四、禁止事项

- 禁止改动四抓取器（qidian/fanqie/qimao/jjwxc）、六壳全部、skills/ 下任何 .md、AGENTS.md、其余 standards（markdown-style/release-and-versioning/agent-design）；README 仅允许改动 10（挂账行勾销）
- 禁止改任何可执行代码行与正文内容 blockquote（本批纯头注/注释级）；禁止 git commit / push；禁止联网采集

## 五、施工方式

清单 10 项含 [B]/[C]——一审 11 findings（2 P0）修订 → 二审 7 findings（0 P0 / 2 P1 / 5 P2）再修订——门槛程序走完，交作者确认，spawn 施工分身执行，规划方核验三步，单批提交。

## 规格自审记录（铁律）

1. 锚点：A/B/D 类审计现场实测；C 类键形态经一审复核更正（初稿误计 file-conventions，现断言即计数命令可复现）；test-prompts 修订批次经 git log 核实为「审计与验收体系三补」批（4a08aa2）——一审 P1-1 两说（016/022）均不采
2. 清单完备：retrofit 全落位（含 matrix 宿主自身头注与 bump、skill-writing 豁免条款）；A/D 零改动结论入档；裁决项附推荐；过程产物三件入清单
3. 验收可判定：断言全部预验且带基线（头注域 head -8 限定、版本行存在型、脚本行为命令、diff 范围按目录分列）——一审 P0-2 恒真断言已重写（`^> \*\*` 形态）
4. 矛盾检查：bump 语义本批立法本批首用（4 追溯 + 1 当批）；「补缺不重排」与 §五.1 新增「顺序不强制」句一致化；参照型第三行变体条款与 glossary 实文闭合；方法论豁免落 skill-writing（真源）而非矩阵（路由层）
5. 术语：裸键/加粗键、追溯 bump、头注域（head -8 操作定义）均内联释义
6. 独立审查分身一审结果：11 findings（2 P0 / 5 P1 / 4 P2）全采纳——P0-1（键形态误计 7/9 → 实测 5/8，file-conventions 自身加粗漏改 + 漏 bump）计数更正并补改动 1(d)；P0-2（`^\*\*` 恒真假绿 + test-prompts 6 行去粗范围缺失）断言重写 + 改动 3 扩全头注块；P1-1（bump 批次归属错误）git log 核实改「审计与验收体系三补」批；P1-2（M 计数）更正；P1-3（五处数字口径）统一；P1-4（省略号边界 + 第四行条款丢失 + 变体无立法）整句替换并补两款；P1-5（方法论豁免未落规范）补改动 5；P2 四条采纳。P0=2 → 送二审
7. 二审结果：7 findings（0 P0 / 2 P1 / 5 P2）全采纳——F-1（验收四字样断言范围错位至 §五.2）按落点分列；F-2（C.1「4 处不动」与 1(a) 示例去粗自相矛盾 + 示例去粗无断言兜底）更正 + 补终态计数断言 =1；F-3（bump 口径三处漂移）统一「3 追溯 + 2 当批」；F-4（skill-writing 024 欠账 + 括注字面风险）补理由改措辞；F-5（保留句「第四行」与实文第三行不符）附加行措辞更正；F-6（README 挂账销账无落点）补改动 10 + 禁令例外 + M×13；F-7（归档件豁免未界定）「在役文档」措辞。0 P0 免三审
8. 作者授权修订（2026-09-07「按推荐来做」）：补改动 11（§六 对齐批原则句——本批复盘立法，与矩阵同文件同批免二次 bump）；断言预验器（六发自噬的守卫化）另立豁免批随后执行
