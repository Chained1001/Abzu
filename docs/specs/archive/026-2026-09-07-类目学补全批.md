# 规格：026 · 类目学补全批（反向审计 R1-R4/R6 落地）

> 本规格自包含。来源：作者指令「从实际文件反过来看规范是否有完善空间」——反向审计七项发现中的 R1（skill 资产类目学缺口）、R2（台账类逃逸）、R3（共享库混类）、R4（微型守卫冗余）、R6（双版本制无说明）合成本批；R5（marketplace 元数据标准）挂账。现状锚点（2026-09-07 现场核验）：skill-writing §三 方法论行现行文本；file-conventions 命名总表 L19 运行时脚本行、矩阵 L90/92/95/96、§五.1 L105/107、§五.2 L115；collab-log 头注两行加粗键（`head -5 | grep -c "^> \*\*"` = 2）。断言基线预验（铁律④）：「各自为政」现值 1、「存量移植文件」现值 1、矩阵数据行现值 10。

## 一、改动要点（五项发现 → 设计）

| # | 发现 | 设计 |
| --- | --- | --- |
| R1 | 四类模板装不下知识库/格式规范/操作手册/命令目录——「存量豁免」是类目学缺口的补丁 | 方法论行**底线化 + 子类划分**：底线三条（头注/分节/目录）辖一切；「问句/操作/反例」标注为**决策方法论**子类形态；**知识素材类**按自然结构组织不强套问句式；子类划分取代存量豁免条款 |
| R2 | collab-log 无版本行+加粗键+从未入审计分母——台账类形态未定义 | 矩阵新增**台账类**行（头注两行式：定位/纪律，豁免版本行与依据行——活账本逐次追加，版本无意义）；collab-log 头注顺带去粗对齐 |
| R3 | cdp-utils.js 共享库与可执行工具混类 | 命名总表增**共享库**行（`{名词}-utils`，被 require 不直接执行，扩展名从表注口径）；矩阵脚本行「用法」段补库形态（导出函数清单）。README 门面「7 个脚本」计数**从宽不改**（裁定——共享库立类不追溯门面措辞） |
| R4 | 4 行的 pre-commit 被要求四要素注释块（比代码长） | §五.1 补弹性句：≤10 行微型守卫可合并为「用途 + 维护指向」两行 |
| R6 | 治理 vX.Y 与 skill X.Y.Z 双版本制并存无说明 | §五.2 补分工句（各管其域，互不换算）——落 file-conventions 免 release 再 bump |

## 二、文件级改动清单

### 1. skill-writing.md §三 方法论行底线化 [A]

现行整段替换为：

```markdown
**方法论参考**：①头部两行声明 ②按主题分节 ③超 100 行加目录——此三条为一切方法论类文件的底线；其下分两子类：**决策方法论**（指导选择的文件，如选题方法）每节用 问句 / 操作 / 反例 形态；**知识素材类**（知识库/格式规范/操作手册/命令目录——如题材库、字段词典、CDP 底座、采集指南）按其自然结构组织（字段表/步骤/命令块），不强套问句式。存量文件按子类归类即合规，不另设豁免。
```

### 1b. skill-writing.md §五.5 主语收窄 [A]（一审 P1-3 补：消解与新 §三 的字面冲突）

「5. 方法论用苏格拉底问句（问句迫使具体回答，杜绝"直面X"式标签答案）；模板空位用填空提示」→「5. **决策方法论**用苏格拉底问句（问句迫使具体回答，杜绝"直面X"式标签答案——知识素材类不强套，见 §三 子类）；模板空位用填空提示」

### 2. skill-writing.md 版本 bump v0.4 → v0.5 [A]

### 3. file-conventions.md 命名总表增共享库行 [A]

L19（运行时脚本行）之后插入（一审 P2-1 修订：扩展名口径从表注，避免 xxx-utils.py 连字符与 Python 惯例相悖）：

```markdown
| 共享库 | `{名词}-utils`（扩展名随表注口径：js/sh 连字符、py 下划线），被同域脚本 require，不直接执行 | `cdp-utils.js` | `skills/abzu-{域}/scripts/` |
```

### 4. file-conventions.md 矩阵三行更新 + 一行新增 [A]

(a) 方法论行（L90）：「skill-writing §三 方法论模板」→「skill-writing §三 方法论底线与子类」

(b) 脚本行（L92）整行替换为：

```markdown
| 运行时脚本与共享库（skills/*/scripts/*.js） | 无统一正文模板（工具程序） | 脚本文档头四要素（§五.1；共享库的「用法」段为导出函数清单） | 仓库既有惯例（Node 工具脚本无单一业界权威，如实标注）＋§一「脚本编写规范」契约 | node --check + 契约对照（审查轨） |
```

(c) 规格行（L95）之后插入台账类行：

```markdown
| 台账类（docs/specs/collab-log.md） | collab-log 自身节序（协作模板 + 核验协议 + 施工记录——一审 P2-3 对齐实节名） | 头注两行式（定位/纪律，裸键）——豁免版本行与依据行（活账本逐次追加，版本无意义） | 宪法 §3.8 协作协议 | 追加登记时规划方自核 |
```

(d) 矩阵定位注（L83）「本表管分类全覆盖与速查；」之后插入「**具体行优先于泛类行**（如 collab-log 归台账类行，不从治理文档行）——」（一审 P1-2 补：消解台账类行与治理文档行对 collab-log 的双重管辖）

### 5. file-conventions.md §五.1 补微型守卫弹性句 [A]

「四要素为目标清单，**顺序不强制**……」段之后追加：

```markdown
≤10 行的微型守卫（如 `hooks/pre-commit`）四要素可合并为两行：用途一句 + 维护指向一句。
```

### 6. file-conventions.md §五.2 补版本制分工句 [A]

括注段（L115）之后追加独立段：

```markdown
版本制分工：治理文档用两段 vX.Y（本节 bump 语义管辖）；skill 套件发布用三段 X.Y.Z（release-and-versioning §二「四处一致」辖套件发布版本，不含治理文档头注 vX.Y——一审 P1-4 补辖域限定；release 侧条文补限定词另批挂账）——两制各管其域，互不换算。
```

### 7. file-conventions.md §五.1 沿革括注删除 [A]（§3.9 沿革纪律顺带清理）

删除 L105：「（现状实证：七脚本均有文档头但节结构各自为政——025 对齐批按四要素统一。）」——立法沿革叙事，条款自身已足。

### 8. file-conventions.md 版本 bump v0.6 → v0.7 [A]

### 9. collab-log.md 头注去粗 [A]

「> **定位**：」「> **纪律**：」两行键名去 `**`（对齐 §五.2 裸键统一；台账类头注两行式自此成文）。

### 10. README.md R5 挂账行 [A]

「内容挂账」行之前插入：

```markdown
- [ ] 发布前置：marketplace.json 元数据（category/keywords/metadata.description）内容标准立法
- [ ] release-and-versioning §二「四处一致」补「辖套件发布版本」限定词（消解与治理文档头注 vX.Y 的字面张力——类目学批 R6 善后）
```

### 11. CHANGELOG 条目 [B]

026 条目：类目学补全批（反向审计 R1-R4/R6）——skill-writing §三 方法论底线化+两子类（决策方法论问句式/知识素材类自然结构，取代存量豁免）；矩阵补台账类（collab-log 头注两行式，豁免版本行）与共享库口径（命名行+脚本行导出清单）+ §五.1 微型守卫弹性 + §五.2 版本制分工句；collab-log 头注去粗；§五.1 沿革括注清理（§3.9 首例主动适用）；marketplace 元数据挂账。版本 bump：skill-writing v0.5 / file-conventions v0.7。

### 12. collab-log 施工记录登记 [C]（规划方自执）

### 13. 本规格验收通过后归档 [A]（规划方自执：mv + 随批 add）

## 三、验收标准（断言基线均已预验）

- [ ] skill-writing §三 含「不强套问句式」「归类即合规，不另设豁免」；`grep -c "存量移植文件" docs/standards/skill-writing.md` = 0（现值 1，旧句被替换）
- [ ] file-conventions 含「共享库」「微型守卫」「互不换算」「台账类」「导出函数清单」「具体行优先于泛类行」各 ≥1；矩阵数据行断言（一审 P1-1 重写，预验器实测抓出原命令形态错误；**基线以规划方复核实跑为准**：现值 11〔表头行 + 10 数据行〕，审查分身报 12 系其口径差，取实测）→ **12**（台账行入表）：`awk '/^## 五、内容规范矩阵/,/^## 六、/' docs/standards/file-conventions.md | grep -c "^| [^-]"`；`grep -c "各自为政" docs/standards/file-conventions.md` = 0（现值 1，L105 括注删除）
- [ ] `head -5 docs/specs/collab-log.md | grep -c "^> \*\*"` = 0（现值 2）
- [ ] README 含「marketplace.json 元数据」挂账行
- [ ] 版本行：skill-writing v0.5、file-conventions v0.7
- [ ] `bash scripts/check.sh` 全绿退出 0
- [ ] 终态 git diff 对照：M×4（skill-writing/file-conventions/collab-log/README）+ CHANGELOG + 规格归档 mv

## 四、禁止事项

- 禁止改动 skills/ 实件（含 cdp-utils.js——本批只立类目规则不改代码）、scripts/、AGENTS.md、其余 standards、architecture/glossary/test-prompts
- R5 系挂账不实施；禁止 git commit / push；禁止联网采集

## 五、施工方式

清单 13 项含 [B]/[C]——成稿审查送审后交作者确认，spawn 施工分身执行，规划方核验三步，单批提交。

## 规格自审记录（铁律）

1. 锚点：skill-writing §三 方法论行、file-conventions L19/L90/L92/L95/L105/L107/L115、collab-log 头注两行、README 挂账区——均现场逐字核验；断言基线四组预验（各自为政 1/存量移植 1/矩阵 10 行/collab-log 加粗 2）
2. 清单完备：五项发现全落位；过程产物三件入清单（铁律④对照 ✓）；R5 挂账
3. 验收可判定：计数断言带现值基线与预验；「各自为政」零命中不与 §六 立例句冲突（立例句不含该词——预验 grep 该词现值 1 恰为 L105）
4. 矛盾检查：子类划分与矩阵方法论行真源同步；台账类行与 §五.2 三行式的关系为显式豁免（版本/依据两行），不与治理文档行冲突；共享库行的 `{名词}-utils` 与 023 表注扩展名口径兼容（.js kebab ✓）；§五.2 版本制分工句与 release §一/§二 无重叠表述冲突
5. 术语：底线/子类/台账类/共享库/微型守卫均首次出现处自带释义；沿革括注清理系 §3.9 首例主动适用（此前均为事后清理）
6. 独立审查分身一审结果：8 findings（0 P0 / 4 P1 / 4 P2）全采纳——P1-1（矩阵计数断言命令形态错误：无参数读空 stdin/带参数计全文件 49——**断言预验器 dogfood 首次实战即抓出**，铁律④预验声明对这条不实，自认）改 awk 区间断言 12→13；P1-2（台账类行与治理文档行双管辖无裁决）补改动 4(d) 定位注「具体行优先于泛类行」；P1-3（新 §三 与 §五.5「方法论用问句」字面冲突）补改动 1b 主语收窄为「决策方法论」；P1-4（版本制分工句单侧立法，release §二字面张力）分工句补辖域限定 + release 侧修订挂账 README；P2-1（共享库 .py 与表注口径）行规则改「扩展名随表注」；P2-2（README 七脚本口径）裁定从宽不改入 R3；P2-3（实节名对齐）「施工记录表」→「施工记录」；P2-4（CHANGELOG 风格）[B] 施工方对齐。P0=0 免重审
