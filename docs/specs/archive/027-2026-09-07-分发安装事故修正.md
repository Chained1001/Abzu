# 规格：027 · 分发安装事故修正（真机首跑五发现）

> 本规格自包含。事故来源：2026-09-07 首次真机安装实测（作者于独立文件夹 test2-1 启动实测，安装命令在仓库根目录执行）暴露五项发现——F1 安装器将 tracked `skills/abzu-*` 六目录符号链接化（删 `.agents/` 后 24 文件悬空，git restore 被悬空链挡路，经「删链接条目 + restore」恢复）；F2 README 安装节「装入用户级」与安装器实际「项目级 vendor + 软链」行为不符（用户级 `~/.claude/skills/` 全程未被触碰）；F3 实测会话无命令执行工具而扫榜域全依赖脚本；F4「安装实测」挂账欠账 12 批次（本次即销账实证）；F5 卸载无文档、离线 cp 未按现行宿主验证。断言基线预验（铁律④）：README「目标项目目录/勿在本仓库/卸载」现值 0、architecture 死路径 `cp -r skills/abzu`（含尾空格形态）现值 1、test-prompts「命令执行」现值 0、check.sh「符号链接」现值 0、AGENTS 反模式第 9 行存在、`ls -L skills/abzu-scan/SKILL.md` 真目录成立。

## 一、逐条裁定（五发现）

| # | 发现 | 处置 |
| --- | --- | --- |
| F1 | 安装器在仓库内运行会 symlink 化 tracked skills 目录（P0 破坏性） | 双防护：check.sh 新增 [0] 真目录守卫（机制层）+ 反模式 #10 入册 + README 醒目警告（纪律层） |
| F2 | README 安装节与 CLI 实际行为脱节 | 安装节按实测口径重写（项目级 vendor、在目标项目目录运行、卸载说明） |
| F3 | 实测前置缺命令执行工具检查 | test-prompts 前置补一句（无命令执行能力的会话不可测） |
| F4 | 安装实测挂账欠账 | 本批即销账（CHANGELOG 留痕），无独立改动 |
| F5 | 卸载无文档；离线 cp 未按现行验证 | 卸载说明并入 README 重写；离线 cp 保留但标注「宿主用户级目录机制，随宿主版本可能变化」 |

## 二、文件级改动清单

### 1. scripts/check.sh 新增 [0] skills 目录完整性守卫 [A]

`echo "[1] agentskills 六壳 validate..."` 行之前插入：

```bash
echo "[0] skills 目录完整性（安装器 symlink 化检测）..."
for d in skills/abzu-*; do
  if [ -L "$d" ]; then
    echo "x $d 是符号链接——skills 目录被安装器 symlink 化（本仓库内运行了 npx skills add；恢复：删链接条目后 git restore skills/；参见 README 安装节警告与宪法反模式 #10）"
    fail=1
  fi
done
if [ -e .agents/skills ] || [ -f skills-lock.json ]; then
  echo "x 检测到 .agents/skills 或 skills-lock.json——安装器曾在本仓库内运行（vendor 副产品）；清理后重查"
  fail=1
fi
```

（头部注释「一键检查（宪法 §0）：六壳 skill 格式校验…」行同步改「skills 目录完整性（符号链接化检测）+ 六壳 skill 格式校验…」——含「符号链接」字样使验收断言 ≥2 成立，一审 P0-1 修正。）

### 2. README.md 安装节重写 [A]

「## 安装」整节（L7-L25）替换为（外层四反引号围栏，容纳内嵌 bash 块）：

````markdown
## 安装

**前置**：Node.js 20+；宿主会话须具备命令执行（Bash）工具——扫榜域全依赖脚本执行，无命令执行能力的会话不可用。番茄/七猫/晋江走 CDP 采集另需 Chrome 与 `npm install -g agent-browser`（一次性）。

**推荐（[skills.sh](https://www.skills.sh/docs/cli) 包管理器）**——项目级安装：在**要使用的目标项目目录**里运行（安装器将技能 vendor 到该目录 `.agents/skills/` 并建 `.claude/skills/` 软链，生成 `skills-lock.json`）：

```bash
npx skills add Chained1001/Abzu -y
```

> ⚠️ **勿在本仓库内运行**——安装器会把本仓库 `skills/` 下的目录替换为符号链接（tracked 内容悬空，check.sh [0] 会拦截）。若已发生：删除 `skills/abzu-*` 链接条目后 `git restore skills/`。

**离线备选（手动复制到宿主用户级目录——宿主机制，随宿主版本可能变化）**：

```bash
cp -r skills/abzu-scan ~/.claude/skills/abzu-scan
```

**卸载**：删除目标项目内的 `.agents/`、`.claude/skills/`、`skills-lock.json`。

安装后新开会话即可使用：敲 `/abzu-scan` 或直接说"扫一下起点榜"。

**本仓库内开发**：skill 在根目录 `skills/` 下（产品源码位，Claude Code 打开本仓库不自动加载）；实测一律在独立的目标项目目录走上面的安装流程，**禁止在本仓库内运行安装器**（见上方警告）。
````

### 3. architecture.md §五 部署循环更新 [A]

循环代码块中该行（**实文行首带 8 空格缩进，整行替换**——一审 P1-3 补锚点口径）`→ npx skills add Chained1001/Abzu -y（skills.sh 安装器，自动装入用户级；离线备选 cp -r skills/abzu ~/.claude/skills/abzu）` 替换为：

```markdown
        → 在目标项目目录 npx skills add Chained1001/Abzu -y（项目级 vendor + 软链——仓库内禁跑，check.sh [0] 守卫；离线备选 cp -r skills/abzu-scan ~/.claude/skills/abzu-scan）
```

### 4. docs/test-prompts.md 前置补句 [A]

「> 前置：安装 `npx skills add Chained1001/Abzu -y`（六壳全部装入），新开会话。」整行替换为：

```markdown
> 前置：在目标项目目录安装 `npx skills add Chained1001/Abzu -y`（六壳全部装入），新开会话；宿主会话须具备命令执行（Bash）工具与 Node.js 20+——无命令执行能力的会话不可测（扫榜域全依赖脚本）。
```

### 5. AGENTS.md 反模式清单增第 10 条 + §0 括注补守卫维度 [A]

(a) 反模式表第 9 行之后插入：

```markdown
| 10 | 在源仓库内运行分发/安装类工具（安装器把 tracked `skills/` 目录符号链接化，清理 vendor 目录后全量悬空） | 分发工具只在目标项目目录运行；仓库加真目录守卫（check.sh [0]）拦截 | 2026-09-07 首次真机安装实测（规格 027） |
```

(b) §0「skill 格式校验」行括注补维度（一审 P2-5 采纳）：「（六壳 validate + markdownlint + 内容轨；agentskills 探测链固化于该脚本）」→「（真目录守卫 + 六壳 validate + markdownlint + 内容轨；agentskills 探测链固化于该脚本）」

### 6. CHANGELOG 条目 [B]

027 条目：分发安装事故修正（首次真机安装实测暴露五发现）——check.sh 新增 [0] 真目录守卫（symlink 化 + vendor 指纹双检测，沙盘验证）；README 安装节按实测口径重写（项目级 vendor、目标项目目录运行、仓库内禁跑警告、卸载说明）；architecture §五 循环更新（含死路径 cp -r skills/abzu 清除）；test-prompts 前置补命令执行工具要求；反模式 #10 入册（在源仓库内运行分发工具）；「安装实测」挂账本批销账。

### 7. collab-log 施工记录登记 [C]（规划方自执）

### 8. 本规格验收通过后归档 [A]（规划方自执：mv + 随批 add）

## 三、验收标准（基线均已预验）

- [ ] `bash -n scripts/check.sh` 过；`grep -c "符号链接" scripts/check.sh` ≥ 2（守卫 + 头注）；check.sh 头部注释含「skills 目录完整性」
- [ ] 守卫沙盘（正向 + 双负向，验完即删；**造链姿势提示**——Git Bash `ln -s` 系复制模式造不出链接，用 `cmd //c "mklink /J skills/abzu-t1 <悬空目标>"` 造 junction（`test -L` 命中），一审 P1-2 补；沙盘件避免 .md 后缀防 lint 扫入，一审 P2-6 补）：① 现状真目录跑 `bash scripts/check.sh` 全绿；② 造 junction → [0] 段报「符号链接」fail=1 → 删后恢复绿；③ 造空 `.agents/skills/` 与 `skills-lock.json` → 报「vendor 副产品」fail=1 → 删后恢复绿
- [ ] README 安装节含「目标项目目录」「勿在本仓库内运行」「卸载」；`grep -c "cp -r skills/abzu " docs/architecture.md` = 0（死路径清除，现值 1——一审 P2-4 补文件参数）；含「仓库内禁跑」
- [ ] test-prompts 前置含「命令执行（Bash）」；AGENTS 反模式表 `grep -c "^| 10 " AGENTS.md` = 1
- [ ] `bash scripts/check.sh` 全绿退出 0（沙盘清理后终态）
- [ ] 终态 git diff 对照：M×5（check.sh/README/architecture/test-prompts/AGENTS）+ CHANGELOG + collab-log 登记 + 规格归档 mv

## 四、禁止事项

- 禁止改动 skills/ 六壳、其余 standards、glossary、scripts/ 其余文件（check.sh 限 [0] 段与头注行）
- 禁止在本仓库内再运行任何安装器（沙盘仅造符号链接与空目录，不跑 npx）
- 禁止 git commit / push；禁止联网采集

## 五、施工方式

清单 8 项含 [B]/[C]——成稿审查送审后交作者确认，spawn 施工分身执行，规划方核验三步，单批提交。

## 规格自审记录（铁律）

1. 锚点：README L7-25 安装节、architecture 循环行、test-prompts L8 前置行、AGENTS L112 反模式尾行、check.sh `[1]` echo 行——均现场逐字核验；断言基线六组预验（README 三词 0/architecture 死路径 1/test-prompts 0/check.sh 0/AGENTS 行 1/真目录成立）
2. 清单完备：五发现全落位（F4 销账式留痕）；过程产物三件入清单（铁律④对照 ✓）
3. 验收可判定：守卫沙盘三态（真目录绿/链接红/指纹红）可操作且用后即删；计数断言带基线与预验——守卫新增文案含「符号链接」两处（守卫体 + 头注），断言 ≥2 无自噬
4. 矛盾检查：[0] 守卫的 glob `skills/abzu-*` 在六壳真目录场景遍历目录条目，`-L` 仅符号链接命中——真目录不误报（预验 `ls -L`）；README 卸载说明与 F1 恢复步骤一致（删链接 + git restore，即本次事故的实际恢复路径）；反模式 #10 格式与表头一致；test-prompts 前置句与 README 前置句口径一致
5. 术语：项目级 vendor/symlink 化/vendor 指纹（.agents+lock 文件）首次出现处自带释义
6. 独立审查分身一审结果：7 findings（1 P0 / 2 P1 / 4 P2）全采纳——P0-1（**断言自噬第八发**：断言「符号链接」≥2 而 [A] 头注改文不含该词，沙盘实证 grep=1）头注行补「（符号链接化检测）」使断言成立；P1-2（Git Bash ln -s 系复制模式造不出链接——沙盘姿势改 cmd //c mklink /J junction，审查分身实测 -L 命中守卫正确报红）验收②补姿势提示；P1-3（architecture 旧行 8 空格缩进未引）补整行口径；P2-4（断言缺文件参数）补全；P2-5（AGENTS §0 括注枚举不全）采纳补「真目录守卫」；P2-6（沙盘 .md 件会被 lint 扫入）施工提示避免 .md 后缀；P2-7（悬空 .agents 极端场景 -e 漏检）知悉不改。P0=1 免重审
