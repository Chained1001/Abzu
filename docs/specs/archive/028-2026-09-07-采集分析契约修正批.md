# 规格：028 · 采集与分析契约修正批（两轮真机实测 12 项发现收编）

> 本规格自包含。来源：扫榜域两轮真机实测（起点 happy path + 番茄故障路径）暴露的契约失真与涌现实践，作者点名 10 项（F1-F4 + F-A/F-B/F-C/F-D + F-E + F-H），规划方顺带收编 2 项（F-G 报告模板可选节、CDP 用后清理——裁定表透明标注可否决），合计 **12 项**（一审 P1-1 更正：初稿漏 F3 裁定行且计数三处不齐）。断言基线预验（铁律④）：collection-guide「单页50-100」=1、「cap≈20/≤ 20 本」=2；workflow「题材方向（对话式）」=1（「对话式」全文 2 处，素材匹配问句不改——断言须限域）；analysis-guide/template 三新锚词 =0；SKILL.md version "0.3.1"=1；scan-analyze.js L163 文件过滤器现状（不读数据质量头，F-C 代码侧证实）。锚点均现场逐字核对（2026-09-07）。

## 一、逐条裁定（12 项）

### 采纳（10 项点名 + 2 项顺带）

| # | 发现（出处跑次） | 处置 | 落位 |
| --- | --- | --- | --- |
| F1（跑 1） | 指南「起点通常单页 50-100 条」实测移动端 SSR 单页 20 | 条数口径改实测 | 改动 1 |
| F2（跑 1） | newsign 移动端 404（榜单下线） | 目标表行标注「移动端已下线，须 --mode cdp」 | 改动 2 |
| F3（跑 1） | `--full` 在无 `--genre` 时无效果——进阶参数契约未说明适用范围（一审 P1-1 补裁定行，初稿遗漏） | 进阶参数行补适用范围说明 | 改动 3 |
| F4（跑 1） | 总推荐全 [待补]——SSR 模式不提供该字段，文档无字段-模式映射 | 起点目标表后补映射说明 | 改动 4 |
| F-B（跑 2） | 380 次详情请求触发番茄整站 /page/ 限流——礼仪无会话级预算与限流处置 | 礼仪新增两条（会话预算+限流处置 / CDP 用后清理）+ 同步修改一处（番茄口径） | 改动 5 |
| F-D（跑 2） | 番茄「每题材 cap≈20」实测页面只展示 10 本 | 两处口径改 10 | 改动 1/5 |
| CDP 清理（跑 2 观察，**顺带**） | 两跑均未执行 cdp-base 清理节（Chrome 跨会话残留） | 礼仪补「用后清理」条（并入改动 5） | 改动 5 |
| F-A（跑 2） | 方向问句标注「对话式」但第二跑自发弹窗——标注约束力弱，两跑模态不一致 | **标注改弹窗（4 选项：全榜概览找趋势/玄幻/都市/其他）**。裁定反转立例：前轮「保持对话式」的依据（防锚定）被实测推翻——标注不被遵循则标注与现实一致优先于与理想一致 | 改动 6 |
| F-H（跑 2） | 终裁记录系会话自发涌现（模板无此节），会话恢复正需要它 | 模板加「终裁记录」可选节（四字段全挂生产策略——一审 P0-1 修正）+ workflow 收尾句 + 方法文件生产步骤 | 改动 7/8/11 |
| F-E（跑 2） | 「分身读大文件出摘要 + 主会话锚点复核」模式成功但未沉淀 | analysis-guide 新增「大文件分析模式」节（含目录同步——一审 P1-5 补） | 改动 9 |
| F-G（跑 2，**顺带**） | 报告自加「跨平台对照」「降级补充」两节超出模板——多平台场景模板缺口 | 报告模板加两个可选节 | 改动 10 |
| F-C（跑 2） | scan-analyze 不跳 [存在问题] 文件——0/180 失败文件污染分布成「其他 180」 | 脚本按头部数据质量跳过并显式警告（唯一脚本改动） | 改动 12 |

### 无拒绝项（本批全部实测发现均采纳；两项顺带收编待作者确认时可否决）

**规范健康度备忘（一审 P2-4 呈作者，不在本批）**：skill-writing §五.2「禁用 UI 词汇（弹窗/界面）描述交互」与 §五.3 标注形态「（弹窗：选项列表）」字面冲突；存量封闭选择标注三形态并存（结构化选项 / AskUserQuestion 选项 / 弹窗：选项列表）——建议下批收口统一标注词形。

## 二、文件级改动清单

### 1. scan-collection-guide.md L35 条数口径改实测 [A]

「判断翻页（起点通常单页50-100条，番茄按题材逐页cap≈20）」→「判断翻页（起点移动端 SSR 单页 20 条/榜〔实测 2026-09-07〕，CDP/PC 端更多；番茄按题材逐页、每题材页面展示 10 条〔实测〕）」

### 2. scan-collection-guide.md L50 newsign 行标注 [A]（一审 P1-6：给完整替换行杜绝管道符歧义）

整行替换为：

```markdown
| 新人签约新书榜 | qidian.com/rank/newsign/ | 作者·题材·签约·免费/VIP·字数·总推荐·标签·简介（**移动端已下线**——m 站该榜 404〔实测 2026-09-07〕，须 `--mode cdp`） |
```

### 3. scan-collection-guide.md 进阶参数行补 --full 适用范围 [A]（F3，一审 P1-1 补）

进阶参数段（L42 区域）「scan-analyze `--dist` 强制输出题材分布（无 --genre/--dup 时默认已开启）。」句后追加：「`--full` 仅与 `--genre` 连用有意义（输出该题材完整条目含简介/标签）；无 `--genre` 时无效果〔实测 2026-09-07〕。」

### 4. scan-collection-guide.md 起点目标表后补字段-模式映射 [A]（一审 P1-3 修正锚点）

起点目标表**全部行之后**、「### 番茄采集目标」标题**之前**的空行处插入：

```markdown
字段-模式映射〔实测 2026-09-07〕：起点移动端 SSR 不提供「总推荐」（全部榜单 [待补]）与部分榜「字数」（新书榜缺）；需 `--detail yes` 补采或 `--mode cdp`（CDP 模式含「最新更新」字段，字段集更全）。
```

### 5. scan-collection-guide.md 采集礼仪节新增两条 + 同步修改一处 [A]（一审 P2-1/P2-2 修正：节名精确、口径统一）

「## 采集礼仪〔abzu 自定〕」列表（L123-127）末尾追加两条：

```markdown
- 会话级详情预算：单次扫榜会话对同一站点的详情页请求总量 ≤ 400〔abzu 自定〕；出现解析率骤降或详情页空白（限流征兆）时立即停采——冷却 ≥ 30 分钟再试一次，仍失败则未采部分降级（用户粘贴/内置知识）并如实标注，不硬闯（2026-09-07 番茄实测：380 次详情请求后整站 /page/ 限流、榜单页不受影响——两路由限流独立）
- CDP 用后清理：采集全部完成且后续为纯分析时，按 references/scan/scan-cdp-base.md「停止 / 清理」节释放 CDP Chrome，不留到下次会话（2026-09-07 两轮实测均未执行，立例）
```

另：「单次上限」行内「番茄每题材 ≤ 20 本」同步改「番茄每题材页面 ≤ 10 本〔实测〕」（F-D 第二处）。

### 6. scan-workflow.md L31 方向问句标注改弹窗 [A]（裁定反转立例）

「- 题材方向（对话式）：「有没有关注的题材方向？」」→「- 题材方向（弹窗：选项列表——全榜概览找趋势 / 玄幻 / 都市 / 其他；细分方向、否定表达与跨平台对照意图走「其他」自由输入）：「有没有关注的题材方向？」（原标注「对话式」两轮实测一轮自发弹窗、模态不一致——标注不被遵循则与现实一致优先，2026-09-07 改；素材匹配问句仍为对话式，见 Stage 5）」

### 7. scan-workflow.md Stage 5 收尾句挂接终裁记录 [A]

「选定后针对该题问素材匹配（对话式：询问是否有设定、灵感、参考作品等可纳入选题的积累）；」→「选定后针对该题问素材匹配（对话式：询问是否有设定、灵感、参考作品等可纳入选题的积累），选定与素材结论记入选题决策.md「终裁记录」节（模板可选节，写法决策与翻转条件按 scan-topic-decision.md 收尾步骤商定）；」

### 8. scan-topic-decision.md 收尾节补终裁生产步骤 [A]（一审 P0-1 修复：四字段全挂生产策略，解禁本文件）

收尾/交付节（「选定后」相关的生产指引处）追加：

```markdown
终裁记录生产步骤：选定选题并完成素材匹配问后——①「选定」与「素材状态」记对话结论；②结合素材答案与平台数据（如强爽/烟火气、主投端）提出「写法决策」与「翻转条件与验证动作」供作者确认；③四项一并落盘选题决策.md「终裁记录」节（模板可选节）。无生产策略的字段不入模板——本条即终裁四字段的生产策略（2026-09-07 收编自真机实测自发实践）。
```

### 9. scan-analysis-guide.md 新增「大文件分析模式」节 [A]（一审 P1-5 补目录同步、P2-3 补来源标注）

「## 扫榜报告模板」（L70）之前插入（**并在文件头部目录节追加对应一行**——目录条目与 H2 的一致性机检不查，纯扫描盲区，012 施工 D3 同型先例）：

```markdown
## 大文件分析模式（采集文件 > 1500 行时〔abzu 自定〕）

主会话不整读大文件——派提取分身逐文件分段读全（Read 带 offset），要求摘要结论全部带硬锚点（书名/作者/在读/字数）；主会话对关键锚点抽样 grep 复核（如 `grep -A1 "^### #1 {书名}"` 比对在读数）后方可写入报告——分身也会幻觉，锚点复核是采信前提（2026-09-07 番茄实测立例：两分身各读 2100 行文件、主会话抽验 7 锚点全中）。
```

### 10. scan-analysis-guide.md 报告模板加两个可选节 [A]

模板正文「## 关键数据洞察」节之后、「## 值得关注的方向」节之前插入：

```markdown
## 跨平台对照（可选——扫榜目录下有多次扫榜产物时）

- {对同方向候选在其他平台的验证/证伪，注明对照目录与日期}

## 降级补充（可选——含内置知识部分时）

- {降级部分的判断，全部标注「未实时校验」，注明降级原因与补扫建议}
```

### 11. scan-topic-decision-template.md 加「终裁记录」可选节 [A]（一审 P1-2 修正锚点）

(a) 模板正文末尾（最后一个「### 选题」节之后、文件尾）追加：

```markdown
## 终裁记录（可选——收尾选定后追加）

- 选定：{选题 N + 一句话}
- 写法决策：{平台与骨架取舍（如强爽/烟火气、主投端）}
- 素材状态：{已确认要点 / 待自检清单}
- 翻转条件与验证动作：{何种情况改选或改写法；下一步验证}
```

(b) 定位注记段「消费方」句尾追加「；收尾选定后追加「终裁记录」节，供会话恢复时汇报终裁状态（四字段生产策略见 scan-topic-decision.md 收尾步骤）」。（终裁记录系收尾元信息而非选题本体字段——字段定义表不加行、节内 {填空} 自足，规划方裁量理由：字段定义表管选题 17 字段，收尾元信息混入会污染字段表语义。）

### 12. skills/abzu-scan/scripts/scan-analyze.js 跳过质量不合格文件 [B]（一审 P2-5：路径从仓库根直书）

文件 `skills/abzu-scan/scripts/scan-analyze.js`，L163 文件过滤器处：解析每个文件头部 `- 数据质量：` 行——标 `[存在问题]` 的文件不进条目统计，输出顶部列出跳过清单（`⚠ 跳过质量不合格文件：{文件名}（{问题摘要}）——不计入分布与跨榜聚合`）。约束：注释级以上最小改动；`node --check skills/abzu-scan/scripts/scan-analyze.js` 过；沙盘验收（造一个 `[存在问题]` 头 + bookId 残行的假文件与一个正常文件，断言分布只计正常文件且警告出现，验完即删）。

### 13. skills/abzu-scan/SKILL.md version bump [A]

`version: "0.3.1"` → `version: "0.4.0"`（references 交互规则实质变更〔F-A 弹窗化〕＝minor）。

### 14. CHANGELOG 条目 [B]（一审 P2-7：落位 [Unreleased] > ### Changed 段顶部，仓库惯例）

028 条目：采集与分析契约修正批（两轮真机实测 12 项收编——10 项作者点名 + 2 项顺带）——条数口径实测化（起点 20/榜、番茄 10/题材）、newsign 下线标注、`--full` 适用范围、字段-模式映射、礼仪新增两条与同步一处（会话详情预算 ≤400 与限流处置 / CDP 用后清理 / 番茄口径）、方向问句弹窗化（裁定反转立例）、终裁记录入模板（四字段含生产策略，方法文件同步）、大文件分析模式成文（含目录同步）、报告模板两可选节（跨平台对照/降级补充）、scan-analyze 跳过 [存在问题] 文件（沙盘验证）；scan 壳 v0.4.0；扫榜域真机验收正式销账（两跑含故障路径全通）。

### 15. collab-log 施工记录登记 [C]（规划方自执）

### 16. 本规格验收通过后归档 [A]（规划方自执：mv + 随批 add）

## 三、验收标准（基线均已预验）

- [ ] collection-guide `grep -c "单页50-100"` = 0（现值 1）；`grep -c "cap≈20\|≤ 20 本"` = 0（现值 2）；含「移动端已下线」「字段-模式映射」「会话级详情预算」「CDP 用后清理」各 ≥1
- [ ] workflow `grep -c "题材方向（对话式）"` = 0（现值 1）；含「题材方向（弹窗」「终裁记录」各 ≥1；「对话式」现值 2 → **2**（L63 素材匹配原句 + L31 新注记内的说明性引用——一审 P1-4 更正预期值）
- [ ] analysis-guide 含「大文件分析模式」「跨平台对照（可选」「降级补充（可选」各 1 且目录节含「大文件分析模式」条目；collection-guide 含「--full 仅与」（F3）；template 含「终裁记录（可选」= 1 且定位注记含「会话恢复时汇报终裁状态」；topic-decision 方法文件含「终裁记录生产步骤」
- [ ] scan-analyze 沙盘：.tmp 造 `[存在问题]` 假文件 + 正常文件各一 → 分布仅计正常文件、跳过警告出现 → 删假文件复跑恢复；`node --check skills/abzu-scan/scripts/scan-analyze.js` 过
- [ ] SKILL.md `grep -c '"0.4.0"'` = 1（现值 0，"0.3.1" 清零）
- [ ] `bash scripts/check.sh` 全绿退出 0（沙盘清理后终态）
- [ ] 终态 git diff 对照：M×8（collection-guide/workflow/analysis-guide/topic-decision-template/topic-decision/scan-analyze.js/SKILL.md/CHANGELOG）+ collab-log + 归档 mv

## 四、禁止事项

- 禁止改动其余 references（cdp-base/output-format/genre-trends/reader-profiling/publishing-guide——**scan-topic-decision.md 解禁**，仅限改动 8 的生产步骤段）、其余五壳、scripts/ 其余文件
- F-A 仅改方向问句——素材匹配问句（对话式）与其余弹窗标注不得触碰
- 禁止 git commit / push；禁止联网采集（沙盘只造假文件不跑网络）

## 五、施工方式

清单 16 项含 [B]/[C]——成稿审查送审后交作者确认，spawn 施工分身执行，规划方核验三步，单批提交。

## 规格自审记录（铁律）

1. 锚点：collection-guide L35/L50/L121-127、workflow L31/L63、analysis-guide L57-70 结构（模板 H2 平铺于文件——可选节插入以「关键数据洞察」与「值得关注的方向」两节名为锚）、template 定位注记/正文尾、scan-analyze L163、SKILL.md version 行——均现场逐字核对；断言基线六组预验（含「对话式」限域口径）
2. 清单完备：12 项发现全落位（含两项顺带收编显式标注；F3 系一审补漏）；过程产物三件入清单（铁律④对照 ✓）
3. 验收可判定：计数断言带现值与预验；scan-analyze 沙盘三步可操作；「cap≈20/≤ 20 本」合并计数断言两处同清
4. 矛盾检查：F-A 弹窗化与 skill-writing §五.2 三规则一致（封闭 ≤4 弹窗——4 选项含其他，方向问句经实测收敛为准封闭）；「其他」自由输入保留开放性，锚定风险由「其他」槽承接（立例句写明）；终裁记录不改模板四段式立法（可选节在模板正文内，skill-writing §三 不动）且四字段全挂生产策略（改动 8——一审 P0-1 修复）；礼仪新增条款均在〔abzu 自定〕标注体系内
5. 术语：会话级详情预算/限流征兆/终裁记录/大文件分析模式首次出现处自带释义
6. 独立审查分身一审结果：14 findings（1 P0 / 6 P1 / 7 P2）全采纳——P0-1（终裁「写法决策/翻转条件」两字段无生产策略——**skill-writing §五.6「加字段没写 INSERT」同型教训重犯**）补改动 8 方法文件生产步骤并解禁；P1-1（F3 裁定行漏 + 计数三处不齐，实为 12 项非 13）补行并全量统一；P1-2（template「一句话」节锚点系记忆串档，实文末尾为最后一个「### 选题」节）锚点更正；P1-3（目标表行号只覆盖表头）锚点改节名定位；P1-4（「对话式」预期值 2→1 与自家 [A] 文本含说明性引用矛盾）预期值更正为 2→2；P1-5（新增 H2 未同步目录——机检盲区实锤）补目录行；P1-6（表格行「行尾追加」管道符歧义）给完整替换行；P2 七条（节名「停止 / 清理」/口径统一「两条+一处」/1500 行标〔abzu 自定〕/§五.2 与 §五.3 弹窗词形矛盾备案呈作者/路径直书/消费方句语义错位微调/CHANGELOG 落位）全采纳。P0=1 免重审
