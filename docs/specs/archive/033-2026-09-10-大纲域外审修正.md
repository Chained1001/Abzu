# 规格：033 · 大纲域外审修正（入口修订分支 + 产物检查措辞）

> 本规格自包含。依据：作者转交的第三方外审报告（2026-09-10，对 abzu-outline SKILL.md 的评审，评级 4.2/5）——按宪法 §3.1 外审纪律转为规格，逐条标注采纳/拒绝及理由（裁定表见 §四）。
> 本批采纳两项：①入口判定③补修订分支（真实设计对齐缺口）；②产物检查条款措辞精确化。其余三项拒绝（裁定与证据见 §四）。
> 前置：032 批（触发方式单一化）提交后施工——本批改动基线为 032 后工作树。

## 一、现状锚点（2026-09-10 现场核验，032 工作树）

- skills/abzu-outline/SKILL.md 入口判定③现行文：「当前阶段为『大纲-已完成』，或已进入卷级阶段（『卷 N-…』）→ 本域职责已完成，告知下一域命令 /abzu-volume 及其建设状态，不在本域执行。」——**与设计文档 §1.10 产物归属表冲突**：project.md「门控①后走修订流」、设定集「门控②后走修订流+变更事件」、总纲「门控②后走修订流」——即门控后修订属 outline 域职责，现行③一刀切移交 volume 把修订意图也挡在门外
- SKILL.md 门控与交互原则现行条款：「产物检查是提示不是门禁：依赖产物缺失时提示用户，不擅自代建。」——「提示」未定义停等还是推进标注，两种解读都通（外审缺陷⑤属实）
- 设计文档 §4.4：设定变更在大纲域（设定集是大纲域产物），影响检查在正文域，接口形态事件驱动；分期真源 §十五——write 域三期建设、设定变更事件四期管理深化——变更事件广播在本批只落「变更记录」落盘格式占位，跨域订阅留四期
- outline-workflow.md 断点续跑节覆盖大纲域四状态（未开始/立项-待确认/大纲-设定大纲并行/大纲-打磨中），无已完成后的修订路径
- outline SKILL.md version 现为 0.4.1（032 工作树）

## 二、文件级改动清单

### 1. skills/abzu-outline/SKILL.md 入口判定③拆分 [A]

改为两条：

```markdown
3. 当前阶段为「大纲-已完成」或已进入卷级阶段，且用户意图为**推进**（继续往下写）→ 本域职责已完成，告知下一域命令 /abzu-volume 及其建设状态，不在本域内执行。
4. 当前阶段为「大纲-已完成」或已进入卷级阶段，且用户意图为**修订/评审已有大纲产物**（改立项要素 project.md、改总纲、改设定、改人物卡、要求评审打磨）→ 进入修订流：重开对应工作流节（project.md 重开 Stage 1 相应交互——不留变更记录，git 历史即账；其余重开设定⟷大纲并行模式），修改走变更记录；涉设定集/总纲/人物卡设定面的修改须在设定集变更记录区登记（广播变更事件的跨域订阅机制随 write 域三期建设与四期管理深化落地——设计文档 §十五；本阶段以变更记录为真源）。
5. 用户意图属于其他 Abzu 域（扫榜、拆书等）→ 告知对应域命令及其建设状态，不在本域内执行。
```

（原④顺延为⑤；修订流与设计文档 §1.10/§4.4 对齐。）

### 2. skills/abzu-outline/SKILL.md 产物检查条款措辞精确化 [A]

「产物检查是提示不是门禁：依赖产物缺失时提示用户，不擅自代建。」改为：

```markdown
- 产物检查是提示不是门禁：依赖产物缺失时，暂停并列出缺失清单，询问用户自补还是授权代建；未获明确指令不推进依赖该产物的步骤，也不擅自代建。
```

### 3. outline-workflow.md 补「修订流」小节 + 涟漪两处 [B]

断点续跑节后新增：修订流入口条件（=SKILL.md 入口判定④）、流程（读产物 → 修改 → 涉设定集/总纲/人物卡设定面者在设定集变更记录区登记〔时间/字段/旧值/新值/影响范围——按 schemas 变更记录格式〕→ 汇报变更摘要；project.md 修订重开 Stage 1 对应交互）、边界（不重开门控——门控产物修订不改变已确认状态；作者明示重走 ⏸② 时当前阶段回写「大纲-打磨中」）。

涟漪同步两处：①节目录（头部）补「修订流」行；②断点续跑节末行「卷级阶段不属本域——域壳入口判定已移交 /abzu-volume。」改「卷级阶段的推进不属本域（移交 /abzu-volume）；已完成/卷级阶段的修订意图走下方修订流。」（与 SKILL.md 新④对齐）。

### 4. 版本 bump 与过程产物 [A]/[B]/[C]

- outline SKILL.md version 0.4.1→0.4.2（patch）；workflow 无版本号不动
- CHANGELOG 条目 [B]**（组队提交计划调整：条目由规划方于提交时点补记，外部施工方不动 CHANGELOG——035 开放项 2 另裁）**；collab-log 施工登记 [C]（规划方）；本规格归档 [A]

## 三、验收标准

- [ ] SKILL.md 入口判定五分支在位：③推进移交 / ④修订流 / ⑤其他域；修订流条含变更记录要求
- [ ] 产物检查条款含「暂停并列出缺失清单」「未获明确指令不推进」两要素
- [ ] outline-workflow.md 含修订流小节（入口条件/流程含 project.md 路径/变更登记/门控边界含 ⏸② 回退写法四要素）+ 目录「修订流」行 + 断点续跑节末行与新④对齐（「卷级阶段的推进不属本域…修订意图走下方修订流」）
- [ ] 设定集模板（outline-settings-template.md）变更记录区字段与修订流登记字段一致（时间/字段/旧值/新值/影响范围）
- [ ] version 0.4.2；`bash scripts/check.sh` 全绿退出 0
- [ ] 终态 git diff 对照：**交付点 M×2（SKILL.md/workflow）+ 提交段 M×2（CHANGELOG/collab-log，规划方）+ 归档 mv（A）**——组队提交计划口径（核验 P1-1 修正：原 M×4 系时序修订前残句）

## 四、外审裁定表（逐条采纳/拒绝及理由）

| # 外审发现 | 裁定 | 理由与证据 |
| --- | --- | --- |
| ①核心工作流内容空心化（血肉外挂单文件，引用失效即废） | **拒绝** | 前提不成立：references/outline/outline-workflow.md（160 行）与 SKILL.md 同目录**随 skill 一同分发**（skill 资产部署自包含——file-conventions 命名总表 skill 资产行），不存在「外部文件丢失」场景；壳/血肉分离是官方渐进披露模式（Agent Skills 规范：SKILL.md <500 行 + 按需加载 references），内嵌反而违反体积纪律并造双真源。引用闭合已由 check.sh [3] 内容轨守卫覆盖 |
| ②「评审」意图未覆盖 | **部分采纳** | 真缺口不在「缺评审模式」而在**入口判定③一刀切**：已完成后想改大纲/评审被挡去 volume——与设计文档 §1.10 修订流条款冲突（本批改动 1 消解）。评审能力本身已内置（Stage 2 轻打磨自检+设定三查；三评委处方化审五期物化 spawn）；「评外部稿/别人的大纲」属拆书域职责，不入大纲域 |
| ③脚手架定义缺失（落地产物不一致风险） | **拒绝** | 前提不成立：脚手架 = scripts/init-book.js（162 行确定性脚本，五目录五模板+创作进度初始化+schema:1，幂等）+ 模板六件 + outline-schemas.md 落位映射表——031 批建设并沙盘四态验证，模板正文围栏提取单一真源，不存在会话间漂移 |
| ④AskUserQuestion 非通用、缺 fallback | **拒绝** | 宪法 §6 明文：唯一宿主 Claude Code，不做第二宿主通用化；交互模态三规则按宿主组件立法。加纯文本 fallback 即做多宿主适配，触碰不做清单 |
| ⑤「产物检查是提示不是门禁」语义模糊（停等 vs 推进标注） | **采纳** | 属实：两种解读都通，与确定性设计相悖——本批改动 2 精确化（暂停+缺失清单+未获指令不推进） |

外审强项确认（无需改动）：状态机文件证据驱动、边界单一、门控位置、反黑话与不粘会话、版本元数据——与设计基线一致。

## 五、禁止事项

- 禁止把 workflow/模板内容内嵌进 SKILL.md（体积纪律+单一真源）；禁止加 AskUserQuestion 文本 fallback（宿主裁定）
- 禁止改动设计文档（§1.10 修订流为既有条款，本批是 skill 对齐它）
- 禁止 git commit / push（提交时作者单独确认）

## 规格自审记录（铁律①）

1. 锚点现场核验：入口判定③/产物检查条款现行文、设计文档 §1.10 权限表与 §4.4、workflow 断点续跑节范围、settings 模板变更记录区、outline version 0.4.1——均本日实读
2. 清单完备：两项采纳各落改动（③拆分/条款措辞/修订流小节/模板一致性断言）；三项拒绝有证据与条款锚点
3. 验收可判定：分支/条款要素/小节四要素/字段一致性/version/diff 计数均可查
4. 矛盾检查：修订流不重开门控的边界已写明（与 ⏸② 语义不冲突）；变更事件广播占位与 write 域三期边界一致；改动基线为 032 后工作树（时序开放项）
5. 术语：修订流/变更记录/推进意图 vs 修订意图首次出现处自带释义

## 审查记录

- 补审（独立审查分身，2026-09-10，作者指出工序违反后执行——本规格初版未送审即呈作者，违反 collab-log 触发线条款，立例见 034 发现 13）：8 findings（1 P0 / 5 P1 / 2 P2）全采纳——A1 workflow 断点续跑节「卷级阶段不属本域」与新④修订流冲突（涟漪漏项，改动 3 补两处同步）；A2 「随 write 域三期落地」与设计文档 §十五分期矛盾（跨域订阅改四期管理深化）；A3 修订流漏 project.md（§1.10 第一行即 project.md 修订流——④枚举与流程补路径，不留变更记录 git 历史即账）；A4 M×3 计数错改 M×4；A5 目录行同步漏；A6 登记范围口径两处不一统一为「涉设定集/总纲/人物卡设定面」；A7 「重走 ⏸②」补状态回退写法（大纲-打磨中）；A8 新⑤恢复原文「不在本域内执行」。P0=1<2 免重审，修订后交作者确认
