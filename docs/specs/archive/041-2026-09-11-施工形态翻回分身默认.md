# 规格：041 · 施工形态翻回「规划方 spawn 分身」默认

> 本规格自包含。依据：作者 2026-09-11 口述裁定——「我要修改施工流程，由你出规格和审查，然后你的分身来执行」。
> 变更实质：施工形态默认**翻回** spawn 施工分身——2026-09-10 的「外部窗口施工为默认」降为备选；**规划方位由持全局上下文的主会话 agent 担任**，施工在本会话内 spawn 分身执行、不经作者中转；作者回归纯裁决位（确认规格／确认提交／重大分歧拍板）。
> 沿革：本批系 035 号（2026-09-10 外部窗口升默认）的对称回翻，两次均为作者主动裁定、非事故驱动；沿革入 CHANGELOG，本规格只记现状与改动。

## 一、现状锚点（2026-09-11 现场核验）

- AGENTS.md:83 三角色条现文含「作者——确认规格、**在外部窗口执行施工（粘贴施工提示词）**、确认提交、重大分歧拍板」；同条**无**「纯裁决位」（逐文件实测 AGENTS.md 0 命中）
- AGENTS.md:84 现文：「**外部窗口施工为默认形态**（2026-09-10 作者裁定）：规格获作者确认后，规划方自 collab-log 模板派生施工提示词交作者，作者在外部 agent 窗口执行施工；审核与审计集中规划方（起草/审查/提示词/核验），施工外置。规划方 spawn 施工分身为备选形态（Agent 工具传规格路径与约束；适用：外部窗口不可用或作者明示）。2026-09-07 立例（014 首轮产出粘贴式提示词被作者纠正）针对的是当时「规划方应 spawn 而退回粘贴中转」的失职路径，不辖本裁定——本次系作者主动翻转默认。」
- AGENTS.md:81 门槛退役括注现文：「（原『大型』前置门槛退役，2026-09-07 外审裁定——**2026-09-10 形态翻转后维持**：门槛退役由规格化协作本身支撑，不依赖具体施工形态）」——括注内嵌形态时点，形态再翻即过时
- AGENTS.md:86 协议细则条现文含「协作记录逐次登记（规格/施工方模型/自由度分布/偏差次数/整改轮数/结果）」
- collab-log.md:19 节标题现文：「### 施工提示词（外部窗口施工为默认形态；分身为备选）」
- collab-log.md:22 现文：「你是施工方。任务：按规格施工 {规格路径}。你没有 git 写权限，也无需尝试提交；完成后只输出核验请求。」
- collab-log.md:30 形态注现文：「规划方 spawn 备选形态：首句身份改为「你是施工分身」，其余逐字不变（2026-09-10 作者裁定默认形态翻转——施工外置，审核审计集中规划方；施工方的【核验请求】与规划方的整改提示词经作者在两窗口间转交）。」
- collab-log.md:195 施工记录表注现文末句：「「施工方模型」列：外部窗口施工时记外部模型标识（作者提供）。」
- **预验基线**（写规格前实跑，铁律④。**口径＝逐文件计数、排除本规格自身引文**——本规格内引用相关字符串 2-6 次不等，按全仓计数会污染基线，故一律指定文件路径）：
  - `grep -c "外部窗口施工为默认形态" AGENTS.md` = **1**；同 pattern 对 `docs/specs/collab-log.md` = **1**
  - `grep -c "在外部窗口执行施工" AGENTS.md` = **1**
  - `grep -c "分身为默认形态" AGENTS.md` = **0**；同 pattern 对 `docs/specs/collab-log.md` = **0**
  - `grep -c "^你是施工分身。任务：按规格施工" docs/specs/collab-log.md` = **0**；`^你是施工方。任务：按规格施工` = **1**
  - `grep -c "纯裁决位" AGENTS.md` = **0**
  - `grep -c "外部窗口施工时记外部模型标识" docs/specs/collab-log.md` = **1**
- **涟漪面勘察**（三词分别 grep，逐词记命中，不合并计数）：`外部窗口` → AGENTS.md :83/:84；collab-log.md :19/:30/:195（现行文）＋:219/:221/:222/:225/:227（施工记录历史行）；CHANGELOG.md 沿革条目。`施工分身` → AGENTS.md :84；collab-log.md :197 与 :201-227（施工记录历史行）。`施工方模型` → AGENTS.md :86；collab-log.md :197。**architecture / standards / test-prompts / README / skills 对三词零命中**——本次改动不外溢；历史行与 CHANGELOG 沿革条目**豁免不改**
- 015 号批曾立「调用双层」相关条款（032 号已修订层次，本次不涉）：本次只翻**开发期协作形态**，不触 skill 运行时的调用双层与宿主裁定

## 二、文件级改动清单

### 1. AGENTS.md §3.8 三处修订 [A]

**替换范围（显式限定）**：三处均为**整行替换**——:81 整行、:83 整行、:84 整行；§3.8 其余各行（:82 空行、:85 铁律条、:86 协议细则条）**原样保留**，不得因替换而增删或重排。

**:81 门槛退役括注**改为（去形态时点，使论据与形态解耦）：

```markdown
8. **分工协作（规划方/施工方/作者）**：非平凡改动（判定表 ✅ 行）适用本条协作协议；平凡改动按本节 1-3 直接执行（原「大型」前置门槛退役，2026-09-07 外审裁定——门槛退役由规格化协作本身支撑，不依赖具体施工形态）。
```

**:83 三角色条**改为（整行替换）：

```markdown
    - **三角色**：规划方（持全局上下文）——勘察、写规格、派生施工提示词、成稿审查（spawn 审查分身）、核验审计、统一提交；施工方——只按规格施工，无 git 写权限（默认：由规划方 spawn 的施工分身；备选形态下为外部窗口施工，作者相应为施工执行者）；作者——确认规格、确认提交、重大分歧拍板（纯裁决位，仅在两确认与拍板时出手）。
```

**:84 默认形态条**改为（整行替换）：

```markdown
    - **施工分身为默认形态**（2026-09-11 作者裁定）：规格获作者确认后，规划方**主动 spawn**（Agent 工具传规格路径与约束），不退回作者粘贴中转、不经外部窗口——审核与施工在规划方会话内闭环。外部窗口施工为备选形态（适用：分身工具不可用或作者明示）。2026-09-07 立例（014 首轮产出粘贴式提示词被作者纠正）恢复直接适用——退回粘贴中转即失职路径。
```

### 2. collab-log.md 施工提示词节四处 [A]

**替换范围（显式限定）**：只动下列四行/句；同节其余（提示词正文 1-5 条、回传模板）与**独立审查分身提示词节（:105-114）、触发线条文（:181 区）原样保留**。

- **:19 节标题**整行改：「### 施工提示词（分身为默认形态；外部窗口为备选）」
- **:22 首句**改（该行仅替换首句，其余「你没有 git 写权限……只输出核验请求。」原样保留）：「你是施工分身。任务：按规格施工 {规格路径}。你没有 git 写权限，也无需尝试提交；完成后只输出核验请求。」
- **:30 形态注**整行改：「外部窗口备选形态：作者在另一窗口施工时，首句身份改为「你是施工方」，其余逐字不变（2026-09-11 作者裁定翻回分身默认——审核与施工在规划方会话内闭环；施工方的【核验请求】与规划方的整改提示词由规划方在会话内直接接续）。」
- **:195 施工记录表注末句**改（该行前半「自由度分布」口径句原样保留）：末句改为「「施工方模型」列：spawn 分身施工时记分身模型标识；外部窗口备选形态下记外部模型标识（作者提供）。」

### 3. CHANGELOG 条目 [B]

记形态回翻（作者 2026-09-11 口述裁定 + AGENTS 三处 + collab-log 四处 + 涟漪面零外溢实测）。落位：`## [Unreleased]` 段 `### Changed` 列表内、现有末条之后，条目以「- 041 号」开头。

### 4. collab-log 施工登记 + 本规格归档 [A]（规划方核验通过后自执，非施工方项）

登记行按新口径记：施工方模型＝分身模型标识；**自由度分布＝[A]×3 [B]×1**（本规格「文件级改动清单」共 4 项：改动 1/2/4 为 [A]、改动 3 为 [B]）；规格 mv 至 `docs/specs/archive/`。

## 三、验收标准

- [ ] `grep -c "外部窗口施工为默认形态" AGENTS.md` = **0**
- [ ] `grep -c "外部窗口施工为默认形态" docs/specs/collab-log.md` = **0**
- [ ] `grep -c "在外部窗口执行施工" AGENTS.md` = **0**
- [ ] `grep -c "施工分身为默认形态" AGENTS.md` = **1**（整串匹配，非子串）；该行同时含「主动 spawn」「不退回作者粘贴中转」「不经外部窗口」
- [ ] `grep -c "纯裁决位" AGENTS.md` = **1**；该行同时含「由规划方 spawn 的施工分身」
- [ ] AGENTS.md:81 整行**不含**「2026-09-10 形态翻转后维持」，且仍含「不依赖具体施工形态」
- [ ] `grep -c "^你是施工分身。任务：按规格施工" docs/specs/collab-log.md` = **1**
- [ ] `grep -c "^你是施工方。任务：按规格施工" docs/specs/collab-log.md` = **0**
- [ ] `grep -c "分身为默认形态" docs/specs/collab-log.md` = **1**（节标题整串）
- [ ] collab-log:30 形态注含「翻回分身默认」，且含外层备选句「你是施工方」
- [ ] `grep -c "外部窗口施工时记外部模型标识" docs/specs/collab-log.md` = **0**；同处含「spawn 分身施工时记分身模型标识」
- [ ] `grep -c "^- 041 号" CHANGELOG.md` = **1**（位于 `## [Unreleased]` 段 `### Changed` 列表内）
- [ ] `bash scripts/check.sh` 全绿退出 0
- [ ] 终态 git diff 对照：M×3（AGENTS/collab-log/CHANGELOG）＋归档 mv（至 `docs/specs/archive/`）；以本规格文件集口径计，工作树含其他并行批时不影响本断言

## 四、开放项（施工前需作者确认）

1. **外部窗口形态去留**：本规格主选为**降为备选**（对称回翻、可逆、改动最小）；备选方案为**完全退役**（删净外部窗口相关条文，collab-log 形态注整条删除）。*审查分身建议维持主选*：退役会连带删形态注并改动 4 处措辞，超出「只翻默认形态」范围。请裁。
2. **「规划方由主会话 agent 担任」是否入条文**：本规格按角色制立法（只写「规划方（持全局上下文）」，不点名具体会话）；若要写成显式指名条款，请明示措辞。
3. **审查主体确认**：本规格的成稿审查按 collab-log 判据核心交 **fresh 独立审查分身**执行（非规划方自审——维度⑥「规范健康度」规划方有确认偏袒，013 三轮自审未发现/fresh 分身首跑即报）。作者「由你出规格和审查」据此落实为「由规划方派审查分身」。已由本次实跑印证可行。请确认此落实方式。
4. **施工分身模型档位**：表注只记「分身模型标识」，是否需要为不同批次指定档位（如重批用强模型、治理措辞批用轻模型）？本规格不预设（§3.9：无实例不预立法）。

## 五、禁止事项

- 禁止改动分工三层原则、P0/P1 偏差分级、核验协议、独立审查分身触发线与工序（本次只翻默认形态与作者角色位）
- **注意分节**：collab-log「施工提示词」节（:19-31）与「独立审查分身提示词」节（:105-114）是**两节**——本批只动前者，后者与触发线条文（:181 区）原样保留
- 禁止改动 skill 运行时内容与宿主相关条文（唯一宿主 Claude Code 裁定不变）
- 禁止 git commit / push（提交时作者单独确认）

## 规格自审记录（铁律①）

1. 锚点：AGENTS:81/:83/:84/:86 与 collab-log:19/:22/:30/:195 逐行 read 实读；涟漪面三词分别 grep；预验基线 8 条逐文件实跑（含规格自身引文计数 2-6 次的口径声明）——无凭记忆
2. 清单完备：变更实质三要素（默认回翻／规划方位／作者归位）全部落位；连带项（:81 形态时点括注、:195 表注口径、:83 备选形态下作者角色）显式列为改动项而非沉默处理；外部窗口去留列为开放项
3. 验收可判定：13 条断言**一条一项**、全部给精确 grep 命令与文件路径；整串/子串口径显式（`施工分身为默认形态` vs `分身为默认形态`）；与 [A] 替换文本不互斥（目标串在替换文本中零出现，已实测）；含零命中目标的替换文本用阿拉伯数字/引号回避汉字自噬
4. 矛盾检查：新形态与三角色/三层原则/触发线/核验协议无冲突；与 035 为对称回翻而非否定；014 立例辖域随形态恢复，已在 :84 显式写明
5. 术语：施工分身／外部窗口／纯裁决位首现处自带释义或指向既有条文
6. 节号复核：本规格引用 AGENTS §3.8、collab-log「施工提示词」节与「独立审查分身提示词」节——逐个现场核对标题
7. 涟漪清单：本批非域建设批，test-prompts 不涉；涟漪面按「形态词分词 grep」列全（AGENTS/collab-log/CHANGELOG 三类，前两类改、CHANGELOG 沿革不改）

## 审查记录

- **一审（独立审查分身，fresh context，2026-09-11）**：findings 共 **11：P0:0 P1:3 P2:8**｜**结论：修订后通过**（P0=0＜2，按 collab-log「P0≥2 修订后须重新送审」规则，修订后无需再送审，径交作者确认）。
  - **P1-F1** 改动 3（CHANGELOG [B]）在验收节零断言——违铁律④「过程产物三件」闭合要求 → **采纳**：验收补 `grep -c "^- 041 号" CHANGELOG.md` = 1 并限定落位。
  - **P1-F2** 预验基线口径不清（规格自身引文污染计数：外部窗口施工为默认形态 全仓 4 vs 目标文件 2） → **采纳**：基线改为逐文件计数并显式声明排除本规格自身。
  - **P1-F3** `分身为默认形态` 与 `施工分身为默认形态` 整串/子串口径混用、同 token 一处要求 0 一处要求 ≥1 → **采纳**：验收改为指定文件路径 + 整串匹配并注明。
  - **P2-F4** :83 括注「由规划方 spawn 的施工分身」与 :84 备选形态不自洽 → **采纳**：改「默认：…；备选形态下为外部窗口施工，作者相应为施工执行者」。
  - **P2-F5** 改动 1 三处未写替换边界 → **采纳**：补「三处均为整行替换，§3.8 其余各行原样保留」；改动 2 同步补边界句。
  - **P2-F6** 验收一条混装多断言 → **采纳**：全部拆为一条一项。
  - **P2-F7** 涟漪面行号范围不准（AGENTS 三词并集误读为单词命中；collab-log 历史行起点应为 :197） → **采纳**：改为逐词记命中并修正范围。
  - **P2-F8** 改动 4 未写自由度分布 → **采纳**：补 [A]×3 [B]×1。
  - **P2-F9** 015 号句未标 032 已修订 → **采纳**：补括注。
  - **P2-F10** 禁止事项未点明「施工提示词节 ≠ 独立审查分身提示词节」 → **采纳**：补分节提醒。
  - **P2-F11** 备选形态下作者角色未声明 → **采纳**：并入 :83 括注（与 F4 同修）。
  - 审查另附已核验为真项：行号锚点零漂移；涟漪面零外溢独立复核成立；035 对称性逐条成立；§3.9 沿革纪律（拟写入文本无批次叙事，`2026-09-11 作者裁定` 属协议要求的例外授权依据）；断言与替换文本主体不互斥。
  - 审查环境限制留档：审查会话 bash/grep 不在 PATH，6 条命令断言经由 PowerShell 等价手段复跑，`:66 bash scripts/check.sh` 须由具备 Git Bash 的会话按核验协议第二步独立重跑。
- **核验修正（规划方，2026-09-11 · 核验三步 + 批次扩展复跑）**：
  - 修正 1｜CHANGELOG 条目落位：施工方按本规格「改动 3·现有末条之后」落于 2026-09-10 旧治理组末尾（:20），与 `### Changed` 的**新组置顶**体例不符（b833d66 即以 prepend 落位 037-040）→ 改置列表顶端（现 L9）。**根因在本规格措辞含糊**，非施工偏差（施工方已于 P1-2 声明并保留判断）。
  - 修正 2｜施工记录表结构：collab-log 施工记录表存在两处空行断点（原行 26、27 已脱离表体、渲染为普通段落）→ 去断点，1-28 行并为单表。
  - 修正 3｜**断言 10 口径（断言自噬·批次扩展暴露）**：原断言「`翻回分身默认` @collab-log = 1」在改动 4（施工登记）落盘后变为 **2**——登记行含规格文件名 `041-…-施工形态翻回分身默认.md`，该串被再次命中。裁定口径改为**行锚定**：`^外部窗口备选形态：.*翻回分身默认` = 1（实测 1）。原断言保留在册存沿革；立例引用见 collab-log 规格自审记录模板第 3 项（零命中目标须回避自噬）。
  - 复跑结论：修正后 12 条断言**在最终工作树全部满足**（含 M×3 改动面、check.sh 退出 0）。
