# 规格：043 · 术语层级收口与命名对齐（采风层级义正名 / 长篇标尺正名）

> 本规格自包含。依据：作者 2026-09-11 两句裁定——①「采风门之类这个步骤应该叫收集资料之类的是不是更好，**采风应该是这个步骤的动作**」；②「**长篇标尺.md 这个命名不准确，应该叫参考标尺**」。
> 变更实质：**纯命名批，零设计语义改动**——只改阶段层级名、门控名、状态值、书目录名与跨阶段资产名；「采风」作为**动作／角色／票据义一律保留**（作者裁定的正是这个分层：词留给动作，层级名让给「资料收集」）。标尺「参考而非硬指标」的**用法机制**不属本批，见 044 号规格。
> 沿革：沿革入 CHANGELOG，本规格只记现状锚点与改动清单。

## 一、现状锚点（2026-09-11 现场核验）

### 1.1 「采风」在真源文档中一义两用（实测 39 处）

- 设计文档「采风」**39 处**、追踪档 **17 处**；glossary 已有权威定义（工作流类第 25 行）：「**问题驱动的定点资料调研**——创作卡点处按当前需要检索参照（史料/范式/氛围/节奏/技巧），区别于扫榜（市场面）与拆书（整本机制）｜field research」——**动作义**。
- 而设计文档第二章及其连带的阶段名/门控名/状态值/书目录，指的是「**参考作品深采**」这一**阶段**（产出 `采风/参考-{书名}.md` 档案与标尺），与 glossary 定义所指不是同一件事——同词两义，正是追踪档 S44 复核记录在册的 A 项。
- 判据（作者原话映射）：**「采风」= 这个步骤的动作** → 动作义留；**阶段/门/状态/目录/阶段产物名 = 这个步骤本身** → 让位给「资料收集」系。

### 1.2 正名映射表

| 层级角色 | 现名 | 正名 | 判据 |
| --- | --- | --- | --- |
| 阶段名 | 采风步 | **资料收集步** | 作者原话「这个步骤应该叫收集资料之类」 |
| 门控名 | 采风门 | **资料门** | 门名取阶段名首二字，与「规格门←规格步」「定稿门←定稿步」同构（见开放项 1） |
| 状态值 | 大纲-采风中 | **大纲-资料收集中** | 与「大纲-规格中/草稿中/打磨中」同构 |
| 书目录 | `采风/` | **`资料/`** | 目录名随阶段名；与既有用户话术「资料包」对齐 |
| 阶段产物 | 采风包／采风⑧⑨⑩字段 | **资料包／档案⑧⑨⑩字段** | 前者与既有话术「看资料包」自相矛盾；后者与全文惯用简称「档案」对齐 |
| 四拍第三拍 | 拍三·标尺校验 | **拍三·量级参照** | 「校验」含硬指标义，与作者对参考标尺的定性冲突（机制见 044） |
| 跨阶段资产 | 长篇标尺.md | **参考标尺.md** | 作者原话② |

### 1.3 预验基线（写规格前实跑，铁律④；口径＝逐文件逐 token 计数）

设计文档（`docs/product/大纲阶段设计文档.md`）「采风」**39 处**，逐处归属后为 **改 23 处字面量（分布 22 行）／留 16 处**，合计 39、无第三类。两个计数口径的换算关系（下表 token 计数＝施工单元；字面量计数＝完备性口径）：

- **改**：token 计数 **24**（下表十二个 token 之和），其中 1 处重叠——`进入采风/` 上的两个 token（`进入采风` 与 `采风/`）落在同一个「采风」字上 → 去重后 **23 处字面量**
- **留**：token 计数 **15**（下表九个 token 之和），另 **1 处**为变更记录区 v0.4 变更行内的裸词「采风」（原文为「采风「争议与雷点」维度」——不匹配任何 token，属沿革行）→ 合计 **16 处字面量**
- **校验**：23 ＋ 16 ＝ **39** ✓（与文件级实测一致）

- **应改 token 基线（合计 24）**：`采风步`=1、`采风门`=5、`采风中`=3、`采风/`=6、`采风包`=1、`采风⑧`=2、`采风⑨`=1、`采风⑩`=1、`采风等量`=1、`进入采风`=1、`规格先于采风`=1、`作者积累+采风`=1
- **应留 token 基线（合计 15 处 / 15 行）**：`触发采风`=3、`采风任务`=2、`采风助手`=1、`采风需求`=3、`采风补充`=1、`开始采风`=1、`采风分派`=1、`采风预算`=1、`采风档案`=2（其中 `采风预算`／`采风档案` 属沿革行，见 1.4）
- **第 16 处留点**：变更记录区 v0.4 变更行内的裸词「采风」（不匹配任何 token；沿革行，见 1.4）
- `长篇标尺`：设计文档 **16 处（15 行）**、追踪档 **1 处**
- `标尺校验`：设计文档 **2 处**；`采风包`：设计文档 1 处；`资料包`：设计文档已有 **3 处**（正名后 3+1=4）
- `skills/abzu-outline/SKILL.md`：「采风」「标尺」**均 0 命中**——本次正名**无 skill 涟漪面**
- glossary「采风」相关词条 4 条：`采风`（动作义，释义需补层级限定）、`采风票据`／`采风员`／`内容比对校验`（动作与角色义，**不动**）

### 1.4 沿革豁免清单（改词断言必须排除，否则断言自吞）

- 设计文档变更记录区的历史变更行（**v0.2／v0.4／v0.8／v0.9 四行**）：`采风预算`（v0.2）、`采风需求`＋裸词「采风」（v0.4）、`采风档案`（v0.8）、`采风档案`（v0.9）
- 追踪档 S37/S40/S44 行（历史复核记录，含 `采风门`／`大纲-采风中`／`采风/` 等旧形态引文）
- `CHANGELOG.md` 全部历史条目；`docs/specs/archive/**` 全部归档规格
- 本规格与 044 号规格正文（引文必然含旧形态）

## 二、文件级改动清单

> 锚点纪律：下表用**行锚文本**定位（行号会随施工漂移，锚文本为准）。凡写「原样保留」处，行内其余文本逐字不动。

### 1. 设计文档：阶段/门/状态/目录/阶段产物系（22 行 24 处）[A]

**替换范围（显式限定）**：仅下列 **20 条锚条目（22 个定位串）**覆盖的 **22 行**——20 个条目对应 22 个定位串（其中 1 条含「数据源＝采风⑧/⑨/⑩字段」3 串，跨 3 行相邻同类改动），22 个串与 22 行一一对应。这 22 行内共 **24 处**「采风」字面量 ＝ **23 处改点**（§1.3 去重后的改点字面量）＋ **1 处保留**（1.2 交互话术行的 `开始采风`；该行同时含 2 处改点）。每行除所列片段外逐字保留。

- 锚文本「创作输入=作者积累+采风+方法论+灵感库」：`作者积累+采风+方法论+灵感库` → `作者积累+资料+方法论+灵感库`
- 锚文本「**门控命名制**（规格门/采风门/定稿门」：`（规格门/采风门/定稿门——阶段级硬停）` → `（规格门/资料门/定稿门——阶段级硬停）`
- 锚文本「参考档案（第二章才采」：`规格先于采风，不拿外部书倒推商品意志` → `规格先于资料收集，不拿外部书倒推商品意志`
- 锚文本「弹窗「规格定了吗？定了就开始采风。」」（本行 2 处，`开始采风` 保留）：
  - `[确认，进入采风/要改/先停下]` → `[确认，进入资料收集/要改/先停下]`
  - `翻「大纲-采风中」` → `翻「大纲-资料收集中」`
- 锚文本「## 第二章 采风步」：`## 第二章 采风步（参考深采+标尺实测）` → `## 第二章 资料收集步（参考深采+标尺实测）`
- 锚文本「作者确认在 2.4 采风门完成」：`在 2.4 采风门完成` → `在 2.4 资料门完成`
- 锚文本「④产物：`采风/参考-{书名}.md`，头部字段」：`采风/参考-{书名}.md` → `资料/参考-{书名}.md`
- 锚文本「且采风⑧⑨⑩字段为数据源」（本行 3 处）：`采风⑧⑨⑩字段` → `档案⑧⑨⑩字段`
- 锚文本「### 2.4 采风门（门控）」：`### 2.4 采风门（门控）` → `### 2.4 资料门（门控）`
- 锚文本「弹窗「采风包和标尺过目吗？」」：`采风包和标尺过目吗？` → `资料包和标尺过目吗？`
- 锚文本「当前阶段五值」：`**大纲-规格中/采风中/草稿中/打磨中/已完成**` → `**大纲-规格中/资料收集中/草稿中/打磨中/已完成**`
- 锚文本「| 采风中 | 2.1/2.2×N/2.3/2.4 |」：行首单元格 `采风中` → `资料收集中`
- 锚文本「均须在产物状态表落行」：`（规格门/采风门/底盘停靠` → `（规格门/资料门/底盘停靠`
- 锚文本「├── 采风/参考-{书名}.md   # 每书一档」：`采风/参考-{书名}.md` → `资料/参考-{书名}.md`
- 锚文本「**卷纲域输入产物（交接清单）**」：`采风/参考-*.md（档完态）` → `资料/参考-*.md（档完态）`
- 锚文本「| 采风门 | 2.4 | 档案标尺过关+确认 |」：行首单元格 `采风门` → `资料门`
- 锚文本「**B-2 参考档案模板**」：把该行内的路径片段 采风/参考-{书名}.md 改为 资料/参考-{书名}.md（该行只此一处）
- 锚文本「数据源＝采风⑧字段」／「＝采风⑨字段」／「＝采风⑩字段」（3 行各 1 处）：`采风` → `档案`
- 锚文本「| S15 | 采风等量纪律」：`采风等量纪律（主辅不改变采集深度）` → `资料收集等量纪律（主辅不改变采集深度）`
- 锚文本「| S19 | 采风/草稿职责分清」：`采风/草稿职责分清（档案条目化）` → `资料/草稿职责分清（档案条目化）`

**不动声明（同批反证）**：下列「采风」处一律保留——`开始采风`（1.2 交互话术，动作义）、`采风分派与预算`／`变成采风任务`／`采风任务单`（2.1 节名与临时产物名，动作义）、`采风助手`（角色义）、`触发采风`×3（动作义）、`采风需求`×3（工单字段，动作义）、`采风补充`（动作义）、2.3 ③「拍一·标尺统计法」中的裸词`标尺`、纪律 6「标尺类数值」、全书各处`对表／对照／区间`类裸词`标尺`（作者原词，术语表已声明保留）。

### 2. 设计文档：长篇标尺 → 参考标尺（15 行 16 处）[A]

**替换范围（显式限定）**：仅把每行内的字面量 `长篇标尺` 改为 `参考标尺`（含文件名 `长篇标尺.md` → `参考标尺.md`）；行内其余文本（含裸词`标尺`：`标尺十组`／`标尺实测`／`其余 8 组`等）逐字保留。

命中行锚文本（15 行）：`9. **统一情绪词表**`（内含 `长篇标尺②情绪基调组`）｜`### 2.3 长篇标尺实测`｜`④产物：`长篇标尺.md`（独立文件，跨阶段资产`｜`④标尺段（占位态，用长篇标尺更准）`｜`| 跨阶段资产 | 长篇标尺·结构要点组+主角压力组 |`｜`| 跨阶段资产 | 长篇标尺·**结构要点组+节奏目标组** |`｜`| 跨阶段资产 | 长篇标尺**全十组** |`｜`| 跨阶段资产 | 长篇标尺·人物梯队组 |`｜`| 2.3 | 长篇标尺统计法 |`｜`├→ 2.2 参考档案×N ─→ 2.3 长篇标尺.md`｜`├── 长篇标尺.md          # 跨阶段资产`｜`**卷纲域输入产物（交接清单）**`（内含 `长篇标尺.md（定格态）`）｜`④长篇标尺〔占位→实测回填〕`｜B-3 模板标题行（`**B-3 长篇标尺模板**`，本行 2 处：模板名＋其后的落位文件名）｜`| 定格范围 | 基本规格＋长篇标尺＋`。

### 3. 设计文档：四拍第三拍定名（2 处）[A]

**替换范围（显式限定）**：仅下列两行内的 `标尺校验` 四字；「拍三」序号与其余文本保留。

- 纪律 2 四拍定义行：`拍一·方法论问句 → 拍二·参考对照 → 拍三·标尺校验 → 拍四·灵感注入` → `拍一·方法论问句 → 拍二·参考对照 → 拍三·量级参照 → 拍四·灵感注入`
- 3.1 合走拍三行：`**合走拍三·标尺校验**` → `**合走拍三·量级参照**`

### 4. 设计文档：版本三件（版本行 / 状态行 / 变更记录）[A]

**替换范围（显式限定）**：版本行整行替换；状态行**仅改版本号**，括注逐字保留；变更记录**只新增最新一条（v0.9.3 行之后）**，历史行不动。

- 版本行现文：`> 版本：v0.9.3（萃取时序矛盾收口——挂载表尾注原持「先行」主张，与本档「萃取时点裁定」的「后置」互斥，按裁定收口；设计语义其余未变）` → `> 版本：v0.9.4（术语层级收口——大纲阶段第二章的阶段层级名统一为「资料收集」系，动作义「采风」保留；跨阶段资产正名；设计语义未变）`
- 状态行现文（**逐字**，勿用省略号缩写）：`> 状态：**v0.9.3 唯一设计真源（设计完备+模板出件——冻结版，仅精修格式与措辞；追踪档四审项全清，S38/S39 已转萃取批规格）**` → 仅把 `v0.9.3` 改为 `v0.9.4`，括注其余逐字保留
- 变更记录：**插入位置＝最新条（`> v0.9.3 变更（…）` 行）之后、`> 边界：` 行之前**——真源变更记录为**升序**（v0.2 → v0.9.3，实测 L7-L17；L12 明载「版本行重排 v0.2→…→v0.6」），**不得插在最上**。新增行：`> v0.9.4 变更（2026-09-11，术语层级收口）：第二章的阶段层级名（阶段名/门控名/状态值/书目录/阶段产物名）统一收口为「资料收集」系——「采风」一词只留动作与角色义，与 glossary 权威定义分层；跨阶段资产正名为「参考标尺」；四拍第三拍定名「量级参照」。`
- **断言自噬防护（写规格时逐处实测确认，覆盖三文件）**：上述版本行与变更记录**刻意不写旧词字面量**（不写「原『采风步』/原『长篇标尺』」式对照）——否则新增文本会自带被 A1 断言归零的 token，断言当场自吞。新增文本只含新形态与裸词「采风」。三处计数影响已逐一换成 A1–A5 的口径：
  - 设计文档：`参考标尺` 出现 **17 处 / 命中 16 行 ＝ 15 行正名 ＋ 新增变更记录行 1**（差额来自附录 B-3 模板标题行一行两处：模板名＋落位文件名）→ A3 断言取行数 **16**；`量级参照` 出现 **3 处 / 命中 3 行 ＝ 2 处换名（四拍定义＋3.1 合走行）＋ 变更记录行 1** → A3 取 **3**；被 A1 归零的 token 一个都不出现在新增文本里。
  - glossary：`参考标尺` 在施工后为 **2 行**（新增词条行 ＋「采风」条追加句里的指针词面，见 §二.6）→ A5 取 **2**。
  - 追踪档：S45 登记行只含裸词「采风」→ 不触碰 A4 的任何 token。

### 5. 追踪档（5 处替换＋1 行新增）[A]

**替换范围（显式限定）**：仅下列 5 处字面量；S37/S40/S44 行与其余内容逐字保留。

- 锚文本「### 二、采风步」：`### 二、采风步` → `### 二、资料收集步`
- 锚文本「| R2.5 | 采回放哪、命名 |」：把判定依据列的 5.3 目录 采风/参考-{书名}.md 改为 5.3 目录 资料/参考-{书名}.md
- 锚文本「| R2.9 | 参考标尺——治内容匮乏 |」：判定依据列 `2.3 长篇标尺十组` → `2.3 参考标尺十组`（R2.9 需求列**已写作「参考标尺」**，本批使真源与之对齐）
- 锚文本「| S15 | 采风等量 |」：`采风等量` → `资料收集等量`
- 锚文本「| S19 | 采风/草稿职责混淆 |」：`采风/草稿职责混淆` → `资料/草稿职责混淆`
- 追加 S45 登记行（乙节表末，`| S44 |` 行之后）。**逐字文本**（同样做自噬防护——只含裸词「采风」，不含任何被 A1 归零的旧形态 token）：
  `| S45 | **术语层级收口批（作者 2026-09-11 裁定：该步的动作与层级名分层、跨阶段资产正名）**——「采风」的层级义与 glossary 权威定义分层收口（阶段/门控/状态/书目录/阶段产物五类正名）；跨阶段资产与四拍第三拍正名 | 设计文档 v0.9.4＋追踪档 5 处替换＋本登记行＋glossary 2 处；S44 的 A 项**已裁定并落地** | ✅ v0.9.4 |`
  S44 行作为历史记录**不改写**（其「A/B 待裁」为当轮事实）

### 6. glossary.md（2 处）[A]

**替换范围（显式限定）**：仅第 25 行「采风」条释义句尾追加一句；新增「参考标尺」一行；表内其余行不动。

- 「采风」条释义追加：`……区别于扫榜（市场面）与拆书（整本机制）。**本词只作动作与角色义**——大纲阶段的阶段名／门控名／状态值／书目录一律用「资料收集」系（资料收集步／资料门／大纲-资料收集中／`资料/`），标尺类跨阶段资产见「参考标尺」。`
- 「二、写作项目文件类」表（`文风指南` 行之后）新增一行（**纯命名/资产定义**；标尺「参考而非硬指标」的**用法机制**不写进本批，由 044 号规格在该行上追加）：`| 参考标尺 | 从参考作品实测出的十组量级画像（范围值＋依据强度三级），供大纲/卷纲/正文跨阶段对表 | reference scale |`

### 7. CHANGELOG.md `### Changed` 顶部新增一条 [A]

- 形如既有条目：`- 043 号规格产物——术语层级收口与命名对齐（阶段名采风步→资料收集步、门控名采风门→资料门、状态值大纲-采风中→大纲-资料收集中、书目录采风/→资料/、跨阶段资产长篇标尺→参考标尺、四拍第三拍标尺校验→量级参照；动作义「采风」保留）：设计文档 v0.9.4 …`

### 8. 过程产物三件（防验收互斥）[A]

- 本规格归档：`docs/specs/043-2026-09-11-术语层级收口与命名对齐.md` → `docs/specs/archive/043-2026-09-11-术语层级收口与命名对齐.md`
- collab-log 施工记录表新增一行（043 号：规格／施工方／自由度分布／偏差次数／整改轮数／结果）
- CHANGELOG 条目（见 §7）

## 三、验收标准

**断言口径声明（三条，直接决定期望值）**：

1. **计数口径＝命中行数**：下列命令一律用 git grep 的**固定串计数**形态（`-F -c`，Windows cmd 与 Git Bash 同形可跑）——数的是**命中行数**，不是出现次数；`-F` 不可省（默认 BRE 下 `12. **…**` 这类模式尾部量词非法 → exit 128，断言语义失效）。**零命中的断言其退出码为 1 且无输出**——预验器会打印 `exit=1`，那是「通过」，不是失败。§1.3 的 token 计数是**出现次数**，两口径共**三处**不同，期望值已逐条按行数口径核算：`长篇标尺`（16 处 / **15 行**）｜`资料包`（改后 4 处 / **3 行**——2.4 交互行原本已含「看资料包」，原「采风包」改成「资料包」后该行仍只计 1 行）｜`参考标尺`（改后 17 处 / **16 行**——附录 B-3 模板标题行一行两处）。
2. **可预验形态**：命令不含 shell 变量（cmd 不展开 `$D`），路径全用字面量，可被 `python scripts/check_spec_assertions.py` 机械预验。**`check.sh` 一律以叙述式提及、不写成可执行形态**——预验器同时提取**围栏整行**与**行内反引号**两种命令，故凡在 cmd 下必假失败的命令（bash／bash scripts/check.sh／无参数的 git grep）都不能加反引号，见下方 A6 条。
3. **扫描范围**：仅真源三件（设计文档／追踪档／glossary）；不含 `CHANGELOG.md`、`docs/specs/**`（本规格与 044 号正文、归档规格）、`docs/standards/agent-design.md`（其「研究采风分身」为动作义，保留）。

```bash
# A1a 采风系改点行归零——基线实测 22 行（＝§二.1 的 22 行），期望「无输出」
git grep -cE -- "采风步|采风门|采风中|采风/|采风包|采风等量|进入采风|规格先于采风|作者积累\+采风|采风⑧|采风⑨|采风⑩" docs/product/大纲阶段设计文档.md
# A1b 标尺系改点行归零——基线实测 17 行，期望「无输出」
git grep -cE -- "标尺校验|长篇标尺" docs/product/大纲阶段设计文档.md
# A2 应留 token 行数不变（设计文档）——期望依次 3 2 1 3 1 1 1 1 2
git grep -F -c -- "触发采风" docs/product/大纲阶段设计文档.md
git grep -F -c -- "采风任务" docs/product/大纲阶段设计文档.md
git grep -F -c -- "采风助手" docs/product/大纲阶段设计文档.md
git grep -F -c -- "采风需求" docs/product/大纲阶段设计文档.md
git grep -F -c -- "采风补充" docs/product/大纲阶段设计文档.md
git grep -F -c -- "开始采风" docs/product/大纲阶段设计文档.md
git grep -F -c -- "采风分派" docs/product/大纲阶段设计文档.md
git grep -F -c -- "采风预算" docs/product/大纲阶段设计文档.md
git grep -F -c -- "采风档案" docs/product/大纲阶段设计文档.md
# A3 新形态到位（设计文档）——期望依次 5 1 3 4 1 3 3 16
git grep -F -c -- "资料门" docs/product/大纲阶段设计文档.md
git grep -F -c -- "资料收集步" docs/product/大纲阶段设计文档.md
git grep -F -c -- "资料收集中" docs/product/大纲阶段设计文档.md
git grep -F -c -- "资料/参考-" docs/product/大纲阶段设计文档.md
git grep -F -c -- "资料/草稿" docs/product/大纲阶段设计文档.md
git grep -F -c -- "资料包" docs/product/大纲阶段设计文档.md
git grep -F -c -- "量级参照" docs/product/大纲阶段设计文档.md
git grep -F -c -- "参考标尺" docs/product/大纲阶段设计文档.md
# A4 追踪档——期望依次 0 1 0 0，随后三条各 1
git grep -F -c -- "采风步" docs/product/大纲阶段追踪档.md
git grep -F -c -- "采风/" docs/product/大纲阶段追踪档.md
git grep -F -c -- "采风等量" docs/product/大纲阶段追踪档.md
git grep -F -c -- "长篇标尺" docs/product/大纲阶段追踪档.md
git grep -F -c -- "### 二、资料收集步" docs/product/大纲阶段追踪档.md
git grep -F -c -- "资料/草稿职责混淆" docs/product/大纲阶段追踪档.md
git grep -F -c -- "S45" docs/product/大纲阶段追踪档.md
# A5 glossary——`参考标尺` 期望 **2**（新增词条行 ＋ 「采风」条追加句内的指针词面，两处同含该词），`本词只作动作与角色义` 期望 1
# 注：0 命中的断言在 git grep 下退出码为 **1**（无输出）——预验器会打印 exit=1，那是「零命中＝通过」，不是失败
git grep -F -c -- "参考标尺" docs/glossary.md
git grep -F -c -- "本词只作动作与角色义" docs/glossary.md
# A6 规格断言预验器（工具可跑；自指行按设计跳过）
python scripts/check_spec_assertions.py docs/specs/043-2026-09-11-术语层级收口与命名对齐.md
```

- **A6 机检（Git Bash 手跑，期望 exit 0）**：在 Git Bash 下执行 bash scripts/check.sh（此处**刻意不加反引号**——预验器会把行内反引号命令一并提取并在 cmd 下执行，产生三条假失败：bash scripts/check.sh、bash、无参数 git grep；工具 docstring 明载提取范围为「行内反引号与围栏整行命令」）。

- **A4 注**：追踪档 `采风/` 期望 **1**——S44 历史行内「书目录 `采风/`」为沿革引文，豁免；其余 2 处（R2.5 目录行、S19 行）已改。
- **A7 人工核验**：设计文档通读一遍，确认无层级名残留（如「资料门」被写成「资料收集门」、旧名与新名并存），且四拍定义行的 `拍三·量级参照` 与 3.1 合走行一致。
- **A8 语义零改动反证**（手跑核验，不作预验器可执行断言——git diff -U0 的中文输出会撑爆预验器的 GBK 解码，见 §四.8）：设计文档 diff 除上列 token 外无其他字符变化，逐行人工确认；**追踪档 diff 共 6 行 ＝ 5 处替换（`### 二、采风步`／R2.5 目录／R2.9 判定依据列／S15／S19）＋ S45 新增行 1 行**。

## 四、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：真源三件逐条现场核验，无凭记忆——设计文档「采风」全 36 行、「标尺」全量行、「怎么想」全量行、全部标题（H2-H4）均已 read/Select-String 导出；token 计数用双口径（`-AllMatches`＝出现次数、`git grep -c`＝命中行数）实测，两口径差异共三处（`长篇标尺` 16/15、`资料包` 4/3、`参考标尺` 改后 17/16）已在 §三.1 显式登记。
2. **清单完备**：改点按 token 分区——**改 token 计数 24（去重后 23 处字面量，分布 22 行）／留 token 计数 15 ＋ 沿革行裸词 1 处 ＝ 16 处字面量**，23 ＋ 16 ＝ **39**（与文件级实测 39 一致，无第三类，见 §1.3）；改点 22 行与 A1a 交替式断言的基线实测 22 行交叉自证。长篇标尺正名 15 行 16 处；四拍第三拍 2 处；版本三件；追踪档 5 处替换＋1 行新增；glossary 2 处；CHANGELOG／归档／collab-log 三件。
3. **验收可判定**：全部断言为 `git grep` 字面量命令（无 shell 变量、无管道、退出码不经管道取）；A1 用 `-E` 交替式单命令（基线实测 22 / 17）＋ A2/A3/A4/A5 逐 token 精确计数；含零命中目标处已用「行数 vs 出现次数」口径声明＋「exit=1 即通过」双重声明回避自噬（collab-log 模板第 3 项）。
4. **矛盾检查**：本批零设计语义改动（A8 反证）；与 044 号的替换范围互斥声明见 **§五.3**；施工顺序 043 → 044 显式声明为时序依赖。
5. **术语**：四个新名（资料收集步／资料门／大纲-资料收集中／`资料/`）与「参考标尺」的首现处均带判据（§1.2 映射表）；glossary 两处同步（采风条补层级限定、新增参考标尺条）。
6. **节号复核**：本规格引用的外部位置——设计文档第二章／2.4／5.3／5.7／附录 B-2／附录 C、真理源版本行与状态行、追踪档乙节末行——逐个现场核对无误。
7. **涟漪清单**（口径：全仓实测，排除 `.git`／`node_modules`／`.tmp`／`.zcode`；**括注是否含规格自身**）：含「采风」**17 文件**（含本规格与 044；排除后 **15**）、含「长篇标尺」**6 文件**（含本规格；排除后 **5**）、含「标尺校验」**2 文件**（含本规格；排除后 **1**）；`skills/` 实为 **31 文件（23 .md＋8 .js）**、其中 `采风` 与 `标尺` **0 命中**；`scripts/` **0** 相关规则（实测）。沿革豁免面（设计文档变更记录区 v0.2／v0.4／v0.8／v0.9 四行、追踪档 S37／S40／S44 行、CHANGELOG、`docs/specs/archive/*`）已在 §1.4 列全；`docs/standards/agent-design.md` 的「研究采风分身」判为动作义保留（判据：该处指 field research 的执行者）。
8. **断言预验摘要**（工具实跑，2026-09-11）：`python scripts/check_spec_assertions.py` 实跑暴露**五类环境缺陷**，逐条已修正——① 裸 `grep` 在 cmd 下不存在（原断言块会全红）→ 改 `git grep`；② `$D` 变量在 cmd 下不展开 → 改字面量路径；③ 旧稿 `for … do … done` 行不被预验器提取（静默漏验）→ 拆为单命令行；④ `git grep` 默认 BRE，模式含尾部量词等元字符即报 `fatal: Invalid preceding regular expression`（exit 128，断言语义失效）→ 全部改固定串形态 `-F -c`（两条交替式断言保留 `-E`，其 `\+` 转义经实跑验证有效）；⑤ 验收节内**被反引号包住的命令会被预验器提取执行**——`git diff -U0` 的中文全量 diff（26042 字节）撑爆预验器解码 → 该核验改为手跑并以正文叙述（不写成可执行形态）。`check.sh` 手跑结果：**exit 0**（两份新规格入树后全绿）。工具侧缺陷（GBK 解码崩溃）另见 044 号规格开放项 5——**作者 2026-09-11 已裁定：本批落地后补 045 号小规格修工具**（不插队，避免打断 043/044 的施工节奏）。

## 五、开放项与风险

1. **门控名「资料门」——已裁定维持**（作者 2026-09-11 确认）：判据＝门名取阶段名首二字，与「规格门←规格步」「定稿门←定稿步」同构。备选全称「资料收集门」不采纳（比另两个门名长一倍）。
2. **书目录名 `资料/`——已裁定维持**（作者 2026-09-11 确认）：理由是与新门名同源、且与既有用户话术「资料包」一致；备选 `资料库/`、`采集/` 不采纳。目录名是写作项目结构契约（卷纲/正文域将继续引用），此裁定即为该项定案。
3. **本批与 044 号的施工顺序与共享行声明**：044 号规格的锚文本按**本规格施工后的形态**书写（其中 `参考标尺` 系本规格正名结果），故施工顺序固定为 **043 → 044**，连续施工、分别验收。两规格的替换范围在**片段级互斥**（本规格只动命名 token，044 只动机制条文与接口句），但有**两行共享**，已逐处钉死作用区间、串行施工不冲突：
   - **2.3④ 产物行**（`④产物：` 开头那行）：本规格改行内 token（`长篇标尺.md`→`参考标尺.md`、`采风⑧⑨⑩字段`→`档案⑧⑨⑩字段`），044 在「⑩**信息揭示节奏**。」之后**插入**分类说明——044 已声明落点钉在该句之后、**不是行末**（行末句属本规格区间）。
   - **附录 B-3 的 ⑩ 行**：本规格改行内 `采风⑩`→`档案⑩`，044 在**该行之后新增一行**分类说明——行内改与行后插不重叠。
   - 另：**四处**（版本行／状态行／变更记录／追踪档乙节表末行）两规格**先后各改一次**（v0.9.4 → v0.9.5；S45 → S46），044 的现状锚点已按本规格施工后形态书写。**其中版本行与状态行是整行替换**——044 的行内锚用前缀式（`> 版本：v0.9.4（术语层级收口`），不复制全行措辞，故不受本规格措辞影响；**变更记录行同理**——044 的插入锚已写明为「043 施工后的最新条 `> v0.9.4 变更（…）` 行之后、`> 边界：` 行之前」，不按施工前的 v0.9.3 定位。
