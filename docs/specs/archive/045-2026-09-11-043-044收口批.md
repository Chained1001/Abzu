# 规格：045 · 043/044 收口批（机制尾巴 + 账目同步 + 预验器修复 + 命名收口 + 治理立例）

> 本规格自包含。依据：043/044 两批落地后由**产物审查分身**（12 findings：P0:0 P1:2 P2:10）与 **042 批挂账**共同暴露的尾巴；其中预验器修复与「采风助手/采风员」两项由**作者 2026-09-11 裁定「本批落地后补 045」/「留待另批」**。
> 变更实质：**不新增机制、不改 044 已定的十组二分与三态四选主干**——只做四类收口：① 刚落地机制的边界收口（判定对象须在本步已成形／动作义范围补句／阈值写法统一／无脚本期代跑声明）；② 账目同步（追踪档版本指针与章标题）；③ 工具修复（预验器中文 Windows 崩溃）；④ 命名与治理（术语表对照行／反模式 #11／工序注记）。
> 沿革：沿革入 CHANGELOG，本规格只记现状锚点与改动清单。

## 一、现状锚点（2026-09-11 现场核验，含 043/044 落地后工作树）

- 设计文档现 **v0.9.5**（783 行）；版本行 L3 与状态行 L4 均写 v0.9.5 → 本批 bump **v0.9.6**（版本三件同步）。
- **3.1⑤** L236 现文（锚「⑤过关：硬检查（四要素/金手指十字段/欲望双写与咬合句/与规格零矛盾/无表皮重合」）内含：`本步装载的标尺可数组已判定——过／说明／已处置三态之一，见写法纪律 13`。
- **纪律 13 三层分工行** L63 现文（锚「**三层分工**」）：`脚本数事实（从产物机械数出可数组实测值）→ LLM 提处置（对照区间给建议）→ 作者裁决（**改标尺**与**红灯处置**须作者确认）。`
- **glossary「采风」条** L26 现文（锚 `| 采风 | 问题驱动的定点资料调研`）：以「本词只作动作与角色义——大纲阶段的阶段名／门控名／状态值／书目录一律用「资料收集」系……标尺类跨阶段资产见「参考标尺」。」结尾。
- **追踪档 R5.4 行** L65 现文（锚 `| R5.4 | 先出详细设计文档 |`）：判定列写 `v0.9.3 唯一设计真源（设计完备+模板出件，冻结）（🔴已清零）`——**落后两版**（设计文档已 v0.9.5），与本档「每发布一版设计文档，必须同步刷新本档」纪律相抵。
- **追踪档乙节章标题** L71 现文：`## 乙·走查发现（S1-S35）`——表内已至 **S46**，范围标注永久滞后。
- **预验器** `scripts/check_spec_assertions.py` L84-88 现文：`subprocess.run(c, shell=True, capture_output=True, text=True, timeout=120, …)` ＋ `out = (r.stdout + r.stderr).strip().splitlines()`——**中文 Windows 下必崩**（实证：`text=True` 按本地编码 cp936 解码子进程输出，遇非 GBK 字节即 `UnicodeDecodeError`；读取线程死后 `r.stdout` 为 `None`，第 88 行直接 `TypeError`）。043/044 两规格靠「只用 `git grep` 字面量命令 + 只取计数不取匹配行内容」**规避**，非修复。**崩溃的稳定触发器是「命令输出含中文的多行内容」**（如带 `-n` 的 git grep 打印中文匹配行、或 git diff 的全量中文 diff）；与 `core.quotePath`（只管路径转义）无关——`git -c core.quotePath=false grep` 被预验器白名单判为 SKIP、根本不执行（2026-09-11 审查 F9 订正）。
- **纪律 10 用户话术对照表** 现 8 行（L39-46，末行为 `| 灵光位 | 顺手翻到的灵感 |`）；而设计文档 2.1⑥ 交互话术用 **「采风助手」**、glossary 权威词为 **「采风员」**——同指一物、两处两名，且对照表未登记该映射。
- **AGENTS.md 反模式表** 现 10 行（末行 L113 `| 10 | 在源仓库内运行分发/安装类工具…`）——2026-09-11 044 批**整改提示词误发复审分身**一事（即时中断撤回、零损失，已登记 collab-log 施工记录第 31 行）尚无对应条目。
- **阈值两套写法**：纪律 13（锚「**三级判定**」）定义「偏离幅度＝**所在侧边界值**（缺口侧取下限 a、超出侧取上限 b）÷3，向下取整、最小 1」，而 L259（3.2 拍三行）／L295（3.3 ⑥交互行）／L330（3.5 四查·量级行）／L711（附录 B-9 量级行）／L720（B-9 示例行）五处用简写 `1/3`——边界值非 3 的倍数时两读不一致（例：上限 20 → 定义幅度＝⌊20/3⌋＝6，简写读作 ≥6.67）。

**预验基线**（写规格前实跑，铁律④；口径＝`git grep -F -c` 命中行数）：

- `标尺可数组已判定`＝1｜`**三层分工**`＝1（**粗体形**；裸词 `三层分工` 为 3 行——版本行与变更记录行各含一次，故断言一律用粗体形）｜`v0.9.3 唯一设计真源`＝1（追踪档）｜`S1-S35`＝1（追踪档）
- `1/3` 在设计文档命中 **5 行**（L259／L295／L330／L711／L720）＋纪律 13 正例（L69，属示例不属规则，**不动**）
- `采风助手`＝1（设计文档 2.1⑥）｜`采风员`＝0（设计文档）／1（glossary）｜`text=True`＝1、`r.stdout + r.stderr`＝1（预验器）
- 预验器复现：对含行内 `git diff -U0` 的临时规格实跑 → `UnicodeDecodeError: 'gbk' codec can't decode byte 0xa7 …` ＋ `TypeError`（043 批实测在册）

## 二、文件级改动清单

### 1. 设计文档：3.1⑤ 判定对象收口 [B]

**替换范围（显式限定）**：仅 L236 行内那一处片段；行内硬检查其余五项与轻审三问逐字保留。

- `本步装载的标尺可数组已判定——过／说明／已处置三态之一，见写法纪律 13` → `本步装载的标尺组已对照——③ 结构要点的判定随其成形步补：条目量在 3.2、卷数在 3.3，见写法纪律 13`（**刻意不用括号**：该句落在 ⑤过关 的外层硬检查括号内，再加括号即成嵌套——044 归档规格对同一处已有「不写成嵌套括号」的显式约束）

**理由（写进规格备查，不写入真源）**：3.1 装载 ③结构要点组＋④主角压力组，而 ③ 的三字段（主线支线比例／暗线数／换图规划）在 3.1 产物（故事核一句话／金手指十字段／主角轮廓／支柱场面／阻力三层）中尚未成形、④ 属不可数组 → 该步**无可测的 n**，要求三态只会逼出空转记录。

### 2. 设计文档：纪律 13 补两条边界（防复发）[B]

**替换范围（显式限定）**：仅下列两处；纪律 13 其余文本逐字保留。

- 三层分工行（锚「**三层分工**」）句末追加：`**本期无脚本**——可数组实测值由 LLM 代跑，须在产物中显式声明「本值系代跑」（标尺脚本属 skill 建设批次）。`（承接 044 §五.6 的开放项，使其在真源可查）
- **三级判定段末**（锚「**记录用词**」所在段，即「……「须处置」是判定态、「已处置」是落盘态，两者不混用。」之后）追加：`**判定对象须在本步已成形**——本步尚未产出该组字段时（如 3.1 的 ③ 结构要点）只做对照、不出三态，判定随其成形步补（条目量＝3.2／卷数＝3.3）。`（**移段理由**：该条是**判定级通则**，若挂在「十组二分」段末会与「不可数组不做偏离判定」混读——两者理由不同：组性不可数 vs 可数组但字段未成形）

### 3. 设计文档：阈值写法统一（5 处）[A]

**替换范围（显式限定）**：仅下列五处 `1/3` 简写；纪律 13 正例（L69「缺口 5＝下限 15 的 1/3」）为示例算数，**不动**。

- 3.2 拍三行（锚「本题材常规世界复杂度量级（条目数范围），超范围按三级判定处置」）：`偏离＜1/3 说明即可；≥1/3 走处置四选` → `偏离＜偏离幅度（所在侧边界值÷3 向下取整，见写法纪律 13）说明即可；≥偏离幅度走处置四选`
- 3.3 ⑥交互行（锚「参考标尺红灯停靠」）：`可数组偏离≥1/3 且处置未落` → `可数组偏离≥偏离幅度（所在侧边界值÷3，见写法纪律 13）且处置未落`
- 3.5 四查·量级行（锚 `- **量级**：标尺全组对照`）：`≥1/3 偏离须带处置记录` → `≥偏离幅度（所在侧边界值÷3，见写法纪律 13）须带处置记录`
- 附录 B-9 量级行（锚为行首单元格「量级」＋紧接的 `标尺全组对照`）：同行同片段做上述同款替换（该格尾部**无** `；`，只换格内本体）
- 附录 B-9 示例行（锚 `- 示例行：`量级｜节点 24 个`）：`缺口 5≥1/3` → `缺口 5≥偏离幅度 5（下限 15÷3）`

### 4. 设计文档：纪律 10 对照表补一行 [A]

**替换范围（显式限定）**：仅在第 10 条对照表末行（`| 灵光位 | 顺手翻到的灵感 |`）之后新增一行；表内其余行与第 10 条正文不动。

- 新增：`| 采风员 | 采风助手 |`（判据：glossary「采风员」是**逻辑角色名**（内部术语），设计文档 2.1⑥ 交互话术里的「采风助手」是**运行时对作者说的话**——同指一物，按纪律 10「内部术语只活在设计文档与文件层」登记映射，**不改任何一处词面**；补记：043 批按「角色义」保留该词面，本批只补记其**运行时话术位**，两批口径不冲突）

### 5. 设计文档：版本三件 [A]

- 版本行 L3 整行替换：`> 版本：v0.9.6（043/044 收口——3.1⑤ 判定对象收口＋纪律 13 补判定对象成形律与无脚本代跑声明；阈值写法统一为「偏离幅度」；纪律 10 补术语映射；设计语义其余未变）`
- 状态行 L4：仅把 `v0.9.5` 改为 `v0.9.6`，括注逐字保留（现括注＝「设计完备+模板出件——冻结版，仅精修格式与措辞；追踪档四审项全清，S38/S39 已转萃取批规格」）
- 变更记录：**插在 `> v0.9.5 变更（…）` 行之后、`> 边界：` 行之前**（真源变更记录为升序），新增行（**刻意不复述任何被 A1 归零／被 A2 计数的 token**——写进真源的新文本自带这些词面会让断言当场自吞，2026-09-11 审查 F1 立例）：`> v0.9.6 变更（2026-09-11，043/044 收口）：3.1⑤ 旧过关句改判为「对照」并挂靠纪律 13（判定随其成形步补）；纪律 13 增两条边界（判定随成形步补／无脚本期代跑须声明）；四处接口句与 B-9 示例的阈值简写统一为「偏离幅度」表述；纪律 10 对照表补一行术语映射。`

### 6. glossary.md：「采风」条补范围句 [A]

**替换范围（显式限定）**：仅 L26 行内句尾追加；该行前段逐字保留。

- 追加：`**范围提醒**：大纲阶段该动作实际指**对参考清单内各作品做全字段等量深采**（见设计文档第二章），不止于本条的卡点定点检索——两者同属 field research，只是层级不同。`

### 7. 追踪档：版本指针与章标题 [A]

- R5.4 行（锚 `| R5.4 | 先出详细设计文档 |`）判定列：`v0.9.3 唯一设计真源` → `v0.9.6 唯一设计真源`（**须写本批产出后的版本**，即设计文档版本三件落地后的值）
- 章标题（锚 `## 乙·走查发现（S1-S35）`）：`## 乙·走查发现（S1 起，逐条追加）`（去掉会永久滞后的范围数字）
- 乙节表末追加 S47 登记行：`| S47 | **043/044 收口批（产物审查 12 findings〔P0:0 P1:2 P2:10〕＋042 挂账驱动）**——3.1⑤ 判定对象无可测 n／glossary「采风」动作义两范围／阈值两套写法／追踪档版本指针与章标题滞后／预验器中文 Windows 崩溃／采风助手-采风员映射缺登记 | 规格 045：设计文档 v0.9.6（3.1⑤ 收口＋纪律 13 两条边界＋阈值统一 5 处＋纪律 10 映射行）＋glossary 范围句＋追踪档 2 处＋预验器修复＋AGENTS 反模式 11＋collab-log 工序注记 | ✅ v0.9.6 |`

### 8. scripts/check_spec_assertions.py：中文 Windows 崩溃修复 [B]

**替换范围（显式限定）**：仅下列**三处代码 + 一处 docstring 行**（三处代码＝解码侧调用、解码侧兜底、编码侧 stdout 重配置）；脚本其余逻辑（提取规则／白名单／退出码恒 0）不动。

- L84-87 调用：`text=True, timeout=120,` → `encoding='utf-8', errors='replace', timeout=120,`（`encoding` 即隐含文本模式；`errors` 须与 `encoding` 同用才生效。实测 `encoding`＋`text=True` 并存**并不报错**，此处替换只为显式化，勿误记为「冲突」）
- L88：`out = (r.stdout + r.stderr).strip().splitlines()` → `out = ((r.stdout or '') + (r.stderr or '')).strip().splitlines()`
- **新增一处代码（编码侧收口）**——加在 `if __name__ == '__main__':` 块内、`report(sys.argv[1])` 之前：`sys.stdout.reconfigure(encoding='utf-8')`，行内注释写「中文 Windows 下 stdout 默认 GBK，打印含 ⏎／中文的输出会 UnicodeEncodeError（2026-09-11 实测）」。（**不加在模块顶层**：该脚本会被外部 import 做探针，模块级重配置会改掉调用方的 stdout。）**只修解码侧不够**：修好后子进程输出能解码了，但 `print()` 里 `' ⏎ '.join(out[:3])` 在 GBK stdout 下、且输出 ≥2 行时必崩（实测 `UnicodeEncodeError: 'gbk' codec can't encode character '\u23ce'`）。
- docstring「依赖与前置」句末追加：`子进程输出统一按 UTF-8 解码，并对不可解码字节容错——中文 Windows 下本地编码为 GBK，按本地编码解码会崩（2026-09-11 实测）。`（**刻意不写 `errors=` 字面量**：该词面是本批断言 A3 的计数对象，写进 docstring 会把期望 1 顶成 2）

### 9. AGENTS.md 反模式表新增第 11 条 [B]

**替换范围（显式限定）**：仅在第 10 条所在行之后新增一行；§5 表头、其余各行与本节其余文本不动。

- 新增：`| 11 | 把整改/施工提示词发给错误对象（未核对目标 id 的角色，把施工指令发给审查分身） | 发送前核对目标 id 的角色与可续性（细则单点权威＝collab-log「施工提示词」节的「分身可续性」注记，此处只留判据） | 2026-09-11 044 批整改提示词误发复审分身（即时 interrupt ＋撤回，该分身回报未改动、零损失；登记于 collab-log 施工记录第 31 行） |`

### 10. collab-log.md：工序注记 + 施工记录第 32 行 [A]

- 「施工提示词（分身为默认形态；外部窗口为备选）」节的**围栏之后、`### 核验请求` 之前**追加一句（**不得追加进围栏内**——围栏是该模板正文，写进去会被此后每份提示词原样复制）：`**分身可续性**：规划方 spawn 施工分身时若预期后续整改，应以**后台** spawn（前台 spawn 的分身结束后不可续，整改只能重新 spawn）；**审查分身一律只读**，不得承接整改或施工。`
- 施工记录表末追加第 32 行：`| 32 | 2026-09-11 | 045-2026-09-11-043-044收口批.md | 施工分身（规划方 spawn；收口批——**11 项改动跨 7 个内容文件**〔设计文档／glossary／追踪档／预验器／AGENTS／collab-log／CHANGELOG〕） | **[A]×7 [B]×4** | （施工时回填） | （回填） | （回填） |`（结果列由规划方核验后回填）

### 11. CHANGELOG.md 与过程产物 [A]

- CHANGELOG `### Changed` 顶部新增 045 条目（形如既有条目，含改动面与核验结论）
- 本规格归档至 `docs/specs/archive/`（**规划方职务**——先例：043/044 施工方均以 P1 申报「归档/登记归规划方」，核验采纳；本规格不再派给施工方）

## 三、验收标准

**断言口径声明**：命令一律用 git grep 的固定串计数形态（-F -c，数命中行）；`-F` 不可省（默认 BRE 下含 `*`／`+` 的模式会 exit 128）；**零命中时退出码为 1 且无输出＝通过**。命令不含 shell 变量、不含 `for` 循环、路径全字面量；`check.sh` 与预验器回归测试以叙述式提及（**行内反引号命令同样会被预验器提取执行**，故本规格正文刻意不给它们加反引号——含中文输出的 git diff 一类命令会撑爆修复前的预验器解码）。扫描范围为设计文档／追踪档／glossary／AGENTS（不含 `docs/specs/**`、`CHANGELOG.md`）。

```bash
# A1 应改旧形态归零（每条期望「无输出」）
git grep -F -c -- "标尺可数组已判定" docs/product/大纲阶段设计文档.md
git grep -F -c -- "偏离＜1/3 说明即可" docs/product/大纲阶段设计文档.md
git grep -F -c -- "可数组偏离≥1/3" docs/product/大纲阶段设计文档.md
git grep -F -c -- "≥1/3 偏离须带处置记录" docs/product/大纲阶段设计文档.md
git grep -F -c -- "缺口 5≥1/3" docs/product/大纲阶段设计文档.md
git grep -F -c -- "v0.9.3 唯一设计真源" docs/product/大纲阶段追踪档.md
git grep -F -c -- "S1-S35" docs/product/大纲阶段追踪档.md
git grep -F -c -- "text=True, timeout=120" scripts/check_spec_assertions.py
git grep -F -c -- "r.stdout + r.stderr" scripts/check_spec_assertions.py
# A2 新形态到位（期望见行末括注）
git grep -F -c -- "本步装载的标尺组已对照" docs/product/大纲阶段设计文档.md
git grep -F -c -- "判定对象须在本步已成形" docs/product/大纲阶段设计文档.md
git grep -F -c -- "本期无脚本" docs/product/大纲阶段设计文档.md
git grep -F -c -- "范围提醒" docs/glossary.md
git grep -F -c -- "| 采风员 | 采风助手 |" docs/product/大纲阶段设计文档.md
git grep -F -c -- "偏离幅度（所在侧边界值÷3" docs/product/大纲阶段设计文档.md
git grep -F -c -- "缺口 5≥偏离幅度 5" docs/product/大纲阶段设计文档.md
git grep -F -c -- "| 11 | 把整改/施工提示词发给错误对象" AGENTS.md
git grep -F -c -- "分身可续性" docs/specs/collab-log.md
git grep -F -c -- "| 32 |" docs/specs/collab-log.md
git grep -F -c -- "## 乙·走查发现（S1 起，逐条追加）" docs/product/大纲阶段追踪档.md
git grep -F -c -- "v0.9.6 唯一设计真源" docs/product/大纲阶段追踪档.md
git grep -F -c -- "| S47 |" docs/product/大纲阶段追踪档.md
git grep -F -c -- "> 版本：v0.9.6" docs/product/大纲阶段设计文档.md
# A3 预验器修复到位（期望各 1）
git grep -F -c -- "errors='replace'" scripts/check_spec_assertions.py
git grep -F -c -- "sys.stdout.reconfigure" scripts/check_spec_assertions.py
```

- **A2 期望值（逐条核算，命中行口径）**：`本步装载的标尺组已对照`＝1｜`判定对象须在本步已成形`＝1｜`本期无脚本`＝1｜`范围提醒`＝1（glossary）｜`| 采风员 | 采风助手 |`＝1｜**`偏离幅度（所在侧边界值÷3`＝4**（3.2 拍三行／3.3 ⑥交互行／3.5 四查行／附录 B-9 量级行——四处；B-9 示例行用的是「偏离幅度 5」故不计入）｜`缺口 5≥偏离幅度 5`＝1｜`| 11 | 把整改/施工提示词发给错误对象`＝1（AGENTS）｜`分身可续性`＝1｜`| 32 |`＝1｜`## 乙·走查发现（S1 起，逐条追加）`＝1｜`v0.9.6 唯一设计真源`＝1（追踪档；设计文档状态行同词面但不在扫描文件内）｜`| S47 |`＝1｜`> 版本：v0.9.6`＝1。
- **A4 预验器回归测试（手工，规格给出可复现步骤）**：在 `.tmp/045/` 造一份最小规格 fixture，其「验收标准」节内含一行**行内反引号命令**：git grep 加 -F -n 参数、模式为「标尺」、目标为设计文档——带 `-n` 会打印整行中文匹配内容，**这是不依赖工作树状态的稳定触发器**（2026-09-11 审查 F4 订正：原稿用 git diff 加 -U0，在干净工作树下无输出、不崩，只有工作树含中文改动时才触发，前提未写明；且修复前若先做第 8 项会自证失效）；修复前实跑 `python scripts/check_spec_assertions.py .tmp/045/fixture.md` → `UnicodeDecodeError: 'gbk' codec …` ＋ `TypeError`；修复后应 **exit=0 且输出 `共 2 条：执行 2｜跳过 0`**，无任何 Traceback。**夹具须含两条行内命令**：① git grep 加 -F -n、模式「标尺」（**长行**中文，触发解码侧崩溃）；② git log 加 --oneline -2（**短行**中文，两行以上才会触发 `' ⏎ '.join(...)` 的编码侧崩溃——实测仅修解码侧时 ① 已不崩、② 仍崩，故两条缺一不可）。跑完删除 `.tmp/045/`（红线 2）。
- **A5 阈值写法一致性反证**：设计文档内**带比较符的 `≥1/3` 应全部消失**（跑语句 git grep -F -c -- "≥1/3" docs/product/大纲阶段设计文档.md，期望「无输出」），而纪律 13 正例的算数式**保留**（跑语句 git grep -F -c -- "缺口 5＝下限 15 的 1/3" docs/product/大纲阶段设计文档.md，期望 **1**）。**注意**：不可用裸 `1/3` 作断言 token——`3.1/3.2`／`3.1/3.3/3.4` 一类串会污染计数（实测该 token 命中 14 行＝**模块号写法 8 行**〔如 `3.1/3.2`〕＋**本批要改的阈值简写 5 行**〔L259/L295/L330/L711/L720〕＋**纪律 13 正例算数 1 行**——三类混杂故不可作断言 token）。
- **A6 版本三件一致**：版本行／状态行／追踪档 R5.4 三处版本号同为 **v0.9.6**；变更记录新行位于 v0.9.5 行之后、`> 边界：` 行之前（升序）。
- **A7 机检（Git Bash 手跑，期望 exit 0）**：在 Git Bash 下执行 bash scripts/check.sh；另跑预验器：python scripts/check_spec_assertions.py docs/specs/045-2026-09-11-043-044收口批.md（期望无异常）。
- **A8 语义零外溢**（手跑核验，不作预验器可执行断言——git diff -U0 的中文输出在修复前会撑爆预验器解码）：除本规格列出的片段外，各文件 diff 无其他字符变化，逐行人工确认；`采风助手`（设计文档）与 `采风员`（glossary）**词面均不得改动**（本批只登记映射）。

## 四、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：本规格 11 条改动的落位逐条现场核验——3.1⑤（L236）、纪律 13 三层分工（L63）与十组二分段、阈值 5 处（L259／L295／L330／L711／L720，另 L69 正例判为不动）、纪律 10 表末行（L46）、版本行（L3）／状态行（L4）／变更记录末条、glossary 采风条（L26）、追踪档 R5.4（L65）与乙节标题（L71）、预验器（L84-88）、AGENTS 反模式末行（L113）、collab-log 施工提示词节。
2. **清单完备**：四类收口逐项落位——① 机制边界 4 项（3.1⑤／纪律 13 两条边界／阈值统一／glossary 范围句）；② 账目同步 2 项（追踪档版本指针／章标题）；③ 工具修复 1 项（预验器中文 Windows 崩溃，含回归测试）；④ 命名与治理 3 项（纪律 10 映射行／AGENTS 反模式 11／collab-log 工序注记）＋过程产物（版本三件／S47／CHANGELOG／归档／施工记录第 32 行）。
3. **验收可判定**：全部断言为 `git grep -F -c` 单行字面量命令（无变量、无循环、无管道取码）；A1 归零组 **9** 条、A2 新形态组 **14** 条、A3 修复到位 **2** 条（合 **25** 条＝预验器实测「共 26 条：执行 25｜跳过 1」中的执行数，跳过项为 A4 的行内自指命令）；A5 为反证断言（2 条）、A6 为位置核验；A4 为可复现的手工回归测试（fixture 步骤与期望输出均写明）；「零命中＝exit 1」已声明。
4. **矛盾检查**：本批**不改** 044 已定的十组二分、三层分工、三态四选主干（只补边界与写法统一），故 044 的 32 条断言除 A1（3.1⑤ 片段被本批再次改写）外仍应全绿——**施工后须复跑 044 断言并逐条说明差异**——**实测预期 4 条变化**（非 1 条）：`可数组` 12→11（3.1⑤ 改写）、`三层分工` 3→2、`三级判定` 4→3、`处置四选` 4→3（后三条成因＝本批 §二.5 版本行整行替换，旧 v0.9.5 版本行列举过这三个词面、新 v0.9.6 版本行未列举）；其余 28 条应不变，任何额外差异须现场判因；与 043 无交集（命名 token 不动）。时序依赖：045 在设计文档 v0.9.5 之上施工，版本 bump 至 v0.9.6。
5. **术语**：新增登记「采风员→采风助手」映射（不改词面）；「偏离幅度」为纪律 13 既有术语，本批只是把简写统一到它。
6. **节号复核**：本规格引用的位置——设计文档 3.1⑤／3.2 拍三行／3.3 ⑥交互／3.5 四查／纪律 10／纪律 13／附录 B-9／版本三件、追踪档 R5.4 与乙节、AGENTS §5、collab-log 施工提示词节——逐个现场核对无误。
7. **涟漪清单**：全仓 grep `1/3`／`标尺可数组已判定`／`v0.9.3 唯一设计真源`／`S1-S35`／`text=True`／`采风助手`／`采风员`；`skills/` 与 `scripts/` 仅预验器一处命中（本批目标）；沿革面（CHANGELOG、archive/*）只增不改。
8. **断言预验摘要**（实跑，2026-09-11）：初稿预验时工具**确曾崩溃**——根因是本规格 A5 注释里一处**带省略号的行内反引号命令**被提取执行、其报错回显中文模式（`UnicodeDecodeError: 'gbk' codec … byte 0xa6`），正是本批要修的缺陷在规格侧的同一表现；去掉行内反引号后复跑 → **共 26 条：执行 25｜跳过 1，零异常零噪声**（跳过项为规格自指的预验器命令，按设计）。脚本类断言（A1 的 `text=True, timeout=120`／`r.stdout + r.stderr` 归零、A3 的 `errors='replace'` 到位）在**施工前**自然显示为命中，属预期基线而非失败。`check.sh` 手跑 exit 0。

## 五、开放项与风险

1. **glossary「采风」条采「补范围句」而非「改动作名」**：另一种解法是把 2.1 的 `采风任务／采风任务单／采风助手` 也收口为「资料收集」系，但那会推翻作者 2026-09-11「采风应该是这个步骤的动作」的裁定，故本批只补范围句、**词面一律不动**；若作者偏好前者，改动面为设计文档 2.1 的三处词面＋追踪档 R2.1/R2.3 沿革不改。
2. **纪律 13 新增的「判定对象须在本步已成形」是**通则**：它同时解释了 3.2⑤（条目量＝③ 的区间，3.2 已成形）为何可判、3.4⑤（只装 ⑤）为何可判；若日后出现新的「已装载但未成形」组合，按此**通则**处理，无须再改条文。
3. **预验器修复的边界**：本批只修**解码与兜底**，不改提取规则与白名单（那属另一议题——042 批挂账的「文件白名单与多批共存」）。修复后，规格的验收节可以重新容纳中文输出命令（如 `git diff`），但这会让预验耗时上升，是否鼓励留待实践。
4. **追踪档 R5.4 的版本指针是易腐项**：本批改为 v0.9.6 后，下一版设计文档 bump 时仍须同步（本档既有纪律已明写）。若希望根治，可在后续批次把该单元格改为「随设计文档版本行（见设计文档 L3）」的**不写版本号**形态——本批不动，留作候选。
5. **反模式 #11 的落位**：本批把「提示词误发」立为反模式（宪法 §5 表），与「事故回填」体例一致；若作者认为这更像**工序注记**而非反模式，可只保留 collab-log 工序注记、撤回 AGENTS 那一条。

6. **`scripts/__pycache__/` 是预验器的副作用产物**：任何以 import 方式加载该脚本的进程都会在 `scripts/` 下生成它，而当前 `.gitignore` 未覆盖 → 它会出现在核验三步第一步「改动面核对」的 untracked 比对里（2026-09-11 审查期间实测复现）。本批**不改 `.gitignore`**（属仓库配置，非本批范围），处理口径＝核验时声明为副作用产物并删除；是否补 ignore 留作候选另批。
