# 规格：047 · 挂账台账与体积纪律

> 本规格自包含。施工前先读仓库根 AGENTS.md。施工方只改本规格「文件级改动清单」列出的文件。
> **背景（2026-09-12 三路治理勘察）**：全部挂账散落四处、无单一来源，导致漏做。实证：036 批审计 F11 发现 AGENTS.md 头部域状态句过时，仅记于 README Roadmap 一行 checkbox，此后经 037–045 共 9 批无一批处理，直至 2026-09-12 才由宪法修订批修复。同类漏做风险对全部存量挂账成立。
> 沿革入 CHANGELOG，本规格只记现状锚点与改动清单。

## 一、现状锚点（2026-09-12 现场核验：grep/read 实做，计数为实测值）

- **挂账四处散落，无单一来源**：① `README.md` Roadmap 的 `- [ ]` 未勾选项（实测 11 项）；② `docs/specs/collab-log.md` 施工记录表单元格（含「挂账」措辞者 5 行，如行 29「挂账另批」）；③ 归档规格「开放项」节（030 起，标题形态如 `## 四、开放项`）；④ `CHANGELOG.md` 正文（含「挂账」者 19 行）。附录 A 为三路勘察合并后的现行清单。
- **批 0 已就地清偿 4 条**（2026-09-12，本规格前置）：collab-log 重复 `#31` 行删除、046 规格作废归档、施工记录表 12 行失效提交状态改记事实、check.sh 红基线复绿。此 4 条在附录 A「已销」组内单列编号 `b0-1`–`b0-4`。
- **030 序号复用**：`docs/specs/archive/` 内 5 个文件共用序号 030（规格本体＝`030-…-写作链产品与技术文档撰写批.md`，附件四件：`030-…-mo-shu架构评估报告.md`、`030-…-写作链设计文档-v0.4.md`、`030-…-思维链调研稿-V3.md`、`030-…-问答迭代记录.md`）。`file-conventions.md` 命名总表规格行（现文含「序号按立项顺序三位递增、永不复用」）未规定同批附件如何编号。
- **体积实况**（2026-09-12 工作树实测，Python `len()` 读 UTF-8 全文）：`CHANGELOG.md` **29,464 字符** / 119 行 / 条目 105 条 / 最长单条 **1,412 字符**（同口径 HEAD 版为 27,874 字符）；`docs/specs/collab-log.md` **21,915 字符** / 251 行。二者均 > 2 万。
- **解冻条件已达成而未处置**：`AGENTS.md` §6 doc-budget 条现文为「……有条件缓建；先用 §7 体积条款约束。**解冻触发条件**：任一热路径文件连续两批触碰行数上限，或单文件实测字符数 > 2 万（〔abzu 自定〕初始阈值，调整须记录 CHANGELOG）。届时以字符实测立法（字符比行数贴近 token 成本），并含超限处理序与预算沿革登记」。按上条实测，触发条件成立，但条目仍写「缓建」，且无处置记录。
- **协议侧无挂账登记环节**：`docs/specs/collab-log.md` 核验协议三步（第一步改动面核对／第二步验收命令独立重跑／第三步内容双轨抽查）均无「挂账登记核对」动作，故核验通过不保证当批挂账已入账。
- **CHANGELOG 存量超限**：按 §二.4 拟立的 400 字符/条口径实测，现存 105 条中 **17 条超限**（最长 1,412）。

## 二、文件级改动清单

### 1. 新建 `docs/specs/open-items.md`：挂账台账（单点来源）[B]

**内容要求**（表体按附录 A 的清单逐条落位，一行一条，不合并）：

- 头部两行式头注（定位／纪律，裸键，**不设版本行**——活账本逐次追加，与 collab-log 同式）：定位＝「全部未办事项的单点来源」；纪律＝「每批核验收尾核对；销账须记销账依据（批号或 commit）；本文件只记**未办事项与触发条件**，不记过程叙事——AGENTS §6 禁者为过程叙事台账（施工日志／审核记录），本件性质为欠账清单」。
- **旧源收口**：纪律行补一句（**句首保留标签「旧源收口」**，供 collab-log 侧按标签指称）——「**旧源收口**：自本批起不再新增第二来源——README Roadmap／collab-log 记录行／规格开放项新产生的挂账一律登记入本文件；存量是否回填另行评估。」
- **已销组口径**：该组的表头**同用四列**（编号｜事项｜触发条件｜状态），触发条件列填「—（已办结）」，状态列填「已销（依据）」。`b0-3` 与 `C7` 为同一事项的两半（`b0-3`＝行 25–31 已改记事实、`C7`＝行 32〔045〕待提交），**两行并存且互相引指，不合并**。
- 表四列：**编号 ｜ 事项 ｜ 触发条件（何时该做） ｜ 状态**。
- 分组小标题七个，与附录 A 的七个 `###` 分组**逐字同名**：`等裁决` / `等域建设` / `等触发条件` / `等另批` / `等在途收尾` / `纯遗漏` / `已销`（「纯遗漏」为事项性质分组——到期未办或文本未清，不属「等什么」；「已销」收全部销账行）。
- 状态取值三态：**待办** / **已销（依据）** / **部分已销（依据＋余项指向）**（第三态仅 C7 一例——该事项一半已销、一半转 S2）。
- 附录 A 中标注「→ 批 N」者，事项文本内保留该指向（供后续批次认领）。

### 2. `docs/standards/file-conventions.md`：命名总表补两条 + §五.2 补变体行 [A]

**替换范围（显式限定）**：仅下列三处，其余文字逐字保留。命名总表内**只动规格行一行**（该行现文含「序号按立项顺序三位递增、永不复用」），**不得触碰 :14 行**（`references 域子目录` 行，A9 以其为哨兵）。

- **命名规则列追加**（在该列现文末尾「（作者面向的治理记录）」之后）：「；**同批附件**（随主规格产出的并列文档）沿用主规格序号、以语义后缀区分（如 `030-…-mo-shu架构评估报告.md`），不另编序号」（正例即 030 批五件；此为追认实况）。
- **位置列改写**（现文为「`docs/specs/`；**实施验收全部通过后移动至** `docs/specs/archive/`（移动不留副本）」——在其后追加、**不新建重复句**）：「；**作废批**（规格前提失效未施工）同移入 archive，文件名追加 `-已作废` 后缀并在正文头注标废」（正例：`archive/046-2026-09-11-机制闸先行批-已作废.md`）。
- **§五.2 头注三行式节追加**（在该节末尾、`## 六、本规范的维护` 之前）：「**变体豁免**：活账本类文件（`collab-log.md`、`open-items.md`）用两行式头注（定位／纪律，裸键），**不设版本行**——逐次追加的账本无版本语义，与台账类豁免同源。」
- **命名总表增行 + 内容规范矩阵实例扩充**（前两处只改「规格」行，未登记新件自身，本处补齐）：命名总表末尾（`协作日志` 行之后）新增一行——「挂账台账 ｜ `open-items.md`（固定名，活账本） ｜ — ｜ `docs/specs/`」；并将 §五 内容规范矩阵「台账类」行现文的实例（仅 `collab-log.md`）扩为 `collab-log.md`、`open-items.md`。

### 3. `docs/specs/collab-log.md`：核验协议补「挂账登记核对」[A]

**替换范围（显式限定）**：仅在「### 结论三态」节**之前**插入一节（该节现位于「### findings 处置与分歧升级阈值」之上）；插入后新节紧邻「### 结论三态」，**位置与自述一致**（本动作为结论前必做，非三步之一）。三步既有正文、七分矩阵、结论三态与其余各节逐字保留。

新增节（含前置空行）：

```markdown
### 挂账登记核对（核验结论前必做）

- 当批产生的挂账（搁置项、开放项、候选）须逐条登记入 `docs/specs/open-items.md`——**未登记即核验未完成**
- 销账须记销账依据（批号或 commit）；存量挂账是否回填由 open-items.md「旧源收口」句辖
- 反例：036 批审计 F11 发现 AGENTS.md 域状态句过时，仅记于 README Roadmap 一行 checkbox 而未入台账，此后 9 批未处理——该反例即本条立例依据
```

### 4. `AGENTS.md` §7：体积纪律补 CHANGELOG 条目限长 [A]

**替换范围（显式限定）**：仅在 §7「引用一层深」条**之前**插入一条；§7 既有三条逐字保留。

新增条目：

```markdown
- **CHANGELOG 条目 ≤ 400 字符**：一条只写「改了什么 + 核验结论」，细节留在当批规格与 collab-log（二者才是台账）。自 2026-09-12 起生效，**存量超限条目不回溯改写**（回填与否另行评估）。
```

（正例／反例：本条自身在 CHANGELOG 的 047 条目即为正例；反例为 044 号条——1,412 字符。）

### 5. `AGENTS.md` §6：doc-budget 条改记解冻处置 [A]

**替换范围（显式限定）**：仅 doc-budget 条的「解冻触发条件」整句；条目其余文字逐字保留。

- 现文（**整句替换**，与 `AGENTS.md` 实文逐字一致）：`**解冻触发条件**：任一热路径文件连续两批触碰行数上限，或单文件实测字符数 > 2 万（〔abzu 自定〕初始阈值，调整须记录 CHANGELOG）。届时以字符实测立法（字符比行数贴近 token 成本），并含超限处理序与预算沿革登记`
- 替换为：`**解冻触发条件**：任一热路径文件连续两批触碰行数上限，或单文件实测字符数 > 2 万（〔abzu 自定〕初始阈值）。**处置裁定（2026-09-12）**：触发条件已由两个活账本文件达成（实测值见当批规格 047），经作者裁定**不建登记表与守卫**，改以 §7 的「CHANGELOG 条目限长」条处置——体积失控的实测成因是单条膨胀而非整体规模，故以单条纪律直击成因，不为此新增台账加守卫两级机制`
- **措辞纪律**：本条**刻意不复述 §7 新条的 token 字面量**（复述会使 §五 的 A6 期望由 1 顶成 2，属断言自噬）；凡指称该条，一律用「§7 的「CHANGELOG 条目限长」条」。
- **措辞纪律二**：本条**不写具体字符数**——字符数随每次追加漂移，写入宪法即成为必然过期的断言（违反模式 #1「文档数字与实测对齐」）；实测值留在当批规格（047 §一）与 CHANGELOG 条目内。
- **理由（写进本规格备查，不写入宪法）**：解冻条款的原意是防体积失控；实测失控源是 CHANGELOG 条目膨胀（最长 1,412 字符、17 条超限）而非整体规模，故以单条纪律直击成因，避免为一个可机械约束的问题新增台账加守卫两级机制（§4 决策树「能否不新增」优先）。

### 6. 过程产物（规划方职务，非施工方项）

- `CHANGELOG.md` `### Changed` 顶部新增 047 条目（**≤ 400 字符**，按本批新立的 §7 条执行）。
- `docs/specs/collab-log.md` 施工记录表末行追加一行（序号按施工时点末行序号顺延）。
- 本规格归档至 `docs/specs/archive/`。

## 三、附录 A · 挂账现行清单（施工方据此填 open-items.md 表体）

> 编号沿用勘察编号（R/C/S/A/G 前缀）；`b0-*` 为批 0 清偿项。七个 `###` 分组与 open-items.md 的分组逐字同名。标「→ 批 N」者为已知归属。

### 等裁决

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| S1 | 043/044 审查记录欠账是否回补 | 待另行评估（历史数据修复，非流程优化） | 待办 |
| S5 | 退出码是否承载预验器崩溃信号 | 作者裁定（须同批改三处载体） | 待办 |
| A6 | glossary 英文对应列语义弱化（model-invoked / user-invoked） | 后续批评议 | 待办 |
| A10 | 038 第四章【待作者校准】标记 | 作者校准 | 待办 |
| A13 | 044 四项规划方判读（阈值系数 1/3、免跑档含 3.5、配档依据披露、1.1 规格收集档） | 作者确认或改档 | 待办 |
| R6 | marketplace.json 元数据内容标准立法 | 发布前置 | 待办 |

### 等域建设

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| R1 | 卷纲／正文／文风三域建设 | 各域立项 | 待办 |
| R2 | test-prompts 六场景全绿 | 各域建设后 | 待办 |
| R10 | 书目录共享语言文件立法（术语/设定/不变量三段式） | write 域真消费方检验 | 待办 |
| R11 | 正文域会话交接契约立法 | write 域建设时 | 待办 |
| R4 | skill 调用契约立法（输入校验/输出格式/失败返回） | 拆书域已裁定搁置，无期 | 待办 |
| G3 | outline 域 project-structure 规范 | outline 域建设（命名权威现旁落至设计文档 §5.3） | 待办 |
| G10 | test-prompts 场景 1-5 待启用契约 | 对应域建设后生效 | 待办 |
| A8 | 书项目脚手架生产能力重建 | 大纲域新设计落地 | 待办 |

### 等触发条件

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| R3 | v1.x 写作项目内 AGENTS.md 生成、示例项目 | v1.x 阶段 | 待办 |
| R5 | 系统性依赖声明清单（Node/pip/全局 CLI） | 任一域引入外部依赖时 | 待办 |
| R8 | junction 免手动部署；域级命令分发方案 | 多域上线后裁定 | 待办 |
| R12 | 发布前置四项（演示示例／最小 CI／插件市场提交／同类项目对比） | 开源发布时 | 待办 |
| G1 | 命令薄壳启用（`.claude/commands/abzu-{域}.md`） | 多域上线、分发方案裁定后 | 待办 |
| G2 | agent 文件启用（`.claude/agents/abzu-{角色名}.md`） | T1 触发 | 待办 |
| G6 | agent-design 骨架立法填充（`[T1-TODO]` 实测 2 处：:14 表单元格、:55 节标题） | T1 触发 | 待办 |
| G7 | 安装预检（依赖版本前置报错） | 脚本依赖复杂化 | 待办 |
| G8 | architecture §七 四项挂账（粒度五次法则／季度评审节律／reflect 机制／精读 document-skills） | 经验积累或节律触发 | 待办 |
| G9 | 采风员 T1 候选物化为 agent | T1 触发 | 待办 |
| G14 | 多人协作治理件（CONTRIBUTING / CODE_OF_CONDUCT / OWNERS） | 收到外部 PR 时 | 待办 |

### 等另批

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| S4 | 施工记录表拆列／hook 提示语／markdownlint 依赖 | 后续批次 | 待办 |
| S7 | 形态条补「后台 spawn」 | → 批 3（协议收口） | 待办 |
| C3 | 预验工具输出区分「命令不可用／退出非零」 | → 批 2（核验比对闸） | 待办 |
| C4 | 直引号成片存量处置 | → 批 4（按类呈报） | 待办 |
| C5 | 复审轮次与「整改轮数」列口径 | → 批 3（协议收口） | 待办 |
| C6 | 文件白名单与多批共存口径缺口 | → 批 3（协议收口） | 待办 |
| R7 | release-and-versioning §二「四处一致」补限定词 | → 批 4（规范对齐） | 待办 |
| R9 | 内容轨加粗密度检查扩展至治理文档 | 阈值另行立法 | 待办 |
| A1 | 三评委 spawn 打磨循环（五期） | 分期施工 | 待办 |
| A2 | 方法论实萃取（挂载点占位） | 萃取批 | 待办 |
| A3 | 卷纲／细纲 schema | 二三期规格 | 待办 |
| A4 | referFile 七书路径现场核定 | 萃取批 | 待办 |
| A14 | 参考标尺「脚本数事实」实现 | skill 建设批 | 待办 |
| A15 | 045 三项候选（追踪档版本指针根治／`.gitignore` 补 `__pycache__`／预验器提取规则） | → 批 2（提取规则）+ 后续 | 待办 |
| N1 | 条目 ≤ 400 字符 条的机械化守卫缺失（判据纯算术：按行首列表符切分后测字符数；宪法 §5 反模式 #9 要求可机械化的纪律固化为工具） | → 批 2（核验比对闸） | 待办 |
| N2 | file-conventions 规格行膨胀（四条规则同格，同列速查可比性下降）——下沉为表注 | → 批 4（规范对齐） | 待办 |

### 等在途收尾

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| C1 | 031 场景 1 真机走查 | 随新版大纲场景走查 | 待办 |
| G11 | 真机走查存量两笔（029 拆书真测、031 场景 1） | 下次使用／走查时 | 待办 |
| S2 | 045 §二.8 锚点重锚 | 045 施工时（046 已作废，045 单独施工或并入后续） | 待办 |

### 纯遗漏

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| C8 | 施工记录满 30 条的 P1 成因回顾 | 已到期 → 本批补做（落点见 §七.2） | 待办 |
| A9 | test-prompts 场景 1 重写（触发条件已达成） | → 批 4（规范对齐） | 待办 |
| G4 | 未来守卫待办：check.sh 增 references 相对链接扫描 | 无归属 → 待认领 | 待办 |
| G5 | 产物模板三要素「现行正例待新批模板产生」（实况已有 5 件合格模板） | 表述与实况相反 → 随批 4 修正 | 待办 |

### 已销

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| b0-1 | collab-log 施工记录表重复 `#31` 行（序号 31→32→31 逆序重复） | —（已办结） | 已销（批 0，2026-09-12 删除） |
| b0-2 | 046 规格（前提全面失效：行号 +2 漂移、替换文本会回退 045 修复、A7 基线不可复现、crashes 期望 6/8 自相矛盾） | —（已办结） | 已销（批 0 作废归档为 `archive/046-…-已作废.md`；有效内容转批 2／批 3） |
| b0-3 | 施工记录表 12 行失效提交状态（行 21–24「组队提交计划待提交」、行 25–31「待作者提交确认」、行 32 红基线措辞） | —（已办结） | 已销（批 0 改记事实：8fe8ff3／7c57d25／各库 sha；与 C7 双行并存） |
| b0-4 | check.sh 红基线（046 两处 MD038） | —（已办结） | 已销（批 0 随 046 归档复绿，退出 0） |
| A11 | 039 施工后「未预验·核验时实跑」标记未销 | —（已办结） | 已销（该批已核验通过，collab-log 行 26） |
| A16 | 030 旧设计文档两项开放问题（断点契约、数据回流） | —（已办结） | 已销（承载件已随 036 批退役归档，问题随件失效） |
| C2 | 组队提交计划（032 → 豁免批 → 035 → 033 → 034） | —（已办结） | 已销（入 8fe8ff3／7c57d25） |
| C7 | 施工记录「待作者提交确认」存量 | 行 32（045）仍待提交 | 部分已销（行 25–31 已改记库 sha；余项与 S2 同批处置；与 `b0-3` 双行并存） |
| G13 | doc-budget 体系（登记表＋守卫） | —（已办结） | 已销（本规格 §二.5 处置：改条目纪律，不建体系） |
| S3 | 预验器 GBK 解码参数修复 | —（已办结） | 已销（已随 045 落盘，`check_spec_assertions.py` 含 encoding/errors 与 stdout 重配置） |
| S8 | 046 待送审与产物审查派发 | —（已办结） | 已销（046 已作废，随件销账） |
| S9 | 046 内 crashes 期望值 6 与 8 不一致 | —（已办结） | 已销（046 已作废，随件销账） |

## 四、禁止事项

- 禁止修改附录 A 清单外的挂账事项内容；禁止在 open-items.md 内写过程叙事（只记未办事项与触发条件）。
- 禁止触碰 `file-conventions.md` :14 行（`references 域子目录` 行——A9 以其为哨兵）。
- 禁止改 `skills/` 下任何文件。
- 禁止 git commit / push。
- 禁止运行联网采集。

**偏差分级**：P0 阻塞型（规格与现状冲突，继续会做错）→ 立即停、输出改为偏差报告；P1 观察型 → 继续施工、记入核验请求。

## 五、验收标准

**断言口径声明**：命令一律取固定串计数形态（`-F -c`，数命中行）；`-F` 不可省。**零命中时退出码 1 且无输出＝通过**。「现状」列为施工前实测值，「到位」列为施工后期望值。

```bash
git grep -F -c -- "同批附件" docs/standards/file-conventions.md
git grep -F -c -- "作废批" docs/standards/file-conventions.md
git grep -F -c -- "活账本类" docs/standards/file-conventions.md
git grep -F -c -- "挂账登记核对" docs/specs/collab-log.md
git grep -F -c -- "未登记即核验未完成" docs/specs/collab-log.md
git grep -F -c -- "CHANGELOG 条目 ≤ 400 字符" AGENTS.md
git grep -F -c -- "处置裁定（2026-09-12）" AGENTS.md
git grep -F -c -- "届时以字符实测立法" AGENTS.md
git grep -F -c -- "物化进书项目" docs/standards/file-conventions.md
```

| # | 断言 | 现状 | 到位 |
| --- | --- | --- | --- |
| A1 | `file-conventions.md` 命名规则列补「同批附件」规则 | 0 | 1 |
| A2 | `file-conventions.md` 位置列补「作废批」规则 | 0 | 1 |
| A3 | `file-conventions.md` §五.2 补「活账本类」变体豁免 | 0 | 1 |
| A4 | `collab-log.md` 补「挂账登记核对」节标题 | 0 | 1 |
| A5 | 该节含判据句「未登记即核验未完成」 | 0 | 1 |
| A6 | `AGENTS.md` §7 补「CHANGELOG 条目 ≤ 400 字符」条 | 0 | 1 |
| A7 | `AGENTS.md` §6 doc-budget 条改记「处置裁定（2026-09-12）」 | 0 | 1 |
| A8 | 旧句「届时以字符实测立法」归零（防只加新句未删旧句） | 1 | 0 |
| A9 | `file-conventions.md` :14 行哨兵「物化进书项目」未被误伤 | 1 | 1 |

- A10 新建件存在：`docs/specs/open-items.md` 文件存在（施工前不存在，故不列入命令块——预验器对不存在路径会报非零）。
- A11 台账结构与附录自洽：七分组标题齐备且与附录 A 逐字同名；每个数据行恰四列（实现判据：`row.count("|") == 5` 且首尾为竖线）；**台账实点条数 = 附录 A 实点条数**（自洽判定，不依赖对外部总数的宣称）；状态列取值限「待办」／「已销（…）」／「部分已销（…）」三态。
- A12 机检：一键检查脚本（`scripts/check.sh`）退出 0。
- A13 改动面：**基线＝批 0 与 045／046 归档提交之后**；执行时点＝本规格归档**之前**。在该基线与时点下，改动面仅 `docs/specs/open-items.md`（新增）、`docs/standards/file-conventions.md`、`docs/specs/collab-log.md`、`AGENTS.md`、`CHANGELOG.md` 五文件，无其他 M/A（归档移动本身不计入改动面）。

## 六、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：§一 全部锚点现场核验——四处挂账源由 `git grep` 实测计数（README `- [ ]` 11 项；collab-log 含「挂账」5 行；CHANGELOG 含「挂账」19 行；归档开放项节抽样）；030 五文件由 `ls docs/specs/archive/ | grep '^030'` 实测；体积计数由 Python `len()` 对工作树实测并附 HEAD 对照（CHANGELOG 29,464／HEAD 27,874；collab-log 21,915）；`[T1-TODO]` 由 Python `count()` 实测 2 处；>400 字符条目由 Python 实测 17/105。AGENTS §6 引文经独立审查分身逐字核对（整句 `in` 判定为真）。
2. **清单完备**：六项改动逐项落位（台账新建／命名总表两条＋§五.2 变体行／核验协议补节／§7 补条／§6 改记）+ 过程产物三件。附录 A 为台账内容的完备清单，七分组与状态两态已定义，批 0 四项以 `b0-1`–`b0-4` 入「已销」组。
3. **验收可判定**：A1–A9 为 9 条命令块断言（现状值经预验器实跑：**7 条零命中且退出 1**；**2 条各命中 1**——其一为待归零旧句「届时以字符实测立法」〔A8〕，其一为 :14 行哨兵「物化进书项目」〔A9〕）；A10 为文件存在性；A11 为结构自洽（分组同名／四列／条数等式／状态两态）；A12 为脚本退出码；A13 为改动面对照（已声明基线与时点）。计数以「台账实点条数 = 附录 A 实点条数」自洽判定，不依赖外部总数。
4. **矛盾检查**：与本批前置的批 0 清账不冲突（批 0 四项在附录「已销」组内单列）。§6 改记与 §7 新增条分工明确（§6 记处置沿革、§7 定条目纪律），且 §6 替换句刻意不复述 §7 的 token（防 A6 被顶成 2）、不写字符数（防焊入过期数字）——两条措辞纪律已写入 §二.5。与 AGENTS §6「不做平行台账」的边界在 §二.1 头注纪律行**自足写明**（不指向规格外）。§二.2 的 :14 行哨兵与 A9 配对。
5. **术语**：「挂账台账」首现自带定义（全部未办事项的单点来源）；「同批附件」「作废批」「旧源收口」「活账本类」均在首次出现处给出判据或正例。
6. **节号复核**：本规格引用的 `AGENTS.md` §4／§6／§7、`file-conventions.md` §一／§五.2／§六、`collab-log.md` 核验协议各节与「### 结论三态」，均已现场 grep 核对存在。
7. **涟漪清单**：新件 `open-items.md` 须回写 `file-conventions.md` **三处**（§一 命名总表增行、§五 矩阵「台账类」行实例扩充、§五.2 变体豁免——均由本批 §二.2 覆盖）；核验协议补节后须复读「结论三态」节与 `collab-log.md` 头注确认无措辞冲突；`AGENTS.md` §6 改记后须确认 §6 其余条目与 §7 新增条无引用冲突。附录 A 逐条事项的出处（README／collab-log／归档／CHANGELOG）以三路勘察记录为准，施工方落表时**逐条回源核对**，不得照抄本规格。

## 七、开放项

1. **台账安放位置的备选**：本规格取 `docs/specs/open-items.md`（与 collab-log 同处，二者皆活账本）。备选为 `docs/open-items.md`（与 glossary／test-prompts 同列「项目参照」）。裁定权在作者。若取备选，须同步改动的硬编码点共五处：§二.1 标题、**§二.3 插入文本**（会写进 collab-log 正文）、§二.1 头注纪律行、A10、A13。
2. **C8（满 30 条 P1 成因回顾）**：附录 A 标为「本批补做」，但未纳入 §二 改动清单——补做产出是分析文本，落点未定。实施时若作者未指定，按「入 collab-log 头注」处理。
3. **存量回填的边界**：§二.1「旧源收口」句只约束**新增**挂账，不要求回填 README／collab-log／归档中的存量挂账文本。是否回填（使其成为单一来源）另案评估。

## 八、审查记录

**规格成稿审查（独立审查分身，2026-09-12）**：findings 17（P0:3 P1:8 P2:6），结论「修订后通过」。三项 P0 全数采纳并整稿重写——① 断言与自家替换文本互斥（§二.5 替换句改称「§7 的条目限长条」，规避 token 自噬）；② A10 计数等式漏批 0 四项、且 §六.4 有事实句与实文相反（附录 A 增「已销」组，`b0-1`–`b0-4` 入组，等式改为 58=58）；③ 体积数字取自批 0 完成前且原设计会将其焊入宪法（改为宪法侧零数字，并立「措辞纪律二」）。

**窄域复审（同一分身，只核修复面，2026-09-12）**：三项 P0 已清（实证 A6=1、A8=0、附录 58 行与七分组逐字同名、体积数字逐项复现）；另抓出 2 项新问题并已修——① `open-items.md` 未在 file-conventions 落位（§二.2 增第四处：命名总表增行 + 矩阵实例扩充）；② §六.3 的现状计数分解错（应为 7 条零命中 + 2 条各命中 1）。

**产物审查（产物审查分身，2026-09-12）**：findings 12（P0:0 P1:6 P2:6），结论「修订后通过」。P1 六条全采纳——① 活账本／台账外延同批内三处互斥（AGENTS §6 改「两个超限文件（`CHANGELOG.md`、`docs/specs/collab-log.md`）」、§7 改「二者的记录才是细节去处」）；② §6 处置只覆盖 CHANGELOG 而触发条件是通则（补「分文件处置」句，其余超限文件凭核定夺）；③ 新条可机械化却无守卫，与 §5 反模式 #9 冲突（登记台账 N1，归批 2 机械化）；④ 生效日「2026-09-12」边界不可判定（改「自 047 批起生效」）；⑤ 矩阵「台账类」行其余单元格对 `open-items.md` 失实（两单元格改写为对两件皆成立）；⑥ §0 索引未同步（必读表增 `open-items.md` 行）。P2 五条采纳（§7 补正例反例、§6 补「本条至此了结」与可寻指称、矩阵行双载体改指针、README Roadmap 加指针行），一条登记台账（N2）。
