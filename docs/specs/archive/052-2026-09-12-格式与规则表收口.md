# 规格：052 · 格式与规则表收口（规范对齐批 4c）

> 本规格自包含。施工前先读仓库根 AGENTS.md。施工方只改本规格「文件级改动清单」列出的文件。
> **背景（2026-09-12 规范全量复核 + 作者逐条裁定「按建议」）**：四项「规范与实况分歧」按裁定方向收口——①**引号形态**：规范要求中文普通引语用弯引号，实测全仓 `.md`（含在制本规格）仅 2 处、**规范自身 0 处**（其正例用的就是直引号）；②**状态符号白名单**只有 ✅🚧，实况 6 种且 `docs/product/大纲阶段追踪档.md:5` 自设图例把 🔴🔨⏳➕ 作状态列正式取值；③**退出码**规范写三分类，`setup-cdp-chrome.js` 实为四值（含 3＝需交互同意但非交互环境）；④**MD034/MD024 与配置不符**：规范的「不采用」表列 MD034 而 `.markdownlint-cli2.jsonc` **未关**（MD034 在跑），配置改写的 MD024 规范未记。另两项：⑤ 规范 §五 机检列写「node --check」而**全仓无该自动化**（此前仅有反模式 #7 的手工纪律要求，无脚本化载体）；⑥ README 薄壳措辞「`abzu skill {域}`」与 `file-conventions.md:32` 权威「`{路由键}`」不一致。
> **本批明确不做**（归 **4d**）：索引与词条补缺、test-prompts 场景重写——**本批即 4c「格式与规则表」**；台账内 N2／A9 原标「→ 批 4c（索引与词条补缺）」的指向须随本批改为 4d；**另不做**：加粗密度条文的文件域范围（台账 R9）、README「115 处」数字刷新（050 批已加时点标注）。
> 沿革入 CHANGELOG，本规格只记现状锚点与改动清单。

## 一、现状锚点（2026-09-12 现场核验，行号为施工前时点实测值）

- **`docs/standards/markdown-style.md`（72 行 / v0.5）**：`:25`「引号三分：节名与需逐字复现的内容用「」…；**中文普通引语用弯引号 ""**；直引号 " 仅出现在代码与参数中。」——**实测全仓 `.md` 的左弯引号 U+201C 仅 2 处**（`archive/032` 与 `scan-genre-trends.md:109`），**该规范文件自身 0 处**，且其 `:44` 的正例「✅ 这就是所谓的"降维打击"（普通引语）」**用的就是直引号**（自违）。`:54`「状态符号**仅限** ✅（已就绪）与 🚧（已立项未建设）用于状态列与清单」——实测 `追踪档` 的 ✅×82／🔴×5／🔨×3／⏳×3／➕×7／⚠️×2，且该档 `:5` 自设图例「甲：✅ 已入设计 ｜ 🔨 校准中 ｜ ⏳ 待讨论 ｜ ➕ 开放集补充；乙：🔴 未修入/部分修入 ｜ ✅ 已修入（注明版本）」——**白名单外的 5 种是该档自设图例与门槛句的正式取值**。`:58-62`「不采用」表列 **MD013／MD034／MD040** 三条；`.markdownlint-cli2.jsonc` 实关 MD013／MD040、并对 **MD024 设 `siblings_only`**——**MD034 未关（在跑）**、**MD024 未在规范登记**。
- **`docs/standards/file-conventions.md`（133 行 / v0.15）**：`:45`「| 退出码 | `0` 成功 / `1` 违规 / `2` 参数或环境错误（**三分类**，缺/空/坏各明示） |」——实件 `skills/abzu-scan/scripts/setup-cdp-chrome.js` 有第四值：启动器 `:42` 注「3 需要同意但当前为非 TTY 且未传 --yes」、`:737` `process.exit(3)`；`scan-cdp-base.md:181` 亦记「`NEEDS_CONSENT` + 退出码 3」。`:98` 机检列「**node --check** + 契约对照（审查轨）」——实测 `scripts/check.sh` 五段（`[0]`–`[4]`）**无 node --check 段**，该命令此前仅作**手工纪律**（`AGENTS.md` §5 反模式 #7「脚本改动后必跑语法检查」）与归档规格验收断言出现——**全仓无该自动化**。
- **`README.md`（96 行）**：`:87`「薄壳样式：`.claude/commands/abzu-{域}.md`，单行 `abzu skill {域}`」——权威表述在 `file-conventions.md:32`「单行内容 `abzu skill {路由键}`」（**路由键** vs **域**，一字之差）。
- **`scripts/check.sh`（93 行）**：五段 `[0]`–`[4]`；段序的**六处载体**（2026-09-12 实测穷尽）：`check.sh` 自身头注**四处**（L2 汇总行／**L5 默认全量**／L7-15 依赖与前置／L19 维护入口）、`AGENTS.md:14`、`docs/standards/file-conventions.md:85`。本批新增段须**六处全部同步**（L5 系 048 批认定的「第四处载体」，本稿初版漏列，见 §二.3e）。

## 二、文件级改动清单

### 1. `docs/standards/markdown-style.md`：引号、状态符号、MD 规则表 [A]

**1a. `:25` 引号三分**（替换范围：仅该行）：

- 现文：`6. 引号三分：节名与需逐字复现的内容用「」（如「扫榜报告模板」节、「你倾向哪个选题？」）；中文普通引语用弯引号 ""；直引号 " 仅出现在代码与参数中。`
- 替换为：「6. 引号三分：① 节名与需逐字复现的内容用「」（如「扫榜报告模板」节、「你倾向哪个选题？」）；② **中文普通引语用直引号 `"`（本仓常态）**——中文与代码混排下弯引号与全角标点易混，且弯引号全仓实测几乎不用（本规范追认实况，沿革见 CHANGELOG）；③ 代码与参数内的引号随语言惯例。」

**1b. `:54` 状态符号白名单**（替换范围：仅该行）：

- 现文：`5. 状态符号仅限 ✅（已就绪）与 🚧（已立项未建设）用于状态列与清单；禁装饰性 emoji。`
- 替换为：「5. **状态符号白名单**（清单与状态列的取值须出自本名单，沿革见 CHANGELOG）：✅ 已就绪/已修入｜🚧 已立项未建设｜🔴 未修入或待处置｜🔨 校准中｜⏳ 待讨论｜➕ 开放集补充｜⚠️ 风险或依据强度提示。产品文档可在其头部另设图例界定本名单内符号的亚义——**图例不得引入名单外符号，新增符号须回改本白名单，禁纯装饰性 emoji**（正例：`docs/product/大纲阶段追踪档.md` 的甲/乙两套，全用名单内符号；反例：自设 `🟡` 之类名单外符号作状态值）。另：示例对照、判定表与自核验模板用的正反例标记（✅／✓／✗／❌），以及命令输出样例与沿革叙述中指称的符号（如 `⏱`／`⏸`），均不属清单与状态列取值，不受本白名单辖。」

**1c. `:61` MD034 行删除 + 表后补 MD024 登记**（替换范围：删该行；表后追加一段）：

- 删除现文行：`| MD034 裸 URL | 本仓 URL 均在链接或反引号中，属误报噪声 |`
- 表后追加：「配置摘要（与 `.markdownlint-cli2.jsonc` 对齐）：上表两条（MD013／MD040）已在该配置关闭；裸 URL 规则（MD034）已自本表移出——该配置未关闭它（裸 URL 会报，属有意保留）；另该配置对 MD024 设 `siblings_only`（重复标题仅同父级下才算——各平台段落下的同名小节是合法平行结构），特此登记以免规范与配置各说一套。」

（`:44`／`:45` 的正例无需改——`:44` 的普通引语正例本就用的直引号，改后与 `:25` 一致。）

### 2. `docs/standards/file-conventions.md`：退出码四分类 + 机检列 [A]

**2a. `:45` 退出码**（替换范围：仅该行规则列）：

- 现文：`` `0` 成功 / `1` 违规 / `2` 参数或环境错误（三分类，缺/空/坏各明示） ``
- 替换为：「`0` 成功 / `1` 违规 / `2` 参数或环境错误 / `3` 需交互同意但当前为非交互环境（**四分类**，缺/空/坏各明示；`3` 的实件为 `skills/abzu-scan/scripts/setup-cdp-chrome.js` 的 `NEEDS_CONSENT`——该脚本 `1`／`2` 的语义与本表不一致，**不得据以仿写**，收口挂账台账 N20）」

**2b. `:98` 机检列**（替换范围：仅该行末列）：「node --check + 契约对照（审查轨）」→「`node --check`（`check.sh` `[5]` 段）+ 契约对照（审查轨）」。

**2c. `:85` 一键入口列举**（替换范围：仅该括注）——**段序第四处载体，必须随 §二.4 同批同步**：「（[0] skills 真目录守卫 + [1] 六壳 validate + [2] markdownlint + [3] 内容轨 + [4] 规格静态自检）」→ 末尾追加 `+ [5] 脚本语法检查`。

### 3. `scripts/check.sh`：新增 `[5]` 段（脚本语法检查）[B]

**改动（三处）**：

- **3a. 新增段**（位置：`[4]` 段之后、`if [ "$fail" -ne 0 ]` 之前）：

```bash
echo "[5] 脚本语法检查（node --check）..."
NODE_BIN=""
command -v node >/dev/null 2>&1 && NODE_BIN=node
if [ -z "$NODE_BIN" ]; then
  echo "x node 未找到（脚本语法检查需要 Node.js）"
  fail=1
else
  for f in skills/abzu-*/scripts/*.js; do
    [ -e "$f" ] || continue
    "$NODE_BIN" --check "$f" || fail=1
  done
fi
```

- **3b. 头注汇总行（L2）**（整行替换）：现文末「+ 规格静态自检」→「+ 规格静态自检 + 脚本语法检查」。
- **3c. 头注「依赖与前置」段**（在该段末行「任一依赖缺失不静默跳过…」**之前**插入一行）：内容为 `#   [5] node —— 脚本语法检查（node --check）；缺失时该段输出 x 提示并置 fail=1`。
- **3d. 头注「维护入口」段**：现文「仿 `[1][2][3][4]` 段式追加一段」中的 `[1][2][3][4]` → `[1][2][3][4][5]`。

- **3e. 头注 L5「默认全量」列举**（整行替换——**该处系 048 批认定的段序第四处载体**，本稿初版漏列）：现文 `#   默认全量（六壳 validate + 全仓 markdownlint + 引用闭合 + 加粗密度 + TOC + 规格静态自检）` → 替换为同文末尾追加「 + 脚本语法检查」（加号前留一个半角空格）。
- **3f. 头注 `--staged` 行（L4，整行替换；产物审查 P2 采纳的核验修正项）**：现文 `#   --staged  仅检查 git 暂存区涉及的 .md 文件（增量模式，日常快速反馈）` → 替换为 `#   --staged  仅 [2] markdownlint 收窄为「暂存区涉及的 .md」（其余段仍全量；增量模式，日常快速反馈）`——新增 `[5]` 段在 `--staged` 下仍全量跑，旧措辞易被读成整脚本增量。

### 4. `AGENTS.md`：§0 段序列举补 `[5]` [A]

**替换范围（显式限定）**：仅 §0「一键检查（提交前必跑…）」条内段序列举的末项之后追加 `＋[5] 脚本语法检查（node --check）`（加号前留一个半角空格）；该条其余文字逐字保留。

### 5. `README.md`：薄壳措辞对齐 [A]

**替换范围（显式限定）**：该行内**两处**「{域}」均改为「{路由键}」（文件名侧 `abzu-{域}.md` 与内容串 `abzu skill {域}` **皆违** `file-conventions.md:32` 权威「`abzu-{路由键}.md`，单行内容 `abzu skill {路由键}`」；只改内容串会留下同一行两名并存）。另 `docs/specs/open-items.md` 的 G1 行同批对齐。

### 6. 两份文件版本行 bump [A]

**理由**（承 050 批 §二.8 立例）：本批对**两份**文件做实质修订，按 `file-conventions.md` §五.2「实质内容修订 minor」各自 bump；替换范围仅 `> 版本：` 行的版本号末位 +1：

- `docs/standards/markdown-style.md:3`：`v0.5` → `v0.6`
- `docs/standards/file-conventions.md:3`：`v0.15` → `v0.16`
（**`skill-writing.md` 本批不动**：该文件不在本批改动面内，故不 bump——A16 改动面清单亦不含它。）

### 7. 过程产物（规划方职务，非施工方项）

- `CHANGELOG.md` `### Changed` 顶部新增 052 条目（≤ 400 字符）。
- `docs/specs/collab-log.md` 施工记录表末行追加一行（序号顺延）。
- 本规格补「审查记录」节；归档至 `docs/specs/archive/`。
- `docs/specs/open-items.md`：① C4（直引号成片存量处置）销账（引号规范已追认直引号为常态，存量无需刷改；**原标「→ 批 4b」已过批**，销账依据记 052 批）；② **N2 与 A9 的指向由「批 4c」改为「批 4d」**（本批 4c 已重定义为格式与规则表）；③ 按 §六 登台账。

## 三、禁止事项

- **禁止改动 `markdown-style.md` 与 `file-conventions.md` 的其它条目**——本批只改 §二 明列的六处 + 两处版本行。
- **禁止改动 `skills/` 下任何脚本**（`node --check` 是只读检查，不得改被检脚本；`setup-cdp-chrome.js` 只被引用、不改）。
- **禁止改 `docs/product/` 两份产品文档**（追踪档只被引用为图例实例，不改）。
- 禁止 git 写操作；禁止联网采集。

**偏差分级**：P0 阻塞型（规格与现状冲突，继续会做错）→ 立即停、输出改为偏差报告；P1 观察型 → 继续施工、记入核验请求。

## 四、验收标准

**断言口径声明**：命令一律取固定串计数形态（`-F -c`，数命中行）；**零命中时退出码 1 且无输出＝通过**。「现状」列为施工前实测值，「到位」列为施工后期望值。

```bash
git grep -F -c -- "中文普通引语用弯引号" docs/standards/markdown-style.md
git grep -F -c -- "🔨" docs/standards/markdown-style.md
git grep -F -c -- "MD034 裸 URL" docs/standards/markdown-style.md
git grep -F -c -- "siblings_only" docs/standards/markdown-style.md
git grep -F -c -- "三分类" docs/standards/file-conventions.md
git grep -F -c -- "NEEDS_CONSENT" docs/standards/file-conventions.md
git grep -F -c -- "[5] 脚本语法检查" scripts/check.sh
git grep -F -c -- "node --check" scripts/check.sh
git grep -F -c -- "＋[5] 脚本语法检查" AGENTS.md
git grep -F -c -- "abzu skill {域}" README.md
git grep -F -c -- "v0.6（沿革见" docs/standards/markdown-style.md
git grep -F -c -- "v0.16（沿革见" docs/standards/file-conventions.md
```

| # | 断言 | 现状 | 到位 |
| --- | --- | --- | --- |
| A1 | 弯引号要求归零（追认直引号为常态） | 1 | 0 |
| A2 | 状态符号白名单扩项（含 🔨 等实况符号） | 0 | ●≥1 |
| A3 | `markdown-style.md` 的「不采用」表内 MD034 行归零（token 取表行独有形态「MD034 裸 URL」；**配置摘要内保留一次「MD034 未关闭」的说明**，故不取裸 token） | 1 | 0 |
| A4 | 补 MD024 `siblings_only` 登记 | 0 | ●≥1 |
| A5 | 「三分类」归零（改四分类） | 1 | 0 |
| A6 | 退出码 3 的实例指认（`NEEDS_CONSENT`） | 0 | ●≥1 |
| A7 | `check.sh` 新增 `[5]` 段 | 0 | 1 |
| A8 | `check.sh` 含脚本语法检查实作 | 0 | ●≥1 |
| A9 | `AGENTS.md` §0 段序列举补 `[5]` | 0 | 1 |
| A10 | README 薄壳措辞对齐（旧串归零） | 1 | 0 |
| A11 | `markdown-style.md` 版本行 v0.6 | 0 | 1 |
| A12 | `file-conventions.md` 版本行 v0.16 | 0 | 1 |

- A13 **六载体**段序复核（手工）：`check.sh` 头注**四处**（L2 汇总行／**L5 默认全量**／依赖与前置／维护入口）与 `AGENTS.md:14`、`file-conventions.md:85` **六处全部**已含 `[5]`，且六处表述一致——**本项是本批最易漏的涟漪**（048 批曾漏 `file-conventions.md:85`；本稿初版又漏 `check.sh` L5，见 §二.3e）。
- A14 机检：一键检查脚本（`scripts/check.sh`）退出 0（**新增的 `[5]` 段须真跑且全过**——施工后须实跑确认 `skills/abzu-*/scripts/*.js` 全部脚本（实测 **8 个**：analyze 域 1 + scan 域 7）均通过该语法检查）。
- A15 语义复核（手工）：`markdown-style.md` 改后三条读通（引号三分／状态符号白名单＋图例例外／不采用表只剩两条＋配置摘要）；`file-conventions.md` 退出码四分类中 **`0`／`3` 两个取值**与 `setup-cdp-chrome.js` 实件语义一致——**口径收窄**（初版作「四分类与实件一致」过宽：该脚本 `1`＝通用错误、`2`＝用户拒绝，与表的 `1`＝违规、`2`＝参数或环境错误不同义，且参数校验走 `1` 无用法说明；收口挂账台账 N20，规范行已加「不得据以仿写」）。
- A16 改动面：**基线＝48b8f4a 之后**；执行时点＝本规格归档**之前**。在该基线与时点下，改动面仅 `docs/standards/markdown-style.md`、`docs/standards/file-conventions.md`、`scripts/check.sh`、`AGENTS.md`、`README.md`、`CHANGELOG.md`、`docs/specs/collab-log.md`、`docs/specs/open-items.md` 八文件，**外加本规格自身**（归档移动不计入）。

## 五、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：§一 全部锚点现场核验——`markdown-style.md` 的 :3／:25／:26／:38／:44／:45／:54／:58-62 逐行 read；弯引号 2 处与规范自身 0 处由 Python 计数实测；追踪档符号集（✅82／🔴5／🔨3／⏳3／➕7／⚠️2）与 `:5` 图例由 read 核对；`.markdownlint-cli2.jsonc` 的 MD013／MD040 关闭与 MD024 `siblings_only` 由 read 核对，MD034 未关（在跑）由临时夹具实测（050 批留痕）；`file-conventions.md` :45／:85／:98 逐行 read；`setup-cdp-chrome.js` 的 `:42`／`:737` 与 `scan-cdp-base.md:181` 由 read 核对；`scripts/check.sh` 五段与段序四处载体（`AGENTS.md:14`／`file-conventions.md:85`／`check.sh` 头注三处）由 grep 实测；`README.md:87` 与 `file-conventions.md:32` 的措辞差由逐字对照。
2. **清单完备**：七项改动逐项落位（markdown-style 三处／file-conventions 三处含段序载体／check.sh 四处含新增段与头注三处／AGENTS 一处／README 一处／三份版本行／过程产物四类 + 台账 C4 销账）。
3. **验收可判定**：A1–A12 为 12 条命令块断言（现状值可实跑复现：A1／A3／A5／A10 各 1，余为 0）；A13 为四载体手工复核（**本批重点**）；A14 为脚本退出码（含新增段真跑）；A15 为语义复核；A16 为改动面对照。**预验摘要（实跑，含 SKIP 项）**：**动态段桶行** `共 12 条：执行 12｜跳过 0｜崩溃 0`；静态段预警按落盘语义逐条核对（§二 的「现文」引文会致过度计数，替换为侧已自查不含须归零 token：`中文普通引语用弯引号`／**`MD034 裸 URL`**（归零 token 取表行独有形态；配置摘要内保留「MD034 未关闭」的说明，不计入）／`三分类`／`abzu skill {域}`）。
4. **矛盾检查**：与 AGENTS §0（检查命令与段序）、§3.9（新条文不写批次叙事——本批 §二.1b／1c 的说明句写在**规格内**、落盘文本只含判据与白名单，**落盘的说明不含批次号**）、§5 反模式 #6（退出码不经管道）一致；与 `file-conventions.md` §五.2（bump 语义）一致——两份规范各自 minor；与 §6 不做清单无涉；与台账：C4 本批销账、R9（加粗密度文件域）**不动**（本批明确不做）。**与 4d 的边界**：不动索引与词条补缺、不动 test-prompts 场景。
5. **术语**：「状态符号白名单」「配置摘要」「四分类」「路由键」在首现处给出判据或指认；`NEEDS_CONSENT` 作为退出码 3 的实例点名。
6. **节号复核**：本规格引用的 `AGENTS.md` §0／§3.9／§5／§6、`file-conventions.md` §一（脚本编写规范）／§五.2、`markdown-style.md` §一／§三／§五，均已现场 grep 核对存在。
7. **涟漪清单**：**段序的四处载体已全部列入改动面**（§二.3b-3d／§二.2c／§二.4），并配 A13 专项复核——048 批的教训是这处载体易漏；`MD034` 移出规范表后须确认配置**仍开着**（有意保留，不是关掉）；`abzu skill {域}` 与 `abzu-{域}.md` 归零后须 grep 全仓确认无第二处用「{域}」指认薄壳（**豁免面**：`archive/**` 历史引文与在制规格自身）；两份版本行 bump 后须确认无第三处引用其旧版本号。

## 六、开放项

1. **`node --check` 段的检出范围**：本批取 `skills/abzu-*/scripts/*.js`（运行时脚本与共享库）；**不检** `scripts/*.py`（Python 侧由 `check_content.py` 与实跑覆盖）与 `scripts/hooks/pre-commit`（shell，无对应语法检查器纳入）。若作者要求覆盖 shell，另批评估（`bash -n` 可用但属新增能力）。
2. **状态符号白名单的「图例例外」**：本批允许产品文档自设图例界定亚义（如追踪档甲乙两套），但**未规定图例本身的格式**；若后续多份产品文档各设图例导致发散，另批收口。
3. **C4（直引号成片存量）**：本批以「追认直引号为常态」销账——存量无需刷改；若作者认为仍有需统一的引号形态（如 `archive/032` 的两处弯引号是否回改），另批评估。

## 七、审查记录

> **行号时点声明**：本规格 §一–§四 内的行号均为**施工前时点**坐标（`markdown-style.md` 72→74 行、`scripts/check.sh` 93→107 行、`open-items.md` 116→118 行），归档后不得据其定位现行位置。

**规格成稿审查（独立审查分身，2026-09-12）**：**F1（P0）段序载体误记「五处」、实为六处**——初稿漏掉 `check.sh` 头注 L5「默认全量」这一处（048 批认定的「第四处载体」）；若放行，施工会漏改 L5，而 A13 按错误的「五处」核还会全绿。处置：§一 `:13` 改正为「六处载体（头注四处＋`AGENTS.md:14`＋`file-conventions.md:85`）」，§二.3e 补 L5 整行替换条目；施工后实测六处齐备。**同轮其余 findings 数与 P1／P2 清单已不可考——如实记**：完整清单随会话上下文压缩丢失，本规格的验收记录因此不完整。**教训（已并入批 3 收口议题）**：审查记录节必须在审查交回时即时落盘，不得留到批次收尾。

**产物审查（产物审查分身，2026-09-12）**：findings 6（P0:0 P1:2 P2:4），深度 D2（逐字核验＋实测复跑），结论「修订后通过」。审查覆盖：5 个改动文件逐字核验、12/12 断言独立重跑、`check.sh` 全量＋`--staged` 双模式实跑、`[5]` 循环手工复跑 8/8、规范与 `.markdownlint-cli2.jsonc` 双向对读、六载体穷尽 grep、全仓符号扫描。两项 P1——**P1-1**：`open-items.md` 的 G1 行仍留 `abzu-{域}.md`，本批要收口的「同一分歧」在第二处存活（全仓复扫确认：除 archive 与在制规格外，用「{域}」指认薄壳者仅此一处）→ 采纳，归规划方过程产物批内对齐为 `{路由键}`。**P1-2**：新「四分类」的 `1`／`2` 语义与本行亲自点名的实件相冲——实件参数缺失/非法与端口非法走 `exit 1` 且无用法说明（同表「输入校验」行要求 2＋用法）、环境错误走 `1`（表 `1`＝违规）、用户拒绝走 `2`（表 `2`＝参数或环境错误），`cdp-utils.js` 以 `exitCode = 2` 表 partial；判读为**规格侧口径缺口**（只收了「值的个数」未收「1／2 的语义」），非施工偏差 → 采纳：规范行把实件指认收窄为「`3` 的实件」并加「不得据以仿写」，**登台账 N20（方向待裁决：收口脚本 or 放宽规范）**，A15 口径同步收窄为「`0`／`3` 一致」。P2×4 全采纳：`:25` 补主语「弯引号」消第二读法、`:54` 豁免括注补 `✓`、`:63` 去「MD034 行」邻接（消检查 B 假阳性）、`check.sh:4` 注释澄清 `--staged` 仅 `[2]` 收窄。

**受限复核（独立审查分身，2026-09-12；范围＝五处核验修正＋直接影响面，非全文审）**：findings 6（P0:0 P1:3 P2:3），结论「修订后通过」。**本轮抓到的三条 P1 全部源于「规划方直改落盘件」这一形态**——F1：**本批把所改文件顶破其自身规则**——`markdown-style.md` 的 `**` 计数由 HEAD 的 15／72（清洗后 4）升到 37／73（清洗后 26），越过该文件 §三.9 的上限「行数÷3＝24」，新增 22 处加粗全在本批改动的三段（`:25`＋2／`:54`＋12／`:63`＋8）；**机检掩盖了它**，因 `check_content.py` 的加粗段只扫 `skills/abzu-*/**/*.md`、不含 `docs/`（正是台账 R9 所挂的缺口）。→ 采纳：按该文件 §六「违反本规范视同文档 bug，当批顺手修」收掉装饰性加粗（`:54` 三处红线并为一句、`:63` 四处尽去），落盘后清洗计数 **10 ≤ 24**。F2：A15 未按新口径落盘（现行文与落盘件及实件三方互斥）→ 采纳，已收窄。F3：规格侧替换文本未随五处修正同步（`:22`／`:27`／`:32`／`:41`）且 §二.7 自列的「补审查记录节」未执行 → 采纳，四处逐字回改并补本节。P2×3：F4（N19 内嵌行数会随件漂移、宜加「在制期」限定）采纳；F5（`:25` 期望文本含加粗而落盘不含）**回令确认**——不加粗更贴合 §三.9 的四类限定，取落盘态；F6（豁免句未覆盖 `collab-log.md` 自核验模板内的 `✓`）采纳，首分句扩为「示例对照、判定表与自核验模板」。复核自报副作用一并处置：其定位假阳性源头时 import 预验器生成的 `scripts/__pycache__/` 已删（红线 2）。

**交付点核验（规划方，2026-09-12）**：三步独立重跑——改动面 **M×5 ＋本规格**；12/12 断言在最终工作树绿；**六载体段序六处齐备**（`check.sh` L2／L5／L16／L20 ＋ `AGENTS.md:14` ＋ `file-conventions.md:85`）；`check.sh` 退出 0（`[5]` 真跑 8/8 脚本）；lint 94 文件 0 违例；内容轨全绿（含本批 CHANGELOG 条目 359 字符 ≤400）；版本行 v0.6／v0.16 无旧号残留。

**过程成本与两条教训**：本批经「成稿审查（1 处 P0）→施工→核验→产物审 6f→改 5 处→受限复核 6f→再修 4 处」，其中五处修正由规划方直改落盘件（承 048 批「核验修正」先例，为省一轮往返）。两条教训均并入批 3：① **规划方直改落盘件时须同批回改规格替换文本，并重跑该文件的全部自身规范**（本批漏前者、破后者，靠受限复核才抓回）；② **审查记录节须在审查交回时即时落盘**（F1 其余 findings 永久丢失的根因）。
