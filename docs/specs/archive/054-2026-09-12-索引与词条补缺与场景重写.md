# 054 · 索引与词条补缺 + 场景重写（批 4d）

> **状态**：待作者确认（AGENTS §3.1 判定表 ✅：修改既有文件的多处内容 + 跨文件）
> **依据**：台账 A9（test-prompts 场景 1 重写）、N2（`file-conventions` 规格行膨胀下沉）；另本轮勘验新得一项（§二.3）
> **基线**：`ba5350d` 之后；**自由度标记**：`[A]` 逐字执行 ｜ `[B]` 按方向自定实现 ｜ `[C]` 按验收标准核验

## 一、现状锚点（2026-09-12 现场勘验；行号与计数为施工前时点实测值）

### 1. `docs/test-prompts.md`（v0.8，151 行）——场景 1–5 整组建在已推倒的旧设计上

该文件把场景分两组：场景 6/7/8「当前可执行」（scan／analyze 域已建设），场景 1–5「待启用契约」（对应域建设后生效）。**问题不止场景 1**：

- `:16` 场景 1 自带免责注：「大纲域已推倒重设（036 批），本场景断言基于旧设计，待新版《大纲阶段设计文档》定稿后重写」——**触发条件已达成**（设计文档 v0.9.6 已成定格真源，§5.3 给出书目录结构）。
- 场景 1–5 的断言写死了一整批**已随 036 批退役的旧产物名**（实测命中行号）：`project.md`（`:27`／`:34`／`:43`／`:59`）、`文风/文风指南.md`（`:27`）、`大纲/总纲.md`（`:28`）、`设定/设定集.md`（`:28`）、`追踪/前情提要.md`（`:57`）、伏笔账本（`:49`／`:61`／`:75`／`:81`）、人物状态（`:49`／`:61`／`:62`／`:75`）。
- **该文件头注自己定了相反的原则**（`:7`）：「**断言写行为不写资产路径细节**（路径/文件名随设计演进，行为级断言稳——三轮冲突立例）」。即这批死名同时违反文件的自定纪律。
- `创作进度.md`（`:29`／`:76`）**在新设计中存续**（§5.3 书目录含之），不是死名——重写时须保留（防误删哨兵）。

**新设计的对应命名（`docs/product/大纲阶段设计文档.md` v0.9.6 实测）**：`§5.3 书目录结构` —— `基本规格.md`（三态：草稿→实测→定格）／`创作进度.md`（状态+产物状态表+断点）／`资料/参考-{书名}.md`／`参考标尺.md`／`大纲/大纲.md`（六节）／`设定/世界观底盘.md`／`设定/人物卡/{角色}.md`／`打磨/`四件。**第一章 规格步**：`1.0 开书与脚手架`（②收集＝查当前目录无 `创作进度.md`；⑥交互＝弹窗「在当前目录开新书吗？」[开新书/只是问问]；④产物＝书目录树＋`创作进度.md` 初始化为「大纲-规格中」）、`1.1 规格收集`（③按〔作家思维：降级〕；④产物＝`基本规格.md` 草稿态五段式；⑥交互三段式＝段一自由聊／段二回读默认／段三弹窗≤3+对话≤3，**终局与感情线不问**（准入律））、`1.2 规格门`（过关＝①硬检查全绿 ＋②作者确认；通过后翻「大纲-资料收集中」）。

**落位归属（写规格前须判清）**：场景 1 属**大纲域**（设计真源已存在，可据实重写）；场景 2–5 属**write／volume／style 域**——这三域**均无设计真源**（写作链设计文档随 036 批退役，新设计只覆盖大纲阶段）。故本轮**只对场景 2–5 做「去死名 + 降为行为级」最小处置**，不做全面重写：无真源的重写属猜测（宪法 §3.9「立法先于实例的条文是猜测」）。

### 2. `docs/standards/file-conventions.md`（v0.17，133 行）——规格行膨胀

`:26` 的「规格」行把**六条规则**塞在同一行的两格里——命名规则格：命名（`{序号NNN}-{YYYY-MM-DD}-{中文主题}.md`）、序号规则（三位递增、永不复用）、**同批附件**（沿用主规格序号、语义后缀）；位置格：在制存放位（`docs/specs/`）、归档去向、**作废批**。同列速查可比性下降（N2 原话）。

### 3. 本轮勘验的三处「旧笔记作废」（如实记，防后续误改）

- **G5 已归零**：050 批（4a）已改 `skill-writing.md:31`，`git grep -F -c -- "现行正例待新批模板产生" docs/standards/skill-writing.md` 实测 **0**。故 G5 本批**销账**，不是待办（台账原有的「随批 4 修正」指示已由 4a 实际完成而不自知）。
- **glossary 无缺项**：051 批已把「施工分身／独立审查分身／产物审查分身」写入「子代理」条（`:74`），`逻辑角色` 条（`:30`）亦在。**我此前「glossary 缺分身族词条」的说法不成立**，不列为改动。
- **`release-and-versioning` §八 无缺项**：`:73` 已记「远程仓库已建立…该步性质由『条件未满足』改为『条件已解除、步骤保留』」——051 批已回写。

### 4. 本轮勘验**新得**的一项（本规格范围外，登记见 §二.3）

`docs/standards/skill-writing.md` 存在**活体自相矛盾**（028 批备案未决）：`:47` 末句「**禁用 UI 词汇（弹窗/界面）描述交互**」与 `:48`「workflow 正文每个用户问句后必须标模态——「**（弹窗：选项列表）**」或「（对话式）」」正面冲突——后者强制使用前者禁止的词。实况用法实测：`skills/abzu-analyze/references/analyze/analyze-workflow.md` 3 处、`skills/abzu-scan/references/scan/scan-workflow.md` 1 处，均用「（弹窗：…）」形态。**处置**：本批不动（改哪一侧都要动 skill 正文或改标注词汇，属口径矛盾清零范畴），改以台账登记并按计划归入**批 3**。

## 二、文件级改动清单

### 1. `docs/test-prompts.md` [B]

**1a. 场景 1 整节重写**（替换范围：`:14`–`:30` 整节；本节为 `[B]` 方向实现，但须满足下列硬要素）：

- 前置条件改为：在一个空目录中启动会话（无任何写作项目文件——**判定证据＝无 `创作进度.md`**，对齐设计 §1.0②）。
- 保留原 prompt（真实使用口吻）：`/abzu-outline` ＋「我想开一本玄幻网文，讲一个废柴少年捡到能吞噬功法的神秘鼎，帮我从头开始」。
- 断言改按新设计，**至少覆盖**：①〔机检〕skill 经显式调用加载（域壳入口判定：无 `创作进度.md` → 全新开书）；②〔机检〕产出 `创作进度.md`（当前阶段＝**大纲-规格中**）＋ 书目录骨架就位（`基本规格.md` 等空模板），**未产出**后续阶段实质内容（`大纲/大纲.md`／`设定/世界观底盘.md`／`资料/参考-{书名}.md` 为空骨架或无）；③〔人工〕`1.0` 的开门问句停靠（「在当前目录开新书吗？」）；④〔人工〕规格收集按三段式推进（段一自由聊 → 段二回读默认 → 段三弹窗≤3+对话≤3），**终局与感情线不问**（准入律）；⑤〔人工〕`1.2` 规格门停靠等作者确认，**未**在确认前进入第二章。
- 删除该节顶部的免责注（`:16`）——触发条件已达成。
- **禁止**在本节引入设计文档未定义的产物名；节内所有产物名须能在 `docs/product/大纲阶段设计文档.md` §5.3 或第一章找到出处（施工时逐名核对）。

**1b. 场景 2–5 去死名 + 降为行为级**（替换范围：`project.md`（`:34`／`:43`／`:59`）、`追踪/前情提要.md`（`:57`）、伏笔账本（`:49`／`:61`／`:75`／`:81`）、人物状态（`:49`／`:61`／`:62`／`:75`）所在处）：

- **覆盖声明（成稿审查 P1-4 采纳）**：`文风/文风指南.md`（`:27`）、`大纲/总纲.md`／`设定/设定集.md`（`:28`）**实为场景 1 内的 token**，由 §二.1a 的整节重写一并清除；本项清单不另处置（列名只为归零指向明确）。施工时勿因其出现在本标题段下而漏清。
- 新设计有对应名的换名：`project.md` → `基本规格.md`（定格态）、`大纲/总纲.md` → `大纲/大纲.md`、`设定/设定集.md` → `设定/世界观底盘.md`。
- **无对应名者**（`追踪/前情提要.md`、伏笔账本、人物状态）降为行为级表述，**不新造文件名**——这三类属 write／volume／style 域，其设计真源尚未产生；措辞用「状态/追踪类产物（前情、伏笔、人物状态）」。**范围补充（核验修正，承反模式 #2「grep 词须含概念近义词不止原句」）**：`伏笔账本` 的**短称「账本」**另有两处须同批改（`:88`「账本登记的计划回收点」、`:91`「不擅自改动账本状态」）——按 token `伏笔账本` grep 抓不到，属姊妹措辞漏网；改为「状态/追踪类产物（伏笔）中登记的…」「不擅自改动该产物的状态」。
- **`创作进度.md` 保留不动**：`:49`／`:61` 原文的「进度」是四个追踪项之一，须改写为具名 `创作进度.md`（不得随其余三项一并降为类别词）——该件在 `docs/product/大纲阶段设计文档.md` §5.3 书目录中**存续**，且是 §四 A7 的防误删哨兵对象。
- `:34` 的「＋基础设定」非文件名（术语表无此条、设计文档 §5.3 无对应目录）→ 降为行为级「＋基础设定（世界观底盘初稿）」，**不新造名**。

**1c. `:8` 状态分组行**（替换范围：该行内场景 1–5 的分组说明）：补一句「场景 2–5 所属 write／volume／style 域尚无设计真源：**旧设计路径名已清**，剩余「未写路径但指向具体文件」的边界措辞（如 `细纲文件`／`卷目录`／`第3章 对应文件`）保持不动，待各域设计出现时随全面重写一并收口」——**注意：括号内的「（§二.1b）」是规格内部定位、不得落进目标文件**（施工方 P1-a 据自包含律省略，本处已同步订正；该括注若落盘即成本文件内的悬空节号引用）。

**1d. `:3` 版本行**：`v0.8` → `v0.9`。

**1e. `:7` 断言纪律加限定（核验修正；产物审查 P1-1 采纳）**（替换范围：整行）：现文「**断言写行为不写资产路径细节**（路径/文件名随设计演进，行为级断言稳——三轮冲突立例）；占位域/待启用域类断言引用头注状态分组，不硬编码域清单。」→ 替换为「**断言写行为；产物名只在设计真源已定格时写入**（未定格域用类别词——正例：场景 1 属大纲域，其产物名出自已定格的设计真源；反例：场景 2-5 所属 write/volume/style 域无真源，故用类别词）。路径细节随设计演进，行为级断言稳（三轮冲突立例）；真源变更时**同批更新断言**，不得只改断言（宪法 §1.5）；占位域/待启用域类断言引用头注状态分组，不硬编码域清单。」

**依据（P1-1 的判读）**：本批场景 1 写入 5 个具名产物，与原 `:7` 的无条件禁止**字面冲突**，且与 §四 A6（要求 `基本规格.md` 命中 ≥1）**互斥**——故让步方必是 `:7` 而非场景 1 具名。另两条支撑：该条出身（034 立法原话）是「行为级**优先**」＋限占位域，现文写成绝对句属措辞越界；场景 6/8（当前可执行）本就大量具名，`:7` 按字面早被自家违反。

### 2. `docs/standards/file-conventions.md`：规格行下沉为表注 [A]

**2a. `:26`「规格」行**（替换范围：**整行**；成稿审查 P1-1 采纳——改为成对逐字，防「只留…」式枚举漏内容）：

- 现文（实测全文）：「| 规格 | `{序号NNN}-{YYYY-MM-DD}-{中文主题}.md`，序号按立项顺序三位递增、永不复用；主题用中文（作者面向的治理记录）；**同批附件**（随主规格产出的并列文档）沿用主规格序号、以语义后缀区分（如 `030-…-mo-shu架构评估报告.md`），不另编序号 | `013-2026-09-06-多技能骨架切换.md` | `docs/specs/`；**实施验收全部通过后移动至** `docs/specs/archive/`（移动不留副本）；**作废批**（规格前提失效未施工）同移入 archive，文件名追加 `-已作废` 后缀并在正文头注标废（正例：`archive/046-2026-09-11-机制闸先行批-已作废.md`） |」
- 替换为：「| 规格 | `{序号NNN}-{YYYY-MM-DD}-{中文主题}.md`，序号按立项顺序三位递增、永不复用；主题用中文（作者面向的治理记录） | `013-2026-09-06-多技能骨架切换.md` | `docs/specs/`；**实施验收全部通过后移动至** `docs/specs/archive/`（移动不留副本） |」

**两处必须保留、不得随下沉丢失**（P1-1 的两处漏项）：命名格末的「**（作者面向的治理记录）**」括注；位置格首段的在制存放位「**`docs/specs/`**」。

**2b. 新增表注**（插入位置：命名总表之后、紧邻的表注区，与该表其它表注同形）：

- 表注文本（**逐字**；首句固定标记供断言锚定，见 §四 **A9**——该处原稿误指 A7，成稿审查 P1-5 采纳；又：本规格内「A9」另指**台账**条目 `A9 test-prompts 场景 1 重写`，引用时须冠「台账」二字以别）：

> 表注（规格行的扩展口径）：**同批附件**（随主规格产出的并列文档）沿用主规格序号、以语义后缀区分（如 `030-…-mo-shu架构评估报告.md`），不另编序号；**作废批**（规格前提失效未施工）同移入 `docs/specs/archive/`，文件名追加 `-已作废` 后缀并在正文头注标废（正例：`archive/046-2026-09-11-机制闸先行批-已作废.md`）。

- 插入位置：紧随现有表注 `:39`「表注（扩展名口径）…」之后（同形同区，不新建区块）。**落盘形（施工方 P1-b 采纳，核验修正）**：两条表注须以**块内 `>` 分段**相接（连续引用块），**不得空行隔断**——空行会把两块分开并触 markdownlint MD028（blank line inside blockquote），施工首跑即因此致 `check.sh` 退出 1；表注文本本身逐字未改。
- 两条规则的**文字逐字保留**（仅换位置，不重写）——N2 的诉求是「同列速查可比性」，不是删信息。

**2c. `:3` 版本行**：`v0.17` → `v0.18`（实质内容修订 minor）。

### 3. 过程产物（规划方职务，非施工方项）

- `CHANGELOG.md` `### Changed` 顶部新增本批条目（≤ 400 字符）。
- `docs/specs/collab-log.md` 施工记录表末行追加一行（序号顺延；**不写提交状态**，承 053 批新例）。
- 本规格补「审查记录」节；归档至 `docs/specs/archive/`。
- `docs/specs/open-items.md`：① **G5 销账**（实测已归零，依据 §一.3；销账依据记「050 批实际完成，本批核实」）；② **新登一项**：`skill-writing.md:47/:48` 弹窗口径矛盾（禁用 UI 词汇 vs 强制标注「（弹窗：…）」；实况 4 处用「（弹窗：…）」形态）→ **批 3**（口径矛盾清零）；③ A9 销账（本批处置）。

## 三、禁止事项

- **禁止改动场景 6／7／8**（当前可执行三场景，与本次无关）。
- **禁止在 test-prompts 中引入设计文档未定义的产物名**（§二.1a 末句为该禁的具体化）；禁止新造 write／volume／style 域的产物名。
- **禁止改动 `docs/product/`**（设计文档只被引用、不改；其真源地位不属本批）。
- **禁止在 `file-conventions.md` 内改动规格表以外的既有行**——**例外两处**：§二.2b 的新增表注与 §二.2c 的 `:3` 版本行（成稿审查 P1-2 采纳：原措辞与「新增表注」互斥，字面执行会漏掉 A9–A11 的唯一交付物）。两条下沉规则的**文字逐字保留**。
- 禁止改动 `skill-writing.md`（弹窗口径矛盾属批 3）。
- 禁止 git 写操作；禁止联网。

**偏差分级**：P0 阻塞型（规格与现状冲突，继续会做错）→ 立即停、输出偏差报告；P1 观察型 → 继续施工、记入核验请求。

## 四、验收标准

**断言口径声明**：命令一律取固定串计数形态（`-F -c`，数命中行）。**零命中时退出码 1 且无输出＝通过**；到位列的 `●` 表示**哨兵型正向计数**（要求非零命中，与归零型相反——该记号承 archive/049／052 用例）。「现状」列为施工前实测值，「到位」列为施工后期望值。

```bash
git grep -F -c -- "project.md" docs/test-prompts.md
git grep -F -c -- "文风/文风指南.md" docs/test-prompts.md
git grep -F -c -- "大纲/总纲.md" docs/test-prompts.md
git grep -F -c -- "设定/设定集.md" docs/test-prompts.md
git grep -F -c -- "追踪/前情提要.md" docs/test-prompts.md
git grep -F -c -- "基本规格.md" docs/test-prompts.md
git grep -F -c -- "创作进度.md" docs/test-prompts.md
git grep -F -c -- "待新版《大纲阶段设计文档》定稿后重写" docs/test-prompts.md
git grep -F -c -- "表注（规格行的扩展口径）" docs/standards/file-conventions.md
git grep -F -c -- "同批附件" docs/standards/file-conventions.md
git grep -F -c -- "作废批" docs/standards/file-conventions.md
git grep -F -c -- "v0.9（沿革见" docs/test-prompts.md
git grep -F -c -- "v0.18（沿革见" docs/standards/file-conventions.md
git grep -F -c -- "产物名只在设计真源已定格时写入" docs/test-prompts.md
git grep -F -c -- "断言写行为不写资产路径细节" docs/test-prompts.md
```

| # | 断言 | 现状 | 到位 |
| --- | --- | --- | --- |
| A1 | `project.md` 归零（旧产物名） | 4 | 0 |
| A2 | `文风/文风指南.md` 归零 | 1 | 0 |
| A3 | `大纲/总纲.md` 归零 | 1 | 0 |
| A4 | `设定/设定集.md` 归零 | 1 | 0 |
| A5 | `追踪/前情提要.md` 归零 | 1 | 0 |
| A6 | 新设计产物名 `基本规格.md` 到位 | 0 | ●≥1 |
| A7 | `创作进度.md` **保留**（防误删哨兵） | 2 | ●≥2 |
| A8 | 场景 1 的免责注归零（重写完成） | 1 | 0 |
| A9 | 规格行表注标记到位 | 0 | 1 |
| A10 | 「同批附件」未丢（哨兵） | 1 | ●≥1 |
| A11 | 「作废批」未丢（哨兵） | 1 | ●≥1 |
| A12 | `test-prompts.md` 版本行 v0.9 | 0 | 1 |
| A13 | `file-conventions.md` 版本行 v0.18 | 0 | 1 |
| A17 | `:7` 新纪律串到位（**核验修正新增**） | 0 | 1 |
| A18 | `:7` 旧绝对句归零 | 1 | 0 |

- A14 语义复核（手工）：场景 1 新节逐条读通，且**节内每个产物名都能在设计文档中找到出处**（施工时逐名核对并记录出处行）；场景 2–5 的改动处读通，`创作进度.md` 的引用未被误改。
- A15 机检：一键检查脚本（`scripts/check.sh`）退出 0（`[2]` lint 须覆盖本批两个 `.md`）。
- A16 改动面：**基线＝`ba5350d` 之后**；执行时点＝本规格归档**之前**。该基线与时点下改动面仅 2 个内容文件（`docs/test-prompts.md`、`docs/standards/file-conventions.md`）＋ 三件过程产物（`CHANGELOG.md`、`docs/specs/collab-log.md`、`docs/specs/open-items.md`）＋ 本规格自身（归档移动不计入）。

## 五、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：§一 全部锚点现场勘验——`test-prompts.md` 全文 read（151 行）＋死名逐 token `grep -n` 实测行号；`大纲阶段设计文档.md` 实测 785 行，其 `§1.0`／`§1.1`／`§1.2`／`§5.3` 四节由 read 取得命名与门控事实，章节结构由 `grep -n "^#\{1,3\} "` 实测；`file-conventions.md` 的 `:3`／`:26` 由 read＋`grep -n` 核对实为 133 行；G5 归零、glossary 存在性、`release:73` 三处「旧笔记作废」均由 `git grep -F -c` 与 read 实测（非沿用记忆——本轮勘验推翻了我此前三条笔记中的三条）。
2. **清单完备**：两类改动逐项落位（test-prompts 四处：场景 1 重写／场景 2–5 去死名／状态分组行／版本行；file-conventions 三处：规格行收窄／新增表注／版本行）+ 过程产物五项（含 G5 销账、新登矛盾项、A9 销账）。
3. **验收可判定**：A1–A13 为 13 条命令块断言（现状值已逐条实跑复现：4／1／1／1／1／0／2／1／0／1／1／0／0）；A7／A10／A11 为**防误删哨兵**（正向保留，非归零）；A14 为语义复核且写明「逐名核对出处」的可复现做法；A15 为脚本退出码；A16 为改动面对照。**防自噬自查**：归零型 token 为 A1–A5／A8——本规格 §一 的「现状引文」含 `project.md`／`大纲/总纲.md`／`设定/设定集.md`／`追踪/前情提要.md`／「待新版…重写」诸 token，而 §二 的替换说明亦提及部分（如 A1 的换名说明含 `project.md`）→ 工具按加法预测会高估（台账 N4 族）；本规格**不引用 `file-conventions` 的完整规格行原文**，故 A9 的替换侧为**新增串**、A10／A11 为**同串迁移**（计数不变、非同值归零），三项均已人工核对到位期望成立。
4. **矛盾检查**：与 AGENTS §3.1／§3.9（无真源的重写属猜测，故场景 2–5 只做最小处置）／§5 反模式 #2（去死名须扫全，不只改原句）一致；与文件自定纪律 `test-prompts.md:7`「断言写行为不写资产路径细节」一致（本批正是把违反该纪律的死名清掉）；与 §6 不做清单无涉。
5. **术语**：「去死名」「防误删哨兵」「表注」在首现处给出定义或例示；设计文档产物名一律照其原文书写。
6. **节号复核**：本规格引用的设计文档 §1.0／§1.1／§1.2／§5.3 已现场 grep 核对存在；`file-conventions.md` §五.2（bump 语义）、`AGENTS.md` §3.1／§3.9／§5 同。
7. **涟漪清单**：① 场景 1 重写后须 `grep` 全文件确认无残留旧名（含注释与前置条件行）；② `file-conventions` 规格行的两条规则下沉后，须确认**全仓无第二处**引用该行位置（如「见规格行」式指认）；③ 版本行 bump 后确认无第三处引用 `file-conventions v0.17`／`test-prompts v0.8`；④ 场景 2–5 去死名时**不得**改动场景 6–8 任何字符（§三 硬边界）。

## 六、开放项

1. **场景 2–5 的全面重写**：待 write／volume／style 域的设计真源出现时同批进行（本批只去死名与降级，不猜测新命名）。已登台账（在 A9 销账行内注明「最小处置，全面重写待各域设计」）。
2. **同一代际的其它评估基准**：`test-prompts.md:10` 的「每域最低测试要求」含「真机走查挂账不得超过下一个域建成」——存量两笔（029 拆书真测、031 场景 1）属 G11，本批不动。
3. **`skill-writing.md:47/:48` 弹窗口径矛盾**：本批只登记（§二.3），批 3 处置；处置方向二选一待那时定——（a）改标注词汇（如「（选项列表）」）并同步 4 处 skill 实况；（b）给 `:47` 的禁用加限定（语义描述禁 UI 词汇、标注标签除外）。

## 七、审查记录

**规格成稿审查（独立审查分身，2026-09-12）**：findings **12（P0:0 P1:5 P2:7）**，结论「修订后通过」。审查独立复跑了 13 条命令断言的现状值（与规格逐条吻合：4／1／1／1／1／0／2／1／0／1／1／0／0）、动态预验（13/13）、`--static`（14 条预警全部非阻断）、`bash scripts/check.sh`（退出 0）、双口径行数、全仓涟漪 grep；read 了 `test-prompts.md`／`file-conventions.md`／`design doc` 全文与 `archive/030 §九`（旧书目录树）作对表；并做了一次独立的死名 sweep。**本批最大风险点（§二.1a 硬要素与 §二.1b 换名映射是否与设计文档相符）经逐条核对未成立**——五项硬要素与 `§1.0/§1.1/§1.2/§5.3` 逐条相符。逐条处置——

- **P1-1（已采纳）**：§二.2a 原为「只留…」式枚举，实测漏两处既有内容（命名格末「（作者面向的治理记录）」括注、位置格首段在制存放位 `docs/specs/`）→ 改为**成对逐字**（现文／替换为整行），并单列「两处必须保留」句。
- **P1-2（已采纳·条款互斥）**：§三「禁止改动规格表以外的行」与 §二.2b「新增表注」字面互斥（表注＝A9–A11 三断言的唯一载体，照 §三 执行会漏交付物）→ §三 改为「禁止改动规格表以外的**既有**行」并列两处例外。
- **P1-3（已采纳·锚点订正）**：`test-prompts.md` 实 **151** 行（规格原写 152）、`file-conventions.md` 实 **133** 行（原写 134）——工具 `--static` 亦直接报出这两条基线漂移；已四处订正（两处节标题 + §五.1 两处）。
- **P1-4（已采纳·范围标签不符）**：`文风/文风指南.md`（`:27`）与 `大纲/总纲.md`／`设定/设定集.md`（`:28`）**实为场景 1 内的 token**，不在「场景 2–5」；若按标题理解会致 A2–A4 归零无所依托（[B] 重写若沿用旧文即失败返工）→ §二.1b 补**覆盖声明**（该三处由 §二.1a 整节重写一并清除，列名只为归零指向明确）。
- **P1-5（已采纳·交叉引用指错）**：§二.2b 原写「见 §四 A7」（A7 实为 `创作进度.md` 防误删哨兵）→ 改指 **A9**；并补「本规格内『A9』另指**台账**条目，引用须冠『台账』二字」的消歧。
- **P2 七条全采纳**：① 口径声明原写「正则匹配」与实况（13 条全为固定串 `-F -c`）不符 → 对齐 052 措辞；② `●` 记号无定义 → 补「哨兵型正向计数（要求非零命中）」并注明承 049／052 用例（审查已实测**现工具不判到位值**，A10／A11 因「到位＝现状」会固定输出非阻断预警——即台账 N8 家族）；③ §五.3 原写「A9–A11 的替换侧为新增串」不确 → 改「A9 新增串、A10／A11 同串迁移」；④ §一.2 原写「四条规则」→ 实为同一行两格塞**六条**；⑤ §二.1c 原写「已去死名并降为行为级」过宽（`:41`／`:58`／`:74` 尚有「未写路径但指向具体文件」的边界措辞）→ 加限定并明示「保持不动，待各域设计出现时随全面重写收口」；⑥ `:49`／`:61` 的「进度」指向未明 → §二.1b 明示「进度→具名 `创作进度.md`，保留不动」（与 A7 哨兵同旨）；⑦ `:34` 的「＋基础设定」为悬空措辞（术语表无此条、设计文档无对应目录）→ 降为行为级「＋基础设定（世界观底盘初稿）」，另把示例替换词「追踪类文件」改为「状态/追踪类产物」（旧 `追踪/` 目录已随 036 退役）。
- **审查的独立 sweep 结论（③a）**：把 `test-prompts.md` 通读后逐个候选判定——`细纲`／`卷纲`／`手稿`／`人物卡` 均**非死名**（前者与 `手稿` 是术语表在役条目，`人物卡` 与设计文档 §5.3 同名存续），旧设计的路径形态（`卷纲/卷N.md`、`细纲/卷N.md`、`手稿/卷N/第M章.md`）**在 test-prompts 内未出现**；场景 8 的平台/域目录名属 scan／analyze 自有真源（§三 亦禁改）。**死名清单与新旧两套树对表后闭合，未发现漏项**。另备查一项（本批不动）：场景 5 的伏笔状态用词「已埋设未回收／已回收」（`:81`）与设计文档 B-4 枚举「已埋设/已排位/已回收」不一致——场景 5 属 write 域、无设计真源。

**产物审查（产物审查分身，2026-09-12）**：findings **10（P0:0 P1:3 P2:7）**，结论「修订后通过」。审查只审 ③b 内容质量与 ⑥ 规范健康度（过程产物属规划方职务，明确排除在对象外）；独立实跑了 17 项 token 计数与全文 diff 通读、`git grep` 全仓涟漪、行长度与 lone `>` 扫描，read 了 `AGENTS.md`／本规格／两目标文件全文／设计文档相关节（写法纪律 1–4、§1.0–§1.2、§5.3、§5.7）／glossary 相关行／两个机检脚本。逐条处置——

- **P1-1（已采纳·本批最需裁定处）**：`test-prompts.md:7`「**断言写行为不写资产路径细节**」是无条件的绝对句，与本批场景 1 新节的 5 个具名产物**字面冲突**；且同批把场景 2–5 的具名降为类别词，**一升一降并列而文本内无判据**。审查给出的判据是「点名不点名不是关键，**设计真源是否定格**才是」——场景 1 属大纲域（真源 v0.9.6 已定格、§5.3 逐字给名），场景 2–5 属无真源域（猜名才违规）。**决定性证据**：§四 A6 要求 `基本规格.md` 在 test-prompts 内命中 ≥1，若按 `:7` 字面撤具名则 A6 无载体——**规格验收与 `:7` 文面互斥**，故让步方必是 `:7`。两条支撑：该条 034 立法原话是「行为级**优先**」＋限占位域，现文写成绝对句属措辞越界；场景 6/8 本就大量具名，`:7` 按字面早被自家违反。处置：新增 §二.1e（`:7` 整行替换，加限定＋正反例＋「真源变更时同批更新断言」），并补 A17／A18 两条断言（新串到位、旧绝对句归零）。
- **P1-2（已采纳·不可唯一判定）**：断言③原写「…为空骨架**或无**」，是析取——「或无」把「骨架压根没建」也判通过，**恰好吞掉设计 §1.0⑤ 唯一的违反形态**（该节要求目录与空模板齐全）。→ 去「或无」，改为「**存在但为空模板**即合规；该件缺失即偏离设计 §1.0⑤」，并给 `{书名}` 加「（书名处为实际书名）」。
- **P1-3（已采纳·引用断裂）**：本批删掉 `:16` 免责注（本文件唯一点名《大纲阶段设计文档》之处）后，三条 [人工] 断言仅凭裸节号 `1.0`／`1.1`／`1.2` 与未定义的「准入律」「三段式」指向一份**未点名**的文档——**与本批自定的判据同型**（§二.1c 刚论证过「落盘悬空节号引用」不可接受）。→ 场景 1 节内新增「**判定依据**：《大纲阶段设计文档》§1.0／§1.1／§1.2（设计真源 v0.9.6）」一行。
- **P2 七条处置**：① `:60` 具名件与类别词混排致 [机检] 落点虚 → 拆为「`创作进度.md` 机检 + 另三项人工核对」；② `:8` 补句使该行四事并载且同句 `1-5`／`2–5` 连字符与半角/全角斜杠混用 → 拆为两行头注并统一半角；③ 引例 `` `第3章 对应文件` `` 与实文形态不符（代码跨度只包 `第3章`）→ 照实写 `` `第3章` 对应文件 ``；④ **glossary 三条追踪类词条（前情提要／伏笔账本／人物状态快照）因本批改动成为零引用孤儿** → **登台账**（本批不改 glossary：A16 改动面锁定 2 文件）；⑤ `file-conventions:26` 无回指表注的内联短语、表注标签与既有「扩展名口径」不同形 → **不采纳**（表注标签本身已具名指向「规格行」，且标签文本系作者确认的 `[A]` 逐字项；`:20` 的内联先例属另一种情形——扩展名口径表注横跨多行、需行内分叉解释）；⑥ 断言①②的可验收性（①的 [机检] 判据实为前置条件、②两判据复合且时序未写明）→ ②已拆明并补「1.0 之后、门控确认之前即时核对」的时序，①按该文件既有惯例保留（场景 6/8 同型）；⑦ 「规格门**停靠**」把设计文档刻意区分的**门控**（阶段级硬停）与**停靠**（模块级条件停）叠用 → 改「`1.2` 规格门（门控）停等作者确认」。

**交付点核验（规划方，2026-09-12）**：三步独立重跑——改动面精确为 2 个内容文件（`git status --porcelain` 核对；场景 6–8 的 diff 止于 `@@ -81`，尾段逐字未动，**由规划方自行核而非采信施工方声明**）；15 条命令断言在最终工作树全达标（13 条原断言 + A17／A18 两条核验修正新增；归零型零命中、到位值全符）；`[A]` 项（`:26` 整行、表注文本、版本行）逐字比对一致；`账本` 短称两处已由核验修正补刀（现 `grep` 归零）；`check.sh` 退出 0（`[2]` lint 96 文件 0 违例——**含本批新增的 lone `>` 分段**，`file-conventions:40` 系全仓首例，该点已在核验时实跑确认）；`[4]` 仅 2 条基线漂移（本批净行数变化，非事实错误）；内容轨全绿。
