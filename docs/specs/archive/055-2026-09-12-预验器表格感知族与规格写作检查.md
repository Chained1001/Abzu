# 055 · 预验器表格感知族 + 规格写作机械检查（批 3a）

> **状态**：待作者确认（AGENTS §3.1 判定表 ✅：脚本改造 + 跨文件 + 修改既有行为）
> **依据**：台账 N8／N11／N19／N21（表格感知族）、N4（并入）；批 3 拆分建议（3a＝工具族／3b＝协议口径清零＋规范立法）经作者确认后本批先做 3a
> **基线**：`367d37c` 之后；**自由度标记**：`[A]` 逐字执行 ｜ `[B]` 按方向自定实现 ｜ `[C]` 按验收标准核验
> **待裁定项**：§二.5（四条规格写作机械检查）是否本批采纳——**本规格按「采纳」写入，若作者否决则 §二.1e 整节删除、断言 A4／A6 同步删**

## 一、现状锚点（2026-09-12 现场核验；行号为施工前时点实测值）

目标文件仅一个：`scripts/check_spec_assertions.py`（**530 行**）。其静态自检阶段现有两道检查（检查 A 自噬预警、检查 B 计数实跑重算），本批在其上做三修一增。

### 1. 四条挂账的成因（逐条落到代码）

| 编号 | 现象 | 成因落点（实测行号） |
| --- | --- | --- |
| **N19** | 把**行项编号**读成文件尺寸声称：「`MD034 行`」→「034 行」、「`G1 行`」→「1 行」，再错配给同段出现的文件路径（052／053 批各实测一例；**本规格自身即含一处活标本**——其 §二.3 的销账说明行已由工具如实报出，形态是「行项编号」紧邻量词「行」） | `CLAIM = re.compile(r'(\d[\d,]*)\s*(字符\|行)')`（**`:59`**）**无左界约束**——数字前紧邻字母时仍匹配 |
| **N8** | 对「到位＝现状」的**哨兵型**断言固定输出 `· 自噬预警（无命中）: token @ target —— 现行 1｜替换文本内 0`，稀释信噪比（053／054 批各 4–7 条） | `check_swallow()`（**`:253–:279`**）：`n == 0` 分支（**`:273–:275`**）不区分「本就该保留的 token」与「[A] 项替换文本漏写」 |
| **N21** | 把**验收标准节散文**里的行内命令名当断言抽取执行——053 批实测产出 `[1] 行内 exit=128: git grep -c / fatal: no pattern given`（该次以改措辞绕开） | `extract()`（**`:67–:89`**）对整节（`SECTION`＝`^##[^#\n]*验收标准.*$`，**`:46`**）扫**所有**行内代码跨度（**`:77–:81`**），不区分「断言」与「散文提及」 |
| **N11** | `[A]` 级「现文／替换为」**无成对逐字核对**——工具只做**加法预测**（`cur + n`，**`:277–:278`**，不计删除） | `check_swallow()` 用 `_subsections()` 做**节级**配对（`:263`），不做「现文/替换为」**成对**解析；故 052 批「规格侧未同步」与 054 批「枚举式描述漏内容」两起事故均无机器拦阻 |
| N4 | 加法预测不计删除（已知高估） | 同上一行；**本批由检查 C 收窄后并入 N11 处置** |

### 2. 现有判据的既有豁免与边界（本批不得回退）

- `LIMIT_MARKS`（**`:61–:62`**）的**限额型数字豁免**（「条目 ≤ 400 字符」不算尺寸声称）——049 批立，**保留**。
- `_segments()`（**`:282–:295`**）的**紧邻段配对**（计数声称只在其所在紧邻段内识别）——050 批立，**保留**；本批只补左界约束，不改配对模型。
- 检查 A／B 的**非阻断**语义（预警与基线漂移不影响退出码）——**保留**；新增检查 C／D 亦为非阻断（**理由**：规格层发现的最终裁决权在作者，机器只呈报；阻断只留给「计数不符」这类确定性违约）。
- `static_report()`（**`:333–:362`**）的末尾三态（`· 有预警 N 条`／`· 全部跳过`／`· 无发现`）——**保留**；N8 修完后「有预警」条的计数会随之下降，属预期。

### 3. 规格写作侧的四类机械可detect缺陷（检查 D 的目标，全部来自本会话实测）

| 类 | 现象 | 实证（**出处类型已标注**——成稿审查 P2-5 采纳：原稿的处数引证在仓内不可复核；另**审查该项判读有一处误判**：其称「055 自身踩 MD038 四处不成立、那 4 处是合法的双反引号补齐」——该 4 处系本会话实测的 lint 输出（行号 `:34`／`:35`／`:55`，即 ②③ 两类的字面示例），已由规划方当批改措辞消除，故审查在改后时点复跑得 0 issues 属预期，非「前提不成立」） |
| --- | --- | --- |
| ① 嵌套反引号 | "替换为" 侧文本用外层反引号包裹、内层又含反引号 → 落盘即 MD038 | **本会话观察**（仓内不可复核——均在落盘前当批改掉，故归档件零违例）：052／053／054 批各 1–3 次、055 批本规格自身 3 处 |
| ② 代码跨度边缘空格 | 行内代码的**首字符或尾字符为空格**（两种形态均落盘即 MD038） | **本会话观察**（同上）：053 批 1 次、055 批本规格自身 1 处 |
| ③ 加粗引导行紧跟列表 | `**小标题**：` 行之后直接接列表行（`-`／`*`／数字序 起首）→ MD032 成片 | **仓内可复核**：`archive/019`（MD032 共 7 错）、`archive/042`（P0-F1：MD031／MD032，实测 `check.sh` exit 1）；另本会话观察 050 批 17 处、052 批 2–3 处 |
| ④ 规格内可解析相对链接 | 规格写在 `docs/specs/`、链接按**落盘目录**解析 → 归档后必断链 | **本会话观察**（同上）：050／051 批各 1 次，致 `内容轨` 报断链、`check.sh` 红 |

## 二、文件级改动清单

### 1. `scripts/check_spec_assertions.py`（本批主体）[B]

**1a. N19 修（左界约束）**：`CLAIM`（`:59`）加左界否定环视——数字前紧邻 `[A-Za-z0-9]` 者不视为尺寸声称（`MD034 行`／`G1 行` 因此出局；`实测 116 行`／`（133 行` 仍匹配）。`LIMIT_MARKS` 豁免与紧邻段配对**不动**。

**1b. N8 修（哨兵型静默）**：`check_swallow()` 的 `n == 0` 分支（`:273–:275`）按 `cur` **分流**——`cur > 0` 者**不再入预警列表**，改以一行**中性**汇总（`· 现行有值／替换侧未提及: N 个（请对照到位列核对——可能是哨兵型保留，也可能是归零型已达成）`）；`cur == 0` 者**亦改中性逐条措辞**（`· 替换侧未提及且现行为 0: <token> @ <target>（可能是 [A] 项替换文本漏写，也可能是归零型已达成）`）并计入末尾三态汇总计数——**本支原稿写「保留原预警」，经产物审查 P1-3 证伪**（工具自己的判据就承认 `cur==0｜n==0` 非必为漏写：053 的「四分类」「不得据以仿写」即「token 被合法删除、到位 0 已达成」型，属假警报）。**为何措辞必须中性（成稿审查 P1-6 采纳）**：`(cur, n)` 二元**无法区分**「哨兵型保留」与「归零型删除」——两者同为 `cur>0｜n=0`；同理 `cur==0｜n==0` 亦非必为漏写（053 的「四分类」「不得据以仿写」即属「到位＝0 已达成」的归零型）。工具不解析到位列，故**不锁定单义**。

**1c. N21 修（行内抽取收窄）**：`extract()`（`:77–:81`）的**行内**抽取加判据——跨度须为该行**剥去列表标记／复选框后的起首内容**（即该行以该代码跨度开头；散文句中提及的命令名不再抽取）。**后果声明（成稿审查 P0-2 采纳，数字为实测）**：复刻 `extract()+unauditable()+readonly()` 于在制与归档规格全体测得——行内抽取共 **144** 条，其中 **71** 条当前进入**执行桶**（原稿写「此前处置也只是 SKIP」**有误**：仅 72 条为 SKIP，另 71 条在跑，如 `archive/016`、`archive/041`／`042` 的成排 `grep` 断言）；按新判据存活 **84** 条（含 36 条在跑）、丢弃 60 条散文提及（含 N21 的活标本 `archive/018:96` 的 `grep -c` 一行）。**故本项对动态阶段是实质变更**（非「只动静态阶段」），§三 的边界句已按此改写。围栏抽取（`:82–:88`）**不动**。

**1d. N11 增：检查 C「`[A]` 成对逐字核对」**（本批最高价值项）：

- **解析**：在「文件级改动清单」节（`CHANGE_SECTION`，`:49`）内识别**成对**条目——`- 现文：…` 与 `- 替换为：…`（亦含 `- 现文（…）：…`／`- 替换为（…）：…` 变体，与 `**Na. …**` 小标题下的多行形态）；取「替换为」侧的文本，剥去外层包裹（`「」`／`` ` ``／`` `` ``）与表格管道，得**逐字核验串**。
- **核对**：该串须在其小标题声明的目标文件（取该节内最近的 `` `<path>` `` 路径 token）中**逐字命中**；未命中即报 `· [A] 未落实或已漂移: …`（**非阻断**，列入输出）。**前缀用 `·` 而非 `x`**——本文件的 `x` 前缀（后接半角空格）的既有语义即阻断（`:328` 的 `x 计数不符` 同时置 `bad=True`），用它会与「阻断项只允许计数不符」自相矛盾（成稿审查 P2-2 采纳；A3 的 token 不含前缀，不受影响）。
- **多行串**：按行切片，逐行要求命中（避免整段因一处空格差异而误判）；单行过短（< 8 字符）者跳过（噪声防护）。
- **载体分布缺口（成稿审查 P1-8 采纳）**：实测归档件内含「替换为」的行 82 条，其中 `- 替换为` **bullet 成对形态仅 23 条**（同行兼含「现文」者 15 条），其余为「整行替换为：」「整节替换为：」「…→ 替换为『…』」等**内联载体**。本项**只覆盖 bullet 成对与 `**Na.**` 多行形态**，内联载体不覆盖——销 N11 时须注明该残留（补覆盖属另批）。
- **运行时机（成稿审查 P2-7 判读）**：检查 C 报的是「[A] 替换文本是否已落盘」，故在**施工前**的 `check.sh [4]` 预验阶段**必然**整片报「未落实」——**这是设计使然**（该阶段本就尚未施工），不是缺陷；其真正生效时点是**施工后的核验**。非阻断若不配「谁在何时读它」，等于无人读。
- **与检查 A 的分工**：检查 A 保留「可能自噬」的强判据（替换侧含须归零 token），检查 C 承担「逐字落实」；**N4 的加法高估随之收窄**（检查 A 的预测行改为仅在「替换侧命中数 > 0 且现行 > 0」时输出）。

**1e. 检查 D（四条规格写作检查）**——**待裁定项**（§六.1）：在**规格文本**上静态检出上述四类，逐类一行为预警（`· 规格写作预警（<类名>）: <行号> <摘录>`）：① 嵌套反引号（`「`…`` ` ``…`」` 内层反引号不配平）；② 代码跨度边缘空格（`INLINE_CODE` 的捕获组首尾为空格）；③ 加粗引导行紧跟列表（`**…**：` 结尾行 + 下一行以列表标记起首，即 `-`／`*`／数字序）。**实现约束（成稿审查 P2-6 采纳）**：①② 两类须**先按 CommonMark 规则剥一层反引号补齐、并跳过代码跨度内的文本**再判——否则本规格自身 `:51` 的 `` `` `<path>` `` `` 与 `:55` 的跨度内示例即会被朴素正则误报。；④ 规格内可解析相对链接（`[..](path)` 且 `path` 按**规格所在目录**解析存在、而按**归档目录**不存在）。

**1f. 头注与维护入口同步**：文件头 docstring（实为 `:1–:32`，其中「维护入口」段在 `:24–:31`——成稿审查 P2-1 采纳，原稿 `:1–:10` 失准）须补检查 C／D 的落点（加在 `:26–:28` 邻域）；`static_report()`（`:333–:362`）末尾三态判据随之补齐：**须显式纳入 1b 的中性汇总行**（否则某规格的条目全为此型时 `warn` 为空 → 落 `· 无发现`，**重建 049 批收紧判据要防的假绿通道**；成稿审查 P1-7 采纳）；检查 C／D 两类输出亦不得被 `· 无发现` 掩盖。

### 2. `docs/specs/collab-log.md`：静态契约条同步 [A]

**2a.** collab-log「断言预验强制」条（该条列静态自检的既有检查项）补一句检查 C／D 的存在与**非阻断**语义（**落盘文本须用全角斜杠**——A9 的 token 与之逐字对应；本条系成稿审查 P1-1 采纳，且施工方 055 批实测确认：断言侧写半角即成「断言吞替换文本」型假红，违 `collab-log` 的「断言字面比对」条），使新检查有协议落点（**仅此一句**，不改该条其它内容、不动协议其它节）。

**2c. `AGENTS.md:14` 的 `[4]` 段括注补全**（`[A]`；**由产物审查 P2-7 采纳追加，不属初稿范围**）：该行现写「`[4]` 规格静态自检（在制规格的断言自噬预警与计数重算）」——只列检查 A／B，而本批已新增 C／D，属 052 批「段序载体必须同步」的同类遗漏。替换为「（在制规格的断言自噬预警、计数重算、[A] 项「替换为」侧文本逐字核对与规格写作机械检查）」——**只改该括注，该行其余文字与 AGENTS.md 其它内容逐字不动**。

### 3. 过程产物（规划方职务，非施工方项）

- `CHANGELOG.md` `### Changed` 顶部新增本批条目（≤ 400 字符）。
- `docs/specs/collab-log.md` 施工记录表末行追加一行（序号顺延；**不写提交状态**，承 053 批新例）。
- 本规格补「审查记录」节；归档至 `docs/specs/archive/`。
- `docs/specs/open-items.md`：**N8／N11／N19／N21 销账**（N4 并入 N11 行说明）。**N19 的销账须注明残留形态**（成稿审查 P1-5 采纳）：1a 的左界约束只消「字母/数字前缀」一类（`MD034 行`／`G1 行`／`N11 行`），**非编号来源的「N 行」计数仍在**（实测 053 归档件仍报 `collab-log 称「7 行」实测 275`，源为「全表 7 行（#32–#38）」——数字前为空格，环视不拦）；该残留需语义解析，不在本批。若 §二.1e 被采纳，另销「四条检查」相关的预备条目（若有）。

## 三、禁止事项

- **禁止改动检查 A／B 的既有豁免**：`LIMIT_MARKS` 限额豁免（`:61–:62`）与 `_segments()` 紧邻段配对（`:282–:295`）须逐字保留（049／050 批立例）。
- **禁止改动 `report()`／`verify_report()` 的函数体与三态判据**（`unauditable()`／`readonly()` 的放行面、三桶计数恒等式）——**注意 `extract()` 是动态阶段与静态阶段共用**（`report()` 在 `:157` 调它），本批 §二.1c 有意收窄其行内抽取，**属对动态阶段的实质变更**（实测执行条数会下降），§二.1c 已附变更前后条数对照；049 批三态化的三桶恒等式与五判据面**不动**。（**本条为成稿审查 P0-3 采纳**：原稿写「禁改动态阶段及其调用链」与 §二.1c 直接互斥——052 批的同类规格内部矛盾曾致施工停摆报偏差，先例见 `collab-log` 施工记录第 38 行。）
- **禁止把检查 C／D 做成阻断项**（非阻断语义见 §一.2）。
- **禁止引入外部依赖**（守卫零外部依赖，宪法 §6）；禁止联网。
- **禁止改动 `skills/` 下任何文件**与 `docs/standards/`（本批只动一个脚本 + collab-log 一句）。
- 禁止 git 写操作。
- **夹具纪律**：所有验证夹具放 `.tmp/`（红线 2），跑完即删；不得以夹具形式在 `docs/specs/` 留残留。

**偏差分级**：P0 阻塞型（规格与现状冲突，继续会做错）→ 立即停、输出偏差报告；P1 观察型 → 继续施工、记入核验请求。

## 四、验收标准

**断言口径声明**：命令一律取固定串计数形态（`-F -c`，数命中行）。**零命中时退出码 1 且无输出＝通过**；到位列的 `●` 表示**哨兵型正向计数**（要求非零命中）。

```bash
git grep -F -c -- "(?<![A-Za-z0-9])" scripts/check_spec_assertions.py
git grep -F -c -- "哨兵型 token" scripts/check_spec_assertions.py
git grep -F -c -- "[A] 未落实或已漂移" scripts/check_spec_assertions.py
git grep -F -c -- "规格写作预警" scripts/check_spec_assertions.py
git grep -F -c -- "检查 C" scripts/check_spec_assertions.py
git grep -F -c -- "检查 D" scripts/check_spec_assertions.py
git grep -F -c -- "LIMIT_MARKS" scripts/check_spec_assertions.py
git grep -F -c -- "_segments" scripts/check_spec_assertions.py
git grep -F -c -- "检查 C／D" docs/specs/collab-log.md
```

| # | 断言 | 现状 | 到位 |
| --- | --- | --- | --- |
| A1 | `CLAIM` 左界约束到位（N19） | 0 | ●≥1 |
| A2 | 哨兵型汇总行到位（N8） | 0 | ●≥1 |
| A3 | 检查 C 报错串到位（N11） | 0 | ●≥1 |
| A4 | 检查 D 预警串到位（§二.1e 采纳时） | 0 | ●≥1 |
| A5 | 头注／维护入口含「检查 C」说明 | 0 | ●≥1 |
| A6 | 头注／维护入口含「检查 D」说明（§二.1e 采纳时） | 0 | ●≥1 |
| A7 | 既有豁免未回退：`LIMIT_MARKS` 限额豁免（哨兵） | 2 | ●≥2 |
| A8 | 既有豁免未回退：`_segments` 紧邻段配对（哨兵） | 4 | ●≥4 |
| A9 | collab-log 静态契约条含检查 C／D | 0 | 1 |

- A10 **行为断言（手工，四支；夹具放 `.tmp/055/`，跑完即删——红线 2）**：① **N19**：夹具须按**乙种形态**造——`` `.markdownlint-cli2.jsonc` `` 在**前**、其后**紧邻段**（反引号之外）写「MD034 行」（**甲种形态现状本就不报**，因 `check_counts` 只在跨度之后的紧邻段上跑 `CLAIM`；成稿审查 P1-2 采纳）→ 复跑 `--static` **不得**报该文件的尺寸声称；**现状复现串**：旧正则下报 `x 计数不符: …jsonc 称「034 行」实测 19`（055 批施工实测：连同 `AGENTS.md 称「1 行」实测 147` 一并出局）；**另须正向对照**：真声称（如写「实测 20 行」而实为 19）必须**仍报**——防「一改就全放行」；② **N8**：对已归档 `053-…md` 复跑 `--static`，`自噬预警（无命中）` 行数**归零**（整改终态实测：0 行），另出**中性逐条行 2 行**（`四分类`／`不得据以仿写`，属「现行为 0 且替换侧未提及」型，措辞含两种可能而不锁定单义）与**中性汇总行 1 行**（`现行有值／替换侧未提及: 5 个`）——**三态末尾行须报「有预警 N 条」而非「无发现」**；**不得为过此断言而删改 §二.1b 判据**（红线 5／反模式 #1）；③ **N21**：**默认模式（不带 `--static`）**对活标本 `docs/specs/archive/018-2026-09-07-前作引用降级.md` 复跑——该件 `:96` 的行内散文提及现状被误抽为断言（055 批实测：`[1] 行内 exit=1: grep -c "mo-shu"`、`[3] 行内 exit=2: grep -c`）；改后**不得**再出现 `[N] 行内 …: grep -c` 行。**原稿写「对 053 复跑不得出现 `exit=128`」不可复现**（053 的散文提及是 `node …` 形态、本就落 SKIP；且 `--static` 根本不执行命令——成稿审查 P1-3 采纳）；④ **N11／检查 C**：造夹具规格，其「替换为」串与目标文件**故意不符** → 工具报 `[A] 未落实或已漂移`；把夹具改成与文件一致 → 不再报。⑤ **假阳性基线（成稿审查 P1-8 采纳）**：对已归档 `053-…md`／`054-…md` 复跑，检查 C **不得**对已落实的 `[A]` 项报错（否则该检查在已完工件上恒红，等于噪声）。**核验时的残留观察（规划方实测，如实记）**：对**归档的 052 规格**复跑，检查 C 报 1 条「· [A] 未落实或已漂移: `0` 成功 / `1` 违规 …（**四分类**…）@ docs/standards/file-conventions.md」 —— 这是**真阳性而非误报**：052 的替换文本当时已落盘，但该行后来被 **053 批合法改写**（四分类 → 五分类），故历史主张与当前实文不再一致。**影响面为零**（`check.sh` `[4]` 段只扫在制规格、不含 archive），但**若将来把扫描集扩展到 archive，须先处理此类「被后续批次取代」的历史噪声**。
- A11 **检查 D 行为断言（手工，仅 §二.1e 采纳时）**：夹具规格各含四类缺陷一处 → 四类预警各命中 1 行；把夹具修干净 → 零预警。**另须核**：对 `055` 本规格自身跑检查 D ⇒ **零预警**（该件的 `` `` `<path>` `` `` 与跨度内示例属合法形态，不得误报——承 P2-6 的实现约束）。
- A12 机检：`bash scripts/check.sh` 退出 0；**且 `[4]` 段对在制本规格跑 `--static` 时，新增检查不得对本规格自身产生阻断**（阻断项只允许「计数不符」）。
- A13 脚本语法：`node --check` 不适用（Python）——以 `python -m py_compile scripts/check_spec_assertions.py` 通过为准，**pycache 用后即删**（红线 2；该文件在 `.gitignore` 无规则，须手工清）。
- A14 改动面：**基线＝`367d37c` 之后**；执行时点＝本规格归档**之前**。该时点下改动面为 **3 个内容文件**（`scripts/check_spec_assertions.py`、`docs/specs/collab-log.md`、**`AGENTS.md`**——后者系 §二.2c 的产物审查 P2-7 采纳追加，非初稿范围）＋ 过程产物三项（`CHANGELOG.md`、`docs/specs/collab-log.md` 的施工记录行、`docs/specs/open-items.md`——**改称「三项」以免与 AGENTS §3.8 铁律④的「过程产物三件」（CHANGELOG／collab-log 登记／规格归档）同名异义**；成稿审查 P2-4 采纳）＋ 本规格自身（归档移动不计入）；**`skills/` 与 `docs/standards/` 零 diff**（另行 grep 核）。

## 五、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：全部锚点现场核验——目标文件 `wc -l` 实测 **530 行**；`CLAIM`／`LIMIT_MARKS`／`SECTION`／`CHANGE_SECTION`／`INLINE_CODE`／`FENCE_CODE` 六处正则由 read 取行号与原文（`:46`／`:49`／`:59`／`:61–:62`／`:63`／`:64`）；`extract()`／`check_swallow()`／`_segments()`／`check_counts()`／`static_report()` 五个函数的边界行号由 `grep -n "^def "` 实测并与 read 核对；三条既有豁免（限额豁免、紧邻段配对、非阻断语义）逐处 read 确认。四类规格写作缺陷的**本会话实证**取自各批规格的「审查记录」节与实际 lint 报错（非推测）。
2. **清单完备**：1 个脚本的六小项（三修一增一裁定项一头注同步）+ collab-log 一句 + 过程产物四项。§二.1f 把「头注与维护入口同步」写成独立子项，防 049 批「机制改了文档没跟」的同类漏项。
3. **验收可判定**：A1–A9 为 9 条命令块断言（现状值已逐条实跑复现：0／0／0／0／0／0／2／4／0——**成稿审查 P2-3 采纳**，原稿的数字与编号均失准）；A10 为五支行为断言且**每支的现状／期望都已实跑确认**（①②③三支的现状缺陷在 052／053／054 批的实际输出中有据，④为本批新增能力，⑤为假阳性基线）；A11 仅在被裁定采纳时适用；A12 为脚本退出码＋**自指无阻断**双判据；A13 补 Python 语法门（该脚本无 node --check 覆盖，此前只用过 `py_compile`）；A14 为改动面对照。**防自噬自查**：本规格 §一 的「成因落点」引用了 `CLAIM` 等正则原文与输出串（如 `· 自噬预警（无命中）`），而 §四 的归零型 token **为零**（全部为正向 ● 与 1）——故**无互斥**；A4／A6 在作者否决 §二.1e 时**整条删除**，不留下「到位无载体」的空断言（**A9 保留**——它依赖 §二.2a 而非 §二.1e；成稿审查 P1-4 采纳，原稿此处误写 A4／A9 与头注冲突）。否决时的连带收口：§二.1f 与 §二.2a 的「检查 C／D」一律改为「检查 C」。
4. **矛盾检查**：与 AGENTS §1.4（禁带病开工——本批基线绿）、§5 反模式 #6（退出码不经管道：A8 的四支命令均裸跑取码）、§6（守卫零外部依赖；不做注册表类守卫）一致；与 §7 体积纪律一致（脚本 530 行、本批预计净增 ~80 行，无上限约束）；与 `open-items` 的 N4／N8／N11／N19／N21 五行逐条对应。
5. **术语**：「表格感知族」「哨兵型 token」「成对逐字核对」「加法预测」「归零型／哨兵型」在首现处给出判据；`[A]`／`[B]` 沿用 collab-log 既有定义。
6. **节号复核**：本规格引用 `AGENTS.md` §1.4／§5／§6／§7、`collab-log` 的「断言预验强制」条与「核验协议」第一／二步，均已现场 grep 核对存在。
7. **涟漪清单**：① 检查 C／D 落地后须确认 `check.sh` `[4]` 段仍以 `--static` 调用（不执行规格内命令）；② `[4]` 段的在制规格扫描集与 `--static` 的默认集合需一致（否则新检查在无人跑 `--static` 时不生效）；③ 三态末尾行判据须覆盖新增两类预警（否则「无发现」会掩盖）；④ `py_compile` 的 `__pycache__` 须即删（红线 2），且不得因该目录未被忽略而在 `git status` 里留噪声。

## 六、开放项

1. **§二.1e 四条规格写作机械检查是否采纳（待作者裁定）**：我的建议是**采纳**——四条全部来自本会话的**实测踩坑**（054 批一次踩三处嵌套反引号），而它们的共同点恰好是「人能看出、机器零成本」；若作者判断规格写作类的检查会抬高送审成本，可只采纳 ①（嵌套反引号）与 ④（相对链接）两类（前者落盘即 lint 红、后者落盘即 `check.sh` 红，均有**确定性后果**），②③ 两类可缓。
2. **N12／N13（预验器文档与健壮性补遗／`--verify` 边界）**不在本批：前者触及动态与 `--verify` 两阶段（§三 已禁），后者属 `--verify` 自指噪声，二者与 3b 的协议口径条款同批更省。
3. **N22（用法三载体同步守卫）**不在本批：它是**新增一项 check.sh 检查**（跨 skill 资产），与「表格感知族」性质不同，归 3b 或独立小批。
4. **N6（脚本更名）**：现名 `check_spec_assertions.py` 已被多处协议条与归档规格引用，更名属独立评估，本批不动。
5. **检查 D 的阈值**：③（加粗引导行紧跟列表）按本批「`**…**：` 结尾行」判据在**现行全仓语料（在制＋归档）实测 0 命中**（055 批施工实测；另注 MD032 尚有「加粗引导行＋尾随说明文字」「普通段落＋列表行」两种触发形态，本批按规格**不覆盖**）。**原稿此处的「归档规格上有大量存量命中（050 批 17 处等）」在现行语料未复现**——系凭记忆写下、未现场核（成稿审查与施工方各报一次）；③ 类的实际触发面比规格设想的窄，若将来要扩展判据须先重测存量。

## 七、审查记录

**规格成稿审查（独立审查分身，2026-09-12）**：findings **18（P0:3 P1:8 P2:7）**，结论「修订后通过（3 项 P0 须先收口再产交底）」。审查独立实跑了：目标脚本 `wc -l`＝530、`grep -n "^def "` 全部函数边界、六处正则原文与行号、A1–A9 现状值（0／0／0／0／0／0／2／4／0）、全仓在制＋归档规格的行内抽取实测（**144 条／其中 71 条在执行桶**）、`check_counts` 新旧正则对照、A10① 三种夹具形态探针、`check.sh` `[4]` 段调用与扫描集、`.gitignore` 全读（**确无 `__pycache__` 规则**）、`npx --no-install markdownlint-cli2` 全仓（0 issues）、archive 证据抽样。逐条处置——

- **P0-1（已采纳）**：A10② 原写「`（无命中）` 行数应**归零**」——但按本规格 §二.1b 的判据，`cur=0` 的 2 条会保留原预警（053 实测 7 条中 2 条为「到位已达成」型），故**7 → 2、永不为 0**。该断言不可达，而施工方要过它只可能去改判据（红线 5／反模式 #1）→ 断言订正为「7 降至 2 ＋ 哨兵型汇总 1 行」，并明写「不得为过此断言而删改 §二.1b 判据」。
- **P0-2（已采纳·纠正事实错误）**：§二.1c 原判据（「行的实质内容即该代码跨度」）经审查实测会**把整条行内通道清零（0/144 存活）**；且原稿「此前它们的处置也只是 `SKIP`」**与事实相反**——144 条里 **71 条在执行桶**。→ 判据改为审查实测可用的「**行首式**」（跨度须为该行剥去列表标记后的起首内容；实测存活 84 条／丢弃 60 条），后果声明按实测数字重写。
- **P0-3（已采纳·条款互斥）**：§三「禁改动态阶段**及其调用链**」与 §二.1c（改 `extract()`，而 `report()` 在 `:157` 调它）**互斥**——与 052 批「规格内部矛盾致施工停摆」同类。→ 边界句重写为「禁改 `report()`／`verify_report()` 的函数体与三态判据面；`extract()` 两阶段共用、本批有意收窄属对动态阶段的实质变更」。
- **P1×8 全采纳**：① A9 的 token 与 §二.2a 落盘文本**字形不一致**（半角 vs 全角）＝「断言吞替换文本」家族——**本项目后在施工时如实暴露为 P0 并已修复，见下**；② A10① 夹具原写甲种形态（编号在前）**现状本就不报**，测不出缺陷 → 改乙种（路径在前、编号在其紧邻段反引号外）＋补正向对照；③ A10③ 原写「对 053 复跑不得出现 `exit=128`」**不可复现**（053 的散文提及是 `node …` 形态本就 SKIP，且 `--static` 不执行命令）→ 改用活标本 `archive/018:96` 的 `grep -c` 并明写「默认模式」；④ 待裁定否决路径自相矛盾（A4／A6 vs A4／A9）→ 统一为 A4／A6 删、**A9 保留**，并补连带收口方案；⑤ 1a 只消「字母/数字前缀」一类，**非编号来源的「N 行」计数残留**（实测 053 归档件仍报「全表 7 行」）→ 销账时须注明；⑥ 1b 的 `(cur, n)` 二元**区分不了**「哨兵型保留」与「归零型删除」（两者同形）→ 汇总行改**中性措辞**；⑦ 静态末尾三态判据未纳入新的汇总行 → **会重建 049 批要防的假绿通道**，已补；⑧ 检查 C 的载体面窄于真实分布（82 行含「替换为」、bullet 成对仅 23 条）→ 补载体缺口说明＋运行时机说明＋一支假阳性基线断言（施工方实测对 `053`／`054` 归档件命中 0，达标）。
- **P2×7 全采纳**：① 头注锚点 `:1–:10` 失准（实为 `:1–:32`，维护入口在 `:24–:31`）；② 报错串前缀原写 `x`（本文件该前缀即**阻断**语义）→ 改 `·`；③ §五.3 的现状数字与编号整体错位一格 → 订正；④ 「三件过程产物」与宪法 §3.8 铁律④同名异义 → 改称「三项」；⑤ §一.3 的处数引证**在仓内不可复核** → 改标注出处类型并补 `archive/019`／`042` 等可查实例（**审查此项另有一处误判如实记**：其称「055 自身踩 MD038 四处不成立、那 4 处是合法的双反引号补齐」——该 4 处系本会话实测 lint 输出（行号 `:34`／`:35`／`:55`，即 ②③ 两类的字面示例），已由规划方当批改措辞消除，故审查在改后时点复跑得 0 issues 属预期）；⑥ 检查 D 的 ①② 类须**按 CommonMark 剥一层补齐并跳过跨度内文本**，否则本规格自身的示例即误报；⑦ 检查 C 在预验阶段必然全量报「未落实」属设计使然，须写明「谁在何时读它」。

**产物审查（待补）**：

**产物审查（产物审查分身，2026-09-12）**：findings **12（P0:0 P1:4 P2:8）**，结论「**通过**」（P0 为零 ⇒ 不触发「P0 ≥2 修复后复审」）。审查独立实跑了：`CLAIM` 的 18 例合成探针与全语料 old/new diff、`check_swallow` 的 053 复跑与新旧对照、`extract` 老新版全语料对照（**143→84／执行 71→36／丢弃 59**）、`check_pairs` 全语料扫描＋逐条命中回查、`check_writing` 全仓 99 文件 sweep＋四类夹具＋合法形态反例、C／D 非阻断退出码夹具、`--static` 对 055、`bash scripts/check.sh`、markdownlint 全仓，以及 `git log -S` 对三处漂移的溯源。**已核实为「无问题」的关键面**（供直接引用）：1a 不误伤（真声称「实测 133 行」「1,234 字符」「第 3 行」仍报、`MD034 行`／`G1 行`／`N11 行` 出局、限额豁免仍在，全语料仅 2 条假声称消失且**无真声称丢失**）；1e 检查 D 对合法形态**零误报**（`` `` `<path>` `` ``／全空格跨度／跨度内示例／外链锚点均不报），055 自身 0 预警；C／D 命中后 **exit=0**、「无发现」假绿通道未被重建；collab-log 插入句与该条自洽；新增代码零外部依赖。逐条处置——

- **P1-1（已采纳·第二轮整改）**：检查 C 的目标解析原为**单一 target 槽、不随 `###` 小标题重置**，且 PATH_TOKEN 取自所有非 PAIR 行（含围栏正文与散文引述）→ 实测归档件上 10 条里 **5 条误配**（`archive/048` 报 `@ scripts/check_spec_assertions.py` 而真落点是 `scripts/check.sh`；`archive/050` §6 一次声明三文件、单槽只留最后一个）。改为**以「当前 `###` 小标题声明的路径 token」为准**（跨小标题重置、多声明则任一命中即算落实、不再从围栏／散文取 token）。整改后全仓 check C 命中 **10 → 6 条**（误配 4 条全消），且 **052 那条真阳性仍报**（「四分类」被 053 批合法改写为五分类）、053／054 仍为 0（A10⑤ 保持绿）。
- **P1-2（已采纳）**：检查 D 的 ①② 归类判据原按「定界符外侧两侧皆非空白」判定，致中文正文里 ② **恒被标成 ①**（实测：**行内代码尾部为空格、且紧贴中文句号**的一例被报「嵌套反引号」，而该跨度内并无反引号）→ 改为「跨度内**含**反引号＝①，否则＝②」；**「报不报」的集合与 markdownlint MD038 逐行一致**这一性质保持不变（实测两版对照 4 行吻合）。
- **P1-3（已采纳·第二轮整改）**：`check_swallow()` 的 `cur==0` 支原写「保留原预警」，但工具自己的判据就承认该型非必为漏写（053 的「四分类」「不得据以仿写」＝ token 被合法删除、到位 0 已达成）→ 一并改**中性逐条措辞**并计入三态汇总。**本项使 §二.1b 与 A10② 的旧字面失效，规格已同步**（施工方按「禁止扩大整改范围」未改规格、仅呈报——处置正确）。
- **P1-4（已记录·不在本批）**：行内抽取收窄后**丢弃 59 条唯一串（35 条原在执行桶、24 条 SKIP）**，且输出无任何痕迹（摘要行只报总数）。当前**零实际影响**（丢弃清单里非归档件命中 0 条），但对将来同风格书写的规格该断言会静默不跑 → 已登台账（见 §二.3 的登记项）：或在摘要行补过滤计数、或在协议增一句写作约定「断言须写成行首跨度或围栏」。
- **P2×8 处置**：① 检查 C 的载体覆盖约 1/4（语料含「替换为」91 行、bullet 成对 23 条），**内联箭头载体不核**——而 052 批事故（段序漏 L5）的载体恰是此形态 → 已登台账（补覆盖属另批）；② 「缩进续行＋其后期望载荷围栏」形态丢弃围栏正文（全仓 0 处，潜伏）→ 登台账；③ 检查 D ④ 类在**非规格件**上会误报（`check.sh` `[4]` 只传 `docs/specs/*.md` ⇒ 闸内零误报）→ 登台账（若将来扩展扫描集须先处理）；④ 同一行的一个嵌套构造产出 2 条同文预警 → 登台账（按行去重）；⑤ docstring 引「见规格 055 §二.1d」未给路径（反模式 #3 精神）→ 登台账；⑥ `cur==0 且 n>0` 支零输出（N4 收窄的声明内）→ 无动作；⑦ **`AGENTS.md:14` 的 `[4]` 段括注只列 A／B** → **已采纳为本批 §二.2c**（改动面 2 → 3 文件）；⑧ 脚本净增 239 行高于估算「~80 行」（§7 不约束开发脚本，无违规）→ 仅记。

**交付点核验（规划方，2026-09-12）**：三步独立重跑——改动面＝**3 个内容文件**（`git status --porcelain` 核对；`skills/` 与 `docs/standards/` 零 diff）；A1–A9 命令断言全达标（1／2／1／3／8／5／2／4／1）；A10 五支逐支实跑复现（含 ① 真形态夹具 `MD034 行`／`G1 行` 归零、② 无命中行 **0** ＋中性行 2 ＋汇总 1、③ 018 默认模式 `grep -c` 假行 0、④ 不符夹具报出且 rc=0、⑤ 053／054 命中 0）；A11 四类各命中 1 行且 055 自指零预警；A12 `check.sh` 退出 0；A13 `py_compile` 通过且 `__pycache__` 即删；A14 改动面如上。**核验中的独立发现（如实记）**：新检查 C 在**归档的 052 规格**上报 1 条——经查为**真阳性**（052 的替换文本当年已落盘、后被 053 批合法改写），影响面为零（`[4]` 不扫 archive），已在 §四 A10⑤ 的残留说明中记账；**本批规格自身在成稿与整改期共踩 MD038 四次**（三次为「外层反引号包裹含内层反引号」＝检查 D ① 类；第四次即本节记录 P1-2 时引用那个「边缘空格」实例本身＝② 类）——**这是采纳四条机械检查的最强论据**（规则最熟、且正在为它写文档的人也会连踩），也已如实写入其证据栏。
