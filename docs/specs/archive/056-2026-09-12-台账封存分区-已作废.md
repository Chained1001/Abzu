# 056 · 台账封存（分区：活区／冻结区）（批 F）

> **本规格已作废**（2026-09-12，作者裁定）：作者在本会话内裁定「台账内部有大量待办…这台账本就不合理…我甚至认为台账不该存在」，遂**全域台账解散**、内容按主题分流（批 057·地基重构）——本规格「分区封存」的前提随之失效。其间未提交的工作树改动已由 057 批覆盖（`open-items.md` 直接删除）。**留档理由**：其勘验数据（60 条待办的性质构成：约 27 条路线构想／约 31 条技术债，其中约 25 条为治理线自产）是 057 批决策的直接依据。作废先例见 `archive/046-…-已作废.md`。
> **状态**：待作者确认（AGENTS §3.1 判定表 ✅：流程规则变更 + 跨文件 ≥2）
> **依据**：作者 2026-09-12 裁定「scan 扫榜技能和 analyze 拆书技能…放在哪里就好了，暂时封存不管」＋「台账真的有修复的意义吗」的质询；规划方评估（60 条待办中 **20 条**触发条件落在未建域或外部时点——该数在 §二.1b 按判定序逐条清点后得出：等域建设 6 ＋ 等触发条件 10 ＋ 等另批 2 ＋ 等在途收尾 2 ＝ 20，为它们保鲜是负收益）
> **基线**：`9bebf76` 之后；**自由度标记**：`[A]` 逐字执行 ｜ `[B]` 按方向自定实现

## 一、现状锚点（2026-09-12 现场核验）

### 1. `docs/specs/open-items.md`（活账本；**83 条数据行**＝行首管道命中 97 − 表头 7 − 分隔行 7；文件 125 行）

- 结构：头注**两行**（定位 `:3`／纪律 `:4`；「旧源收口」句在 `:4` 行内、不另成行）＋ **七分组** H3 表——行号实测全对：`等裁决`（`:8`）／`等域建设`（`:19`）／`等触发条件`（`:32`）／`等另批`（`:49`）／`等在途收尾`（`:84`）／`纯遗漏`（`:92`）／`已销`（`:101`）。**各組数据行数实测为 6／8／12／29／3／4／21＝83**（成稿审查 P0-4 采纳：原稿 7／9／13／30／4／5／22＝90 系**把各组表头行计为数据行**，每组恒多 1）；状态列分桶实测：待办 **60**／已销 22／部分已销 1（＝83）。
- 纪律行（`:4`）现写「每批核验收尾核对」——**未区分活区与冻区**，即 60 条待办每批都要过一遍。另实测一处**台账内部不一致**：`A9`／`G5` 两行状态已是「已销（…）」却仍留在「纯遗漏」组内（本批一并归位）。

### 2. 「挂账登记核对」条（`docs/specs/collab-log.md`，核验结论前必做）

现三条：当批挂账逐条登记（**未登记即核验未完成**）／销账记依据／036 批 F11 反例。**未限定范围**——按字面，每批都要把 68 条全部视为待核。

### 3. 引用面（决定涟漪；**判定表按载体逐处 read 得出**，其余命中为归档规格与沿革行、属历史不回改——成稿审查 P1-2 采纳：原稿的「8 文件／3 处不动」两数不可复现，已删）

| 载体 | 现状 | 本批是否动 |
| --- | --- | --- |
| `AGENTS.md:32`（§0 必读表行） | 「挂账台账（全部未办事项的单点来源）｜非平凡改动收尾核对」——**不含量或结构描述，分区后仍成立** | 不动 |
| `AGENTS.md:131`（§6 台账类定义） | 「准欠账清单…只记未办事项与触发条件」 | 不动 |
| `AGENTS.md:142`（CHANGELOG 条对照） | 只提「挂账台账 `open-items.md` 记未办事项」 | 不动 |
| **`docs/standards/file-conventions.md:104`** | 台账类节序写「open-items：**挂账清单七分组表**」——**分区后此数与实不符** | **须同步** |
| `file-conventions.md:37`（命名总表行）· `:128`（活账本头注变体豁免） | 只述文件名与头注形制 | 不动 |
| `README.md:74`（Roadmap 指针行） | 「现行挂账以 open-items.md 为准」 | 不动 |
| `scripts/check.sh:83` · `check_spec_assertions.py:86–87` | 只按**文件名**排除该件（扫规格集／NEVER_ARCHIVED）——分区不改文件名，**机检零影响** | 不动 |
| `CHANGELOG.md:17`（047 条目内） | 写「挂账台账（60 条七分组；…）」——**属历史沿革**（该批当时的实况），CHANGELOG 不回改 | 不动（本批在 A5 断言面注明：归档与沿革内的「七分组」为历史，不回改） |

### 4. 勘验中查实的一条 stale（本批顺手销）

**N2**（`file-conventions` 规格行膨胀 → 下沉为表注）：**054 批已完成**（§二.2a 整行成对替换 + §二.2b 新增表注，实测该件现含「表注（规格行的扩展口径）」），但当时漏销账，台账仍标「→ 批 4d」。本批销账并记依据。

## 二、文件级改动清单

### 1. `docs/specs/open-items.md`：分区重构 [B]

**1a. 头注纪律行（`:4`）**：在「每批核验收尾核对」之后补一句分区判据——

> **分区**：条目按**触发条件**分两区——**活区**（触发条件落于本仓治理线或大纲域：`AGENTS.md`／`docs/standards/`／`docs/specs/`／`scripts/`／`docs/product/` 及其实现）每批核验；**冻结区**（触发条件依赖**未建域**或**外部时点**：scan／analyze 域的改进欠账、volume／write／style 域建设、T1 触发、v1.x、开源发布、多域上线）**解冻时评估**，不在日常核验面内。

**1b. 分区落地**（目标结构＝**9 个「区·组」+ 已销**；条目**只移不删**）：
**分区判据（判定序——自上而下取首个命中；成稿审查 P0-5 采纳：原散列式判据在「大纲域」一词上可推出相反归属）**：

1. 触发条件指向**本仓可执行的下一步**（**已排期或已在册**的批号／某份本仓文件的下一步修订／某项可即刻执行的治理动作）→ **活区**（「某批」但未排期者不算，见判定序 2）
2. 否则，触发条件指向**某域立项或外部时点**（T1／v1.x／开源发布／多域上线／外部 PR）→ **冻结区**
3. 否则，触发条件指向**已建成但按作者 2026-09-12 裁定封存的域**（scan／analyze 的改进与真机走查）→ **冻结区**（**第三类**，原判据未点名，成稿审查 P2-3 采纳）。**③ 优先于 ④**：某条目字面像死条件但其对象落于封存域者（如 R4「拆书域已裁定搁置，无期」），按本条入冻区
4. 三条均不命中（条件式或已成死条件）→ **活区**；**不另设尾注标记**（施工方 P1-2 采纳：本表已把 60 条逐条列明落点，归类可核；尾注会与「条目文字逐字保留」互斥。**故本批只改归属，全表零文字改写**——唯一例外仍是 N2 的状态列）

**目标结构与实测条数**（逐条按判定序清点后得出；**各区·组条数之和（活区 38 ＋ 冻结区 21 ＝ 59）＋ 已销 24 ＝ 83**）：

| 区·组 | 条数 | 组成（原组 → 去向） |
| --- | --- | --- |
| 活区 · 等裁决 | 6 | 整组留（判定序 1／4）；**R6「发布前置」按第 2 条本应入冻区，但已列入批 E 即时面，故留活区并在此点名**——**同类词不同判的唯一点名例外**：其余发布类（如 N23「下次发布 bump」）一律按 ② 入冻区（成稿审查 P2-7＋产物审查 P2-4 采纳） |
| 活区 · 等另批 | **28** | 原 29 − N2（销账）− N14／N15／N25（入冻区）＋ **G3／A8**（自等域建设迁入，判定序 1）＋ **N18**（自等另批迁回，判定序 1）——**A15 属活区**（触发条件「批 2 + 后续」＝本仓批号；成稿审查 P1-1 采纳：原清单漏列 A15、且误列不在本组的 R10／R11） |
| 活区 · 等在途收尾 | 1 | 仅 S2（045 §二.8 锚点重锚，判定序 1） |
| 活区 · 纯遗漏 | 2 | 仅 C8／G4（**A9／G5 状态已是「已销（…）」**，本批归位入已销组——成稿审查 P1-5 采纳） |
| 活区 · 等触发条件 | 2 | 仅 G7（脚本依赖复杂化）／G8（经验积累或节律触发）——**条件式，判定序 4**（落点活区一致，原稿标 1 属标注失真，产物审查 P2-4 采纳） |
| 冻结区 · 等域建设 | 6 | 原 8 − G3／A8（迁活区） |
| 冻结区 · 等触发条件 | 10 | 原 12 − G7／G8（迁活区） |
| 冻结区 · 等另批 | **2** | N14／N15（触发条件「新书项目设计批」**未排期**，按判定序 1 的限定不算「本仓可执行的下一步」→ 落 2；**N18 本就在活区·等另批**（其触发条件「设计文档下次修订批」＝本仓文件的下次修订，判定序 1）——原稿误列为冻区、后又重复计入，两处均订正） |
| 冻结区 · 等在途收尾 | 2 | C1／G11（**C1＝判定序 2**（031 场景 1 属 outline 未建域，非封存域）；**G11 混装**：029 拆书＝③、031 场景 1＝②；落点冻区均成立，原稿一律标 3 属标注失真，产物审查 P2-4 采纳） |
| 已销 | 24 | 原 21 ＋ A9／G5（归位）＋ N2（054 已完成，本批销账） |

- **`已销` 不加区前缀**（成稿审查 P2-1 采纳：原稿「七个 H3 改为区＋组」与其下「已销不动」自撞）。
- **拆表须各自带表头与分隔行**（成稿审查 P2-2 采纳；现台账 `:78` 一带已有缺表头的破损行，本批顺带补齐）。
- **条目文字与「状态」列一律逐字保留**（只改归属与组标题）；**唯一例外是 N2**：其状态列由「待办」改为「已销（054 批：规格行下沉已完成）」。
- **落点在此表内穷尽**——施工方**不得自创组名**（成稿审查 P0-3 采纳：原稿责令 G3／A8／C1／G11 跨区却未给承接组，落点自定必破 A1／A2）。

**1c. 施工纪律**：**只改分组归属与标题，条目文字与「状态」列一律逐字保留**（含引号、批次号与括注；唯一例外是 N2 的状态列）；分区**一律按 §二.1b 的判定序与条数表执行**——四类判据均不命中者留活区（**零尾注、零文字改写**；施工方 P1-2 采纳，替代原「尾注判据缺环」的设计）；落点见 §二.1b 条数表，**不得自创组名**。

### 2. `docs/specs/collab-log.md`：核验协议条加限定 [A]

「挂账登记核对（核验结论前必做）」条首句后补两句（**其余两条文字逐字不动**——该条共 3 个 bullet，首句在第 1 条内；成稿审查 P2-4 采纳）：

> 核验面仅及 `open-items.md` 的**活区**；冻结区（触发条件依赖未建域或外部时点）**解冻时评估**，不在每批核验面内——分区判据见该件头注。**另**：本批若触及某冻区组的解冻条件（域立项／发布／T1 触发／多域上线），须对该组**重判并将一行结论登记入当批核验结论**（含「仍不触发」；落点＝核验结论，产物审查 P2-3 采纳）——否则触发到来时无人翻冻区，会形成既不在活区核验面、也无人观测的静默欠账（成稿审查 P1-9 采纳）。

### 3. `docs/standards/file-conventions.md:104`：节序描述同步 [A]

现文「…（open-items：**挂账清单七分组表**）」→ 改为「（open-items：**挂账清单分区表**——活区／冻结区两区，区内按状态分组；**另有已销组，不属两区**；分区判据见该件头注）」（成稿未含、产物审查 P2-2 采纳）。**理由**：把会漂移的「七」换成分区结构描述（分区数亦会变，故只写「两区」这一层稳定的语义）；该文件版本行 v0.18 → **v0.19**（实质修订）。

### 4. 过程产物（规划方职务，非施工方项）

- `CHANGELOG.md` 顶部新增条目（≤ 400 字符）。
- `docs/specs/collab-log.md` 施工记录表末行追加一行（不写提交状态）。
- 本规格补「审查记录」节；归档至 `docs/specs/archive/`。
- `open-items.md` 自身：**N2 销账**（依据 054 批）。

## 三、禁止事项

- **禁止删除或改写任何挂账条目**（只改归属；条目文字、状态列逐字保留）。
- **禁止改动 `AGENTS.md`**（实测其**三处**引用——`:32`／`:131`／`:142`——均不含量或结构描述，分区后仍成立；若施工中发现确需改，按 P0 停摆呈报，不得擅自改）。
- 禁止改 `check.sh` 与 `check_spec_assertions.py`（机检只按文件名引用，零影响）。
- 禁止自创 §二.1b 条数表以外的组名；禁止对任何挂账条目做文字改写（含尾注类标记）。
- 禁止 git 写操作；禁止联网。

**偏差分级**：P0 阻塞型 → 立即停、输出偏差报告；P1 观察型 → 继续施工、记入核验请求。

## 四、验收标准

**断言口径声明**：命令一律取固定串计数形态（`-F -c`）；**归零型断言（到位＝0）零命中时退出码 1 且无输出＝通过**（该语义**不适用于**到位为 ● 的正向断言与 A9 哨兵）；到位列 `●` 表示哨兵型正向计数。【成稿审查 P2-6 采纳：原「零命中即通过」为无范围通句】

```bash

git grep -F -c -- "### 活区 · " docs/specs/open-items.md
git grep -F -c -- "### 冻结区 · " docs/specs/open-items.md
git grep -F -c -- "活区**（触发条件落于本仓治理线或大纲域" docs/specs/open-items.md
git grep -F -c -- '核验面仅及 `open-items.md` 的**活区**' docs/specs/collab-log.md
git grep -F -c -- "七分组" docs/standards/file-conventions.md
git grep -F -c -- "挂账清单分区表" docs/standards/file-conventions.md
git grep -F -c -- "v0.19（沿革见" docs/standards/file-conventions.md
git grep -c -E -- "^\| N2 \|.*已销" docs/specs/open-items.md
git grep -c -E -- "^\| [A-Za-z]" docs/specs/open-items.md
git grep -F -c -- "若触及某冻区组的解冻条件" docs/specs/collab-log.md

```

| # | 断言 | 现状 | 到位 |
| --- | --- | --- | --- |
| A1 | 活区组标题到位（**5 个**：等裁决／等另批／等在途收尾／纯遗漏／等触发条件——**与 §二.1b 条数表对齐**；施工方 P1-1 采纳，原稿 4 系 1b 重写前的旧值） | 0 | 5 |
| A2 | 冻结区组标题到位（**4 个**：等域建设／等触发条件／等另批／等在途收尾） | 0 | 4 |
| A3 | 头注含分区判据句 | 0 | 1 |
| A4 | 协议条含「核验面仅及活区」限定句 | 0 | 1 |
| A5 | `file-conventions` 的「七分组」归零 | 1 | 0 |
| A6 | 「挂账清单分区表」到位 | 0 | 1 |
| A7 | 版本行 v0.19 | 0 | 1 |
| A8 | N2 状态列已改「已销（054 批…）」（唯一允许改状态列者） | 0 | 1 |
| A9 | 台账**数据行**不减（哨兵：只移不删；口径＝行首管道符后紧跟字母或数字者，排除表头与分隔行） | **83** | ●≥83 |
| A13 | 协议条含「解冻观测点」句（成稿审查 P1-9 采纳） | 0 | 1 |

- A10 语义复核（手工，**基准确认＝83**）：分区后**各区·组条数之和（活区 38 ＋ 冻结区 21 ＝ 59）＋ 已销 24 ＝ 83**（只移不删的可判定证据；逐组条数须与 §二.1b 条数表的 6／28／1／2／2／6／10／2／2／24 逐项相符——**发现任何一组数不符即为 P0，停摆呈报**）；另核冻区**四组**内不含判定序 1／4 命中者（判据见 §二.1b）。
- A11 机检：`bash scripts/check.sh` 退出 0。
- A12 改动面：**基线＝`9bebf76` 之后**；执行时点＝本规格归档**之前**。改动面＝3 个内容文件（`docs/specs/open-items.md`、`docs/specs/collab-log.md`、`docs/standards/file-conventions.md`）＋ 过程产物四项（CHANGELOG 条目／collab-log 施工记录行／本规格补审查记录节并归档／**open-items 的 N2 销账**——其中 N2 落在已列的内容文件内，故不计入改动面文件数）＋ 本规格自身；**`AGENTS.md` 与 `skills/` 零 diff**（另行 grep 核）。【成稿审查 P1-8 采纳：原稿「三项」与 §二.4／§五.2 的「四项」及本规格自身的单列口径三样不一】

## 五、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：§一 全部锚点现场核验——台账七分组的行号与条数由 `grep -n "^### "` + 逐组计数实测（7/9/13/30/4/5/22＝90）；引用面由 `git grep -ln "open-items"` 实测 8 文件，其中逐处 read 判定「本批是否动」（3 处不动／1 处须同步）；「挂账登记核对」条由 `sed -n` 区间抽取 read；N2 已完成一事由 `git grep -F -c -- "表注（规格行的扩展口径）"` ＋ 054 批 §二.2 复核。
2. **清单完备**：3 个内容文件 + 过程产物四项（含 N2 销账）。§二.1b 给出**每组的去向与实测应留/应移清单**，并把「判不准留活区」写成纪律，防施工方自由裁量。
3. **验收可判定**：A1–A13 为 13 条命令块断言（现状值已逐条实跑复现：0／0／0／0／1／0／0／0／**83**／0——A9 的口径是**数据行**（行首管道符后紧跟字母或数字），实测 83（＝行首管道总命中 97 − 表头 7 − 分隔行 7）；原稿曾以「逐组条目相加＝90」为基准，该数系把表头计入所致，已订正）；A9 为**只移不删哨兵**（正向 ≥90）；A10 为手工清点且给出可判定等式；A11 为脚本退出码；A12 为改动面对照（含 `AGENTS.md` 零 diff 的额外核）。**防自噬自查**：归零型 token 为 A5（「七分组」）与 A8（`N2`）——本规格 §一 的「现状」引用了「七分组」与 N2 条目，替换侧不含，故**不互斥**（工具按加法预测会高估，属 N4 族已收窄）。
4. **矛盾检查**：与 AGENTS §2 文件账本律（每步落盘）一致——分区不减少落盘要求，只把核验面收敛；与 §6 台账类定义（准欠账清单）一致；与 collab-log「未登记即核验未完成」一致（改为「活区未登记即核验未完成」）；与 §3.9（无实例的条文标预立法）——本条有实例（22 条冻项即首批实例）。
5. **术语**：「活区」「冻结区」「解冻」在首现处给出判据；分区判据以**触发条件**为准（不是按域机械切分）。
6. **节号复核**：本规格引用 `collab-log` 的「挂账登记核对」与「核验协议」、`AGENTS.md` §0／§2／§3.1／§6、`file-conventions` §五.1 台账类行，均已现场 grep 核对存在。
7. **涟漪清单**：① 分区后须确认 `file-conventions:104` 同步（否则规范与实况不符）；② `AGENTS.md` **三处**引用已逐处 read 判定**不需改**——施工时若认为需改，按 P0 呈报；③ 机检两处只按文件名引用，**分区零影响**（不改文件名）；④ 「判不准留活区」的保守条款须在核验时抽查（防施工方把活区项误移冻区以图省事）。

## 六、开放项

1. **冻区解冻的触发**：解冻条件＝对应域立项或外部时点到来（如 T1 触发、开源发布）。解冻时评估的具体做法（逐条重判 vs 整区重判）本批不规定，留待首次解冻时立例。
2. **存量 stale 的系统排查**：本批只销了 N2 一条已查实的 stale（054 已完成未销账）。是否对 63 条活/冻项做一次「是否其实已完成」的全面复核，本批不做（成本高于收益；下次批 3b 若顺路可抽查数条）。
3. **头注行数与体积**：分区判据句**并入既有纪律行（`:4`）之内**，头注**仍为两行式**（定位 `:3`／纪律 `:4`）——故 `file-conventions:128`「两行式头注（定位／纪律）」**无需改**，A5/A6 只动 `:104` 的节序描述。【成稿审查 P1-7 采纳：原稿先称「由三行变四行」、又判「不构成第三类头注」，自相打架且与 `:128` 实文不符。】
