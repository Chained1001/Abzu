# 057 · 地基重构（立总纲＋立 testing＋解散台账＋收编追踪档）

> **状态**：验收通过（待作者确认提交）（AGENTS §3.1 判定表 ✅：架构级变更 + 新增/删除多个文件 + 跨文件 + 流程规则变更）
> **依据**：作者 2026-09-12 会话内三轮裁定——①「台账内部有大量待办…这台账本就不合理…我甚至认为台账不该存在」（解散全域台账）②「总纲放 product/，做成产品加技术文档，回头讨论和开发都在其中，还包含 architecture + glossary」（立总纲并吸收两件）③「test-prompts 是否真的有用啊，我一般测试都是我自己实测的」（其 [机检] 项入守卫、会话行为项入清单）
> **基线与分期**：本文为**一个批次**（同一架构决策），**分三段提交**——G1 立总纲＋立 testing＋删四件＋引用改指；G2 债分流＋机制条改；G3 收编追踪档。三段共享同一份改动清单与验收面，逐段提交便于回退。
> **自由度标记**：`[A]` 逐字执行 ｜ `[B]` 按方向自定实现 ｜ `[C]` 按验收标准核验

## 一、现状锚点（2026-09-12 现场核验）

### 1. 四个待退件与它们的 live 引用面（`git grep -ln`，排除 `docs/specs/archive/`）

| 待退件 | 行／字符 | 定位（原文摘要） | live 引用（须改指） |
| --- | --- | --- | --- |
| `docs/architecture.md` | 112 行 / 6,543 | 「架构选型与理由的**唯一权威**」（裁定记录／选型理由／落定结构／精准入口／部署循环／流程哲学／权威规范对齐／维护） | `AGENTS.md`、`docs/standards/file-conventions.md` |
| `docs/glossary.md` | 98 行 / 4,689 | 术语叫法权威（`file-conventions:5` 明写「与术语表分工」） | `AGENTS.md`、`docs/standards/file-conventions.md`、`docs/standards/markdown-style.md` |
| `docs/test-prompts.md` | 152 行 / 4,436 | skill 行为验收基准（8 场景，宪法 §3 要求「每次修改 skill 后必须重跑全部」） | `AGENTS.md`、`README.md`、`docs/standards/file-conventions.md`、`docs/standards/skill-writing.md` |
| `docs/specs/open-items.md` | 142 行 / 7,501（含本会话 056 批未提交的分区改动） | 挂账台账（全域欠账清单） | `AGENTS.md`、`README.md`、`docs/specs/collab-log.md`、`docs/standards/file-conventions.md`、`scripts/check_spec_assertions.py` |

（`CHANGELOG.md` 的命中一律**不回改**——沿革属历史。）

### 2. 台账内容的实测构成（解散后必须逐条有去处，故先勘验）

按性质分桶（056 批已分区，基线数据）：**路线／构想 ≈ 26–28 条**（各域建设、T1/发布/多域上线类触发、A 系列大纲域工作）；**技术债 ≈ 31 条**，其中**约 25 条是治理线自产**（规范措辞／守卫边界／协议口径／脚本体积——与产品功能无关），仅约 6 条与产品相关。**结论**：这张表在为自己的维护生产工作。

### 3. 追踪档的实测状态（G3 的对象）

`docs/product/大纲阶段追踪档.md`（149 行 / 57 条 S 项）：头注自带**定稿条件**（「甲节无 ⏳/🚧 且乙节无 🔴」）与同步纪律（「每发布一版设计文档，必须同步刷新本档状态」）——**机制优于台账（有终态）**；但实测三处缺陷：① **冻结未闭档**（设计文档 `:4` 已标「v0.9.6 冻结版」，而 S44–S47 是冻结后追加）；② **57 条中绝大多数是已修历史**（乙节逐条标 ✅ 已修入 + 版本号，价值已由 git/CHANGELOG 承载）；③ **双向耦合**（设计文档 `## S 编号索引` 把追踪档条目内联进正文，每加一条 S 要改两份件）。

### 4. 待分流的两组内容「去处必须先在」

- **路线／构想**的去处＝**总纲 §路线与阶段**（本批新建）；
- **技术债**的去处＝**按主题归属的规范件「已知缺口」小节**（本批新建的 `testing.md` 承担机检／工具类，其余按主体分散）——**债跟着主题走，不跟着时间走**（这条是解散台账后防止它借尸还魂的机制）。

## 二、文件级改动清单

### 1. 新建 `docs/product/总纲.md`（产品与技术总纲，全域层）[B]

**定位**：`product/` 域的唯一全域层文件——「这是什么、怎么搭起来、往哪走」。**一层深**：只索引域层文档（如《大纲阶段设计文档》），**不复述其内容**。

**内容结构（八节，须齐备）**：

1. **§一 定位与范围**——六域套件（各域一 skill，本名即命令）／目标宿主 Claude Code（唯一）／**当前阶段：只规划到大纲阶段**／**封存域声明**（scan、analyze 已建成但按作者 2026-09-12 裁定**封存**，不再投入）／v1 范围（仅六域；导入／独立审查／抓取底座独立化均不做）。
2. **§二 六域地图与状态**——六域表（域｜skill 名｜状态：已建成／占位／封存｜一句话职责）+ 落定目录结构（吸收 `architecture.md` §三：`skills/` 树与 `abzu-setup` 的 T1 预留）。
3. **§三 设计理念**——吸收 `architecture.md` §六 流程哲学（域是平行的门／生产者只落盘消费者定义读取／阶段门控只管域内工序／会话恢复按文件证据）＋ 文件账本律。
4. **§四 基础设施选型**——吸收 `architecture.md` §一 §二 §四 §五：承载架构（多技能骨架，含选型判据与**拆分触发器 T1–T3**）／精准入口（**唯一入口＝`/abzu-{域}` 显式调用，自然语言模糊触发禁止**）／部署与实测循环／项目结构（仓库目录树，含各件的分工一句话）／质量闸分层（守卫／预验器／审查分身）／版本与兼容。
5. **§五 路线与阶段**——三张表：**近期**（治理线收尾：批 E 触发形态契约、批 B 宪法形态、批 C 规范补缺、批 D 流程分级与预案）／**中期**（大纲域实现：A 系列——三评委打磨循环、方法论萃取、卷纲细纲 schema、脚手架重建、参考标尺脚本数事实）／**构想池**（各域建设、T1 物料、多域上线与发布类，含触发条件）。**来源**：原 `open-items.md` 的路线／构想类条目（§二.3 的路由规则）＋ `architecture.md` §七 的挂账四项。
6. **§六 外部对标与规范依据**——吸收 `architecture.md` §七：T0/T1/T2 权威源分级清单／设计决策对照（多技能骨架＝menu approach、域间文件通信＝Pipes and Filters、eval-first、宪法＝spec-kit Constitution、术语表＝DDD Ubiquitous Language…）／**外部参考纪律**（前作 mo-shu 不作设计依据，仅限沿革与教训；架构参照研读例外条款）。
7. **§七 术语（叫法权威）**——吸收 `glossary.md` **全文**，保持「全仓唯一的术语定义处」；表头注明「**叫法权威**：skill 正文、产品文案、规范条文一律以本节为准」。
8. **§八 维护**——吸收 `architecture.md` §八 + 立三条：架构变更同批修订本文件与 CHANGELOG；新域准入过宪法 §4 决策树 + §四 选型判据；**本文件为热文件**——「索引不复述」纪律，规模逼近 `AGENTS.md` §6 的 2 万字符线时先压缩再下沉。

### 2. 新建 `docs/standards/testing.md`（守卫与验收规范）[B]

**1）§一 守卫全景**——哪道闸管什么：`check.sh` 六段（[0] 真目录守卫／[1] 六壳 validate／[2] lint／[3] 内容轨／[4] 规格静态自检／[5] 脚本语法）／预验器三态（执行｜跳过｜崩溃 + 三桶和恒等于提取数）／静态阶段的四道检查（A 自噬预警／B 计数重算／C `[A]` 逐字落实／D 规格写作四类）／**何时必须跑**（改任何文件后 `check.sh` 全绿；改 skill 资产后另加真机走查）。
**2）§二 域上线验收清单**——一域一节，每条 3–5 项**人判要点**（如大纲域：入口判定按文件证据、规格门停靠等作者确认、**终局与感情线不问**（准入律）、分卷五件套齐全）。吸收 `test-prompts.md` 的**会话行为型**断言（触发形态、门控停靠、交互模态、不越阶段）。
**3）§三 真机走查做法与记录**——做法：装到目标项目（`npx skills add`，仓库内禁跑）→ 新会话 → 敲 `/abzu-{域}` 跑主流程；记录：**落当批规格**（不另建台账）。
**4）§四 已知缺口**——承接原台账的**机检／工具类技术债**（按 §二.3 路由规则）。

**`test-prompts.md` 的 [机检] 型断言处置（不搬进清单）**：它们本质是**产物格式契约**（如「采集文件头含数据质量三行」「章节索引五列表头」「选题决策十七字段非空」）——按 §二.3 路由到 `file-conventions` / 域 references 的产物格式节，并由守卫机检；`testing.md §二` 只留**指针**。

### 3. 删除四个件 [A]

`docs/architecture.md`、`docs/glossary.md`、`docs/test-prompts.md`、`docs/specs/open-items.md`——**内容先吸收后删**（顺序不可颠倒；施工时先建后删并在核验请求中列明「吸收完成」的证据）。

### 4. 引用改指（live 引用面，逐处）[A]

| 载体 | 改法 |
| --- | --- |
| `AGENTS.md`（实测命中：`test-prompts` 3 处含 `:17` 行为验收行与 `:48` 禁带病开工行、`glossary.md` 3 处——**全部命中处均在改动面内**，不得只改必读表） | §0 必读表：`architecture.md`／`glossary.md`／`test-prompts.md`／`open-items.md` 四行合并为**两行**（`product/总纲.md`＝定位/选型/路线/术语；`standards/testing.md`＝守卫与验收）；§3 的「每次修改 skill 后必须重跑全部场景」**改**为「改 skill 资产后：`check.sh`（含守卫）必须全绿 + **真机走一遍该域主流程**（做法见 testing.md §三）」；§6 的「准欠账清单（open-items.md…）」条**改**为「**不做全域欠账清单**——构想进 `product/总纲.md` §路线与阶段，已知缺陷**当批修完**，修不完的写进**当批规格的开放项**（随规格归档、由 git 历史承载），或该主题所属规范件的「已知缺口」小节」；§7 若有引 `open-items` 之处同步 |
| `README.md` | 架构／术语／验收／Roadmap 挂账指针行 → 总纲与 testing.md；`项目宪法` 行的指针不变 |
| `docs/standards/file-conventions.md` | `:5`「与术语表分工」→「与 `product/总纲.md` §术语分工」；命名总表中的 `架构决策记录`／`术语表`／`挂账台账` 三行 → **改为 `产品技术总纲` 一行**（`docs/product/`）；`设计文档` 行补「全域层＝总纲／域层＝各域设计文档」；`规格` 行的同批附件等不变；**版本行 bump** |
| `docs/standards/markdown-style.md` | glossary 的两处引用 → 总纲 §术语 |
| `docs/standards/skill-writing.md` | `test-prompts` 引用 → `testing.md` §二 域上线验收清单 |
| `docs/specs/collab-log.md` | 「挂账登记核对」节**整节重写**为「**当批开放项核对**」：当批产生的欠账须当批修完，修不完的写进**当批规格的开放项**（无落点＝核验未完成）；删「未登记入 open-items 即核验未完成」旧句；核验协议其余节若引台账，同步 |
| `scripts/check_spec_assertions.py` | `NEVER_ARCHIVED = ('collab-log.md', 'open-items.md')` → 去掉不存在的件（**只删条目、不动判据与注释语义**；若实现要求元组非空则保留 `collab-log.md` 单元素并注明） |
| `scripts/check_content.py` | `gov_files` 用 glob 覆盖 `docs/**/*.md` → **无需改**；若其内有硬编码引用四件之一，同步 |
| `docs/product/大纲阶段设计文档.md`（G3 时） | 追踪档引用与 `## S 编号索引` 节按 §二.5 处置 |

### 5. 债的分流（G2；**路由规则见下，逐条落位并回报**）[B]

**路由规则**（按「主题所属件」而非「时间」）：

| 债的类型（原编号族） | 归属件 |
| --- | --- |
| 协议／协作／流程（S 族、C 族、A10／A13） | `docs/specs/collab-log.md` 的「已知缺口」小节 |
| 机检／工具／守卫（N 族大部、G4、C8、R9） | `docs/standards/testing.md §四 已知缺口` |
| 规范文本（N24 弹窗矛盾、N25 术语孤儿、N18 设计文档术语表） | 各主题所属规范件的「待收口」注（`skill-writing.md`／总纲 §术语／设计文档） |
| 产品／文档（N26 设计文档体积、A6、S1、S5、R6） | 总纲 §五 路线（作为待决项）或 `file-conventions`（体积阈值在宪法 §6／§7） |
| 路线／构想（A1–A4、A8、A14、A15、G1–G3、G7／G8、G10、G14、R 族大部、N14／N15、C1／G11） | `product/总纲.md §五 路线与阶段` |
| 真机走查（C1、G11、S2） | `testing.md §三` 的记录约定 + 总纲 §五 路线 |

**施工要求**：**逐条落位并回报一份「落位清单」**（编号｜归属件｜落位形式），**清单条数须等于原台账的待办条数（60）**——漏一条即 P0。

### 6. 收编追踪档（G3）[B]

- `docs/product/大纲阶段追踪档.md` 的内容**并入设计文档尾节**「## 待议与验收基准」：甲节 → 「**需求来源表**」（每条标「已采纳（落在第 N 步）／已否决（理由）／待议」——采纳或否决**即定稿**，不再是待办）；乙节 → 「**历次走查记录**」（已修项**删除**，历史在 git/CHANGELOG；只留**未修项**）。
- 删除设计文档的 `## S 编号索引` 节（双向耦合的产物；待议项已在同一文件内，不需索引）。
- **立闭档律**（写进设计文档头部状态行与 `file-conventions` 的设计文档行）：**设计「冻结」这个动作必须同时清零待议节**——清不掉的**上移到总纲 §五 路线**（未采纳的构想＝路线，不是设计债）。
- 删除 `docs/product/大纲阶段追踪档.md`。

### 7. 过程产物（规划方职务）

- `CHANGELOG.md` 顶部条目（每段 ≤ 400 字符；三段各一条或在一条内分段）。
- `docs/specs/collab-log.md` 施工记录表追加行（不写提交状态）。
- 本规格补「审查记录」节；归档至 `docs/specs/archive/`。
- **056 批作废归档**：按 046 先例——`docs/specs/056-2026-09-12-台账封存分区.md` 移入 archive 并改名追加 `-已作废`，正文头注标废（原因：作者裁定解散台账，本规格前提失效）；其未提交的工作树改动由本批覆盖（`open-items.md` 直接删除、`collab-log` 与 `file-conventions` 的 056 改动被本批 §二.4 取代）。

## 三、禁止事项

- **禁止先删后吸收**：四件的删除必须在「总纲／testing 建成且引用改指完成」之后；施工顺序即验收顺序。
- **禁止留双份**：吸收后原件**必须删除**（单一真源；反模式 #4）。
- **禁止改动 `CHANGELOG.md` 的历史条目**（沿革不回改）；禁止改 `docs/specs/archive/` 下任何件。
- **禁止改 `skills/` 下的业务实现**（本批只动治理件与守卫）。
- **禁止把台账换个名字重建**（如「技术债清单」「待办总表」）——债只可进「当批规格开放项」或**主题所属规范件的「已知缺口」小节**。
- 禁止 git 写操作；禁止联网；夹具放 `.tmp/` 用完即删。

**偏差分级**：P0 → 停摆呈报；P1 → 继续并记入核验请求。

## 四、验收标准

**断言口径声明**：命令一律取固定串计数形态（`-F -c`）；**归零型断言（到位＝0）零命中时退出码 1 且无输出＝通过**；`●` 为哨兵型正向计数。

```bash
git grep -F -c -- "docs/product/总纲.md" AGENTS.md
git grep -F -c -- "docs/standards/testing.md" AGENTS.md
git grep -F -c -- "修改 skill 后必跑" AGENTS.md
git grep -F -c -- "不做全域欠账清单" AGENTS.md
git grep -F -c -- "当批开放项核对" docs/specs/collab-log.md
git grep -F -c -- "open-items" scripts/check_spec_assertions.py
git grep -F -c -- "glossary.md" AGENTS.md
git grep -F -c -- "test-prompts" AGENTS.md
```

| # | 断言 | 现状 | 到位 |
| --- | --- | --- | --- |
| A1 | 总纲建成并在 AGENTS 必读表中被指认 | 0 | ●≥1 |
| A2 | testing.md 建成并在 AGENTS 必读表中被指认 | 0 | ●≥1 |
| A3 | 宪法 §3 的假账纪律句归零（**实文为 `:17` 的「（修改 skill 后必跑）」**，非「重跑全部场景」——预验订正） | 1 | 0 |
| A4 | 宪法 §6 立「不做全域欠账清单」条 | 0 | ●≥1 |
| A5 | 总纲含「叫法权威」（术语节）／§五 路线与阶段／基础设施选型三节 | —（新建件） | ●≥3（**手工**：件不存在，`git grep` 不可作归零型，故移出命令块） |
| A6 | testing.md 含「域上线验收清单」与「已知缺口」两节 | —（新建件） | ●≥2（**手工**，同上） |
| A7 | collab-log 的核对节改为「当批开放项核对」 | 0 | ●≥1 |
| A8 | `check_spec_assertions.py` 不再引用 `open-items`（实测 2 处：常量行＋注释行） | 2 | 0 |
| A9 | AGENTS 不再引用 `glossary.md`／`test-prompts`（实测各 3 处） | 3／3 | 0／0 |

- A10 **闭包断言（机检，本批最强一道闸）**：**全仓（排除 `docs/specs/archive/` 与 `CHANGELOG.md`）不得再出现四个待退件的文件名**。命令：对 `architecture.md`／`glossary.md`／`test-prompts.md`／`open-items.md` 四个串逐个 `git grep -n`，**命中集必须为空**（`docs/specs/archive/` 与 CHANGELOG 的历史命中豁免；本规格自身因含「删件清单」而计入豁免，施工后按归档语境复核）。任何活体残留即 P0。
- A11 债分流完整性（手工）：施工方回报的**落位清单条数＝60**，且每条注明归属件；抽查 5 条核对其落位形式确在所指件内。
- A12 语义复核（手工）：总纲逐节读通，**六域地图与状态**与 `skills/` 实况一致（scan/analyze 建成、four 占位、封存声明明确）；§六 术语节与 `AGENTS.md`／`file-conventions` 的术语纪律不冲突。
- A13 机检：`bash scripts/check.sh` 退出 0（本批删除多件 → `[2]` lint 与 `[3]` 内容轨须复跑确认无断链）。
- A14 改动面：**基线＝`9bebf76` 之后**（含 056 未提交改动由本批覆盖）；删 4 件、增 2 件、改 8 类载体；`skills/` 零 diff。

## 五、规格自审记录（铁律①，按 collab-log 七项模板）

1. **锚点**：§一 引用面由 `git grep -ln` 对四件逐个实测（输出已列）；四件行数字符由 Python 实测；台账构成分桶依据 056 批已提交前的工作树分区实测；追踪档三处缺陷由 `grep -n "冻结\|^## S"` 与 S40–S47 逐行 read 实测；`architecture.md` 全文（112 行）与 `glossary.md` 的结构由 read 取得。
2. **清单完备**：新建 2 件（总纲 8 节／testing 4 节）+ 删 4 件 + 引用改指 10 类载体 + 债分流 6 类路由 + 收编追踪档 4 步 + 过程产物 4 项。**顺序耦合已写成硬约束**（§三 第一条：先吸收后删）。
3. **验收可判定**：A1–A9 为 9 条命令块断言（现状值已逐条实跑复现：0／0／1／0／0／0／0／1／1／1）；A10 为**闭包断言**（本批的核心风险面，机检可直接判定）；A11 为条数等式（60）；A12 为语义复核；A13 为脚本退出码；A14 为改动面对照。**防自噬自查**：归零型 token 为 A3（「每次修改 skill 后必须重跑全部场景」）、A8（`open-items`）、A9（`glossary.md`／`test-prompts`）——本规格 §一／§二的「待退件」引用了这些串，替换侧不含，故不互斥；A10 的四个串**在本规格文本内出现**，故其豁免规则已在断言内写明（本规格自身豁免、按归档语境复核）。
4. **矛盾检查**：与 AGENTS §2（文件账本律）一致——删台账**不减少**落盘要求（改为当批规格开放项）；与 §6「不做平行台账」**回归一致**（047 批建台账偏离了该条，本批纠正）；与 §7 体积纪律一致（总纲为热文件，已立「索引不复述」防火墙）；与 §3.9（立法须附实例）一致（债分流规则有 60 条实例）。
5. **术语**：「全域层／域层」「一层深」「闭包断言」「债跟着主题走」「闭档律」在首现处给出判据。
6. **节号复核**：引用 `AGENTS.md` §0／§2／§3／§4／§6／§7、`collab-log` 的「挂账登记核对」与「核验协议」、`file-conventions` §一命名总表与 §五 矩阵，均已 grep 核对存在。
7. **涟漪清单**：① 四件删除后，**全仓闭包**（A10）是唯一能证明无死链的手段——须在施工后立即跑；② `README` 的用户可见文案不得留旧指针；③ `check_content.py` 的 glob 覆盖需确认（若其内含四件硬编码须改）；④ 056 作废归档后，其工作树改动被本批覆盖，故 `git status` 应只见本批的删除与修改——**不得出现半覆盖态**。

## 六、开放项

1. **总纲的「产品」面深度**：本批总纲以**技术侧**为主（选型／结构／路线／术语），产品叙事（给读者看的「Abzu 能帮你做什么」）留在 `README.md`。若作者希望总纲也承担产品叙事，另批扩写（不在本批，防体积失控）。
2. **域上线验收清单的粒度**：本批按「一域 3–5 条」立骨架；大纲域之外的四域其内容待各域建设时补（**预立法，随首批实例校准**）。
3. **债分流的「已知缺口」小节形态**：本批只规定「必须有该小节 + 逐条落位」；小节内的排序／编号／清理时机留待首次清理时立例。
4. **追踪档收编（G3）与设计文档体积**：删 S 编号索引后设计文档预计缩减；若仍超 2 万字符线，N26 的体积处置另行裁定。

## 七、审查记录

**产物审查（产物审查分身，2026-09-12）**：findings **16（P0:1 P1:6 P2:9）**，结论「吸收完整性达标，引用改指未清干净」。审查用 `git show HEAD:` 取回四件全文做**逐节/逐词条对照**，并程序化核对 60 处落点集合。**吸收结论**：architecture 八节全部落地（无整节丢失，6 处定义/出处级走形）、glossary **58/58 逐条同名且定义零改写**（仅 3 条改指消歧）、test-prompts 的 [人工] 15 条落 12＋1 存疑＋2 递延。逐条处置——

- **P0-1（已采纳）**：`release-and-versioning` 的**发布流程（六步）**仍以已删的《评估场景》件为验收口径（`:39`／`:44`）——该件为本批改动面外，故漏改；属「架构级变更必须 grep 旧概念词」的靶心。整改后清 **4 处**（另 2 处为 testing 内的同类来源句）。
- **P1×6 全采纳**：① 「术语表」裸词残留——实测**6 处**（审查点名 2 处），按反模式 #2 一次清尽；② 两处「两份台账」注释与实况（现单份）不符；③ 总纲 A15 行陈述失实（「另两项已随 056／本批处置」——实测 `.gitignore` 无 `__pycache__` 规则，该项未处置）→ 如实改为「余两项待办」；④ C7 已销项复活（与本件 `:289` 的「C7 销账」矛盾）→ 删该行；⑤ **N2 无 live 落位**——其销账原记在将作废的 056，改记于 collab-log 施工记录表注「已销登记」行，并把条数口径由「60」订正为「**59 待办**（N2 与 C7 均不计入）」；⑥ **已建域 6 条 [机检] 断言整条消失**（未建域豁免不适用）→ 落 testing §2.1／§2.2 的「机检指针」＋ `file-conventions §五.3` 补两行产物契约。
- **P2×9 处置**：P2-1（未建域 13 条断言只作空递延 → testing 新增 **§2.8「未建域待启用验收点」** 逐条列表）、P2-2（「031 场景 1」为已删件内部编号 → 改按域指认「031 批大纲域走查」）、P2-4（architecture 吸收的 6 处定义/出处丢失 → 逐条补回）、P2-5（目录树补 `scripts/hooks/`、根配置三件、过渡期追踪档）、P2-6（README Roadmap 与总纲 §五 存量重复 → 记入下文开放项）、P2-7（collab-log 的 S 族编号与设计文档 S1–S47 撞号 → 节头注加消歧句）、P2-9（`scan-analyze.js` 是消费端过滤、非「三行齐全」校验 → 载体标注如实）、P2-8／P2-10 属过渡期与规划方职务（见开放项）。

**交付点核验（规划方，2026-09-12）**：三步独立重跑——改动面＝**删 4 ＋ 增 2 ＋ 改 11**（`git status` 核对；`skills/`／`CHANGELOG`／`archive/` 零 diff）；A1–A4／A7–A9 命令断言全绿；**闭包断言四串归零**（规划方独立跑，tracked 口径）；60→59 条落位清单逐条有归属件；`check.sh` 退出 0、lint 97 文件 0 违例；内容轨全绿（新增件的相对链接全部解析）。

**本批的一次流程调整（如实记）**：本批把「成稿审查」与「产物审查」**合成一轮**（省一次往返）——理由是**本批的「产物」就是文档本身**，且主要风险（引用闭合、分流落位）均可由闭包断言与条数等式机检判定。**代价实证**：合成一轮后，16 条 findings 中仍出现 P0（死指针）与 6 条 P1——说明**该合成没有降低检出质量**，但也说明**`grep` 类覆盖面的漏网（改动面外载体）靠单轮审仍会漏**，故 A10 的豁免口径已在核验时补注（见开放项）。
