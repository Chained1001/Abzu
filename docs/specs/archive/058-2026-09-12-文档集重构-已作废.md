# 058 · 文档集重构（收敛为三份）——已作废

**本规格已作废**（2026-09-13，作者裁定）：作者决定**不采用「增量收敛」**，改为**开 `rebuild` 分支从空目录重建**（「很乱了，算了，我开一个新项目吧」「重建一个新分支 rebuild，然后这个分支要求是空目录，我们重头开始做」）。本规格的施工**已启动但被作者中止**，其半成品改动已回退。**留档理由**：本规格的 §二（三份文档的结构与内容来源）与 §四（禁止编造未设计域的设计）仍是重建时的**直接参考**；其「不从零发明、把已验证机制收进三份文档」的判据亦成立。作废先例见 `archive/046-…-已作废.md`。

> **状态**：作者已口头确认方向（2026-09-12），本规格为施工依据
> **依据**：作者口述——「我需要一份产品文档（用作产品和开发使用，包含全流程的设计，还没有设计的可以留空待设计），我还需要一套标准规范文档，还需要一套施工机制。就这么多，你就当现在的项目是一个空文件夹，你重头开始弄，修改太麻烦了，还不如重头写……项目文档是最优先级高的，内部应该包含设计流程，项目文件树等等很详细的东西，我甚至拿给其他 ai，他根据这个项目文档就知道该怎么构建项目，知道我的意图」
> **实现逻辑**：**不是从零发明**——把 2026-09-12 地基重构后的件**收敛为三份**（内容沿用已验证部分，重排读者面与层级）。**第一读者是「其他 AI」**：三份文档须自足到「据此可独立构建本项目」。
> **自由度标记**：`[A]` 逐字执行 ｜ `[B]` 按方向自定实现 ｜ `[C]` 按验收标准核验

## 一、目标文件集（就这么多，不再多一个）

```text
AGENTS.md                 # 入口与路由（短）：项目一句、红线、去哪读、常用命令
README.md                 # 用户向：安装与使用
CHANGELOG.md              # 沿革
docs/产品文档.md           # ① 产品＋开发两用
docs/规范.md               # ② 标准规范
docs/施工机制.md           # ③ 施工机制
docs/大纲阶段设计文档.md    # 唯一详档（产品文档 §二 索引它）
docs/specs/archive/       # 历史规格（沿革，只读）
skills/  scripts/  .claude-plugin/
```

## 二、三份文档的结构与内容来源

### 1. `docs/产品文档.md`（由 `docs/product/总纲.md` **升格 + 补两节**）[B]

九节，节序如下（原八节的**内容整节搬迁、不重写**；新增两节）：

1. **一 这是什么**——（原 §一）意图与定位／目标用户／形态（Claude Code skill 套件，`npx` 安装、本名即命令）／边界与不做／当前阶段与**封存域声明**。
2. **二 全流程设计（六域）**——**新增，本规格的核心**：
   - 先给**总览表**（域｜skill 名｜状态｜一句话职责）+ 域间关系（**平行的门**：跨域零调用、只经落盘文件衔接）。
   - 再**每域一节**，统一字段（缺一不可）：**目标 ｜ 何时用 ｜ 输入（上游产物）｜ 步骤与门控 ｜ 产物（落点与文件名）｜ 状态与续跑 ｜ 设计状态**。
   - **大纲域**：已设计 → 按《大纲阶段设计文档》提炼**骨架摘要**（五章：规格步／资料收集步／草稿步／打磨与定稿步／横切机制 + 门控总表 + 书目录结构），并加指向详档的指针。**摘要只提炼，不改写详档**。
   - **卷纲域／正文域／文风域**：**留空待设计**——每节保留上述七字段的**空表骨架** + 明确标「**待设计**」（可写已知的意图要点，但**不得编造设计**；判据：凡详档里没有的步骤/产物名一律不写）。
   - **扫榜调研域／拆书分析域**：已建成但**封存** → 写「已建成」+ 封存理由（作者 2026-09-12 裁定）+ **解冻条件**。
   - **跨域约定**：书项目文件树（书目录结构，含各产物与状态文件）、共享资产（跨阶段资产）、命名约定——三者各一小节。
3. **三 项目文件树**——**新增**（合并原 §二 的 `skills/` 树与原 §4.4 的仓库树并扩写）：**本仓完整树 + 每个文件一句话职责**（详细到「一个 AI 照此建目录」）。须包含 `docs/` 三份文档、`skills/` 六域、`scripts/` 守卫、`.claude-plugin/`、根配置件。
4. **四 设计理念**（原 §三）／5 **五 基础设施选型**（原 §四：承载架构与拆分触发器／精准入口／部署与实测循环／质量闸分层／版本与兼容）／6 **六 路线与阶段**（原 §五：近期／中期／构想池三表）／7 **七 外部对标与规范依据**（原 §六）／8 **八 术语**（原 §七，叫法权威）／9 **九 维护**（原 §八 + 闭档律）。

### 2. `docs/规范.md`（合并 `docs/standards/` 现有 **7 件**）[B]

逐节（节序即下表），**条文与实例逐条保留、不新增不删减实质内容**：

| 节 | 来源件 |
| --- | --- |
| §一 命名与目录 | `file-conventions.md` |
| §二 Markdown 风格 | `markdown-style.md` |
| §三 skill 内容设计 | `skill-writing.md` |
| §四 规格写作 | **新建**——从 `collab-log` 的规格模板与 `AGENTS.md` §3.1 抽取（规格三要素／自审七项／自由度标记／断言写法） |
| §五 测试与验收 | `testing.md` |
| §六 版本与发布 | `release-and-versioning.md` |
| §七 Agent 设计 | `agent-design.md` |

**要求**：① 去掉各件的版本行与相互引用，改为**本文内节引用**（如「见 §一.3」）；② 文末增**「机检锚点」表**——列出守卫脚本按路径或模式扫描的条目（如 `check_content.py` 的加粗密度/引用闭合、`check_spec_assertions.py` 的静态四检查），注明「锚点已内化于脚本判据，非本文字面」；③ `docs/standards/` 目录**整目录删除**。

### 3. `docs/施工机制.md`（合并 `collab-log.md` 的**协议部分**）[B]

逐节：① 角色与职责（规划方／施工方／作者）② 规格流程（何时需规格·判定表／三要素／自由度标记／自查）③ 施工与验收（核验三步／断言预验／过程产物三件／挂账落点）④ 机器闸（守卫全景，指向 `规范.md §五`）⑤ 审查（成稿审查分身／产物审查分身／报告落点／止损——全文审上限 2 轮、P0≥2 后只核修复面）⑥ 偏差与异常（P0／P1 分级、停摆呈报、失败回滚、多会话并发）⑦ 提交与登记（Conventional Commits／提交前自检／记录去向）⑧ **模板**（施工提示词／核验请求／整改提示词／审查报告——**逐字保留**）。

**要求**：旧 `collab-log.md` 的 **43 行施工记录不并入**（历史）；原件**移入 `docs/specs/archive/` 保留**（作沿革）。

## 三、入口与目录改动

| 动作 | 对象 |
| --- | --- |
| 移动 | `docs/product/总纲.md` → `docs/产品文档.md`（随后补两节）；`docs/product/大纲阶段设计文档.md` → `docs/大纲阶段设计文档.md`；`docs/product/` 目录随之消失 |
| 删除 | `docs/standards/` 整目录（7 件已并入） |
| 归档 | `docs/specs/collab-log.md` → `docs/specs/archive/collab-log-2026-09-12-归档.md`（协议已并入；原件的 43 行记录作为历史保留） |
| 改写 | `AGENTS.md` → **短入口**（目标 80–120 行）：项目一句／红线（含 git 只读、临时产物、外部参考只读、失败先判因、密钥）／**三份文档的路由表**（什么内容去哪份）／常用命令／不做清单；**§3 开发节奏与 §5 反模式清单下沉到 `施工机制.md`**（AGENTS 只留触发条件与指针） |
| 更新 | `README.md` 的文档指针三处改指；`scripts/check_content.py` 与 `check_spec_assertions.py` 若引旧路径则同步（**判据与行为一字不改**） |

## 四、禁止事项

- **禁止编造未设计域的设计**（卷纲／正文／文风三节留空 + 「待设计」标记；判据见 §二.1.2）。
- **禁止删减既有规范条文与实例**（合并是搬迁，不是精简）；禁止改写详档（大纲设计文档）正文。
- **禁止留双份**（合并即删原件）；禁止改 `skills/`；禁止改 `docs/specs/archive/` 既有件；禁止 git 写操作；禁止联网。
- 禁止把「施工记录」类台账**重新引入**（记录去向：git + CHANGELOG + 过程产物三件）。

**偏差分级**：P0 阻塞型 → 停摆呈报；P1 观察型 → 继续并记入核验请求。

## 五、验收标准

```bash
git grep -F -c -- "docs/产品文档.md" AGENTS.md
git grep -F -c -- "docs/规范.md" AGENTS.md
git grep -F -c -- "docs/施工机制.md" AGENTS.md
git grep -F -c -- "待设计" docs/产品文档.md
git grep -F -c -- "全流程设计" docs/产品文档.md
git grep -F -c -- "项目文件树" docs/产品文档.md
git grep -F -c -- "规格写作" docs/规范.md
git grep -F -c -- "核验三步" docs/施工机制.md
git grep -F -c -- "施工提示词" docs/施工机制.md
git grep -F -c -- "file-conventions" docs/规范.md
```

| # | 断言 | 到位 |
| --- | --- | --- |
| A1 | AGENTS 路由三份文档齐（各 ≥1） | ●≥1 ×3 |
| A2 | 产品文档 §二 全流程设计＋「待设计」标记齐 | ●≥1 ×2 |
| A3 | 产品文档含「项目文件树」节 | ●≥1 |
| A4 | 规范.md 含「规格写作」节（新建节） | ●≥1 |
| A5 | 施工机制.md 含「核验三步」与「施工提示词」 | ●≥1 ×2 |
| A6 | 旧件名在 `docs/规范.md` 内**不作为自我指认**出现（`file-conventions` 等零命中——合并后不再自称旧件） | 0 |
| A7 | `docs/standards/` 与 `docs/product/` 目录不存在；`docs/产品文档.md`／`docs/规范.md`／`docs/施工机制.md`／`docs/大纲阶段设计文档.md` 四件存在 | 手工 + `ls` |
| A8 | `AGENTS.md` 行数 ≤ 150 | ● |
| A9 | `bash scripts/check.sh` 退出 0（守卫若引旧路径须同步） | 0 |

- A10 闭包（手工）：全仓（排除 `docs/specs/archive/` 与 `CHANGELOG.md`）对旧件名（`docs/standards/`、`总纲.md`、`collab-log.md`、`大纲阶段追踪档`）**零活体命中**。
- A11 自足性抽查（手工，**本批的核心目标**）：请审查分身**只读 `docs/产品文档.md`**，回答「照这份文档，你会怎样建这个项目？六域各要做什么？文件建在哪？」——回答若与项目实况不符，即为不达标（**这是「给其他 AI 看」的可执行验收**）。

## 六、开放项

1. **设计详档的去留**：本批把《大纲阶段设计文档》保留为独立详档并由产品文档索引（一层深）。若作者希望连详档也并入产品文档，产品文档将超 2 万字符线，须先做体积压缩（宪法 §7 处理序）。
2. **规范.md 的体量**：7 件合并后预计 2.5–3 万字符（超 `AGENTS.md` §6 的 2 万线）——本批按「单一真源优先于体积」处置；若作者要求压到阈值内，另批做「冷路径下沉」。
3. **旧记录的去留**：`collab-log` 的 43 行施工记录随件归档（不作新台账）。
