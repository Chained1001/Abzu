# 060 附件｜基座审计报告（两轨）

> 定位：060 批两轨独立审计报告原文，供回溯「当时为什么这么判」。主规格见 [060-2026-09-12-基座审计整改.md](060-2026-09-12-基座审计整改.md)；本附件随主规格验收后同批移入 `docs/specs/archive/`（移动不留副本）。
> 审计方式：两个 fresh 只读分身分轨审计（审计面＝`AGENTS.md` ＋ `docs/standards/` 11 份 ＋ `docs/specs/施工机制.md`；分支 `rebuild`，HEAD＝`8f3c29b`）。规划方对每条 findings 逐条复跑复核，误报与分级调整记于主规格 §七。
> 处置列指向主规格 §2.1 的改动项编号；「已覆盖」表示本仓现状已满足、无需改动。

## 一、结构轨（findings 11 条）

| 编号 | 文件:行 | 事实与证据 | 处置 |
| --- | --- | --- | --- |
| SP1-1 | `文档写作标准.md`:87、:88、:89 | 机检锚点表引用「§一.6 引号三分与 §二.3 标点不叠用」「§四 状态符号白名单」「§五 编码」——该文 H2 仅「一、条文／二、正例与反例／三、机检锚点」，无 §四／§五；§二 为无编号示例列表，无 §二.3。证据：`grep -n "^## " docs/standards/文档写作标准.md` | F6 |
| SP1-2 | `规格写作标准.md`:40、:93；`施工机制.md`:48、:180 | 同一规格节的节名与项数两处互斥权威（七项 vs 三条）；按 `施工机制 §八` 模板产出的规格（059 归档件即「规格自审（三条）」）恒不满足 §4 七项与 `check.sh` [4] 待建的机检 | F1 |
| SP1-3 | `skill形态标准.md`（全文 168 行） | 首个 H2 即「一、条文」，头部仅定位／依据两行，无节目录；违反 `文档写作标准 §一.3` 与同文 :86 机检锚点行。证据：`wc -l` ＝168 | F10 |
| SP1-4 | `命名标准.md`（全文 117 行） | 同上（H2 为 一／二／三，无目录）。证据：`wc -l` ＝117 | F10 |
| SP1-5 | `版本与分发标准.md`（全文 106 行） | 同上（H2 为 一／二／三，无目录）。证据：`wc -l` ＝106 | F10 |
| SP2-1 | `产物契约标准.md`:52 | `## 四、维护` 是该文第 4 个 H2，11 份标准中唯一在机检锚点表之后再挂内容者；同类维护条在另四份均落 §一 末节 | F12 |
| SP2-2 | `AGENTS.md`:3–:7 | 头注三段引用块、无「依据」行，与 `skill形态标准 §13` 矩阵「治理与门面文档…头注两行式（定位／依据）」不符 | F11 |
| SP2-3 | `测试与验收标准.md`:10、:25、:32、:34 | `1.1`／`1.2`／`1.3`／`1.4` 非 Markdown 有序列表标记（以 `^\s*\d+\.` 为行首编号正则扫描，零命中），渲染为普通段落；11 份标准中仅此件如此 | F20 |
| SP2-4 | `skill形态标准.md`:3、:11、:16、:131；`施工机制.md`:222 | 裸文件名未用反引号包裹（同表 :132–:134 均为反引号形态，:131 破例）；违 `文档写作标准 §一.3.7` | F21 |
| SP2-5 | `命名标准.md`:25 对 :44 | §1 目录树与 §2 命名总表口径互斥：总表要求开发守卫为 `check-{对象}`，实件与目录树为无对象段的 `check.sh` | F22 |
| SP2-6 | `文档写作标准.md`:43、:47 对 11 张机检锚点表 | 白名单范围未收口：按 :43 字面，11 张「条文｜强制方式｜状态」表的状态列取值（`已实现`／`待建`／`无守卫（人判）`）不在白名单内；:47「本仓尚无使用状态符号的清单或状态列」已过时 | F23 |

**已核清（结构轨）**：四段骨架 11/11 齐备（第 3 行定位、第 4 行依据、三节名逐字一致）；H3 编号 1–13／1–9 全连续无跳号重号；有序列表起于 1 步进 1；表格内编号 G1–G13／V1–V3／反模式 #1–#11 连续；11 张机检锚点表表头逐字一致；13 文件无 H4 及以上、每文件恰一个真 H1、H1 与文件名逐字一致 12/13（AGENTS 例外经 059 裁定保留）；`施工机制.md` 围栏内第二个 H1 非真标题、围栏成对；相对链接 75 条断链 0；`施工机制.md` 目录 8 条与 H2 逐字一致；体积最大 `施工机制` 229 行／18468 字符，未触 300 行拆分线与 2 万字符解冻阈值；13 文件无 BOM／CRLF、单一尾换行、无 HTML 注释、无 `*`／`+` 列表符、无弯引号、表格竖线已转义；状态列无名单外符号；`check.sh` 头注段序 [0]–[5] 与 `测试与验收标准 §1.1` 逐段一致。

## 二、内容轨（findings 17 条）

| 编号 | 文件:行 | 事实与证据 | 处置 |
| --- | --- | --- | --- |
| CP1-1 | 全仓对「立法质量」 | 宪法级立法质量条（新增规范条目须附正例或反例；无实例条文标注「预立法」）在 rebuild 零落点，而「预立法」标记被 7 处使用（`skill形态标准`:52、`命名标准`:45、`文档写作标准`:27／:47、三份域设计）——标记成为无定义的黑话。证据：`grep -rIn "正例或反例\|立法质量" AGENTS.md docs/` 零命中；`git show main:AGENTS.md` 有原文 | F2 |
| CP1-2 | `规格写作标准.md`:40 对 `施工机制.md`:48、:180 | 自审判据两套（三条 vs 七项）且节名两样，两套互不为子集（三条的「禁止事项写明」不在七项内；七项的「术语／节号复核／涟漪清单」不在三条内） | F1 |
| CP1-3 | `skill形态标准.md`:164、:167；`文档写作标准.md`:85、:87 | 机检锚点节号语义对不上目标条文：TOC 条实为 §5.3（§5.2 是超限处理序、§6 是文件结构模板）；「引用闭合」实为 §9；加粗密度实为 §一.3.8（§一.8 是「无 HTML 注释」，同表 :90 已占用，两行撞号）；引号三分实为 §一.2.6 | F6 |
| CP1-4 | `触发与交互标准.md`:34–39、:69 | §4「准入律（规格字段）」整节是立项域业务规则（三问段式／终局口径与感情线／收拢式重问），破底座可移植定位；未被 059 断言 2 的 token 集捕获（属反模式 #2 近义词漏扫同型） | F3 |
| CP1-5 | `安全与合规标准.md`:11、:40 | 「灵感库」是已停摆旧域件的业务概念，产品层八份与术语表均无此词条——条文唯一实例悬空。证据：`grep -rIn "灵感库" docs/product` 零命中；`git grep -c 灵感库 main` → 旧域件 14 处 | F4 |
| CP1-6 | `命名标准.md`:54、`术语标准.md`:22、`术语表.md`:25／:27、`skill形态标准.md`:132 | T1 被当规范触发条件用而无定义；且旧仓 T1 至少两义（触发档位／引用分级 T1，后者见 `main:docs/standards/file-conventions.md`:95 与 `main:docs/product/总纲.md`:207），使用者无法判定取哪义 | F5 |
| CP1-7 | `运行环境标准.md`:4／:11／:12／:14；`安全与合规标准.md`:16／:17；`触发与交互标准.md`:4；`架构设计.md`:30 | 《评估场景》《架构决策记录》《术语表》三件在 `main` 存档中不存在（`git -c core.quotepath=false ls-tree -r --name-only main` 实测无此三件），出处不可按件核；`架构设计 §四` 更指示「去 main 取《架构决策记录》」，该动作无法执行（内容在 `总纲.md`） | F7 |
| CP1-8 | `scripts/check.sh`:27–31 对 `运行环境标准.md` §5、`测试与验收标准.md`:23 | 同一门禁脚本两套退出码口径（五分类 vs 二值），实件按二值实现（`fail=1` → `exit 1`），§5 的「`1` 与 `2` 的判别看调用方动作」在唯一实件上不成立 | F9 |
| CP2-1 | `施工机制.md`:89 | 审查判据维度标号 `③b`／`⑥` 在新基座无定义（旧仓「审查维度七分」矩阵未随条文迁移），读者无法判定边界 | F16 |
| CP2-2 | `施工机制.md`:99 | 止损条 3「触发条件放宽为任意 P0 族」丢失被放宽对象，与同节条 2「P0 ≥ 2」并读时像互斥 | F17 |
| CP2-3 | `skill形态标准.md`:39、`触发与交互标准.md`:11、`架构设计.md`:19 | 「未实测」状态多载体而回写指令只覆盖一处，首个域落地实测后易漂移（反模式 #4 同型） | F24 |
| CP2-4 | `安全与合规标准.md`:22／:44 对 `书目录契约.md`:31 | 同一状态标注两种括注（底座「公开样本」／产品层「试读段」），术语表未定义「锚定」 | 已覆盖 |
| CP2-5 | `AGENTS.md`:24–28、:53 | ① 路由表无「查沿革」入口（全文 CHANGELOG／沿革零命中，而 CHANGELOG 是四处规范的硬落点）；② doc-budget 解冻判据引「热路径文件／行数上限」，两者全仓无定义 | F18／F19 |
| CP2-6 | `施工机制.md`:149 | commit scope 示例 `outline` 是旧仓坐标系（现行域名为 `abzu-outline`，唯一真源 `架构设计 §一`） | F15 |
| CP2-7 | `版本与分发标准.md`:74（V2） | 「四处一致现只能覆盖三处」与实况不符（四处载体现存 0 处：无 tag、无 `.claude-plugin`、`CHANGELOG` 仅 `## [Unreleased]`、`skills/` 未建） | F14 |
| CP2-8 | `运行环境标准.md`:13 | §1.4 以现状陈述句写未实现事实（「探测链固化在 `scripts/check.sh` 内」而实件 39 行、仅 `[0]` 与 `[1]` 两段、无 python 探测）；同类未实现处有显式标记，口径不一 | F13 |
| CP2-9 | `AGENTS.md`:18 对 `测试与验收标准.md`:30 | 同一条「禁带病开工」两套判据（机器全绿 vs 无已知失败＋基线豁免），AGENTS 未指向基线定义 | F8 |

**已核清（内容轨）**：残留扫描逐词实跑（`collab-log`／六域／`abzu-scan`／`abzu-analyze`／`abzu-style`／mo-shu／总控／单技能 均零命中；`施工记录表` 3 处全为否定句；`协作日志` 2 处为出处行）；指向闭合（相对链接 0 断链；60 余处节号引用除 CP1-3 所列外全部命中）；一致性六组（调用控制键同源、三条路径与判定表逐行对齐、过程产物两项同口径、体积数值同源、门禁三处命令逐字一致）；状态标记与 `check.sh` 逐段核对；可执行性与空转逐条看过；红线与不做清单 12 条无抵触；`CHANGELOG` 条目 180 字符、术语表 38 词条、正文域机检点 10 条与产品层表述一致。
