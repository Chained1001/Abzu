# 060-2026-09-12-基座审计整改

> 定位：处置 059 之后对基座（`AGENTS.md` ＋ `docs/standards/` 11 份 ＋ 施工机制）的结构轨与内容轨双轨审计 findings——28 条（结构轨 11／内容轨 17，去重归并为 24 条改动项），按类整改，并逐条标注采纳／拒绝与落位（见 §2.2）。
> 自由度口径：[A]＝源文本即目标文本（整段搬运、仅链接路径改写）｜[B]＝按条目所述方向改写，完整性由 §三 断言判定｜[C]＝新写。
> 必读输入：`AGENTS.md`、`docs/standards/` 全部 11 份、`docs/specs/施工机制.md`、`docs/product/架构设计.md`、`docs/product/大纲域设计.md`、`docs/standards/测试与验收标准.md`；审计证据见同批附件 [`060-…-审计报告附件.md`](060-2026-09-12-基座审计整改-审计报告附件.md)
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**审计基线**：分支 `rebuild`，HEAD＝`8f3c29b`，审计时工作树干净。两条独立只读分身分轨审计（结构轨 11 条、内容轨 17 条），规划方逐条复跑取证；本规格的改动项与断言均以复跑值为准。

**main 存档（出处可核性的判据来源）**——`git -c core.quotepath=false ls-tree -r --name-only main` 实测，除 `docs/specs/archive/` 外仅：

- 根级：`AGENTS.md`、`README.md`、`CHANGELOG.md`、`LICENSE`、`.gitattributes`、`.gitignore`、`.markdownlint-cli2.jsonc`、`.claude-plugin/marketplace.json`
- `docs/product/`：`大纲阶段设计文档.md`、`大纲阶段追踪档.md`、`总纲.md`（**无**《评估场景》《架构决策记录》《术语表》三件）
- `docs/standards/`：六份**英文名**旧件 `agent-design.md`／`file-conventions.md`／`markdown-style.md`／`release-and-versioning.md`／`skill-writing.md`／`testing.md`
- `docs/specs/collab-log.md`；`scripts/`（`check.sh`／`check_content.py`／`check_spec_assertions.py`／`hooks/pre-commit`）；`skills/` 六域

件名映射（底座引用名 → main 实际件，核对通过、本批不改）：《项目宪法》→`AGENTS.md`；《文件规范》→`file-conventions.md`；《Markdown 写作规范》→`markdown-style.md`；《Skill 写作规范》→`skill-writing.md`；《守卫与验收规范》→`testing.md`；《版本与发布规范》→`release-and-versioning.md`；《协作日志》→`collab-log.md`；《大纲阶段设计文档》→`docs/product/大纲阶段设计文档.md`。

**预验基线**（下述断言在改动前的实测值）：

| 编号 | 判据 | 基线 |
| --- | --- | --- |
| A1 | 底座内「七项」 | 2 处（`规格写作标准.md`:40、:93） |
| A2 | 底座内「正例或反例」 | **0 处**（旧仓 `main:AGENTS.md` 有 1 处） |
| A3 | `触发与交互标准.md` 业务词（终局口径／感情线／收拢式／三问段式） | 4 处（:36、:37、:38、:69） |
| A4 | 底座内「灵感库」 | 2 处（`安全与合规标准.md`:11、:40） |
| A5 | 底座＋`架构设计.md` 内「T1」 | 4 处（触发档位义 3 处：`命名标准.md`:54、`术语标准.md`:22、`架构设计.md`:30；引用分级义 1 处：`skill形态标准.md`:132） |
| A6 | 三处不可核出处件名（《术语表》《评估场景》《架构决策记录》） | 10 处（`安全与合规标准.md`:4／:16／:17、`术语标准.md`:4、`触发与交互标准.md`:4、`运行环境标准.md`:4／:11／:12／:14、`架构设计.md`:30） |
| A7 | 错锚点字样（`§5.2／§6`／`§1.2 引用闭合`／`§一.8 加粗密度`／`§一.6 引号三分`／`§四 状态符号白名单`／`§五 编码`） | 6 处（`skill形态标准.md`:164、:167；`文档写作标准.md`:85、:87、:88、:89） |
| A8 | 超 100 行却无节目录 | 3 份（`skill形态标准.md` 168 行、`命名标准.md` 117 行、`版本与分发标准.md` 106 行） |
| A9 | 底座内「热路径」（无定义） | 1 处（`AGENTS.md`:53） |
| A10 | `AGENTS.md` 路由表行数（不含表头） | 3 行；全文「CHANGELOG／沿革」命中 **0** |
| A11 | `施工机制.md` 审查维度编号 `③b` | 1 处（:89） |
| A12 | `测试与验收标准.md` `1.1` 式伪编号 | 4 处（:10、:25、:32、:34） |
| A13 | `产物契约标准.md` 的 `## 四、维护` | 1 处（:52） |
| A14 | `未实测` 标记载体 | **字面 2 处**（`skill形态标准.md`:39、`触发与交互标准.md`:11）；另 `架构设计.md`:19 有同源实测口径无标记 |
| A15 | 底座相对链接 | 104 条、断链 0；`check.sh` 退出 0（24 文件 lint 0 违例） |

## 二、文件级改动清单

### 2.1 改动项（按类归并；编号 F1–F24）

### A 类｜旧仓残留与去业务化不彻底

| # | 文件:位置 | 改动（现状 → 目标） | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `规格写作标准.md`:40–48、:52、:77、:93；`skill形态标准.md`:138；`施工机制.md`:48、:180 | 规格自审**七项 vs 三条**互斥：以**三条**为真源（与 `施工机制 §二`／§八 模板／059 实践一致），`规格写作标准 §4` 收敛为三条；**七项中的有效项并入三条括注**——「节号复核」与「术语首现」归入条 1（锚点现场核过）射程，「涟漪清单」作为域建设批附加句保留；`:93` 锚点行改「§4 自审三条齐备」；**同义名全扫**：四处「自审记录」统一为「规格自审」（`skill形态标准.md`:138、`规格写作标准.md`:4／:52／:77）；§4 加一句「与 `施工机制` §二 同源，本件不复制细则（单一真源）」 | 结构轨 P1-2 ＋ 内容轨 P1-2 ＋ 成稿 P1／P2 | [B] |
| F2 | `规格写作标准.md` §一 末新增两条（**现条 1–7 编号不动，顺延为条 8／条 9**——禁止插在 §4 前：那会使 §4–§7 全体顺延、连锁改断全仓对 §6／§7 的引用） | **立法质量两条补回**（旧仓宪法 §3.9，rebuild 零落点）：① 新增或修改规范条目须至少附一个正例或反例；无实例的条文标注「预立法，随首批实例校准」（标注者＝立法批，首批实例落地后由该批校准并删去标记）。② 治理文档条文不写批次叙事与裁定标记（立例与例外授权依据除外），沿革记 `CHANGELOG`。**过渡句**（本批自身即新增／修改条文而未附例，属一次性例外，下一批起全面适用） | 内容轨 P1-1＋P2-6 | [B] |
| F3 | `触发与交互标准.md`:3、:34–39、:69；`docs/product/大纲域设计.md`:17 | §4「准入律（规格字段）」整节是立项域业务规则 → 迁入 `docs/product/大纲域设计.md`（新增「域交互细则」节，[A] 逐字）；同时**声明与 `大纲域设计.md`:17 既有准入律条的关系**（:17 为验收要点、新节为其流程细则，:17 加「（细则见本节『域交互细则』）」消解双载体）；底座该节改一行指针；`:69` 锚点行同步改指；**:3 定位行的「准入律」字样**同步改为「（域内准入律实例见产品层）」 | 内容轨 P1-4＋P1-9 | [B] |
| F4 | `安全与合规标准.md`:11、:40 | 「灵感库」实例在底座与产品层均无对应概念 → 改通用表述「外部素材库位置由环境变量指认」（:40 同）；:11 括注里的旧仓接口说明同步去业务名 | 内容轨 P1-5 | [B] |
| F5 | `命名标准.md`:54、`术语标准.md`:22（触发档位义）；`skill形态标准.md`:132（**引用分级义，分流处置**）；`架构设计.md`:30、§四 | T1 两义被混用：①**触发档位 T1**（运行时组件物化）无定义 → `命名标准.md`:54 与 `术语标准.md`:22 各加「（触发档位定义见 `docs/product/架构设计.md` §四）」；`架构设计.md §四` 补 T1／T2／T3 三档简定义（[B] 摘写自 `main:docs/product/总纲.md`:80，保留「选型理由待迁移」标注）。②`skill形态标准.md`:132 的「旧仓 T1 元规范」是**引用分级 T1**（T0 官方规范／T1 官方实物与社区标杆／T2 目录评测），改写为「旧仓 superpowers 元规范（引用分级 T1）」以免误指 | 内容轨 P1-6 ＋ 成稿 P0 | [B] |

### B 类｜指向失准

| # | 文件:位置 | 改动（现状 → 目标） | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F6 | `skill形态标准.md`:164、:167；`文档写作标准.md`:85、:86、:87、:88、:89、:90、:91 | 机检锚点节号锚错（**根因：该文「§一.N」有时指子节、有时指 §一.1 的条**）→ 统一约定「子节用 `§一.N`、条用 `§一.{子节}.{条}`」并逐行改正：`skill形态标准.md`:164→`§5.3`、:167→`§9`；`文档写作标准.md`:85→`§一.3.8`、:86→`§一.1.3`、:87→`§一.2.6`＋`§一.2.3`、`§88`→`§一.4`、:89→`§一.5`、:90→`§一.1.8`、:91→`§一.1.5` | 结构轨 P1-1 ＋ 内容轨 P1-3 ＋ 成稿 P1 | [B] |
| F7 | `术语标准.md`:4；`运行环境标准.md`:4、:11、:12、:14；`安全与合规标准.md`:4、:16、:17；`触发与交互标准.md`:4；`架构设计.md`:30 | 不可核出处件名改指可核件：《术语表》→`main:docs/product/总纲.md` §七；《评估场景》→`main:AGENTS.md` §0 必读文档与环境约定段；《架构决策记录》→`main:docs/product/总纲.md`（并在 `架构设计.md`:30 写明「旧仓未单独成件，内容在 `总纲.md`」）；可核的六件名（件名映射见 §一）不改 | 内容轨 P1-7 | [B] |
| F8 | `AGENTS.md`:18（红线 6） | 补指针：「基线定义见 [`测试与验收标准`](../../standards/测试与验收标准.md) §1.2（建设期内未建设能力的失败不计入红基线）」 | 内容轨 P2-9 | [A] |

### C 类｜口径收口

| # | 文件:位置 | 改动（现状 → 目标） | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F9 | `运行环境标准.md` §5 表前（**:46 表格前新增一句**，§5 标题后现直接是表格） | 退出码五分类与 `check.sh` 二值实况冲突 → 加适用范围限定：「本契约适用于 skill 运行时脚本（`skills/` 域脚本）；仓库开发守卫 `scripts/check.sh` 为二值闸——`0` 全绿／`1` 任一段红或环境缺失，不适用本节五分类」 | 内容轨 P1-8＋P2-2 | [B] |

### D 类｜结构合规

| # | 文件:位置 | 改动（现状 → 目标） | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F10 | `skill形态标准.md`、`命名标准.md`、`版本与分发标准.md` 头部；`文档写作标准.md`:12 | 三份超 100 行文件补 `## 目录`（手动列表，条目与 H2 标题逐字一致，置于定位／依据两行之后、`## 一、条文` 之前）；**同步把 `文档写作标准.md`:12 的「超过 100 行的参考文件」改为「超 100 行文件」**（与该文 :86 机检锚点行逐字一致；**:86 措辞保持不动**）；`skill形态标准.md`:54／:61 的 reference 专属措辞不改（其管辖面本就是 references） | 结构轨 P1-3／4／5 ＋ 成稿 P2 | [B] |
| F11 | `skill形态标准.md`:137 矩阵行 | `AGENTS.md` 头注为三段且无依据行、`README.md` 无头注，均与矩阵「治理与门面文档…头注两行式」不符 → 在该矩阵行登记例外：「入口类文件（`AGENTS.md`／`README.md`）头注可为自由形态——其读者是首次进入者，须先答『这是什么仓库、开发与运行如何分工、业务在哪』；其余治理文档仍两行式」 | 结构轨 P2-2 ＋ 成稿 P1 | [B] |
| F12 | `产物契约标准.md`:52–:55 | 唯一多挂的 `## 四、维护` 节 → **追加为 §一 末节 `### 4. 维护`**（与 `命名标准` §一.6／`文档写作标准` §一.7／`术语标准` §一.4／`版本与分发标准` §一.9 同形），只搬 :53–:55 两条，其余原样保留 | 结构轨 P2-1 ＋ 成稿 P1 | [A] |

### E 类｜时效性陈述

| # | 文件:位置 | 改动（现状 → 目标） | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F13 | `运行环境标准.md` §1.4；`scripts/check.sh` 头注 | §1.4 以现状陈述句写未实现事实 → 改「探测链**将**固化在 `scripts/check.sh`（现未实现，见 §三 机检锚点）」；`check.sh` 头注补「依赖与前置」行（Node.js；npx 缺失即置红），凑齐脚本文档头四要素 | 内容轨 P2-8 | [B] |
| F14 | `版本与分发标准.md`:74（V2）、:99 | 「四处一致现只能覆盖三处」与实况不符（四处载体现存 0 处） → :74 改「四处载体（已建壳、插件清单、tag、`CHANGELOG` 版本段）均未落实；首个发布批逐项建齐」；**:99 机检锚点行的「§2 四处（现三处）版本号一致」同步改「§2 四处版本号一致（载体均未落实）」** | 内容轨 P2-7 ＋ 成稿 P1 | [B] |
| F15 | `施工机制.md`:149 | commit scope 示例 `outline` 是旧仓坐标 → 改为「单域建设＝域名（见 `docs/product/架构设计.md` §一 域清单，如 `abzu-outline`）」 | 内容轨 P2-6 | [B] |

### F 类｜可读性与判据定位

| # | 文件:位置 | 改动（现状 → 目标） | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F16 | `施工机制.md`:89 | 审查维度编号 `③b`／`⑥` 无定义（旧仓七维矩阵未迁移） → 去编号改名词：「两个维度——内容质量（语义通顺／逻辑自洽／数字一致／无断裂截断，重点看替换边界与新旧文衔接处）；规范健康度（本次改动是否使既有条文被现实违反或过时）」 | 内容轨 P2-1 | [A] |
| F17 | `施工机制.md`:99 | 止损条 3「触发条件放宽为任意 P0 族」丢了被放宽对象 → 补：「本条 1／2 的触发条件由原『同类精度 P0（计数／断言互斥／范围失控三类）』放宽为任意 P0 族」 | 内容轨 P2-2 | [A] |
| F18 | `AGENTS.md` 路由表 | 缺「查沿革」入口（全文 CHANGELOG／沿革零命中，而 CHANGELOG 是四处规范的硬落点） → 补一行「某个决定／某次变更是怎么回事（沿革）→ [`CHANGELOG.md`](../../../CHANGELOG.md) 与 git 历史」 | 内容轨 P2-5 | [A] |
| F19 | `AGENTS.md`:53 | doc-budget 解冻判据引「热路径文件／行数上限」两者全仓无定义 → 补指针并就地括注定义：「（热路径＝触发即全量加载的文件；行数上限见 `skill形态标准` §5）」 | 内容轨 P2-5 ＋ 成稿 P1 | [A] |
| F20 | `文档写作标准.md` §一.1 末（**结构规则节，非 §一.5**）；`测试与验收标准.md`:10、:25、:32、:34 | `1.1` 式伪编号（渲染为普通段落，且仅此一件如此）→ 本批取**立条**：在 §一.1 增「条文分组可用 `章.节[.条]` 前缀标记（如 `1.1`／`1.2.3`），机检锚点表以该前缀引用」；实件与既有 N 处引用不动（**与 F6 的两级／三级形式统一为同一约定**） | 结构轨 P2-3 ＋ 成稿 P1 | [B] |
| F21 | `skill形态标准.md`:3、:11、:16、:131；`施工机制.md`:222 | 五处**裸文件名**（如 `SKILL.md`／`references`／`AGENTS.md`；:11 的 `{skill 名}` 已带反引号，裸名是同行的其余词）未用反引号包裹 → 按 `文档写作标准 §一.3.7` 给裸名补反引号，只加反引号、不动其余字符。**本项降为 [B]**（同行局部改动，无整行逐字替换文本） | 结构轨 P2-4 ＋ 成稿 P1 | [B] |
| F22 | `命名标准.md` §2 命名总表 | 总表要求开发守卫为 `check-{对象}`，而实件与 §1 目录树是固定名 `check.sh` → §2 补一行「一键入口｜`check.sh`（固定名）｜—｜仓库根 `scripts/`」，消解守卫重建时按表误改名 | 结构轨 P2-5 | [A] |
| F23 | `文档写作标准.md`:43、:47 | 状态符号白名单范围未收口（照字面实现会误报全部标准件） → §一.4 明示「白名单只管**符号型**状态取值；文字型取值（`已实现`／`待建`／`无守卫（人判）` 等）不受辖」，并把 11 张机检锚点表登记为实例；:47 现状句同步改写（**:88 行由 F6 一并处理，本项不重复触及**） | 结构轨 P2-6 ＋ 成稿 P2 | [B] |
| F24 | `skill形态标准.md`:39、`触发与交互标准.md`:11、`架构设计.md`:19 | 「未实测」**字面标记**仅两处（前两者），回写指令只覆盖「本条」 → 改为「两处标记同批回写并删标记」；`架构设计.md`:19 无标记但有同源实测口径，措辞改为「三处实测口径同批订正」 | 内容轨 P2-3 ＋ 成稿 P1 | [A] |

### 2.2 审计 findings 处置表（逐条标注采纳／拒绝）

结构轨 11 条：P1-1 采纳→F6；P1-2 采纳→F1；P1-3 采纳→F10；P1-4 采纳→F10；P1-5 采纳→F10；P2-1 采纳→F12；P2-2 采纳→F11；P2-3 采纳→F20；P2-4 采纳→F21；P2-5 采纳→F22；P2-6 采纳→F23。

内容轨 17 条：P1-1 采纳→F2；P1-2 采纳→F1；P1-3 采纳→F6；P1-4 采纳→F3；P1-5 采纳→F4；P1-6 采纳→F5；P1-7 采纳→F7；P1-8 采纳→F9；P2-1 采纳→F16；P2-2 采纳→F17；P2-3 采纳→F24；P2-4 **已覆盖**（`安全与合规标准.md`:22／:44 的「锚定（公开样本）」本批已统一，无需新增改动项）；P2-5 采纳→F18＋F19；P2-6 采纳→F15；P2-7 采纳→F14；P2-8 采纳→F13；P2-9 采纳→F8。

**拒绝 0 条。** 两轨的「已核清项」（结构轨 15 项、内容轨 8 项）已复核：残留扫描（`collab-log`／六域／旧域名／mo-shu 零命中）、指向闭合（104 链接 0 断链、60 余处节号引用除上述 6 处外全部命中）、三条路径与判定表逐行对齐、体积数值同源、门禁三处命令一致、不做清单 12 条齐备、红线无抵触——均无需改动。

## 三、验收断言（含预验记录）

### 断言 1｜「七项」清零

```bash
grep -rn "七项" AGENTS.md docs/standards/ docs/specs/施工机制.md
```

期望 0 行；预验基线 2 行（`规格写作标准.md`:40、:93）。

### 断言 2｜立法质量条在位

```bash
grep -rn "正例或反例" AGENTS.md docs/standards/ docs/specs/施工机制.md
```

期望 ≥1 行；预验基线 **0 行**（旧仓 `main:AGENTS.md` 有 1 行，可核）。

### 断言 3｜底座业务词清零

```bash
grep -rn -E "终局口径|感情线|收拢式|三问段式|灵感库" AGENTS.md docs/standards/ docs/specs/施工机制.md
```

期望 0 行；预验基线 6 行（A3 的 4 行 ＋ A4 的 2 行）。

### 断言 4｜T1 有定义可依

```bash
grep -rn "触发档位定义见" AGENTS.md docs/standards/ docs/product/架构设计.md
```

期望 ≥2 行（`命名标准.md`:54、`术语标准.md`:22 两处触发档位指针）；预验基线 0 行。另核 `grep -c "触发档位" docs/product/架构设计.md` ≥1（§四 三档定义在位；预验基线 0）。

### 断言 5｜不可核出处件名清零

```bash
grep -rn -E "《术语表》|《评估场景》|《架构决策记录》" AGENTS.md docs/standards/ docs/specs/施工机制.md docs/product/架构设计.md
```

期望 0 行；预验基线 10 行。

### 断言 6｜错锚点字样清零

```bash
grep -rn -E "§5\.2／§6|§1\.2 引用闭合|§一\.8 加粗密度|§一\.6 引号三分|§四 状态符号白名单|§五 编码" docs/standards/
```

期望 0 行；预验基线 6 行。

### 断言 7｜三份文件目录就位

```bash
grep -c "^## 目录" docs/standards/skill形态标准.md docs/standards/命名标准.md docs/standards/版本与分发标准.md
```

期望各 1；预验基线各 0。另核目录条目与 H2 标题逐字一致（走查项）；并核 `grep -c "超 100 行文件" docs/standards/文档写作标准.md` = 2（:12 条文与 :86 锚点行逐字一致；预验基线 1）。

### 断言 8｜AGENTS 路由与指针

```bash
grep -c "CHANGELOG" AGENTS.md
```

期望 ≥1；预验基线 **0**。另核 `grep -c "行数上限见" AGENTS.md` ≥1（不依赖行号）。

### 断言 9｜产物契约四节骨架

```bash
grep -c "^## 四、维护" docs/standards/产物契约标准.md
```

期望 0；预验基线 1。

### 断言 10｜实测口径三处同批订正

```bash
grep -rn "两处标记同批回写" docs/standards/skill形态标准.md
```

期望 1 行；预验基线 0。

### 断言 11｜退出码适用范围限定在位

```bash
grep -n "为二值闸" docs/standards/运行环境标准.md
```

期望 1 行；预验基线 0。

### 断言 12｜链接与守卫

扫描命令（脚本内实现）：以 `git ls-files '*.md'` 取全仓 .md（含 `docs/specs/archive/`），提取相对链接并按所在目录解析存在性。期望断链 0 且条数 ≥104；预验基线 104 条／0 断链。另 `bash scripts/check.sh` 退出 0；预验退出 0。

### 断言 13｜过程产物两项

`CHANGELOG.md` 的 `[Unreleased]` 节含本批一行（≤400 字符）；规格验收通过后移入 `docs/specs/archive/`（移动不留副本）。

### 断言 14｜施工机制维度编号清零

```bash
grep -c "③b" docs/specs/施工机制.md
```

期望 0；预验基线 1。

### 断言 15｜版本与分发「现三处」清零

```bash
grep -c "现三处" docs/standards/版本与分发标准.md
```

期望 0；预验基线 1。

### 断言 16｜运行环境探测链时态改写

```bash
grep -c "现未实现" docs/standards/运行环境标准.md
```

期望 ≥1；预验基线 0。

### 断言 17｜命名总表固定名行

```bash
grep -c "一键入口" docs/standards/命名标准.md
```

期望 1；预验基线 0。

### 断言 18｜白名单范围收口

```bash
grep -c "符号型" docs/standards/文档写作标准.md
```

期望 ≥1；预验基线 0。

### 断言 19｜止损条先行词回补

```bash
grep -c "同类精度 P0" docs/specs/施工机制.md
```

期望 1；预验基线 0。

### 断言 20｜分组编号约定立条

```bash
grep -c "条文分组可用" docs/standards/文档写作标准.md
```

期望 1；预验基线 0。

### 断言 21｜check.sh 头注四要素

```bash
grep -c "依赖与前置" scripts/check.sh
```

期望 1；预验基线 0。

### 断言 22｜scope 示例旧坐标清零

```bash
grep -c "域名（如" docs/specs/施工机制.md
```

期望 0；预验基线 1。

**未预验项**：第 5–8 条（目标文件尚不存在）与第 13 条（收尾动作）——交审查位核验；第 6、7 条已在规格内给出源件实测值。其余 16 条（含新增 14–22）均已实跑记录基线。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 的 15 项预验基线、main 存档结构与件名映射均为本轮现场实跑；A14 一处判据曾写错（git 默认给中文路径加引号转义导致误判 `总纲.md` 不存在），改用 `-c core.quotepath=false` 重核后修正。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–12 全部实跑记录基线；**零命中断言（1／3／5／6／9／12／14／15／22）的 token 一律不得出现在任何「替换为」侧文本中**；存在性断言（2／4／7／8／10／11／16／17／18／19／20／21）的期望 token 须在替换文本中存在且不被吞。已逐条核对：本批「替换为」侧文本与零命中断言的 token 无交集。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`scripts/hooks/pre-commit`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 下任何归档件（059 归档件里的历史字样不改）。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；不得删除 `docs/standards/` 任何文件；不得改 `scripts/check.sh` 的段序与判定逻辑（F13 只允许改头注注释，禁动任何 `fail=1`／`exit` 行为）。
- 禁止越权：不改 `skills/`（尚未创建）；不改版本号与 tag；不得把业务细则再写回底座（F3 只允许「迁出＋留指针」）；不得改动本批附件（审计报告按原文留档）。

## 六、开放项

1. **F1 方向已定：三条**（作者授权规划方裁定）。规格自审收敛为三条，与 `施工机制` §二／§八 及 059 实践同源；七项中的「节号复核」并入条 1 射程、「涟漪清单」以域建设批附加句保留。若未来裁定回到七项，需反向改 `施工机制 §二`／§八 模板与 059 归档件口径（改动面翻倍）。
2. `架构设计.md §四` 的 T1–T3 选型理由与成长路径仍待迁移（来源 `main:docs/product/总纲.md`），本批只补三档定义。
3. 底座「可移植模板」形态（槽位清单／使用说明）未立——待首次移植时按需立法。
4. 审计报告全文已落档为**同批附件** [`060-2026-09-12-基座审计整改-审计报告附件.md`](060-2026-09-12-基座审计整改-审计报告附件.md)（两轨 28 条 findings 逐条含证据与处置、两轨「已核清项」）；随主规格验收后同批移入 `docs/specs/archive/`。
5. **引用闭合扫描须覆盖未跟踪文件**（本批实证：施工方扫描器基于 `git ls-files` 只扫已跟踪件，漏掉未跟踪的在制规格，自报「0 断链」为假绿）——`check.sh` [3] 段内容轨检查器落地时，扫描面须取「文件系统遍历」或「已跟踪 ＋ 未跟踪」，并把该口径写进 `测试与验收标准` §4 缺口表。
6. 结构轨在「已核清项」中另记两处非管辖符号（`↔`／`∈`／`–`）未设条规，暂无处置需要。

## 七、审查记录

成稿审查分身（fresh、只读）首轮：**findings 共 25：P0:1 P1:15 P2:9**。规划方逐条复跑取证后**全部采纳（25／25）**，已全数进本版：

P0 一条（**T1 两义混用**）：`skill形态标准.md`:132 的「旧仓 T1 元规范」是**引用分级 T1**（源 `main:docs/standards/file-conventions.md`:95「T1 superpowers 元规范」＋ `main:docs/product/总纲.md`:207 分级定义），与**触发档位 T1**（`总纲.md`:80「运行时组件物化需求出现」）是两义——原 F5 按「三处 T1 首现处」一把改会写错指向。已把 F5 拆为两义分流处置，`skill形态标准.md`:132 改「旧仓 superpowers 元规范（引用分级 T1）」，A5 基线与断言 4 期望（≥3→≥2）同步修正。

P1 十五条：① 标题「F1–F22」与实表 F1–F24、定位行「25 条改动项」两处计数错 → 改为 F1–F24／24 条；② 断言 5 基线实为 10 行（原写 9）→ 修正，F7 位置列 10 处核对无误；③ F1 漏扫同义词（「自审记录」另有 4 处：`skill形态标准.md`:138、`规格写作标准.md`:4／:52／:77）→ 并入 F1（反模式 #2 同型）；④ F6 锚点清单不全（`:86`／`:90`／`:91` 同属错锚，且改后与 `:91` 撞号）→ 补全七行并确立「子节 `§一.N`／条 `§一.{子节}.{条}`」统一约定；⑤ F20 落位错节（应在 §一.1 而非 §一.5）且与 F6 的三级形式不配套 → 改落位并统一为 `章.节[.条]`；⑥ F11 行号错（矩阵行为 `:137`）且 README 同属违例 → 一并登记例外；⑦ F14 漏第二载体（`:99`「§2 四处（现三处）」）→ 纳入；⑧ F24「三载体」不实（`架构设计.md`:19 无字面标记）→ 改「两处标记 ＋ 一处口径同批订正」；⑨ F3 未指定迁移落点、未与 `大纲域设计.md`:17 去重、漏 `触发与交互标准.md`:3 定位行 → 全部纳入；⑩ F19 只解一半（「热路径」仍无定义）→ 补就地括注定义；⑪ **24 项中 11 项无验收断言** → 新增断言 14–22（8 条，基线已预验）；⑫ F21 标 [A] 却无逐字替换文本且描述不准（裸名非全行）→ 降 [B] 并写明「只加反引号、不动其余字符」；⑬ F12「并入 §一 末条」歧义 → 明确为「追加为 §一 末节 `### 4. 维护`，只搬 :53–:55」；⑭ 断言 12 不可执行（无命令、口径含 75 vs 104 两说）→ 写明扫描命令与范围（`git ls-files '*.md'` 全仓、含 archive）；⑮ F2 与本批自身冲突（本批新增／修改条文未附正反例）→ 加一次性过渡句并记入条文。

P2 九条：处置表把内容轨 P2-4 标「采纳→F4」实为**已覆盖**（`安全与合规标准.md`:22／:44 本批已统一）→ 改标；F9 位置描述不实（§5 无「首句」）→ 改「表前新增一句」；断言 7 与 F10 措辞不配套（三种串并存、`:86` 会被误改）→ 统一为「超 100 行文件」并核 `=2`；F1 在 §二 已定改法而 §六 标「待确认」姿态不一 → F1 加标记；F2 只管了旧仓 §3.9 的第一款 → 第二款「治理文档条文不写批次叙事与裁定标记」一并补回；`审计报告转为规格附件` 声明无落点 → 改为「按类归并、逐条标注落位」并把「报告未留档」记入 §六 开放项 4；F1 三条化与 `施工机制 §二` 成第二副本 → 加「同源、不复制细则」句；F6 与 F23 同改 `文档写作标准.md`:88 → F23 注明该行由 F6 处理；§四.2 反引号游离且互斥清单只覆盖 5 条 → 修正并把**零命中断言全覆盖**写进清单。

**核验结论（规划方，施工后三步）**：

- **第 1 步 改动面核对：通过（本次起恢复 git 基线法）**——`git status` 与 `git diff` 对照：改动 16 文件与 §2 清单逐一吻合、无清单外文件；禁改面（`scripts/hooks/pre-commit`／`LICENSE`／`.gitattributes`／`.gitignore`／`docs/specs/archive/`）零 diff；`scripts/check.sh` 仅头注 +1 行，`fail=1`／`exit` 判定与段序零改动（逐 hunk 核过）。
- **第 2 步 断言独立重跑：22/22 通过**。两处 FAIL 均为**核验侧假阳性/真发现**，已分别处置：① 断言 9 是我的检查判据写错（`grep -c` 输出 `0` 亦为一行，不该按行数判空），实件计数确为 0；② 断言 12 抓出**规格自身两处相对链接断裂**（`docs/standards/…` 与 `CHANGELOG.md` 写成仓库根相对路径，在 `docs/specs/` 下必断）——施工方的扫描器基于 `git ls-files` 只扫已跟踪文件、漏掉未跟踪的本规格，故其自报「110 条 0 断链」为**假绿**；修正后实测 115 条／0 断链。**此即核验第 2 步必须由规划方独立复跑的价值实证。**
- **第 3 步 内容抽查（标准路径＝规划方通读抽查）：通过**。抽读 8 处关键改动：`规格写作标准` §4 三条化（七项有效项以「附加项」保留，比规格字面更完整）、`文档写作标准` §三 七行锚点两级/三级编号约定、`触发与交互标准` §4 改指针、`大纲域设计` §四 新增「域交互细则」及 :17 去重、`运行环境标准` §1.4 时态与 §5 表前限定句、`架构设计` §四 T1–T3、`施工机制` :89／:99、`AGENTS.md` 红线 6 与路由表新行——均语义通顺、指向实存、无断裂截断。本批无用户可观测行为改动，不涉真机走查。
- **施工方 4 条 P1 裁定**：P1-a（F17 缺句末句号）采纳——由规划方作**收尾微修**补一字（避免为单字再起一轮施工），已落；P1-b（指针写「§四『域交互细则』」而非「本节」、定位行补「域交互细则」）采纳；P1-c（保留旧节名括注「（规格字段）」便于底座↔产品层对照）采纳；P1-d（`.tmp/060/linkcheck.js`）——已按红线 2 删除 `.tmp/060/`，本轮独立复跑改用规划方自写扫描器。
- **施工方 2 条未决裁定**：① 规格与附件的归档动作由规划方收尾（提交时一并做）；② `测试与验收标准.md` 本批零改动**正确**——F20 明确「实件与既有 N 处引用不动」，该件 `1.1` 式编号由 `文档写作标准` §一.1 新条赋予合法位。

审查位另核已清（16 项）：main 存档结构、件名映射 8 条、A1–A15 基线（除 A6 计数外全对）、F 项锚点、处置表 28 条全覆盖、断言命令可执行性与不依赖行号、F1 真源主张（`施工机制`:48／`:180` 与 059 归档件均为三条）、F2 追加不动编号、F3 与断言 3 token 配套、F13 与 §五 边界自洽、F14 前提（四处载体实测 0 处）、补行补节类改动的连锁面、§五 禁止面与现场一致。
