# 061-2026-09-12-mattpocock-skills深度研究

> 定位：把第三方技能仓库 `mattpocock/skills` 的开发规范与机制研究到位，逐条裁定「采纳／已覆盖／拒绝」并落位；本批只做**底座写作方法论与结构机制**的首轮落地，产品层范式与发布链改造记开放项。
> 自由度口径：[A]＝源文本即目标文本（整段搬运、仅链接路径改写）｜[B]＝按条目所述方向改写｜[C]＝新写。
> 必读输入：`AGENTS.md`、`docs/standards/` 全部 11 份、`docs/specs/施工机制.md`、`docs/product/架构设计.md`；研究证据见 §一 基线与 §二 裁定表
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**研究可复现性**：被研仓库 `https://github.com/mattpocock/skills`，commit `3cca18b368ae95cdbdebbff572ccafa662551015`（2026-09-04，merge PR #1025）；只读副本在 `.tmp/061/mattpocock-skills/`（本批结项时删除；红线 2／3）。样本量：164 个文件、约 6900 行 markdown、**37 份 `SKILL.md`**。

**研究方式**：规划方通读元层文件按件计 20 份（根级 2、`.agents/` 5、`.out-of-scope/` 3、桶 README 5、`writing-for-agents` 2、`CHANGELOG` 头部与 `to-spec` 文档页各 1）——（`CLAUDE.md`、`CONTEXT.md`、`.agents/invocation.md`、`.agents/writing-docs.md`、`.agents/install-block.md`、`.agents/adr/0001`、`.agents/adr/0002`、`.out-of-scope/` 三份、`README.md`、五份桶 README、`skills/productivity/writing-for-agents/SKILL.md` 与 `SKILL-MECHANICS.md`、`CHANGELOG.md` 头部、`docs/engineering/to-spec.md`）；另派只读分身横向提取 37 份 `SKILL.md` ＋ 侧文件 ＋ 脚本 ＋ 发布链 ＋ 成文禁令，全部结论带 `文件:行` 与原句。

**我方基线**：分支 `rebuild`，HEAD＝`3ef6175`，除本规格外工作树干净。断言预验基线见 §四。

**关键机械事实（对照用）**：他们的 `SKILL.md` frontmatter 只有 4 个可能键（`name`／`description`／`disable-model-invocation`／`argument-hint`）；user-invoked 22 份（带 `disable-model-invocation: true`）、model-invoked 15 份；`description` 随轴换写法（user-invoked 剥触发列表、model-invoked 带 `Use when…` 触发分支）；正文无统一骨架（`## Process` 6 次、`## Steps` 5 次（恰名标题）为高频）；侧文件 22 份、15–207 行，正文以「何时读它」的条件句点名；实件 `SKILL.md` 最长 140 行、侧文件最长 207 行，全仓**无行数上限条款**。

## 二、参考研究裁定表

> 结论列取值声明：模板三态为**采纳／已覆盖／拒绝**；本表在三态之上扩展两个标记——**部分采纳**＝主张主体采纳、落位待定或记开放项；**印证**＝我方已有该机制且被外部独立验证、无落位义务。另有 **记录**（数值或实践值仅登记、不产生改动）。

### 2.0 速览（借鉴／增强／补充／不采纳）

> 本表是 §二 裁定表的三分速览（证据与逐条理由见下表各条；改动项编号见 §三）。

**借鉴**（机制移植，本批落地）：指针措辞优先（M1）｜两种负荷（M2）｜下沉判据＝分支（M3）｜完成判据两杠杆 ＋ premature completion（M4）｜否定式反弹（M6）｜修剪四件事：scattering／环境即真源／no-op／sediment（M7）｜廉价检查点不阻塞（S2）｜不做清单加「已有逃生口」（G1）｜「被否决的替代方案」节（G2）｜实测回写四要素（M8）｜守卫「跳过不静默」（S5）｜产品层文档范式（G3）。

**增强**（我方已有，被其判据加固）：单一真源律补 scattering 维（F1）｜体积纪律判据由行数升级为分支（F5）｜停靠加第三取值「廉价检查点」（F7①）｜禁令须配正向目标的成文义务（F2）｜规格增「被否决的替代方案」（F9）｜版本一致性改可跑校验（F11）｜守卫输出补齐三分项（F10）；另三项**仅印证、无改动**——独立审查双分身（S4，理由同为防上下文污染）、退役即删除且 record 点名取代者（S6）、安装叙述单点真源（G4）。

**补充**（本批新增实体）：`文档写作标准` 子节 7／8／9（F1／F2／F3）｜`术语标准` §1「优先取通用词」条（F6）｜`skill形态标准` §3 回写四要素（F7②）｜`测试与验收标准` §1.1 守卫输出条款（F10）｜`AGENTS.md` §四 逃生口栏（12 条，F8）｜`规格写作标准` 子节 10 ＋ 规格模板同步（F9）｜`文档写作标准` 与 `规格写作标准` 补 `## 目录`（F12，后者现 110 行已超）。

**不采纳**（附理由）：跨技能调用形式化（我方域间禁互调；仅借其「让用户跑 /X」的措辞纪律）｜路由技能（我方无中央路由）｜`AGENTS.md` 符号链接（单宿主）｜文档页绝对链接（我方文档不发布）｜发布自动化（仅借 `--check` 核法）｜面向外部的强制 AI 披露（产物不对外）｜新增 ADR／out-of-scope 独立目录（角色已由规格与不做清单承担）。

### 面一 写作方法论（`writing-for-agents` 及其 `SKILL-MECHANICS`）

| # | 参考条目的主张（证据） | 结论 | 落位或理由 |
| --- | --- | --- | --- |
| M1 | **context pointer**：凡留在上下文里指向别处材料并携带触发条件者皆是指针（description／`AGENTS.md` 一指路行）；「决定 agent 何时去读的是**指针的措辞**，不是目标」，目标必需而措辞弱＝**方差 bug**，先磨措辞、磨不动才内联（`writing-for-agents/SKILL.md:12-18`） | **采纳** | 落 `文档写作标准` 新子节；这正是本项目「施工机制怎么被读」的症结：路由行＝指针，措辞决定它是否被读 |
| M2 | **两种负荷**：context load（常载材料每轮烧 token 与注意力，不论是否触发）vs cognitive load（人的记忆负担，「人就是索引，这不是要最小化的成本，而是人的能动性代价」）（同 `:20-28`） | **采纳** | 同上；本项目一路以 cognitive load 换零机制，需有词汇把这笔账写明 |
| M3 | **信息层级与下沉判据**：文件内步骤 → 文件内参考 → 下沉参考；下沉的判据是**分支**（每个分支都要的内联、只有部分分支会读的沉下去），不是行数；`co-location`＝概念的定义／规则／注意同处一节（`:30-43`） | **采纳** | 落 `文档写作标准` 新子节 ＋ `skill形态标准` §5（体积纪律的判据由行数升级为分支） |
| M4 | **完成判据两杠杆**：清晰度（能否分清做完没做完）与要求度（「每个改动模型都交代」vs「给个改动清单」）；失败模式 **premature completion**（后续步骤可见→注意力滑向「想做完」）；防御顺序＝先磨边界，磨不动才拆分藏后续步骤，且**藏只在真实上下文边界（交接／子分身）有效**（`:45-52`） | **采纳** | 落 `skill形态标准` §8 流程设计纪律（F4）；**第二落面「域设计的门控写法」属产品层，记 §七 开放项 8** |
| M5 | **leading word**：优先取预训练里已有的词（免费招募先验），生造词要付定义 token（`:56-66`） | **采纳** | 落 `术语标准` §一 用法（新增一条） |
| M6 | **否定式反弹**：以禁令驱动行为会把被禁行为拉进上下文并**提高其可用度**（「别想大象」）；只写正向目标；禁令仅在无法正向表达的硬护栏上保留，且须配正向目标句（`:68-76`） | **采纳（并对本项目自身先例作校正）** | 落 `文档写作标准` 新子节；我方红线（6 条）与反模式清单属「硬护栏＋事故记录」例外，其「正确方式」列即正向目标，保留；但 standards 里「禁止 X」式条文须补正向目标句 |
| M7 | **修剪纪律**：duplication（同一意思写两处）与 **scattering**（同一意思碎在多处）是两种病；**环境也是真源**（复述配置＝缓存，只在查不到时才值得写）；逐句找 **no-op**（模型默认就会照做的指令白花负荷），失败就**删整句**而非改字；不修剪的默认命运是 **sediment**（陈旧层沉积）（`:77-81`） | **采纳** | 落 `文档写作标准` 新子节；`scattering` 是我方单一真源律未覆盖的一维 |
| M8 | **另一宿主的同类键曾因过度设置被整表过滤**：Codex 的 `policy.allow_implicit_invocation: false` 使该技能被剔出模型可见列表，description 无法触发、只剩显式 `$skill` 可用，他们因此回退并记入 CHANGELOG（`CHANGELOG.md` 1.2.2 段） | **采纳（作为通则证据）** | 佐证「机制键须实测**可见性**」这一通则；我方键（`disable-model-invocation`）的实测项**两项已载** `skill形态标准`:45，本批只新增**回写格式四要素**（宿主版本／实测日期／结论／发现的不一致） |
| M9 | **user-invoked 的 description 应剥离触发列表**（面向人读），model-invoked 才写触发分支（`.agents/invocation.md:5-9`） | **部分采纳／记开放项** | 我方三段式含「触发方式：仅 /abzu-{域} 显式调用；自然语言提及不触发」——若机制键生效，此句即 M7 意义上的 **no-op**；是否压缩待首个域实测后裁定（记 §七 开放项 1） |
| M10 | **跨技能调用形式化**：点名叫「Call the Skill tool with X」、一次一个、两步即两次调用；前置若是 user-invoked 技能则写成「让用户去跑 /X」（`.agents/invocation.md:14-22`；`.changeset/skill-tool-invocation-terminology.md`） | **拒绝** | 我方立法为**域间禁互调**（跨域只经落盘文件衔接）——产品形态不同：他们 30+ 技能成链（grill→spec→tickets→implement→review），我方 3 域且开书顺序由作者定。**但借其措辞纪律**：域间引导只能写「让用户跑 `/abzu-{域}`」，禁写成技能调用（我方中断契约已是此形，见 M11） |
| M11 | **前置条件的措辞分级**：硬依赖写显式一句「跑 /setup 若未配」，软依赖只写泛化散文、缺了也能降级工作；理由是避免把 setup 指针 cargo-cult 到非承重处（`.agents/adr/0001`） | **部分采纳／记开放项** | 我方 `触发与交互标准` §4 域内准入律指针 ＋ 中断契约句式（「建议使用对应 /abzu-<域> 命令，不强行走完本域流程」）即此分级；ADR-0001 的**硬／软依赖判据**可作将来域设计的判据补注 |

### 面二 技能机制与结构（invocation／拆分／停靠）

| # | 参考条目的主张（证据） | 结论 | 落位或理由 |
| --- | --- | --- | --- |
| S1 | **侧文件两处点名**：先集中列 `## Reference docs`，再由步骤按条件点名；同目录侧文件用 markdown 链接、**跨技能一律不链接**（`triage/SKILL.md:19-22、:79、:85`；`codebase-design/SKILL.md:113-114`） | **已覆盖** | 我方 `skill形态标准` §9 引用纪律（资产内用 skill 根相对路径 ＋ 节名；references 间不互链）等价；「集中列 ＋ 步骤点名」的双处点名写法可作写法细化（记 §七 开放项 2，低优先） |
| S2 | **停靠点分级**：硬门（`wizard`「confirm before any irreversible action」）与**廉价检查点**（`diagnosing-bugs:98`「Show the ranked list… Cheap checkpoint, big time saver. **Don't block on it**; proceed with your ranking if the user is AFK.」） | **采纳** | 落 `触发与交互标准`：停靠点分两级，廉价检查点不阻塞（用户 AFK 时按默认继续并在通报中声明） |
| S3 | **完成判据写法**：逐步 `**Done when:** …`；整技能一条 `Definition of done`；强要求度范式「done when an implementer agent could build it **without asking a single question**」（`wizard/SKILL.md:25、:31`；`setup-ts-deep-modules/SKILL.md:45-95`；`loop-me/SKILL.md:27`） | **采纳** | 与 M4 合并落 `skill形态标准` §8：每步须给可判定的完成判据，且判据取强要求度 |
| S4 | **子分身用法**：并行双轴审查「so they don't pollute each other's context」（`code-review/SKILL.md:11、:58`）；后台研究「so you keep working while it reads」（`research/SKILL.md:6`）；`implement-spec` 并行实现者各占 worktree＋分支 | **已覆盖（强印证）** | 我方独立审查双分身位（成稿／产物）＋ 后台施工分身的理由与其一字不差（防上下文污染／可续跑）；其「每个实现者独立 worktree」对应我方「分身无 git 写权限 ＋ 规划方统一提交」 |
| S5 | **脚本与 SKILL.md 分工**：库区固定「never hand-edit it」、作者只写 STAGES 段；`set -euo pipefail`；幂等 upsert；**跳过项收集并在收尾汇总并给手动路径**（`wizard/SKILL.md:10、:37`；`template.sh:9、:130、:152`） | **部分采纳** | 分工与幂等已覆盖（我方 `运行环境标准` §5 脚本契约）；**采纳其「跳过项显式登记 ＋ 给手动路径 ＋ 收尾汇总」**为守卫输出规范：跳过不得静默（我方 `check.sh` [0] 段已给原因，补"手动路径／收尾汇总"口径） |
| S6 | **晋升桶模型 ＋ 可机检不变量**：晋升桶技能必须同时出现在顶层 README 与 `plugin.json`，非晋升桶**必须都不出现**（`CLAUDE.md:7-11`）；退役即删除，删它的 changeset 点名取代者（`skills/deprecated/README.md:3`） | **采纳（实施待 skills/ 落地）／已覆盖（退役处置）** | 桶／晋升概念对应我方「域建设状态 ＋ 是否随包分发」，须与 `架构设计` §一 域清单一致（记 §七 开放项 3，待首个域落地时立断言）；退役处置我已覆盖（命名标准 §6／术语标准 §4：删行 ＋ `CHANGELOG` 记录） |
| S7 | **路由技能**：user-invoked 技能多到记不住时，用一个 user-invoked 的 router 名称与时机（`SKILL-MECHANICS.md:19-22`）；其 `ask-matt` 另配**阶段边界五分法**（Continue／clear／handoff／subagent／compact，按序判定，「compact 是默认而非第一反应」）（`ask-matt/SKILL.md:9、:63`；`PHASE-BOUNDARIES.md:9-40`） | **拒绝（router）／记开放项（阶段边界判据）** | 我方 3 域且入口唯一，斜杠菜单即索引，无中央路由（旧仓教训）；其**会话卫生五分法**对长书多卷创作有实际价值，但属产品层设计，记 §七 开放项 4 |

### 面三 仓库治理（可迁移到底座的部分）

| # | 参考条目的主张（证据） | 结论 | 落位或理由 |
| --- | --- | --- | --- |
| G1 | **`.out-of-scope/`：一条被拒的需求一个文件**，含「为什么不做」的深层理由、**已有的逃生口**、**既往请求与编号**（`.out-of-scope/mainstream-issue-trackers-only.md`、`question-limits.md`、`setup-skill-verify-mode.md`） | **采纳（格式）／拒绝（新建目录）** | 不新增目录（我方不做清单在 `AGENTS.md` §四，已有理由与解冻触发条件）；**采纳其「逃生口」一栏**：不做清单条目升级为「理由 ＋ 已有逃生口 ＋ 解冻触发条件」 |
| G2 | **ADR 格式**：标题即决策句 → 约束 → **试过并否决的替代（附失败原因）** → 决策 → **该决策创造的不变量** → 带日期与实测环境的回写（`.agents/adr/0002`，含「官方清单 sha 落后两个提交，故列 22 个而非 24 个」的实测记录） | **采纳（一节）／已覆盖（其余）** | 我方规格承担 ADR 角色（决策＋理由＋验收断言＋开放项＋归档）；**采纳「被否决的替代方案」为规格可选节**（防复查时重复纠结），并采纳其「实测回写：宿主版本 ＋ 日期 ＋ 结论 ＋ 不一致」格式 |
| G3 | **文档页范式**（面向人）：固定骨架 ＋ **defining constraint**（使之区别于显然默认做法的那一个事实）＋ **It's working if**（读者不打开 SKILL.md 就能自查的信号）＋ **Common questions**（去真实来源找、数量忠于证据、说不好听的实话）＋ **Where it fits**（角色命名：链步／一次性配置／周期性／随时可用）（`.agents/writing-docs.md:15、:25、:44-68`；实例 `docs/engineering/to-spec.md`） | **采纳（产品层）** | 落 `文档写作标准` 新增「产品层文档范式」子节，供 `docs/product/` 各域设计与 README 采用；**本批只立规范，不写内容**（域设计实质待业务设计阶段） |
| G4 | **install-block 单点真源**：一段安装叙述，各处只准说这一段，「先改这里再传播」；两条安装路线互斥须明说 pick one（`.agents/install-block.md:3、:57`） | **已覆盖** | 我方「段序真源＝`check.sh` 头注，其他文档不复述」（059／060 立）；「互斥路线须明说」可作 README 写法补注（低优先） |
| G5 | **发布链**：版本号两处（`package.json` ／ `plugin.json`）由脚本**单向复制**（package.json → plugin.json），`--check` 模式只比对、差异即非零退出（`.agents/adr/0002:28` 的不变量同为此向）；changesets 生成版本段与 CHANGELOG（`scripts/sync-plugin-version.mjs:2-4、:29、:36-37`；`.github/workflows/release.yml`） | **采纳（核的部分）／拒绝（自动化）** | 自动化曾拒（单人阶段无需 CI）；**采纳其 `--check` 思路**：把「四处一致」从人工纪律变为**可跑的版本一致性校验**（实施待 `skills/` 与插件清单落地，记 §七 开放项 5） |
| G6 | **多入口单真源**：`AGENTS.md` 为指向 `CLAUDE.md` 的符号链接（`AGENTS.md` 0 行；`CHANGELOG.md` 1.2.0 段记「Add AGENTS.md as a symlink to CLAUDE.md so Codex reads the same repo instructions」） | **拒绝** | 我方宿主唯一（Claude Code），且开发侧不要求用 Claude Code，`AGENTS.md` 即唯一入口；多入口是为跨宿主（Codex 读 AGENTS.md、Claude 读 CLAUDE.md），我方无此需求 |
| G7 | **文档页链接全绝对**：因发布到站点，相对链接必断（`.agents/writing-docs.md:9`） | **拒绝** | 我方治理文档在编辑器与 GitHub 中阅读，相对链接是可用性加分（`命名标准` §5 已裁）；若将来有文档发布到站外，再按该件补齐绝对链接 |
| G8 | **成文禁令（约 12 条，散见多件）**（术语禁同义替换；文档禁署作者名；分支禁写段落；禁重复内容应引用；禁引用文件路径与行号「会过期」；原型禁加测试／接真库／泛化／上框架；禁「log everything and grep」；禁编造不存在的步骤；禁追问用户本可自取的事实；禁覆盖用户已有设置；等） | **多数已覆盖／一条部分采纳** | 已覆盖：术语禁同义替换（`术语标准` §一）、禁署作者名（060 新立「条文不写批次叙事与裁定标记」）、禁重复应引用（反模式 #4）、禁编造（`产品契约` 未知即降级＋「不虚标」条款）。**部分采纳「禁引用行号」**：我方规格以 `文件:行` 作锚点且有「现场核过」纪律，保留；但**对外／跨会话产物**（施工交底、核验请求）应给符号锚点（节名／token）而非行号——记 §七 开放项 6（低优先） |

### 面四 研究结论对本项目方向的印证

| # | 观察 | 结论 | 说明 |
| --- | --- | --- | --- |
| V1 | 他们把「审查两轴并行、互不污染上下文」作为明文理由 | **印证** | 与我方两个 fresh 分身位（成稿／产物）与核验双轨同源 |
| V2 | 他们把「同一个意思写两处」列为病并单列 scattering | **印证并补一维** | 我方单一真源律（反模式 #4）只覆盖 duplication；scattering 已入 M7 采纳 |
| V3 | 他们的 `SKILL.md` 实件 ≤140 行、无行数上限条款 | **记录（不改阈值）** | 我方 500／300 阈值更宽松，属有意（Agent Skills 规范硬约束 500）；实践值记此供将来校准，不本批改 |
| V4 | 他们把面向人的文档当「降低认知负荷」的产品件来做（每技能一页、四节硬判据） | **印证我方产品层缺口** | 我方 `docs/product/` 八份中三份域设计仍是骨架——G3 采纳即为此缺口立规范 |

## 三、首轮落地改动清单（F1–F12）

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `文档写作标准.md` §一 新增子节 `### 7. 给 agent 读的文档`（**§一 现为子节 1–7、7＝维护**；新节取 **7／8／9**，`### 7. 维护` 顺延为 `### 10. 维护` 以保住「维护在末位」惯例并**避免缺号**；全仓对 §一.6／§一.7 的引用实测为 0，无需同步） | 新写四组规则：① **指针**（定义；措辞决定触发；目标必需而措辞弱＝方差 bug，先磨措辞再内联）；② **两种负荷**（context load／cognitive load 的定义与取舍）；③ **信息层级与下沉判据**（文件内步骤→文件内参考→下沉参考；按分支下沉；co-location）；④ **修剪**（duplication vs scattering；环境即真源，复述配置＝缓存；逐句找 no-op，失败删整句；sediment） | M1／M2／M3／M7 | [C] |
| F2 | `文档写作标准.md` §一 新增子节 `### 8. 禁令须配正向目标` | 规则：以禁令驱动行为会把被禁行为拉进上下文（否定式反弹）；条文只写正向目标；禁令仅在无法正向表达的硬护栏上保留，且须在同一处给出正向目标句（红线与反模式清单的「正确方式」列、以及 `AGENTS.md` §四 不做清单的「不做＋理由＋逃生口＋解冻条件」形态（F8 同批升级）即义务履行） | M6 | [C] |
| F3 | `文档写作标准.md` §一 新增子节 `### 9. 产品层文档范式` | 规则：`docs/product/` 各件与 README 的域条目须含——**defining constraint**（该域区别于显然默认做法的那一件事，写成陈述句、不用「关键点：」式标签）、**It's working if**（作者不打开 skill 内部即可自查的信号；判据是"在他自己的工作里可见"，不得拿内部合规检查冒充）、**角色定位**（链步／一次性配置／周期性／随时可用）；「常见问题」须来自真实来源且数量忠于证据，不得注水 | G3 | [C] |
| F4 | `skill形态标准.md` §8 流程设计纪律（产品层域设计的同款落位见 §七 开放项 8） | 补两条：① 每步须给**完成判据**（可判定；要求度取强范式，如「能不问一个问题就开工」）；② 警惕 **premature completion**——防御顺序为先磨边界，磨不动才拆分藏后续步骤，且藏只在真实上下文边界（交接／子分身）有效 | M4／S3 | [B] |
| F5 | `skill形态标准.md` §5 预算分级 | 补一句：**下沉（progressive disclosure）的判据**优先取**分支**（每个分支都要用的内联、只有部分分支会读的下沉），行数阈值只是兜底（**拆分判据不在本条，见 F4**）；并把 `skill形态标准.md:58` 的 reference 行数条款改为「兜底」措辞（**数值不动、仅改判据优先级**，并在 `CHANGELOG` 记一句） | M3 | [B] |
| F6 | `术语标准.md` §一 用法 | 新增一条：术语优先取**既有的通用词**（预训练先验免费）；生造词须在同一处给定义，并意识到其定义 token 成本；同一概念禁止同义词替换（已有条） | M5 | [B] |
| F7 | `触发与交互标准.md` §2；`skill形态标准.md` §3 | ① **停靠判据落 `触发与交互标准` §2**：新增**廉价检查点**为 §2.2 停靠的第三种取值（**不阻塞**——用户 AFK 时按默认继续并在通报中声明），与 §2.1 门控＝阶段级硬停、§2.2 停靠＝模块级暂停并列，须在 §2 内写明层级关系。② **机制键的回写格式落 `skill形态标准` §3**（该节是键定义的单点权威，避免第二载体，见 060 结构轨教训）：该节 `:45` **已有两项实测点**（`/` 菜单是否仍列出 ＋ 模型是否仍能自主触发），本批只新增**回写格式四要素**（宿主版本／实测日期／结论／发现的不一致）；`触发与交互标准` §1 只保留引用，并同步该件 §三 机检锚点行 `:60`（该行同载两项实测内容，构成第二载体残留） | S2／M8 | [B] |
| F8 | `AGENTS.md` §四 不做清单 | 条目格式升级为「不做 ＋ 理由 ＋ **已有逃生口** ＋ 解冻触发条件」（现有 12 条逐条补「逃生口」；无逃生口者写明「无」）；**同步面**：`运行环境标准.md`:42 括注逐项列了字段名（「含理由与解冻触发条件」），须补「逃生口」（`命名标准.md`:94 与 `测试与验收标准.md`:48 只按节名引用，无需改） | G1 | [B] |
| F9 | `规格写作标准.md` §一 新增子节 `### 10. 被否决的替代方案（规格可选节）`（追加在现 §一 末条 9 之后，无编号连锁） | 新增**可选节**「被否决的替代方案」：规格记录了考虑过但未采用的做法与未采用原因（价值：复查时不必重新推演）；模板同步（`施工机制` §八 规格模板加该可选节一行） | G2 | [B] |
| F10 | `测试与验收标准.md` §1.1 段表 [0] 行（**守卫输出条款，不落 `运行环境标准` §5**——该节 :46 已声明「本契约适用于 skill 运行时脚本，`check.sh` 为二值闸、不适用本节五分类」，落那里会与之互斥） | 补守卫输出规范：**跳过不得静默**——须显式登记原因，并在收尾汇总「跳过项 ＋ 手动路径」；同批处置 `运行环境标准.md`:46 边界句（加一句「守卫输出格式见 `测试与验收标准` §1.1」）。实例说明：`check.sh` [0] 段现只满足「显式原因」一项（缺手动路径与收尾汇总），F 项须写明待补两项 | S5 | [B] |
| F12 | `文档写作标准.md` 与 `规格写作标准.md` 头部 | 补 `## 目录`（手动列表、条目与 H2 逐字一致）：`文档写作标准.md` 经 F1–F3 必超 100 行；`规格写作标准.md` **现 110 行已超**（060 的 F2 新增两子节所致）——两者均已触发 `文档写作标准` §一.1.3「超 100 行文件头部放节目录」与同文 §三 机检锚点行 | 060 F10 先例（G 组无此项，属本条自查补入） | [B] |
| F11 | `版本与分发标准.md` §2 版本四处一致 | 补「可跑校验」措辞：四处版本号一致性由人工纪律升级为**可跑的一致性校验**（借其「只改 version 行 ＋ 差异即非零退出」核法；我方四处含 git tag 与 `CHANGELOG` 版本段，属人工核对项，须写明），实施排期见 §七 开放项 5（本批只落规范措辞） | G5 | [B] |

## 四、验收断言（含预验记录）

### 断言 1｜写作方法论子节在位

```bash
grep -c "指针" docs/standards/文档写作标准.md
```

```bash
grep -c "no-op" docs/standards/文档写作标准.md
```

两命令各期望 ≥1；预验基线 0／0。（逐词各判：`grep -c -e A -e B` 数的是匹配**行数**而非各模式命中数，合并判会在两词同行时假红。）

### 断言 2｜否定式反弹条在位

```bash
grep -c "正向目标" docs/standards/文档写作标准.md
```

期望 ≥1；预验基线 0。

### 断言 3｜产品层文档范式条在位

```bash
grep -c "显然默认做法" docs/standards/文档写作标准.md
```

```bash
grep -c "自查的信号" docs/standards/文档写作标准.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 4｜完成判据与 premature completion

```bash
grep -c "完成判据" docs/standards/skill形态标准.md
```

期望 ≥1；预验基线 0。

### 断言 5｜术语取通用词

```bash
grep -c "生造" docs/standards/术语标准.md
```

期望 ≥1；预验基线 0。

### 断言 6｜两级停靠（触发与交互标准）与实测回写格式（skill形态标准）

```bash
grep -c "廉价检查点" docs/standards/触发与交互标准.md
```

```bash
grep -c "宿主版本" docs/standards/skill形态标准.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 7｜不做清单含逃生口

```bash
grep -c "逃生口" AGENTS.md
```

期望 ≥1；预验基线 0。

### 断言 8｜规格可选节与模板同步

```bash
grep -c "被否决的替代方案" docs/standards/规格写作标准.md
```

```bash
grep -c "被否决的替代方案" docs/specs/施工机制.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 9｜跳过不静默

```bash
grep -c "跳过项" docs/standards/运行环境标准.md
```

```bash
grep -c "跳过项" docs/standards/测试与验收标准.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 10｜版本一致性改为可跑校验（措辞）

```bash
grep -c "一致性校验" docs/standards/版本与分发标准.md
```

期望 ≥1；预验基线 0。（本条只落规范措辞，脚本实施见 §七 开放项 5）

### 断言 11｜链接与守卫

扫描命令（脚本内实现）：以 `git ls-files '*.md'` 取**已跟踪** .md，并对工作树**未跟踪**的 .md 一并遍历（060 教训：只扫已跟踪会假绿）；提取相对链接（排除 `http`／`#`／`mailto:`），按所在目录解析存在性。期望断链 0 且条数 ≥115；预验基线 115 条／0 断链。另 `bash scripts/check.sh` 退出 0；预验退出 0。

### 断言 12｜过程产物两项

`CHANGELOG.md` 的 `[Unreleased]` 节含本批一行（≤400 字符）；规格验收通过后移入 `docs/specs/archive/`（移动不留副本）。

**未预验项**：第 12 条（收尾动作）。其余 11 条均已实跑记录基线。

## 五、规格自审（三条）

1. **锚点现场核过**：§一 的研究基线（repo ＋ commit sha ＋ 副本路径 ＋ 样本量）、§二 各条证据的 `文件:行` 均由规划方或受派分身现场读取；§四 断言基线为规划方实跑值。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–10 全部实跑取基线；期望 token（「指针」「no-op」「正向目标」「区别于默认」「自查信号」「完成判据」「生造」「廉价检查点」「宿主版本」「逃生口」「被否决的替代方案」「跳过项」「双向校验」）均为本批新写文本中的既定用词，且不出现在现有底座文本中（故基线为 0，与断言互斥性无冲突）。
3. **禁止事项已写明**：见 §六。

## 六、禁止事项

- 禁止触碰：`.git/`、`scripts/check.sh`（本批不改其行为；F10 只落规范）、`scripts/hooks/pre-commit`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 下归档件、本批规格与其附件。
- 禁止动作：联网（研究已完成，本批不再拉取外部仓库）、安装依赖、在参考副本内跑任何构建或联网命令（红线 3）；不得改 `.tmp/061/mattpocock-skills/` 内任何文件（只读副本，批结删除）。
- 禁止越权：不改 `skills/`（尚未创建）；不改版本号与 tag；不得把产品层业务内容写进底座（F3 只立规范，不写域设计内容）。
- 禁止把研究结论直接落文件：本批一切改动以 §三 清单为准，清单外的一律记 §七 开放项。

## 七、开放项

1. 三段式 description 的「触发句」是否为 no-op：待首个域落地、机制键实测生效后裁定是否压缩或删除（M9）。
2. 侧文件「集中列 ＋ 步骤点名」双处点名写法：低优先，待首个域 references 成型时对照。
3. 域建设状态与分发清单的一致性断言（桶／晋升模型）：待首个域落地、`skills/` 与插件清单出现时立法（S6）。
4. 会话卫生判据（继续／清上下文／交接／子分身／压缩五分法）：对长书多卷创作可能有实际价值，属产品层设计，待业务设计阶段评估（S7）。
5. 版本一致性由人工纪律改为可跑校验（`--check` 模式，差异即非零退出）：待 `skills/` 与插件清单落地时随守卫实施（G5）。
6. 对外／跨会话产物不写行号、改符号锚点：低优先，待施工交底／核验请求模板下次修订时一并（G8）。
7. 域设计（产品层）的门控与停靠写法：M4／S3 的完成判据写法本批落底座（F4），域设计侧的落位待业务设计阶段与开放项 4 同批处理。
8. `AGENTS.md` 加粗密度存量超兜底阈值（59 处 `**` ＞ 66 行 ÷ 3）：本批**未引入**（改动前后同为 59），且 `文档写作标准` §一.3.8 的定性规则（加粗仅用于红线／路径／数字／闸门四类、一段至多一处）已满足；属存量债，待 `check.sh` [3] 段内容轨检查器落地时一并裁定（调阈值或改格式），记此以免将来被误当回归。
9. 研究副本（`.tmp/061/mattpocock-skills/`，只读）于批结时删除；如需复核本批证据，按 §一 的 repo ＋ commit sha 重新克隆（同一 commit 内容一致）。

## 八、审查记录

**核验结论（规划方，施工后三步）**：

- **第 1 步 改动面核对：通过**——`git diff --name-only` 对照 §三 清单：改动 12 份（`AGENTS.md`、`CHANGELOG.md`、`docs/specs/施工机制.md`、8 份标准，以及末轮整改新增的 `docs/product/架构设计.md`）与清单逐一吻合、无清单外文件；禁改面（`scripts/check.sh`／`scripts/hooks/pre-commit`／`LICENSE`／`docs/specs/archive/`）**零 diff**；`.tmp/061/mattpocock-skills/` 研究副本全程未被改动。
- **第 2 步 断言独立重跑：12/12 通过**（规划方自跑，退出码不经管道；链接 117 条／0 断链、`check.sh` 退出 0、CHANGELOG 条目 180 字符）。
- **第 3 步 内容抽查（标准路径＝规划方通读抽查）：通过**。抽读 7 处关键改动：`文档写作标准` §一.7／8／9 三新节与「维护顺延为 §一.10」＋ `## 目录`、`AGENTS.md` §四 格式行与 12 条条目（逃生口 ＋ 解冻触发条件齐备）、`施工机制` §八 模板新增可选节、`规格写作标准` §10 与目录、`skill形态标准` §3 回写四要素、`触发与交互标准` §2.2 三级停靠、`测试与验收标准` §1.1 段输出规范——均语义通顺、指向实存、无断裂截断。本批无用户可观测行为改动，不涉真机走查。
- **施工方 4 条 P1 裁定**：P1-1（`架构设计:19` 的「三处实测口径」在 F7② 收口后应为两处）**采纳** → 末轮整改；P1-2（立法质量以内联正／反例履约、不扩各件 §二，以守住改动清单范围）**采纳**；P1-3（`规格写作标准:85` 过渡口径句自相矛盾且属批次叙事）**采纳** → 末轮整改（整句删除）；P1-4（`AGENTS.md` 加粗密度存量超兜底阈值）**记录为债**，非本批引入，已入 §七 开放项 8。
- **施工方 2 条未决裁定**：① `施工机制` §八 模板插入可选节致编号顺延（开放项→七、审查记录→八）**接受**——模板内部编号无外部引用（已 grep 核），节序语义合理（决策记录置于禁止事项之后、开放项之前）；② 临时脚本 `.tmp/061-build/link_scan.py` 与 `.tmp/061/` 研究副本**由规划方批结时一并删除**（红线 2），本轮核验第 2 步已改用规划方自写扫描器独立复跑。

成稿审查分身（fresh、只读；被要求**拿只读副本逐条核证据**）首轮：**findings 共 29：P0:1 P1:13 P2:15**。规划方逐条复核后**全部采纳（29／29）**，已全数进本版：

- **P0**（引文张冠李戴）：M8 原写「`disable-model-invocation` 设过头会被宿主整体过滤」，实为**另一宿主的同类键**——`CHANGELOG.md` 1.2.2 段回退的是 Codex 的 `policy.allow_implicit_invocation: false`；`.agents/invocation.md:5-6` 亦不含该主张。已改述为「另宿主的同类键曾因过度设置被整表过滤」，只作**通则证据**（机制键须实测可见性）。
- **P1 十三条**：M3 引文范围 `:30-38`→`:30-43`；M7 引文越界 `:78-96`→`:77-81`（该文件仅 81 行）；`## Process` 计数 9→**6**、`## Steps` 5（恰名）；M4 第二落面无承接 → 记 §七 开放项 8；**F7② 现状前提不实**（`skill形态标准`:45 早已载两项实测点）→ 改为「本批只新增回写格式四要素」并加 §三 锚点行 `:60` 同步面；F5 锚点 `:54`→`:58`（原为沿用过期行号）；**F1 编号缺号**（原方案 7 空出）→ 改取 7／8／9、维护顺延 10，并删无对象的「同步 §一.7 引用」句；§三 标题 F1–F10→**F1–F12** 且 F11 原落入第二表格（删空行）；断言 3／10 的 token **不在替换文本中**（验收必红）→ 改 token 对齐措辞；**F10 落点互斥**（`运行环境标准`:46 已声明守卫不适用 §5）→ 改落 `测试与验收标准` §1.1 并加边界句同步面；G5「双向」不实（实为**单向复制 ＋ `--check` 比对**）→ 改述并同步 F11／断言 10；F8 漏引用连锁（`运行环境标准`:42 括注逐项列字段名）→ 补同步面。
- **P2 十五条**：G3 引文范围→`:15、:25、:44-68`；M11「已覆盖」不实→**部分采纳／记开放项**；G8「12 条」无锚点→「约 12 条（散见多件）」；侧文件区间 20–207→**15–207**；元层份数口径写明（按件 20）；「工作树干净」→「除本规格外干净」；F7① 补层级归并（廉价检查点＝§2.2 停靠的第三种取值）；F5 限定为「下沉判据」并把拆分判据留给 F4；F2 例外扩列 §四 不做清单；断言 11 补命令与「已跟踪＋未跟踪」口径；**结论列取值扩展已声明**（部分采纳／印证／记录三个标记）；**新增 F12**——`文档写作标准`（F1–F3 后必超 100 行）与 `规格写作标准`（**现 110 行已超**）补 `## 目录`。

审查位另核已清：§一 机械事实（副本 `3cca18b`：164 文件／md 6913 行／37 份 SKILL.md／22 user-invoked ＋ 15 model-invoked／frontmatter 恰 4 键／SKILL.md 最长 140、侧文件最长 207／无行数上限条款／`AGENTS.md` 0 行且模式 120000＝符号链接），不符项已列入上表；**24 组引文抽样**（M1／M2／M4／M5／M6／M9／M10、S1–S7、G1／G2／G4／G6／G8 逐条 `sed -n` 对照）；裁定映射逐条核（M1→F1、M2→F1、M3→F1＋F5、M4→F4、M5→F6、M6→F2、M7→F1、M8→F7②、S2→F7①、S3→F4、S5→F10、G1→F8、G2→F9、G3→F3、G5→F11；M9／S1／S6／S7／G5／G8 分别落开放项 1–6）；F10／F11 与 §六 禁止事项自洽；断言 1–10 基线实跑与我方所记完全一致；断言 11 实测 115／0 且 `check.sh` 退出 0。
