# 062-2026-09-12-产品层内容第一批

> 定位：把存档分支（`main`）里的产品层材料迁入 `docs/product/` 两件——`架构设计.md`（选型理由与成长路径）与 `产品定义.md`（定位、范围、目标用户、成功判据）；五类旧口径（六域地图、退役域词条、台账编号族、部署循环、术语与维护）**废弃不入库**。
> 自由度口径：[A]＝逐字迁移或逐字改写（源文即目标文本）｜[B]＝按条目方向改写｜[C]＝新写。
> 必读输入：`AGENTS.md`、`docs/product/架构设计.md`、`docs/product/产品定义.md`、`docs/product/术语表.md`、`docs/standards/触发与交互标准.md`（§2 门控与停靠）、`docs/standards/skill形态标准.md`（§8 流程设计纪律）、`docs/specs/施工机制.md`；迁移原料见 §一 存档基线
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**存档原料**（`git show 'main:<路径>'` 实测）：

| 件 | 体量 | 结构（H2 级） |
| --- | --- | --- |
| `main:docs/product/总纲.md` | 319 行／15687 字符 | 定位与范围｜六域地图与状态｜设计理念｜基础设施选型｜路线与阶段｜外部对标与规范依据｜术语（叫法权威）｜维护 |
| `main:docs/product/大纲阶段追踪档.md` | 148 行／7007 字符 | 甲·作者要求（验收基准）｜乙·走查发现｜交叉引用｜维护 |
| `main:docs/product/大纲阶段设计文档.md` | 787 行／39805 字符 | 写法纪律｜规格步｜资料收集步｜草稿步｜打磨定稿步｜横切机制｜附录 A／A-2／B／C｜S 编号索引（**归 063 批**） |

**我方两件的现状**：`架构设计.md` 41 行（§一 域清单／§二 调用形态 5 条／§三 域间衔接／§四 成长路径，§五 开放项挂「§四 选型理由待迁移」）；`产品定义.md` 35 行（§一 产品一句／§二 产品范围（三域）／§三 与开发底座的分界／§四 目标用户（**待设计**）／§五 成功判据（**待设计**）／§六 开放项）。

**作者裁定（本批输入）**：① **目标用户＝先自用、结构留门**（写法兼顾"自己能读懂"与"陌生人能读懂"，不专门做新手指引）；② **保留三评委**——旧总纲「导入／独立审查／抓取底座独立化均不做」指不把独立审查做成**独立域或技能**，不是不做评审角色；评审归域内步骤（三评委角色），其设计细则归 063 大纲域设计。

**勘验范围提示**：本批映射的首次勘验只扫了 `docs/product/`，故「在 rebuild 无归属」的初判两处失准（兼容四原则与外部参考纪律其实早已落 `docs/standards/` 两件，见 §2.2 修正行）——凡判「无归属」须全仓 grep，不得只扫产品层。

**两处同名异义（迁移时禁止混用）**：总纲 §六 的 **T0／T1／T2＝权威源分级**（官方规范／官方实物与社区标杆／目录评测）；总纲 §四 的 **T1／T2／T3＝触发档位**（组件物化／选择性安装／域解耦），后者现行定义在 `docs/product/架构设计.md` §四。

**本批判定的我方现行依据**（逐条读过）：`AGENTS.md`（宿主唯一／红线／不做清单 12 条／三条分工原则）、`架构设计.md` §一 域清单与调用形态、`触发与交互标准.md`（§1 入口唯一与机制键／§2 三级停靠）、`skill形态标准.md`（§3 frontmatter／§8 完成判据）、`施工机制.md`（§二 路径分级／§三 过程产物两项）。

## 二、迁移映射（来源节 → 落点）

### 2.1 迁入 `架构设计.md`

| 来源节（`main:docs/product/总纲.md`） | 落点 | 方式 | 改写要点 |
| --- | --- | --- | --- |
| §三 设计理念·域是平行的门／生产者只落盘（53–54） | §三 域间衔接 | **[A] 逐字补入** | 现状实测：「生产者只落盘，消费者定义读取」已承接（`架构设计` §三，含 `书目录契约` §3）但**「域是平行的门，不是管道」全仓零命中（从未承接）**，须逐字补入总纲:53；「Pipes and Filters／数据耦合」模式名一并补入（现行全仓零命中，下同） |
| §三·文件账本律（57） | **不迁**（现口径在 `AGENTS.md` §五 第 3 条） | 废弃 | 产品层不复制底座条文 |
| §四.1 单／多 skill 对照表与实测数据（61–74） | §四 成长路径 | [B] 改写 | 六处「六域」→三域；「单 skill 六域（未采纳）」列名→「单 skill 三域」；**前作实测数据保留但加时态标注**（「旧仓六域时期实测：合计 188 份 references／约 26,000 行、跨 skill 同步副本 64 组；三域现态未复测」）；「前作为此养 64 组字节对账＋守卫」类表述按《外部参考纪律》降级为**沿革出处**（不作设计依据） |
| §四.1 逃生舱（76） | §四 成长路径 | [B] 改写 | 旧前提（输入侧 scan／analyze 可低税拆出）随域退役消失 → 保留「产出侧域深度耦合共享状态，不拆」并三域化 |
| §四.1 成长路径与 T1／T2／T3（78–82） | §四 成长路径 | 逐字迁移（已承接） | 已逐字在册；补回引言「内容量增长不构成拆分理由（渐进披露下内容躺磁盘不进上下文）；共享约定随域增加而增值」 |
| §四.2 精准入口（84–88） | §二 调用形态 | 逐字迁移（已承接） | 5 条已覆盖；删举例中的 `/abzu-scan`；「模型自主匹配层已退役」属沿革入 `CHANGELOG`，不入正文 |
| §四.3 setup 触发条件与职责边界（100） | §四 成长路径（T1 条补注） | [B] 改写 | 「setup 只处理硬依赖；软配置（题材偏好、平台选择）不进 setup」保留；示例须与 063 的域交互细则对齐（本批只搬条文，不涉域细则） |
| §二 setup 行与共享知识库安放（46、49） | §四 成长路径 | [B] 摘要 | 与 T1 条同源，只补一句「共享知识库由 setup 物化进书项目」 |
| §一 套件形态与当前阶段（11–12） | **迁 `产品定义`**（见 2.2） | — | — |

### 2.2 迁入 `产品定义.md`

| 来源节 | 落点 | 方式 | 改写要点 |
| --- | --- | --- | --- |
| §一 定位与范围（11–12） | §一 是什么 | [B] 改写 | 「六域各一 skill」→三域；「卷纲／正文／文风三域占位待建」→**文风域不存在**、三域均未建且 `skills/` 尚未创建（依据 `AGENTS.md` 头注与 `架构设计` §一）；宿主唯一 Claude Code |
| §一 封存域声明（13–14） | — | **废弃** | 扫榜调研（scan）／拆书分析（analyze）已退役，沿革入 `CHANGELOG` |
| §一 v1 范围（15） | §三 范围与不做 | [B] 改写 | 「仅六域」→「仅三域」；「导入／独立审查／抓取底座独立化不做」按作者裁定改写为「**不做独立审查技能**——评审以域内角色（三评委）承担」 |
| 新写 | §二 目标用户 | [C] | **先自用、结构留门**：文档写法兼顾自己回看与陌生人读懂，不专门做新手指引与安装教程 |
| 新写 | §四 成功判据 | [C] | 写可判定的三条（见 §三 F5），标「待作者校准」 |
| §二 六域地图与状态·域清单表（19–26） | `架构设计` §一 | 已承接（三域化） | 三域清单与建设状态已在册；六域行全删（沿革入 `CHANGELOG`） |
| §二·域层真源指向（28） | — | **废弃＋改指** | 旧真源《大纲阶段设计文档》已作废 → 现行真源为 `docs/product/大纲域设计.md`（该件头注已指认）；scan／analyze 的 references 指向整句删除 |
| §二·落定目录结构（30–47） | — | **废弃** | 仓库侧目录树含已退役域与 `skills/` 未建现状；现行结构真源＝`命名标准` §1 目录树 ＋ `AGENTS.md`；abzu-setup 物化说明按 §2.1「§二 setup 行」处置 |
| §三·阶段门控（55） | — | **废弃（已承接于底座）** | 现行口径＝`触发与交互标准` §2 门控与停靠（三级停靠）；产品层不复制 |
| §三·会话恢复（56） | — | **废弃（已承接于底座）** | 现行口径＝`触发与交互标准` §1 正反例与 `skill形态标准` §2.1（入口判定按文件证据） |
| §六 设计决策对照（213） | — | **废弃（已散入）** | 多技能骨架 → `架构设计` §一；域间文件通信 → §三；阶段门控 → `触发与交互标准` §2；逐条重列无增量 |
| §六 历次调研采纳与拒绝（215–218） | — | **废弃（已入规格）** | 属规格沿革，已在 `docs/specs/archive/`（060／061）；现行程序＝`施工机制` §八 参考研究裁定表 |
| §五.2 中期·大纲域实现（167–177） | — | **转 063** | 三评委打磨循环／方法论萃取／schema／书目录脚手架均属大纲域设计 |
| §五.1／§五.3 路线（152–165、179–203） | §五 路线（摘要） | [B] 摘要 | 只留三域路线与当前阶段；R1 卷纲／正文（删文风）；「R4 skill 调用契约」前提消失（拆书域已退役） |
| §四.4 项目结构（112–132）／§四.5 质量闸分层（134–140）／§四.3 部署与实测循环（90–98、102–110） | — | **废弃** | 属开发底座：结构归 `AGENTS.md`／`命名标准`，质量闸归 `施工机制` §四／§五，走查归 `测试与验收标准` §3 |
| §四.6 版本与兼容（142–146） | — | **已落（无须迁移）** | 实测 `版本与分发标准.md`:54–58 §4「写作项目兼容」已逐字载兼容四原则、迁移执行边界与「替代机制待设计」——初判「无归属」失准（初勘未扫底座，见 §一 勘验范围提示） |
| §六 外部参考纪律与分级（205–207） | — | **已落（纪律）＋废弃（清单）** | 实测 `安全与合规标准.md`:13–18 §2「外部参考的边界」已载只读、引用分级如实标注、**前作不作设计依据（例外：架构参照研读）**与研究程序四类；T0／T1／T2 权威源清单属研究沿革，废弃 |
| §五 引言·债的落点（150）／§五.1 编号族（A／G／N／R／S／C） | — | **废弃** | 台账机制已废止；现行落点＝当批修完／规格开放项／所属标准已知缺口（`施工机制` §三、§六） |
| §七 术语（220–312） | — | **已承接** | 38 词条已在 `术语表.md` §一–§三（总纲 §7.1–7.3 实数 **39** 条，少 1 条为「文风指南」——随文风域退役删除，与 术语标准 依据行一致）；治理与资产类词条（子代理／分身／skill 资产／仓库治理文档／调用双层）现由 `术语标准.md`:23–27 §3 承载；扫榜／拆书类全删 |
| §八 维护（314–319） | — | **废弃** | 维护纪律归 `施工机制` §三／`AGENTS.md` §四／`skill形态标准` §5 |

### 2.3 备料（不入本批，交 063）

`main:docs/product/大纲阶段追踪档.md`：**甲·作者要求（验收基准）9–67 行**已逐条摘出并按「产品定义的成功判据面／大纲域设计的验收基准面」分面（R1.1–R1.5、R2.1–R2.11、R3.1–R3.7、R4.1–R4.4、R5.1–R5.6）；**乙·走查发现与交叉引用**随追踪档机制废止，不入库——**追查档的"逐条追加"机制不复活**（060 已裁：走查记录落当批规格、不建台账）。

## 三、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F0 | `产品定义.md` 目标节序（**施工前先定**） | 目标序：§一 产品一句（语义＝「是什么」；**节名不改**）／§二 产品范围（三域，保留）／§三 与开发底座的分界（保留）／§四 目标用户／§五 成功判据／§六 范围与不做（新增）／§七 路线（新增）／§八 开放项（原 §六 顺延）——**只在尾部追加与填实，既有 §一–§三 节号不动** | 审查 P0-2 | [B] |
| F1 | `架构设计.md` §四 成长路径＋头注依据行 | 由「待设计」改写为完整选型理由与成长路径：① 决定性判据（共享状态是分水岭）＋单／多 skill 对照（三域化，含旧仓实测数据与其时态标注）；② 逃生舱（产出侧域不拆）；③ 「内容量增长不构成拆分理由」引言；④ T1／T2／T3 三档（已逐字在册，保留）；⑤ T1 补注：setup 只处理硬依赖、共享知识库由 setup 物化；⑥ 撤下「选型理由待迁移」标注、§四 标题去「（待设计）」；⑦ **头注依据行同步改写**（撤「选型理由与成长路径待设计」）。**豁免说明**：实测数据的时态标注「旧仓六域时期实测……三域现态未复测」中的「六域」属**沿革标注**，允许出现——断言 1 已按词形收窄，不与之互斥 | 2.1 各行 ＋ 审查 P0-1／P0-3 | [B] |
| F2 | `架构设计.md` §三 域间衔接 | **[A] 逐字补入**总纲:53–54 两条：①「**域是平行的门，不是管道**——跨域无顺序锁，大纲可反复打磨、随时回改；唯一依赖是文件账本律的产物检查（缺产物＝提示，非门禁）」；② 生产者只落盘一条的**模式名括注**（对应 Pipes and Filters 架构模式与数据耦合）。现状：「生产者只落盘…」已承接，①与模式名**全仓零命中**，属补入而非保留 | 2.1 §三 行 ＋ 审查 P1-2 | [A] |
| F3 | `架构设计.md` §五 开放项 | **只撤下**「§四 成长路径的选型理由待迁移」一行；**不补**版本兼容与外部参考两条（实测二者早已落 `版本与分发标准` §4 与 `安全与合规标准` §2，补入即重复条目＝反模式 #4） | 2.2 两行 ＋ 审查 P1-1 | [B] |
| F4 | `产品定义.md` §一 ＋ **头注依据行** | §一「产品一句」三域化（**写法须避断言 2 的禁词**：不写「文风域不存在」而写「三域均未建设，`skills/` 尚未创建——套件范围即此三域」；宿主唯一 Claude Code）；**头注依据行改写**（撤「目标用户与成功判据待设计」——否则断言 3 不可达） | 2.2 两行 ＋ 审查 P0-3 | [B] |
| F5 | `产品定义.md` §四／§五（**填实既有节，非新增**） | §四 **目标用户**：先自用、结构留门（写法兼顾自看与陌生人读懂，不专门做新手指引与安装教程）；§五 **成功判据**（可判定，三条，标「待作者校准」）：① 能力——拿一本新书从立项到定稿全程只用本套件、不借助会话外说明；② 质量——域交付时无「当时该问没问」的洞（零待定推进）；③ 可维护——六个月后只看 `docs/` 能重建意图与取舍 | 作者裁定 ＋ 甲节「产」面条目 | [C] |
| F6 | `产品定义.md` §六／§七（尾部新增两节） | §六 **范围与不做**：三域范围＋「导入／抓取底座独立化不做」＋按作者裁定改写「独立审查不做」为「**不做独立审查技能**——评审以域内角色（三评委）承担」；§七 **路线**：三域路线与当前阶段（摘要自总纲 §五，删文风域与已失效前提） | 2.2 §一 v1／§五 两行 | [B] |
| F7 | `产品定义.md` §六→**§八**（开放项顺延） | 原 §六 开放项顺延为 §八，并**改写内容**：撤下「§四 目标用户与 §五 成功判据待设计」行（本批已填实），保留其余开放项 | 审查 P0-2／P0-3 | [B] |

## 四、验收断言（含预验记录）

### 断言 1｜旧六域框架词形清零

```bash
grep -c -E "六域地图|六域各一|单 skill 六域|六域套件" docs/product/架构设计.md
```

```bash
grep -c -E "六域地图|六域各一|单 skill 六域|六域套件" docs/product/产品定义.md
```

两命令各期望 0；预验基线 0／0（含英文裸词 `scan`／`analyze`——旧总纲以裸词指退役域，须按词形一并清零，注意不误伤 `analysis` 类）。**词形说明**：单字「六域」不作零命中判据——F1 的实测数据时态标注（「旧仓六域时期实测……三域现态未复测」）必须保留「六域」以如实标注出处域数；本断言只禁**旧框架词形**（六域地图／六域各一 skill／单 skill 六域）。

### 断言 2｜退役域在改动件清零

```bash
grep -c -E "扫榜|拆书|文风|abzu-scan|abzu-analyze|abzu-style|(^|[^a-z])(scan|analyze)([^a-z]|$)" docs/product/架构设计.md
```

```bash
grep -c -E "扫榜|拆书|文风|abzu-scan|abzu-analyze|abzu-style|(^|[^a-z])(scan|analyze)([^a-z]|$)" docs/product/产品定义.md
```

两命令各期望 0；预验基线 0／0。

### 断言 3｜目标用户与成功判据就位

```bash
grep -c "先自用" docs/product/产品定义.md
```

```bash
grep -c "待设计" docs/product/产品定义.md
```

```bash
grep -c "待设计" docs/product/架构设计.md
```

第一命令期望 ≥1（预验基线 0）；第二、三命令期望 **0**（预验基线 **6**／**2**——「待设计」标记即本批要消灭的对象；用清零侧断言，因为「成功判据」现值已有 4 处且全属待设计标记，按存在性断言会假绿）。

### 断言 4｜选型理由迁移就位（含实测数据时态标注）

```bash
grep -c "决定性判据" docs/product/架构设计.md
```

```bash
grep -c "未复测" docs/product/架构设计.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 5｜旧真源标注撤下

```bash
grep -c "选型理由待迁移" docs/product/架构设计.md
```

期望 0；预验基线 1（`架构设计.md`:40「§四 成长路径的选型理由待迁移」，属 §五 开放项行，F3 撤下）。

### 断言 6｜追踪档机制不复活

```bash
grep -c -E "追踪档|台账|编号族|走查发现|部署与实测循环|质量闸分层" docs/product/架构设计.md
```

```bash
grep -c -E "追踪档|台账|编号族|走查发现|部署与实测循环|质量闸分层" docs/product/产品定义.md
```

两命令各期望 0；预验基线 0／0。**词表说明**：§六 禁止复活四类机制（台账／追踪档编号族、走查发现逐条追加、部署与实测循环、质量闸分层），故按词表判而非单词判（单判「追踪档」会漏其余三类）。**注**：「总纲」是本仓**合法词**（既指大纲域的覆盖阶段「立项、设定、全书总纲」，也指存档件 `main:docs/product/总纲.md`），故不作零命中判据——本批只保证「追踪档」这一废止机制不复活。

### 断言 7｜T0／T1／T2 与 T1／T2／T3 不混用

```bash
grep -c -E "T0|T1 权威源|T2 目录评测|官方实物与社区标杆" docs/product/架构设计.md
```

期望 0（权威源分级不入本件；词形含 T0 与「T1 权威源／T2 目录评测／官方实物与社区标杆」，防以 T1／T2 措辞混入——总纲 §六 分级与 §四 触发档位同形异义）；预验基线 0。

### 断言 8｜链接与守卫

```bash
grep -rhoE '\]\([^()]*\)' $(git ls-files '*.md') | grep -vE '\]\((http|#|mailto)' | wc -l
```

扫描口径（脚本内实现）：以 `git ls-files '*.md'` 取**已跟踪** .md，并遍历工作树**未跟踪**的 .md（060 教训：只扫已跟踪会假绿）；提取相对链接（排除 `http`／`#`／`mailto:`），按所在目录解析存在性。期望断链 0 且条数 ≥117；预验基线 117 条／0 断链。另 `bash scripts/check.sh` 退出 0；预验退出 0。（本批为内容批，链接面向 `docs/` 内互引与 `docs/product/` → `../standards/` 引用。）

### 断言 9｜范围与不做、路线两节就位

```bash
grep -c "范围与不做" docs/product/产品定义.md
```

```bash
grep -c "路线" docs/product/产品定义.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 10｜「域是平行的门」补入到位

```bash
grep -c "平行的门" docs/product/架构设计.md
```

期望 ≥1；预验基线 **0**（全仓从未承接，本批 [A] 逐字补入；F2 的完工凭此断言而不仅是"零命中保持"）。

### 断言 11｜过程产物两项

`CHANGELOG.md` 的 `[Unreleased]` 节含本批一行（≤400 字符）；规格验收通过后移入 `docs/specs/archive/`（移动不留副本）。

**未预验项**：无（断言 3／4 的期望 token 为本批新写文本中的既定用词，现文 0 命中）。

## 五、规格自审（三条）

1. **锚点现场核过**：§一 存档三件的体量与结构、我方两件现状与开放项、两处同名异义的出处行号均为现场实测（`git show main:` ＋ `grep`）；§四 断言 1–8 基线为规划方实跑值。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 3／4 的期望 token（「先自用」「成功判据」「决定性判据」「未复测」）在改动前 0 命中、且为本批目标文本的既定用词；断言 1／2／5／6／7 均为零命中侧（词形已按审查 P0-1／P2-1／P2-2 收窄与扩表），与替换文本无交集；断言 10／11 为存在性侧，其 token（「范围与不做」「路线」「平行的门」）在改动前 0 命中。**F2 走 [A] 逐字补入，替换文本即总纲:53–54 原文；F3／F6 的正面验收见断言 10／11，F2 的逐字性由核验第 1 步 diff 为零兜底。**
3. **禁止事项已写明**：见 §六。

## 六、禁止事项

- 禁止触碰：`.git/`、`scripts/`、`LICENSE`、`.zcode/`、`docs/standards/` 全部（本批为**纯内容批**，不动任何规范条目）、`docs/specs/施工机制.md`、`docs/specs/archive/`、本批规格自身；`docs/product/` 内**只改 `架构设计.md` 与 `产品定义.md`**（`术语表.md`／`书目录契约.md`／`验收场景.md`／三份域设计件本批零改动）。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；不得把 `main` 存档件的原文整段复制（只迁 §二 映射表列出的节，其余五类一律丢弃）。
- 禁止越权：不改版本号与 tag；不新增目录；**不得把本批扩成"动规范"**（版本兼容四原则与外部参考纪律只记 §七 开放项）。
- 禁止复活已废机制：台账／追踪档编号族、走查发现逐条追加、部署与实测循环、质量闸分层——一律不入库。

## 七、开放项

0. **加粗密度数值兜底与入口／架构类文档的冲突（存量）**：`架构设计.md` 加粗 26 处 ＞ 行数÷3（20），**改动前基线即超**（20 处 ＞ 13）——与 061 批记录的 `AGENTS.md`（59 处 ＞ 22）同型。定性规则（加粗仅用于红线／路径／数字／闸门）已满足；属存量债，待 `check.sh` [3] 段内容轨检查器落地时与 `AGENTS.md` 一并裁定（调阈值或改格式）。

1. **归档规格的体量**：`docs/specs/archive/` 已 4 件（059–061 含附件），后续内容批会产生更多；是否立法「归档规格的引用与检索方式」待观察两批后再定。
2. **063 批范围**：`docs/product/大纲域设计.md` 迁移（源 `main:docs/product/大纲阶段设计文档.md` 787 行 ＋ 追踪档甲节分面后的「域」面条目）；届时须决定附录模板（六件套／情绪词表）暂留产品层还是直接进 `skills/abzu-outline/references/`。
3. **三评委的实现形态**：本批只裁定"保留"，其角色定义、审什么、产出物（处方报告）归 063 大纲域设计。

## 八、审查记录

**核验结论（规划方，施工后三步 ＋ 单维核对）**：

- **第 1 步 改动面核对：通过**——`git diff --stat` 恰 3 文件（`CHANGELOG.md` ＋ `架构设计.md` ＋ `产品定义.md`，共 +59/−14），与 F0–F7 声明的范围一致；禁改面（`docs/standards/` 全部、`docs/specs/施工机制.md`、`术语表`／`书目录契约`／`验收场景`／三份域设计、`scripts/`）**零 diff**。
- **第 2 步 断言独立重跑：11/11 通过**（规划方自跑；含 F2 逐字性程序化比对源件两条均 True）。首次跑出 1 处 FAIL：**063 规格内两条链接指向其待建件**（`大纲域设计-附录.md`）——裁定为「在制规格引用尚未创建的目标件**用行内代码、不用 markdown 链接**」（依据 `命名标准` §5「文件名提及（无需跳转）→ 行内代码」），已修 063 规格两处；复归 123 条／0 断链。断言 8 的口径维持「已跟踪 ＋ 未跟踪」（060 教训），不因在制规格而放宽。
- **第 3 步 内容抽查：通过**。两件通读：`架构设计` §一 域清单真源声明／§三「域是平行的门，不是管道」与模式名括注逐字补入／§四 选型理由（决定性判据 ＋ 三域化对照表 ＋ 实测数据时态标注 ＋ 逃生舱）＋ 拆分触发器（T1–T3 ＋ 硬软依赖补注）；`产品定义` §一 三域化（避禁词写法）／§四 目标用户（先自用、结构留门）／§五 成功判据三条可判定／§六 范围与不做（含「不做独立审查技能——评审以域内三评委角色承担」）／§七 路线。均语义通顺、指向实存。
- **单维核对（内容批分级）**：① 引用闭合——本批两件 0 断链，全仓 123 条／0 断链；② 数字一致——三域清单在两件与 `架构设计` §一 一致；③ 既有条文冲突——「三评委」与 `术语表` 词条一致、「不做独立审查技能」限定于产品侧不与 `施工机制` §五 的开发侧审查分身冲突、域清单真源唯一（两处均为指针）。
- **施工方 3 条 P1 裁定**：P1-1（F0 的「§一 是什么」与「节名不改」措辞互斥）**采纳**——保留原节名「产品一句」（语义即「是什么」），规格 F0 同步改写；P1-2（`架构设计` 加粗密度 26 ＞ 行数÷3＝20，**改动前基线即超** 20 ＞ 13）**判为存量债**，已入 §七 开放项 0（与 061 批记录的 `AGENTS.md` 同型，待内容轨检查器落地时一并裁定）；P1-3（F4 的「文风域不存在」与断言 2 禁词自斥）**采纳**——施工方按断言执行、规格 F4 同步改写为避禁词写法。
- **规划方收尾微修一处**（同 061 批先例，已声明）：`产品定义.md`:53 的引用精度——原「清单与做法见 …§一.2」把两节压成一个节号，改为「清单见 §一.2、做法见 §一.3；跨域场景见验收场景」。除此一处外未改动本批产物。

成稿审查分身（fresh、只读）首轮：**findings 共 15：P0:3 P1:5 P2:7**。规划方逐条复核取证后**全部采纳（15／15）**，已全数进本版：

- **P0-1**｜断言 1（「六域」清零）与 F1（写入「旧仓六域时期实测…」）**直接互斥** → 断言 1 按**词形收窄**（六域地图／六域各一 skill／单 skill 六域／六域套件），单字「六域」在沿革标注中豁免，并在 F1 写明豁免。
- **P0-2**｜`产品定义.md` 节号冲突：F4／F5／F6 自称「§三 范围与不做」「§二／§四（新增）」与实文（§一 产品一句／§二 产品范围／§三 与开发底座的分界／§四 目标用户／§五 成功判据／§六 开放项）撞号 → 新增 **F0 目标节序**（§一–§三 保留、尾部追加 §六 范围与不做／§七 路线、原 §六 开放项顺延 **§八** 并列 F7），F4／F5／F6 节号全部重标为「填实既有节」。
- **P0-3**｜断言 3（「待设计」清零）**不可达**：三处「待设计」不在任何 F 行内（`架构设计.md`:4 依据行、`产品定义.md`:4 依据行、:34 开放项）→ 分别并入 F1（⑦ 头注依据行）、F4（头注依据行）、F7（开放项顺延改写）。
- **P1-1**｜两条「转开放项」的目标内容**早已落地**（`版本与分发标准.md`:54–58 §4 载兼容四原则与迁移执行边界；`安全与合规标准.md`:13–18 §2 载外部参考纪律含「前作不作设计依据，例外是架构参照研读」）→ §2.2 两行改标「已落（无须迁移）」；F3 收窄为「只撤下选型理由待迁移」；§七 开放项 1／2 删除。**根因记 §一 勘验范围提示**：初勘只扫 `docs/product/`，判「无归属」须全仓 grep。
- **P1-2**｜F2 现标「保留 Pipes and Filters／数据耦合」但**全仓零命中**（「保留」指向不存在的现状）→ F2 改为 **[A] 逐字补入**，并**新增补入项「域是平行的门，不是管道」**（总纲:53，实测全仓从未承接）；2.1 行方式同步改「[A] 逐字补入」；新增断言 11 作正面验收。
- **P1-3**｜断言 8 无命令与口径 → 内嵌扫描命令与「已跟踪 ＋ 未跟踪」口径（060 教训）。
- **P1-4**｜§二 映射表漏 7 块（总纲 §二 六域地图表／域层真源行／落定目录结构、§三 阶段门控／会话恢复、§六 设计决策对照／历次调研）→ 逐块补处置行（废弃／已承接及出处）。
- **P1-5**｜断言覆盖不足（F2／F3／F6 无正面断言）→ 新增断言 10（范围与不做／路线两节就位）与断言 11（平行的门补入），过程产物顺延为断言 12。
- **P2 七条**：断言 7 加词形（防以 T1／T2 措辞混入权威源分级）；断言 6 扩为四类机制词表（台账／编号族／走查发现／部署与实测循环／质量闸分层）；断言 2 补英文裸词 `scan`／`analyze`（按词形避免误伤 `analysis`）；必读输入节名改正（`触发与交互标准` §2＝门控与停靠；`skill形态标准` §8＝流程设计纪律）；§2.2 §七 承接指认改指 `术语标准.md`:23–27 §3 并记录 39→38 的差额（少「文风指南」，随文风域退役）；§一 补列 `产品定义` §六 开放项；2.1 §四.1 源区间 61–72 改 **61–74**（实测数据在 :74）。

审查位另核已清（9 项）：存档三件体量与 H2 结构；我方两件现状与「成功判据」4 处／「目标用户」4 处的语境；断言 1–7 基线逐条复现；两处同名异义的出处行号；三条「已承接」抽查（§四.2 精准入口 → `架构设计`:18–22；38 词条 → `术语表` 实数 38；治理与资产类词条 → `术语标准`:23–27）；五类废弃无误伤；与 `术语表` 三评委、`AGENTS.md` §四 不做清单无冲突；断言 9／12 可执行；`docs/product/` 八件清单与 §六 禁止面吻合。

- 施工后核验与**单维核对**（引用闭合／数字一致／既有条文冲突）结论：待回填。
