# 063-2026-09-12-大纲域设计迁移

> 定位：把作者既有的大纲域设计（存档件 `main:docs/product/大纲阶段设计文档.md`，v0.9.6 冻结版，787 行）按现行基座口径**改写迁入** `docs/product/`——分五件：主件（职责／定义性约束／流程总览／写法纪律／评审体系／成本降级／跑通信号／验收清单）＋三个分步件（规格与资料步／草稿步／打磨定稿步）＋附录件（模板规范与十件模板）。作者裁定：**那是他的设计，走改写迁入，不重写**。
> **本批性质＝写文档，不是建技能**：产物只有 `docs/product/` 下的设计文档（供人与后续施工读）；**不创建 `skills/`、不写 `SKILL.md`／`references/`／脚本、不触发 `check.sh` [2]–[5] 段建设**。把本批文档拆成 `skills/abzu-outline/` 运行时资产（域壳 ＋ references ＋ 脚本）是**后续独立的「域建设批」**，那一步才是建技能。
> 自由度口径：[A]＝逐字迁移（源文即目标文本，仅按改写要点替换旧口径与链接）｜[B]＝按条目方向改写｜[C]＝新写。
> 必读输入：`AGENTS.md`、`docs/product/大纲域设计.md`（现骨架）、`docs/product/书目录契约.md`、`docs/product/架构设计.md`、`docs/product/术语表.md`、`docs/standards/` 全部 11 份、`docs/specs/施工机制.md`；迁移源见 §一
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**源件**（`git show 'main:docs/product/大纲阶段设计文档.md'` 实测 **787 行／39805 字符**）：头注 v0.9.6（含 20 行版本变更史 v0.2→v0.9.6）｜写法纪律 13 条（25–76）｜第一章 规格步 1.0–1.2（77–120）｜第二章 资料收集步 2.1–2.4（121–190）｜第三章 草稿步 3.1–3.5 ＋ 方法论挂载总表（191–375）｜第四章 打磨与定稿步 4.1／4.1.5／4.2／4.3／4.4（376–464）｜第五章 横切机制 5.1–5.7（465–549）｜附录 A 模板规范 v2 六件套（550–553）｜附录 A-2 统一情绪词表 v1（554–564）｜附录 B 模板全件 **B-1…B-10**（565–743）｜附录 C 追踪档对照（744–747）｜S 编号索引（748–787）。

**我方现状**：`docs/product/大纲域设计.md` 34 行（§一 域职责与状态／§二 **流程骨架（待设计）**／§三 上线验收清单／§四 域交互细则／§五 开放项）；`docs/product/` 共 8 件、无「大纲域设计-」前缀件；`docs/product/书目录契约.md`:49 的「生产端真源」栏仍写「该件本批只落职责与验收清单，**契约真源仍待设计**」。

**迁移原料之二**：`大纲阶段追踪档.md` **甲·作者要求**（9–67 行，R1.1–R5.6）已按面标注——「产」面（R2.8／R2.9／R2.10／R5.2 等）归 `产品定义` §五 成功判据（062 批 F5 已写）；**「域」面（其余 20 余条）归本批**，作为各步验收清单的**作者原话验收基准**。

**五处与现行口径的冲突（本批逐条处置）**：① **六域 → 三域**（边界句「卷纲归卷纲域、细纲与正文归正文域、文风归文风域」须改；`abzu-style` 已退役）；② **追踪档机制已废止**（附录 C 与 S 编号索引不得迁入；走查结论落当批规格）；③ **拆书／扫榜已退役**（源文「拆书/扫榜搁置不参与」须改；方法论挂载总表的「拆书域六维分层」萃取源须改指）；④ **灵感库**：源件 5.6 依赖《灵感库契约 v1》与环境变量 `ABZU_INSPIRATION_DIR`；**作者裁定：灵感库继续做**（本机库侧已有契约 v1 ＋ 15 条目，独立 git、跨书、**只消费不生产**），定位为**初期状态的可选外部资产**——本批落「接口 ＋ 降级 ＋ 不依赖三条」，不改其名、不复述其内容、不复制其资产（见 §二 该行与 §七 开放项 1）；⑤ **缺新范式**：源件无 061 F3 立的定义性约束／跑通信号条目，主件须补。

**预验基线**（断言在改动前的实测值）：

| 编号 | 判据 | 基线 |
| --- | --- | --- |
| A1 | `docs/product/大纲域设计*.md` 件数 | 1（仅主件骨架） |
| A2 | 主件内「定义性约束」／「It's working if」 | 0／0 |
| A3 | 主件内「待设计」 | **4** |
| A4 | 主件内「作家思维」／「四拍」／「刻意不装」 | 0／0／0 |
| A5 | 全 `docs/product/` 内「追踪档」／「S 编号」／「六域」／「文风域」／「灵感库契约」 | 0／0／0／0／0 |
| A6 | 源件 1.0⑥ 话术 token「书项目建好了」 | 1（源件内） |
| A7 | `书目录契约.md` 内「契约真源仍待设计」／「大纲域设计」 | **1**／1 |
| A8 | 附录件内 `**B-` 行数 | 0（件不存在） |
| A9 | 相对链接条数／断链、`check.sh` | 117 条／0 断链、退出 0 |

## 二、迁移映射（源段 → 目标件）

| 源段（行号） | 目标件 | 方式 | 改写要点 |
| --- | --- | --- | --- |
| 头注（1–21） | 主件头注 ＋ §一 | [B] | **14 行**版本变更史（7–20 行，v0.2→v0.9.6）不入正文（治理文档不写批次叙事，见 `规格写作标准` §9）：沿革由 `main` 存档件（git 历史逐版原文）与 `CHANGELOG` 本批一行回溯，**不另立沿革节**；新头注两行式（定位 ＋ 依据＝迁移自 `main` 存档《大纲阶段设计文档》v0.9.6）；**:21 边界句**（「本域管到定稿门为止——卷纲归卷纲域、细纲与正文归正文域、**文风归文风域**；拆书/扫榜搁置不参与（创作输入＝作者积累＋资料＋方法论＋灵感库）」）**改写后并入主件 §一 域职责与状态**：删「、文风归文风域」（该域已退役），「拆书/扫榜搁置不参与」改「已退役域不参与」，「灵感库」按本轮术语统改（见下行） |
| 写法纪律 13 条（25–76） | 主件 §写法纪律 | [A] | 逐字迁移；① 第 10 条「用户语言规范」术语对照表内的**待收口注（N18）删除**（「采风助手」已是单点权威名，见 `术语表`）；② 对照表左列「采风员」行按现术语改（`采风助手`）；③ 第 11 条「亲和簇原则」与第 13 条「参考标尺用法」的旧编号引用（如「见第三章模块序段」）改指新件章节名 |
| 第一章 规格步 1.0–1.2（77–120） | 分步件·规格与资料步 | [A] | 互动话术**逐字保留**（可执行部分）；1.0⑥ 的「弹窗」措辞与 `触发与交互标准` §3 交互模态对齐；1.2 规格门并入该件门控节 |
| 第二章 资料收集步 2.1–2.4（121–190） | 分步件·规格与资料步 | [A] | 站点白名单／黑名单**逐字保留**（合规口径与 `安全与合规标准` 一致性核对）；「试读段」标注按 `安全与合规标准` 现行用词（「公开样本」）统一；2.2 的「媒介类型／等量纪律」逐字 |
| 源件全档 `Sxx`／`Rxx` 走查与裁定编号（`Sxx` 实测 **126 处／89 行／39 个编号**，如 `S33⑤`、`S26 三型`、`（S21）`） | 各落点所在件 | [B] | **去编号留内容**：括注形态（如「（S21）」「（S33⑤）」）一律删除；已成专名者改描述性名称（`S26 三型` → 「金手指三型」）；保留其**内容与结论**（结论本身已在正文在位，见 §二 附录 C 行）。依据：`规格写作标准` §9「治理文档条文不写批次叙事与裁定标记」＋ 062 §二 已裁「台账编号族废弃不入库」——**两批口径必须一致** |
| 第三章 草稿步 3.1–3.5（191–337） | 分步件·草稿步 | [A] | 逐字；「文风归文风域」等边界句三域化；模块序／四拍权重总表／交互总纲／接口验证（S33⑤）／伏笔速记律 逐字 |
| 方法论挂载总表（339–372） | 分步件·草稿步（附节） | [B] | **26 行**逐条迁移（源件 v0.8 变更行自述「终态 26 行」可交叉印证）；「拆书域六维分层」等萃取源改指（拆书域已退役，改标「舊仓拆书域方法论（已退役，仅作萃取参考）」）；「萃取批后置、过渡层问句即施工期标准」**保留**（现行有效裁定）；`referFile` 仓外只读目录路径保留并标注「仓外只读参考，不随仓分发」 |
| 第四章 4.1／4.1.5／4.2／4.3／4.4（376–464） | 分步件·打磨定稿步 | [A] | 三评委隔离装载表／人设三层／处方工单五件／五环闭环／孤儿检查 逐字；「spawn 分身」与 `施工机制` §五 的分身纪律交叉引用（本域分身＝运行时物化，属 T1 之后；当前以主会话自跑＋降级通报为准） |
| 第五章 5.1 状态机与断点（467–479） | 主件 §流程总览 | [A] | 逐字（五值状态＋产物状态表＋断点续跑＋「门控与停靠确认点必须落行」） |
| 第五章 5.2 产物依赖图（481–490） | 主件 §流程总览 | [A] | 逐字 |
| 第五章 5.3 书目录结构（492–506） | **指向 `书目录契约.md`** ＋ 该书契约 §2 补值 | [B] | 目录树与交接清单**已在 `书目录契约` §1／§3 在册**（060 已迁），本件只留一行指针（单一真源）。**已列差异清单（本批须处置）**：源件 :506 交接清单要求四项带「**定格态**」（`大纲/大纲.md`／`设定/世界观底盘.md`／`设定/人物卡/*.md`／`参考标尺.md`），而 `书目录契约.md`:28–34 §2 状态值列无「定格」 → 本批**补入** `书目录契约` §2 相应状态值（见 F6），不按「以书目录契约为准」静默丢弃 |
| 第五章 5.4 评审体系总表（508–517） | 主件 §评审体系 | [A] | 逐字（六行：完备律／模块轻审／3.5 四查／4.1 三维度／4.2 级联复审／4.3 终验） |
| 第五章 5.5 成本与降级（519–528） | 主件 §成本与降级 | [A] | 逐字（六条） |
| 源件全档「灵感库」（**14 行／15 处**：纪律 9（:35）、3.1／3.3／3.4 装载图谱层（:218／:285／:312）、3.1 拍四（:235／:242）、3.2 拍四（:264）、5.2 依赖图（:489）、5.5 降级表（:526）、5.6（:532）、附录 A-2 标题（:554）等） | 各落点所在件 | [A] | **原名保留**（作者裁定：灵感库继续做）——「灵感库」当**动作义与资产义**用均可，「拍四·灵感注入」不涉库名；`术语表` 本批新增该词条。**三条硬约束**：① 仓内**不复述**库侧契约内容（只写接口形态与指认）；② **不得复制库内容或库侧契约进本仓**（`AGENTS.md` §四 不做清单「shared-assets 多副本同步」条）；③ 库侧契约是**待消费验证的假设**，其字段／交互／协议可能演进——本仓引用只认「存在该契约 ＋ 接口形态」，不认版本细节 |
| 第五章 5.6 灵感库接口（530–532） | 主件 **§七 灵感库接口（可选外部资产）** | [B] | 改写为**可选外部资产接口**：契约件在仓外（独立 git、跨书、不随仓分发），未声明路径或库不存在即降级（拍四跳过＋字段标「库不可用」）；删除《灵感库契约 v1》的**仓内**指向（改为「契约在库侧、仓内只写接口」）。**须写进的四条**：① **只消费不生产**（作者原话，见库侧契约定位行）；② **初期状态**＝演进三步走的第一步（手动库 → 消费验证 → 触发信号立项工具化；触发信号＝条目过百／回写需求成型／多书同时消费）；③ **不依赖三条**——命中则用、未命中即跳过并如实通报、库不可用＝降级而非失败，且**任何判据不得引用它**（不得出现「必须命中 N 条」类断言）；④ **契约可演进**（字段／交互／协议待消费验证；修订由库侧自持，本仓不追） |
| 第五章 5.7 门控总表（534–546） | 主件 §流程总览 | [A] | 逐字（九行门控／停靠／条件停靠）；与 `触发与交互标准` §2 三级停靠（门控／停靠／廉价检查点）建立对应说明 |
| 附录 A 模板规范 v2（550–553） | 附录件 | [A] | 六要素逐字 |
| 附录 A-2 统一情绪词表（554–564） | 附录件 | [A] | 四轴词表＋取值规则逐字；标注「五处共用：基调／标尺②／灵感库爽点标签／评委情绪判断／分卷情绪弧线」 |
| 附录 B 模板全件 B-1…B-10（565–743） | 附录件 | [A] | 十件模板逐字（基本规格／参考档案／参考标尺／大纲六节／世界观底盘／人物卡／处方报告／级联记录／草稿验收单／定稿验收单） |
| 附录 C 追踪档对照（744–747） | — | **废弃** | 追踪档机制已废止（060 裁：走查记录落当批规格、不建台账） |
| S 编号索引（748–787） | — | **废弃** | 同上；其走查发现的结论**已落在源文各处**（各条「落位」列指向的章节），无内容丢失；沿革归 `CHANGELOG` |
| 追踪档甲节（9–67 行，实测 **33 条**：R1.1–R1.5／R2.1–R2.11／R3.1–R3.7／R4.1–R4.4／R5.1–R5.6） | 各步验收清单（F2／F3／F4）＋ 主件 §十 | [B] | 逐条落位（**去编号**，写成「作者验收基准：{内容}」）——可追溯性由本规格本表与存档追踪档承担。**归属修正**：R2.8（分析准确）／R2.9（标尺治匮乏）／R2.10（成本分层）／R5.2（零待定推进）四条在 062 F5 的三条成功判据中**无对应**，改归本批（分别落 2.2／2.3 验收清单与主件 §四 写法纪律 3）。**注**：「产／域」分面是 062／063 规格的叙述，存档追踪档原表无此标记（本批按内容判归属） |

## 三、文件级改动清单

| # | 文件 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `docs/product/大纲域设计.md`（重写） | 新结构（**头部 `## 目录`，条目与 H2 逐字**）：头注（定位／依据）→ §一 域职责与状态（含源件 :21 边界句三域化）→ §二 **定义性约束**（[C] 新写，**只写一件事**：约束集先行——规格先于资料、资料先于草稿）→ §三 流程总览（状态机与断点／产物依赖图／门控总表）→ §四 写法纪律（13 条）→ §五 **域交互细则**（**保留该节名**：`触发与交互标准`:36／:66 指认其为立项域准入律的单一真源；内容＝现骨架四条 ＋ 指向分步件的三步式细则）→ §六 评审体系总表 → §七 **灵感库接口（可选外部资产）**（源件 5.6 改写落此；四条要求见 §二 5.6 行）→ §八 成本与降级 → §九 **跑通信号（It's working if）**（[C] 新写，如「门控停靠时你说得出当前在哪一步、卡在哪个产物」「标尺红灯给的是处置选项而非数字达标」）→ §十 上线验收清单（含**去编号**的作者验收基准）→ §十一 开放项。**范式四要素处置**：9.1＝§二、9.2＝§九、9.3 角色定位＝头注定位行＋§一 首句、9.4 常见问题＝**显式写「先收 0 条」**（无真实来源不注水）。撤下「§二 流程骨架（待设计）」标记与 4 处「待设计」 | §二 映射 ＋ 061 F3 范式 ＋ `文档写作标准` §一.9 | [B]／[C] |
| F2 | `docs/product/大纲域设计-规格与资料步.md`（新建） | 第一章 1.0–1.2 ＋ 第二章 2.1–2.4 逐字迁入；步内结构保持七字段；补该两章的上线验收清单（并入甲节对应 R 条目）；**头部 `## 目录`**（条目与 H2 逐字） | §二 映射 | [A] |
| F3 | `docs/product/大纲域设计-草稿步.md`（新建） | 第三章 3.1–3.5 ＋ 模块序 ＋ 四拍权重总表 ＋ 交互总纲 ＋ 方法论挂载总表（24 行，萃取源改写）＋ 该章验收清单；**头部 `## 目录`**（条目与 H2 逐字） | §二 映射 | [A] |
| F4 | `docs/product/大纲域设计-打磨定稿步.md`（新建） | 第四章 4.1／4.1.5／4.2／4.3／4.4 ＋ 该章验收清单；分身纪律交叉引用 `施工机制` §五；**头部 `## 目录`**（条目与 H2 逐字） | §二 映射 | [A] |
| F5 | `docs/product/大纲域设计-附录.md`（新建） | 附录 A 模板规范 v2 ＋ A-2 统一情绪词表 ＋ B 十件模板（**唯一落点**，域建设时随 `skills/abzu-outline/references/` 分流，见 §七）；**头部 `## 目录`**（条目与 H2 逐字） | §二 映射 | [A] |
| F6 | `docs/product/书目录契约.md`:49 ＋ §2 状态值列 | ① :49「生产端真源」栏撤「契约真源仍待设计」，改指附录件——**链接写法用文件相对形式** `docs/product/大纲域设计-附录.md`（禁写仓库根相对路径，否则从 `docs/product/` 解析必断，060 事故同型）；② §2 状态值列按 §二 5.3 差异清单**补「定格」**（大纲／世界观底盘／人物卡三行；参考标尺行的「实测」与「定格」关系须核后择一或并列） | A7 基线 ＋ 审查 P1-10／P1-11 | [B] |
| F7 | 引用面通链复核 ＋ `README.md`:23 | 全仓引用「大纲域设计」共 **6 处**（实测 `grep -rl`），逐处核其在批后仍成立：`README.md`:23（产品层文件清单——**须补四件新件**，本项含改动）、`书目录契约.md`:49（F6 改）、`验收场景.md`:21、`测试与验收标准.md`:40（域设计验收清单指针）、`触发与交互标准.md`:36／:66（「域交互细则」节——F1 保留该节名故仍成立）。（原列「061 产品层范式要求」不属引用面，删） | 审查 P1-1／P1-4 | [B] |
| F8 | `CHANGELOG.md` | 本批一行（≤400 字符） | 过程产物两项 | [A] |

## 四、验收断言（含预验记录）

### 断言 1｜五件齐备

```bash
ls docs/product/ | grep -c "^大纲域设计"
```

期望 5（主件 ＋ 规格与资料步＋草稿步＋打磨定稿步＋附录）；预验基线 1。

### 断言 2｜主件含定义性约束与跑通信号

```bash
grep -c "定义性约束" docs/product/大纲域设计.md
```

```bash
grep -c "It's working if" docs/product/大纲域设计.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 3｜主件「待设计」清零

```bash
grep -c "待设计" docs/product/大纲域设计.md
```

期望 0；预验基线 **4**。（新开放项以「待萃取」「待裁定」表述，与「待设计」区分。）

### 断言 4｜流程语法就位

```bash
grep -c -E "作家思维|四拍|刻意不装" docs/product/大纲域设计.md
```

期望 ≥3；预验基线 0。

### 断言 5｜废弃面清零（**作用域＝本批实改件**）

```bash
grep -c -E "追踪档|S 编号|六域地图|六域各一|单 skill 六域|文风域|灵感库契约" docs/product/大纲域设计.md
```

```bash
grep -c -E "追踪档|S 编号|六域地图|六域各一|单 skill 六域|文风域|灵感库契约" docs/product/书目录契约.md
```

（另四件新件同命令逐件核。）期望全部 0；预验基线：两件均 0（新件尚不存在，随施工核）。

**两处口径说明**：① **「六域」按词形收窄**（不含单字「六域」）——与 062 断言 1 同口径；062 会向 `架构设计.md` §四 写入「旧仓**六域**时期实测」的沿革标注，故本批**作用域不含 `架构设计.md`**（本批禁改该件），若按全 `docs/product/` 判必假红；② **「灵感库」不入词表、改以「灵感库契约」判**——原名保留（作者裁定继续做），只禁仓内出现**契约路径／内容的引用**；③ 判据读**逐件计数**、不读退出码（`grep -c` 全 0 时退出码为 1）。

### 断言 6｜交互话术逐字保留

```bash
grep -c "书项目建好了" docs/product/大纲域设计-规格与资料步.md
```

期望 ≥1；预验基线 0（该 token 现存于存档源件，共 1 处）。

### 断言 7｜书目录契约指针改指

```bash
grep -c "契约真源仍待设计" docs/product/书目录契约.md
```

期望 0；预验基线 **1**。另核 `grep -c "大纲域设计-附录" docs/product/书目录契约.md` ≥1。

### 断言 8｜十件模板齐备

计数口径：以**行首模式**`**B-{N}`（N＝1…10）逐条计数（可用 `grep -c` 或脚本；**本机 `grep` 被 ugrep 接管，`^\*\*` 类转义语义与 GNU grep 不一致——核验方以 Python 正则复核为准**）。期望 10 行且 B-1…B-10 各一次、无重复；预验基线 0（件不存在）。

### 断言 9｜链接与守卫

```bash
grep -rhoE '\]\([^()]*\)' $(git ls-files '*.md') | grep -vE '\]\((http|#|mailto)' | wc -l
```

上式为**已跟踪件**的链接条数下限命令（**注意：本机 `grep` 被 ugrep 接管，`$(git ls-files)` 输出的中文名带转义引号，该命令在本环境不可直接复现——以 Python 扫描器实测为准**）；**完整口径＝已跟踪 ＋ 未跟踪**（未跟踪部分由核验方脚本补扫——只扫已跟踪会假绿，060 批实证）。期望断链 0 且条数 ≥117；预验基线 117 条／0 断链。另 `bash scripts/check.sh` 退出 0；预验退出 0。口径先例见 `docs/specs/archive/` 内 060／061／062 三批规格的链接断言记录。

### 断言 10｜过程产物两项

`CHANGELOG.md` 的 `[Unreleased]` 节含本批一行（≤400 字符）；规格验收通过后移入 `docs/specs/archive/`（移动不留副本）。

**未预验项**：无。

## 五、规格自审（三条）

1. **锚点现场核过**：§一 源件体量、节段行号（逐段实测）、我方两件现状、甲节分面、五处冲突、A1–A9 基线均为现场实跑；「源件 1.0⑥ 话术 1 处」为 `git show` 计数实测。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 2／4／6／8 为存在性侧，其 token（「定义性约束」「It's working if」「作家思维／四拍／刻意不装」「书项目建好了」「`**B-`」）在改动前 0 命中（「书项目建好了」仅存于存档源件）；断言 3／5／7 为零命中侧，与替换文本无交集（新开放项已改用「待萃取／待裁定」表述，不与断言 3 冲突；断言 5 的 token 表已按审查 P0 按词形收窄并限定作用域，理由见该条口径说明）。
3. **禁止事项已写明**：见 §六。

## 六、禁止事项

- 禁止触碰：`.git/`、`scripts/`、`LICENSE`、`.zcode/`、`docs/standards/` 全部（本批为**内容批**，不动规范条目）、`docs/specs/施工机制.md`、`docs/specs/archive/`、本批规格自身；`docs/product/` 内**只改** `大纲域设计.md`（重写）、`书目录契约.md`（:49 一行指针 ＋ §2 状态值补「定格」）与 `README.md`:23（产品层清单加四件），其余**六件**零改动（现 8 件 － 本批实改 2 件）。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；**不得改写设计语义**（作者裁定：改写迁入，不是重写）——只允许 §二 列明的口径替换与形式调整；交互话术、判据、阈值、四选处置、装载图谱一律逐字。
- 禁止复活已废机制：追踪档／S 编号索引／附录 C；走查结论一律落当批规格。
- 禁止越权：**不改 `skills/`（尚未创建）——本批是写文档、不是建技能**；不改版本号与 tag；不新增目录（新件落 `docs/product/` 平铺）；不把《灵感库契约 v1》的仓内指向写回（可选外部资产接口写法落 F1 §七，见 §七 开放项 1）；**不得把 `Sxx`／`Rxx` 编号带入产品层**（去编号留内容）；**不得把库内容或库侧契约复制进本仓**（红线 3 外部参考只读 ＋ 单一真源）。

## 七、开放项

1. **灵感库：定位已定，工具化立项待触发**——作者裁定继续做；当前为**初期状态**（手动库、契约 v1 ＋ 15 条目、用过 0 次），本批只落「接口 ＋ 降级 ＋ 不依赖三条」。**工具化立项的触发信号由库侧契约定义**（条目过百／回写需求成型／多书同时消费），三条任一成立时才评估做成工具或技能。
2. **契约本身的演进归库侧**：字段／交互／检索协议属**待消费验证的假设**；域建设期若发现接口或字段缺口，**记入当批规格的开放项**，由作者汇总回库侧契约修订（库侧自持 git、自持版本号）——本仓不做副本、不做镜像。
3. **跨仓指针待同步（跨仓，不由本仓改）**：库侧契约第 4 行的「消费方」现写「Abzu 大纲阶段设计文档 5.6 接口」，本批迁移后该名不再存在 → 建议改为泛化措辞（不带文件名），由作者在库侧仓库自行提交（红线 1／3：外部资产只读）。
4. **方法论萃取批**：挂载总表 6 项「最高」优先级方法论（情感线／冲突／节奏／非修炼类体系／人物设计／大纲骨架）的萃取批**后置**（源件裁定），不阻塞本批；萃取批启动时按源件「萃取四层设计」立规格。
5. **附录件的终极归宿**：附录模板（十件）与情绪词表在 `skills/abzu-outline/references/` 落地时是否随域资产分流（部分留产品层、部分进 skills），待首个域建设批裁定。
6. **甲节「域」面条目的落位**：本批按步分面并入各分步件验收清单；若作者希望保留「R 编号」原编号体系，须在 063 施工前追加说明（当前按「作者裁定 R 编号」标注保留）。
7. **S 编号索引与版本史的沿革**：S 索引结论已落源文各处（无内容丢失），14 行版本变更史（7–20 行）亦以 `main` 存档件原文留存；本批只在 `CHANGELOG` 记一行，不在产品层另立沿革节（治理文档不写批次叙事）。如需逐版裁定可查存档分支。
8. **本批与 062 的顺序**：062（架构设计＋产品定义）与本批同时处于在制状态，两批改动面无交叠（062 不动 `大纲域设计`／`书目录契约`，本批不动 `架构设计`／`产品定义`）；施工**须串行**（062 先、本批后），避免同工作树并行改动。
9. **跨层计数耦合（机制问题）**：底座 `docs/standards/术语标准.md` 陈述产品层词条数（本次即因术语表新增 1 条而出现「38 → 39」的过期数字，已规划方微修）。此类「底座复述产品层事实」宜在下个规则批裁定处理方向（底座只写指针 ＋ 节名，不写计数），并全仓排查同类（如 `测试与验收标准` 陈述产品层清单条数等）。

## 八、审查记录

成稿审查分身（fresh、只读）首轮：**findings 共 18：P0:1 P1:11 P2:6**。规划方逐条复核取证后**全部采纳（18／18）**，已全数进本版：

- **P0**｜断言 5（全 `docs/product/` 内「六域」等清零）**与 062 批互斥**：062 F1 会向 `架构设计.md` §四 写入「旧仓**六域**时期实测……三域现态未复测」的沿革标注，本批又禁改该件 → 必假红。已改：**「六域」按词形收窄**（对齐 062 断言 1）＋ **作用域限定为本批实改件**（`大纲域设计.md`／`书目录契约.md`），并在该条写明理由。
- **P1-1**｜F1 新结构**删掉了现骨架 §四「域交互细则」**，而它是跨件被指认的单点：`触发与交互标准.md`:36／:66 明文指认该节为立项域准入律的单一真源（060 F3 裁定），且该节第 4 条（过程性动作默认执行）**在源设计文档中零命中**（系 060 迁入内容）→ 删除即丢内容并断链。已改：**F1 保留该节名**，内容＝现骨架四条 ＋ 指向分步件三步式细则。
- **P1-2**｜5.6 落点三处互指不一（映射说「主件 §外部资产接口」、F1 结构无该节、§一④／§六 又写「F5」＝附录件）→ F1 结构显式增列 **§七 外部资产接口**，§一④／§六 的「F5」改 **F1**。
- **P1-3**｜源件 :21 边界句（含「文风归文风域」「拆书/扫榜搁置不参与」「创作输入＝…＋灵感库」）被头注替换整句吞掉且无落点 → 头注行补写明「**改写后并入主件 §一 域职责与状态**」及删改内容。
- **P1-4**｜F7 引用面清单漏两处且混入非引用件 → 改为实测的 **6 处**（`README.md`:23 须补四件、`书目录契约`:49、`验收场景`:21、`测试与验收标准`:40、`触发与交互标准`:36／:66），删去「061 产品层范式要求」（非引用面）；`README.md` 随之升为 F7 的改动项。
- **P1-5**｜F1–F5 五件均未安排 `## 目录`（按源区间估算至少 4 件超 100 行）→ 五件各补「头部 `## 目录`（条目与 H2 逐字）」。
- **P1-6**｜两处计数错：「20 行版本变更史」实为 **14 行**（源件 7–20）、「方法论挂载总表 24 行」实为 **26 行**（源件 v0.8 变更行自述「终态 26 行」交叉印证）→ 均改。
- **P1-7**｜甲节「产／域」分面无落盘依据（存档表列仅「#｜要求｜落位｜状态」），条数应为 **33**（非「20 余」），且 R2.8／R2.9／R2.10／R5.2 在 062 F5 三条判据中无对应 → 改：33 条逐条落位（去编号）、四条归属改判归本批、并注明「分面系规格叙述」。
- **P1-8**｜「灵感库」实为 **14 行／15 处**（纪律 9、三步装载图谱层、各步拍四、5.2／5.5／5.6、附录 A-2 标题），规格只安排 5.6 一处 → 新增「**术语统改：灵感库 → 外部资产库**」映射行，并把「灵感库」纳入断言 5 词表。
- **P1-9**｜`Sxx`／`Rxx` 编号实测 **126 处／89 行／39 个编号**，F1–F4 按 [A] 会随文带入产品层，与 062 §二「台账编号族废弃不入库」**两批口径相反** → 新增「**去编号留内容**」映射行（括注删除；`S26 三型` 改「金手指三型」；内容与结论全部保留），两批口径对齐。
- **P1-10**｜5.3 折为指针时，源件交接清单的「**定格态**」四处在 `书目录契约` §2 状态值列无对应（会被「以书目录契约为准」静默丢弃）→ 列出差异清单并定处置：**补入 `书目录契约` §2 状态值**（F6 扩为两项）。
- **P1-11**｜F6 的目标写成仓库根相对路径（从 `docs/product/` 解析必断，060 事故同型）→ 改为文件相对写法 `docs/product/大纲域设计-附录.md`。
- **P2 六条**｜F1 的「定义性约束」须**只写一件事**（分模块流水移 §三）；补 §一.9.3／9.4 的处置（角色定位＝头注＋§一 首句；常见问题＝显式「先收 0 条」）；纪律 10 的「采风员」行改后成自映射 → 整行删；断言 5 判据须读逐件计数不读退出码；断言 9 补可执行命令；§六「其余五件」→ **六件**；断言 9 的出处「见 `施工机制` 062 批先例」不实 → 改指 060／061／062 规格记录。

审查位另核已清（多项）：**源件 787 行的块级覆盖完整**（唯一未配区间的非空行为 :465 章标题）；A1–A9 基线逐条复现（含 117 条／0 断链自写扫描器实测）；**附录 C／S 索引「无内容丢失」抽样 5 条 ＋ 补验 2 条全部在位**（S3→1.1③、S17→2.2④、S20→3.1④+5.3、S23→挂载表节后、S26→纪律 11、S31→各步②、S11／S12 结论在位）；头注版本史去向交代成立；五处冲突逐条落实（文风域仅 :21 一处、追踪档 7 处、拆书/扫榜 :21／:343）；**F1 的 [C] 新写两节判为「补形不越界」**（依据 `文档写作标准` §一.9 ＋ 061 F3「本批只立规范、内容待业务设计阶段」，且两条实质在源设计中有据）；「定义性约束／跑通信号」与 061 用名为**同概念异措辞**（后者是 §9.1／9.2 条文内的解释句）；与 062 的**文件面无交叠**（冲突仅在断言／词形层，即 P0）；内容批定级与过程产物两项自洽。

- **审查后按作者澄清修订（规格修订记录）**：① 灵感库定位经作者说明后落定（继续做、**初期状态**、跨项目外置、**只消费不生产**、契约在库侧且**待消费验证**）→ 规格取消「灵感库 → 外部资产库」的术语统改（原名保留）、该节四条要求写全（只消费不生产／初期状态三步走／不依赖三条／契约可演进）、§六 增「不得复制库内容或库侧契约进本仓」、断言 5 词形由「灵感库」改「**灵感库契约**」（只禁仓内契约引用）；② §七 开放项重写为 8 条（新增「工具化立项触发信号」「契约演进归库侧 ＋ 查缺补漏通道」「跨仓指针待同步」）；③ 小修两处：开放项编号补连续（1–8）、版本变更史行数 20 → **14**（7–20 行，与 §二 映射行一致）。以上修订均在成稿审查之后，规划方逐条核过。

**核验结论（规划方，施工后三步 ＋ 单维核对）**：

- **第 1 步 改动面核对：通过**——`git status` 显示 4 改（`CHANGELOG.md`／`README.md`／`书目录契约.md`／`大纲域设计.md`）＋ 4 新建（三个分步件与附录），与 F1–F8 声明一致；禁改面（`docs/standards/`、`施工机制.md`、`产品定义`／`架构设计`／`术语表`／`验收场景`／其余域设计件、`scripts/`）**零 diff**。**过程事故核查**：施工方自报「首次落盘误替换 062 的 `CHANGELOG` 行、已当即复原」——规划方实核 Changed 段 **6 条完整**、062 行未被污染 ✓。
- **第 2 步 断言独立重跑：9/10 通过，1 处为工具假阳性**（见下）。五件行数：主件 204／规格与资料步 141／草稿步 224／打磨定稿步 109／附录 206（合计 884 行）。链接 **164 条／0 断链**（较基线 117 增 47，为五件互引与 README 新链）；`check.sh` 退出 0。
- **工具假阳性（本批新增环境事实）**：断言 8 原以 `grep -c "^\*\*B-"` 计数，**本机 `grep` 被 ugrep 接管**，同一模式施工方报 10、规划方报 11；以 Python 正则复核为**恰 10 行、B-1…B-10 无重复** ✓ 内容无误。同因，断言 9 给出的 `$(git ls-files)` 命令在本环境不可直接复现（中文名转义）。→ 本批两处断言已改为**可移植写法**（行首模式 ＋ Python 扫描器口径），并把「ugrep 接管 grep」记入 §七 开放项（宜入 `运行环境标准` §2 Windows 约定）。
- **第 3 步 内容抽查：通过**。主件 11 节 ＋ 头部目录（条目与 H2 逐字）；**§五「域交互细则」节名保留**（`触发与交互标准`:36／:66 的指认仍成立）；§七 灵感库接口四条与规格逐条对应（含「任何判据不得引用它」「本仓不做副本、不做镜像」）；§二 定义性约束（一件事：约束集先行）与 §九 跑通信号为 [C] 新写且与 `文档写作标准` §一.9 四要素对齐（9.4「常见问题」显式「先收 0 条」）。抽查 `书目录契约` §2 状态值补「草稿／定格」、:49 指针改指（文件相对写法）。全档无 `Sxx` 括注残留（`S26 三型` → 「金手指三型」），「拆书」原词仅存于挂载总表 1 处「旧仓拆书域方法论（已退役，仅作萃取参考）」（规格 F3 明令文本）。
- **单维核对（内容批分级）**：① 引用闭合——164 条／0 断链；② 数字一致——五件行数、十件模板（B-1…B-10 各一次）、33 条作者验收基准分落四处（主件 6 ＋ 规格与资料步 16 ＋ 草稿步 7 ＋ 打磨定稿步 4）均自洽；③ 既有条文冲突——「灵感库」原词保留且 `术语表` 补词条（末轮整改）、灵感库接口与 `安全与合规标准` §一.3（外部参考只读／引用分级）不冲突、`书目录契约` 与域设计组互为指针而非副本。
- **施工方 10 条 P1 裁定**：全部采纳（纪律 10「采风员」行整行删；三件统一「选项列表」；挂载总表按 F3 明令保留「旧仓拆书域方法论」；日期类批次叙事去除留「作者裁定」；`R2.10` 落 2.1 清单、`R5.2` 落主件 §十；「试读段」在迁移源内统一「公开样本」；§2 状态值补「草稿／定格」并并列；§五 反例句悬空指针去除；9.4 常见问题落 §十一 末；步骤号保留 ＋ 跨件指针补法与旧节号引用改链接）。
- **规划方末轮裁定（四项已派整改）**：① `术语表` 补「灵感库」词条（**规划方交底漏列之误**——规格 §二 5.6 明写要补，我却在禁面误列为零改动）；② `书目录契约` §2 参考标尺行「锚定（试读段）」→「锚定（公开样本）」（与 `安全与合规标准` §一.3 统一）；③ 同件 §4 第 2–5 行「同上」改指域设计组；④ 主件 3 处「弹窗」→「选项列表」（授权破 [A] 逐字，依据 `触发与交互标准` §一.3.6 方向 (a)）。第 5 项（挂载总表来源标记）裁定**保留、无需改动**。

- **末轮整改复核（4 改 1 不改）：通过**——① `术语表` 补「灵感库」词条（落 §一 工作流类之末，词条数 38→**39**，头注与依据行计数同步）；② `书目录契约` §2 参考标尺行「锚定（试读段）」→「**锚定（公开样本）**」（`试读段` 计数 0）；③ 同件 §4 的 **4 处**「同上」全部去隐式链式、改显式指针（`| 同上 |` 计数 0）；④ 主件 3 处「弹窗」→「选项列表」（`弹窗` 计数 0，全组五件＋术语表亦 0）。第 5 项（挂载总表来源标记）按裁定保留、无需改动。复核：违禁面逐件合计 0、链接 **168 条／0 断链**、`check.sh` 退出 0。
- **规划方收尾微修两处（已声明）**：① 连带核查发现底座 `docs/standards/术语标准.md` 两处陈述「38 词条」（产品层计数），词条数变 39 后成过期数字 → 同步改「39 词条」（**只同步事实数字、未动规范条目**，故未破「内容批不动规范」的边界精神，但仍属跨批微修，记此备案）；② 062 批遗留的断言编号缺号与措辞自斥已在该批归档前修正（见 062 §八）。
- **暴露的机制问题（已在 §七 开放项 9 记录）**：底座文件陈述产品层计数（词条数）＝**跨层事实重复**——本次即因产品层增一条导致底座过期。宜在下个规则批裁定「底座只写指针、不写产品层计数」。

- 末轮整改后的提交确认：待作者确认。
