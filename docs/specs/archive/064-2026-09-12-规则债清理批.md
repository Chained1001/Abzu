# 064-2026-09-12-规则债清理批

> 定位：清四条机制债（①命令行工具语义、②跨层计数、③加粗密度口径、④待收口关闭）＋ **新立一条机制**（⑤开放项核销）＋ 首次核销五份归档规格的 35 条开放项（核销表见 §三）。本批**不动 `skills/`**（尚未创建）、不动产品层内容。
> 自由度口径：[A]＝逐字执行（短句改写按条目给定文本）｜[B]＝按条目方向改写｜[C]＝新写。
> 必读输入：`AGENTS.md`、`docs/standards/` 全部 11 份、`docs/specs/施工机制.md`、`docs/specs/archive/` 五份规格（059–063）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**债① 命令行工具语义（环境事实）**：`grep --version` → `ugrep 7.8.4 WIN64`；`type -a grep` → `grep is a function grep ()`——本机 `grep` 是 **ugrep 的函数封装**。实证（依 063 批规格 §八 原始记录）：同一模式在本环境**施工方报 10、规划方报 11**；**该差异在本轮不可复现**（ugrep 与 Python 对 `^\*\*B-` 现均得 10）——故本条按**预防性条文**立（不依赖具体复现值）。063 的两条断言已改可移植写法。**现行 `运行环境标准` §2「Windows（Git Bash）约定」无此条**。

**债② 底座复述产品层计数**：`docs/standards/术语标准.md`:3 写「（39 词条，词条与叫法以该表为准）」、:17 写「（39 词条，原文搬家）」——词条数属产品层事实（`docs/product/术语表.md` 为真源），底座复述即跨层重复；本批 063 微修时刚体验过一次（38→39 的过期数字）。

**债③ 加粗密度口径与适用范围**：规则原文「单文件 `**` 数 ≤ 行数 ÷ 3」（`文档写作标准` §一.3.8）。按**标记出现次数**实测（命令：`python -c "print(open(F).read().count(chr(42)*2))"`，**计 glob 等非加粗用途**）：`AGENTS.md` **57 处**／65 行（阈值 21，超；其中 1 处为 `**/*.md` glob）、`架构设计.md` **26 处**／62 行（阈值 20，超）、`产品定义.md` 14 处／58 行（阈值 19，不超）。（归档 061／062 所记「59 处／66 行」为该件当时实测，现为 65 行 57 处。）**规则未写计数口径，也未写适用范围**——入口与清单型文档（红线、不做清单、域清单表）天然高密度加粗。

**债④ 待收口未关**：`docs/standards/触发与交互标准.md`:32「6. **待收口**：本条与旧仓『禁用 UI 词汇（弹窗／界面）』条字面互斥……方向二选一：(a) 改标注词汇为（选项列表）；(b) 给禁用句加限定」——063 批已按**方向 (a)** 落地——**作用域**：063 迁移件（五件 ＋ 新增「灵感库」词条的 `术语表`）内 `弹窗` 计数 **0**（F4 撤条后，该词仅存于本条**禁词列举**中（`触发与交互标准.md`:32），标准件内无其他用法）。该待收口条可关。

**债⑤ 归档开放项无核销机制**：`docs/specs/archive/` 现 **6 件**（059／060／060 审计报告附件／061／062／063；实测 `ls | wc -l` ＝ 6），其中 **5 份规格**带开放项节（附件件无），合计 **35 条**；**无任何机制在批次结束后核对其状态**——已出现 **10 条**「条件早已满足但条目仍躺在归档里」（按份计，见 §三 核销表）。本仓不做平行台账（`AGENTS.md` §四），故核销须落在**新批规格自身**。

**预验基线**：

| 编号 | 判据 | 基线 |
| --- | --- | --- |
| A1 | `docs/standards/运行环境标准.md` 内「ugrep」 | 0 |
| A2 | `docs/standards/术语标准.md` 内「词条」计数式（`（39 词条` 等） | 2 |
| A3 | `docs/standards/文档写作标准.md` 内「标记数」／「叙事型」 | 0／0 |
| A4 | `docs/standards/触发与交互标准.md` 内「待收口」 | 1 |
| A5 | `docs/specs/施工机制.md` 内「核销」 | 0 |
| A6 | `docs/specs/archive/` 件数（本批禁止改动归档件，仅计数） | **6** |
| A7 | 相对链接条数／断链、`check.sh` | 168 条／0 断链、退出 0 |

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `运行环境标准.md` **§一.2「Windows（Git Bash）约定」末**（续为第 6 条；该件 §二 是正例与反例，不落那里） | 「命令行工具语义不可假定为 GNU 版：本机 `grep` 可为 ugrep 的函数封装（实测 `ugrep 7.8.4`），`\*` 等**转义语义与 GNU grep 不一致**（同一模式两种计数）。**守卫与断言实现不得依赖转义差异**——固定串用 `-F`、模式匹配优先用脚本语言（Python／Node）；核验复核以脚本语言实测为准。〔出处：063 批规格 §八 工具假阳性记录〕」 | 债① ＋ 063 批实证 | [A] |
| F2 | `术语标准.md`:3、:17 | 去掉产品层计数：「（39 词条，词条与叫法以该表为准）」→「（词条与叫法以该表为准）」；:17「（39 词条，原文搬家）」→「（原文搬家）」。**依据**：底座不复述产品层事实（单一真源 ＋ 060 双载体教训） | 债② | [A] |
| F3 | `文档写作标准.md` §一.3.8（**逐字追加四句**）＋ 两处同步面 | 在 §一.3.8 该条末**逐字**追加：「**计数口径**：本文的「`**` 数」指 `**` 出现次数（含 glob 等非加粗用途）。**适用范围**：本兜底阈值适用于**叙事型文档**；**入口与清单型文档**（`AGENTS.md` 红线／不做清单、产品层域清单表等）以定性规则为准，不计兜底阈值。**目录要求**：§一.1.3 的节目录适用于**常驻文档**；**规格与归档件不适用**（在制与历史工作件，读者从头读）。〔出处：061 开放项 8／062 开放项 0 存量债〕」（实测：`AGENTS.md` 57 处／65 行、`架构设计` 26 处／62 行即入口与清单型之列）。**同步面两处**：`文档写作标准.md`:117 缺口表行与 `测试与验收标准.md`:34 `check.sh` [3] 段须标同一适用范围，否则将来检查器按旧口径实现、两件必假红 | 债③ ＋ 061-8／062-0 ＋ 规格目录适用面 | [A] |
| F4 | `触发与交互标准.md`:32 | 撤下「6. **待收口**」条（方向 (a) 已落地）：改为一句话现行口径——「交互标注一律用**（选项列表）**形态；宿主 UI 词（弹窗／界面）不入条文」 | 债④ ＋ 063 全部件已统一 | [A] |
| F5 | `施工机制.md` §三 过程产物两项之后新增一段 | **开放项核销机制**：「新批规格须核对**相关归档规格的开放项**，在本批规格内逐条标「**已履行／仍待／作废**」（含理由）；核销落本批规格，**不另建台账、不建索引件**（`AGENTS.md` §四 平行台账不做）。触发条件：**本批改动面与某归档规格相关**（不设时间触发——债跟着主题走，不跟着时间走，与 `规格写作标准` §六 同句）。」 | 债⑤ | [C] |
| F6 | `施工机制.md` §七 提交与登记 末 | 补一句归档件纪律：「`docs/specs/archive/` 只读（仅允许作废头注；**作废批移入时即带 `-已作废` 后缀，归档后不再重命名**）；跨批检索以各批规格的核销表为唯一索引落点」 | 债⑤ ＋ 062 开放项 1 | [B] |
| F7 | `CHANGELOG.md` | 本批一行（≤400 字符） | 过程产物两项 | [A] |

## 三、首次核销表（五份归档规格 35 条逐条）

| 来源 | 已履行 | 作废 | **本批处置** | 仍待 | 待作者 |
| --- | --- | --- | --- | --- |
| 059（7 条） | 1（架构设计选型理由 → 062 F1）、3（产品定义目标用户与成功判据 → 062 F5） | 4（核验前提缺口：零提交已结束，条件消失） | — | 2（可移植模板形态）、7（卷纲／正文域设计仍占位；大纲域已由 063 成文） | 5／6（实际标识保留、出处专名照录——**已裁定、无动作**） |
| 060（6 条） | 1（F1 方向已定并施工）、2（T1–T3 迁移 → 062 F1）、4（审计报告已落附件） | — | — | 3（可移植模板形态，与 059-2 同题）、5（扫描面含未跟踪 → 待 `check.sh` [3] 段） | 6（非管辖符号：**已裁定不处置**） |
| 061（9 条） | 9（研究副本已删） | — | **8（加粗密度 → F3）** | 1（触发句是否 no-op → 待首个域实测）、2（侧文件双处点名）、3（桶／晋升不变量）、4（会话卫生五分法）、5（版本可跑校验）、6（对外产物不写行号）、7（域设计门控写法 → 063 已部分落） | — |
| 062（4 条） | 2（063 批已完成）、3（三评委 → 063 落地） | — | **0（加粗密度 → F3）、1（归档规格检索 → F6）** | — | — |
| 063（9 条） | 2（契约演进归库侧：机制已立）、7（S 索引与版本史沿革已记） | 8（本批与 062 顺序：已串行执行完毕） | **9（跨层计数 → F2）** | 1（灵感库工具化触发信号）、4（方法论萃取批后置）、5（附录归宿 → 待域建设批） | 3（跨仓指针：库侧自提）、6（甲节 R 编号是否保留） |

**汇总（按份计，可逐格复核）**：已履行 **10**｜作废 **2**｜本批处置 **4**｜仍待 **14**｜待作者 **5** ＝ **35** ✓。（「本批处置 4 处／3 个独立主题」——加粗密度一事在 061-8 与 062-0 各记一次；另两个主题＝归档检索 062-1、跨层计数 063-9。）**处置后仍待余额 14**（按份）。同一性注：059-2 与 060-3 为**同题两条**（可移植模板形态），未合并计数，各自列于本表。**本批 4 条处置映射**：061-8／062-0（加粗密度）→ F3；062-1（归档检索）→ F6；063-9（跨层计数）→ F2。`CHANGELOG` 记一句「首次核销完成」。

## 四、验收断言（含预验记录）

### 断言 1｜ugrep 条在位

```bash
grep -c "ugrep" docs/standards/运行环境标准.md
```

期望 ≥1；预验基线 0。

### 断言 2｜底座去产品层计数

```bash
grep -c -E "（39 词条|（38 词条" docs/standards/术语标准.md
```

期望 0；预验基线 2。

### 断言 3｜加粗密度口径与适用范围

```bash
grep -c "标记出现次数" docs/standards/文档写作标准.md
```

```bash
grep -c "清单型文档" docs/standards/文档写作标准.md
```

两命令各期望 ≥1；预验基线 0／0。

### 断言 4｜待收口清零

```bash
grep -c "待收口" docs/standards/触发与交互标准.md
```

期望 0；预验基线 1。

### 断言 5｜核销机制在位

```bash
grep -c "核销" docs/specs/施工机制.md
```

期望 ≥2（机制段 ＋ 归档纪律句）；预验基线 0。

### 断言 6｜归档件零改动

```bash
git status --porcelain docs/specs/archive/
```

期望空输出（本批只读归档件；用 `status --porcelain` 而非 `diff --stat`——后者看不见未跟踪件，060 批已实证该假绿）。预验基线空。

### 断言 7｜链接与守卫

全仓相对链接断链 0 且条数 ≥168（口径＝已跟踪 ＋ 未跟踪，脚本语言实现）；`bash scripts/check.sh` 退出 0。预验基线 168 条／0 断链、退出 0。

### 断言 8｜过程产物两项

`CHANGELOG.md` 的 `[Unreleased]` › `### Changed` 小节含本批一行；字符数 ≤400（含标点与反引号，实测命令：取以「- 」开头且含「064」的行求长度）；规格验收通过后移入 `docs/specs/archive/`（移动不留副本）。

**未预验项**：第 8 条（收尾动作）。

## 五、规格自审（三条）

1. **锚点现场核过**：§一 四条债的行号与实测值（`grep --version`／`type -a grep` 输出、两处计数式、三件加粗标记数、:32 待收口原文、归档 7 件 35 条分类）均为本轮现场实跑；§四 基线同源。
1.1 **计数自纠（申报）**：§一 债⑤ 初稿写「归档 7 件」、且 §一 实列 债①–⑤ 而定位行写「四条债」——前者实跑 `ls docs/specs/archive | wc -l` ＝ **6**，后者统一为「四条机制债 ＋ 新立一条机制（⑤核销）」；教训＝计数一律以命令实测为准（`规格写作标准` §一.3 已立法，本处为执行漏项）。

2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1／3／5 为存在性侧，其 token（「ugrep」「标记出现次数」「清单型文档」「核销」）在改动前 0 命中；断言 2／4 为零命中侧，基线各 2／1，与替换文本无交集（F2 去计数、F4 撤待收口条）。
3. **禁止事项已写明**：见 §六。

## 六、禁止事项

- 禁止触碰：`.git/`、`scripts/`（本批不改守卫）、`LICENSE`、`.zcode/`、**`docs/specs/archive/` 全部归档件**（只读）、本批规格自身、`docs/product/` 全部（本批不动产品层内容）。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；**不得把归档开放项「搬进」本批规格**（只核销、不复述全文）。
- 禁止越权：不改版本号与 tag；不新增目录；**不建台账／不建索引件**（核销落本批规格，见 F5）。
- 禁止改断言、改数字、删检查让检查变绿。

## 七、开放项

1. 「可移植模板形态」（059-2／060-3 同题）：待底座首次移植到另一项目时按需立法。
2. 甲节 33 条作者验收基准是否恢复原「R 编号」体系：待作者裁定（063-6）。
3. 灵感库契约「消费方」行的跨仓指针：待作者在库侧仓库自行提交（063-3）。
4. **跨层计数同类排查结论（已做，备案）**：`docs/standards/` 内陈述产品层计数的仅 `术语标准.md`:3／:17 两处（本批 F2 处置）；其余（`测试与验收标准.md`:40、`触发与交互标准.md`:10 等）均为指针非计数；`运行环境标准.md`:42「12 条」与 `命名标准.md`:26「11 份」属**同层自计数**不在辖内。将来新增跨层计数即回本项。
5. **F4 撤条后的历史指针**：`docs/specs/archive/063-*.md` 内「依据 `触发与交互标准` §一.3.6 方向 (a)」在 F4 撤条后成悬空指针——归档件只读（本批禁止面），属历史记录，**不改**；记此以免将来被当缺陷重报。
6. **加粗密度超阈值件分类留痕（本批实测，备案）**：按 F3 判据式适用范围（兜底只针对叙事型文档；以表格与编号条目为主要结构者不计兜底；规格与归档件不适用目录要求），现存超阈值件全部落入豁免类——入口与清单型：`AGENTS.md` 57／65、`docs/product/架构设计.md` 26／62；表格与编号条目为主（`docs/product/` 四件）：`大纲域设计.md` 206／204、`大纲域设计-草稿步.md` 212／224、`大纲域设计-规格与资料步.md` 88／141、`大纲域设计-打磨定稿步.md` 64／109；规格与归档件（目录要求不适用）：`docs/specs/064-*` 180／147、归档 `063` 415／220、`061` 379／254、`062` 254／248、`059` 162／267、`060` 134／318。计数口径＝`**` 标记出现次数／行数（Python `str.count` 实测）；超阈件中无叙事型件——即按新判据零假红。

## 八、审查记录

- 成稿审查分身（fresh、只读）首轮：**findings 共 19：P0:1 P1:6 P2:12**。规划方逐条复核取证后**全部采纳（19／19）**，已全数进本版：P0＝核销表汇总算不平（表列 10／2／19／4 而汇总写 11／2／16／2／2；且「本批处置」4 条同时计进「仍待」自相矛盾）→ 核销表增「本批处置」列并重算（按份 10／2／5／14／4＝35 ✓）；P1 六条＝债③ 实测值口径（56→**57**，含 glob；归档 59 为当时值）、债① 的「得 11」不可复现→改为引用 063 原始记录并标预防性条文、F1 落点歧义（§2→**§一.2**）、F3 自由度 [B]→**[A]** 并给逐字文本、F3 漏两处同步面（缺口表行／[3] 段）、F5 的时间触发与「债不跟时间走」抵触→删；P2 十二条＝表列归属、自纠未同步、债④ 作用域、撤销条致归档悬空指针、债② 引文失真（:17 为「原文搬家」）、063-9 同类排查结论、F6 作废重命名、断言 6 判据面（`status --porcelain`）、断言 8 计数命令与落点、§八 措辞（分身只读不落笔）、F1 補〔出处〕、计数表述统一、以及**规格是否适用目录要求**（结论：不适用，已并入 F3 第四句）。

- 施工期发现：`git ls-files` 默认 `core.quotepath` 引号化非 ASCII 路径，按后缀过滤失效，须用 `-z`。
- 核销机制落点自证：核销表落点恒在「当批规格」内，属 `AGENTS.md` §四 该条逃生口所列「当批规格」范围，故不触「触碰本节即停」流程。
**产物审查分身（fresh、只读；双维）**：**findings 共 15：P0:1 P1:6 P2:8**。规划方逐条复核后**全部采纳（15／15）**，已全数进本版：

- **P0**｜核销表汇总仍不平（**同一处第二次**）：表内「本批处置」仅 4 格而汇总写 5；060-6 未落任何档（致行合计 34）；「处置 5 处对应 4 条」与「按条目 15」均无来源 → 060-6 独立列「待作者」档，汇总改「已履行 10｜作废 2｜本批处置 4｜仍待 14｜待作者 5 ＝ 35 ✓」，删无来源项，并改为「处置 4 处／3 个独立主题」。
- **P1 六条**｜① F4 的替换文本**过宽致互斥**（「一律用（选项列表）」与同件 §3 第 5 条「（选项列表）或（对话式）」及产品层在用模态冲突）→ 改为「不用宿主 UI 词（弹窗／界面）」＋按既有口径分模态，并并入 §三 `§3.5` 锚点行；② F3 的目录豁免**漏三处同步面**（`文档写作标准` 锚点行、`测试与验收标准` [3] 段与锚点行）→ 三处补「（规格与归档件不适用）」；③ `测试与验收标准` **102 行无目录**，F3 第四句生效即为现行违例 → 补 `## 目录`（并按新口径全仓筛「常驻件 ＞100 行无目录」＝仅此 1 件）；④ F3 的「适用范围」缺判据 → 改判据式（叙事型＝以连续段落为主；表格与编号条目为主者不计兜底），并把实测超阈件分类留痕入 §七 开放项 6；⑤ F1 第二处出处不可核（064 §八 无 quotepath 记录）→ 改引 060 批 A14，并把施工期 quotepath 实证转述入 §八；⑥ F5 机制只定义三档而表用五档 → 机制条补「另可标『本批处置／待作者』」。
- **P2 八条**｜两处「待收口注」路由句补「（现无存量）」；债④ 改述（该词仅存于禁词列举）；F1 的「同类实例」拆为新起第 7 条；两条新立/改条文各补立法质量实例（`运行环境标准` §二 与 `文档写作标准` §二 各一条反例）；核销表落点自证（恒在「当批规格」内，不触 `AGENTS.md` §四「触碰本节即停」）；`CHANGELOG` 断言口径改「8 条通过（含规格归档）」；`施工机制` 重复句归一；F1「（同一模式两种计数）」改「（个案记录，未稳定复现）」。
- **整改后复核（规划方自跑）：7/7 断言项通过**（ugrep 2 行、去计数 0、口径 token 2／3、待收口 0、核销 2 行、越界面空、链接 168／0 ＋ `check.sh` 退出 0）；抽查落点：`触发与交互` §3 第 6 条新文、`测试与验收标准` 目录就位、核销表逐格合计 35 ✓。
- **规划方收尾微修两处（已声明）**：① `触发与交互标准` §3 第 6 条的「按 `:31` 既有口径」**行号指针改节锚点**（「本件 §3 第 5 条」）——与 061-6「对外/跨会话产物不写行号」同精神；② `CHANGELOG` 本批条目的「7 条断言过」改「**8 条断言通过（含规格归档）**」（归档就在本次提交内完成，届时为真）。保留项：`文档写作标准` §二 新反例的 ✅ 半句保留（与该件「✅｜✗」对照体例一致）。

- 提交确认：待作者确认。
