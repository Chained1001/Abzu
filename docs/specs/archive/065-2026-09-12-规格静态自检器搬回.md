# 065-2026-09-12-规格静态自检器搬回

> 定位：把旧仓的规格自检工具（`main:scripts/check_spec_assertions.py`，783 行）移植重建为 `scripts/check_spec.py`，接入 `check.sh` [4] 段——把「断言与自家文本互斥／不可达」与「计数错」这两类**每批必犯**的规格缺陷变成**送审前可见**。**非阻断**（候选类发现只呈报，`AGENTS.md` §五.2 候选永不拦截）。
> 自由度口径：[A]＝照搬源件（逻辑与判据不变，仅按适配项改写）｜[B]＝按条目方向改写｜[C]＝新写。
> 必读输入：`main:scripts/check_spec_assertions.py`（移植源，只读）、`AGENTS.md`、`docs/specs/施工机制.md`（§三 核验与过程产物／§四 机器闸）、`docs/standards/测试与验收标准.md`、`docs/standards/规格写作标准.md`、`docs/standards/运行环境标准.md`、`scripts/check.sh`
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**源件**（`git show main:scripts/check_spec_assertions.py`，**783 行**）功能四块：① **动态阶段**——从规格「验收标准」节提取命令断言（行内反引号 ＋ 围栏整行），白名单只读执行，三态呈报（可审计／不可审计／未跑成）；② **静态阶段**`--static`——检查 A 自噬预警／B 计数重算／C `[A]` 项「替换为」逐字核对／D 规格写作四类机械检查；③ **核验比对**`--verify`——改动面 ↔ `git status`、过程产物三件、施工记录表结构；④ **模式分派** `__main__`（真退出决定点）。顶层符号实测 **49 项**（28 个 `def` ＋ 21 个常量）。**事故出身**：020–025 六发断言自噬（断言吞自家 `[A]` 文本／对象错／计数错／恒真假绿）。

**六处形态差异（逐条实测，本批适配面）**：

| # | 差异 | 旧仓 | 重建后（实测） |
| --- | --- | --- | --- |
| ① | 节名（两处） | `^##[^#\n]*验收标准`／`^##[^#\n]*文件级改动清单` | 断言节：`## 三、验收断言（含预验记录）` 或 `## 四、…`——**位次随批次变（三／四均有），判据只认节名，不带位次**；改动清单节：**061 作「首轮落地改动清单」**（现正则不命中），且 **`施工机制.md` §八 模板内嵌这两节标题**（若被扫即假发现源） |
| ② | 检查 C 的配对形态 | bullet「`- 现文（…）：`／`- 替换为（…）：`」 | 我方为**表格**；且**表头命名不统一**——065／062／064 作「改动」、063 作「文件」、060 作「改动（现状 → 目标）」、`施工机制` §八 模板作「改动（现状 → 目标）」且**依据列在末位**。配对**字面**实测（非归档件）：`现文` 4 处／`替换为` 8 处，全在散文与自审句中，**无 bullet 配对** |
| ③ | 计数声称形态 | 声称＝文件尺寸 | 我方断言区写「预验基线 N 行」＝**命令输出行数**非文件尺寸；旧 B 对 **059–064 六件**实跑报 **8 条**（`x 计数不符` 4 ＋ `· 基线漂移` 4；含 065 则 7 件 9 条），按词形排除表（基线／预验／期望／实测／命令）仅抑制 1 条 → **7 条假阳性**，对象口径分别属**表行数／diff 增量／零命中数／节行数／引用行号**（后者如 059:27 的「称『4 行』」实为引用源件第 4 行）；**其中 1 条为真阳性**（060 附件:44「`scripts/check.sh` 称『39 行』实测 40」＝整件尺寸） |
| ④ | 过程产物 | **三件**（`CHANGELOG` 条目、`collab-log` 施工记录行、archive 归档）＋施工记录表结构 | **两项**（规格 ＋ `CHANGELOG` 条目）；`collab-log` 与施工记录表**机制已废**（`施工机制` §三／§七） |
| ⑤ | 退出契约 | 静态阶段「计数不符」置 1 | 我方 `AGENTS.md` §五.2「**候选永不拦截**——机器检出的候选类发现只呈报，不阻断流程、不影响退出码」 |
| ⑥ | 段序 | `check.sh` 六段全建 | 我方 `check.sh` **第 [0]、[1] 段已实现、[2]–[5] 段为 TODO**；**头注是段序唯一真源** |

**其他现状**：**在制规格 0 份**（`docs/specs/` 根仅 `施工机制.md`，且它**不是规格**）→ ① 工具的有效性验收必须用夹具（`.tmp/065/`，红线 2）；② **[4] 段的枚举口径必须显式排除 `施工机制.md`**，否则模板内嵌节标题会被当规格扫；③ 提交时在制规格通常为 0（规格在验收后即归档），[4] 段走「明示跳过」路径属**预期常态**。`python -V` 实测 **Python 3.12.8** 可用；`运行环境标准` §一.4 的三级链为 **`python3 → python → py`**，其「将固化在 `scripts/check.sh` 内（现未实现）」仍为待建，§三 机检锚点行标「待建」。源件把 `py_compile` 列为**越权命令判据**（留缓存产物），本批语法校验改用 `ast.parse`（无副作用）。

**预验基线**：

| 编号 | 判据 | 基线 |
| --- | --- | --- |
| A1 | `scripts/check_spec.py` 是否存在 | 否 |
| A2 | `check.sh` 内 `TODO: [4]`／`^# [4]` 行数 | 1／0 |
| A3 | `check.sh` 内 `python3` 字样 | 0（**口径收紧**：`python`／裸 `py` 作判据会命中 TODO 行内的 `.py` 文件名——实测 2 处） |
| A4 | `测试与验收标准.md` 内 [4] 相关行（§1.1 与 §三 各一，均标「待建」） | 2 行待建 |
| A5 | `测试与验收标准.md` 内 G3（工具更名）行 | 1 |
| A6 | `测试与验收标准.md` 内「已实现」计数 | **6**（本批后应为 8） |
| A7 | `规格写作标准.md` §三 内「待建」行 | **10**（其中与本批工具直接相关 **8 行**：§1 判定表／§4 自审三条／§5.1 断言预验／§5.3 计数实跑／§5.4 自噬预警／§5.5 `[A]` 逐字／§5 写作四类／§6 过程产物比对；另 2 行＝§2 命名、§2 归档，不属本批）；§5.3 行文含「阻断」） |
| A8 | 相对链接条数／断链、`check.sh` 退出 0 | 168 条／0 断链、退出 0 |

## 二、移植映射（函数级；顶层符号 49 项的处置）

| 源件（函数／常量） | 处置 | 适配要点 |
| --- | --- | --- |
| `extract`／`unauditable`／`readonly`／`_outside_quotes`／`ALLOWED`／`MUTATING`／动态三态报告 | [A] 照搬 | 动态阶段与节名解耦，源件逻辑不受我方形态影响 |
| `SECTION` | [B] 改写 | 只认节名不带位次（差异①） |
| `CHANGE_SECTION` | [B] 改写 | 容错「首轮落地改动清单」等变体 → `^##[^#\n]*(?:首轮)?(?:落地)?文件级改动清单.*$`；并**排除 `施工机制.md`**（差异①） |
| `ASSERT_CMD`／`_assert_refs`／`check_swallow`／`_subsections`／`_sub_paths`（检查 A） | **[B] 深度改写（本批 P0；判据层须重做，非仅扩形态）** | 三处：① **载体**——源件只认 `git grep -F -c -- "TOKEN" FILE`，我方为 `grep -c "TOKEN" FILE`（实测 059–065 各 6–16 处）→ **两种形态都认**（token 取引号内、目标取末参）；② **判据层（源件形态在我方恒空转的真因）**——源件 hit 门要求「改动清单节内有 `###` 子节并声明目标路径」，**实测我方改动清单节 `###` 计数：062／063／064／065 均 0，仅 059＝4、060＝8** → 须**改为表格形态判据**：`[A]` 行的**目标文本**取该行内的**引号块**（逐字文本，如「逐字…『…』」）优先、缺则取该行「改动」单元格全文；hit 门改为「**该断言的目标件出现在某 `[A]` 行**」；③ **空转明示（触发点改正）**——明示条件由「提取数＝0」改为「**提取 N＞0 而命中 0**」并打印「A：提取 N 条断言、命中 0 条——判据未适配本仓形态？」；明示行由 `static_report` 负责输出（**不得与「· 无发现」同屏**——命中 0 时抑制该行） |
| `_segments`／`CLAIM`／`LIMIT_MARKS`／`check_counts`（检查 B） | **[B] 改写** | `_segments` 是 B 的配对判据（源件自注），随 B 一并改写；判据由**词形黑名单**改**对象口径**：仅当紧邻段显式指向整件尺寸（含「全文／本件／实件／总行数／共 N 行／N 字符」——**「实件」须入表**，语料真阳性用它）或与 `wc -l` 类命令同段时才判，否则一律降为候选呈报（差异③；**校准与正负控见 §四 断言 11／12**）；**B 亦须空转明示**：候选配对数为 0 时打印「B：未提取到可判定的尺寸声称」 |
| `check_pairs`／`_pair_bodies`／`_collect_block`／`_strip_wrap`／`PAIR_NOW`／`PAIR_NEW`／`PAIR_NEW_HEAD`（检查 C） | **[B] 深度改写（判据层同样须重做）** | ① **列角色按表头别名定位**：路径列＝表头含「文件」或「新产品文档」的列（**059 §二 A 表路径在第 1 列、表头为「新产品文档」**；C 表表头为「文件｜改法（方向）｜自由度」）；自由度列＝表头含「自由度」者（无则取末列，且**单元格内含 `[A]` 者视同 `[A]` 行**——059 有「[A]＋[C]」写法）；**无任何别名可识别时整表跳过并明示**；② **逐字判据只对「目标文本来自引号块」的行生效**（我方规格的目标文本多为描述性散文，逐字核对会成片假阳性——**实测 065 F3／F4／F6／F8、059 B3／B6／D2–D4／D6／D9 等 `[A]` 行第 3 列均为描述性文字**）；无引号块的行**降为候选呈报**（提示「该 `[A]` 行无逐字目标文本，建议补引号块」）——此做法使「补引号块」成为可机械鼓励的写法；③ **目标侧块判据（本批 P0 修复，产物审查 P0-1）**——单元格内若出现方向标记（`→`／`改述为`／`改为`／`改作`／`替换为`／`换为`），逐字核对面＝**最后一个标记之后**的引号块（标记**之前**的引号块是**现文**、改后已被移除，核之必假阳性——实测 065 F3／F6 两条全属此类）；标记之后无引号块者（如 059 D2「路由表删「…」行」）**该行不核逐字**、降为候选呈报；无方向标记的行维持「全部引号块」口径。**已知残留**（本批不修，记 §七 开放项 7）：目标文本按规格相对路径或标点书写者仍会报「未落实」（060 F18／F19 实测）——属逐字判据的固有严格性，降噪做法是目标文本照实件形态书写 |
| `check_writing`／`_inline_spans`／`_edge_space`／`BACKTICK_RUN`／`BOLD_LEAD`／`LIST_LEAD`／`MD_LINK`／`FENCE_BLOCK`（检查 D） | [A] 照搬 | 四类：嵌套反引号／代码跨度边缘空格／加粗引导行紧跟列表／规格内相对链接可解析 |
| `static_report` | [B] 改写 | 退出码**恒 0**（仅呈报；差异⑤）；末尾三态判据降级为提示行，但**须计入 A／B／C／D 四类全部输出**——B 的计数行与 C 的候选行同样计入，「· 无发现」只在四类全空时打印（源件对计数发现已有此守卫，本批保留并扩至四类；漏计即假绿——实测 B 单源时「计数不符」与「无发现」同屏，见 §四 断言 14） |
| `_git_changes`／`_archive_equiv`／`_find_table`／`_table_findings`／`verify_report` | **[B] 改写／部分删除** | 过程产物改**两项**；**删除施工记录表结构检查**（`_find_table`／`_table_findings` 随之删或改判他物——按 §四 断言 12 的说明处置）（差异④）；`--verify` 保留但**不进 `check.sh`** |
| `__main__`／usage 串 | **[B] 改写（本批 P0）** | **零位置参数 → 打印「无在制规格，跳过」并 `exit 0`**（覆盖源件的「打印用法 ＋ exit 2」）；**参数错误／文件不存在 → `exit 2`**；usage 串内的旧文件名改为 `check_spec.py`；**「恒 0」专指静态发现不置红**，头注须写明该限定 |
| `_emit`／`repo_root`／`_abs`／`_section`／`LINE_LEAD`／`PATH_TOKEN` 等辅助函数与常量 | [A] 照搬 | 除上表明列者外一律照搬（`PATH_TOKEN` 属 B 的路径识别载体，随 B 判据一并复核）；**`FENCE_CODE`／`H3_HEAD` 不移植**——二者原是旧 A／C 判据的载体，判据重做后已无引用（产物审查 P2-2 实核） |
| 文件名 | [B] 更名 | `check_spec_assertions.py` → **`check_spec.py`**（同时**收 `测试与验收标准` §4 G3「工具更名」缺口**） |
| 呈报前缀约定 | **[B] 新立** | **非阻断发现一律 `·` 前缀**；`x` 仅留给参数错误类（须把源件的 `x 计数不符` 改为 `· 计数不符`，与差异⑤一致——源件自述「`x` 即阻断」） |

## 三、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py`（新建） | 按 §二 完整移植；头注按脚本文档头四要素（**用途**／**用法与参数**／**依赖与前置**＝Python 3 标准库、零外部依赖／**维护入口**），并含**事故出身**（020–025 六发断言自噬）、**退出契约**（「静态发现恒 0 仅呈报；参数错误 2」**须带限定词与 `--verify` 的区分**——同时销 G5）、**载体形态说明**（两种断言形态、表头别名、对象口径判据） | §二 ＋ 差异①–⑥ ＋ G5 | [B] |
| F2 | `scripts/check.sh` | ① 落 **[4] 段实段**：跑 `python scripts/check_spec.py --static`，**枚举口径＝`docs/specs/[0-9][0-9][0-9]-*.md`（显式排除 `施工机制.md`）**；无在制规格时打印「无在制规格，跳过」（**属预期常态，不置红**；**打印由工具负责**（零参分支），`check.sh` 只调用与透传，避免两处重复打印）；② **Python 三级链探测** `python3 → python → py`（与 `运行环境标准` §一.4 同序），缺失即明示置红（依据该件 §4 ③级行）；③ 头注 `TODO: [4]` 行改实段描述；④ 「依赖与前置」行补 Python 字样 | 差异⑥ ＋ 运行环境标准 §一.4／§4 ③级 | [B] |
| F3 | `docs/standards/测试与验收标准.md` §1.1 段表 [4] 行 ＋ §三 机检锚点对应行 | 两行状态「待建」→「**已实现**」；强制方式描述改「`scripts/check_spec.py --static`（接入 `check.sh` [4]；**非阻断**——候选类只呈报）」；§1.1「管什么」列中「`[A]` 项『替换为』侧文本」改述为「`[A]` 项目标文本（表格形态）」 | A4／A6 | [A] |
| F4 | `docs/standards/测试与验收标准.md` §4 已知缺口 G3 | **删除该行**（工具更名已随本批落地；不留「已销」措辞以免断言 8 假红），并在本批规格 §三 记「G3 已销」 | A5 | [A] |
| F5 | `docs/standards/测试与验收标准.md` §4 其余到期缺口（**G4／G5／G6／G7／G8／G11**，触发条件均为「工具重建时」） | 逐条处置并落表：**G5**＝随 F1 头注落地（销）；**G4／G6／G7**＝逐条判断是否已由本批移植解决（已解决则销，未解决则改述触发条件为「下批」）；**G8／G11**（子串匹配偏松／非规格件误报）＝本批**不修**但在该行写明「维持现状＋理由」（=A 的载体适配已覆盖 G11 的「非规格件误报」场景，若你判定未覆盖则保留） | P1-8 | [B] |
| F6 | `docs/standards/运行环境标准.md` §一.4 ＋ §三 机检锚点行 | §一.4 的「将固化（现未实现）」改述为「Python 三级链探测已落 `check.sh` [4] 段」；§三 该项状态行同步改「已实现（[4] 段）」 | 探测已落 | [A] |
| F7 | `docs/standards/规格写作标准.md` §三 机检锚点行（**8 行**：§1 判定表／§4 自审三条／§5.1 断言预验／§5.3 计数实跑／§5.4 自噬预警／§5.5 `[A]` 逐字／§5 写作四类／§6 过程产物比对） | 随本批落地者状态改「**已实现**」并写明载体（`check_spec.py`）；未接门禁者明示「未接」；**§5.3 行文的「『计数不符』阻断」改述为「只呈报」**（与差异⑤一致） | A7＋P1-7 | [B] |
| F8 | `CHANGELOG.md` | 本批一行（≤400 字符） | 过程产物两项 | [A] |
| F9 | `docs/standards/命名标准.md` §1 目录树 | `scripts/` 下补 `check_spec.py` 一行（该树标「现状」，本批新增件未入即失实） | 产物审查 P1-3（规范健康度） | [A] |

## 四、验收断言（含预验记录）

**夹具**（落 `.tmp/065/`，红线 2，跑完即删）——按**两个不同目标物**构造（A／C 与 B 的要求互斥：一个须入改动清单、一个不得入）：

- `fixture-target-a.md`（**A／C 用**）：内容含 token；**须写入**夹具的改动清单节（A 的 hit 门要求目标件出现在某 `[A]` 行）。
- `fixture-target-b.md`（**B 用**）：**不得**写入改动清单节；夹具在「现状锚点」节写一句「全文 999 行」（≠ 实测行数）→ 触发 `· 计数不符`（对象口径＝整件尺寸）。
- 夹具的改动清单须为**表格**（列：序号／文件:位置／改动／依据／自由度），含两行标 `[A]`：**正控**＝「改动」单元格带引号块**且含 token**（须触发**自噬预警**）；**负控**＝带引号块**但不含 token**（**不得**触发预警——其判别力＝目标文本按引号块取值、而非退化为取整格全文，若判据退化成整格取值则负控行会被误报为预警）；另在「现状锚点」节放 4 条假阳性样本（宣称表行数／diff 增量／零命中数／引用行号）供 B 的正控。
- 断言写在**围栏内**；计数声称（`999 行`）的数字前无 `LIMIT_MARKS`。
- `probe-b-only.md`（**B 组第二件夹具**，供断言 14）：**只含**「现状锚点」节一条整件尺寸错称（`全文 5 行`，目标件 `probe-t.md` 实测 3 行），**无断言节、无 `[A]` 行、无写作违例**——构造出「B 为唯一发现源」的形态。

### 断言 1｜新件语法有效（无副作用）

```bash
python -c "import ast,io;ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read());print('OK')"
```

期望输出 `OK`；预验基线：文件不存在。

### 断言 2｜[4] 段已落为实段

```bash
grep -c "TODO: \[4\]" scripts/check.sh
```

期望 0；预验基线 1。另核 `grep -c "^# \[4\]" scripts/check.sh` ≥1（预验基线 0）。

### 断言 3｜Python 三级链探测在位

```bash
grep -c "python3" scripts/check.sh
```

期望 ≥1；预验基线 0。另核探测顺序含 `python3` 在前。（**口径说明**：不用 `python|py` 松匹配——会命中 TODO 行里的 `.py` 文件名，实测 2 处。）

### 断言 4｜夹具四类缺陷全部报出（**逐字子串**）

```bash
python scripts/check_spec.py --static .tmp/065/fixture-spec.md
```

期望输出含以下四个**逐字子串**各 ≥1（源件实串，本批保留措辞）：`自噬预警`／`计数不符`／`[A] 未落实或已漂移`／`规格写作预警`；预验基线：工具不存在。

### 断言 5｜非阻断（退出码恒 0，且须以 B 真产出为前提）

```bash
python scripts/check_spec.py --static .tmp/065/fixture-spec.md; echo "rc=$?"
```

期望输出**含 `计数不符`**（证明 B 真触发，防旧件白过）**且** `rc=0`；预验基线：工具不存在。

### 断言 6｜无参行为（工具直测）

```bash
python scripts/check_spec.py --static; echo "rc=$?"
```

期望输出含「无在制规格」且 `rc=0`（**直测零参分支**）；预验基线：工具不存在。

另核（**收尾步**，须在本批规格归档之后）：`bash scripts/check.sh 2>&1 | grep -c "无在制规格"` ≥1 —— **口径说明**：`docs/specs/[0-9][0-9][0-9]-*.md` 在验收时会命中本批规格自身（实测 1 件），故「无在制规格」路径**须待归档后才成立**；归档前不触发属预期，不作为本批失败判据。

### 断言 7｜§1.1／§三 [4] 两行状态已改

```bash
grep -c "已实现" docs/standards/测试与验收标准.md
```

期望 **≥8**（实测基线 6，F3 改两行 ＋2）；预验基线 6。

### 断言 8｜G3 缺口已销

```bash
grep -c "工具更名" docs/standards/测试与验收标准.md
```

期望 0；预验基线 1。

### 断言 9｜`规格写作标准` §三 无「阻断」误述

```bash
grep -c "计数不符" docs/standards/规格写作标准.md
```

期望：该串若保留则须与「只呈报」同句（人工核）；`grep -c "阻断" docs/standards/规格写作标准.md` 期望 0；预验基线 1。

### 断言 10｜链接与守卫

全仓相对链接断链 0 且条数 ≥168（口径＝已跟踪 ＋ 未跟踪，脚本语言实现）；`bash scripts/check.sh` 退出 0（含新 [4] 段）。预验基线 168 条／0 断链、退出 0。

### 断言 11｜B 的正负控（夹具）＋ 无 `x` 前缀

```bash
python scripts/check_spec.py --static .tmp/065/fixture-spec.md
```

期望：**恰含 1 条 `· 计数不符`**（负控 `fixture-target-b.md`，对象口径＝整件尺寸），且**不得**报夹具内四条假阳性样本（表行数／diff 增量／零命中数／引用行号）；**输出不含以 `x`（后跟一空格）起首的行**（`x` 前缀只留给参数错误类）；另核**负控的判别力**：输出须含 `近似预测 3`（正控行的预测值）——判据若退化为取整格单元格文本，该值会变为 4（负控行散文里另含 token 一次）；预验基线：工具或夹具不存在。

### 断言 12｜旧语料回归（防过度抑制与空转）

```bash
python scripts/check_spec.py --static docs/specs/archive/059-*.md docs/specs/archive/060-*.md docs/specs/archive/061-*.md docs/specs/archive/062-*.md docs/specs/archive/063-*.md docs/specs/archive/064-*.md
```

期望：`计数不符` 条数 **≤1**，且**必须伴随明示行**（`B：未提取到可判定的尺寸声称` 或命中计数行）——**「0 条但无明示」判不通过**（区分"保守化成功"与"B 空转"）；若报出 1 条，须逐条判为「整件尺寸真阳性」并说明（**语料内确有一条真阳性**：060 审计报告附件「`scripts/check.sh` 称『39 行』」实为整件尺寸）；另核**邻段误判不得回潮**：输出**不得**含 `AGENTS.md 称「3 行」`（源 060:34 的「全文」属同段相邻**另一**声称；实测收窄前 1 条、收窄后 0 条，故该串即 P1-1 修复的判别力钉）；另核**检查 C 残留上界**：归档语料 `[A] 未落实或已漂移` 条数 **≤2**（实测修复前 5、修复后 2；残留两条为 §七 开放项 7 的路径形态／标点差——判据若回归到核「现文」块即 >2，故该上界是 P0-1 修复的判别力钉）；预验基线：工具不存在。

### 断言 14｜B 单源时不得打印「无发现」（三态判据须计入四类）

```bash
python scripts/check_spec.py --static .tmp/065/probe-b-only.md
```

期望：输出含 `计数不符` 且**不含** `无发现`（「· 无发现」只在 A／B／C／D 四类全空时打印）；rc=0；预验基线：工具与夹具不存在。**判别力**：修复前实测同屏（「· 计数不符」＋「· 无发现」），故本断言对旧行为必红。

### 断言 13｜过程产物两项

`CHANGELOG.md` 的 `[Unreleased]` › `### Changed` 含本批一行（≤400 字符）；规格验收通过后移入 `docs/specs/archive/`（移动不留副本）。另核 `施工记录表` 相关检查**已从工具中移除**（`grep -c "施工记录表" scripts/check_spec.py` 期望 0，预验基线：文件不存在）。

## 五、规格自审（三条）

1. **锚点现场核过**：§一 源件行数与顶层符号 49 项、六处差异（含：断言节位次实测「三／四均有」、改动清单表头四件实测变体、配对字面实测 4／8、旧 B 于 9 件实跑 8 条发现、`AGENTS.md` §五.2 原文、`check.sh` 段序与头注）、在制规格 0 份、`python -V` 3.12.8、`运行环境标准` §一.4 三级链与 §4 ③级行、A1–A8 基线，均为现场实跑。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1／2／3／7／8／9／10 已实跑基线（文件不存在／TODO 1 与 0／python3 0／已实现 6／工具更名 1／阻断 1／链接 168）；断言 4／5／6／11／12 依赖**尚不存在的工具或夹具**，属**未预验项**（下述）——其可判定性交审查位重点核；断言 13 的 `CHANGELOG` 条目待施工时落。
3. **禁止事项已写明**：见 §六。

**未预验项**：断言 4／5／6／11／12／14（工具与夹具尚不存在）。判别力设计：5 已加「须含 `计数不符`」前提；11 改为**夹具正负控**（真阳性 1 条须报、假阳性 4 类须不报）；12 加「须伴明示行」以防空转白过。

## 六、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/`（只读）、`docs/product/` 全部（本批不动产品层）、`docs/specs/施工机制.md`、本批规格自身；**不得改 `scripts/check.sh` 第 [0]、[1] 段的既有行为**（只在 [4] 处与头注／依赖行增改）。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；**不得在源件所在仓做任何写操作**（红线 3：源件经 `git show` 读出）。
- 禁止越权：不改版本号与 tag；不新增目录（`scripts/` 平铺）；**工具不得写任何文件**（只读规格、磁盘与只读 git 子命令）。
- 禁止改断言、改数字、删检查来变绿；**不得把工具做成阻断**（静态发现恒 0，见 §二 `__main__` 与 `static_report` 行）。

## 七、开放项

1. `--verify` 模式（改动面 ↔ `git status`、过程产物两项核对）本批随工具落地但**不进 `check.sh`**——理由是它与核验第 1 步的人工对照重复、且其阻断语义会与 `check.sh` 的二值闸／候选不拦截相混；使用两批后再评是否并轨。
2. `check.sh` [3] 段（内容轨）与 [5] 段（脚本语法）仍待建——本批只落 [4]。
3. 动态阶段（白名单执行规格内断言命令）本批**不接**（只接 `--static`，零执行风险）；是否纳入门禁待评估。
4. 新 B 的对象口径判据是**保守化**结果（宁可漏报不可误报）——若后续出现「整件尺寸声称」被漏报，按 §二 B 行的口径扩词表；校准数字见断言 11／12。
5. 检查 A 的 hit 门由源件的「`###` 子节声明目标路径」改为「目标件出现在某 `[A]` 行」（原判据在本仓真规格上 `###` 计数恒 0 → 恒空转；实测 062–065 均 0、仅 059／060 有）——**本批相对源件最大的判据偏离**，两批使用后复核是否需要恢复子节形态；空转明示（提取 N＞0 而命中 0）为过渡期的兜底可见性。
6. 检查 C 的逐字判据收窄为**只对含引号块的行生效**（无引号块者降为候选呈报）——对源件「`[A]` 项须成对」的放宽，代价是未按引号块书写目标文本时逐字核对不生效；接续做法是把「目标文本用引号块书写」作为规格写作建议（见 F 系列改动），若后续出现漏报按 §二 C 行口径收紧。

7. 检查 C 的**逐字严格性残留**（产物审查 P0-1 的未修面）：目标文本按规格相对路径（060 F18）或标点（060 F19）书写时会报「未落实」——判据本身正确（实件确实与该串不逐字相等），属固有严格性而非缺陷；缓解靠写作侧（目标文本照实件形态书写），两批后按实测决定是否加路径形态归一。
8. `测试与验收标准` §4 G6 的销理由**不完全**（产物审查 P2-1）：三项中「头注补不可审计类枚举」无落点（判据只在内联 docstring）——本批按「销」处理，若两批内再无人补，恢复该行并把触发条件写成「下批」。同 §4 G4 的「下批」口径。
9. `py_compile` 的跨件口径张力（产物审查 P2-7）：`运行环境标准` §5 行与 `skill形态标准` 仍把 `py_compile` 列为 [5] 段语法门禁手段，而本件头注明示不用（留缓存产物）、`MUTATING` 亦把它列为越权判据——[5] 段待建时统一裁定。

10. 检查 A 的**逐条跳过行**噪声（窄域复审 P2-1）：归档件单跑可占输出的四分之三（061 实例 19 行中 15 行）。该行为系**照搬源件**（`· 跳过（…）` 属 [A] 面），本批不改；若两批后仍觉噪声压过信息，改为一行计数（`· 跳过 N 条`）。
11. 检查 A 与 C 的「目标文本」两口径并存（窄域复审 P2-2）：A 取**全部**引号块（§二 A 行明定，只产候选行）、C 取**末个方向标记之后**的块（§二 C ③）——P0-1 的修法理由对 A 的 `n` 同样成立，但 A 的口径是本批据以校准时序的既有判据，故不动；附带代价是 C 在多目标行只核末个目标（漏报方向，已在 §二 C ③ 写明）。两批后按实测评估是否统一。

## 八、审查记录

- 成稿审查分身（fresh、只读）首轮：**findings 共 17：P0:2 P1:8 P2:7**。规划方逐条复核取证后**全部采纳（17／17）**，已全数进本版：**P0-1**＝检查 A 的抽取器只认旧仓 `git grep -F -c --` 形态，我方载体为 `grep -c "…" 文件` → A 在真规格上**恒空转**（夹具过而真规格假绿）→ §二 A 行改为「两种形态都认 ＋ 空转明示」，并新增断言 11 作对象口径校准；**P0-2**＝源件 `__main__` 零参即打印用法并 `exit 2`，而断言 6 期望「无在制规格 → rc=0」、§二 又漏 `__main__` 行 → 新增 `__main__` 映射行（零参 → 明示 ＋ exit 0；参数错误 → 2），断言 6 改为**经 `check.sh` 测**。**P1 八条**＝§二 缺 17 项符号且 `_segments` 误归 A（补全并归位 B）／改动清单表头四件变体致 C 空转（改按列角色 ＋ 别名容错）／B 的词形黑名单实测仅抑制 1 条（改对象口径 ＋ 给校准数字）／断言 4 的四关键词有两个在源件输出中不存在（改逐字子串）／夹具三条隐藏条件（逐条写清 ＋ 断言 5 加前提）／F4 留裁量致断言 8 必红（定死删行）／`规格写作标准` §三 七行未同步且 §5.3 行文含「阻断」（新增 F7）／`测试与验收标准` §4 六条「工具重建时」缺口到期（新增 F5 逐条处置，G5 随 F1 落地）。**P2 七条**＝断言节位次（判据去位次）／配对字面计数口径／`py_compile` 表述（源件仅列为判据）／依据改引 `运行环境标准` §4 ③级行与 §三 锚点／断言 7 基线改「＋2」／枚举口径须排除 `施工机制.md`／`--verify` 不进 `check.sh` 的依据改述。

- **窄域复审分身**（fresh、只读；按止损条 2「P0≥2 后只核原 P0 修复项 ＋ 直接影响面」）：原 P0 修复 **1/2**（P0-2 已闭环；**P0-1 未闭环**），新发现 **P0:1 P1:4 P2:3**，结论＝**修订后通过**。逐条处置：

  - **P0-1 未闭环（关键）**：载体形态已扩为两种都认，但**判据层未解**——A 的 hit 门仍要求「改动清单节内有 `###` 子节声明目标路径」，实测本仓真规格该节 `###` 计数 062／063／064／065 **均为 0**（仅 059＝4、060＝8）→ A 在真规格上**仍只出「跳过」**（夹具通过而真规格假绿），且「提取数＝0 才明示」是**死代码**（真规格提取 6–16 条）。→ **已改**：hit 门改为「目标件出现在某 `[A]` 行」；空转明示触发点改为「**提取 N＞0 而命中 0**」（真规格可达）；夹具按两个不同目标物重设计（A／C 与 B 的要求互斥）。记入 §七.5。
  - **新 P0×1**：断言 11 无判别力——059–064 旧 B 报 8 条（`x 计数不符` 4＋`· 基线漂移` 4），**其中 060 附件:44「实件 39 行 vs 实测 40」是真阳性**；新口径词表不含「实件」会把真阳性一并抑制，「0 条」既可能是保守化成功也可能是**空转**，无正负控。→ **已改**：断言 11 重写为**夹具正负控**（真阳性 1 条须报、4 类假阳性须不报）；新增**断言 12**（旧语料 ≤1 条 ＋ **须伴明示行**，「0 条但无明示」判不通过）；「实件」入对象口径词表。
  - **P1×4**：① 空转明示触发点覆盖不到主导形态（随 P0-1 改）；② 原夹具不足以产出 `自噬预警`（且条件 1／2 同用「目标件」若同指则互斥）→ 重设计为**两件目标物** ＋ 引号块正负控；③ C 的列角色判据在 059 表形态上错位（真规格第 3 列为描述性散文，逐字核对会成片假阳性）→ 改按**表头别名**（含「新产品文档」「改法（方向）」变体、`[A]＋[C]` 写法视同 `[A]`）且**逐字判据只对含引号块的行生效**（记入 §七.6）；④ 断言 6 可达性前提与枚举口径互斥（`docs/specs/[0-9][0-9][0-9]-*.md` 实测命中 1 件＝本批规格自身）→ 断言 6 拆**两段**（工具零参直测 ＋ 归档后经 `check.sh` 核），并写明归档前不触发属预期。
  - **P2×3**：① 断言 11 曾把 `x 计数不符` 列为可接受输出，与前缀约定矛盾 → 加入「不含 `x` 起首行」判据；② §一③ 件数与对象归类口径 → 已在 pass A 改；③ F2 打印层未写明 → 已写明「由**工具**打印（零参分支），`check.sh` 只调用与透传」。

- **核验三步（规划方实跑，非采信施工方转述）**：

  1. **改动面核对**：`git status --short` ＝ 5 改（`CHANGELOG.md`／三份标准件／`scripts/check.sh`）＋ 2 未跟踪（`scripts/check_spec.py`、本规格），与 §三 F1–F8 逐条吻合、无越界（§六 禁止面零改动）；`git diff --stat` ＝ 5 文件 45＋／22−；未跟踪新件 809 行、顶层符号与 §二 映射表可对。
  2. **断言独立重跑**：规划方按施工方附注四**重造夹具**后逐条实跑 1–13 —— 断言 1 `ast.parse` OK；2 `TODO: [4]`=0／`^# [4]`=1；3 `python3`=5 且顺序 `python3→python→py`；4 四逐字子串齐（含 `自噬预警`／`计数不符`／`[A] 未落实或已漂移`／`规格写作预警`）；5 rc=0 且含 `计数不符`；6 零参打印「无在制规格，跳过」rc=0；7 已实现=8；8 工具更名=0；9 阻断=0；10 链接 168／0 断链、`check.sh` rc=0；11 `· 计数不符` 恰 1、4 类假阳性零上报、无 `x` 起首行；12 `计数不符` 1（真阳性：060 附件「称 39 行」实测 **66**，随本批 `check.sh` 变长而变、真阳性性质不变）＋明示行「`· B：判定 2 条整件尺寸声称`」在场；13 `CHANGELOG` 条目 291 字符、工具内 `施工记录表`=0。
  3. **内容抽查**：读工具头注（四要素／事故出身／退出契约**含限定词**／载体形态说明／维护入口）与 `check_swallow()`／`check_counts()`／`check_pairs()`／`static_report()`／`__main__()`，逐条对 §二 映射表核；读 `check.sh` 与三份标准件 diff 核替换边界；全仓 grep 核已销缺口 ID（G3／G5／G6／G7）无存量悬挂引用（061 归档件的同名 G 编号属其自身裁定表，非本表引用）。

- **双维产物审查分身**（fresh、只读；时点＝核验第 1／2 步走完后）：**findings 共 12：P0:2 P1:3 P2:7**。规划方逐条复核取证后处置如下：

  - **P0-1（采纳）**＝检查 C 把 `[A]` 行内的**现文**引号块也当目标文本核 → 已落改动的行恒报「未落实」：本批规格自身 2／2、归档语料另 5 条，全为假阳性（夹具正控行的现文块恰仍在目标件内，故夹具全绿而真件翻车）。修法＝**目标侧块判据**（方向标记之后的引号块、标记后无块者不核逐字），已写入 §二 C 行 ③；严格性残留（路径形态／标点）记 §七 开放项 7。
  - **P0-2（采纳）**＝三态判据漏计 B → 「`· 计数不符`」与「`· 无发现`」同屏（B 单源时假绿；源件对计数发现本有守卫，本批随差异⑤删去后未并回）。修法已写入 §二 `static_report` 行，并新增**断言 14** 固化该形态（夹具 `probe-b-only.md`，对旧行为必红）。
  - **P1-1（部分采纳）**＝B 对「表行数」类残余假阳性（`AGENTS.md 称「3 行」`，源 060:34 的「全文」属相邻另一声称）——修法为「整件标记只看 N 之前的紧邻文本」，**须自证不伤 060 附件:44 的真阳性**，做不到则维持现状并记入 §七 开放项 4。
  - **P1-2（采纳）**＝`规格写作标准` §三「§4 自审三条齐备」误翻「已实现」（工具无该判据）→ **回退「待建」**（F7 只允许「随本批落地者」翻状态）。
  - **P1-3（采纳）**＝`命名标准` §1 目录树（标「现状」）漏新件 → 新增 **F9** 补 `check_spec.py` 一行。
  - **P2 七条**：P2-1（G6 销理由不完全——「头注补不可审计类枚举」无落点）、P2-7（`py_compile` 跨件口径张力）→ 规格侧记 §七 开放项 8／9；P2-2（死常量 `FENCE_CODE`／`H3_HEAD`）、P2-3（C 的静默跳过补明示）、P2-4（`NEVER_ARCHIVED` 注释指向已废件）、P2-5（病句）、P2-6（负控判别力须钉住 `近似预测 3`）→ **全部采纳**，落施工修订。
  - **审查未验证项三条**（如实转述）：断言 6 后半须待本规格归档后方可达（规格已自述属预期）；`--verify` 全绿路径归档前不可能成立；动态阶段的「不可审计／未跑成」两态未另造样本（`report()` 与源件逐字相同，判据未改）。

- **修订轮核验（规划方实跑，独立复核施工方自证）**：P0-1 → `--static docs/specs/065-*.md` 的 `[A] 未落实或已漂移` **2→0**；归档 059–064 **5→2**（残留 2 条即 §七 开放项 7 的路径形态／标点差，判据正确）；夹具仍报 2 条且**均为方向标记之后的目标块**（F1 正控 `alpha-token-甲 单行说明`／F2 负控 `delta-未定稿替换`），现文块不再被核——检查 C 未因此变空转。P0-2 → `.tmp/065/probe-b-only.md` 实跑含 `计数不符`、**不含** `无发现`（修复前同屏）。P1-1 → 归档语料 `基线漂移` **1→0**（`AGENTS.md 称「3 行」` 消失）而真阳性仍在（`计数不符` 仍 1 条＝060 附件:44 的「实件 39 行」），故予以实施；其判别力已钉入断言 12。断言 11 的 `近似预测 3` 在场（负控判别力成立）、4 类假阳性仍零上报、无 `x` 起首行。P1-2 → `规格写作标准` §三「§4 自审三条齐备」已回退「待建」。P1-3 → `命名标准` §1 目录树已补 `check_spec.py`（F9）。P2-2／P2-3／P2-4／P2-5 均已落实施工。施工方另提未决 2：`H3_HEAD` 已删而本规格 §二 照搬清单仍列之 → **规格侧已同步**（标注二者不移植并写明理由）。

- **窄域复审分身**（fresh 复用同一审查位；按止损条 2 只核原 P0 修复项 ＋ 直接影响面，逐条实跑自证）：**原 P0 修复 2/2｜新发现 P0:0 P1:0 P2:2｜结论 通过**。

  - **P0-1 闭环**：本批规格自身 `[A] 未落实或已漂移` 0 条（修订前 2）；归档 059–064 **5→2**（残留两条经其逐条核归因为 060 F8 的**路径形态**与 F19 的**标点差**，与「现文块」不同成因，属 §七 开放项 7，非回归）。**反向检查无新漏报**：(a) 无标记行仍核全部块且抓到缺失值、(b) 标记落在引号块内者落「不核」、(c) 多标记取末者、(d) 标记后无块者不误核现文——四型样本行为与 §二 C ③ 声明一致，且带标记行仍能抓真缺陷（C 未空转）。059–064 的「`· C：N 行无核对面`」明示行各件在场（059＝6／060＝4／063＝1／064＝4）。
  - **P0-2 闭环**：`probe-b-only` 输出改为「`· 计数不符` ＋ `· 有预警 1 条`」；「`· 无发现`」在四类全空时**仍可达**（`quiet.md` 实证）；A 空转明示行与「`· 无发现`」仍不同屏（`idle-probe` 实证）；归档跑末行由 38 → **42** 条（B 的条数已并入 `findings`）。
  - **P1-1 无副作用**：归档 `基线漂移` 0 条、`计数不符` 仍 1 条真阳性；新造「标记在数字之前」样本（`全文 9 行`）仍被抓；原探针的「表行数 7 行」不再上报。P1-2／P1-3／P2-2～P2-6 逐条 grep／读文件核实到位；无新伤（`check.sh` rc=0、lint 0 issues、链接 168／0、改动面仍为 F1–F9）。
  - **复审新发现两条 P2（不阻通过）**：P2-1（A 的逐条跳过行噪声，照搬源件未改）→ §七 开放项 10；P2-2（A／C 两「目标文本」口径并存、C 只核末个目标）→ §七 开放项 11，并按建议把「C 残留 ≤2」钉入**断言 12**（与 P1-1 的判别力钉并列）。
