# 066-2026-09-12-核销入模板与补核销

> 定位：修一条机制债——064 批新立的**开放项核销机制**只活在 `施工机制` §三 条文里、**未进 §八 规格模板**，导致次日写 065 规格时没有任何位置提示要对账，065 批因此**漏做核销**。本批把核销位写进模板、给「相关」定可判据，并补做 064→065 那一环的核销（18 条逐条标状态）；**另修本批实跑触发的工具崩溃一处**（F4，见 §一⑥）。
> 自由度分布：[A]×1 [B]×4
> 必读输入：`docs/specs/施工机制.md`（§三 过程产物两项与核销机制／§五 审查与止损／§八 模板）、《规格写作标准》§10（规格可选节的落点惯例）、`docs/specs/archive/064-2026-09-12-规则债清理批.md`（§三 首次核销表格式与 §七 六条）、`docs/specs/archive/065-2026-09-12-规格静态自检器搬回.md`（§七 十一条）、`AGENTS.md`（§四 平行台账不做、§五 三条分工原则）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**债的现场**：

| # | 事实 | 实测命令与结果 |
| --- | --- | --- |
| ① | 核销机制条文在位、**模板无核销位** | `docs/specs/施工机制.md`:74 载机制全文（「新批规格须核对相关归档规格的开放项……触发条件：本批改动面与某归档规格相关」）；§八 规格模板（`:158` 起、节标题 `:168`–`:203`）为**八节**——一 现状锚点／二 文件级改动清单／三 验收断言／四 规格自审／五 禁止事项／六 被否决的替代方案（可选）／七 开放项／八 审查记录，**无核销位** |
| ② | 065 批**漏做核销**（直接证据） | `grep -c "核销" docs/specs/archive/065-*.md` ＝ **0**（该件八节全读完亦无对账表）。**非归档件的「核销」命中共三处**：`施工机制.md`:74（机制条）／`:154`（归档件纪律句）、`CHANGELOG.md`:12（064 条目）——即条文只在机制件与沿革里，**规格侧零落地** |
| ③ | 064 的核销表**落点不规范**（就地塞进改动清单节） | `docs/specs/archive/064-*.md`:44 节标题为「`## 三、首次核销表（五份归档规格 35 条逐条）`」——§三 在模板中是「文件级改动清单」，即 064 未按模板节序落核销，属「模板无位 → 就地造节」的实证 |
| ④ | 追加节的**既有惯例**是「开放项节之前」 | `docs/standards/规格写作标准.md`:91：「落点：追加在规格的「开放项」节之前；模板见 `施工机制` §八」（§10 被否决的替代方案）——核销节沿用同惯例即可自洽 |
| ⑤ | 待核销条目总数 | 064 §七 **6 条**（实测该节 `1.`–`6.`）；065 §七 **11 条**（实测 `1.`–`11.`）；**061-6 一条**（按 F2 判据「有文件交集」补入，见 §七 范围注）；合计 **18 条** |
| ⑥ | 工具对**空引号对**崩溃（本批实跑触发，F4 的出身） | **触发链**：本批规格修订轮把 `[A]` 行的措辞写成含**空引号对** `「」`（「节名一律用「」」）→ `bash scripts/check.sh` 的 [4] 段抛 `AttributeError: 'NoneType' object has no attribute 'strip'`（`check_pairs()` → `_strip_wrap(None)`）、**rc=1 置红**（规划方现场观测）；该措辞随后已改写为「节名一律用直角引号」（见 F2／F3 行），故**当前规格自身不再复现**。**稳定复现载体＝夹具**：`.tmp/066/crash.md`（一张改动清单表、其 `[A]` 行「改动」单元格含空引号对）实测同样崩（施工方复跑 rc=1 → 修复后 rc=0，见断言 9）。**根因**：`_target_blocks()` 的 `m.group(1) or m.group(2)` 在空对时取到 `None`。**影响面**：任意规格的 `[A]` 行写空引号对即可置红基线、造成**提交门禁假红**——守卫对合法输入崩溃，属 P0。**附记**：`_change_rows()` 取节体时在下一个 `^##` 行截断，故**围栏内的示例表不进 [4] 段工具的解析面**——规格里放夹具示例对工具是安全的（本次实测）；**规格自身的计数式断言不享此豁免**（断言 4 的一行式扫全件、不跳围栏，见该断言口径注） |

**机制空转的因果链（本批要断的）**：§三 立条文（有机制、无路径）→ §八 模板无位（写规格时无提示）→ 065 未写核销节（对账缺失）→ 064 §七 六条自 064 归档后**无人过问**。

**不变量（施工前后须同）**：`docs/specs/archive/` 全部件零改动（只读）；`scripts/`、`docs/product/`、`skills/` 零改动；`施工机制` §一／§二／§四–§七 既有条文与段序表述零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md` §八 规格模板（`:195`–`:207` 区间） | 在「六、被否决的替代方案（可选）」与「开放项」之间**新增一节**（节标题含「本批核销」四字），原「七、开放项」「八、审查记录」顺延为**八／九**；节内三件：① 适用判据一行（相关则必写、无相关整节省略——与模板「六」的「无比较则整节省略，不留空节」同构）；② 核销表骨架（**真表头 ＋ 分隔行**，列含「理由／落点」）；③ 汇总句式一行（恒列五档，无该档计 0——消解 §三 机制条「视需要」与固定句式的张力）。**逐字插入文本见本节末「F1 逐字文本」段——须逐字照录** | §一①②③④（事实） | [B] |
| F2 | `docs/specs/施工机制.md` §三 `:69`／`:74`（核销机制条与同节落点清单） | 三处：① **「相关」判据**——**本批改动面与某归档规格相关** → **本批改动面与某归档规格有交集**（**同一文件／同一主题／同一产物**，三者任一）；② **落点指针**——`核销落本批规格，` → `核销落本批规格的「本批核销」节（模板见 §八），`；③ **同节 `:69` 落点清单**——`（含「开放项」节）` → `（含「本批核销」（相关时）与「开放项」节）`。**该条其余文字（含「（不设时间触发）」「不另建台账、不建索引件」）原样保留**——原句无重复语，不得删改；**节名与档名一律用直角引号**——该句内『本批处置／待作者』与「已履行／仍待／作废」混用，一并归一为「」（`文档写作标准` §一.2.6） | §一② ＋ §一 因果链 | [B] |
| F3 | `docs/standards/规格写作标准.md` §10.3 `:91` | 落点句**只替换该句**：`追加在规格的「开放项」节之前` → `追加在规格的「本批核销」节之前（该节不存在时即「开放项」节之前）`（节名一律用直角引号，`文档写作标准` §一.2.6；**不得裹粗体**——原句无粗体，加粗会破对标记）——F1 插入新节后原句与模板节序不再一一对应。**该行其余文字（含行末模板链接）原样保留** | F1 的直接后果 | [A] |
| F4 | `scripts/check_spec.py`（`_target_blocks()` `:350` 起 ＋ `_quoted_blocks()` `:334`） | 修空引号对失效（出身见 §一⑥），**两处同一口径**：① `_target_blocks()`——按**组是否参与匹配**取块内容（`m.group(1) if m.group(1) is not None else m.group(2)`）并**滤除空块**（原 `m.group(1) or m.group(2)` 遇空对取到 `None` → 崩）；② `_quoted_blocks()`——同法取值并滤空（原 `a if a else b` 遇空对得 `''`，使 `_row_target_text()` 误以空串为「有目标文本」而**不再回落到改动单元格全文**，A 的真实预警被吞、改显一条误导性中性行——产物审查 F-02／F-05 实证）。改后 **A 与 C 对空引号对行为一致**。同文件只此两处；其余函数、判据、输出措辞与退出契约零改动 | §一⑥（实跑触发）＋ 产物审查 F-02／F-05 | [B] |
| F5 | `docs/specs/施工机制.md` `:153`（提交与登记节的登记句）＋ `AGENTS.md` `:52`（不做清单·平行台账逃生口） | 两处落点枚举补「本批核销（相关时）」：`规格（含审查记录）` → `规格（含本批核销与审查记录）`；`当批规格的开放项与审查记录节` → `当批规格的本批核销（相关时）、开放项与审查记录节`——本批新增核销节后，两处枚举不再完整（产物审查 F-06；模板「落点纪律」bullet 所引依据即后者） | 产物审查 F-06 | [B] |

**F4 夹具**（落 `.tmp/066/crash.md`，红线 2，跑完即删；供断言 9）：

```markdown
# 崩溃探针

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| R1 | `AGENTS.md`:1 | 空引号对样本（节名一律用「」以外的写法） | §一 | [A] |
| R2 | `AGENTS.md`:2 | 非空引号块「这一串在目标件里不存在，应被报出」 | §一 | [A] |
```

（R1 的空引号对是崩溃诱因；R2 的非空块须仍被核——修复不得把非空块一并吞掉。目标件 `AGENTS.md` 实存，故 R2 应报 1 条「`[A]` 未落实或已漂移」。）

**F1 逐字文本**（施工方须逐字插入，节标题与列名不得改字）：

```markdown
## 七、本批核销（相关归档规格的开放项）

- 触发：本批改动面与某归档规格**有交集**（**同一文件／同一主题／同一产物**，三者任一）时，此节必写；无相关归档规格则整节省略，不留空节。
- 核销面：**必列**——① 前一批规格的全部开放项；② 本批改动直接触及或使其状态可能变化的条目（含前批应销未销者）。**不必重列**——已由既往核销表承载、且本批未使其状态变化者，本节只给该表的索引指针（避免两份并行口径）。
- 写法：**只核销、不复述原文**——一行一条，状态取五档之一（已履行／作废／本批处置／仍待／待作者），理由写当时判据或落点（无理由的「仍待」等于没核）。
- 表（列名固定，末附汇总）：

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |

- 汇总：`按条计 n 条：已履行 a｜作废 b｜本批处置 c｜仍待 d｜待作者 e ＝ n`——**恒列五档、无该档计 0**，且逐格可复核、不得与表列不符。
- 落点纪律：核销落**本批规格**，不另建台账、不建索引件（`AGENTS.md` §四 平行台账不做）；跨批检索以各批规格的核销表为唯一索引落点。
```

## 三、验收断言（含预验记录）

### 断言 1｜模板核销节在位（两处 token）

```bash
grep -c "本批核销" docs/specs/施工机制.md
```

期望 ≥2（实跑 **4 处**：`:69` 落点清单／`:74` 落点指针／`:153` 登记句／§八 模板节标题——后两处由 F5 与 F1 落位）；预验基线 **0**。

### 断言 2｜模板节序正确（新增节在开放项之前、审查记录在最后）

```bash
grep -n "^## 七、本批核销\|^## 八、开放项\|^## 九、审查记录" docs/specs/施工机制.md
```

期望恰三行且行号递增；预验基线：**本命令实跑 0 行**（三模式全无命中）。现模板的对应标题为 `:199 ## 七、开放项`／`:203 ## 八、审查记录`。**误命中已复核**：该件自身的 `## 七、提交与登记`／`## 八、模板（…）` 不含「开放项／审查记录」字样。

### 断言 3｜「相关」判据与表列名在位

```bash
grep -c "有交集" docs/specs/施工机制.md; grep -c "同一文件／同一主题／同一产物" docs/specs/施工机制.md; grep -c "理由／落点" docs/specs/施工机制.md; grep -c "无相关归档规格则整节省略" docs/specs/施工机制.md; grep -c "不设时间触发" docs/specs/施工机制.md
```

期望依次 **≥1／≥1／≥1／≥1／≥1**（第二项＝判据括注，第五项＝「原句其余文字原样保留」的判据，F2 不得删该限定语）；预验基线 0／0／0／0／**1**。

### 断言 4｜本规格核销表逐格可复核（18 条）

```bash
python -c "import io,re;s=io.open('docs/specs/066-2026-09-12-核销入模板与补核销.md',encoding='utf-8').read();print(len([l for l in s.splitlines() if re.match(r'\| 0\d\d-\d+ \|', l)]))"
```

期望 **18**（与 §七 汇总句的 `n` 相等）；预验基线：本表行数实测 **18**（补 061-6 前为 17；该断言对初稿即可跑，故不列入未预验项）。**口径注**：该一行式的判据＝**「来源列形如 `| 0NN-N |`（条目号）」的行**，故不受 §二 规则行与 §二 夹具示例（R1／R2）干扰——初版按「行首竖线 ＋ 空格起首、且含状态词」判，被本轮 F2 行（含档名枚举的表格行）多计 1，实测 19 → 改正后 18 ✓。

### 断言 5｜`规格写作标准` §10.3 落点句已改述

```bash
grep -c "本批核销" docs/standards/规格写作标准.md
```

期望 1；预验基线 0。

### 断言 6｜禁改面零改动（含 F4 的单点例外）

```bash
git status --porcelain docs/specs/archive docs/product | wc -l
git status --porcelain scripts
git diff -U0 scripts/check_spec.py | grep -c "^@@"
git diff -U0 scripts/check_spec.py | grep "^@@" | grep -vc "_target_blocks\|_quoted_blocks"
test -e skills && echo "skills 存在（不合预期）" || echo "skills 未创建（符合）"
```

期望依次：**0** ／**恰一行**且为 `M scripts/check_spec.py`（`git status` 输出该行首列有一空格；本批 `scripts/` 内唯一改动件＝F4 的**两处函数**）／**4** ／**0** ／「skills 未创建（符合）」。**第三、四项须成对看**——「hunk 数 ≥1」与「全部 hunk 落在 F4 授权的两函数（`_quoted_blocks()`／`_target_blocks()`）」合起来才表示「有 diff 且恰在本批授权面」，单看第四项（施工前平凡为 0）无判别力。预验基线：0／0 行／0／0／「未创建」。预验基线：0／0 行／0／0／「未创建」（实跑：`skills` 不存在，故不走 `git status` 路径以免该命令因路径不存在而报错）。另核 `git diff -U0 docs/specs/施工机制.md` 的 hunk 仅落 §三 `:69`／`:74`、§七 `:153`（F5-a）与 §八 模板区间；并走查 `:74` 仍含「不设时间触发」（判据另见断言 3 末项）。

### 断言 7｜链接与守卫

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split('\0');f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md')];L=[(p,u) for p in f for u in re.findall(r'\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print('链接',len(L),'断链',sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 `链接 168 断链 0`——**该一行式即断言取值口径**（口径＝已跟踪 ＋ 未跟踪 `.md`，排除 `http`／`#`／`mailto`；根级文件 `dirname` 为空串，`join` 后退化为仓根相对路径）；另核 `bash scripts/check.sh` 退出 0。预验基线 168／0、退出 0。（本批**不新增任何 Markdown 链接**，故施工后应仍为 168／0。）

### 断言 8｜过程产物两项

验收通过后本规格移入 `docs/specs/archive/`（移动不留副本）；`CHANGELOG.md` 的 `[Unreleased]` › `### Changed` 含本批一行（≤400 字符）：

```bash
grep -c "核销入模板与补核销（066）" CHANGELOG.md
grep -c "核销" docs/specs/archive/066-*.md
```

期望各 ≥1（第二项须归档后方可跑）；另核 `CHANGELOG` 该行 ≤400 字符。

### 断言 9｜空引号对不再致崩（夹具，且非空块仍被核）

```bash
python scripts/check_spec.py --static .tmp/066/crash.md; echo "rc=$?"
```

期望：**输出无 `Traceback`**、含「`== 静态自检 ==`」与「有预警 N 条」摘要行、`rc=0`；且输出**恰 1 条** `[A] 未落实或已漂移`（R2 的非空块——证明修复只跳空块、未吞非空块）。预验基线：**崩溃**（`AttributeError: 'NoneType' object has no attribute 'strip'`，`rc=1`，实测见 §一⑥）。

### 断言 10｜孪生夹具：空引号对不得改变判读（A 侧）

两件夹具仅差一处**空引号对**（落 `.tmp/066/`，红线 2，跑完即删）：

````markdown
# 探针 B（对照组：无引号块）

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| X1 | `AGENTS.md`:1 | 散文提到 Abzu 一词 | §一 | [A] |

## 三、验收断言

```bash
grep -c "Abzu" AGENTS.md
```
````

````markdown
# 探针 A（空引号对 ＋ 散文提及 token）

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| X1 | `AGENTS.md`:1 | 空引号对「」＋散文提到 Abzu 一词 | §一 | [A] |
| Y1 | `AGENTS.md`:3 | 改为「」 | §一 | [A] |
| Y2 | `AGENTS.md`:4 | 空引号对「」 | §一 | [A] |

## 三、验收断言

```bash
grep -c "Abzu" AGENTS.md
```
````

```bash
python scripts/check_spec.py --static .tmp/066/probe-a.md
python scripts/check_spec.py --static .tmp/066/probe-b.md
```

期望：两件输出的 `自噬预警: Abzu` 行**逐字相同**（`现行 2｜目标文本内 1｜近似预测 3`），即空引号对**不改变**判读。**判别力**：修复前 probe-a 落「现行有值／目标文本未提及」中性行、**预警消失**（真实目标文本被空块吞掉）；故本断言对修复前行为必红。预验基线：修复前 probe-a 无预警行（实测见 §九）。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 五条事实均实跑——`施工机制.md`:74 机制全文与 §八 模板八节（节标题行号 `:168`／`:172`／`:178`／`:184`／`:190`／`:195`／`:199`／`:203`）、065 规格「核销」命中 **0**、064 核销表落 `:44`（§三）、`规格写作标准.md`:91 惯例句、064 §七 6 条与 065 §七 11 条（`1.`–`6.`／`1.`–`11.`），连同按 F2 判据补入的 061-6 共 18 条。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1／2／3／4／5／6／7 已实跑——断言 1 基线 **0**；断言 2 基线为两行（`:199 ## 七、开放项`／`:203 ## 八、审查记录`）；断言 3 基线 **0／0**；断言 4 实得 **18**（与 §七 汇总 n 相等）；断言 5 基线 **0**；断言 6 实得 **0**（`skills` 不存在，已改述命令避开该路径的报错）；断言 7 实跑 168 条／0 断链且 `check.sh` 退出 0（**[4] 段首跑本规格**：C 检查把 F3 的目标文本报为「`[A]` 未落实或已漂移」——改动尚未施工，属预期真实阳性，非缺陷）。断言 9 已实跑**基线（崩溃）**、其修复后期望值属**未预验项**；断言 8 后半（归档后自查）亦属未预验项。
3. **禁止事项已写明**：见 §五。

**未预验项**：断言 8 后半（归档后自查）与断言 10 修复前期望值（见下）——前者归档后可跑，后者以孪生夹具取基线；不另设夹具。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 全部（只读）、`docs/product/` 全部、`skills/`（未创建）、本批规格自身；`scripts/` 内**只允许 F4 的单点修改**（`check_spec.py` 的 `_target_blocks()`），`scripts/check.sh`／`scripts/hooks/` 与 `check_spec.py` 其余部分零改动。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；**不得把归档开放项「搬进」本批规格（只核销、不复述原文）**——此纪律承 064 §六。
- 禁止越权：不改版本号与 tag；不新增文件（本批只改**三件**既有文件：`施工机制.md`／`规格写作标准.md`／`check_spec.py`）；**不建台账／不建索引件**。
- 禁止改断言、改数字、删检查来变绿；**不得给核销配机器闸**（见 §六 方案 B）。

## 六、被否决的替代方案（可选）

- **只补 064→065 那一环核销、不动模板** → 否决：漏做的**成因**是模板无位（§一 因果链），只补表则下批仍无提示，属治症不治因。
- **给核销配机器闸**（工具检查「规格是否带核销节」）→ 否决：本仓不做清单列有「注册表类守卫」（引入须先有事故出身），且「相关」判定需语义（那份归档规格与本批相关吗）——非确定性可判；本批以模板位 ＋ 落点指针替代，机械化留待真出现漏做复发时再评估。
- **核销改落独立索引件**（如 `docs/specs/open-items.md`）→ 否决：`AGENTS.md` §四 平行台账明确不做；跨批检索已由「各批规格的核销表」承担（`施工机制` §七 归档件纪律）。
- **只改本批规格的措辞、不修工具**（把「节名用「」」改写掉以避开崩溃）→ 否决：崩溃诱因是**任意** `[A]` 行的空引号对，属守卫对合法输入崩溃；只绕不修，则下一位把空引号对写进规格的人会看到红基线并把工具缺陷误判成**自己的规格问题**（症状误导），且 `check.sh` 的 [4] 段会给提交门禁带来假红。
- **另开一批修工具、本批只做模板与核销** → 否决：`check.sh` 现为红（[4] 段崩溃即置红），按 `AGENTS.md` 红线 6「禁带病开工」不得在红基线上叠改动收尾；且该缺陷由本批实跑首次暴露（事故出身就在本批），并批修最省成本。

- **把核销节排到「审查记录」之后（模板末位）** → 否决：`规格写作标准` §10.3 的既有惯例是「追加在开放项节之前」，且对账（清旧账）宜先于记账（列新欠账）——本批与既有惯例对齐。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 064-1 | 可移植模板形态（与 059-2／060-3 同题） | 仍待 | 待底座首次移植到另一项目时按需立法；触发未到 |
| 064-2 | 甲节 33 条是否恢复原「R 编号」体系 | 待作者 | 待作者裁定（063-6 同题） |
| 064-3 | 灵感库契约「消费方」行的跨仓指针 | 待作者 | 外部资产只读，须作者在库侧仓库自行提交（红线 3） |
| 064-4 | 跨层计数同类排查结论（备案） | 仍待 | 「将来新增跨层计数即回本项」——本批未新增跨层计数，触发未到 |
| 064-5 | F4 撤条后的历史指针（备案不改） | 仍待 | 归档件只读，属历史记录；留此以免将来被当缺陷重报 |
| 064-6 | 加粗密度超阈值件分类留痕（备案） | 仍待 | 备案件；口径裁定已落 `文档写作标准` §一.3.8，动作项在 061-8（已由 064 处置） |
| 065-1 | `--verify` 是否并轨进 `check.sh` | 仍待 | 规格载明「使用两批后再评」；时点未到 |
| 065-2 | `check.sh` [3] 段与 [5] 段仍待建 | 仍待 | 本批不动 `scripts/`；待相应主题批 |
| 065-3 | 动态阶段是否纳入门禁 | 仍待 | 同上，待评估 |
| 065-4 | 新 B 的对象口径判据是保守化结果 | 仍待 | 若后续出现「整件尺寸声称」被漏报，按规格 §二 B 行扩词表 |
| 065-5 | 检查 A 的 hit 门改动（本批最大判据偏离） | 仍待 | 规格载明「两批使用后复核」；时点未到 |
| 065-6 | 检查 C 逐字判据收窄为「只对含引号块的行生效」 | 仍待 | 已实施；条目为「若漏报则收紧」的观察项 |
| 065-7 | 检查 C 的逐字严格性残留（路径形态／标点） | 仍待 | 判据本身正确，缓解靠写作侧；两批后按实测决定是否加形态归一 |
| 065-8 | `测试与验收标准` §4 G6 销理由不完全 | 仍待 | 「两批内无人补则恢复该行」；本批不补（属工具面） |
| 065-9 | `py_compile` 跨件口径张力 | 仍待 | [5] 段待建时统一裁定 |
| 065-10 | 检查 A 的逐条跳过行噪声 | 仍待 | 照搬源件的既有行为；两批后评估改一行计数 |
| 065-11 | 检查 A 与 C 的「目标文本」两口径并存 | 仍待 | 两批后按实测评估是否统一 |
| 061-6 | 对外／跨会话产物不写行号、改符号锚点 | 仍待 | 该条触发＝「施工交底／核验请求模板下次修订时一并」，落点即 `施工机制.md` §八；本批只改同节的**规格模板**与 §三 条文，未修订交底／核验请求两模板——触发未到（本行系按 F2 新判据「有文件交集」补入） |

**汇总**：按条计 18 条：已履行 0｜作废 0｜本批处置 0｜仍待 16｜待作者 2 ＝ 18 ✓（逐格可复核）。

**本批另记一笔流程账**：064 §七 六条本应在 **065** 批核销而漏做（065 规格「核销」命中 0，见 §一②）——该环由本表补上（064-1…064-6 六行即其核销），成因由 F1 修除。**同一性注**：064-1 与 059-2／060-3 为同题三条（可移植模板形态），064 核销表已注明未合并计数，本表承其口径。**范围注**：059–063 其余条目的状态由 064 §三 核销表承载（那五份的索引落点即该表）；本批只补与本批改动面**有文件交集且触发可能在本次到期**的 061-6 一条——059 D6／060 F1／061 F9 等虽同改 `施工机制.md`，但其条目本身不因本批而状态变化，不重列（否则与 064 表成两份并行口径）。

## 八、开放项

1. 核销的执行仍属**人判**（无机器闸，见 §六 方案 B）——若将来出现「相关却漏写核销节」的复发，按 §六 的机械化评估重新立项（届时以本次复发的实际成本为事故出身）。
2. 064 核销表的「处置后仍待余额 14 条」（源自 059–063）仍在 `docs/specs/archive/064-*.md` §三——该表是跨批检索的唯一索引落点。本批按「有文件交集」判据补核了其中 061-6（见 §七），其余条目虽与本批同改 `施工机制.md`／`规格写作标准.md`，但条目本身不因本批而状态变化，故不重列、不复制口径；留该表待相关批认领。
3. 工具健壮性（F4 的同类面）：本批修的是**空引号对**一条崩溃路径，**未做异常输入的成体系排查**（本次为工具首次在他件上实跑即崩）——若后续再出现同类崩溃，按下批主题立一轮「静态自检器的健壮性面」评估（含空块、超长行、畸形表格等）。
4. `施工机制.md` `:102` 仍有 `『同类精度 P0（计数／断言互斥／范围失控三类）』` 一处『』用法（本批只归一了 `:74` 的档名，那处属历史引语，未纳入 F2）——下批顺手归一时顺带处理。
5. **修订轮反复暴露的系统性缺口（本批最重要的留痕）**：三轮共 **5 例**同族瑕疵（断言 4；断言 6；断言 3／4／6）——**全部是「规格正文改了、断言的 token／期望没跟着改」**，且每次都由施工方按红线 4 拦下。现有 [4] 段的 token 门只对「目标件出现在某条 `[A]` 行」的断言生效，本批三例的目标件均属 `[B]` 项 → 工具落「跳过」，这类失配**只能靠人工对账兜住**。这正是 `065` §七 开放项 11 指向的面（A／C 口径与覆盖），**建议作下批的首要候选**：把 [4] 段的 token 门扩到 `[B]` 行目标件，或固化一条「每轮修订后逐条对账断言 vs 正文」的收尾动作（后者属改规则、前者属改工具，路径不同）。
6. 「相关」判据现定为「有交集」——是否过窄（如「同主题但文件不同」的情形会漏核销），待两批实测后校准。

## 九、审查记录

- 成稿审查分身（fresh、只读）首轮：**findings 共 13：P0:1 P1:3 P2:9｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（13／13，含标「可选」的 F-12）**，已全数进本版：

  - **P0（1）**＝§一 与 §四 把模板节数写作「七节」，而同一句自己枚举到「八 审查记录」（模板区间实为 **8 个** `^##` 行首标题）→ 两处改「**八节**」。**值得记一笔**：这正是本批所修工具（065 批的规格静态自检器）专治的「计数错」类缺陷，且 065 批刚把该检查接进 `check.sh` [4] 段——工具在，人仍会犯，机制的价值即在送审前可见。
  - **P1（3）**＝① F2 句中「删除原句末重复的『（不设时间触发）』」**前提不成立**（该串全件仅 1 次、非重复；删之即改既有规则语义且无断言兜住）→ 改为「该条其余文字（含该限定语）原样保留」，并补判据（断言 3 末项 `grep -c "不设时间触发"` ≥1）与走查（断言 6）；② §八 开放项 2 称与本批无交集的那批条目（059–063 余额 14 条）**按 F2 新判据并不全无交集**——061-6 的落点即 `施工机制.md` §八 模板 → 补核销 061-6 一行（仍待，触发＝交底／核验请求模板下次修订，未到），并把其余条目的不重列写成「范围注」（其状态由 064 §三 核销表承载，避免两份并行口径）；③ §一② 称「治理面『核销』命中仅 `:74` 与归档 064」**不实**（另有 `施工机制.md`:154 与 `CHANGELOG.md`:12）→ 改述为准确三处。
  - **P2（9）**＝断言 2 的基线与其命令不同源（改用同命令的实跑 0 行 ＋ 现模板两个标题行号）、F1 描述含「或写『不适用』」与逐字文本不一致（删）、汇总句式与 §三「视需要」的张力（模板内加「恒列五档、无该档计 0」）、表骨架为散文列名（改真表头 ＋ 分隔行）、依据栏「026」在本仓无对应件且「债①②③④」与 §一 表头命名不一（改「§一①②③④（事实）」与「§一 因果链」）、断言 4 的「未预验」与实得 17 自相矛盾（改「初稿行数实测 17」）、断言 1–3 只核 token 致删句仍全绿（补三项判据并给 F3 写「其余原样保留」）、`:69` 落点清单未含核销节（并入 F2 ③）、断言 7 无现成命令（补 `python -c` 一行式，口径写死为「已跟踪 ＋ 未跟踪 `.md`，排除 `http`／`#`／`mailto`」）。
  - **已实核通过项（摘要）**：`:74` 机制全文与规格引文一致；065 规格「核销」＝0；064 核销表落 `:44`（§三）；`规格写作标准.md`:91 原文与其引述一致；064 §七 6 条 ＋ 065 §七 11 条 ＝ 17 条逐条比对**无漏无重**；断言 1／3／5 基线 0；断言 4 实得 17；断言 6 实得 0／「skills 未创建（符合）」；断言 7 实测 168 条／0 断链、`check.sh` 退出 0；断言 2 的误命中风险已排除（该件自身 `## 七、提交与登记`／`## 八、模板（…）` 不含目标字样）；模板加节不使别处对模板的说法失实（全仓枚举模板节序的只有本规格自身）。
  - **修订后静态复检**：`check.sh` 退出 0（[4] 段对本规格实检：C 检查把 F3 的目标文本报为 `[A] 未落实或已漂移`——改动未施工，属预期真实阳性；A 的三条 token 落「跳过」，因目标件未出现在 `[A]` 行，属既有设计）。

- **施工期停摆申报（P0，已裁决）**：施工方实测断言 4 得 **18**，而规格三处仍写 17（断言 4 的节标题与期望值、§一⑤、§四.2），按红线 4 **未改数字、如实申报**并继续完成 F1–F3。**归因＝规划方**：采纳成稿审查 F-3 补入 061-6 那一行时，只同步了 §七 的表与汇总句，漏改上述三处。**处置**：三处一律改 **18**（与表、汇总句一致），表与汇总句不动（即施工方给出的①方案；②方案「删 061-6 行」违背 F-3 的采纳内容，不取）。

- **P1-1（引号形态，采纳＋裁定改文件）**：F2②／F3 的目标文本原以『』书写（源自我在规格里为规避「」嵌套），而 `文档写作标准` §一.2.6 明定「**节名与需逐字复现的内容用「」**」。裁定：`施工机制.md`:74 与 `规格写作标准.md`:91 两处的 `『本批核销』`／`『开放项』` **改回「」**；同时规格 F2／F3 两行的目标文本改写为**反引号代码跨度**形态（消嵌套），check C 的核对面随之落在「本批核销」「开放项」两词——二者在施工后均实存，故不产生误报。**此裁定同步了「规格内引用形态」与「落进文件的目标形态」的分野**：规格行可用代码跨度承载逐字串，无需为了喂给工具而把非标准引号写进正式文件。

- **P1-2（F2① 的落位形态，采纳施工方实现）**：原加粗整句保留、仅末词「相关」→「有文件交集」，判据词表以独立括注紧随，形成相邻两括注（「（同改一件标准、同动一主题或同涉一产物）」「（**不设时间触发**）」）。**不改**：拆原 `**…**` 会破加粗配对（检查 D 与 lint 均以成对标记为前提），且「（不设时间触发）」按 F2 要求原样保留。

- **施工侧已实核通过项（规划方复核 diff 与实跑）**：F1 插入块与规格 §二 逐字文本**逐行比对一致**（施工方以 `python` 比对自证「True」，规划方复核 diff 无改字）、代码围栏未破损、节序实测 `:199 ## 七、本批核销（相关归档规格的开放项）`／`:211 ## 八、开放项`／`:215 ## 九、审查记录` 递增；F2③ 与 F3 落点句就位；断言 1＝**3**／断言 2＝恰三行递增／断言 3＝**2／1／1／1**／断言 5＝**1**／断言 6＝**0** 且 `skills` 未创建／断言 7＝**168 条 0 断链**、`check.sh` **rc=0**、markdownlint 0 issue；[4] 段对 F3 目标文本的判读由施工前「`[A]` 未落实或已漂移」转为施工后**消失**（前后对照即该判据的实跑实证）；`git diff` 的 hunk 仅落 §三 `:69`／`:74` 与 §八 模板区间。

- **修订轮停摆申报（P0，已裁决）**＝断言 6「`scripts/` 禁改面 0 改动」与 F4（本批即改 `check_spec.py` 单点）**互斥**——施工方按红线 4 未改断言、如实申报并给出改法二选一。**归因＝规划方**（同一成因复现**第二次**：断言未随规格修订同步，与上轮断言 4 同型）。**处置**：采纳其①方案——断言 6 改写为「`archive`／`product` 0 改动 ＋ `scripts` 恰一行 `M scripts/check_spec.py` ＋ diff 的 hunk **数为 3 且全部落在 `_target_blocks()`**」，并明确「hunk 数 ≥1」与「全落本函数」两项**须成对看**（单看后者在施工前平凡为 0，无判别力）。

- **P1-a（§一⑥ 实测口径，采纳更正）**＝原记「对本批规格实跑即崩」在**当前**规格状态下不成立（空引号对措辞已在修订轮改写为「直角引号」）；崩溃的稳定载体是**夹具**。§一⑥ 已按事实重写为三段：触发链（含规划方现场观测）／夹具复现／当前规格不复现，并附记「围栏内示例表不进解析面」这一实测结论。

- **P1-b（规划方交底表述更正）**＝规划方在交底中称「你在自核验里应该也看到了 [4] 段崩溃」，而施工方两轮实跑均为 rc=0（其时规格尚无空引号对）——**规划方表述有误**，据实留痕；不影响 F4 的出身（崩溃由规划方在规格改写后现场观测，事后由夹具稳定复现）。

- **P2-a／P2-b（采纳施工方记录）**＝[4] 段的「自噬预警：本批核销 @ `规格写作标准.md`」属「**未计删除**」的既有加法口径（人工核对：到位期望 1／实测 1，**达成**），非缺陷、非阻断；其验证副产物 `scripts/__pycache__/` 已删（本在 `.gitignore`）。

- **双维产物审查分身**（fresh、只读；时点＝核验第 1／2 步后）：**findings 共 13：P0:0 P1:4 P2:9｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（13／13）**：

  - **P1 四条**：**F-01**＝核销表的「重列／不重列」细则缺失（本批首跑自行取「不重列」，与新判据成两套准入）→ 模板新增「**核销面**」bullet（必列＝前一批全部开放项 ＋ 本批触及或可能变状态的条目（含前批应销未销）；不必重列＝已由既往核销表承载且状态未变者，只给索引指针），F2 判据措辞同步改「**有交集（同一文件／同一主题／同一产物）**」（原括注「同改一件标准…」与正文「文件交集」不等价，F-07 同处）；**F-02／F-05**＝**A 侧 `_quoted_blocks()` 同型取值未修**——空引号对使 `_row_target_text()` 得空串而不再回落改动单元格，**真实预警被吞、改显误导性中性行**（假绿家族）→ F4 扩为**两处同一口径**（A／C 对空引号对行为一致），并新增**断言 10**（孪生夹具：仅差一处空引号对，`自噬预警: Abzu` 行须逐字相同）；**F-03**＝§四「未预验项」仍列断言 4（与 §三 的自纠矛盾，上轮修复不完整）→ 删；**F-04**＝§一 表格**断裂**（⑥ 行被空行切出表外、渲染成含字面管道的段落；lint 与 [4] 段均抓不到，由审查以 markdown-it 实体渲染证出）→ ⑥ 并回表内。**本条值得记**：这是「人写 Markdown 的结构缺陷，机器闸看不见」的实例，也说明产物审查的渲染核不可省。
  - **P2 九条**：F-06（同类落点枚举三处只同步一处 → 新增 **F5**：`施工机制` `:153` 与 `AGENTS.md` `:52` 各补「本批核销（相关时）」）、F-08（§九 对「check C 未报」的归因不成立——真因是核对面 4／3 字低于工具 **8 字噪声门**，与实存与否无关 → 已改述）、F-09（断言 8 的命令写在散文行内，[4] 段把它当断言、末 token 取成说明文字 → 断言 8 改为独立围栏命令）、F-10（同句档名引号『』／「」混用 → 随本批引号裁定一并归一，见 F2 行）、F-11（模板汇总句式与两处实跑写法不一 → §七 汇总句改模板句式）、F-12（断言 1 括注称两处、实为三处 → 已改正）、F-13（断言 4 的取数不跳围栏，与 §一⑥ 附记的普适表述相冲 → 断言 4 补口径注、§一⑥ 附记限定为「对 [4] 段工具成立」）、F-07（并入 P1 栏）。
  - **已实核通过项**：F1 插入块与规格逐字文本**逐行相等**；模板四条 bullet 自洽、表骨架可直套、汇总张力已消解；F4 真治因且**未误伤合法输入**（夹具 rc=0、恰 1 条未落实；`『abc』`／`『』` 两形态实测正常）；与 `065` §二 C③ 所载判据一致；规格 066 的 F1–F4 与自由度、断言 1–9、核销表 18 行与汇总逐格相符；核销表抽 5 行（064-1／064-2／065-5／065-8／061-6）与归档原文相符；引号合规（全仓 `『本批核销』` 残留 0）；`AGENTS.md` §四 不做清单与 `施工机制` §七 归档件纪律均与本批相容（未配机器闸）。

- **第三轮（施工方停摆申报，三条，已裁决）**：本轮修订（F1 更新／F2① 判据措辞与引号归一／F4 扩为两处／F5 两处）落地后，施工方逐条重跑断言并报**三条红——全部是规划方的断言未随正文同步**（按红线 4 未改数字、未动正文）：**P0-1**＝断言 3 首 token「有文件交集」随 F2 判据改措辞而失效 → token 改「有交集」并补「同一文件／同一主题／同一产物」一项；**P0-2**＝断言 4 的取数被本轮 F2 行（含档名枚举的表格行）多计 1（实测 19≠18）→ 一行式判据改为「来源列形如条目号」的行，仍取 18；**P0-3**＝F4 扩为两处函数后断言 6 的 hunk 期望（3／0）与模式（只认 `_target_blocks`）不相容 → 期望改 **4／0**、模式补 `_quoted_blocks`。三条改毕由规划方独立复跑通过（`有交集`＝2／判据括注＝2／`理由／落点`＝1／整节省略＝1／`不设时间触发`＝1；断言 4＝18；断言 6 ③＝4、④＝0；断言 1＝4）。**并由此立 §八 开放项 5**：三轮共 **5 例**同族瑕疵（正文改了、断言未同步），且本批三例的目标件均属 `[B]` 项 → [4] 段 token 门落「跳过」、**工具兜不住**——建议作下批首要候选。

- **收尾**：`CHANGELOG` 条目按产物审查 P2-4 同步（判据措辞与「10 条断言」）；规格归档后 `check.sh` [4] 段如实打印「无在制规格，跳过」（本机制的首次空态实跑）；`.tmp/066/` 三件夹具（`crash.md`／`probe-a.md`／`probe-b.md`）已按红线 2 清理。**归档顺序留痕**：规划方首度归档时 §九 尚未回填第三轮记录，随即移回补齐记录再行归档（批次未提交、归档态无人消费，属同批内纠正；记为一次「过程产物未齐即归档」的失手）。
