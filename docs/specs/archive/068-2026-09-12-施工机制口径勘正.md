# 068-2026-09-12-施工机制口径勘正

> 定位：落一次对 `施工机制.md` 的**审读 findings**（作者转来，P0:0／P1:5／P2:4 ＋ 三条上版未收口项——**其中两条与前者同题**（轻路径条件＝P1-1、外部窗口限定＝P2-2），去重后 **10 项**；规划方逐条对原文实核，**10 项全部成立**）——清掉三批改动（065 核销机制／066 核销入模板／067 自核验四条）留下的口径冲突与表述不完整。本批是**改规则**：单文件、但触及判据口径与分工，故走标准路径（非「轻」）。
> 自由度分布：[A]×1 [B]×10
> 必读输入：`docs/specs/施工机制.md`（本文，全部）；`docs/standards/规格写作标准.md` §1 判定表（分流判据来源）；`scripts/check.sh` 头注（段序真源）；`docs/specs/archive/066-*.md` §八／`archive/067-*.md` §八（核销与对账的出身）；`AGENTS.md`
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

审读 findings 逐条对原文实核（行号＝现文）：**本节的计数与命令不重复列出——各项的实测与预验基线见 §三**（本件 §一 只记「现文 → 判读」）。

| # | 现文 | 判读 |
| --- | --- | --- |
| ① | `:39` 分流表「轻」行＝「单文件且 < 5 行纯措辞／格式」；`:43` 判据来源却含「复盘驱动的治理微调 **≤ 3 文件**（另需作者会话内逐项确认）」 | **口径不一致**：分流表窄于判据来源——按表定路径会误判为「标准」 |
| ② | `:63` 核验第 3 步＝「重路径派产物审查分身，**标准路径由规划方通读抽查**」；`:92` 产物审查行＝「**改规则／结构／契约**的批次（含重路径与动标准条文的批次）」须派产物审查 | **冲突**：标准路径中改规则的批次（如 066／067，实际均已按 `:92` 派分身）按 `:63` 却只由规划方抽查 |
| ③ | `:92` 只写产品层纯文档内容批「改派**单维核对**」，**未写执行主体** | **判据悬空**：`:87` 定「评审者与创作者必须为不同上下文」，若由规划方自查即与之相悖 |
| ④ | `:74` 核销机制＝「逐条标「已履行／仍待／作废」（含理由；另可标「本批处置／待作者」两档，**视需要**）」；`:203`／`:209` 模板＝「状态取**五档**之一」「**恒列五档**」 | **档位口径三处不一**（机制条 vs 写法条 vs 汇总条） |
| ⑤ | `:199`–`:210` 模板「本批核销」节：围栏内混入「触发／核销面／写法／落点纪律」四条**元说明** | **模板不可逐字复用**：照录会把说明写进规格正文（其余节次无此形态） |
| ⑥ | `:24` 作者＝「规格确认、提交确认**两处出手**，重大分歧拍板」 | **不完整**：`§二` 轻行另需作者会话内逐项确认 |
| ⑦ | `:28` 分身纪律＝「不退回作者粘贴中转、**不经外部窗口**」；`:250` 外部窗口备选形态仍有效 | **张力**：需限定「默认流程」 |
| ⑧ | `:154` 归档件纪律＝「**只读**（**仅允许**作废头注；…）」 | 措辞自相矛盾（只读 vs 例外未分层） |
| ⑨ | `:76` 自核验四条＝「（**[4] 段**能报的由工具报…）」 | **缺上下文**：不熟 `check.sh` 段序者不可解 |
| ⑩ | `:61` 核验第 1 步＝「untracked 且未忽略的新文件须对应规格预期新增」 | **判据留白**：清单级对应可判（`git status --short` 即可），但未跟踪新文件的**内容**无法 diff 核对——「逐 hunk 对照」对它不适用 |

**不变量（施工前后须同）**：`docs/specs/archive/`、`docs/product/`、`scripts/`（含 `check.sh`）全部零改动；本件除下表点名的行外零改动（§一 角色三条与分身纪律其余条、§二 分流表另两行与三要素、§四 全节、§五 止损三条与立例依据、§六 全节、§七 前五条、§八 另两份模板与交底六项）。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md` `:39`（分流表「轻」行） | 触发条件补全为「**单文件且 < 5 行纯措辞／格式；或复盘驱动的治理微调 ≤ 3 文件**（后者豁免条件缺一不可、另需作者会话内逐项确认、**当批 `CHANGELOG` 留痕不计入**；**与其他行命中时以本行为准——具体优于一般**）」——与 `:43` 判据来源及 `规格写作标准` §1 判定表原文一致；**并在 `:43` 判据来源末补一句**「（轻行细则以 `规格写作标准` §1 判定表原文为准——含『`CHANGELOG` 留痕不计入』与『与其他行命中时以本行为准』）」 | §一① | [B] |
| F2 | `docs/specs/施工机制.md` `:63`（核验第 3 步） | 改述为按 `§五` 分工：**触发产物审查条件的批次（改规则／结构／契约，含重路径与动标准条文的批次）派产物审查分身**；产品层纯文档内容批走**单维核对**；**其余（含轻路径）由规划方通读抽查** | §一② | [B] |
| F3 | `docs/specs/施工机制.md` `:92`（产物审查行·「改派单维核对」处） | 补执行主体：「**单维核对由独立的 fresh 只读分身执行**——判据同 §五 判据核心句（评审者与创作者必须为不同上下文）；覆盖面见本行既列三面；并引本条「分身不复用」条——**不写行号**（本件此前零行号引用，写 `:NN` 会随插入漂移；与 061-6「对外产物不写行号、改符号锚点」同向））」；**并删**同格紧随其后、与上述覆盖面重复的那句「只查引用闭合、数字一致与既有条文冲突」 | §一③ | [B] |
| F4 | `docs/specs/施工机制.md` `:74`（核销机制条） | 「逐条标「已履行／仍待／作废」（含理由；另可标「本批处置／待作者」两档，视需要）」→「逐条标五档之一（已履行／作废／本批处置／仍待／待作者，含理由）」——与模板 `:203`／`:209` 口径统一 | §一④ | [A] |
| F5 | `docs/specs/施工机制.md` `:199`–`:210`（模板「本批核销」节） | 围栏内**只留**四行：节标题、表头、分隔行、汇总占位行（`- 汇总：按条计 n 条：已履行 a｜作废 b｜本批处置 c｜仍待 d｜待作者 e ＝ n`）；**移出围栏**者六项——「触发」「核销面」「写法」「落点纪律」四条 ＋ `:204`「表（列名固定，末附汇总）：」＋ `:209` 汇总行的尾注「**恒列五档、无该档计 0**，且逐格可复核、不得与表列不符」（尾注并入说明段、措辞原样）。说明段的**位置与层级钉死**：紧随 `:221`（规格模板头注四行）之后、参考研究裁定表之前；标题用 `###`（**不得用 `##`**——否则新增顶级节、与 `:9` 目录及全仓对「§八 模板」的引用冲突）。**文字原样保留**；唯二例外：① `:204` 的「表（列名固定，末附汇总）：」在说明段内**补成完整句**（「表：列名固定为『来源｜条目｜状态｜理由／落点』，末附汇总行」——原为悬空引导项）；② 汇总行尾注**加引导词**「汇总行尾注：」以便成条（**尾注本体一字不改**）。两处均在规格中留痕（§九） | §一⑤ | [B] |
| F6 | `docs/specs/施工机制.md` `:24`（作者条） | 改述为「**规格确认、提交确认两处常规出手**；另有**治理微调的会话内逐项确认**（`§二` 轻行）与**重大分歧裁决**（`§六`）两类例外」 | §一⑥ | [B] |
| F7 | `docs/specs/施工机制.md` `:28`（分身纪律首条） | 「不经外部窗口」前加限定：「默认流程不经外部窗口；**作者主动选择 `§八` 外部窗口备选形态时除外**」 | §一⑦ | [B] |
| F8 | `docs/specs/施工机制.md` `:154`（归档件纪律） | 改述为「**原则只读**；**唯一例外是移入时的作废头注**，归档后不再修改」＋原有「作废批移入即带 `-已作废` 后缀、归档后不再重命名」与「跨批检索以核销表为唯一索引落点」两句原样保留 | §一⑧ | [B] |
| F9 | `docs/specs/施工机制.md` `:76`（自核验四条） | 「（`[4]` 段能报的由工具报…）」→「（**能机械化者由 `check.sh` 的规格静态自检段报——段序真源见该脚本头注**；报不到者以此条兜）」 | §一⑨ | [B] |
| F10 | `docs/specs/施工机制.md` `:61`（核验第 1 步末） | 补可执行命令：「untracked 且未忽略的新文件须对应规格预期新增（首选 `git diff --no-index /dev/null <文件>`——不需写索引；次选 `git add -N <文件>` 后 `git diff`。`--no-index` 有差异时**退出码＝1 属预期**（勿按失败读）；`git add -N` 是索引写操作，只由规划方在核验期执行、**不得由施工方执行**（本条粗体共**两处**，余处不加粗——`文档写作标准` §一.3.8 的定性规则）），否则仍 P0」 | §一⑩ ＋ 上版未收口项 | [B] |
| F11 | `CHANGELOG.md` | 本批一行（≤400 字符），**以「施工机制口径勘正（068）：…」起首**（该 token 即断言 8 的判据，二者绑定） | 过程产物两项 | [B] |

## 三、验收断言（含预验记录）

### 断言 1｜轻路径触发条件补全（两处一致）

```bash
grep -c "复盘驱动的治理微调" docs/specs/施工机制.md
```

期望 **≥2**（`:39` 分流表 ＋ `:43` 判据来源各一）；预验基线 **1**。

### 断言 2｜核验第 3 步与 §五 口径统一

```bash
grep -c "由规划方通读抽查" docs/specs/施工机制.md; grep -c "派产物审查分身" docs/specs/施工机制.md
```

期望：第一项 **≥1**（F2 改述后仍保留「其余（含轻路径）由规划方通读抽查」一支）；第二项 **≥2**（`:63` 改述后**仍含**该串——现文即有、非新增；另一处在 `:101` 止损条。**`grep -c` 计行数**，故不得按「新增一处」预期 ≥3）。预验基线 **1／2**。

### 断言 3｜单维核对执行主体在位

```bash
grep -c "单维核对由独立的 fresh 只读分身执行" docs/specs/施工机制.md
```

期望 **1**；预验基线 0。

### 断言 4｜核销档位口径统一为五档

```bash
grep -c "逐条标五档之一" docs/specs/施工机制.md; grep -c "两档，视需要" docs/specs/施工机制.md
```

期望 **1**／**0**；预验基线 0／1。

### 断言 5｜模板围栏内无元说明、说明段在围栏外

```bash
grep -c "规格模板「本批核销」节的口径" docs/specs/施工机制.md
python -c "import io,re;s=io.open('docs/specs/施工机制.md',encoding='utf-8').read();b=re.findall(r'\`\`\`markdown\n(.*?)\n\`\`\`',s,re.S)[0];print(sum(1 for k in ('触发：','核销面：','写法：','落点纪律：') if k in b))"
```

期望第一项 **1**；第二项 **0**（四条元说明已不在规格模板围栏内）；预验基线 0／**4**。

### 断言 6｜作者出手点、外部窗口限定、归档纪律、[4] 段上下文、untracked 命令五处到位

```bash
grep -c "两处常规出手" docs/specs/施工机制.md; grep -c "作者主动选择" docs/specs/施工机制.md; grep -c "唯一例外是移入时的作废头注" docs/specs/施工机制.md; grep -c "段序真源见该脚本头注" docs/specs/施工机制.md; grep -c "git add -N" docs/specs/施工机制.md
```

期望依次各 **≥1**；预验基线 0／0／0／0／0。

### 断言 7｜禁改面零改动

```bash
git status --porcelain docs/specs/archive docs/product scripts | wc -l
git diff -U0 docs/specs/施工机制.md | grep -c "^@@"
```

期望：第一项 **0**；第二项 **≤ 13**（可判定上界：**10 处单行替换**（`:24`／`:28`／`:39`／`:43`／`:61`／`:63`／`:74`／`:76`／`:92`／`:154`）＋ F5 的删除 2 处 ＋ 插入 1 处 ＝ 13；施工实测 13），并**逐 hunk 走查归属**——每个 hunk 须落 §二 某个 F 项，无表外行（走查项，通过判据＝无表外行）。预验基线 0／0。

### 断言 8｜过程产物两项与链接

```bash
grep -c "施工机制口径勘正（068）" CHANGELOG.md
```

期望 ≥1；另核该行 ≤400 字符：

```bash
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '施工机制口径勘正（068）' in l][0].rstrip('\n')))"
```；全仓相对链接条数与断链＝基线（**168 条／0 断链**）：

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split('\\0');f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md')];L=[(p,u) for p in f for u in re.findall(r'\\]\\(([^)\\s]+?)\\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print('链接',len(L),'断链',sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

`bash scripts/check.sh` 退出 0；验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 十条逐条对现文实核（`:24`／`:28`／`:39`／`:43`／`:61`／`:63`／`:74`／`:76`／`:92`／`:154`／`:199`–`:210`），含三条上版未收口项（轻路径条件、untracked 核对、外部窗口限定——前两条与 P1-1／P2-2 **同题**，去重后**本表实列 10 项**）；**10 项全部成立，无一条误报**。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1／2／3／4／5／6／7／8 的基线已逐条实跑——`复盘驱动的治理微调` 1、`由规划方通读抽查` 1、`派产物审查分身` **2**、`单维核对由独立的 fresh 只读分身执行` 0、`逐条标五档之一` 0、`两档，视需要` 1、模板围栏内元说明 **4**、后五处 token 各 0、禁改面 0、链接 168／0、`check.sh` 退出 0。**无未预验项**（断言 7 的 hunk 上界与归属走查属施工后实核项——**本批实测 13，与算式相符**）。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 全部（只读）、`docs/product/`、`skills/`（未创建）、`scripts/` 全部（本批不动工具与守卫）、本批规格自身。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；**不得顺带改本批未点名的条文**（§一 不变量列了范围）——尤其**不得**改动 §五 止损三条、§六 反模式清单、§八 另两份模板。
- 禁止越权：不改版本号与 tag；不新增文件（本批只改**两件**既有文件：`施工机制.md`／`CHANGELOG.md`）；**不建台账／不建索引件**。
- 禁止改断言、改数字、删检查来变绿；**不得**借本批给 §一 增补新纪律（口径勘正 ≠ 加规则）。

## 六、被否决的替代方案（可选）

- **按「轻路径」直接改**（单文件、复盘驱动）→ 否决：本批改的是**判据口径与分工**（谁派分身、档位取几档），不是「≤ 3 文件的治理微调」那种措辞级调整；走轻路径会跳过成稿审查与产物审查，与 §五「无裁量：标准与重路径一律送审」冲突。
- **只改 P1、P2 留待下批** → 否决：P2 四条中三条（作者出手点、外部窗口限定、归档纪律措辞）同样在定义**谁能做什么**，留着就是第二处口径冲突源；且全部改动同在一件、同一批清完成本最低。
- **借本批把 `:63` 的「抽查」整个删掉**（改为全部派分身）→ 否决：轻路径与产品层单维核对之外尚有无分身的场景，删掉「其余由规划方通读抽查」会让那类批次无落点；保留一支并写明触发面即可。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 066-1 | 核销执行仍属人判（无机器闸） | 仍待 | 本批只统一**档位口径**（F4），未机械化；触发＝「相关却漏写核销节」复发 |
| 066-2 | 059–063 余额 14 条的索引落点 | 仍待 | 该表（064 §三）仍为跨批检索索引；本批与其主题无交集 |
| 066-3 | 工具健壮性的成体系排查 | 仍待 | 本批不动 `scripts/`；触发＝再出现同类崩溃 |
| 066-4 | `docs/specs/施工机制.md` `:102` 的 `『』` 归一 | **已履行** | 067 F3② 已落地（实测 `grep -c "『同类精度"` ＝ 0） |
| 066-5 | 修订轮的系统性对账缺口 | **已履行** | 067 F1／F2／F3 三面落地（核对面扩至 `[B]` 行 ＋ 零命中待复核 ＋ 自核验四条） |
| 066-6 | 「相关」判据是否过窄 | 仍待 | 067 已新增一次核销实践（其漏项 065-8 经产物审查补入）为其样本；再攒一批后校准 |
| 067-1 | `[B]` 面纳入后输出量上升（评估分档输出） | 仍待 | 本批不动工具；触发＝再两批后仍觉噪声压过信息 |
| 067-2 | F2 判据只认字面 token（漏报面） | 仍待 | 已记 `测试与验收标准` §4 **G14**；触发＝觉漏报过度时评估「按载体形态分流」 |
| 067-3 | F2 聚合期望句不判（分配型漏报面） | 仍待 | 已记 §4 **G15**；触发＝同类误报再现时改「按命令序号映射」 |
| 067-4 | 零命中判据只认 `期望 ≥N` 写法 | 仍待 | 本批不扩；触发＝出现漏报后按类扩 |
| 067-5 | 工具仍无「断言与正文的语义对账」能力 | 仍待 | 该层交人工与两个审查位；本批不动工具 |
| 061-6 | 对外／跨会话产物不写行号、改符号锚点 | 仍待 | 落点即 `施工机制.md` §八（交底／核验请求模板）；本批改动正落在同节（改规格模板并新增 §八 子节）→ 按 066 §七「有文件交集即纳入」的先例**必列**；本批未修订交底／核验请求两模板，触发未到 |
| 065-11 | 检查 A 与 C 的「目标文本」两口径并存 | 仍待 | 067 §七 记「评估时点顺延下批」＝本批；本批不动 `scripts/`、未触及「全部引号块 vs 末标记之后」的口径分歧 → 顺延下批（与 065-5 同型时点条目，故必列而非只给索引指针） |

**汇总**：按条计 **13 条**：已履行 2｜作废 0｜本批处置 0｜仍待 11｜待作者 0 ＝ 13 ✓（逐格可复核）。**范围注**：066 §八 六条与 067 §八 五条**全部列入**——本批改动面正落在 066／067 所改的同一批条文（核销机制条、规格模板、自核验条），按「同一文件／同一主题」判据属**必列**（非「已由既往核销表承载且未变」的情形）。

## 八、开放项

1. `:63` 改述后，「触发产物审查」的判定仍靠规划方**读 §五**（改规则／结构／契约）——本批不加机器闸（`AGENTS.md` §四 注册表类守卫不做）；若再出现「该派未派」，按事故出身评估机械化。
2. 审读 findings 里未点、但我实核时注意到的一处**同类风险**：§八 其余两份模板（参考研究裁定表／核验请求模板）仍带少量括注式说明，本次未动（超本批范围）；若后续再报「模板混说明」，按同一改法一次性清。
3. **改动清单路径列的写法**（本批核验时实证）：本规格 F2–F10 的路径列原写「同件」，`check_spec.py` 的检查 C 因此**解析不到目标件**、只能落「无核对面」计数（并不误报）；路径列写全路径后 C 才真核逐字。**前提须写对**（本条初稿的前提经产物审查 F-02 更正）：路径列改**全路径**（须是**仓根可达**，如 `docs/specs/施工机制.md`；只写文件名不解析）后 C 才真核逐字；本批初稿把路径列写成文件名形式，C 仍落「无核对面」——**故「C 未报 `[A]` 未落实」当时并不等于逐字落地（假绿读法）**，此即本条的实测反例。**规格写法建议**（本批未立法）：改动清单每行的路径列写**仓根可达的完整路径**，不用「同件」「同上」这类指代——否则 `[A]` 逐字核对与「零命中待复核」都失去核对面。落点候选：`规格写作标准` **§2.2（改动清单）** 或 **§5.8（计数式断言条旁的写法条）**；待下批评估（立法时依 §8 附本批为反例）。
4. untracked 新文件的 `git add -N` 法会给索引留 intent-to-add 条目——核验收尾须确认未误提交（本批 F10 只写做法，未加守卫）。

## 九、审查记录

- 成稿审查分身（fresh、只读）首轮：**findings 共 14：P0:1 P1:5 P2:8｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（14／14）**：

  - **001（P0）**＝断言 2 第二项期望 **≥3 不可达**——`grep -n "派产物审查分身"` 实得 **2 行**（`:63`／`:101`），F2 是对 `:63` 的**改述**（改后仍含该串）、F3 在 `:92` 插的是另一串，故改后恒为 2；其理由里「`:92` 既有」为假，且 §四.2 的基线写 1 亦错（实测 2）→ 期望改 **≥2**、理由改述、§四.2 改 2。**根因记一笔**：我又犯了「不点数就写计数」——正是 067 批刚机械化的那一类。
  - **P1 五条**：**002／009**（F5 未交代 `:204` 行去向、未钉新段层级——写成 `##` 会新增顶级节并与目录／全仓「§八 模板」引用冲突；`:209` 汇总行的尾注同属元说明却留在围栏内）→ F5 重写：围栏内只留四行、移出六项、位置钉死「紧随 `:221` 之后」、层级钉死 `###`；**003**（F11 标 `[A]` 却无逐字文本，[4] 段首跑即自报「建议补引号块」）→ 改 `[B]` 并把 token 与断言 8 绑定；**004**（断言 7 的「相称（约 9–11）」不可判定，审查模拟实得 **12** 个 hunk）→ 改为「≤ 12 ＋ 逐 hunk 归属某 F 项」的可判定式，断言 8 补字符数命令；**005**（§一 实列 10 项而头注称 12 条，规格内无法逐项对上）→ 头注与 §四.2 改述为「去重后 10 项」；**006**（核销表漏 **065-11**——其「评估时点顺延下批」正落在本批，与 065-5 同型）→ 补一行，汇总 11 → **12**。
  - **P2 八条**：007（§一⑨ 引述给 `[4]` 多加了反引号，现文是加粗）、008（F1 未同步 §1 判定表的另两条子规则「`CHANGELOG` 留痕不计入」「与其他行命中时以本行为准」）、010（F10 补 `--no-index` 退出码＝1 属预期、`git add -N` 是索引写操作只由规划方在核验期执行、首选 `--no-index`）、011（F3 的「判据同本条」指代错位——该句在 `:87` 不在 `:92`；覆盖面与行内既有文字重复）、012（§一 缺「实测命令／计数」列 → 加前言声明「计数与命令见 §三，本节不重复」）、013（§一⑩ 判读偏强——清单级对应可判，真缺口是**内容**不可 diff）、014（软判据收口，已并入 004）→ 全部采纳。
  - **已实核通过项（摘要）**：§一 十条锚点逐字一致（除 007 的 markup）；唯一 ```markdown 块 = `:160`–`:219`（核验请求模板的是非 `markdown` 围栏，不干扰解析）；围栏内元说明实测 **4**；断言 4／6 的 token 落点经「模拟改后」实跑**均落在 F4／F6–F10 的目标文本内**（不互斥）；F4 的五个档名与 `:203`／`:209` 字面完全一致；F8 与原两句及 `命名标准` §2 作废条、`施工机制` `:100` 止损条**不矛盾**；F9 的「段序真源见该脚本头注」与 `check.sh`:2／:10／:35 实文相符；核销表 066 §八 六条与 067 §八 五条**全部列入且逐条对得上**，066-4／066-5 的「已履行」有据（`grep -c "『同类精度"` ＝ 0；067 F1／F2／F3 三面在位）；跨件无并行版本，F1–F10 无需同步他件；链接 168／0、`check.sh` rc=0、本规格 lint 0 issue。
  - **未验证（如实声明）**：F5 移出后的新段若用 `###`，lint 与目录是否需同步——属施工后事项（审查为只读，不得试改仓库）；施工后 [4] 段对本批规格的输出亦待施工后实核。

- **双维产物审查分身**（fresh、只读；时点＝核验第 1／2 步后）：**findings 共 10：P0:0 P1:2 P2:8｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（10／10）**：

  - **F-01（P1）**＝头注自由度分布写 `[A]×3 [B]×7`，而 §二 实为 11 行 `[A]×1 [B]×10`（三处皆错，且与「10 项」的说法打架）→ 改 `[A]×1 [B]×10`（**本批主题的同族计数瑕疵，第三次由审查在规格自身上抓到**）。
  - **F-02（P1，本批最硬的一条）**＝**路径列写法我修得不彻底**：改成 `施工机制.md` 后仍非仓根可达（`_row_target_file()` 以仓根为基准解析）→ C 落「`· C：1 行无核对面`」，**F4（本批唯一 `[A]` 行）的逐字核对根本没跑** → 故我在核验里说的「无 `[A]` 未落实＝施工逐字落地」是**假绿读法**，审查另更正了我对「9 条候选」的归类（＝6 自噬 ＋ 2 中性 ＋ 1 无核对面）→ 十行路径列一律改为 `docs/specs/施工机制.md`；改后 C 真核 F4 逐字（该行五档文本实存，无未落实）。**教训已并入 §八 开放项 3 的实测反例。**
  - **P2 八条（全采纳）**：P2-1（`:43` 判据来源补细则指针）、P2-2（`:92` 同格删重复三面的那句）、P2-3（`:61` 粗体压到两处——该行由 0 对骤增 5 对，含 `**注意**` 一类非「红线／路径／数字／闸门」用法；本批新增粗体其余全部来自规格目标文本或 HEAD 既有）、P2-4（§八 开放项 3 的前提改述 ＋ 落点候选由 §3 更正为 **§2.2／§5.8**）、P2-5（说明段的「表（列名固定，末附汇总）：」补成完整句 ＋ 汇总尾注加引导词，两处例外留痕）、P2-6（F3 并引 `:94`「分身不复用」——否则存在把本批成稿审查分身复用为单维核对的读法空间）、P2-7（核销表补 **061-6**——066 §七「有文件交集即纳入」的先例；汇总 12 → **13**）、P2-8（`:61` 删对反模式 #6 的引用——#6 是相反族「退出码被管道吞掉」，与「预期非零退出码」不同义）。
  - **已实核通过项（摘要）**：F5 结构四查全绿（围栏闭合且 `markdown` 块数＝1；移出六项文字 5 项逐字、第 6 项尾注本体逐字仅加引导词；`###` 落 `:217`、位于头注四行与参考研究裁定表之间、不进目录；照录可产出合法规格）；十处口径一致（F2 与 `:92`／`:96` 同三分、冲突已消且兜底无真空；F4 的档名在四处字面一致；F3 引用落点正确）；分项：F11 的 CHANGELOG 行与十处改动逐项对上、206 字符；**加粗密度判定＝兜底阈值「不适用」**（表格＋条目行占 79%，属 `文档写作标准` §一.3.8 的定性规则面），本批新增粗体无一处施工自加；引用面全仓核过（`AGENTS.md`／`README`／`docs/standards`／`docs/product` 对七个被改短语**零命中**；归档件属历史记录）；规格 068 自洽（断言 1–8 重跑全符、§七 表与汇总逐格相符、§八 编号连续、§九 数字与实测一致）；单维核对分身与「分身不复用」**相容**（单维核对＝产物审查位的降级形态）。
  - **未验证（如实声明）**：§九 首轮「成稿审查 14 条」等历史会话内容无仓内载体可复核（采信自述）。

- **审查修订轮（施工，规划方裁定）**：四处文档细化全部落地；施工方申报四条 P1，裁定如下——**P1-1（本批唯一断言不符）**＝断言 7 的 hunk 上界「≤12」是 F1 只改 `:39` 时定的，本轮 F1 按规格新增 `:43` 第二个改动点后实测 **13** → **采纳其①方案**：上界改 **≤13** 并同步算式（10 处单行替换 ＋ F5 删除 2 ＋ 插入 1）；**P1-2**＝`:92` 保留「不逐段通读」半句（与「覆盖面」一为深度一为范围，不同义）→ 采纳；**P1-3**＝F3 原写「本条 `:94`」为**行号锚点**，而本件此前零行号引用（写 `:NN` 会随插入漂移，与 061-6「对外产物不写行号、改符号锚点」相悖）→ 采纳入规格**改符号锚点**（「本条「分身不复用」条」）；**P1-4**＝两处补句加句末「。」合同节体例 → 采纳。

- 按止损条 2：产物审查 **P0＝0**，不触发强制复审；修订后由规划方复核 + 归档。
- 施工后核验与双维产物审查：待回填。
