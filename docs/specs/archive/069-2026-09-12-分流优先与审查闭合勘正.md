# 069-2026-09-12-分流优先与审查闭合勘正

> 定位：落**第二轮审读 findings**（作者转来，P0:0／P1:2／P2:6；规划方逐条对现文实核，**8 条全部成立**）——补两类边界限定（**分流冲突时的优先规则**、**单维核对的引用闭合**）与**五处**流程表述闭合（标准行流程缺产物审查节点、轻路径在核验三步里的歧义、静态自检段的实现状态、轻路径的提交自检四项、止损对称条款）。跨两件文件、改规则 → 标准路径。
> 自由度分布：[A]×2 [B]×7
> 必读输入：`docs/specs/施工机制.md`（§二 分流表／§三 核验与过程产物／§五 审查与止损／§七 提交与登记）；`docs/standards/规格写作标准.md` §1 判定表（**判据来源**，与本批 F1／F8 强联动）；`docs/standards/测试与验收标准.md` §1.1（[4] 段现状的唯一落点）；`docs/specs/archive/068-*.md`（上批口径勘正的出身）；`AGENTS.md`
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

| # | 现文 | 判读 |
| --- | --- | --- |
| ① | `施工机制.md`:39 轻行含「**与其他行命中时以本行为准——具体优于一般**」；`:43` 判据来源同句；`规格写作标准.md`:24 的 ❌ 行亦为「与其他行命中时以本行为准——具体优于一般」 | **可架空重路径**：某批若既是「复盘驱动的治理微调 ≤ 3 文件」又动 `skills/` 或改域内流程／规则／产物契约，按字面走轻路径、绕过产物审查与真机走查（与 `:35`「不确定属哪条时取较重的一条」相悖） |
| ② | `施工机制.md`:94「分身不复用：同一批的**成稿审查与产物审查**必须是两个独立 fresh 分身」；`:92` 已把「单维核对由独立 fresh 只读分身执行」写入 | **引用未闭合**：单维核对是产物审查位的降级形态，却未并入「分身不复用」条面 |
| ③ | `施工机制.md`:40 标准行流程列＝「勘验现状 → 写规格 → 独立审查（成稿）→ 施工 → 核验 → 请求提交」 | **流程列缺节点**：`§五` 要求「改规则／结构／契约」批派产物审查（在核验第 1／2 步之后），流程列未体现 |
| ④ | `施工机制.md`:63 核验第 3 步末支＝「**其余（含轻路径）**由规划方通读抽查」 | **歧义**：轻路径不写规格、流程表亦无核验节点，此处「含轻路径」易被读成轻路径也要走核验三步 |
| ⑤ | `施工机制.md`:76「能机械化者由 `check.sh` 的规格静态自检段报——段序真源见该脚本头注」 | 该段**已实现**（065 批落 [4] 段），但本句未标状态；读者可能误以为「待建」（本件 §四 已声明各段现状见 `测试与验收标准` §1／§4） |
| ⑥ | `施工机制.md`:101 对称条款＝「产物审查 findings 中 P0 ≥ 2 时，修订后重派产物审查分身复审一次」 | **未覆盖单维核对**（同一审查位的降级形态应同此处置） |
| ⑦ | `施工机制.md`:150「提交前自检四项：**改动未越出规格范围**；…」 | 轻路径无规格可依，首项对它不适用 |
| ⑧ | `规格写作标准.md`:24 的 ❌ 行与 `施工机制.md`:39／`:43` 是**同一判据的两处载体** | 本批改 ① 的优先规则时**两处必须同改**，否则两处判定表不一致（P2-2） |

**不变量（施工前后须同）**：`docs/specs/archive/`、`docs/product/`、`scripts/` 全部零改动；本两件除下表点名的行外零改动（§一 另两条与分身纪律其余条、§二 分流表另两行的**其余格**与三要素／自审三条／自由度、§三 过程产物两项与核销机制、§四 全节、§五 表内其余格与止损另两条与立例依据、§六 全节、§七 其余六条、§八 全节；`规格写作标准` 除 §1 ❌ 行外零改动）。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md` `:35`／`:39`／`:43`（分流表前导语／轻行／判据来源） | **三处**：① `:39` 优先级子句改述为「与其他行命中时——**若同时命中「重」行触发条件，以「重」为准**；否则以本行为准（具体优于一般）」；② `:35` 前导语末补澄清半句「（**同时命中两条 ≠ 「不确定属哪条」**——前者按「轻」行的优先规则定（「标准 × 重」由「重」行流程含标准路径判定），后者才取较重的一条）」——**不得写「按该行」**（另两行的单元格内均无优先规则子句，指代不落；审查 F-07）；③ `:43` 判据来源里的**截短引文**改为**纯指针、不再引规则原文**——写作「（轻行细则以 `规格写作标准` §1 判定表原文为准——含「`CHANGELOG` 留痕不计入」与冲突优先规则）」；理由：截短引文丢了「否则以本行为准」半句、只读 `:43` 者会以为规则是无条件重优先，且该引文已在 068／069 两批各产生一次过时同步成本——**载体由 3 降为 2**（`:39` 分流表须独立可用 ↔ `规格写作标准` §1；审查 F-02 的最小收敛方案） | §一① ＋ 与 `:35` 一致 ＋ 审查 002／013／F-02／F-07 | [B] |
| F2 | `docs/specs/施工机制.md` `:94`（分身不复用条） | 「成稿审查与产物审查必须是两个独立 fresh 分身」→「成稿审查与产物审查（含单维核对）必须是两个独立 fresh 分身」——**目标文本不带任何强调标记**（否则断言 2 的 token 与 check C 的逐字核对面互斥，审查 001 的 P0） | §一② | [A] |
| F3 | `docs/specs/施工机制.md` `:40`（分流表「标准」行·流程列） | 在「核验」与「请求提交」之间补「→ **（触发产物审查条件时）产物审查**」 | §一③ | [B] |
| F4 | `docs/specs/施工机制.md` `:63`（核验第 3 步末支） | 改述为「**其余批次（不含轻路径——轻路径不写规格、不走核验三步）由规划方通读抽查**」；**并在同条第一支末补「（轻路径批次除外——不派审查分身，改后由规划方自查）」**；**补句不加粗**（`[B]` 面实现选择：加粗会使断言 9 的连续串断裂，且与 F-08 立的「适用面指向不加粗」口径相冲——施工方 P1-1）；**并同步 §五 `:92` 的触发面**——该格现写「**改规则／结构／契约**的批次（**含重路径与动标准条文的批次**）」，与 `:63` 的就地排除相冲（`:63` 开篇即「按 §五 分工走」，把读者送回 `:92`）→ 在该括注的「动标准条文的批次」后补「；**轻路径批次除外**，见 §三 第 3 步」——F1 让轻行压标准行后，「轻路径 × 改标准条文」这类治理微调会落进第一支（「含…动标准条文的批次」）却无核验时点可绑（审查 008） | §一④ | [B] |
| F5 | `docs/specs/施工机制.md` `:76`（自核验四条·工具面括注） | **粗体段之后、分号之前**补「（各段现状见 `测试与验收标准` §1.1）」——**只给指针、不复述状态**（本件 §四 已定「本文件不复制段序列表」；写「已实现」会成 §1.1 现状表的第二载体，触反模式 #4 的边），位置钉死为「…段序真源见该脚本头注**（各段现状见 `测试与验收标准` §1.1）**；报不到者以此条兜」 | §一⑤ ＋ 审查 012／014 | [B] |
| F6 | `docs/specs/施工机制.md` `:101`（止损条 2·对称条款） | 末补「（单维核对同此）」——**不加粗**（该短语属「适用面指向」，不落 `文档写作标准` §一.3.8 的「红线／路径／数字／闸门」四类；审查 F-08 按 068 P2-3「`**注意**` 一类非四类用法」的先例判） | §一⑥ | [B] |
| F7 | `docs/specs/施工机制.md` `:150`（提交前自检四项） | 首项补括注：「改动未越出规格范围（**轻路径无规格，该项改按「改动未越出轻路径触发条件」自检**）」 | §一⑦ | [B] |
| F8 | `docs/standards/规格写作标准.md` `:24`（§1 判定表 ❌ 行·治理微调条） | 优先级子句与 F1 **同判据、同方向**改述，但**指代按所在件写实**——本表无「重」行（「重」只在 `施工机制` §二 分流表），故写作「与其他行命中时——若同时命中 `施工机制` §二 分流表「重」行的触发条件，以「重」为准；否则以本行为准（具体优于一般）」。**该行其余文字原样保留**（含「当批 `CHANGELOG` 留痕不计入」「作者会话内逐项确认」「豁免条件缺一不可」）；**引号块按落文字面书写**（用「重」，规格内**不再二次转写**——审查 003 的 P0 即『』与「」两版本互卡） | §一① ⑧ ＋ 审查 003／004 | [A] |
| F9 | `CHANGELOG.md` | 本批一行（≤400 字符），以「分流优先与审查闭合勘正（069）：…」**起首**；**九处改动须逐项对上**（含 F5：`:76` 静态自检段补现状指针——审查 F-06 指出原行漏 F5）——`[B]` 而非 `[A]`：目标文本带省略号（示意后续内容），逐字核无意义且会恒报「未落实」（068 F11 同型）；判据即断言 8 第 1 点的前缀 grep | 过程产物两项 | [B] |

**F1／F8 的联动要点（易错）**：两句是**同一判据的两处载体**，故须**同判据、同方向、同细则**（「重」优先 ＋ 否则本行优先 ＋ 括注「具体优于一般」），但**指代按所在件写实**（`施工机制` 写「重」行；`规格写作标准` 写「`施工机制` §二 分流表「重」行」）——改完须 **`grep` 三处对照**（`施工机制` `:39`／`:43` ＋ `规格写作标准` `:24`）。**引号一律按落文字面书写**（用「」），规格内不再二次转写（068／069 两轮的教训：`[A]` 目标文本里写一套、要求落文另一套，两向都会卡）。

## 三、验收断言（含预验记录）

### 断言 1｜优先规则两处同改且逐字一致

```bash
grep -c "若同时命中「重」行触发条件，以「重」为准" docs/specs/施工机制.md
grep -c -F '若同时命中 `施工机制` §二 分流表「重」行的触发条件，以「重」为准' docs/standards/规格写作标准.md
grep -c "与其他行命中时以本行为准——具体优于一般" docs/specs/施工机制.md docs/standards/规格写作标准.md
```

**两件各一模式**（`规格写作标准` 侧含 `施工机制` §二 的指代，与 `施工机制` 侧**非逐字相同**——同判据同方向即可，见 §二 联动要点；**不得用同一模式核两件**，否则标准件侧恒红，审查/施工已两度实证）。期望：第一项 **≥1**、第二项 **≥1**（后者用 `-F` 固定串匹配，避免 `§`／反引号被当元字符）、第三项两件**各 0**（旧句已去）；另核 `施工机制` `:43` 已改为**纯指针**（不再引规则原文）：

```bash
grep -c "若同时命中「重」行触发条件" docs/specs/施工机制.md
grep -c "冲突优先规则" docs/specs/施工机制.md
```

期望 **1**（仅 `:39`——`:43` 已不引原文）／**≥1**（指针句在位）；预验基线 0／0。

### 断言 2｜单维核对并入分身不复用条

```bash
grep -c "产物审查（含单维核对）必须是两个独立 fresh 分身" docs/specs/施工机制.md
```

期望 **1**；预验基线 0。

### 断言 3｜标准行流程列补产物审查节点

```bash
grep -c "（触发产物审查条件时）产物审查" docs/specs/施工机制.md
```

期望 **1**；预验基线 0。

### 断言 4｜轻路径在核验三步中的歧义已消

```bash
grep -c "不含轻路径——轻路径不写规格、不走核验三步" docs/specs/施工机制.md
```

期望 **1**；预验基线 0。

### 断言 5｜静态自检段的现状指针在位（不复述状态）

```bash
grep -c "各段现状见 \`测试与验收标准\` §1.1" docs/specs/施工机制.md
grep -c "该段已实现" docs/specs/施工机制.md
```

期望 **1**／**0**（只给指针、不复述状态——状态是 `测试与验收标准` §1.1 的单一真源）；预验基线 0／0。

### 断言 6｜止损对称条款与提交自检两项

```bash
grep -c "单维核对同此" docs/specs/施工机制.md
grep -c "改动未越出轻路径触发条件" docs/specs/施工机制.md
```

期望各 **≥1**；预验基线 0／0。

### 断言 7｜禁改面零改动与改动面归属

```bash
git status --porcelain docs/specs/archive docs/product scripts | wc -l
git diff -U0 docs/specs/施工机制.md docs/standards/规格写作标准.md | grep -c "^@@"
```

期望：第一项 **0**；第二项 **≤ 10**（**`-U0` 计法实测 10**＝`施工机制` 9 ＋ `规格写作标准` 1——F-01 在 `:92` 新增 1 个 hunk 后由 9 升至 10，经施工方 P1-2 申报更正；其中 F1 的 `:39` 与 F3 的 `:40` 相邻被合并为 1 个 hunk，故非按 F 项数计——**注**：早期草稿曾记「实测上界 7」，那是 `git diff` **默认上下文**下的计法，与本断言的 `-U0` 不同，经施工方 P1-1 更正），并**逐 hunk 走查归属**——每个 hunk 须落 §二 某个 F 项，无表外行（走查项，通过判据＝无表外行）。**口径注**：`grep -c` 计数为 0 时退出码为 1——本断言**取计数、不看退出码**（反模式 #6 族：勿把「0 命中」读成失败）。预验基线 0／0。

### 断言 8｜过程产物两项与链接

```bash
grep -c "分流优先与审查闭合勘正（069）" CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '分流优先与审查闭合勘正（069）' in l][0].rstrip('\\n')))"
```

期望第一项 ≥1、第二项 ≤400；全仓相对链接条数与断链＝基线（**168 条／0 断链**）：

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split('\0');f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md')];L=[(p,u) for p in f for u in re.findall(r'\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print('链接',len(L),'断链',sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

`bash scripts/check.sh` 退出 0；验收通过后本规格移入 `docs/specs/archive/`。

### 断言 9｜审查修订轮的四项闭合（产物审查 F-01／F-06／F-07／F-08）

```bash
grep -c "轻路径批次除外，见 §三 第 3 步" docs/specs/施工机制.md
grep -c "按「轻」行的优先规则定" docs/specs/施工机制.md
grep -c "（单维核对同此）" docs/specs/施工机制.md
grep -c "静态自检段补现状指针" CHANGELOG.md
```

期望依次各 **≥1**（`:92` 与 `:63` 的轻路径例外已同步／`:35` 指代写实／`:101` 去粗体后短语形态／CHANGELOG 含 F5 半句）；预验基线 0／0／0／0。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 八条逐条对现文实核——`:39`／`:43`／`:40`／`:63`／`:76`／`:94`／`:101`／`:150` 与 `规格写作标准.md`:24 的原文（含「与其他行命中时以本行为准——具体优于一般」旧句、「成稿审查与产物审查」、「其余（含轻路径）由规划方通读抽查」、「含」等被引短语）；**8 条 findings 全部成立，无一条误报**。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–8 基线已逐条实跑——`若同时命中…` 0／0、`与其他行命中时以本行为准——具体优于一般` **1／1**、`产物审查（含单维核对）` 0、`（触发产物审查条件时）产物审查` 0、`不含轻路径——轻路径不写规格、不走核验三步` 0、`该段已实现` 0、`单维核对同此` 0、`改动未越出轻路径触发条件` 0、禁改面 0、hunk 0、CHANGELOG 0、链接 168／0、`check.sh` 退出 0。**无未预验项**（断言 7 的 hunk 上界与归属属施工后实核项）。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 全部（只读）、`docs/product/`、`skills/`（未创建）、`scripts/` 全部（本批不动工具与守卫）、本批规格自身。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；**不得顺带改本批未点名的条文**（§一 不变量列了范围）——尤其**不得**改动 §六 反模式清单、§八 模板、`规格写作标准` §1 表内其他行。
- 禁止越权：不改版本号与 tag；不新增文件（本批只改**三件**既有文件：`施工机制.md`／`规格写作标准.md`／`CHANGELOG.md`）；**不建台账／不建索引件**。
- 禁止改断言、改数字、删检查来变绿；**不得**借本批新增纪律（口径与闭合勘正 ≠ 加规则）。

## 六、被否决的替代方案（可选）

- **只改 `施工机制` 一处、不动 `规格写作标准` §1** → 否决：§1 是**判据来源**（`施工机制`:43 明写），只改下游会让两处判定表在「冲突时谁优先」上不一致——第二处口径冲突源，正是 068 批刚清过的那类。
- **把轻行的优先规则整句删掉**（冲突时一律按 `:35`「不确定属哪条时取较重的一条」）→ 否决：该条只覆盖「不确定属哪条」的情形，而「同时命中两行」是**确定命中两条**，两问题不同；显式写「重优先」比留给解释更省事。
- **在分流表里逐条列出「轻 × 重」交叉矩阵** → 否决：两行交叉只有一种实质情形（轻行是「≤3 文件的治理微调」，与重的 `skills/`／域契约面几乎不可能重叠），写矩阵属过度设计；一句优先规则足矣。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 068-1 | `:63` 改述后「触发产物审查」仍靠规划方读 §五（无机器闸） | 仍待 | 本批只补**流程表述**与**优先规则**，未加机器闸（`AGENTS.md` §四 注册表类守卫不做）；触发＝再出现「该派未派」 |
| 068-2 | §八 其余两份模板（参考研究裁定表／核验请求模板）仍带括注式说明 | 仍待 | 本批不动 §八；触发＝再报「模板混说明」时按同一改法清 |
| 068-3 | 改动清单路径列的写法（须仓根可达全路径） | 仍待 | 本批未立法（落点候选 `规格写作标准` §2.2／§5.8）；**本批自身仍犯一次**（F2–F7 路径列初稿写「同件」，经成稿审查 005 改为全路径）——触发＝下批评估立法 |
| 068-4 | untracked 新文件的 `git add -N` 会给索引留 intent-to-add 条目 | 仍待 | 本批不动 §三 核验第 1 步的该句；触发＝实际出现误提交风险时加守卫 |
| 065-11 | 检查 A 与 C 的「目标文本」两口径并存 | 仍待 | 067 §七 与 **068 §七** 两表均记「评估时点顺延下批」＝本批到期；本批不动 `scripts/`、未触及该口径分歧 → 再顺延（**068 §七 已判其属「时点条目，故必列而非只给索引指针」**，本批承其口径列此一行） |

**汇总**：按条计 **5 条**：已履行 0｜作废 0｜本批处置 0｜仍待 5｜待作者 0 ＝ 5 ✓（逐格可复核）。**范围注**：本批改动面落在 `施工机制` §二／§三／§五／§七 与 `规格写作标准` §1；066／067 的工具面与核销面条目**均未被本批触及、状态不变**，故不重列（**065-11 为例外**——其评估时点已到期，故必列）（其状态由 `068` §七 表与 `064` §三 表承载——后者为 059–063 的索引落点）。

## 八、开放项

1. 「重优先」的新规则**无机器闸**（分流判定仍靠规划方自判）——本批不加（同 068-1 的口径）；若再出现「轻行降级了重路径」的实例，按事故出身评估。
2. `规格写作标准` §1 的「非平凡改动流程」行（「探索现状 → 写规格 → 作者确认 → 实施 → 验收 → 请求提交」）未列审查两个节点，但该行末已**指向** `施工机制`（单一真源）——本批判为**无需同步**（若后续出现「照 §1 流程走、漏了成稿审查」的实例，再改为显式列节点）。
3. **存量加粗留痕**（审查 F-08 附注，非本批引入）：`施工机制.md` 的 `**` 由 HEAD **90／254** 升至 **98／254**，两值均超 `254÷3≈84.7` 的**数值兜底**；但按 `文档写作标准` §一.3.8 的**适用范围**条，该件「表格＋条目行占 66%」→ 兜底阈值**不适用**，只受定性规则约束（本批新增四对粗体已逐处判为「闸门」类）。然而 `064` §八 6 的超阈件留痕清单里**没有本件**——属存量债，建议下次做加粗类留痕时按存量记一笔（防将来被误当回归）。
4. 「轻路径的提交自检四项」本批只补了首项的例外（其余三项——验收断言全绿／术语一致／新增文件命名——对轻路径仍成立）；若事后发现第二项「验收断言全绿」对无规格批次也需例外，按类补。

## 九、审查记录

- 成稿审查分身（fresh、只读）首轮：**findings 共 15：P0:1 P1:7 P2:7｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（15／15）**：

  - **001（P0）**＝F2 的 `[A]` 目标文本裹了粗体而断言 2 的 token 不带星号 → **两向互卡**（照录带星号则断言 2 恒红；省星号则 check C 报「未落实」）→ 目标文本改为**不带任何强调标记**。
  - **P1 七条**：**002**＝改动清单漏 `:43`（068 批补入的 §1 引文截短片），F1/F8 改后成**过时引文** → F1 扩为三处（`:35`／`:39`／`:43`）；**003**＝F8 的 `[A]` 字面写『重』而联动要点要求落文「重」——同 001 的双向卡 → 引号块一律按**落文字面**书写，规格内不再二次转写；**004**＝`规格写作标准` §1 表内**无「重」行**，逐字照抄会让标准件出现无法在本件解析的指代 → 改为「同判据、同方向，**指代按所在件写实**」；**005**＝路径列写「同件」→ C 无核对面（**068 F-02 同类复发，本批自身又犯一次**）→ 六行改全路径；**006**＝自由度分布 `[A]×2 [B]×6` 而实为 `[A]×3 [B]×6`（**同族第三次**）→ 改正；**007**＝核销表漏 **065-11**（068 §七 已判其属「时点条目，故必列」而本批即到期）→ 补一行，汇总 4 → **5**；**008**＝F1 让轻行压标准行后，「轻路径 × 改标准条文」会落进 `:63` 第一支却无可绑时点 → 该支末补「（轻路径批次除外——不派审查分身，改后由规划方自查）」。
  - **P2 七条**：009（断言 7 算式按 F 项数算错——F1 的 `:39` 与 F3 的 `:40` 相邻被合并，实测上界 **7**；并补「取计数不看退出码」的口径注）、010（断言 8 补链接命令）、011（§一 不变量「另两行」与 F3 相冲 → 改「另两行的其余格」）、012（F5 的「括注末」两读法 → 钉死「粗体段之后、分号之前」）、013（`:35` 与新 F1 句的读法张力 → 补澄清半句「同时命中两条 ≠ 不确定属哪条」）、014（F5 原写「该段已实现」会成 §1.1 现状表的第二载体、触反模式 #4 的边 → 改为**只给指针不复述状态**）、015（§七 的 068-3／068-4 **编号互换**——核销表是跨批唯一索引，编号失真会误导检索）→ 全部采纳。
  - **已实核通过项（摘要）**：八条锚点逐条实跑**无一误报**（含 ⑤「该段已实现」经 `check.sh`:10 与 `测试与验收标准.md`:26 双向实证）；F3 的节点插入位置与 §五 时点一致；F4 与 §二 轻行流程自洽；断言 1／3／4／5／6 的 token 均在 F 项目标文本内（仅断言 2 例外，见 001）；被改短语的其他引用面全仓扫过（`AGENTS.md`／`README`／`docs/standards` 其余件／`docs/product`／`scripts` **零命中**，无需同步他件；归档件属历史记录）；`check.sh` rc=0、markdownlint 0 issue、链接 168／0。
  - **未验证（如实声明）**：作者转来的第二轮审读报告原文不在仓内（采信其自述的 P0:0／P1:2／P2:6）。

- **施工（规划方逐条裁定）**：F1–F9 全落（恰 3 件文件；**两件 9 个 hunk**（`施工机制` 8 ＋ `规格写作标准` 1）＋ `CHANGELOG` 1 个 hunk ＝ 10，全部可归属——审查 F-04 指出原行「9 个 hunk」未随断言 7 的两件口径收窄）；施工方申报 1 条 P0 ＋ 2 条 P1，**全部采纳**：**P0-1**＝断言 1 第 1 点想「同一模式核两件」，而 F8 的**指代写实**（`施工机制` §二 分流表「重」行）使该连续片段在标准件内必然为 0 → **改为两件各一模式**（标准件侧用 `grep -F` 固定串），并写明「不得用同一模式核两件」；**P1-1**＝断言 7 的「实测上界 7」是 `git diff` **默认上下文**的计法，与本断言的 `-U0` 不同（`-U0` 实测 **9**）→ 注订正；**P1-2**＝F9 标 `[A]` 而目标文本带省略号（示意后续内容）→ 逐字核无意义且恒报「未落实」（068 F11 同型）→ 改 `[B]`，判据交断言 8 的前缀 grep。施工方按红线 4 未改断言、未改数字、如实申报，处置正确。

- **双维产物审查分身**（fresh、只读；时点＝核验第 1／2 步后）：**findings 共 8：P0:0 P1:1 P2:7｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（8／8）**：

  - **F-01（P1）**＝**F4 的例外没同步到 §五**：`:63` 新增「（轻路径批次除外…）」而 `:92` 的触发面仍写「改规则／结构／契约的批次（含重路径与动标准条文的批次）」——而 `:63` 开篇即「按 §五 分工走」，把读者送回 `:92`，同一组合在 `:92` 落「派」、在 `:63` 落「不派」→ `:92` 括注后补「；轻路径批次除外，见 §三 第 3 步」（取**不新增第二判定落点**的方案）。
  - **P2 七条**：**F-02**＝`:43` 的**截短引文**丢了「否则以本行为准（具体优于一般）」半句（只读 `:43` 者会以为**无条件重优先**），且该引文已在 068／069 两批各产生一次同步成本 → 改**纯指针**（载体由 3 降为 2：`:39` ↔ `规格写作标准` §1；审查判「三载体触反模式 #4 的边但可接受」，收敛后更稳）；**F-03**＝定位行「四处」而括号内列 5 项 → 改「五处」；**F-04**＝§九 的「9 个 hunk」未随断言 7 的两件口径收窄（三件口径实为 10）→ 改述；**F-05**＝§九 尾部占位行**重复**（`:172`／`:173` 逐字相同）→ 删一行；**F-06**＝F9 的 CHANGELOG 行**漏 F5** → 补半句；**F-07**＝`:35` 新半句的「按**该行**的优先规则定」指代不落（「标准」／「重」两行均无优先规则子句）→ 写实为「按「轻」行的优先规则定（「标准 × 重」由「重」行流程含标准路径判定）」；**F-08**＝`:101` 的「**单维核对同此**」粗体不落四类 → **去粗体**（并附存量加粗留痕建议，记 §八 开放项 3）。
  - **已实核通过项（摘要）**：**本批最需要判的问题——「重优先」是否架空 §1 的豁免行 → 未架空**（审查逐词比对：「域内」在仓内口径一致指**产品层 skill 域**，故「复盘驱动的治理微调」即便必然「修改规范条目」仍走轻——与改前效力等价，本批只把「重」从被架空状态拉回，**无反向误伤**）；三个边界情形判定无第三解释空间；`规格写作标准` §1 的 ✅ 行**无需**同加优先说明（会制造多载体）；§二 三行逐行自洽、`:24` 的跨件指代可解析；引用面全仓扫过（除三改动件与在制规格，唯一命中面是归档 068，属历史记录）；**轻路径五处（`:39`／`:40`／`:63`／`:150`／`规格写作标准`:24）首尾一致**，唯一残留即 F-01；§七 核销表抽 3 行有据、必列面无该进未进、汇总 5 条逐格相符；F2／F3／F5／F6／F7 的改动质量与目标文本逐字复核无误；**`规格写作标准`:24 的 `**` 实测 0→0**（规划方交底中「该处新增若干粗体」的描述不实，审查按实测更正）。
  - **未验证（如实声明）**：作者转来的第二轮审读报告原文不在仓内（采信 §九 前段记录）。

- **产物审查修订轮（施工，规划方裁定）**：五处全落（`:92` 同步／`:43` 改纯指针／`:35` 指代写实／`:101` 去粗体／CHANGELOG 补 F5 半句，整行 **311 字符**）；断言 1–9 全绿；两件 hunk **10**（`施工机制` 9 ＋ `规格写作标准` 1）＋ `CHANGELOG` 1 ＝ 11，全部可归属。施工方申报两条 P1，**均采纳其处置**：**P1-1**＝F-01 补句的**粗体实现选择**——交底渲染的「**轻路径批次除外**，见 §三 第 3 步」会使断言 9 的连续串断裂（与上一轮 P0-1 同族），施工方改落**不加粗整句**并申报；裁定**维持不加粗**（与 F-08 立的四类口径一致，且 `施工机制` 粗体不新增）；**P1-2**＝断言 7 的上界未随 F-01 的新增 hunk 同步（`-U0` 实测由 9 升 **10**）→ 上界订正为 ≤10 并同步算式。**[4] 段的施工前后对照**：`[A] 未落实` 于 CHANGELOG 的那条**已消**（F9 改 `[B]`）、`规格写作标准` 侧的互斥预警**已消**（断言 1 拆两件各一模式）。

- 按止损条 2：产物审查 **P0＝0**，不触发强制复审；修订后由规划方复核 + 归档。
