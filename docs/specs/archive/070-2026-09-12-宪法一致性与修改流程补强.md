# 070-2026-09-12-宪法一致性与修改流程补强

> 定位：落一份对 `AGENTS.md` 的**评审报告**（作者转来，14 条：4 高／6 中／4 低；**其中 #4 与 #11 同题**（门禁启用），去重后 **13 项**）——规划方逐条对现文实核：**成立 8 项（含 1 项轻度）／部分成立 3 项／不采纳 2 项**。本批补三处：**表述一致性**（宪法自述与正文矛盾、运行侧状态两处不一）、**工具链离线约束的口径**（`npx` 与「守卫零外部依赖」的张力，按「登记例外 ＋ 环境前提」解决）、**宪法修改流程与权威顺序**（缺落点）；并顺手补术语指针与四处细则。
> 自由度分布：[A]×2 [B]×12
> 必读输入：`AGENTS.md`（本文，全部）；`docs/standards/术语标准.md`（§1 用法／§3 词条表）；`docs/standards/运行环境标准.md`（§一.3／§4 ③级行／§三 锚点行）；`docs/standards/测试与验收标准.md`（§1.1 现状表／§1.2／§1.3）；`docs/specs/施工机制.md` §一／§五／§六／§七；`scripts/check.sh`（头注段序 ＋ 各段实现状态：**`[0]`／`[1]`／`[4]` 已实现，`[2]`／`[3]`／`[5]` 为 `TODO:` 注释占位**）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

| # | 评审条目 | 现文 | 判读 |
| --- | --- | --- | --- |
| ① | 「规则条文不在这里」与正文矛盾 | `AGENTS.md`:3「**规则条文不在这里**——命名与格式在标准里，协作流程在施工机制里」；同件 §一 红线／§四 不做清单／§五 分工原则**本身就是规则** | **成立**：自述与正文自相矛盾 |
| ② | `npx` 可能联网 与「守卫零外部依赖」冲突 | `:35` 常用命令＝`npx markdownlint-cli2 "**/*.md"`；`:47` 不做清单＝「CI 里跑 LLM 或联网——守卫零外部依赖」；实测**无 `package.json`／`node_modules`**（`ls` 报不存在）→ `npx` 走本地缓存，**首跑需联网取包** | **成立**；但评审给的修法（`npx --no-install`）**解决不了**：该 flag 只影响「是否允许自动安装」，不改变「[1] 段依赖外部工具链」这一事实（**本机实测 `npx --no-install markdownlint-cli2 --version` rc=0**，因包已在 npx 缓存内——冷机行为未测）→ 按「**登记例外 ＋ 环境前提**」解决，见 F6／F11 |
| ③ | 缺「修改本文件」流程与权威顺序 | 全件**无**「修改本件」条款；全仓**无**全局权威顺序（`grep -rn "权威顺序"` 仅命中本规格自身） | **成立**：宪法自身的修改与冲突裁决无落点 |
| ④ | 门禁启用手动配置易漏 | `:34` 已写明启用命令且**已注明**「未设此配置的克隆不会自动拦截」；`施工机制` §七 亦写「提交前必跑」 | **部分成立**：逃生口已在，缺的是**决定记录**（按 §四 条目格式落「不做」，不新增 `setup.sh`） |
| ⑤ | 路由到目录未列具体文件 | `:26` 已列 **11 类主题名**（实测 11），只是没列**文件名** | **部分成立**：补「文件清单见 `命名标准` §1 目录树」即可 |
| ⑥ | 术语未定义 | `术语标准` §3 现词条 **5 条**；实测 `作者`／`候选`／`红基线`／`未建设`／`负向流程守卫`／`注册表类守卫`／`停摆` 在该件命中 **各 0** | **成立**：宪法高频用语无定义、无指针 |
| ⑦ | 运行侧表述两处不一 | `:5`「运行＝`skills/` 下的 skill 组（**尚未创建**）」；`:9`「**当前仓库只有开发侧基座**——`skills/` 与产品层各 skill 均尚未创建」 | **轻度成立**：语义一致、两种说法并存 |
| ⑧ | 红线 1 的 git 写操作未列举 | `:13`「commit／push 等一切 git 写操作」——`add` 算不算写操作会被争 | **成立**：补列举 |
| ⑨ | 红线 6 需工具区分失败类型 | `:18` 指 `测试与验收标准` §1.2；该处原文＝「建设期内，验收清单条目因「能力尚未建设」而失败的，不计入红基线；只有**已建设能力出现回归**才算红」 | **成立（判据需可判）**：现为散文判据，且规格初稿「待建段不执行」的表述与本脚本现况**不符**（`[4]` 已实现并执行）→ 补可判据 ＋ 更正事实 |
| ⑩ | 「注册表类守卫」条目格式不匹配 | `:53`「注册表类守卫（行为契约／能力接线／引用闭包）——引入任何机制须先登记事故出身」——这是**准入规则**，非「不做」项 | **成立**：改写为四件式，**但须保留**原句「引入任何机制须先登记事故出身」（并入理由） |
| ⑪ | 解冻触发条件应标「作者裁决」并记 `CHANGELOG` | `:59` 末句已写「触碰本节即停，先修订本节并取得作者确认」 | **部分成立**：缺「记入 `CHANGELOG`」与「解冻一律由作者裁决」的明示 |
| ⑫ | 项目一句补「状态／里程碑」链接 | `:9` 句内已含当前状态；**治理侧**无里程碑件（项目阶段在产品层 `docs/product/产品定义.md` §七 路线） | **不采纳**：无治理侧落点，状态已在句内（产品层阶段不在宪法复述） |
| ⑬ | 未来膨胀时下沉 ADR | 现件 `wc -l` ＝ **65** 行 | **不采纳（暂无动作）**：评审自判「当前长度尚可接受」；真到阈值时按 §四「内生必要性自问」评估 |

**不变量（施工前后须同）**：`docs/specs/archive/`、`docs/product/`、`scripts/`、`skills/`（未创建）零改动；`AGENTS.md` 除下表点名处外零改动（§一 红线 2–5、§二 路由表其余行、§三 三行、§四 其余 11 条、§五 三条）；三份标准件除点名处外零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `AGENTS.md`:3（头注首行） | 「**规则条文不在这里**——命名与格式在标准里，协作流程在施工机制里」→「**具体标准与操作细则不在这里**——命名与格式在标准里、协作流程在施工机制里；**宪法级约束（红线／不做清单／分工原则）在本文件**；本文件的修改与权威顺序见 §六」 | §一① | [A] |
| F2 | `AGENTS.md`:9（项目一句） | 状态表述统一为「**当前状态：运行侧（`skills/`）与产品层各 skill 均尚未创建——本仓库只有开发侧基座**」（与 `:5` 的「运行＝`skills/` 下的 skill 组」措辞对齐） | §一⑦ | [B] |
| F3 | `AGENTS.md`:13（红线 1） | 「commit／push 等一切 git 写操作」→「**一切 git 写操作**（包括但不限于 `add`／`commit`／`push`／`tag`／`reset`／`checkout -b`）」；该条其余文字原样 | §一⑧ | [B] |
| F4 | `AGENTS.md`:18（红线 6） | 括注补可判据指针：「（建设期内未建设能力的失败不计入红基线——**何谓「未建设」见 `测试与验收标准` §1.2 的判据句**）」——其中 `测试与验收标准` 加 Markdown 链接，**目标写仓根相对 `docs/standards/测试与验收标准.md`**；**同句不得两次链向同一目标**（审查 P2-3——第二节名改述为「该节判据句」） | §一⑨ ＋ F12 | [B] |
| F5 | `AGENTS.md` §二 路由表（`:24`–`:29` 表体） | ① 第一行末补「（**文件清单见 `命名标准` §1 目录结构**）」——`命名标准` 加链接、目标写 `docs/standards/命名标准.md`（**节名照该件实标题「1. 目录结构（现状）」取，不写「目录树」**）；② 表末**新增一行**：「某些词在这里是什么意思（作者／候选类发现／红基线／负向流程守卫……） → `docs/standards/术语标准.md`」——标签即该路径文字、另加 Markdown 链接指向同一路径（仓根相对）；**示例词只列 F13 真定义的四条**（不得列「停摆」「守卫二分」等未定义词） | §一⑤ ⑥ | [B] |
| F6 | `AGENTS.md`:47（不做清单·CI／联网条） | 末补**例外**（**指针式，不写机制细节**）：「（**例外**：`check.sh` [1] 段的 lint 工具链属环境前提，见 `运行环境标准` §一.3——该处加链接、目标写 `docs/standards/运行环境标准.md`）」——**不复述**机制句（「首次使用可能联网取包／此后走本地缓存」只在 §一.3 一处落文；两处逐字重复触 `文档写作标准` §一.7.3 与反模式 #4，审查 P2-5）；**落文保留工具身份串**「`check.sh` [1] 段的 lint 工具链经 `npx` 调 markdownlint」（断言 4 第 3 项以此为 token——只删机制句，**追认，施工期 P1-10**） | §一② | [B] |
| F7 | `AGENTS.md`:53（不做清单·注册表类守卫条） | 改写为四件式并**保留原准入句**：「无事故出身的注册表类守卫（行为契约／能力接线／引用闭包）——不做：守卫的价值取决于事故出身，凭空建表既无判据也无维护人（**引入任何此类机制须先登记事故出身**）；逃生口：人工审查 ＋ 当批规格的核验清单（内容轨检查器按需扩展）；解冻触发条件：同类事故复发，且该检查可机械化为零外部依赖的断言」 | §一⑩ | [B] |
| F8 | `AGENTS.md`:59（§四 末句） | 补「；**解冻一律由作者裁决并记入 `CHANGELOG`**」 | §一⑪ | [B] |
| F9 | `AGENTS.md` 文末（§五 之后） | **新增 §六**「本文件的修改与权威顺序」（三句）：① **修改本文件须作者确认**；触碰 §四 不做清单先按该节末句修订本节；② **权威顺序**：本文件的宪法级约束 ＞ `docs/standards/` ＞ `docs/specs/`（规格与施工机制）＞ 其他文档；**冲突时按 `施工机制` §五／§六 裁决**（该处加链接、目标 `docs/specs/施工机制.md`）；③ **例外**：**具体真源的指定不在此序**——某事项以哪份为准若已有明文指定（如术语叫法以 `术语表` 为准、域清单以 `架构设计` 为准；两处各加链接，目标分别 `docs/product/术语表.md`／`docs/product/架构设计.md`），从其指定；本序只解决「同一事项被多处规定且相抵」时的取舍 | §一③ ＋ 审查 P1-8 | [B] |
| F10 | `AGENTS.md` §四 追加组末 | 新增不做项（四件式）：「门禁启用的机械自检（`setup.sh` 或 `check.sh` 检测 `core.hooksPath`）——不做：启用命令与「未设不拦截」已明文（§三 ＋ `施工机制` §七 的「提交前必跑」），再建自检即第二载体；逃生口：**提交前必跑 `bash scripts/check.sh` 的纪律**（hook 只是自动化、不增检测面——克隆未设 hooksPath 时由该纪律覆盖同一风险）；解冻触发条件：出现「未启用 hooksPath 而漏拦截」的实际事故」 | §一④ ＋ 审查 P2-11 | [B] |
| F11 | `docs/standards/运行环境标准.md` §一.3（「宿主与版本前提」第 3 条） | 在「（脚本 + lint 工具链）」**之后、句末「。」之前**补：「；**该工具链经 `npx` 调用**——首次使用可能联网取包，此后走本地缓存（离线可用）；本仓不含 `package.json`，故 `--no-install` 形态不解决该依赖——此为「守卫零外部依赖」的**已登记例外**」——**不复述**守卫纪律原句（见 F6 注）；**该条〔出处〕栏补 070 批**（新落事实不得仍挂在旧存档出处下，审查 P2-7） | §一② ＋ 审查 P2-3／P2-4 | [B] |
| F12 | `docs/standards/测试与验收标准.md` §1.2（基线定义句） | 保留原句并补**可判据**：「…不计入红基线；只有**已建设能力出现回归**才算红。**判据**：『未建设』＝该段未实现（`check.sh` 中以 `TODO:` 注释占位、**不执行**）或前置缺失而**明示跳过**（如 `skills/` 未建时的 [0] 段）——两者均不置红、不计基线；**已实现段（现为 [0]／[1]／[4]）的置红一律计红**（**失败＝置红条件**；候选类发现与明示跳过不算失败）」 | §一⑨ ＋ 审查 P1-5／P2-8 | [B] |
| F13 | `docs/standards/术语标准.md` §3（词条表） | 新增四条（照该表三列：术语／定义／英文对应）：**作者**＝纯裁决位——规格确认、提交确认两处常规出手，另有治理微调的会话内逐项确认与重大分歧裁决（`施工机制` §一）／author；**候选类发现**（简称**候选**）＝机器检出但非阻断的发现——只呈报、不影响退出码（与「失败即置红」的守卫相对）／candidate finding；**红基线**＝当前已知失败集合；建设期内「未建设」不计入（判据见 `测试与验收标准` §1.2）／red baseline；**负向流程守卫**＝「不许发生某事」的检查（与「拦截式宿主 hook」相对）／negative-path guard | §一⑥ | [A] |
| F15 | `docs/standards/文档写作标准.md`:108（反例行） | 该行硬编码「`AGENTS.md` **57 处**」——本批给该件加 16 处加粗（57 → **89**）→ 改为「（`AGENTS.md` 现 **91 处**属红线加粗，非赘述——**该数随该件改动而变，判读看性质不看数值**）」，并记 §八 加粗留痕 | 审查 P1-2 | [B] |
| F16 | `AGENTS.md` §六 ②／③（本批新增段） | ① **② 的裁决指针写实**：「冲突时按 `施工机制` §五／§六 裁决」→「**两处规定相抵时以本序定取舍；规划方／施工方对同一 finding 的分歧按 `施工机制` §六 升级**」（§五／§六 无「两处规定相抵」的对应条文，原指针为薄指针——审查 P1-3）；② **③ 的例外扩到类级**：「某事项以哪份为准若已有明文指定」→「**某事项或某一类规则**以哪份为准若已有明文指定（**如协作与流程类以 `施工机制` 为准**、术语叫法以 `术语表` 为准、域清单以 `架构设计` 为准），从其指定——此时本序的层级**不适用**」（**删**原 ③ 尾句「本序只解决「同一事项被多处规定且相抵」时的取舍」——与 ② 新增的「两处规定相抵时以本序定取舍」重复，施工期 P1-11） | 审查 P1-3 | [B] |
| F17 | `docs/standards/运行环境标准.md` §4（零依赖承诺句） | 该句末补「（**[1] 段 lint 工具链为例外**，见 §一.3）」——本批已在 §一.3 立例外，而 §4 的绝对承诺句未收口，同件内自相矛盾（审查 P1-4） | 审查 P1-4 | [B] |
| F19 | `docs/standards/运行环境标准.md`:44（「明确不做」行） | 「12 条完整清单」→「**完整清单**（**去数字化**——本批使该数由 12 变 13，硬编码必腐；与 `文档写作标准` §一.3 的计数口径同理），并同步 §七 的 064-4 行 | 审查 P1-1 | [B] |
| F18 | `docs/standards/测试与验收标准.md` §1.3（守卫纪律句） | 同句末补同一指针「（[1] 段 lint 工具链为例外，见 `运行环境标准` §一.3）」——**只加指针、不改纪律句本身**；**指针用代码跨度而非链接**（该件惯例虽多用链接，但照字面落即无断链风险——追认，施工期 P1-13） | 审查 P1-4 | [B] |
| F14 | `CHANGELOG.md` | 本批一行（≤400 字符），以「宪法一致性与修改流程补强（070）：…」起首，覆盖 F1–F13 十三项 | 过程产物两项 | [B] |

**目标文本里的链接形态（通则，审查/施工两度实证的坑）**：本批 F 项的目标文本是要**落进 `AGENTS.md`（仓库根）**的，故其中 Markdown 链接目标**一律写仓根相对路径**（`docs/standards/…`／`docs/specs/…`／`docs/product/…`）；规格自身所居的 `docs/specs/` 才用 `../` 形态——**规格里展示的 `../` 形态不得搬入落文**（照搬会造 7 条断链，与断言 9 的「断链 0」互斥；施工方按 P0 停摆申报，处置＝裁定 A）。为免规格自身出现不可解析链接，本节各目标文本**不写完整链接语法**，而以「`某文档` 处加链接，目标写 `docs/standards/某文档.md`」的形态描述；断言侧改用**不含「右括号＋左括号」连写**的 token（两条 `grep -F`：一条取标签串、一条取目标路径字面）——否则规格自身会出现不可解析链接、使断言 9 的「断链 0」恒红（施工期 P1-8 实证）。

## 三、验收断言（含预验记录）

### 断言 1｜宪法自述与正文一致（头注改述）

```bash
grep -cF '宪法级约束（红线／不做清单／分工原则）在本文件' AGENTS.md
grep -cF '规则条文不在这里' AGENTS.md
```

期望 **1**／**0**；预验基线 0／1。

### 断言 2｜运行侧状态表述统一 ＋ 红线 1 列举

```bash
grep -cF '当前状态：运行侧' AGENTS.md
grep -cF '包括但不限于 `add`／`commit`／`push`／`tag`／`reset`／`checkout -b`' AGENTS.md
```

期望各 **1**；预验基线 0／0。

### 断言 3｜路由表两处（文件清单指针 ＋ 术语行）

```bash
grep -cF '文件清单见 `命名标准` §1 目录结构' AGENTS.md
grep -cF 'docs/standards/命名标准.md' AGENTS.md
grep -cF 'docs/standards/术语标准.md' AGENTS.md
```

**口径注**：三条 token **均不含「右括号＋左括号」连写**（否则规格自身留不可解析链接、断言 9 恒红——施工期 P1-8 实证）。第 2 项判 F5① 的链接目标（仓根相对）；第 3 项判 F5②。期望各 **≥1**；预验基线 **0／0／0**（第 2 项施工前在 `AGENTS.md` 零命中）。**F5① 的落文形态（追认，施工期 P1-6）**：链接**包住整个指针短语**（即标签＝加粗的「文件清单见 `命名标准` §1 目录结构」整串，目标＝`docs/standards/命名标准.md`；把链接只包在 `命名标准` 四字上会使第 1 项 token 恒为 0）——该形态与 F5① 的文字要求逐字一致。**本注不写完整链接语法**（避免规格自身出现不可解析链接，同 §二 通则段）。

### 断言 4｜不做清单两处改写 ＋ 末句

```bash
grep -cF '无事故出身的注册表类守卫' AGENTS.md
grep -cF '解冻一律由作者裁决并记入' AGENTS.md
grep -cF 'lint 工具链经 `npx` 调 markdownlint' AGENTS.md
grep -cF '引入任何此类机制须先登记事故出身' AGENTS.md
```

期望依次 **1**／**1**／**1**／**1**（**F7 的条目标题不加粗**——与 §四 其余 12 条体例一致，审查 P2-4；末项＝F7 落的准入句——**含「此类」**；施工期 P1-1 更正：现文为「引入任何机制须先登记事故出身」（**无**「此类」），故基线为 **0**，非 1）；预验基线 0／0／0／0。

### 断言 5｜新增 §六（修改流程与权威顺序）

```bash
grep -c '^## 六、本文件的修改与权威顺序' AGENTS.md
grep -cF '修改本文件须作者确认' AGENTS.md
grep -c '^## ' AGENTS.md
```

期望 **1**／**1**／**6**（**扫描面＝整件**：现 5 个二级标题，新增 §六 后 6 个）；预验基线 0／0／5。

### 断言 6｜新增不做项（门禁自检）

```bash
grep -cF '门禁启用的机械自检' AGENTS.md
python -c "import io;L=io.open('AGENTS.md',encoding='utf-8').read().split('**追加（事故驱动）**')[1].split('引入任何新机制前')[0];print(sum(1 for l in L.splitlines() if l.startswith('- ')))"
```

**口径注**：第二项的扫描面＝「**追加（事故驱动）**」标题起、至「引入任何新机制前」句止的区间内、行首「连字符 ＋ 空格」的条数。期望 **1**／**7**（现 6 条 ＋ F10 一条）；预验基线 0／6。

### 断言 7｜三份标准件的四处细则

```bash
grep -cF '该工具链经 `npx` 调用' docs/standards/运行环境标准.md
grep -cF '『未建设』＝该段未实现' docs/standards/测试与验收标准.md
grep -cF '候选类发现' docs/standards/术语标准.md
grep -cF '负向流程守卫' docs/standards/术语标准.md
```

期望各 **≥1**；预验基线 0／0／0／0。

### 断言 8｜禁改面与改动面归属

```bash
git status --porcelain docs/specs/archive docs/product scripts | wc -l
git diff -U0 AGENTS.md docs/standards/运行环境标准.md docs/standards/测试与验收标准.md docs/standards/术语标准.md docs/standards/文档写作标准.md | grep -c "^@@"
```

期望：第一项 **0**；第二项 **≤ 18**（**实测 18**：F1–F4／F6–F13 各 1（12）＋ **F5 分两处落点**（`:26` 改写 ＋ `:29` 后新增行；`-U0` 下必为 2）＝13 落点＋ F16 并入 §六 新增块（+0）＋ 本轮 F15／F17／F18／F19 各 1 ＝ **18**——施工期 P1-7／终版一并更正；**命令面须含 `docs/standards/文档写作标准.md`**，否则只得 17）；逐 hunk 走查归属，无表外行——**取计数、不看退出码**：`grep -c` 计 0 时退出码为 1。预验基线 0／0。

### 断言 9｜过程产物两项与链接

```bash
grep -cF '宪法一致性与修改流程补强（070）' CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '宪法一致性与修改流程补强（070）' in l][0].rstrip('\\n')))"
```

期望第一项 ≥1、第二项 ≤400；**链接**：断链 **0** 且条数**不低于** 169（＝**本规格把「右括号＋左括号」连写字面全部改掉后的实测值**；**该数随规格自身文字变动**——首稿 177 → 描述式 171／断链 2 → 去掉字面后见 §四.2 的实测——故判据取「不低于 ＋ 断链 0」而非等值；**断链 0 的前提是规格自身不含「右括号＋左括号」字面串**，见 §二 通则段的同名口径注）：

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split('\\0');f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md')];L=[(p,u) for p in f for u in re.findall(r'\\]\\(([^)\\s]+?)\\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print('链接',len(L),'断链',sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

`bash scripts/check.sh` 退出 0；验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 十三项逐条对现文实核——`AGENTS.md`:3／:5／:9／:13／:18／:24–:29／:34／:35／:47／:53／:59、`运行环境标准.md` §一.3 与 §4 ③级行／§三 锚点行、`测试与验收标准.md` §1.1 现状表／§1.2／§1.3、`术语标准.md` §1／§3；**术语七词命中各 0** 与**无 `package.json`／`node_modules`** 实跑确认；**`check.sh` 各段实现状态实核为 `[0]`／`[1]`／`[4]` 已实现、`[2]`／`[3]`／`[5]` 为 `TODO:` 占位**（审查 P1-5 更正）；**成立 8（含 1 轻度）／部分成立 3／不采纳 2 ＝ 13**。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–9 基线已逐条实跑——`宪法级约束…` 0／`规则条文不在这里` **1**；`当前状态：运行侧` 0／列举句 0；`文件清单见…`（**含链接形态**）0／`docs/standards/术语标准.md` **0**；`无事故出身的注册表类守卫` 0／`解冻一律由作者裁决并记入` 0／`lint 工具链经…` 0／`引入任何此类机制须先登记事故出身` **0**（F7 落文句的基线——含「此类」；现文无此字，施工期 P1-1 更正）；`^## 六、` 0／`修改本文件须作者确认` 0／全件二级标题计 **5**；`门禁启用的机械自检` 0／追加组条数 **6**；四处细则 token 各 0；**F15–F18 的四处 token 各 0**（`89 处`／`某一类规则`／`[1] 段 lint 工具链为例外` 两处）；禁改面 0；链接＝**见 §九 产物审查修订轮的终测**；`check.sh` 退出 0。**无未预验项**（断言 8 的 hunk 上界与归属属施工后实核项）。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 全部（只读）、`docs/product/`、`skills/`（未创建）、`scripts/` 全部（本批不动工具）、本批规格自身。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具；**不得顺带改本批未点名的条文**（§一 不变量列了范围）——尤其**不得**改 §四 其余 11 条与 §五 三条的既有文字。
- 禁止越权：不改版本号与 tag；不新增文件（本批只改**五件**既有文件：`AGENTS.md`／`运行环境标准.md`／`测试与验收标准.md`／`术语标准.md`／`CHANGELOG.md`）；**不建台账／不建索引件**（F5 是**加指针**，不是新建索引文件）。
- 禁止改断言、改数字、删检查来变绿；**不得**借本批新增未列机制（尤其**不得**顺手加 `setup.sh` ——F10 明写不做）。

## 六、被否决的替代方案（可选）

- **按评审建议把常用命令换成 `npx --no-install`（或加本地 devDependency）** → 否决：① 该 flag 只影响「是否允许自动安装」，**不改变 [1] 段依赖外部工具链**这一事实（本机实测 `npx --no-install …` rc=0，因包在缓存内）；② 加 devDependency 会往本仓引入 `node_modules`（现无），与「文件即真相、零安装」的取向相悖；③ 冷机（无缓存无安装）行为未测，而 `--no-install` 在那种环境下会**直接失败**——把一个「首跑联网」的小问题换成「守卫全红」的大问题。正解是**登记例外 ＋ 写明环境前提**（F6／F11），并把边界说清：**「守卫零外部依赖」约束的是守卫逻辑本身（不跑 LLM、不联网取数据）；[1] 段的工具链属 `运行环境标准` §4 ③级「仓库开发工具链」**——该等级已存在（义务＝探测可用性、缺失报红）。
- **新增 `scripts/setup.sh` 并在 `check.sh` 里检测 `core.hooksPath`** → 否决：已明文「未设此配置的克隆不会自动拦截」（`:34`），再建自检即第二载体；按 `AGENTS.md` §四 的条目格式记为**不做项 ＋ 逃生口 ＋ 解冻触发条件**（F10），把重新评估的钥匙交给作者与将来的实际事故。
- **为红线 6 引入 `RED_BASELINE`／`UNBUILT` 机械标签** → 否决：现状是**待建段不执行**（`[2]`／`[3]`／`[5]` 为 `TODO:` 注释占位），「未建设的失败」目前**不可能出现**；引入标签属为假想问题加机制（触 §四「内生必要性自问」）。改为把「未建设」的**判据**写实（F12）。
- **新建 `docs/standards/` 索引件**（评审 #5 的另一解）→ 否决：`AGENTS.md` §四 不建索引件；F5 用**指针**（「文件清单见 `命名标准` §1 目录树」）达到同一目的。

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 069-1 | 「重优先」新规则无机器闸（分流判定仍靠规划方自判） | 仍待 | 本批不动分流判据与 `scripts/`；触发＝出现「轻行降级了重路径」的实例 |
| 069-2 | `规格写作标准` §1 的非平凡流程行未列审查节点（判为无需同步） | 仍待 | 本批不动该件；触发＝出现「照 §1 流程走、漏了成稿审查」的实例 |
| 069-3 | 存量加粗留痕（`施工机制.md` 的 `**` 超数值兜底、兜底本身不适用；`064` **§七 6**（066 表记作 064-6）清单未含本件） | 仍待 | 本批不动 `施工机制`；触发＝下次做加粗类留痕时按存量记一笔 |
| 069-4 | 「轻路径的提交自检四项」只补了首项例外 | 仍待 | 本批不动 `施工机制`；触发＝第二项「验收断言全绿」对无规格批次也需例外时 |
| 068-1 | `:63` 的产物审查判定仍靠规划方读 §五 | 仍待 | 本批新增 §六 权威顺序**未**给该判定加闸；触发同上 |
| 068-2 | §八 其余两份模板仍带括注式说明 | 仍待 | 本批不动 `施工机制` §八；触发＝再报「模板混说明」 |
| 068-3 | 改动清单路径列的写法（须仓根可达全路径） | **本批处置** | 本批规格 F1–F14 **每行路径列均写仓根可达全路径**（已内化 068 的教训，经审查 P1-7 复核）；立法落点仍候选 `规格写作标准` §2.2／§5.8 |
| 064-4 | 跨层计数同类排查结论（备案；「将来新增跨层计数即回本项」） | **本批处置** | 本批改正 `运行环境标准`:44 的「12 条」（现为 13 条，入 F19）与 `文档写作标准`:108 的「57 处」（**终态 91 处**，入 F15）——两者均属「底座复述他层数字」的同型，触发已到 |
| 064-6 | 加粗密度超阈值件分类留痕（备案） | **本批处置** | 本批新增加粗的留痕入 §八 开放项（`AGENTS.md` 57 → **89**） |
| 065-11 | 检查 A 与 C 的「目标文本」两口径并存 | 仍待 | 本批不动 `scripts/` |

**汇总**：按条计 **10 条**：已履行 0｜作废 0｜**本批处置 3**｜仍待 7｜待作者 0 ＝ 10 ✓（逐格可复核）。**范围注**：本批改动面（`AGENTS.md` ＋ 三份标准件）与 068／069 的条目落点（`施工机制`／`规格写作标准`／工具）**无交集**，故只列时点到期者（069 四条 ＋ 068 三条 ＋ 065-11）与因本批而状态变化者（068-3）。

## 八、开放项

1. 评审 #12（项目一句加「状态／里程碑」链接）**不采纳**：**治理侧**无里程碑件（项目阶段在产品层 [`产品定义`](../product/产品定义.md) §七 路线），宪法不复述产品层阶段。
2. 评审 #13 的「解冻触发条件主观」：本批只补「作者裁决 ＋ 记 `CHANGELOG`」（F8）；是否逐条标注解冻判据的**可测化**留待下次触碰 §四 时评估。
3. 评审 #14（膨胀后下沉 ADR）**暂无动作**：本件 **65** 行 → 本批后 **73** 行；触发＝按 §四「内生必要性自问」评估。（**目录要求**：`文档写作标准` §一.1.3 要求超 **100 行**的常驻文档补「目录」节——本件未达阈值，故免。）
4. **加粗留痕（本批实测，挂 064-6）**：`AGENTS.md` `**` 由 **57 → 89**（本批净 +34：条目标题去加粗 −4 ＋ §六 新增 +6 ／其余为闸门／路径／红线四类）、`运行环境标准.md` 17 → **25**、`测试与验收标准.md` 27 → **37**；三件均属「以表格与编号条目为主要结构者」，按 `文档写作标准` §一.3.8 的适用范围条**兜底阈值不适用**——留痕以防将来被误当回归（069-3 同口径）。
5. **`施工机制` §四 的零依赖纪律句**（`施工机制.md`:82）：本批未动该件，故其「守卫零外部依赖」句未加例外指针（`AGENTS.md` 与 `运行环境标准`／`测试与验收标准` 三处已收口）——触发＝下次触碰 `施工机制` 时补同一指针。
6. **权威顺序的例外面**（F9 ③）：本批只举了「术语叫法以 `术语表` 为准」「域清单以 `架构设计` 为准」两例；若后续出现第三类「明文真源指定」，按同口径补例（不重写本序）。

## 九、审查记录

- 成稿审查分身（fresh、只读）首轮：**findings 共 22：P0:2 P1:9 P2:11｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（22／22）**：

  - **P0-1**＝断言 3 第 1 项的 token 写成**无链接的裸串**，而 F5① 的目标文本是**含 Markdown 链接**的形态 → **两向互卡**（照录落文则断言恒红）→ 改为按落文字面取 token（含链接）并附口径注（同 069 批 001 型）。
  - **P0-2**＝断言 9 的「链接基线 168」**不可复现**（按所给命令实测 **173**，因本规格自身新增 5 条链接）→ 改判据为「断链 **0** 且条数**不低于 173**」并写明来历；**（规划方修后自查又踩同坑一次：重写规格使自身链接 5 → 9，实测 173 → 177，已把判据数同步为 177；**施工期又把目标文本改成描述式（去 9 条链接），实测降为 169，判据数再次同步**——正是本条所指的「基线随规格自身变动」，故判据取「不低于 ＋ 断链 0」。）**
  - **P1 九条**：**P1-1**＝自由度分布写 `[A]×4 [B]×9` 而实为 `[A]×2 [B]×12`（**同族复发**，实跑 python 解析规格表得 `{B:12, A:2}`）→ 改正；**P1-2**＝头注「14 条」与 §一 13 行无法对账（实为 #4 与 #11 同题、去重后 13 项）→ 头注写实；**P1-3**＝F13 缺「英文对应」列值（该表三列硬要求）→ 补齐四个（author／candidate finding／red baseline／negative-path guard）；**P1-4**＝F5② 的示例词「停摆」「守卫二分」**无定义**（F13 未收）→ 示例词收窄为 F13 真定义的四条，术语名收口为「候选类发现（简称候选）」；**P1-5**＝规格三处写「`[2]`–`[5]` 为 TODO 占位」，而**`[4]` 已实现并执行**（实跑可见）→ 全文更正为「`[2]`／`[3]`／`[5]` 占位、`[0]`／`[1]`／`[4]` 已实现」（**事实错，非措辞**）；**P1-6**＝核销表 069 行**编号错位**（「069-3」实为 069 第 4 条）且漏 069 第 3 条（存量加粗留痕）→ 校正并补行；**P1-7**＝068-3 标「本批处置」**无据**（F2–F10 路径列原写「同件」）→ **本规格每行路径列一律改写成仓根可达全路径**，处置遂成立；**P1-8**＝F9 的权威顺序与产品层**具体真源指定**相抵（`术语标准` §1 明写叫法以术语表为准）→ 新增 F9③ 例外句；**P1-9**＝否决 1 的理由句**未获证实**（实跑 `npx --no-install` rc=0）→ 改述为可证事实（见 §六）并保留「冷机未测」的如实声明。
  - **P2 十一条**：F5 位置列不实（`:30` 是空行、表末为 `:29`）、「现件 66 行」实为 **65**、断言 8 上界偏松（**≤13**——落在四文件的编辑恰 13 项）、F11 落点须钉插入点（「（脚本 + lint 工具链）」之后、「。」之前）且**删去复述 §四 的括注**、F7 须**保留**「引入任何此类机制须先登记事故出身」（否则属现行条文净减）、断言 5／6 补**扫描面**口径、固定串断言改 `-F`（本机 `grep` 为 ugrep 封装）、F12 补「失败＝置红条件（候选类发现与明示跳过不算失败）」、§八 的目录要求引文更正为 `文档写作标准` **§一.1.3** 且免目录理由＝「未达 100 行」、⑫ 不采纳理由改述（治理侧无里程碑件；项目阶段在产品层）、F10 逃生口改述为「提交前必跑纪律 ＋ 核验第 1 步 `git status` 对照」→ 全部采纳。
  - **已实核通过项（摘要）**：§一 十三条引述**逐字一致**（`:26` 的 11 类主题名实测 11）；**七词命中各 0**、**无 `package.json`／`node_modules`**、`测试与验收标准` §1.2／§1.3 原文**三项特别核全部成立**；F9 落点层级无冲突（现 5 节 65 行、未达 100 行故免目录）；F13 四术语与既有用法一致（`作者`＝`施工机制` §一、`候选类发现`＝§五.2 原词、`红基线`＝红线 6／§1.2、`负向流程守卫`＝§四 原词）；断言计数项全对（二级标题 5→6、追加组 6→7、四处 token 各 0）；权威顺序**无第三载体**（全仓仅本规格命中）；被改短语的引用面**零命中**（仅 `AGENTS.md` 本体 ＋ 归档件）；F10 四件式完备合规；核销表汇总逐格相符。
  - **未验证（如实声明）**：① 评审报告原文不在仓内，「14 条」与 §一 13 行的映射只能判为不自洽（无法断定漏哪条，故按「去重后 13 项」处置）；② `npx --no-install` 在**无缓存／无安装**的冷机上的行为需联网实测，受「勿联网」约束未做（本机 rc=0 已记入 §六）；③ 施工后 `[4]` 段对本批规格的输出与 lint 结果属施工后事项。

- **施工期 P0（停摆申报，规划方裁定）**：施工方按红线与 §六 停摆纪律**零改动申报**——F4／F5①／F5②／F6／F9 的目标文本里的链接写成 `../standards/…` 等形态（那是**规格自身**在 `docs/specs/` 下的正确相对路径），而它们要**落进仓库根的 `AGENTS.md`**：照落即造 **7 条断链**，与断言 9 的「断链 0」互斥，同时与断言 3 的 token 互卡。施工方给出的实测证据（7 条逐条 `os.path.exists=False`；现存 `AGENTS.md` 的 8 条链接全为仓根相对）与处置建议（**裁定 A**：落文改仓根相对）均成立。**归因＝规划方**：我把「规格自身展示用的相对路径」与「要写入落文的路径形态」混为一谈——**同一批内第二次犯这条**（前一次是三份 `docs/` 链接在规格里写成仓根相对、致 5 条断链）。**处置**：① 采裁定 A；② 在 §二 表后**新立通则口径**（目标文本里的链接一律仓根相对；规格内不写完整链接语法，改以「处加链接 ＋ 目标路径」描述）；③ 断言 3 改「标签串 ＋ 目标片段」组合判；④ F5① 的节名照 `命名标准` 实标题改「§1 目录结构」（施工期 P1-2）；⑤ 断言 4 第四项与 §四.2 的基线由 1 更正为 **0**（施工期 P1-1：现文无「此类」二字）；⑥ F13 的落表形态按施工期 P1-3／P1-4 取定（术语列不加粗、三列对齐、英文逐字入第三列）；⑦ F11 无删除动作（该条现文无复述守卫纪律的句子，施工期 P1-5）。

- **施工（裁定 A 后重落，规划方逐条裁定）**：F1–F14 全落（恰 5 件文件；`AGENTS.md` 65 → 73 行）；施工方申报三条 P1，**全部采纳其处置**：**P1-6**＝断言 3 的 token 与 F5① 的「`命名标准` 加链接」**不可同真**（把链接只包在那四字上会使 `文件清单见` 后的 token 恒 0）→ 采其**最小偏离形态**（链接包住整个指针短语，其余逐字一致）并**追认**，同时把断言 3 的三条 token 改为均不含「右括号＋左括号」连写的形态；**P1-7**＝断言 8 上界 `≤13` 差 1（F5 分两处落点，`-U0` 必计 2 个）→ 上界改 **≤14** 并写明算式；**P1-8**＝断言 9 的「断链 0」**在施工禁改面内不可满足**——两条断链**全在本批规格自身**（通则段文句里的**右括号＋左括号连写**字面、断言 3 命令里的同形字面；扫描器与 `check_spec.py` 均不跳代码块与行内字面——**本批第三次同族，故本规格全件禁用该连写**）→ 规格侧改述（通则段去字面、断言 3 换 token），并使基线与判据自洽。**附带事实**：施工前按同一命令实测为 **171 条／断链 2**（非规格所记 169／0——其 169 是「排除代码块」口径下的可达数），此差异即 P1-8 的量化证据。

- **产物审查修订轮（施工，规划方逐条裁定）**：F15–F19 ＋ P2 五处全落（**改动面 6 件**；`CHANGELOG` 覆盖 F1–F19、396 字符）。施工方申报五条 P1，**全部采纳其处置**：**P1-9**＝F15 的数值须按**施工终态**实测（`AGENTS.md` `**` 57 → **91**、`运行环境标准.md` 17 → **25**、`测试与验收标准.md` 27 → **37**）——`文档写作标准`:108 已按 91 落文，本规格 §八.4／§七 064-4 的留痕同步为终态；**P1-10**＝F6 的引号内落文与断言 4 第 3 项**不可同真** → 取最小偏离（**保留工具身份串**、只删机制句）并**追认**；**P1-11**＝F16③ 原尾句与 ② 新增句语义重复 → 删（净减 1 句）；**P1-12**＝§六 ③ 第三例落**代码跨度、不加链**（避同段两次链向同一目标）；**P1-13**＝F18 的指针落代码跨度而非链接（照字面、无断链风险）。**断言 8 的命令面与上界一并更正**（补 `文档写作标准.md`、上界 18）。

- 施工后核验与双维产物审查：**产物审查已完成（P0:1 P1:4 P2:9，全采纳，见上）**；按止损条 2（P0＝1 < 2）不触发强制复审，终版由规划方实跑核验后归档。
