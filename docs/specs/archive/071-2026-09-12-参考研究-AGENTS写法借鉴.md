# 071-2026-09-12-参考研究-AGENTS写法借鉴

> 定位：点名研读一个外部 skill 仓库（`multica-ai/andrej-karpathy-skills`，MIT，**只读引用**），把它的入口规则文件 `CLAUDE.md` 当**同层载体**与我们 `AGENTS.md` 比对，逐要素裁定「采纳／已覆盖／拒绝」，只把三项**真正缺失的装置**集成进 `AGENTS.md`：**规则自测句**（F1／F2）、**治理件自检信号段**（F3）、**裁量分级指针**（F4）。其余（接入说明、多载体、加粗体例、示例件、出处、安装面、skill 壳形态）逐条记理由**不搬**。
> 自由度分布：[A]×5 [B]×1
> 必读输入：`AGENTS.md`（主改件，**现文 73 行 6 节**）；参考件三件（`CLAUDE.md`／`README.md`／`SKILL.md`，**只读、不装入、不在其上跑任何命令**，红线 3；其关键原文见 §三 末「参考件原文」）；`docs/standards/文档写作标准.md` §一.3.8（加粗四类）／§一.8（禁令须配正向目标，**含「红旗自检」子条**）／§一.9.2（产品层跑通信号，与本批 §七 的判据分工见 F3）；`docs/standards/规格写作标准.md` §5.8（计数式断言须限定扫描面）／§8（立法质量）；`docs/specs/施工机制.md` §二／§五／§八
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

**参考件**（`CLAUDE.md`，2,357 字节；同体另见其 `skills/karpathy-guidelines/SKILL.md`）：标题 ＋ 一句定位（含「Merge with project-specific instructions as needed」）＋ 一句 **Tradeoff** ＋ 四条原则（每条＝命令式标题 ＋ 加粗一句话论点 ＋ bullets ＋ **自测问句**）＋ 结尾 **「These guidelines are working if: …」**（**三条**——其 `README.md` 的 How to Know It's Working 段是**四条**，两者不同，勿混）。逐字原文见 §三 末。

**我方对应物的实测差额**：

| # | 参考件要素 | 我方现状（口径已限定） | 差额 |
| --- | --- | --- | --- |
| ① | 每条规则配**自测问句** | **规则句**形式的自检问句仅 `AGENTS.md`:61「内生必要性自问」一处（全仓另有「自测／自问」字样见 `命名标准.md`:95／`运行环境标准.md`:44／归档件，均他义或实例） | **缺**：红线六条与原则 2／3 为纯禁令 |
| ② | 结尾**跑通信号** | 「跑通信号（It's working if）」只作为**产品层范式**立在 `文档写作标准.md`:90（该节 `:87` 明写适用于 `docs/product/` 各件与 `README.md` 域条目） | **缺**：治理件自身无同类段 |
| ③ | 开头**裁量主轴**一句 | 裁量真源在 `施工机制` §二（轻／标准／重）＋ §五（无裁量：标准与重路径一律送审） | **半缺**：只读 `AGENTS.md` 者看不到主轴（§二 前导语只讲定路径） |
| ④ | 「Merge with project-specific instructions」＋ Customization 示例块 | 欠账 **059-2／060-3／064-1**（「可移植模板形态」，待首次移植时立法） | **不搬**：触发条件未到；本批记参考形态（§八 开放项 1） |
| ⑤ | 同一内容多载体（`CLAUDE.md`／`SKILL.md`／`.cursor/rules/*.mdc` 逐字重复） | `AGENTS.md` §四 明写「各标准引用本节，不复述」＋ `施工机制` §六 反模式 #4 | **拒绝**：多宿主代价（我们宿主唯一） |
| ⑥ | 加粗的一句话论点 ＋ bullets | 红线为「**主题**：正文」形态；`文档写作标准` §一.3.8 把加粗限在「红线／路径／数字／闸门」四类、一段至多一处 | **已覆盖且更强**（主题即论点，且有加粗纪律——**本批新增内容一概不加粗**，见 F3／F4） |
| ⑦ | 独立 `EXAMPLES.md`（八个正反例） | `规格写作标准` §8「立法质量」要求新增／修改条文附实例；**各标准件均有**「正例与反例」节（实测 `grep -lc '^## 二、正例与反例' docs/standards/*.md` → **11/11**；窄域复审自我更正了上一轮的「7 件」——那是把 `grep` 输出接给 `head` 造成的截断）；`施工机制` §六 反模式清单 11 条（错误做法 → 正确方式 ＋ **事故出处**） | **已覆盖且更硬**（每条有事故出处） |
| ⑧ | 逐字引用问题出处 ＋ 原帖链接 | 标准件用〔出处：…〕行、规格与工具头注用「事故出身」 | **已覆盖且更硬** |
| ⑨ | 安装／License 段 | 属分发与安装面 | **拒绝**：归 `README.md`（§四「多宿主适配」条既定分工） |
| ⑩ | `SKILL.md` frontmatter 与 `.claude-plugin/` 两件 | 属 skill 形态与分发面；`命名标准` §2 已有「插件清单（预留）」、`skill形态标准` 辖 frontmatter | **不在本批射程**（将来对齐 skill 壳形态时另批） |

## 二、参考研究裁定表（`施工机制` §八 形态，逐条填写）

| # | 参考条目的主张 | 结论 | 落位或理由 |
| --- | --- | --- | --- |
| 1 | 每条行为规则配一个**当场可问的自测判据**（「资深工程师会说这过度复杂吗」「每一行改动都能追溯到用户请求吗」） | **采纳** | 落 F1／F2。**同向参照＝`文档写作标准` §一.8.3**（「补不出正向目标的硬护栏须同处给出理由与**红旗自检**」）——但该条射程写的是「**各标准件**中『禁止 X』式条文」，而同件 §一.8.2 已把粉线的红线**明文列为「已履行本节义务」**，故本项准确定性是**自愿补强**、不属该条的硬要求、也不是第二载体。原则 1 已内含提问动作（「先问该哪层做」），不重复 |
| 2 | 文末给**可观察信号**而非断言自夸 | **采纳（改形）** | 落 F3：信号**写成现象句 ＋ 真源指针**（不逐字复述判据——避免与本批刚拒的「多载体」同罪，**重复性裁定见本节末**）。**改名**：不叫「跑通信号」而叫「治理件自检信号」，并在节内注明与 `文档写作标准` §一.9.2 的**判据分工**（该处管产品层「作者不打开 skill 即自查」，本处管治理文档「开发 AI／开发者可核对」） |
| 3 | 开头一句**裁量主轴**（偏谨慎；琐碎任务自行裁量） | **采纳（改指针句）** | 落 F4，且**只写指针**：「（裁量分级与送审纪律见 `施工机制` §二／§五）」——**不写「琐碎可自裁量」**（那会与判定表及「不确定属哪条时取较重的一条」相抵），**也不复述「非平凡改动不得逃审」**（与 `施工机制` §五「无裁量」同义，已删）；**不加粗**（该段已有加粗一处，触四类与「一段至多一处」） |
| 4 | 「本文件是基底，与项目专属规则合并使用」＋ Customization 示例块 | **已覆盖（记参考）** | 等价物＝059-2／060-3／064-1「可移植模板形态」；触发条件是首次移植，**现无移植对象**；本批记形态参考（§八 开放项 1） |
| 5 | 同一内容写进多个宿主载体 | **拒绝** | 多宿主代价＋禁复述（反模式 #4）；**反向印证**：本会话两次为「同句两处载体」收口（070 的 npx 例外与机制句） |
| 6 | 靠加粗抓记忆点 | **拒绝** | 加粗四类纪律；读者是 AI，检索优先于记忆点 |
| 7 | 独立示例件（完整 before/after 代码） | **已覆盖** | `规格写作标准` §8 ＋ 多数标准件的「正例与反例」节 ＋ §六 反模式清单（带事故出处）；治理文档用一行 ✅/✗ 更合适。**本批新增条文按 §8 补实例，见 §三 末「立法质量」** |
| 8 | 逐字引用问题出处／原帖链接 | **已覆盖且更强** | 〔出处〕＋「事故出身」 |
| 9 | 安装与 License 段 | **拒绝** | 分发面归 `README.md` |
| 10 | skill 壳 frontmatter 与插件清单形态 | **不在射程** | 属 `skill形态标准`／`命名标准` 预留面 |

**裁定 2 的重复性裁定**（逐条判「新信息 vs 复述」）：① 「数字与锚点可独立复算」在 `规格写作标准` §5.3 有同名要求 → **只写现象（重跑输出与规格所写一致）＋ 指该条，不复制其判据措辞**；② 「`check.sh` 全绿／断链 0／hunk 可归属」在 `施工机制` §三 核验三步有 → **只写现象「拦下问题的是机器与审查，不是自觉」＋ 指该节**；③ 「工具报得出＝机制在起作用」原为循环自证且不可观察 → **改为可核对判据**（本批 `[4]` 段静态发现为 0，或逐条判为预期；连报数批「跳过」而无人核＝空转）；④ 「规格 ＋ `CHANGELOG` ＋ git 历史」＝`施工机制` §三 过程产物两项 → **只写现象「三者能还原为什么这么改」＋ 指该节**。

## 三、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `AGENTS.md` §一（`:13`–`:18`，红线**六条**） | 每条末补「（自测：…）」，**六条为**（逐字，句内不加粗）：① 自测句「这条命令是读还是写？写＝本次确认拿到了吗？」② 「这是产物还是临时件？后者＝在 `.tmp/` 且收工即删吗？」③ 「会让外部目录发生变化吗？会联网跑东西吗？」——**须覆盖该条的联网禁项**（原句只问目录变化，在外部目录跑联网只读命令会漏网）④ 「我改的是原因，还是断言／数字／检查？」⑤ 「里面有没有 token／key／密码／连接串之类字面？」⑥ 「我是跑了 `check.sh`，还是记得它绿？」——**四句内的路径与脚本名一律加反引号**（`.tmp/`／`check.sh`，与同件他处一致，`文档写作标准` §一.3.7）；⑤ 不写隐喻（写实为可检索字面） | §二 裁定 1 | [A] |
| F2 | `AGENTS.md` §五（`:65`–`:67`，三条分工原则） | 原则 2 末补 `（自测：这条发现会挡住谁吗？会＝它到底是不是候选？）`；原则 3 末补 `（自测：这一步落在哪个文件上了？答不出＝该步不存在。）`——原则 1 **已内含提问动作**，不动 | §二 裁定 1 | [A] |
| F3 | `AGENTS.md` §六 之后（新增节）＋ 头注 `:3` 末 | 新增 `## 七、本文件在起作用时（治理文档自检信号）`：含口径注一句 ＋ 四条现象句（各带真源指针）。口径注＝「本节信号用于治理文档（读者＝开发 AI／开发者）；与 `文档写作标准` §一.9.2 的产品层信号（作者不打开 skill 即可自查）判据不同，两者不得互引为据。」；① 「规格里的数字与锚点能被独立复算——重跑一下就与规格所写一致（判据见 `规格写作标准` §一.4／§5）」；② 「拦下问题的是机器与审查，不是自觉——提交前的变动里没有清单外文件（判据见 `施工机制` §三 核验三步）；链接若被改动，断链按当批规格的链接命令现场核（机检段 `[3]` 待建，尚无常驻闸）」——**不得把「断链为 0」挂在 §三 名下**（该节无链接判据）；③ 「工具报得出的与应当报出的一致——`[4]` 段的每条预警都已逐条判为「属预期」或「待处置」、无未核项；若某个检查连续几批只报「跳过」而无人核，先查它是不是空转」——**不得写成「静态发现为 0」**（那是工具的退出契约、恒真＝no-op）也不得写「本批…为 0」（批次叙事＋与实跑相反）；④ 「每批改动可追溯——规格、`CHANGELOG` 一行与 git 历史三者能还原「为什么这么改」（判据见 `施工机制` §三 过程产物两项）」。四条一律不加粗；头注末补「；本文件在起作用时的信号见 §七」 | §二 裁定 2 | [A] |
| F4 | `AGENTS.md` §二 前导语（`:22`） | 句末补「（裁量分级与送审纪律见 `施工机制` §二／§五）」——**一次指源即可**（原句「§二」已在前半句出现，且「非平凡改动不得逃审」与 `施工机制` §五「无裁量」同义重复）；**该句不得加粗**（该行已有一对加粗「动手改任何非平凡内容前」，再加即触 `文档写作标准` §一.3.8 的四类与「一段至多一处」；本批新增内容一概不加粗） | §二 裁定 3 | [A] |
| F5 | `CHANGELOG.md` | 本批一行（≤400 字符），落 `[Unreleased]` › **`### Changed`**，以「AGENTS 写法借鉴（071）：…」起首，覆盖 F1–F4 | 过程产物两项 | [B] |
| F7 | `CHANGELOG.md`:13（070 条内） | 该条写「`文档写作标准`:108 的加粗计数改「**89 处**」」，而活文件现文为 **91 处**、实测 `AGENTS.md` 全件 `**` ＝ 91 → 改「91 处」（**活文件、数字与实测对齐**；归档件 `070` §八.4 记的 89 不可改，按其核销行并列两数处理） | 审查 #6 | [A] |
| F6 | `docs/specs/archive/070-2026-09-12-宪法一致性与修改流程补强.md`:188 | **机械修复一条断链**：`](../product/产品定义.md)` → `](../../product/产品定义.md)`——规格归档位移一层后该相对路径失效（在制位置写对、归档后必错）。**只改路径、不改该件任何文字**；**这是本批唯一的归档件改动，属明文例外（请作者追认，见 §八 开放项 3）** | 审查 P0-1（原为未声明的越界改动） | [A] |

**立法质量（实例，`规格写作标准` §8；择要举例）**——本批共动 13 条条文，下列 8 条各配一例，余 5 条为同构复用：

- **F1 ⑤（凭据）**：反例＝`export ABZU_TOKEN=…` 写在某脚本里被提交；正例＝同值只从环境变量读，脚本里只出现变量名。
- **F2 原则 3（文件账本律）**：正例＝每批收尾两处落点实存（规格含核销与审查记录 ＋ `CHANGELOG` 一行）；反例＝「我在会话里说过了」而仓库里无对应文件。
- **F3 ①**：正例＝本批断言 1 的「8」与「0」两个数由 `grep -c` 当场复制；反例＝规格写「约 10 处」（量词含混、无法复算）。
- **F3 ③**：正例＝`[4]` 段本批报 0 条静态发现；反例＝某检查连续数批「跳过」而无人核（是空转，不是通过）——该例取自本会话 `066` 那类假绿的教训。
- **F1 ①（git 默认只读）**：反例＝`git push` 前没问过作者（即便上一批刚提交过，本次仍是新的一次写操作）；正例＝提交前单独取得本次确认。
- **F2 原则 2（候选永不拦截）**：正例＝`check.sh` [4] 段报预警而退出码仍 0；反例＝把候选行改成 `x` 前缀并置退出码 1（那就成了失败而非候选）。
- **F3 ②**：正例＝本批 `git status` 里除规格与归档件那一行外无清单外文件；反例＝提交前出现一个「顺手改」的文件而无人核。
- **F4**：反例＝把两份文件的措辞改动按「琐碎」自裁量走轻路径（判定表判「跨文件 ≥2 文件」＝标准行，不得自裁量）。

**参考件原文（逐字，供施工与审查核对；成稿审查分身当时网络不通、未读到本体，本处补齐）**：

- 定位句：`Behavioral guidelines to reduce common LLM coding mistakes. Merge with project-specific instructions as needed.`
- Tradeoff 句：`These guidelines bias toward caution over speed. For trivial tasks, use judgment.`
- 结尾段：`These guidelines are working if: fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, and clarifying questions come before implementation rather than after mistakes.`（**三条**）

## 四、验收断言（含预验记录）

### 断言 1｜自测句八处在位且未外溢

```bash
grep -c '（自测：' AGENTS.md
grep -c '（自测：' docs/standards/*.md docs/specs/施工机制.md
```

期望第一项 **8**（红线六 ＋ 原则 2／3 各一）；第二项 **0**（不铺到标准件）；预验基线 0／0。

### 断言 2｜自检信号节在位（含节序、头注与常驻预警说明）

```bash
grep -c '^## 七、本文件在起作用时（治理文档自检信号）' AGENTS.md
grep -c '^## ' AGENTS.md
grep -cF '本文件在起作用时的信号见 §七' AGENTS.md
grep -cF '判据不同' AGENTS.md
```

期望 **1**／**7**（原 6 节 ＋ §七）／**1**／**≥1**（口径注在位）；预验基线 0／6／0／0。**口径说明**：`[4]` 段对本批的预警属预期——其 token 多为计数式正则（如行首标题计数），不在目标文本内，施工后按人工核对，非失败。

### 断言 3｜裁量指针句在位

```bash
grep -cF '裁量分级与送审纪律见 `施工机制` §二／§五' AGENTS.md
python -c "import io;l=[x for x in io.open('AGENTS.md',encoding='utf-8') if '裁量分级与送审纪律见' in x][0];print(l.count('**'))"
python -c "import io;print(io.open('AGENTS.md',encoding='utf-8').read().count('**'))"
```

期望 **1** ／ **2**（**该行恰一对加粗**——原有的「动手改任何非平凡内容前」；新句不加粗）／ **91**（全件计数不变，与 §八 070-4 的核销理由互证）；预验基线 0 ／ 0 ／ 91。另核 **F7**：

```bash
grep -cF '加粗计数改「91 处」' CHANGELOG.md
grep -cF '加粗计数改「89 处」' CHANGELOG.md
```

期望 **1** ／ **0**；基线 0 ／ 1。

### 断言 4｜禁改面零改动（明文例外除外）与改动面归属

```bash
git status --porcelain docs/specs/archive docs/product scripts | grep -v '070-' | wc -l
git diff --stat docs/specs/archive/070-*.md
git diff -U0 AGENTS.md | grep -c "^@@"
```

另补一条**精确面**（防 `grep -v` 掩蔽面偏松——将来若误建同前缀文件会被一并滤掉）：

```bash
git diff --name-only docs/specs/archive | wc -l
```

期望：第一项 **0**（除 070 归档件那条路径外零改动）；**本条 1**（归档件改动恰一件）；第二项＝**1 file changed, 1 insertion(+), 1 deletion(-)**；第三项 **≤ 10**（F1 红线六条相邻、`-U0` 下或合 1–2 个 hunk、F2 一处、F3 两处（新节 ＋ 头注）、F4 一处；逐 hunk 走查归属，无表外行——**取计数、不看退出码**）。预验基线 0／0／0。

### 断言 5｜链接与守卫（依赖声明）

全仓相对链接**断链 0** 且条数**不低于 175**（＝施工前实测；**该「断链 0」依赖 F6** —— 不回退 F6 才成立）：

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split('\\0');f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md')];L=[(p,u) for p in f for u in re.findall(r'\\[[^\\]\\n]*\\]\\(([^)\\s]+?)\\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print('链接',len(L),'断链',sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

`bash scripts/check.sh` 退出 0；`npx markdownlint-cli2 "**/*.md"` 0 issue。

### 断言 6｜过程产物两项

```bash
grep -cF 'AGENTS 写法借鉴（071）' CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if 'AGENTS 写法借鉴（071）' in l][0].rstrip('\\n')))"
```

期望 ≥1／≤400；验收通过后本规格移入 `docs/specs/archive/`。

## 五、规格自审（三条）

1. **锚点现场核过**：① 参考件三处逐字原文已补入 §三 末（本机可读 `raw.githubusercontent.com`；**成稿审查分身当时连接超时、未读到本体**，其「三条还是四条」之疑由此消解——`CLAUDE.md` 结尾段是**三条**，其 `README.md` 的 How to Know It's Working 是**四条**）；② 我方锚点：`AGENTS.md` **73 行 6 节**（`awk 'END{print NR}'` 实测；先前记「74 行」系 `split('\n')` 含尾空元素所致）、红线六条与原则三条原文、`:22` 前导语、`:61` 内生必要性自问；③ **唯一性声称已限定口径**（规则句形式的自检问句仅 `:61` 一处；跑通信号作为**范式**仅 `文档写作标准.md`:90 一处，其余字样为他义／实例／归档件）；④ `文档写作标准` §一.8.2／§一.8.3 原文（红线视为已履行正向目标义务；硬护栏须给理由与**红旗自检**）——F1／F2 的依据；⑤ `064`：「§八」是其审查记录，该条在 **§七 6**（066 表记作 064-6）；⑥ **本批对参考件只读**（未 clone、未装入、未在其上跑命令）。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–6 基线已实跑（`（自测：` 0／0；`^## 七、` 0／行首 `##`（后跟空格） **6**／头注 0／口径注 0；裁量句 0；禁改面 0、070 归档件 diff 恰 1＋1−、AGENTS hunk 0；链接 **175／断链 0**；CHANGELOG 0）。逐条核**断言 token 均落在 F 项目标文本内**（自测句取「（自测：」前缀；信号节取节标题整串；裁量句取整串（**不含加粗标记**——本批新增内容一概不加粗，与断言 3 的第二项「该行恰一对加粗」互证））；**F6 的 token 是箭头两侧的路径串**，施工后须 `grep -c` 复验两串各 0／1。
3. **禁止事项已写明**：见 §六。

## 六、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/product/`、`skills/`（未创建）、`scripts/` 全部、`docs/standards/`（**不铺自测句**）、`docs/specs/施工机制.md`、本批规格自身。
- **归档件例外（本批唯一）**：`docs/specs/archive/` 原则只读；**唯一例外＝ F6 对 `070` 归档件 `:188` 那一行的路径修复**（只改 `../` 为 `../../`，**不得改该件任何文字**）。除此之外归档件零改动。
- 禁止动作：**对外部参考件的任何写操作**（不 clone、不装入宿主、不在其上装依赖或跑命令——红线 3）；施工期**不得联网**（参考件原文已抄入 §三 末）；安装依赖；在源仓库内跑分发／安装工具。
- 禁止越权：不改版本号与 tag；不新增文件（本批改**三件**既有文件：`AGENTS.md`／`CHANGELOG.md`／`070` 归档件一行）；**不建台账／不建索引件**。
- 禁止改断言、改数字、删检查来变绿；**不得**借本批夹带别的改动（尤其**不得**顺手立 059-2 的「可移植模板形态」，见 §八 开放项 1）。

## 七、被否决的替代方案（可选）

- **按参考件把 `AGENTS.md` 改写为「四条原则」式准则集** → 否决：我们的文件是**入口与路由 ＋ 宪法级约束**（读者是开发用 AI，检索优先）；照搬会把路由、不做清单、分流指针挤出文件，与头注自述冲突。
- **把自测句铺到标准件** → 否决：本批只动 `AGENTS.md`；标准件的判据已在机检锚点与正反例里，再铺一层即第二载体（断言 1 第二项钉住该边界）。
- **F6 抽成独立轻路径批** → **本批取并入方案（明文化）**：两条路线都要作者就「归档后修改」这一**实质例外**表态（`施工机制` §七 定归档件原则只读），而**并入的好处是例外随本批规格留存、可追溯**（抽批则例外落在别批的核销／开放项里，与本批的断链判据分离）；F6 只有 1 行路径、与「归档位移」这一机械因果同源，故：故**并入本批并在 §六 开明文例外 ＋ §八 开放项 3 记录 ＋ 报告请作者追认**。若作者不追认，则回退 F6 并把断言 5 的判据改为「断链 ≤1（070 归档件机械损坏，待专批处置）」。
- **照抄参考件的 Tradeoff 段（两句并列）** → 否决：我们已有分级分流，并列两套裁量口径会冲突；只取不重复的指针句（F4）。
- **沿用「跑通信号」这一名称** → 否决：`文档写作标准` §一.9.2 的「跑通信号」有**特定判据**（不得拿内部合规检查冒充）且限产品层；同名异判据会误导，故改名并加口径注（F3）。

## 八、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 070-1 | 评审 #12（项目一句加状态／里程碑链接）不采纳 | 仍待 | 本批不动该句；触发＝出现治理侧里程碑件 |
| 070-2 | 解冻触发条件的可测化（逐条标注） | 仍待 | 本批新增的自测句与信号节**均不涉解冻**；触发＝下次触碰 §四 时评估 |
| 070-3 | 评审 #14（膨胀后下沉 ADR）暂无动作 | 仍待 | `AGENTS.md` **73 → 82 行**（`wc -l` 实测；仍 < 100，目录要求仍免）；触发＝按 §四「内生必要性自问」评估 |
| 070-4 | 加粗留痕（挂 064-6） | 仍待 | **归档原文记「57 → 89」、终态实测 91**（070 批 P1-9 已判、归档件不改，故此处并列两数以免将来对账误判）；**本批新增内容一概不加粗**，计数不变（仍 91）；触发＝下次触碰该件时按存量记 |
| 070-5 | `施工机制` §四 的零依赖纪律句未加例外指针 | 仍待 | 本批不动 `施工机制`；触发＝下次触碰该件时补指针 |
| 070-6 | 权威顺序的例外面（补第三类明文真源指定） | 仍待 | 本批新增 §七 不进权威顺序；触发＝出现第三类真源指定 |
| 064-1 | 底座「可移植模板形态」（与 059-2／060-3 同题） | 仍待 | **本批裁定不搬**（参考件的「合并使用 ＋ Customization 示例块」即其最小实现，已记 §九 开放项 1 作形态参考）；立法触发条件不变（首次移植） |
| 064-4 | 跨层计数同类排查结论（「将来新增跨层计数即回本项」） | 仍待 | 本批 §一 表引的字节数／行数属**规格内锚点**，非「底座复述他层数字」，不触发该项 |

**汇总**：按条计 8 条：已履行 0｜作废 0｜本批处置 0｜仍待 8｜待作者 0 ＝ 8 ✓（逐格可复核）。**范围注**：本批改动面＝`AGENTS.md`／`CHANGELOG.md`／`070` 归档件一行；066–069 的条目落点在 `施工机制`／`规格写作标准`／工具，未被触及，以其各自核销表为索引。

## 九、开放项

1. **可移植模板形态的参考（064-1／059-2／060-3）**：参考件写法可作形态参考——① 开头一句「本文件是基底，与项目专属规则合并使用」；② 一处 `## 本项目规则` 槽位示例。**触发条件不变**（首次移植到另一项目时立法）；本批不立，仅记指认以免将来重找。
2. **自测句的铺开边界与覆盖口径**：本批只给 `AGENTS.md` 的红线与分工原则配自测（8 处）；标准件未铺。**覆盖口径＝取该条最易失守的一环，不求逐句全覆盖**（如红线 2 的「`test_*` 前缀」、红线 5 的「`.env` 入 git」、红线 6 的「有红先呈报」三处正文要点未被问句直接覆盖，属有意取舍）——免得后来按「全覆盖」把它们判成缺陷。若出现「条文读了但不当场校验」的实例，按类评估（落点候选：各标准件条文行内括注）。
3. **归档件机械修复的纪律例外（待作者追认）**：F6 修的是**归档动作本身**造成的断链（相对路径基准位移一层），非改记录内容；本批按「明文例外 ＋ 只改路径」处理并请作者追认。**根因未除**——候选解法：① 规格内跨件引用一律用文字形式（`` `产品定义` §七 ``，不用 Markdown 链接）；② 或归档动作附带统一改写（属机制，须先过 §四「内生必要性自问」）。触发＝再出现第二例。**闸与配套**：① **追认＝本批提交前必过闸**（未获追认不得提交）；退路执行者＝规划方（回退属 git 写操作，须作者确认）；② 若走退路，§五.1／§五.2 的「链接 175／断链 0」须同步改为「断链 ≤1（070 归档件机械损坏）」，并在 §一 表后**新增一行**记该断链为既存状态（原写「§一 现状锚点的『既存状态』」——该子节不存在，属悬空落点，此处改正）；③ **本例外属「改规则」**（`施工机制` §七 归档件纪律的第二个例外）：其登记须走标准路径，而本批 §六 禁改 `施工机制.md`，故**留待下一批触碰该件时补一笔**（否则例外的依据只存在于本批规格里，将来无处可查）；④ 先例对照：`064` §七 第 5 条对同类「归档位移致悬空引用」**选择不改**（属历史记录）——本批与之不同之处在于**该引用会致全仓断链 1 条**（064 那处只是指向失效、不成链），故按「机械损坏可修、只改路径」处理并请作者裁量。
4. **自检信号的自校验**：§七 四条现为**人读判据**（谁核对、多久核一次未定）；若两批后仍无人据此核对，考虑在核验请求模板加一问（「本批是否出现 §七 四条之一的**反例**」）。

## 十、审查记录

- 成稿审查分身（fresh、只读）首轮：**findings 共 20：P0:2 P1:10 P2:8｜结论 修订后通过**。规划方逐条复核取证后**全部采纳（20／20）**：

  - **P0-1**＝**F6 原是未声明的越界改动**：070 归档件的机械修复既在「禁止触碰」列内、又不在改动清单里 → 按核验第 1 步属「清单外文件＝P0 打回」；且断言 5 的「断链 0」隐性依赖它 → **改法＝明文化**：F6 入改动清单（改动面 2 → 3 件）、§六 开明文例外、断言 4 与 5 写明依赖、§九 开放项 3 记录并**请作者追认**；不追认则回退 F6 并改断言 5 判据。
  - **P0-2**＝**核销表必列面缺行 ＋ 编号错位**：070 归档件 §八 实为**六条**（我误记四条，且「070-4」实为其第 6 条），漏了第 4 条（加粗留痕）与第 5 条（`施工机制` 零依赖句），另 064-4 亦属该面 → **补齐为 8 行**、汇总逐格相符；**本批新增内容一概不加粗**，故 070-4 的计数不变（仍 91）。
  - **P1 十条**：**#3** 自由度分布写 `[A]×1 [B]×4` 而五格全 `[B]` → 改 **`[A]×5 [B]×1`**；**#4** 新增条文未按 `规格写作标准` §8 附实例 → §三 末新增「立法质量」五例；**#5** F4 的加粗会与同段既有加粗撞「一段至多一处」且不属四类、F3 的加粗亦未归四类 → **F3／F4 一概不加粗**；**#6** §七 与既有条文重复（①＝§5.3、②＝核验第 1 步、④＝过程产物两项）而我们刚以「不复述」拒参考件 → **逐条给重复性裁定**（改现象句 ＋ 指针）；**#7** 「跑通信号」与 `文档写作标准` §一.9.2 同名异判据（该条明禁「拿内部合规检查冒充」）→ **改名「治理件自检信号」＋ 口径注**；**#8** 原信号③ 不可观察且循环自证（且与本会话实证相反）→ **改为可核对判据**；**#9** F4 的「琐碎可自裁量」与判定表及「取较重」相抵 → **改指针句**；**#10** 「全仓仅一处」类唯一性声称与现场不符 → **限定口径**；**#11** F1／F2 与 `文档写作标准` §一.8.3「红旗自检」同向却未引为依据 → **补引**；**#12** 「74 行」失实（实为 **73**）→ 两处改正。
  - **P2 八条**：F1 的「⑥ 条为」歧义 → 改「六条为」；断言 4 算式漏头注那一个 hunk → 补；F5 未指明 `CHANGELOG` 小节 → 写明 `[Unreleased]` › `### Changed`；「原则 1 已是问句」失准 → 改「已内含提问动作」；断言 2 的常驻预警未声明 → 补口径说明；裁定 ⑦ 的「各标准有正例与反例节」不实（7／11）→ 改「多数」；F1⑤ 的「钥匙」隐喻 → 写实为可检索字面。
  - **已实核通过项（摘要）**：参考件元数据（9 文件、`CLAUDE.md` 2,357 字节）与结构描述属实；我方锚点逐字相符（含 `文档写作标准.md`:87 确写「适用于 `docs/product/` 各件与 `README.md` 域条目」）；裁定表 10 行落点验真（⑤⑥⑦⑧ 四处「已覆盖」站得住）；`064-1` 与归档原文一致；全部断言基线可复现；§九 开放项 3 的记录与 070 归档件现文一致。
  - **未验证（如实声明）**：① 审查分身**未能读到参考件 `CLAUDE.md` 本体**（各镜像连接超时／JS 渲染）——其「结尾段或为四条」之疑由规划方以本机读取的**逐字原文**消解（`CLAUDE.md` 三条；其 `README.md` 四条），原文已补入 §三 末；② F3 施工后 `[4]` 段的实际输出属施工后事项。

- 按止损条 2（成稿审查 P0≥2）：修订后已走**窄域复审**（只核原 P0 修复项 ＋ 直接影响面，逐条实跑自证）：**原 P0 修复 2/2｜新发现 P0:1 P1:0 P2:6｜结论 修订后通过**。处置：**N1（新 P0）**＝F4 的目标文本自带加粗，与本批「新增内容一概不加粗」、断言 3 的走查判据、070-4 的核销理由**三方互斥** → 采其最小修：F4 目标文本与断言 3 的 token **去加粗**、§五.2 改述为「不含加粗标记」，并补「全件 `**` 计数须仍 91」为第三判据；**N2**＝断言 4 的 `grep -v '070-'` 掩蔽面偏松 → 补精确面 `git diff --name-only docs/specs/archive | wc -l` ＝ 1；**N3**＝070-4 行的数与其归档原文（89）不一致 → 并列「原文 89／终态实测 91」并注明归档件不改；**N4**＝§九 开放项 3 的退路缺配套 → 补「追认＝提交前必过闸、退路执行者＝规划方（需作者确认）」与基线句同步；**N5**＝立法质量只配 5／13 条 → 题名改「择要举例」并补至 8 条；**N6**＝裁定 ⑦ 的计数由「7/11」更正为 **11/11**（复审自我更正：前报系 `grep … | head` 截断；规划方独立复跑 `grep -lc` 得 11 佐证）；**N7**＝新节名「治理件」与既定术语「治理文档」不一致（且 `AGENTS.md`:58 的「多人协作治理件」是另一指称）→ 全批统一为「治理文档」。复审另核通过项：§七 四条已消「复述」之嫌、口径注与 §一.9.2 的判据分工相符、自由度与断言基线全通、**F6 的未提交态不影响守卫与链接扫描**（`check.sh` 无链接段、lint 41 件 0 issue）。**未验证**：§三 末参考件原文与「本机可读」之说复审方仍无法独立复核（其网络 7 次失败），已按其建议保留「由规划方本机读取」的出处口径。
- **双维产物审查分身**（fresh、只读）：**findings 共 10：P0:1 P1:5 P2:4｜结论 修订后通过**（P0＝1 < 2，按止损条 2 的对称条款不触发强制重派）。规划方逐条复核取证后**全部采纳（10／10）**：
  - **#1（P0）**＝§七 第三条「本批 `[4]` 段的静态发现为 0」**两读皆错**——读作工具契约「静态发现恒 0」＝恒真 no-op（正是前版被判「循环自证」的同一病，修订未除根），读作字面「发现数为 0」则**与实跑相反**（`check.sh` [4] 段实报 10 条预警）；且「本批…」属**批次叙事**（`规格写作标准` §九 禁）。→ 改写为无批次、可当场核的「工具报得出的与应当报出的一致——`[4]` 段的每条预警都已逐条判为『属预期』或『待处置』、无未核项」，并在 F3 行写明**两处禁写**。
  - **P1 五条**：**#2** 070-3 行「本件 73 → 约 79 行」属目视估计且失实（实测 **82**，`wc -l`）→ 改「`AGENTS.md` 73 → **82** 行」；**#3** §七 第二条把「断链为 0」挂 `施工机制` §三 名下**指空**（该节无链接判据、`check.sh` 亦无链接段）→ 拆谓词、把断链改为「按当批规格的链接命令现场核（机检段 `[3]` 待建）」；① 的指针补 §一.4；**#4** 红线 3 的自测句**漏「跑联网命令」**（在外部目录跑联网只读命令不改目录却正是禁项）→ 问句补「会联网跑东西吗？」；**#5** F6 的例外依据只在本批规格、且退路落点「§一 现状锚点的『既存状态』」**不存在** → 开放项 3 补「本例外属改规则，其登记留待下一批触碰 `施工机制` 时补一笔」＋修正落点为「§一 表后新增一行」＋先例对照（064 §七 5 对同类选择不改，本批差异在于该引用致全仓断链）；**#6** 070 的 `CHANGELOG` 条写「89 处」而活文件现为 **91 处**（非本批造成，但本批承诺并列两数以免误判）→ 新增 **F7** 改活文件那处。
  - **P2 四条**：**#7** F4 同句两次指 `施工机制` §二、「非平凡改动不得逃审」与 §五「无裁量」同义复述 → 改「（裁量分级与送审纪律见 `施工机制` §二／§五）」；**#8** 自测句内的 `.tmp/`／`check.sh` 未加反引号（违 §一.3.7，同件他处均带）→ 补；**#9** 三处自测句属**部分覆盖**（非 no-op 亦非问错）→ 在 §九 2 明写「取该条最易失守的一环，不求全覆盖」的覆盖口径；**#10** 裁定 1 把 §一.8.3 称作「依据」偏强（该条射程是「各标准件」，且 §一.8.2 已把红线列为已履行）→ 改「同向参照」并写明属自愿补强。
  - **已实核通过项（摘要）**：断言 1–6 逐条复现；新增行 `**` 30 ＝ 删除行 30（净 0，全件 91 与 `文档写作标准`:108 一致）；§七 口径注与 `文档写作标准` §一.9 的适用范围／判据句相符；术语「治理文档」全批统一且未与 `AGENTS.md`:58 的「多人协作治理件」混用；§八 核销表 8 行抽核 3 行有据、必列面齐、汇总逐格相符；F4 与 §二／§五 不冲突；规格自洽（20＝2＋10＋8、窄域 P0:1 P2:6）；全仓无第三载体需同步。
  - **`[4]` 段残留一条属预期**：工具仍报一条 `[A] 未落实或已漂移: 非平凡改动不得逃审 @ AGENTS.md`——该串现仅作为**记录与理由栏里的引文**出现（§二 裁定 3 行与 §十 #7 条引旧措辞说明「已删」），**不是改动清单的目标文本**（F4 行现文为「（裁量分级与送审纪律见 `施工机制` §二／§五）」）。非阻断，不为消灭该预警而改记录措辞（记录须如实引用旧串）。

  - **未验证**：参考件本体（不在仓内、本会话禁联网）——其原文由规划方**导入时本机读取**并抄入规格 §三 末；070 归档件「89 vs 91」的成因（`git log -S` 零命中，判为 070 批内部不自洽，归档件不可改，以本批核销行并列两数处置）。
