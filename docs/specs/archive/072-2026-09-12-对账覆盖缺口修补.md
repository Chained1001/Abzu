# 072-2026-09-12-对账覆盖缺口修补

> 定位：修「规格静态自检器」**两处对账覆盖缺口**——① 零命中核对的期望取数在「期望子句多值」时**整条静默跳过**（跳过不可见＝该断言的零命中核对没跑，而屏上没有任何痕迹）；② 检查 B 的量词只认「行／字符」，「某件现 91 处（加粗）」这类计数声称**根本不进判定面**（070／071 咬人的正是这一类）。两处缺口**都已在 `测试与验收标准` §4 登记**（G15／G2）且两条触发条件**均已到达**——本批是按触发的响应。F1 取**两支路**：K＝M 按序配对（治「配对」）＋ K≠M 多值时产出**「未判」候选行**（治「静默」——真语料上被它照亮的实例是 `067`／`069`／`070` 三处，见 §一②）；F2 的判据按实测**收紧了一档**（宽松闸在归档语料上假阳性 3／7，收紧后 0）。另把由此得到的一条写法纪律落进 `规格写作标准` §5。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**——改动含工具判定逻辑、新增规范条目（§5 第 9 条）、改两条缺口行；除 `CHANGELOG` 留痕外恰三件，但**不属**「轻」行的「纯措辞／格式」或「复盘驱动的治理微调」（豁免要求「微调」，本批改判定逻辑与判定面）。
> 自由度分布：[A]×1 [B]×5
> 必读输入：`scripts/check_spec.py`（`:36`–`:37` 模块 docstring 载体说明 ③／`:71`–`:73` `CLAIM` 与其上方注释／`:79` `WHOLE_MARKS`／`:456` `expect_re`／`:461` `expect_stops`／`:463` `literal_token`／`:492` 零命中门／`:496`–`:507` 期望解析／`:536` `_whole_size()`／`:542` 字符早退分支／`:580`·`:603` 两处调用点／`:607` 取实测值／`static_report()` 的 `findings` 收列表，行号以现文为准）；`docs/standards/测试与验收标准.md` §4 已知缺口表（**G2**`:68`／**G15**`:77`——F4 的逐字核对面）；`docs/standards/规格写作标准.md` §5（断言写法，现 8 条）／§8（立法质量）；`docs/specs/archive/067-*.md` §八（1–5 五条开放项原文：F1 对应 **3**，非 4）＋`:100` 起的三命令断言；`docs/specs/archive/068-*.md` §七（067-3＝「同类误报再现时改按命令序号映射」）；`docs/specs/archive/069-*.md`:86 起、`docs/specs/archive/070-*.md`:60 起（同型实例）；`scripts/check.sh`（[4] 段调用形态）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 零命中核对的**取数规则**（`check_swallow()`，`:496`–`:507`） | 窗口＝命令所在行起（含本行）其后 4 行；取窗口内**首个**含「期望」的行；分配型句（命令序数分配／「各命令」／「分别」）`break` 不判；否则取「期望」子句（止于 `；`／`。`／`（预验`／行尾）内数字，**全同才取、不唯一即跳过**——**跳过无任何输出**（G15 的保守化） |
| ② | 该规则与**多期望值断言**相遇即静默（缺口一） | 夹具 `.tmp/072/fixture-triad.md`（3 条命令、第二条 token 实为 0 命中、期望行「期望 0 ／ 1 ／ 0」）实跑：**无「零命中待复核」行**（子句数字 `{0,1}` 不唯一 → `exp=None` → 整条不判）；同型对照 `.tmp/072/fixture-single.md`（1 命令、「期望依次 ≥1／×5」）实跑：**有**该行 ✓。**真规格实例**（本批实测，即 F1 ② 支路将照亮者，也是 §七 各该批核销表的落点）：`067`:100 起的三命令断言（子句 `[1,0,0]`、K＝3，该规格 M＝6）／`069`:86 起的二命令断言（`[1,0,1,1]`、K＝4，M＝18）／`070`:60 起的二命令断言（`[1,0]`、K＝2，M＝21）——**三处皆 K≠M** |
| ③ | 取数所需的三个量都取得到 | `refs = _assert_refs(text)`（长度 M＝断言数）；主循环 `for (token, target), line_no in zip(refs, located)`——序号 `i` 随手可得（现未用）；子句数字按 `re.findall` 得**有序列表**（现仅用于 `set` 判全同） |
| ④ | 检查 B 的量词面（缺口二） | `CLAIM = re.compile(r'(?<![A-Za-z0-9])(\d[\d,]*)\s*(字符\|行)')` —— **只有「字符」与「行」**；`_whole_size()` 的整件口径判据＝量词为「字符」即真（`:542` 早退）／数字前紧邻文本含 `WHOLE_MARKS`（全文／本件／实件／总行数）／「共 N 行」／同行 `wc -l` 类命令 |
| ⑤ | 缺口二的现场（活文件） | `docs/standards/文档写作标准.md`:108 载「（`AGENTS.md` 现 **91 处**属红线加粗，非赘述……）」；`AGENTS.md` 的 `**` 实测 **91**（命令：`python -c "print(open('AGENTS.md',encoding='utf-8').read().count('**'))"`）——现值正确；但 070 的 `CHANGELOG` 同句曾写 **89**，工具**全程未报**（现量：`--static docs/standards/文档写作标准.md` 输出「B：未提取到可判定的尺寸声称」） |
| ⑥ | 两处缺口的**共同点** | 都不是「判据错」而是**覆盖缺口**：判据在其覆盖的面内是对的（071 §十 已记「不为消灭预警改文本」），缺的是「该进这个面却没进」——故本批属**扩面**，不是放宽 |
| ⑦ | 「处」的闸**须收紧**（预验实测定） | 宽松闸（数字前紧邻文本含 `**` 或「加粗」即判）在 `docs/specs/archive/` 059–071 上 **7 条判定 6 条报到，其中 3 条是假阳性**（`063`:70「全仓引用共 **6 处**」、`064`:14「其中 1 处为 `**/*.md`」、`070`:45「本批给该件加 16 处加粗」——数字的**对象**分别是引用面／glob／增量，与加粗计数无关）。**收紧后**（见 F2）：活文件 **1 条判定 0 条报到**（即 ⑤ 那条，值 91 与实测一致）、归档 **4 条判定 3 条报到且全部是真阳性历史值**（`061`:235 与 `062` 的「59 处」、`070`:45 的「57 处」——均为 AGENTS.md 加粗数的当时实测，与 064 核销所言一致） |
| ⑧ | 两处缺口**都已登记、触发条件都已到** | `测试与验收标准` §4：**G15**「F2 的聚合期望句（数字不唯一）不判」／触发＝「同类误报再现时改**按命令序号映射**」（原登记见 067 §八.**3**，068 §七 记作 `067-3`）；**G2**「计数重算量词扩展（`条`／`项`／`处` 类…未覆盖）」／触发＝「待出现漏报后**按类扩**」——070／071 已现漏报，本批即扩第一类（`处`）。故 F4 是**按触发就地收窄既有两行**（067 F7 已就 G15 预登记该触发），不新增缺口行 |
| ⑨ | 一条写法纪律的出身 | F1 的按序配对依赖「数字序＝命令序」——**写反即静默误配**（比漏判更难发现；G15 的保守化本是为防误报）；故配 F3 一条写法纪律，与 §5 既有 8 条同型 |
| ⑩ | 另两个可分辨的面 | ① **窗口拾取**：取数行＝窗口内**首个**含「期望」的行，理论上可能取到邻近断言的期望行（现语料无实例，记 §八 开放项 3）；② **配对支路的真语料触发面为零**（归档 14 件＋在制 1 件实测皆 K≠M）——配对由合成夹具见证，记 §八 开放项 5 |
| ⑪ | 新前缀行须**计入**末尾三态 | `static_report()` 的 `findings = warn + zero + sentinel + extra + counts`（显式列举前缀）——新前缀「· 零命中核对未判」不在其中。该函数注释明写「零命中待复核（F2）同属检查 A 的发现项：**漏计则「· 有预警 N 条」少计、屏上发现行多于计数行**」（067 批已为同类问题修过一次），故 F1 须同步把该前缀补入收列表（**否则新支路的产出在汇总里隐形**） |

**不变量（施工前后须同）**：`docs/specs/archive/`、`docs/product/`、`skills/`（未创建）、`scripts/check.sh`、`scripts/hooks/` 零改动；`check_spec.py` 除 F1／F2 点名面外零改动（含检查 A 的 hit 门与自噬判读、检查 B 的 `WHOLE_MARKS`／「共 N 行」／`wc -l` 判据与「条／项／个」不扩、检查 C／D、`static_report()` 的**空转明示与三态判据文本**（仅允许 F1 点名的 `findings` 收列表补一个前缀）、退出契约、「静态发现恒 0」）；`docs/standards/` 除 F3／F4 点名两件外零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py` `check_swallow()`（`:492`–`:507` 一带，含该函数 docstring 的取数面描述与例）＋ `static_report()` 的 `findings` 收列表一处 ＋ 模块 docstring 用途段的检查 A 子输出枚举（`:4`–`:5`，补「零命中核对未判」） | **期望取数两支路 ＋ 未判行计入三态**。取数按**下列先后**判定（顺序即口径，不得颠倒）：① 取子句数字有序列表 `nums`（截断规则同现文）；② `len(nums) == M` → 取 `nums[i]`（i＝该断言在 `refs` 中的序号，**0 基**；主循环改 `enumerate`）——此支路**不做「全同」要求、也不按分配型词面跳过**（该形态要求**每个数字各对一条命令**）；③ `len(nums) != M` 且该行是分配型句（`distrib_re`／「各命令」／「分别」）→ **不判**（现行为不动）；④ `len(nums) != M` 且子句数字**全同** → 取该值（现行为不动）；⑤ 其余（K≠M 且不全同）→ **不再静默**，产出 `· 零命中核对未判（期望子句多值 {nums}，与命令无法对齐）: {token} @ {target}`（`nums` 按出现顺序列举）。`static_report()` 的 `findings` 收列表补入该前缀（与「· 零命中待复核」同理——漏计即屏上发现行多于计数行）。三道门（`exp >= 1`／`cur == 0`／字面集 `literal_token`）与窗口宽（其后 4 行）**逐字不动**；docstring 内「全同才取之、不唯一则跳过」一句按新口径改写 | ①②③⑧⑪ | [B] |
| F2 | 同件 `CLAIM`（`:73`）＋ `_whole_size()`（`:536` 起含其 docstring）＋ `check_counts()` 取实测值处（`:607`）＋ `CLAIM` 上方注释（`:71`–`:72`）＋ 模块 docstring 的载体形态说明 ③（`:36`–`:37`） | **量词扩「处」并收紧口径**：`CLAIM` 量词组补 `处`；`_whole_size()` 对 `unit == '处'` **仅当紧邻段（整个 `seg`）含「加粗」，且数字之前紧邻文本（`seg[:start]` 去尾空白后）**不以「加」／「增」收尾**（增量式不判）时**判为整件口径，否则降为候选（只计数、不呈报）；取实测值处补 `unit == '处'` → `content.count('**')`（加粗标记出现次数，与 `文档写作标准` §一.3.8 计数口径一致）；上述注释、模块 docstring 与 `_whole_size()` docstring 的「仅当该数字**之前**的紧邻文本」按新口径改写（「处」看**整段**）。**「条／项／个」不扩**（见 F4） | ④⑤⑥⑦⑧ | [B] |
| F3 | `docs/standards/规格写作标准.md` §5（`:65` 之后追加为第 9 条） | 补「**一条断言只配一条命令与一个期望值**：多命令断言须拆为多条、每条自带期望。理由＝零命中核对的期望取数在数字不唯一时保守跳过（`check_spec.py` 的 G15 口径）、按序配对又依赖「数字序＝命令序」（**写反即静默误配**）；拆开写还能免掉「未判」候选行」。**须附正反例**。**逐字文本见本节末「F3 逐字文本」段** | ⑨ ＋ §8 立法质量 | [B] |
| F4 | `docs/standards/测试与验收标准.md` §4 已知缺口表 **G2**（`:68`）与 **G15**（`:77`）两行 | **按触发响应并收窄**（不新增行）：G15 记按序配对已落地（要求每个数字各对一条命令）、K≠M 时分配型不判／其余全同取值／多值产出「未判」候选（不再静默）、残留（序写反即静默误配）与缓解（§5.9）；G2 收窄为已覆盖「行／字符／处」（限加粗标记计数）、「条／项／个」仍未覆盖。**列结构与其余行一律不动**；**不加粗**（该表其余行以加粗为非常态，本批不新增体例）。**逐字替换文本见本节末的 F4 逐字文本段**（须逐字照录）；本条核对面两串（须逐字落进对应行）：「口径限死为加粗标记计数」（G2 行）与「子句数字个数 K 与断言数 M 相等时」（G15 行） | ⑧ ＋ ①⑦ | [A] |
| F5 | `CHANGELOG.md` | 本批一行（≤400 字符），落 `[Unreleased]` › `### Changed`，以「对账覆盖缺口修补（072）：…」起首，覆盖 F1–F4 | 过程产物两项 | [B] |
| F6 | `docs/standards/测试与验收标准.md`:26（§1.1 段表 `[4]` 行的「管什么」列） | **产物审查 B5-① 的同步面**：该列枚举检查 A 的子输出（现文含「含零命中待复核」），本批新增的「零命中核对未判」须同型补入（067 批曾为「零命中待复核」同步同一列，属同型先例）；措辞＝「含零命中待复核**与零命中核对未判**」 | 产物审查 B5-① | [B] |

**F1 的口径要点（易错）**：① `M` 取**本规格提取到的断言总数**（`len(refs)`），不是本次窗口内的命令数；② `K`＝子句内数字个数（截断规则与现文同——`；`／`。`／`（预验`／行尾，故「预验基线 0／1／1」的尾数不计）；③ 数字按**出现顺序**取列表（`re.findall`），不得用 `set`（`set` 只用于判「全同」）；④ **配对序号从 0 起**（`enumerate` 默认）——夹具 triad 的期望行「0 ／ 1 ／ 0」中第 2 条命令应取到 `1`；⑤ 第 ③ 步（分配型不判）**先于**第 ④ 步（全同取值）——`062`:123 那类「分配型 ＋ 全同」的实例必须仍不判（067 §八.3 记档的假阳性不得复活）；⑥ 未判候选行的前缀与关键串（`· 零命中核对未判`）须逐字落，便于断言与核验识别。

**F2 的口径要点（易错）**：① 「加粗」判据落在**整个紧邻段 `seg`**（不看数字之后还是之前——「属红线加粗」在数字后、「加粗密度…（59 处」在数字前，两形态都须认）；② 实测值口径＝**`content.count('**')`**（标记出现次数，同 §一.3.8）；③ 增量式排除＝`seg[:start].rstrip()` 以「加」／「增」收尾（`070`:45 的「本批给该件加 16 处加粗」即此型；不加此排除则该条成假阳性）；④ 非「加粗」语境的「N 处」（如「全仓引用共 6 处」）**不得上报**，只计入候选。

**F3 逐字文本**（施工方须逐字插入为 §5 第 9 条）：

```markdown
9. **一条断言只配一条命令与一个期望值**：一条断言写多条命令时，须拆成多条、每条自带期望（`期望 1` 式），**禁写**「一条三命令 ＋ 期望 1／2／91」这类多值聚合句。理由：零命中核对的期望取数在数字不唯一时保守跳过（`check_spec.py` 的 G15 口径），而按序配对依赖「数字序＝命令序」——**写反即静默误配**。反例（`067`:100 起的实测形态）：三命令断言写「期望 1／0／0」，子句多值使该断言的零命中核对长期未判。
```

**F4 逐字文本**（两行整体替换，列数与表头一致；不得改字、不得增删列、不加粗）：

```markdown
| G2 | 计数重算的量词面：`行`／`字符` 已覆盖；`处` 于 072 批按类扩，口径限死为加粗标记计数（紧邻段须含「加粗」；数字前紧邻「加」／「增」的增量式不判）；`条`／`项`／`个` 仍未覆盖（口径随文件类型而异，不自动重算）；不言明加粗的「N 处 `**`」类措辞不判 | 待出现该类漏报后按类扩（072 批已按此触发扩第一类） |
| G15 | F2 的聚合期望句：072 批起子句数字个数 K 与断言数 M 相等时按命令序号配对取数（须每个数字各对一条命令）；K≠M 时分配型句不判、其余全同取值，而子句多值时不再静默——产出「零命中核对未判」候选行；残留＝数字序与命令序写反即静默误配（缓解＝`规格写作标准` §5.9） | 出现误配实例时加「数字序与命令序可疑不一致」提示（072 批已按原触发响应：原触发为「同类误报再现时改按命令序号映射」） |
```

## 三、验收断言（含预验记录）

**夹具**（落 `.tmp/072/`，红线 2，跑完即删）：`fixture-triad.md`（三条 `grep -c` 命令，第二条 token 为 `z-never-exists-token` 零命中、期望行「期望 0 ／ 1 ／ 0」）／`fixture-single.md`（一条命令、「期望依次 ≥1／≥1／≥1／≥1／≥1」）／`fixture-bold.md`（两条声称：一条真阳性——含目标件名与 89 处标记计数声称，须报到；一条负控——同一目标件的 89 处泛指引用，不得报到）。

### 断言 1｜K＝M 按序配对（零命中不再静默跳过）

```bash
python scripts/check_spec.py --static .tmp/072/fixture-triad.md
```

期望：输出**含** `· 零命中待复核: z-never-exists-token @ scripts/check_spec.py`；**不含** `零命中核对未判`；另两条命令的 token 现行命中非 0、不满足零命中门故不出零命中行；`rc=0`。**预验基线：无该行**（子句数字不唯一 → 整条跳过，已实跑复现）。

### 断言 2｜K≠M 分支逐字保留（回归「1 命令 5 值」型）

```bash
python scripts/check_spec.py --static .tmp/072/fixture-single.md
```

期望：输出**含** `· 零命中待复核: z-never-exists-token @ scripts/check_spec.py`（1 命令、5 值全同 → 取 1）；**不含** `零命中核对未判`；`rc=0`。**预验基线：含该行**（现判据已覆盖此型，本断言防 F1 改坏它）。

### 断言 3｜「N 处」加粗口径：真阳性必报、非加粗语境不报

```bash
python scripts/check_spec.py --static .tmp/072/fixture-bold.md
```

期望：输出**含** `· 计数不符: AGENTS.md 称「89 处」实测 91`（逐字以实现的输出为准，但须含 `计数不符`、`89 处`、`91` 三要素）；**不含**负控那条；`rc=0`。**预验基线：两条均不报**（现量词面不含「处」，输出恰为「· B：未提取到可判定的尺寸声称」＋「· 无发现」）。

### 断言 4｜F2 在**活文件**上确实触发（非只认合成夹具）

```bash
python scripts/check_spec.py --static docs/standards/文档写作标准.md
```

期望：输出**含** `B：判定 1 条整件尺寸声称`，且**不含** `计数不符`（`文档写作标准.md`:108 的「91 处」与 `AGENTS.md` 实测 91 一致）、**不含** `未提取到可判定的尺寸声称`。**预验基线：输出「· B：未提取到可判定的尺寸声称」**（该声称现完全在判定面外）。

### 断言 5｜归档语料回归（逐条存在性 ＋ 三态增量点名）

```bash
python scripts/check_spec.py --static docs/specs/archive/05[9]-*.md docs/specs/archive/06[0-9]-*.md docs/specs/archive/07[01]-*.md > .tmp/072/arch.txt
grep -c "^· 计数不符:" .tmp/072/arch.txt
grep -cF 'AGENTS.md 称「59 处」' .tmp/072/arch.txt
grep -cF 'AGENTS.md 称「57 处」' .tmp/072/arch.txt
grep -c "零命中核对未判" .tmp/072/arch.txt
grep -cE 'AGENTS.md 称「(6|1|16) 处」' .tmp/072/arch.txt
grep -c "无发现" .tmp/072/arch.txt
grep -c "有预警 121 条" .tmp/072/arch.txt
```

期望依次 **3**（060 附件「`check.sh` 称 39 行」＋`061`／`062`）／**2**／**1**（`070`，前缀为 `· 基线漂移（本批改动件，不阻断）`——该规格的改动清单节含 `AGENTS.md`，故不走「计数不符」）／**3**（F1 的未判候选：`067`／`069`／`070` 各一）／**0**（三条**假阳性**不得出现）／**0**／**1**（三态 = 基线 115 ＋ F2 的 3 行报到（2 计数不符 ＋ 1 基线漂移）＋ F1 的 3 行未判候选）。`rc=0`。**预验基线**：`计数不符` 恰 **1**（060 附件）、其余六项 **0**、三态「有预警 115 条」、B「判定 2 条整件尺寸声称（另有 18 条非整件口径声称未判）」。**若 121 不符**须逐条归因（多出者判真阳性或口径差，少者查 `findings` 收列表是否漏计）。

### 断言 6｜F3／F4 两件标准件落位

```bash
grep -c "一条断言只配一条命令与一个期望值" docs/standards/规格写作标准.md
grep -c "期望 1／2／91" docs/standards/规格写作标准.md
grep -c "按命令序号配对" docs/standards/测试与验收标准.md
grep -c "口径限死为加粗标记计数" docs/standards/测试与验收标准.md
```

期望各 **≥1**（F3 的条名与反例各一、F4 两行各一）；**预验基线 0／0／0／0**。

### 断言 7｜禁改面与改动面归属

```bash
git status --porcelain docs/specs/archive docs/product skills scripts/check.sh scripts/hooks | wc -l
git diff -U0 scripts/check_spec.py | grep -c "^@@"
git diff -U0 scripts/check_spec.py | grep "^@@" | grep -vc "check_swallow\|_whole_size\|check_counts\|static_report"
```

期望：第一项 **0**（命令型）。第二／三项**只作记录、不设数量阈值**（命令型仅呈报计数，供走查索引）——判定走**走查判据**：**每一个 hunk 都须落在 F1／F2 点名面内**（`check_swallow()` 含其 docstring／`static_report()` 的收列表／`_whole_size()` 含其 docstring／`check_counts()` 的取实测值处／模块 docstring 的用途段与载体形态说明 ③／`CLAIM` 及其上方注释），**无表外行**；第三项列出的 hunk 若 hunk 头不含函数名，须逐一确认其为**模块级**（git 对模块级行不给函数名上下文，hunk 头标出的是上一条模块级行如 `PATH_TOKEN = …`）。**预验基线 0／0／0**。**施工实测（含产物审查轮四处同步／勘误后）**：hunk **16**、非函数名 hunk **4**——`@@ -5`（模块 docstring 用途段）／`@@ -37`（载体形态说明 ③）／`@@ -71`（`CLAIM` 上方注释）／`@@ -73`（`CLAIM` 本行），**四处均为模块级且在 F1／F2 点名面内**；其余 12 处 hunk 头分别落在 `check_swallow`（8 处：docstring／`enumerate` 行／取数块 6 处）／`_whole_size`（2 处：docstring／「处」支路）／`check_counts`（1 处：取实测值）／`static_report`（1 处：收列表），逐条走查归属见 §九。**判据形态说明**：本项原写数量上界（≤9／≤2），施工与产物审查两轮实测为 15／3 与 16／4——数量取决于 `-U0` 的**切块粒度**且随同步性修订漂移，无法稳定表达「无表外行」这一实质要求，故改走查判据（`规格写作标准` §3：验收断言可机检**或可走查**）。

### 断言 8｜过程产物两项与链接

```bash
grep -cF '对账覆盖缺口修补（072）' CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '对账覆盖缺口修补（072）' in l][0].rstrip('\n')))"
```

期望第一项 ≥1、第二项 ≤400；全仓相对链接**断链 0** 且条数**不低于 175**（预验实测 175／0；命令同 071 规格 §四 断言 5 的一行式）；`bash scripts/check.sh` 退出 0（**含 [4] 段对本规格自身的实跑**——本规格自身的静态发现须逐条判「属预期／已处置」，记入核验结论）；`npx markdownlint-cli2 "**/*.md"` 0 issue；验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 十一条逐条实跑——`check_spec.py` 的 `:36`–`:37`／`:71`–`:73`／`:79`／`:456`／`:461`／`:463`／`:492`／`:496`–`:507`／`:536`／`:542`／`:580`／`:603`／`:607`／`static_report()` 的 `findings` 收列表现文；缺口一以两件夹具**双向复现**（triad 无该行／single 有该行）＋三处真规格实例逐条实跑（`067` K＝3／M＝6、`069` K＝4／M＝18、`070` K＝2／M＝21）；缺口二以活文件基线复现（`--static docs/standards/文档写作标准.md` 打印「未提取到可判定的尺寸声称」）；`AGENTS.md` 的 `**` 实测 91、行数 82；G2／G15 两行原文与触发条件现场读取；`067` §八.3／.4 与 `068` §七 的对应关系现场核对（F1 对应 **067-3**，非 067-4）；`065` §八.11 与 `069` §八.2 的落点现场读取（见 §七 范围注）。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–8 的**施工前基线**已逐条实跑——夹具三件；活文件单件；归档语料（计数不符 1／三态 115／B 判定 2）；F1 两支路的代价量（未判候选：归档 **3** 条、活文件 **0** 条；施工前另有 **4** 条属本规格自身断言未落位所致，落位即消——已成对实测）；F2 的闸**两轮定形**（宽松闸实测 3 条假阳性 → 收紧为「紧邻段含『加粗』＋增量排除」→ 假阳性 0；另测 5 个候选闸变体，见 §六）；F3／F4 四处 token 各 0；禁改面 0、hunk 0、链接 175／0、`check.sh` 退出 0。**逐条核断言 token 均落在 F 项目标文本内**（F3 取条名与反例串；F4 取两行的两个特征串；F1 取未判候选行的关键串）。**无未预验项**（断言 7 的 hunk 与归属、断言 8 的规格自跑发现，属施工后实核项）。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 全部（只读）、`docs/product/`、`skills/`（未创建）、`scripts/check.sh`、`scripts/hooks/`、`docs/specs/施工机制.md`、本批规格自身；`docs/standards/` 只许改 F3／F4 点名两件。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具。
- 禁止越权：不改版本号与 tag；不新增文件（本批只改**四件**既有文件：`check_spec.py`／`规格写作标准.md`／`测试与验收标准.md`／`CHANGELOG.md`）；**不建台账／不建索引件**、**不新增缺口行**（G2／G15 就地收窄）。
- 禁止改断言、改数字、删检查来变绿；**不得借本批放宽判据**（本批是**扩面**不是放宽：K≠M 的分配型不判与全同取值、三道门、`WHOLE_MARKS` 与「共 N 行」判据、检查 A／C／D、退出契约**逐字保留**）；**不得把「条／项／个」一并扩进去**（口径不唯一，见 F4）；**不得为压低归档语料的三态条数而调闸**（新增的报到与未判候选正是新判别力的正常产出）。

## 六、被否决的替代方案

- **只做 F1 的按序配对、不做「未判候选」支路** → 否决：配对要求 K＝M，在真语料上**零触发**（实测 `067`／`069`／`070`／`071` 皆 K≠M），那类断言的零命中核对仍**静默**——而「静默」才是缺口的实质；两支路成本差仅 3 行输出（归档语料），故并列保留。
- **只做「未判候选」、不做配对** → 否决：那会把「本可判出真问题」的 triad 型（K＝M）只降级为一行提示，丢掉最有力的产出（零命中待复核）。
- **让未判候选行游离在三态之外（不动 `static_report()`）** → 否决：该函数注释明写「漏计则『· 有预警 N 条』少计、屏上发现行多于计数行」（067 为同类问题修过一次）；新支路若不计入，其产出在汇总里**隐形**——与「修缺口」的立法目的相悖。故 F1 补一行收列表，并由断言 5 钉住三态 121。
- **去掉「期望 ≥1」这个前提**（token 零命中就报）→ 否决：会把「施工前目标文本尚未落地」的断言全量报出（067 立该前提正是为此限噪），信噪比崩坏。
- **把「处」的闸放宽到「紧邻段含 `**` 或『加粗』」** → 否决：**实测**在归档语料上产出 3 条假阳性（见 §一⑦）；收紧为「含『加粗』＋增量排除」后归零。另测三变体皆劣：① 去掉增量排除（判定 5／报到 4，多 1 假阳）；② 只看数字**之前**含「加粗」（判定 1／报到 1，**丢 3 条真阳**，含活文件那条）→「看整段」是承重的；③ 要求「加粗」距数字 ≤6 字（只剩弃用那条，更差）。
- **把「处」也扩成「条／项／个」** → 否决：那些量词的对象随文件类型而异（「12 条」可能是清单条目／规格条目／命中行数），无法确定实测对象，纳进来必生假阳性；按 F4 收窄 G2、触发条件保留。
- **F2 改用「`N 处` 后须紧跟 `` `**` ``（单位符）」作判据** → 否决：活文件那条真声称（「现 **91 处**属红线加粗」）不带单位符、`061` 那条带单位符，任一单形态判据都会丢掉一半真声称。
- **顺手扩检查 A 的字面集**（`071`:98 那条 token 含反引号、不在字面集，属 G14）→ 否决：本批不动该面；F1 的未判候选已在**取数层**照亮同型实例，先让扩面落地、再按实测决定是否扩字面集。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**本批改动面触及的既有欠账**：071 与 067 的全部开放项（必列面 ①），以及本批改动**直接触及**的两条标准件缺口行（`测试与验收标准` §4 的 G2／G15——按 §三 核销面 ②「本批改动直接触及或使其状态可能变化的条目」，标准件的缺口行同属核销对象）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 071-1 | 可移植模板形态的参考（064-1／059-2／060-3 同题） | 仍待 | 本批不动 `AGENTS.md`；触发＝首次移植 |
| 071-2 | 自测句的铺开边界与覆盖口径 | 仍待 | 本批不涉 `AGENTS.md` §一／§五；触发＝出现「条文读了但不当场校验」实例 |
| 071-3 | 归档件机械修复的纪律例外（已获作者追认；登记留待触碰 `施工机制` 时补） | 仍待 | 本批 §五 禁改 `施工机制.md`（小工具批不扩面到机制件），该登记仍留待下批触碰该件时补一笔 |
| 071-4 | 自检信号的自校验（两批后评估是否入核验请求模板） | 仍待 | 本批不动该模板；触发＝两批后仍无人据此核对 |
| 067-1 | `[B]` 面纳入后输出量上升（评估分档输出） | 仍待 | 本批不动该面；触发＝再两批后仍觉噪声压过信息 |
| 067-2 | F2 判据只认字面 token（漏报面） | 仍待 | 已记 §4 **G14**；本批不动字面集（见 §六 否决项） |
| 067-3 | F2 聚合期望句不判（分配型漏报面） | **本批处置** | 触发原文＝「同类误报再现时改**按命令序号映射**」——F1 ① 即该映射；F1 ② 另把该面的「静默」改为显式候选；F4 就地收窄 G15 |
| 067-4 | 零命中判据只认 `期望 ≥N` 写法（「各 ≥1」「均为 1」类变体漏报） | 仍待 | **F1 不改 `expect_re` 的写法识别**，故该面未变（成稿审查 P0-1 更正了规划方初稿的错记）；触发＝出现该类漏报 |
| 067-5 | 工具仍无「断言与正文的语义对账」能力 | 仍待 | 本批只修覆盖缺口，未涉语义层 |
| `测试与验收标准` §4 G15 | 聚合期望句（数字不唯一）不判 | **本批处置** | 触发条件原文即「改按命令序号映射」——F1 落地（配对 ＋ 未判候选 ＋ 计入三态）；F4 就地把该行改为「已配对／不再静默 ＋ 残留与缓解」 |
| `测试与验收标准` §4 G2 | 量词扩「条／项／处」未覆盖 | **本批处置（部分）** | F2 覆盖「处」（限死加粗标记计数口径）；「条／项／个」仍未覆盖，F4 就地收窄该行、触发条件保留 |

**汇总**：按条计 11 条：已履行 0｜作废 0｜**本批处置 3**｜仍待 8｜待作者 0 ＝ 11 ✓（逐格可复核）。**范围注（判据＝同节交集，非同名文件）**：本批在工具侧只动「检查 A 的取数面（`check_swallow`）」与「检查 B 的量词面」、在标准侧只动 `规格写作标准` §5 与 `测试与验收标准` §4；据此，`065` §八.11（落点＝检查 A／C 的**目标文本两口径**）与 `069` §八.2（落点＝`规格写作标准` **§1 判定表**）**均非同节交集、其状态不变**，故不列；065／066／068／069／070 的其余开放项同理，以其各自核销表为索引（不重列）。

## 八、开放项

1. **字面集是否扩**（`literal_token`：含 `；`／`（）`／反引号／`「」` 的 token 现被跳过——`071`:98 那条命令的 token 即反引号型）：本批不动，触发＝再出现「含中文标点或反引号的 token 失效」实例（G14 同题）。
2. **「处」闸的残留面**：① 不言明加粗而实指加粗的措辞（如 `064`:14 的「**57 处**／65 行」写法——同句的「加粗」落在紧邻段之外）**不判**；② 紧邻段内提「加粗」而他指的句子**会**进判定面（自指句「非加粗语境」之类）。两向残留均为漏报／有限假阳，方向安全；触发＝两向任一在活文件上真实咬了人。
3. **期望取数的两类错配面**：① 按序配对假设「数字序＝命令序」，写反属**静默误配**（缓解＝F3 的写法纪律）；② **窗口拾取**——取数行＝窗口内首个含「期望」的行，理论上可能取到邻近断言的期望行（若其数字个数恰＝M，配对会取到别人的值；现语料无实例）。触发＝出现任一实例时按 G15 的新触发条件加「数字序与命令序可疑不一致」候选提示。
4. **未判候选行的既有案例回归**：`067` §八.3 记档的「修复后自核验三条 @ `施工机制`.md」在本批后会以**「未判」候选**（而非当年的假阳性「零命中待复核」）重新出现在归档语料输出里——语义已不同（只报「无法对齐」、不声称「期望 ≥N 而命中 0」），属预期；触发＝若核验时被误判为回归，按本条辨正。
5. **配对支路的真语料触发面为零**：实测归档 14 件＋在制 1 件皆 K≠M，配对仅由合成夹具见证——触发＝出现 K＝M 的真规格实例（届时应确认它确实按序取到了正确数字序）。
6. **`check_counts()` 内有一段基线的重复死块**（现文两处 `listed = _section(text, CHANGE_SECTION)`／两个同构 for 循环，第一段结果被第二段整体覆盖；`git show HEAD` 证实**非本批引入**）：本批按「不改点名面外的行」未动，F2 的取实测值处亦因此改用「按目标件另读一次」而非扩 `cache` 元组（施工期 P2-1）。**该块属 §4 缺口表主题**（工具内冗余／双跑），下次触碰该件时登记并清理（清理属行为等价改写，须走标准路径）。
7. **未判候选行的 `nums` 会纳入子句内的非期望数字**（产物审查 F7）：`K`（数字个数）按子句内**全部**数字计，故节号之类的数字也计入——理论上可使 K 意外等于 M 而进配对支路（`069`:86 的未判行列出 `[1, 0, 1, 1]`，末两个 `1` 即来自子句内的「`测试与验收标准` §1.1」）。方向安全（仍为候选、不误报结论），与开放项 3② 的「窗口拾取」同族；触发＝出现因此误配的实例时一并收窄（须另批）。

## 九、审查记录

- 2026-09-12｜成稿审查（fresh 只读分身）｜findings 共 12：P0:1 P1:4 P2:7｜结论 **修订后通过**。处置：**P0-1**（§七 核销表把 F1 记到 067-4、且必列的 067-3 未列）→ 核销表改为 067-3＝本批处置、067-4＝仍待并补 067-2，汇总改 11 条（经规划方独立复核 `067` §八.3／.4 与 `068` §七 `:152`–`:153` 成立）；**P1-1**（断言 7 第三项不可达——模块级 hunk 无函数名上下文）→ 判据改「≤2 且两处均为模块级」并写明理由（**窄域复审 N1 进一步指出 F2 有两处模块级改动，改后亦须复核**）；**P1-2**（断言 5 的「新增三条计数不符」与实况不符——`070` 那条走「基线漂移」前缀）→ 断言 5 改为逐条存在性判定；**P1-3**（F1 举例「第一命令期望 ≥1；第二、三命令期望 0」其实 K≠M，「序数分配句天然正确」不成立）→ 删该说法，并在 F1 明写支路先后（**窄域复审 N2 进一步指出全同取值与分配型不判的顺序须钉死**）；**P1-4**（「071 批实测…」在 071 无载，且按 M／K 复算 F1 对 `071`:103 不触发）→ 例证换为本批实测的三处真规格实例（**窄域复审 N4 进一步指出 `071`:103 连窗口都到不了**）；**P2-1**（模块 docstring 等未列入同步面）→ F2 补两处（复审指出改写句须点名 `_whole_size()` docstring，已补）；**P2-2**（本规格自指发现）→ 措辞改到「无自指计数不符行」＋§八.4 兜底；**P2-3**（断言 1 括注理由不实）→ 改为「token 现行命中非 0，不满足零命中门」；**P2-4**（`:70` 实为 `:71`–`:72`；§一④ 漏「字符」早退）→ 两处改正；**P2-5**（头注缺路径判定）→ 头注补「标准路径」判定句；**P2-6**（F4 两行将增加 8 处加粗）→ 逐字文本**不加粗**；**P2-7**（「新增即撞平行台账」依据引用偏松）→ 改为「按触发就地收窄（067 F7 已预登记该触发）」。审查方两项独立判断被采纳（F2 的闸维持「整段含『加粗』＋增量排除」；断言 5 改逐条存在性）。
- 2026-09-12｜窄域复审（fresh 只读分身，只核修复面 ＋ 直接影响面）｜findings 共 7：P0:0 P1:3 P2:4｜结论 **修订后通过**。处置：**N1**（断言 7 第三项「≤1」与 F2 的两处模块级改动冲突→施工后必红）→ 改「≤2 且两处均为模块级」（hunk 上界同步 8→9）；**N2**（F1「K≠M 全同取值」与「分配型不判」自相矛盾，按句序读法会复活 `062`:123 的既有假阳性）→ F1 明写五步先后（分配型不判**先于**全同取值），F4 的 G15 行同句改述；**N3**（三态增量算式与冻结面冲突）→ 采纳「补 `static_report()` 收列表」方案（该函数注释自证「漏计即屏上发现行多于计数行」），并放宽 §一 不变量与 §五 的冻结面，断言 5 钉三态 **121**；**N4**（`071`:103 例证不实：子句实为 `[1,2]`、窗口到不了 `:103`）→ 例证换为 `067`／`069`／`070` 三处实测实例（含 M／K 值）；**N5**（配对支路真语料零触发）→ §八 开放项 5；**N6**（「活文件 0 条」时点不符）→ §四.2 改述（施工前 4 条属本规格自身、落位即消）；**N7**（§七 范围注判据）→ 补判据句并点名 `065` §八.11／`069` §八.2 为何不算交集。复审另附：F4 逐字文本新增 1 处**代码跨度内**的字面 `**`（该件计数 37→38），按 §一.3.8／G1「入口与清单型文档不计兜底阈值」**不构成违例**，仅登记。
- 2026-09-12｜核验三步（规划方执行）｜**通过（含一处按实测改述的断言，定因见下）**。① 改动面核对：4 件（`check_spec.py`／`规格写作标准`／`测试与验收标准`／`CHANGELOG`）＋本批规格未跟踪；`check_spec.py` **15 hunk 逐条走查全在 F1／F2 点名面内**（模块 docstring／`CLAIM` 上方注释／`CLAIM` 本行／`check_swallow` docstring＋取数块／`_whole_size` docstring＋「处」支路／`check_counts` 取实测值／`static_report` 收列表），无表外行；禁改面 0；`[A]` 项（F4 两行、F3 第 9 条）与规格逐字文本一致（施工方行级比对 2/2、1/1；规划方另以 `grep -c` 复验四处 token 各 1）。② 断言独立重跑（规划方自己的会话）：断言 1–6、8 全绿（1／2／3／4 输出与规格期望逐字一致；5 的七项实测 **3／2／1／3／0／0／1**；6 四处各 1；8 链接 **175／0**、`check.sh` 全绿、lint 42 件 0 issue、`CHANGELOG` 行 311 字符）。③ 内容抽查：读全 4 件 diff，F1 五步先后与「分配型不判先于全同取值」、F2 的「处」支路早退（`return '加粗' in seg and not head.rstrip().endswith(('加', '增'))`）与实测值 `count('**')`、`static_report` 收列表一处新增，均与规格一致。
- 2026-09-12｜**断言 7 按实测改述（定因与担因）**｜施工期报 P1-1：第二／三项实测 **15／3**，与规格所写「≤9／≤2」不符。规划方复核**成立且由规划方担因**：原估按「每改动点一 hunk」计，而 `git diff -U0` 按**变更块**切分（F1 取数块切成 6 个 hunk；F2 的注释与代码行之间夹未改行故各拆 2 个；第三项原写「两处」漏数了 `CLAIM` 本行这一模块级 hunk）。施工方另以对照件量得「最省 hunk 的忠实实现」仍需 **11** 个（>9），证明原估不可达。**处置**：先改为「≤15／≤3 ＋ 该 3 处均为模块级」；产物审查轮四处同步修订后实测升至 **16／4**，故**最终改为走查判据**（「每一 hunk 须落在 F1／F2 点名面内、无表外行」＋ 非函数名 hunk 须逐一确认模块级），数量只作记录——理由：数量取决于 `-U0` 切块粒度且随同步性修订漂移，无法稳定表达实质要求（`规格写作标准` §3 允许走查型断言）。**非「改断言求绿」**：检查项未删、实现形态未动，且「为凑 hunk 数改写注释行」属反模式 #1 同型，已写进断言口径予以禁止；实质判据由规划方逐轮走查确认（15/15 → 16/16 全在面内）。
- 2026-09-12｜施工期 P2 两条｜**P2-1**（`check_counts()` 内基线的重复死块，`git show HEAD` 证实非本批引入；F2 因此改用「按目标件另读一次」而非扩 `cache` 元组）→ 记 §八 开放项 6，本批不动。**P2-2**（不为降 hunk 数改写注释）→ 采纳，已写入断言 7 的口径说明。
- 2026-09-12｜本规格自身静态发现逐条判属（规划方与施工方两侧实测一致）｜`--static` 输出 12 行中 4 条发现项：2 条自噬预警（`口径限死为加粗标记计数 @ 测试与验收标准`、`对账覆盖缺口修补（072） @ CHANGELOG`）——均为工具的「现行＋目标文本」加法预测未计删除的**已知近似**，到位期望＝1＝实测；1 条中性行（`^@@` 属 G14 型字面集外 token）；1 条 B 判定行（`文档写作标准.md`:108 的「91 处」，即 F2 扩面的**正面现场证据**）。7 条跳过行均为断言 5 的 grep 模式目标指向 `.tmp/072/arch.txt`（临时产物不入改动清单，hit 门不过，属既有行为）。汇总「有预警 4 条」＝2 自噬＋2 中性，与发现行逐一对账无多计／少计（含本批新补前缀）——**工具报得出的与应当报出的一致**。
- 2026-09-12｜**产物审查（fresh、只读、双维）**｜findings 共 7：P0:0 P1:1 P2:6｜结论 **修订后通过**。两维结论：**内容质量**——四件与规格的逐字文本**逐字节相等**（F3 第 9 条、F4 两行：程序比对 True/True/True），八条断言审查方**独立重跑全部复现**（含三件原始夹具与归档语料七项 3／2／1／3／0／0／1、「计数不符」恰 3、末行三态 121）；`check_spec.py` 新写的 docstring 与注释**与实际行为逐句相符**（「处」支路确早退于 `WHOLE_MARKS` 之前、五步先后与 if/elif 链一一对应、③ 确在 ④ 之前）。**规范健康度**——全仓无「已被本批推翻」的残留表述；未触红线（临时产物只在 `.tmp/`、未新增台账、未引外部依赖、禁改面 0）；工具新增两处可报项均已在标准登记（名实相符）。处置：**F1**（`.tmp/072/` 未「用完即删」，违红线 2）→ **已删除**；**F2**（规格 §九 记「`CHANGELOG` 行 311 字符」而实测 341——因规划方在产物审查前把核验尾句改入 `CHANGELOG`）→ **已更正为 341**；**F3**（`测试与验收标准.md`:26 的 §1.1 `[4]` 行未同步新子输出，067 有同型先例）→ **已补，并作为 F6 列入改动清单**；**F4**（`check_spec.py` 模块 docstring 用途段未枚举新子输出）→ **已补，并入 F1 改动面**；**F5**（`_whole_size()` docstring「3／7 条报到为假阳性」分母错位）→ **已改为「7 条判定 6 条报到，其中 3 条为假阳性」**；**F6**（`check_swallow()` docstring 例的主谓悬空、两例未挂分支）→ **已改写为逐例挂 ③／⑤ 分支**；**F7**（未判候选的 `nums` 会纳入子句内非期望数字）→ 记 §八 开放项 7。
- 产物审查（fresh、只读、双维）：**已完成**（见上条）；本批三处审查位（成稿／窄域／产物）均已回填。
