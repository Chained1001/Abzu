# 073-2026-09-12-计数重算死块清理

> 定位：删掉 `check_counts()` 内**一段被整体覆盖的重复死块**（现文 `:588`–`:610` 与 `:611`–`:633` 逐行相同，前者算出的结果被后者重初始化后丢弃）——**行为等价改写**，以「全语料静态输出**逐字节不变**」为最强断言（该比对的语料**排除本批改动件** `CHANGELOG.md`，以保「只改一处变量」——见 §三 断言 1，并由断言 7 作配套守卫）。起因＝072 批产物审查／施工期实证：该重复段使「取实测值处」的改动必须连改两处解包行，072 因此绕道（改用「按目标件另读一次」），并把它记为开放项 6 待本批处置。**不改任何判据、不碰任何标准条文**。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**——跨文件（2 件：工具 ＋ `CHANGELOG` 留痕），且**非**「< 5 行纯措辞／格式」（删 23 行逻辑）、非「微调」（改的是函数体结构）；不命中「重」行（不动 `skills/`、非架构级、不改规则）。
> 自由度分布：[A]×0 [B]×2
> 必读输入：`scripts/check_spec.py` `check_counts()`（`:580`–`:649`：docstring／第一段 `:588`–`:610`／第二段 `:611`–`:647`／`return` `:649`）；`docs/specs/archive/072-*.md` §八 开放项 6–7（本批核销对象）＋§九 产物审查 B3／施工期 P2-1（死块出身的实测记录）；`AGENTS.md` 红线 2（临时产物）／红线 4（禁止改数字求绿）；`docs/specs/施工机制.md` §三（核验三步／过程产物两项）／§五（审查）／§八（模板）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 重复块**两处的位置** | `grep -n "listed = _section(text, CHANGE_SECTION)" scripts/check_spec.py` → **`:588`** 与 **`:611`** 各一 |
| ② | 第一段是第二段的**真正前缀**（逐行相同） | `diff <(sed -n '588,610p' …) <(sed -n '611,633p' …)` → **输出为空**（23 行逐行相同） |
| ③ | 第二段是**生效段**（第一段结果被丢弃） | 第二段在 `:611`–`:614` 重新初始化 `listed`／`lines`／`judged`／`cand`／`cache`，函数在 `:649` 返回**第二段**的 `lines, judged, cand`；第二段正文止于 **`:648`**（`:644` 拼 `head`／`:645`–`:648` 两类输出行）；第一段只有「累计 `cand`」的半截循环（无 `judged += 1`、无实测值判定、无输出行） |
| ④ | 该重复**非本批引入**（基线既有） | `git show c486ac1:scripts/check_spec.py \| grep -c "listed = _section(text, CHANGE_SECTION)"` → **2**；`git show c486ac1~1:…` → **2**（072 批未动其结构） |
| ⑤ | 代价与本批的绕道 | 该函数体 70 行（`:580`–`:649`）里 23 行是死代码，且**每条计数声称被扫两遍**；072 的 F2 要在「取实测值处」加「处」分支，若扩 `cache` 元组须**连改两处**解包行（第二处在生效段、第一处在死段）→ 改名面必然越出点名区，故 072 改用「按目标件另读一次」并将本项记入开放项 6 |
| ⑥ | 删前基线（**行为等价的比对基准**） | 全语料静态输出（**语料刻意排除本批改动件**——两条范围说明见 §三 断言 1）：**370 行**、md5 **`6d24131cc34640532f21f650bb6ec10c`**、末行「· 有预警 **290** 条」、`rc=0`；**可复现性已验**（连跑两次 `diff` 为空，输出无时间戳／无序字典依赖）；命令见 §三 断言 1，逐字复用 |
| ⑦ | 其它基线 | `listed` 计数 **2**（删后应为 1）；`grep -c "judged += 1"` **1**（保留段完整性锚，全件唯一）；三件夹具输出：triad ＝ 1 行「零命中待复核」／single ＝ 1 行／bold ＝ 1 行「计数不符: AGENTS.md 称「89 处」实测 91」；链接 **175／断链 0**；`git diff -U0 scripts/check_spec.py \| grep -c "^@@"` **0**（工作树干净）；**删后零歧义锚（副本实测）**：`wc -l` ＝ **969**、`:588` ＝ `listed = _section(text, CHANGE_SECTION)`（缩进 4 空格）、`:611` ＝ `judged += 1`（缩进 16 空格）；`CHANGELOG.md` 单件的 B 汇总行 ＝「· B：未提取到可判定的尺寸声称（**1 条**非整件口径声称未判）」（断言 7 的守卫基线） |

**不变量（施工前后须同）**：`check_counts()` 的**行为**（对任意输入的输出行、`judged`／`cand` 计数、异常面）逐字节不变；`check_swallow()`／`_whole_size()`／`static_report()`／`check_pairs()`／`check_writing()` 等其余全部函数、模块常量（含 `CLAIM`／`WHOLE_MARKS`／`LIMIT_MARKS`）、`static_report()` 的 `findings` 收列表、退出契约（静态发现恒 0）零改动；`docs/standards/`、`AGENTS.md`、`docs/specs/施工机制.md`、`docs/specs/archive/`、`docs/product/`、`skills/`（未创建）、`scripts/check.sh`、`scripts/hooks/` 零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py` `check_counts()`（删 `:588`–`:610`） | **删去第一段**（`:588` 的 `listed = _section(text, CHANGE_SECTION)` 起、至其半截循环末行 `:610` 的 `continue` 止，共 23 行），**保留第二段**（`:611` 起至 `:648`，含 `judged += 1`、实测值判定与两类输出行）与 `:649` 的 `return`——删后该函数体 `:580`–`:626`（47 行）、全件 `wc -l` ＝ **969**、`:588` ＝ `listed = _section(text, CHANGE_SECTION)`、`:611` ＝ `judged += 1`。**只删、不改、不搬**：保留段任何一行（含空格与注释）不得变动；不得为「对称」或「顺序」调整删除边界 | ①–⑤ | [B] |
| F2 | `CHANGELOG.md` | 本批一行（≤400 字符），落 `[Unreleased]` › `### Changed` 首条，以「计数重算死块清理（073）：…」起首，写明「行为等价（全语料输出逐字节不变）」与核验结论 | 过程产物两项 | [B] |

**F1 的口径要点（易错）**：① 以**行号**为准（`:588`–`:610`），不以「第一段／第二段」的措辞推断——删前须先 `diff` 复核两段前缀关系（§一②）；② **不得**改删保留段（`git diff` 应只有**删除行**、无 `+` 行）；③ 删除后 `listed = _section(text, CHANGE_SECTION)` 在全件**恰 1 处**；④ 删后**零歧义锚**：全件 `wc -l` ＝ **969**、`:588` ＝ `listed = _section(text, CHANGE_SECTION)`（缩进 4 空格）、`:611` ＝ `judged += 1`（缩进 16 空格）（不依赖 git 的对齐策略，比读 hunk 起始行号稳）；⑤ 不得顺手重命名 `listed`／收敛 `cache` 元组／调整注释（见 §六）。

**F2 的口径要点（易错）**：① 落 `[Unreleased]` › `### Changed` **首条**，与 072 条目同体例（单行、≤400 字符、以「计数重算死块清理（073）：…」起首、末尾「核验：…」句）；② 必写「行为等价（全语料静态输出逐字节不变）」与核验结论；③ **不得引入新的计数声称**——行内不得出现「数字 ＋ 行／处／字符」跟在**实存路径**的行内代码跨度之后：写「`scripts/check_spec.py` 删 23 行」会让 `--static CHANGELOG.md` 的 B 候选数 +1、**断言 7 转红**；写成「`check_counts()` 内删 23 行」或「删去重复段 23 行」则安全（**三式均已实测**：**无实存路径跨度的两式 B 行皆为「1 条非整件口径声称未判」**、陷阱式（实存路径跨度 ＋「删 23 行」）为「2 条」——本条初稿曾把「删去重复段 23 行」误记为「2 条」，由施工期 P2 指出、规划方复测更正）。`check_counts()` 不是实存路径、故其后的数字不入判定面；`check.sh`／`CHANGELOG.md` 等亦须注意同类形态。

## 三、验收断言（含预验记录）

### 断言 1｜行为等价：全语料静态输出**逐字节不变**（最强一道）

```bash
python scripts/check_spec.py --static docs/specs/archive/*.md docs/specs/施工机制.md docs/standards/*.md docs/product/*.md AGENTS.md README.md > .tmp/073/after.txt 2>&1
diff .tmp/073/before.txt .tmp/073/after.txt && echo "[逐字节相同]"
python -c "import hashlib;print(hashlib.md5(open('.tmp/073/after.txt','rb').read()).hexdigest())"
wc -l < .tmp/073/after.txt
```

期望：`diff` **无输出**；md5 ＝ **`6d24131cc34640532f21f650bb6ec10c`**；行数 ＝ **370**。**预验记录**：该命令的**删前**输出已实跑并冻结为 `.tmp/073/before.txt`（370 行／md5 同上／末行「· 有预警 290 条」／rc=0），**并验其可复现**（连跑两次 `diff` 为空）——**before.txt 须保留到本批验收毕**（属临时产物，验收后随 `.tmp/073/` 删除）。

两条范围说明（缺一即比对失效）：
① 命令**不含 `docs/specs/` 下的在制规格**（只取 `archive/*.md`），否则本批规格自身进入输出、比对不可复现。
② 命令**不含 `CHANGELOG.md`**——它是本批的改动件（F2），改后其新行若含「数字 ＋ 行／处／字符」且跟在实存路径跨度之后，会使 B 汇总行变化、md5 必变（**成稿审查 P0**：原方案把 `CHANGELOG.md` 计入语料，与 F2 直接冲突）。等价性比对的要义是**只改一处变量**，故把它移出语料，另由**断言 7** 单独守卫其措辞面。

### 断言 2｜结构锚：重复段确实只余一处、保留段完整

```bash
grep -c "listed = _section(text, CHANGE_SECTION)" scripts/check_spec.py
grep -c "judged += 1" scripts/check_spec.py
python -c "import ast,io;ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read());print('[语法自检通过]')"
wc -l < scripts/check_spec.py
sed -n '588p;611p' scripts/check_spec.py
```

期望依次 **1**（预验 2）／**1**（预验 1，保留段完整）／`[语法自检通过]`（工具头注约定用 `ast.parse`，不用 `py_compile`——后者留缓存产物）／**969**／`:588` ＝ `listed = _section(text, CHANGE_SECTION)`（缩进 4 空格） 且 `:611` ＝ `judged += 1`（缩进 16 空格）（**后两项为零歧义锚**，不依赖 git 对齐策略；预验值来自副本实测）。

### 断言 3｜夹具行为不变（三个支路各一）

```bash
python scripts/check_spec.py --static .tmp/073/fixture-triad.md
python scripts/check_spec.py --static .tmp/073/fixture-single.md
python scripts/check_spec.py --static .tmp/073/fixture-bold.md
```

期望：triad **含** `· 零命中待复核: z-never-exists-token …`（K＝M 配对支路）；single **含**同一行（K≠M 全同支路）；bold **含** `· 计数不符: AGENTS.md 称「89 处」实测 91` 且**不含**负控那条；三者 `rc=0`。**预验基线**：三件均各 **1** 行命中（triad／single 为「零命中待复核」、bold 为「计数不符」）。

### 断言 4｜回归 072 的判别力（归档语料七项）

```bash
python scripts/check_spec.py --static docs/specs/archive/05[9]-*.md docs/specs/archive/06[0-9]-*.md docs/specs/archive/07[01]-*.md > .tmp/073/arch.txt
grep -c "^· 计数不符:" .tmp/073/arch.txt
grep -cF 'AGENTS.md 称「59 处」' .tmp/073/arch.txt
grep -cF 'AGENTS.md 称「57 处」' .tmp/073/arch.txt
grep -c "零命中核对未判" .tmp/073/arch.txt
grep -cE 'AGENTS.md 称「(6|1|16) 处」' .tmp/073/arch.txt
grep -c "无发现" .tmp/073/arch.txt
grep -c "有预警 121 条" .tmp/073/arch.txt
```

期望依次 **3／2／1／3／0／0／1**（＝072 批的实测值，本批**不得**改动任何一项）。**预验基线**：（072 批实测）与期望同值。

### 断言 5｜禁改面与 hunk 单一

```bash
git status --porcelain docs/specs/archive docs/product skills scripts/check.sh scripts/hooks docs/standards AGENTS.md | wc -l
git diff --numstat scripts/check_spec.py
git diff -U0 scripts/check_spec.py | grep -c "^@@"
git diff -U0 scripts/check_spec.py | grep "^@@" 
```

期望：第一项 **0**；第二项 **`0 23 scripts/check_spec.py`**（**0 增、23 删**——出现任何 `+` 行即越界）；第三项 **1**（单处删除）；第四项 hunk 头须满足三点——① 上下文含 `def check_counts(text, root)`（＝落点名函数内）；② 旧侧计数为 **23**；③ 新增侧计数为 **0**（纯删除）。**实测（副本模拟）**：`@@ -589,23 +588,0 @@ def check_counts(text, root):`——**起始行号读 `588` 或 `589` 均属正确**：两段逐行相同，git 的对齐取「保留第一份」或「保留第二份」二者等价（产物文件一致），不得以行号读法判越界。**预验基线 0／（无 diff）／0**。

### 断言 6｜过程产物两项与全仓守卫

```bash
grep -cF '计数重算死块清理（073）' CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '计数重算死块清理（073）' in l][0].rstrip()))"
bash scripts/check.sh
npx markdownlint-cli2 "**/*.md"
```

另核：全仓相对链接**断链 0** 且条数**不低于 175**（预验 175／0；命令同归档 072 规格 §四 断言 5 的一行式）；验收全部通过后本规格移入 `docs/specs/archive/`。期望：第一项 ≥1、第二项 ≤400、`check.sh` 退出 0、lint 0 issue。**预验基线**：第一项 **0**、链接 **175／0**、`check.sh` 退出 0、lint **0 issue**（**43 件**——含本批规格；072 批时点为 42 件）。
**另须**（照归档 072 规格 §四 断言 8）：`check.sh` 的 `[4]` 段会扫**本规格自身**，其静态发现须**逐条判属**「属预期／已处置」并记入核验结论。**预验实测**：末行「有预警 **2 条**」＝`^@@` 中性行 ＋「现行有值／目标文本未提及: 2 个」中性行；另有 **7 条「跳过」行**（断言 4 的 grep 模式目标指向 `.tmp/073/arch.txt`、hit 门不过）与 B 行的 **6 条非整件口径候选**（含 F2 口径要点里为演示陷阱写出的「`scripts/check_spec.py` 删 23 行」那处）。**施工后实测为「有预警 3 条」**——多出的 1 条是「自噬预警: 计数重算死块清理（073） @ CHANGELOG.md —— 现行 1｜目标文本内 1｜近似预测 2」，成因＝断言 6-1 的 grep token 恰是 F2 行首要求的「计数重算死块清理（073）」，F2 落盘后该 token 现行数由 0 变 1 → 命中检查 A 的 `n>0 且 cur>0` 支路；**任何合规 F2 写法都会触发它**，且「近似预测 2」是 `cur+n` 的上限启发式（实跑 `grep -cF` ＝ 1，无重复）——**均属预期**，非缺陷；条数若再有变化即须逐条归因。

### 断言 7｜F2 措辞不得引入新的计数声称（断言 1 的配套守卫）

```bash
python scripts/check_spec.py --static CHANGELOG.md
```

期望：输出**含** `· B：未提取到可判定的尺寸声称（1 条非整件口径声称未判）`（＝预验基线），且**不含** `计数不符`。**预验记录**：现树实测为「1 条」（来自第 13 行 071 条目的既有候选）；**已实测反例**——若 F2 新行写成「`scripts/check_spec.py` 删 23 行重复死块，行为等价。」则该行为「2 条」→ 断言红；若写成「删去 `check_counts()` 内被整体覆盖的重复段（23 行…）」（预验草稿，118 字符）则仍为「1 条」✓。判据成因：检查 B 只对「行内代码跨度内容**是实存路径**」的行做重算，`check_counts()` 不是路径、故其后的数字不入面。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 七条逐条实跑——两处 `listed` 行号（`:588`／`:611`）、两段前缀关系的 `diff`（空输出）、生效段的判据（第二段重初始化 ＋ `:649` 返回其值）、基线既有性（`git show c486ac1`／`c486ac1~1` 各 2 处）、函数体边界（`:580`–`:649`，70 行）、删后行数（47 行）、三件夹具与链接／lint／hunk 基线。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–7 的**施工前基线**已逐条实跑并冻结（断言 1 的 `before.txt`、断言 2 的五项、断言 3 的三件夹具、断言 4 的七项、断言 5 的禁改面与空 diff、断言 6 的 `CHANGELOG` 0／链接 175／lint 0、断言 7 的 B 行「1 条」）。**断言 1 的等价性另在副本上预演过**（充分证据）：`cp scripts/check_spec.py .tmp/073/sim.py` → 删 `:588`–`:610` → 补丁 `repo_root()` 指向本仓（副本置于 `.tmp/` 下会推导出错误基准，须打补丁）→ 跑**修正后的语料**（不含 `CHANGELOG.md`）得 **370 行／md5 `6d24131cc34640532f21f650bb6ec10c`／`diff` 空** ✓；施工后须在**真件**上重跑一次作为验收证据。**该预演只覆盖 F1**（不含 F2 的 `CHANGELOG` 改动）——因此语料才须排除该件，其措辞面另由断言 7 守卫（**该冲突由成稿审查 P0 抓出**，见 §九）。**断言 7 的判据已双向预演**：安全写法（预验草稿 118 字符）→ B 行仍「1 条」✓；陷阱写法（`scripts/check_spec.py` ＋「删 23 行」）→「2 条」✗。**本批无 `[A]` 项**（纯删除无「替换文本」可逐字核，见 §六 否决项）——故断言面全为「行为／结构／计数」型，不存在断言吞替换文本的风险；断言 token（`listed = _section(text, CHANGE_SECTION)`／`judged += 1`）均为**保留段**文本，改后仍在。**无未预验项**。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` 全部（只读）、`docs/product/`、`docs/standards/` 全部、`AGENTS.md`、`docs/specs/施工机制.md`、`scripts/check.sh`、`scripts/hooks/`、本批规格自身。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具。
- 禁止越权：不改版本号与 tag；**不新增文件**（本批只改**两件**既有文件：`scripts/check_spec.py`／`CHANGELOG.md`）；不建台账／不建索引件。
- 禁止改断言、改数字、删检查来变绿；**F1 只许删、不许改**（`git diff --numstat` 出现任何 `+` 行即越界）；**不得顺手**收敛 `cache` 元组、重命名 `listed`、调整注释或格式（见 §六）；**不得趁本批触碰判据**（072 的开放项 7 触发器未到）。
- 临时产物（`.tmp/073/`：三件夹具、`before.txt`／`after.txt`／`arch.txt`）用毕即删（红线 2）；`before.txt` 保留至验收毕。

## 六、被否决的替代方案

- **把 F1 标为 `[A]` 并给逐字核对面** → 否决：纯删除**无「替换文本」**可逐字核；硬套 `[A]` 只能引**被删段**（删后必被检查 C 报「[A] 未落实或已漂移」——恒假）或引**保留段**（现状即已存在、恒真无信号），两者都不是有效核对面。故取 `[B]`（方向钉死到行号）＋ 断言 1 的「逐字节等价」作实质判据。
- **顺手收敛 `cache` 元组**（把字符数／行数／`**` 计数三项一并缓存，替掉 072 遗留的「按目标件另读一次」）→ 否决：属行为等价但**扩大改动面**（要动 072 刚钉住的取实测值处与两处解包行），且与「删死块」这一件事不同主题——按「债跟着主题走」记 §八 开放项，不与本批混做。
- **顺手做 072 开放项 7**（收窄「期望值数字」的形态判据，剥掉节号等噪声）→ 否决：其触发条件（真出现误配实例）**未到**；且一旦收窄，G15 里「子句数字个数 K」的 K 语义随之变化 → 从「改实现」升级为「动标准条文」，批的成本跳一档（须双维产物审查）。按原触发继续留待。
- **把重复段改成「先算后判」的单段结构（重排顺序）** → 否决：那是重构不是清理，`git diff` 会出现 `+` 行、行为等价的证明面变大（`judged`／`cand` 的时序），与本批「零风险清理」的定位不符。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批（072）规格的全部开放项**（必列面 ①）——本批改动面与 072 有交集（**同一文件** `scripts/check_spec.py`，且开放项 6 即由 072 记入待本批处置）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 072-1 | 字面集是否扩（`literal_token` 跳过含反引号／中文标点的 token） | 仍待 | 本批不动字面集；触发＝再出现该类 token 失效实例（G14 同题） |
| 072-2 | 「处」闸的残留面（不言明加粗的措辞不判／紧邻段提「加粗」而他指者会进面） | 仍待 | 本批不碰检查 B 的判据；触发＝两向任一在活文件上咬了人 |
| 072-3 | 期望取数的两类错配面（序写反／窗口拾取） | 仍待 | 本批不碰取数面；触发＝出现实例时加「数字序与命令序可疑不一致」提示 |
| 072-4 | 未判候选行的既有案例回归（`067` 的「修复后自核验三条」会以「未判」再现） | 仍待 | 本批不改其输出（断言 4 已把该行计数钉在 3）；触发＝核验时被误判为回归则按 072 §八 4 辨正 |
| 072-5 | 配对支路的真语料触发面为零 | 仍待 | 本批只删死块，配对逻辑未动；触发＝出现 K＝M 的真规格实例 |
| 072-6 | `check_counts()` 内的重复死块 | **本批处置** | **F1 即该项的落地**（删 `:588`–`:610`，行为等价由断言 1 的逐字节比对钉住）；本批另在 §八 记其遗留（`cache` 元组的收敛） |
| 072-7 | 未判候选行的 `nums` 会纳入子句内非期望数字（节号等） | 仍待 | **触发条件未到**（现语料零误配实例）；且收窄会牵动 G15 的 K 表述 → 属改规则、成本更高（见 §六 否决项） |

**汇总**：按条计 7 条：已履行 0｜作废 0｜**本批处置 1**｜仍待 6｜待作者 0 ＝ 7 ✓（逐格可复核）。**范围注（判据＝同节交集，非同名文件）**：本批只动 `check_spec.py` 的 `check_counts()` 函数体与 `CHANGELOG`；据此，**067 的五条开放项**虽同在该工具内，但其登记落点分别在 `测试与验收标准` §4 的 G14（判据只认字面 token）／G2（量词面）行与工具的「分档输出」面，与本批所删的死块**无同节交集、状态不变**，故不列；071 及更早各批的开放项落点在 `AGENTS.md`／`施工机制`／其余标准件，同样不触及——均以其各自核销表为索引（不重列）。

## 八、开放项

1. **`cache` 元组的收敛**（072 F2 的遗留）：现「处」的实测值走「按目标件另读一次」（每次多读一遍文件），而 `cache` 只存字符数与行数两项。行为等价但多一次 IO，且与「同件多处计数声称」时的去重设计不齐。触发＝下次触碰 `check_counts()` 的取实测值处时顺带收敛（须扩 `cache` 元组并改两处解包——本批删块后只剩一处，成本已降）。
2. **`check_counts()` 的阈值与分档**：本批不动输出体量（断言 1 已证逐字节相同）；072-1／067-1（分档输出 `--quiet`）仍在等触发，与本项同族。
3. **「等价性比对语料不得含本批改动件」是否入标准**（本批成稿审查 P0 的教训）：以「输出逐字节不变」作等价性断言时，比对语料须**排除本批所有改动件**，否则被测变量不止一个、断言必假红。候选落点＝`规格写作标准` §5 追加一条写作律（与第 9 条「一条断言只配一条命令」同型的**断言设计律**）。触发＝下一次以「逐字节不变」作断言的批次（若再踩同一坑即立法；本批已按此律把 `CHANGELOG.md` 移出语料并由断言 7 单独守卫）。

## 九、审查记录

- 2026-09-12｜成稿审查（fresh 只读分身）｜findings 共 7：**P0:1** P1:1 P2:5｜结论 **修订后通过**。处置：**F-1（P0：断言 1 的语料含本批改动件 `CHANGELOG.md`，与 F2 直接冲突 → 改完 md5 必变、强断言恒红；规划方的副本预演只覆盖 F1 故未暴露）**→ 语料**移出 `CHANGELOG.md`**（等价性比对只留一处变量）、基线**重新冻结**（行数 **370**／md5 `6d24131c…`）（井验「连跑两次一致」的可复现性）、新增**断言 7** 守卫该件措辞面、F2 补「不得引入新计数声称」的口径要点（并双向实测：安全写法 B 行 1 条／陷阱写法 2 条）、§四.2 注明预演只覆盖 F1；**F-2（P1：§七 范围注缺判据句，067 五条开放项同为工具落点、读者无法自证「未触及」）**→ 照归档 072 §七 句式补判据句并点名 067 各条的落点；**F-3（P2：第二段末行 `:647` 实为 `:648`）**→ 两处改正；**F-4（P2：lint 件数 42 实为 43）**→ 改正；**F-5（P2：断言 6 缺「本规格自身静态发现逐条判属」）**→ 补（照 072 断言 8）；**F-6（P2：hunk 起始行号读法可硬化为零歧义锚）**→ 采纳，断言 2／§一⑦ 补「删后 `wc -l` ＝ 969、`:588`／`:611` 行内容」三项锚（副本实测）；**F-7（P2：头注「单件」与两件改动面不一致）**→ 改「跨文件（2 件）」。审查方另核通过且被采纳的独立判断：等价性**反例搜索为零**（42 件语料＋合成语料差异 0、注入 `OSError` 两侧同型同消息）、**同类问题无漏扫**（`scripts/**/*.py` 内 ≥4 行完全重复块仅本处与一处不同函数的正当重复）、`[B]` 定级正确、「不派产物审查分身」定级正确、§七 核销算术平、§六 各否决项站得住。**该 P0 的教训记 §八 开放项 3**。
- 2026-09-12｜核验三步（规划方执行）｜**通过**。① 改动面核对：恰 2 件（`scripts/check_spec.py` `0 23`／`CHANGELOG.md` `2 0`）＋本批规格未跟踪；`check_spec.py` **1 个 hunk、新增行 0**，hunk 头 `@@ -589,23 +588,0 @@ def check_counts(text, root):`——起始行号读 589 属规格预判的**两种等价对齐之一**（两段逐行相同，git 留第一份或留第二份产物文件一致），上下文含落点名函数 ✓；禁改面 **0**。② 断言独立重跑（规划方自己的会话）：**断言 1–7 全绿**——1 语料 `diff` 空／md5 `6d24131c…`／**370 行**；2 `listed` 1／`judged += 1` 1／`wc -l` 969／`:588` 与 `:611` 两锚逐字命中；3 夹具三支路各 1 行；4 七项 **3／2／1／3／0／0／1**（与 072 实测同值）；5 hunk 1 ＋ `0 23`；6 `CHANGELOG` 命中 1／行长 **170** ≤400／`check.sh` 全绿／lint **43 件 0 issue**／链接 **175／0**；7 `CHANGELOG` 单件 B 行仍「1 条」。③ 内容抽查（通读两件 diff）：删除段＝第一段完整 23 行（其半截循环＋4 行初始化），保留段与 `return` 未动，函数体读来为单份干净实现；`CHANGELOG` 行措辞安全（`check_counts()` 非实存路径）、含「行为等价」与核验句、与 072 条目体例一致。
- 2026-09-12｜施工期 P1-1（`[4]` 段对本规格自身的三态由预验 2 条变 3 条）｜**判属预期、非缺陷**（规划方复核成立）：多出的一条是 `自噬预警: 计数重算死块清理（073） @ CHANGELOG.md`，成因＝断言 6-1 的 grep token 恰为 F2 行首要求的前缀串，F2 落盘后 `cur` 由 0 变 1 → 命中检查 A 的 `n>0 且 cur>0` 支路；任何合规 F2 写法都会触发，且「近似预测 2」是上限启发式（实跑 `grep -cF` ＝ 1）——已回填 §三 断言 6 的说明。施工方**未改断言、未改数字**，按红线 4 呈报后由规划方判定 ✓。
- 2026-09-12｜施工期 P2（规格 §二 F2 口径要点 ③ 的括注自相矛盾）｜**成立、已更正**：初稿写「写『删去重复段 23 行』则安全（…后者为『2 条』）」——同一句里既称安全又称 2 条。规划方复测三式：无实存路径跨度的两式 B 行皆「1 条」、陷阱式「2 条」；已按实测改正该括注并注明系施工方指出。
- 2026-09-12｜**产物审查**：按 `施工机制` §五，本批属「改实现（行为等价）」而非「改规则／结构／契约」，**产物审查位不派分身**；核验第三步已由规划方通读抽查完成（见上「核验三步」③）。
