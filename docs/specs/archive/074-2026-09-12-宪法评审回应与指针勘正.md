# 074-2026-09-12-宪法评审回应与指针勘正

> 定位：回应一份对 `AGENTS.md` 的评审报告（9 条：3 高／3 中／3 低），逐条对现文实核后**采纳 5 条、不采纳 4 条**——采纳者＝**§六.3 例外边界**（评审高 1）、**红线 1 总括式改写**（低 7）、**红线 4 补「判因确证」出口**（中 6）、**§七 ③ 补段序指针**（中 4）、**§四 末句改「增删改本节条目即停」**（低 9）；不采纳者＝术语双真源（**已在册**）、npx 策略缺失（**已在册**，且评审系顺着错指针读到空节）、门禁机械自检（**已在册**）、头注加版本与日期（会引入第二载体）。**另发现并修一处评审未见的病根**：三处载体把 npx 例外指向 `运行环境标准` `§一.3`，而该文在 `§一.1.3`（本仓 `§一.3` ＝小节 3「编码与行尾」）——**评审「npx 策略缺失」正是顺着错指针读空节所致**。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**——改宪法级条文（红线／§四／§六／§七）＋跨三件（`AGENTS.md`／两份标准件）；不命中「重」行（不动 `skills/`、不改域内流程／规则／产物契约、非架构级）。**送审深度**：改规则的批次 → **双维产物审查**（`施工机制` §五）。
> 自由度分布：[A]×1 [B]×6
> 必读输入：`AGENTS.md`（`:13` 红线 1／`:16` 红线 4／`:36` §三 lint 行／`:48` §四 CI 联网条／`:61` §四 末句／`:73` §六.3／`:81` §七 ③；全件 82 行）；`docs/standards/运行环境标准.md`（`:12` ＝§一.1.3 的 npx 例外句全文／`:22` ＝§一.2 第 5 条的命令形态契约／`:34` ＝§一.4 承诺句自指／`:81` ＝§三 机检锚点表的二级形残留）；`docs/standards/测试与验收标准.md`（`:40` ＝§1.3 守卫纪律指针／§1.2 红基线口径）；`docs/standards/文档写作标准.md`:108（加粗计数从句，**本批须保其成立**）；`docs/standards/术语标准.md`（定位行与 §一.1 的分工句）＋`docs/product/术语表.md`（定位行）——**评审高 2 的判据**；`docs/specs/archive/073-*.md` §八（核销 ①）、`archive/071-*.md` §九／`archive/070-*.md` §八（**六条**）／`archive/064-*.md` §七 第 6 条（核销 ②）；`docs/specs/施工机制.md` §二／§三／§四／§五／§六 ＋ `:148`（红线 1 的同题复述面，见 §六 裁定）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（评审 9 条逐条实核）与不变量

| # | 评审条目 | 现文实测 | 判读 |
| --- | --- | --- | --- |
| 高 1 | §六.3 例外可能架空权威顺序 | `AGENTS.md`:73「…从其指定——此时本序的层级**不适用**。」**无**「不得抵触宪法级约束」的限定 | **成立** → F1 |
| 高 2 | 术语双真源（`术语标准` vs `术语表`） | 两件**已互指并分工明文**：`术语标准` 定位行「产品语言词条表落 `docs/product/术语表.md`（**词条与叫法以该表为准**），本件管用法、维护规则」＋其 §一.1「词条与叫法以〔术语表〕为准；本标准管用法与维护纪律」；`术语表` 定位行「**词条表与叫法权威**……用法与维护纪律见 `docs/standards/术语标准.md`」。`AGENTS.md` 两处指认（§二 路由表→`术语标准` 管「某些词在这里是什么意思」；§六.3→「术语叫法以 `术语表` 为准」）与该分工**一致** | **不采纳（已在册）**：无冲突可解——「叫法」与「用法」是两个维度，各有单一真源且两件互指；再立「冲突时以 X 为准」即凭空造一个不存在的冲突 |
| 高 3 | `npx` 的联网与版本策略未在常用命令体现 | `运行环境标准` **`:12`（＝§一.1.3）**已四要素齐备：「**该工具链经 `npx` 调用**——首次使用可能联网取包，此后走本地缓存（离线可用）；本仓不含 `package.json`，故 `--no-install` 形态不解决该依赖——此为『守卫零外部依赖』的**已登记例外**」；离线／缺工具的行为＝`check.sh` [1] 段 `command -v npx` 缺失即 `fail=1`（`运行环境标准` §一.4 ③ 级行「报红或明示跳过」＋机检锚点 §1.3 行已登记「已实现」），且 `测试与验收标准` §1.2 明定「已实现段（含 [1]）的置红一律计红」 | **不采纳（已在册）**＋**病根另修**（F6）：评审所指「§一.3 没有强制版本与缓存策略」——该指针是**错的**（`§一.3` 在本仓读作「小节 3」＝编码与行尾），故评审读到了空节；例外文的四要素**全在** `§一.1.3`。锁版本一项另在 §六 逐条否决 |
| 中 4 | §七 的 `[3]`／`[4]` 段缺定义或链接 | `AGENTS.md`:81 §七 ③ 引「`[4]` 段」而全件无段序说明；段序真源＝`scripts/check.sh` 头注（`施工机制` §四 明定） | **成立** → F4（补指针，不复述段序） |
| 中 5 | 门禁机械自检不做 与「靠自觉」有张力 | `AGENTS.md`:59 该条**已有四件式**：逃生口＝「**提交前必跑 `bash scripts/check.sh` 的纪律**（hook 只是自动化、不增检测面——克隆未设 hooksPath 时由该纪律覆盖同一风险）」＋解冻触发条件＝「出现『未启用 hooksPath 而漏拦截』的实际事故」；`施工机制` §七 另有「提交前必跑……且退出 0」与「提交前自检四项」 | **不采纳（已在册）**：评审要的两件（逃生口／风险接受）就是该条的「逃生口」句本身；再写一遍即触反模式 #4（同一规则复制多份） |
| 中 6 | 红线 4 绝对禁止改断言，缺「断言本身错误」的出口 | `AGENTS.md`:16 禁的是「改断言、改数字、删检查**来变绿**」——判因后据裁定修订规格**本不在禁列**，但现文未点明 | **成立（措辞补全）** → F3 |
| 低 7 | 红线 1 可补总括句，覆盖 fetch／pull／merge／rebase／stash | `AGENTS.md`:13 现为「**一切 git 写操作**（包括但不限于 `add`／`commit`／`push`／`tag`／`reset`／`checkout -b`）」——「包括但不限于」已开列，但**总括是概念式的**（「写操作」），评审要的是**可自测的形态判据**（「凡改变 `.git/` 或工作区状态的命令」） | **成立** → F2（总括句 ＋ 列举保留；自测句同步改述） |
| 低 8 | 头注可加版本号与最后更新日期 | 全仓标准件与宪法**普遍无**版本／日期头注（`术语标准`／`运行环境标准` 等头注只有「定位／依据」）；`AGENTS.md` §二 路由表已载「某个决定／某次变更是怎么回事（沿革）→ `CHANGELOG.md` 与 git 历史」 | **不采纳**（理由入 §六） |
| 低 9 | §四 末「触碰本节即停」应改「增删改本节条目即停」 | `AGENTS.md`:61「…；触碰本节即停，先修订本节并取得作者确认；…」——「触碰」可被读成涵盖「引用／讨论」 | **成立** → F5 |
| **新** | （评审未见）三处 npx 例外指针指向 `§一.3` | 三处：`AGENTS.md`:48（**链接形态**：链接文字 `运行环境标准` → `docs/standards/运行环境标准.md`，其后为 `§一.3`）／`运行环境标准`:34（同件自指「见 §一.3」）／`测试与验收标准`:40「见 `运行环境标准` §一.3」——**均应为 `§一.1.3`**（三处**同题**、同一错形；`产品定义.md`:53 的「§一.3」指 `测试与验收标准` 的小节 3，**正确，不动**） | **成立（新发现）** → F6 |

**不变量（施工前后须同）**：`AGENTS.md` 的**行数 82**（本批五处均为行内改写，不增删行）与 `**` 计数 **91**（本批**不新增加粗**——`文档写作标准`:108 的「现 **91 处**」须继续成立，否则即触发 `064-6` 的加粗留痕面）；§一 红线 2／3／5／6 与 §二／§三／§五／§七 其余各行零改动；`docs/specs/施工机制.md`、`docs/product/`、`skills/`（未创建）、`scripts/`（含 `check.sh` 与 `hooks/`）、`docs/specs/archive/` 零改动；两份标准件除 F6 点名的**指针串**外零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `AGENTS.md`:73（§六.3 末） | 句末**追加限定**：「——该指定只在宪法级约束未覆盖的范围内有效，不得抵触 §一 红线／§四 不做清单／§五 分工原则；冲突时按 `施工机制` §六 升级，升级结果同样不得违反宪法级约束。」（**不加粗**——§六 通篇无加粗体例） | 评审高 1 | [B] |
| F2 | `AGENTS.md`:13（§一 红线 1） | 改**总括式**且**保住 git 域限定**（成稿审查 P1：初稿的「凡改变 `.git/` 或工作区状态的**命令**」会覆盖一切改文件的命令、且 `git status` 的索引刷新会被自测句读成写）：总括＝「**凡改变仓库状态的 git 命令**（改 `.git/` 内任何内容——引用／对象／索引／配置等，或改工作区文件；`fetch` 等只动 `.git/` 者同样在内；只读命令的索引缓存刷新不计）」（替换现「**一切 git 写操作**」），其后括注列举**补全**为「（`add`／`commit`／`push`／`pull`／`fetch`／`tag`／`reset`／`checkout`／`merge`／`rebase`／`stash` 等，包括但不限于）」；自测句同步改述为「（自测：这条 git 命令会改到仓库吗？会＝本次确认拿到了吗？）」（**自测句内不得带 `**`**）。**加粗对数不变**（仍两处：条名 ＋ 总括句） | 评审低 7 ＋ 成稿审查 P1（F-06） | [B] |
| F3 | `AGENTS.md`:16（§一 红线 4） | 禁列句后**追加出口**：「——判因确证断言或数字本身有错时，先呈报并取得裁定，由规划方修订规格后再改（呈报与分级见 `施工机制` §六），不在此禁列。」（**不加粗**；禁列本身不动。成稿审查 P2 已把初稿的「先呈报并由裁定修订规格」补出**呈报对象与执行者**——防被引作「改断言合法」的口实，并与 `施工机制` §六 的 P0 停摆／P1 申报衔接） | 评审中 6 ＋ 成稿审查 P2（F-13） | [B] |
| F4 | `AGENTS.md`:81（§七 ③） | 「`[4]` 段的每条预警」前**补段序指针**：「机检段（段序见 `scripts/check.sh` 头注）里 `[4]` 段的每条预警……」（**不复述**段序内容——段序真源唯一） | 评审中 4 ＋ `施工机制` §四 | [B] |
| F5 | `AGENTS.md`:61（§四 末句） | 「触碰本节即停」→「增删改本节条目即停（引用与讨论不必停）」（**目标文本不得带 `**`**——成稿审查 P0：初稿把新短语写成加粗，落文即 `**` 91→93、与断言 3 和不变量互斥；后半句「先修订本节并取得作者确认」与「解冻一律由作者裁决并记入 `CHANGELOG`」不动） | 评审低 9 ＋ 成稿审查 P0（F-03） | [B] |
| F6 | `AGENTS.md`:48 ＋ `docs/standards/运行环境标准.md`:34 ＋ `docs/standards/测试与验收标准.md`:40 | **三处指针串逐字改正**：`运行环境标准` 的 `§一.3` → `§一.1.3`（三处形态各一——`AGENTS.md`:48 为**链接形态**：链接文字 `运行环境标准` → `docs/standards/运行环境标准.md`，其后紧邻 `§一.3`；`运行环境标准`:34 为**同件自指**「见 §一.3」；`测试与验收标准`:40 为「见 `运行环境标准` §一.3」。**三处均只改节号那五个字符**，链接目标、句内其余文字与 `〔出处〕` 全部保留） | 新发现（评审高 3 的病根） | [A] |
| F7 | `CHANGELOG.md` | 本批一行（≤400 字符），落 `[Unreleased]` › `### Changed` 首条，以「宪法评审回应与指针勘正（074）：…」起首，写明「采纳 5／不采纳 4」与「三处 npx 指针勘正」 | 过程产物两项 | [B] |

**口径要点（易错）**：① **不得新增加粗**（F1–F5 五处改写中仅 F2 涉及加粗、且对数与现文相等——条名 ＋ 总括句两对；F1／F3／F5 零处、F6 只动节号不动加粗；改后 `**` 计数须仍为 **91**）；② **改动行共 6 行**（F1–F5 各一行 ＋ F6 的 `AGENTS.md`:48 一行）均为**行内改写**，不得增删行（`AGENTS.md` 须仍 82 行）；③ F6 是**逐字串替换**——只动 `§一.3`→`§一.1.3` 五个字符，链接目标、句内其余文字与 `〔出处〕` 全部保留；④ 不得把段序（`[0]`–`[5]` 的清单）复述进 `AGENTS.md`（`施工机制` §四：段序真源＝`check.sh` 头注）；⑤ **落文文本不得带 `**`**（F5 的目标短语尤其——成稿审查 P0 即此）。

## 三、验收断言（含预验记录）

### 断言 1｜五处改动的 token 落位

```bash
grep -c "不得抵触 §一 红线" AGENTS.md
grep -c "凡改变仓库状态的 git 命令" AGENTS.md
grep -c "先呈报并取得裁定" AGENTS.md
grep -c "段序见 \`scripts/check.sh\` 头注" AGENTS.md
grep -c "增删改本节条目即停" AGENTS.md
grep -c "触碰本节即停" AGENTS.md
```

期望依次 **1／1／1／1／1／0**。**预验基线 0／0／0／0／0／1**（末项＝现文形态，改后须消失）。

### 断言 2｜三处指针勘正（F6）

```bash
grep -c "运行环境标准.md) §一\.3" AGENTS.md
grep -c "见 §一\.3" docs/standards/运行环境标准.md
grep -c "运行环境标准\` §一\.3" docs/standards/测试与验收标准.md
grep -c "运行环境标准.md) §一\.1\.3" AGENTS.md
grep -c "见 §一\.1\.3" docs/standards/运行环境标准.md
grep -c "运行环境标准\` §一\.1\.3" docs/standards/测试与验收标准.md
```

期望依次 **0／0／0／1／1／1**。**预验基线**：**1／1／1／0／0／0**（三项均已实测——三处载体各一，且改后目标串现均为 0）。
**口径说明**：不用「全仓找 `§一.3`」式宽命令作判据——实测该类命令会命中**无关自指**（`产品定义.md`:53 的「§一.3」指 `测试与验收标准` 的小节 3，正确）、`archive/` 内历史记录与**本规格自身**的叙述，故判据**指名到三处载体各自的唯一串**；`CHANGELOG` 与 `archive/` 属历史记录、本批不改。

### 断言 3｜加粗计数与行数不变（守 `文档写作标准`:108 与 `064-6`）

```bash
python -c "print(open('AGENTS.md',encoding='utf-8').read().count('**'))"
wc -l < AGENTS.md
python scripts/check_spec.py --static docs/standards/文档写作标准.md
```

期望：**91**／**82**／`--static` 输出**不含** `计数不符`（该件 `:108` 载「`AGENTS.md` 现 **91 处**」，本批若改动该计数即在此转红——`072` 批 F2 扩面后的现成闸）。**预验基线**：91／82／不含 `计数不符` ✓（三项均实测）。

### 断言 4｜红线的自测句仍在（071 批装置未破）

```bash
grep -c "（自测：" AGENTS.md
```

期望 **8**（071 批立的红线六条 ＋ §五 原则 2／3 各一）。**预验基线 8**；F2 的自测句系**改述**（仍以「（自测：」起首），F3 不新增自测句。

### 断言 5｜禁改面与改动面

```bash
git status --porcelain . ':!AGENTS.md' ':!CHANGELOG.md' ':!docs/standards/运行环境标准.md' ':!docs/standards/测试与验收标准.md' ':!docs/specs/074-2026-09-12-宪法评审回应与指针勘正.md' | wc -l
git diff --numstat AGENTS.md docs/standards/运行环境标准.md docs/standards/测试与验收标准.md
```

期望：第一项 **0**——**判据取补集式**：命令为「以 `.` 为基、排除本批**四个**改动件（`AGENTS.md`／`CHANGELOG.md`／两份标准件）与**在制规格自身**」，故 0 等价于「**除声明的改动面之外，全仓零改动**」（比逐一枚举禁改面更严，且免漏项——成稿审查 P1 指出初稿误把 `AGENTS.md` 列进该命令、施工后必为 1；窄域复审 N-3 指出初稿枚举的「九件」实只列 2 件）；第二项＝**`6 6`／`1 1`／`1 1`**（`AGENTS.md` 五个 F 项改五处、F6 另改 `:48` 一处 → 共 **6 行**，行内改写不增删行；两份标准件各 1 行；出现**只增不减**即越界）。**预验基线** 0／（无 diff）。

### 断言 6｜过程产物两项与全仓守卫

```bash
grep -cF '宪法评审回应与指针勘正（074）' CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '宪法评审回应与指针勘正（074）' in l][0].rstrip()))"
bash scripts/check.sh
npx markdownlint-cli2 "**/*.md"
```

另核：全仓相对链接**断链 0** 且条数**不低于 175**（预验 175／0；**判据不含本规格自身**——初稿曾在规格内逐字引用链接形态而自引 2 条断链，成稿审查 P0 已改为描述式引用，见 F-02 处置）；`check.sh` 的 `[4]` 段对本规格自身的静态发现须**逐条判属**（照归档 072 规格 §四 断言 8）并记入核验结论——**条数不设期望值**：该数随规格文本的每次修订而变（本轮修订中实测在 11／12 之间摆动，即小工具批 073 的同类教训），故判据是「**每一条都有归属**」而非计数。**施工前实测（记录，非期望）＝「有预警 11 条」＋「B：未提取到可判定的尺寸声称（4 条非整件口径声称未判）」**。期望：第一项 ≥1、第二项 ≤400、`check.sh` 退出 0、lint 0 issue。**预验基线**：第一项 **0**、链接 **175／0**（**含／不含本规格同值**——本规格自身**不含任何链接语法**，故对总数无贡献；判据命令同归档 072 规格 §四 断言 5 的一行式）、`check.sh` 退出 0（**本规格在制，`[4]` 段实跑产出（**记录值，非期望值**——该数随规格文本修订而变）「有预警 11 条」＋「B：…（4 条非整件口径声称未判）」，须逐条判属）、lint **44 件 0 issue**（含本规格；073 时点为 43）。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 十条逐条实跑——**六个行号的现文**（F1 `:73`／F2 `:13`／F3 `:16`／F4 `:81`／F5 `:61` ＋ F6 的 `:48`）逐行读取；`AGENTS.md` 82 行／`**` 91；两件标准件的互指句与分工句逐字读取（术语双真源判据）；`运行环境标准`:12 的 npx 四要素逐字读取；三处错指针以 `grep` 全仓复核（并确认 `产品定义.md`:53 的 `§一.3` 指 `测试与验收标准` 的小节 3、**正确不动**）；`check.sh` [1] 段的 `command -v npx` 缺失置红行为读取；`测试与验收标准` §1.2 的红基线口径读取。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1–6 的**施工前基线**已逐条实跑（断言 1 的六项 0／0／0／0／0／1；断言 2 的 3／各 0；断言 3 的 91／82／不含 `计数不符`；断言 4 的 **8**；断言 5 的 0；断言 6 的 0／175／0／`check.sh` 0／lint 0）。**逐条核断言 token 与目标文本互斥性**：断言 1 的六项中前五项是**目标文本内**串、末项「触碰本节即停」是**改后须消失**串（预验为 1 ✓ 反向断言）；断言 4 的 `（自测：` 在 F2 改述后仍为 8 ✓。**无未预验项**。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/` 全部（含本批规格自身与 `archive/`）、`docs/product/`、`skills/`（未创建）、`scripts/`（含 `check.sh` 与 `hooks/`）、`README.md`、`docs/standards/` 除 F6 点名的**两份**（且只许动那一处指针串）。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具。
- 禁止越权：不改版本号与 tag；不新增文件（本批只改**四件**既有文件：`AGENTS.md`／`运行环境标准.md`／`测试与验收标准.md`／`CHANGELOG.md`）；不建台账／不建索引件。
- 禁止改断言、改数字、删检查来变绿；**不得新增加粗、不得增删 `AGENTS.md` 行**（断言 3 钉住，**新增文字不得带 `**`**（F2 的总括句加粗系**既有体例、对数不变**，不在禁列；F5 的目标短语尤其不得自行加粗））；**不得把段序清单复述进 `AGENTS.md`**；**不得顺带改动 §六.3 的三处真源举例**（`施工机制`／`术语表`／`架构设计` 的指认本批不动——术语双真源一事业经判为**已在册**，见 §六）。

## 六、被否决的替代方案（含评审四条不采纳项的逐条理由）

- **§六.3 只写「从其指定」不加限定**（维持现文）→ 否决：评审高 1 成立——现文可被读成「指定可架空宪法级约束」；加一句限定即可闭合，且与 §六.2 的层级表自洽（限定句写明「宪法级约束未覆盖的范围」内指定才有效）。
- **术语「合并为单一真源」或「定冲突时以 `术语标准` 为准」** → 否决：两件已分工明文且互指（§一 高 2 行引原文），「叫法」与「用法／维护」是两个维度、各有单一真源；再立一条冲突规则＝为一个**不存在的冲突**增设口径，且会与两件的自述互相打架（第二真源）。
- **`npx` 改 `npx --no-install markdownlint-cli2@<版本>` ＋ 在 `运行环境标准` 补策略** → 否决（三层理由）：① **`--no-install` 已于 070 批裁定否决**（`运行环境标准`:12 明载「本仓不含 `package.json`，故 `--no-install` 形态不解决该依赖」，且 070 §一② 记明「该 flag 只影响『是否允许自动安装』，不改变『[1] 段依赖外部工具链』这一事实」）；② **「版本固定」成本大于收益**——要同时改**四处载体**：`AGENTS.md` §三 的常用命令行（`:36`）、`运行环境标准` §一.1.3（`:12`，例外句的缓存／离线述）与 §一.2 第 5 条（`:22`，「npx 工具显式传 glob」的命令形态契约）、以及 `scripts/check.sh` [1] 段的**实际命令**（动门禁脚本本身）；而锁版本对**冷机**行为毫无改善（仍取包），唯一收益（杜绝上游新规则致红）已有现成出口：`[1]` 属**已实现段**、置红即计红，配合 §一.4 ③ 级「报错或明示跳过，不得静默失败」与「失败先判因」即可处置；③ 评审所指「`§一.3` 没有策略」系**错指针**所致的误读——四要素（首次联网／本地缓存／离线可用／`--no-install` 不适用）**全在** `§一.1.3`，本批改指针即可（F6）。
- **在 `施工机制` §七 补「提交前检查清单」以承认风险接受** → 否决：`AGENTS.md` §四 该条的「逃生口」句已是风险接受的明文（§一 中 5 行引原文），`施工机制` §七 另有「提交前必跑……退出 0」与「提交前自检四项」；再补即第二载体（反模式 #4）。
- **`施工机制.md`:148 的「一切 git 写操作」复述**（成稿审查 P2）→ **裁定：本批不改**（§五 禁改该件）：该句是**带指针的复述**（句末即「（AGENTS.md 红线 1）」），F2 改述后**概念完全一致**、仅措辞略异；同题复述的收口按「债跟着主题走」留待下批触碰该件时按 F2 口径复述（**触发＝下次触碰 `施工机制`**）。同题第二载体 `版本与分发标准.md`:50 为纯指针（无复述），不受影响。
- **头注加版本号与最后更新日期** → 否决：① 全仓标准件与宪法**均无**此类头注（体例一致）；② 沿革的单一真源已是 `CHANGELOG` ＋ git 历史（§二 路由表已载该行），头注加版本即**第二载体**且立刻引入「何时 bump、谁 bump」的新规则债（本仓无此纪律）；③ 宪法是**规则文件**而非**发布物**，`版本与分发标准` 的 bump 语义不适用于它。
- **F6 顺带把 `AGENTS.md`:48 的指针改成纯文本（去链接）** → 否决：链接形态是该行的既有体例（070 落地时所定），本批只勘节号；改形态属体例变更、与本批主题无关。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批（073）规格的全部开放项**（必列面 ①）＋**本批改动直接触及或可能变状态者**（必列面 ②）。**判据＝同节交集**（照归档 073 规格 §七 的口径）——「**同文件**」单独**不构成**必列理由（故同在 `AGENTS.md` 的 `070-1`〔项目一句加链接〕与 `070-4`〔已折入 `064-6` 行〕不另列；`070` §八 六条中确与本批同题者 2／5／6 已列）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 073-1 | `cache` 元组的收敛（072 F2 遗留：`处` 的实测值走「按目标件另读一次」） | 仍待 | 本批不动 `check_spec.py`；触发＝下次触碰 `check_counts()` 时顺带收敛 |
| 073-2 | `check_counts()` 的阈值与分档（072-1／067-1 同族） | 仍待 | 同上；触发＝再两批后仍觉噪声压过信息 |
| 073-3 | 「等价性比对语料不得含本批改动件」是否入 `规格写作标准` §5 | 仍待 | 本批不动该标准（F6 只动 `测试与验收标准` 的指针串）；触发＝下一次以「逐字节不变」作断言的批次 |
| 071-1 | 可移植模板形态的参考（064-1／059-2／060-3 同题） | 仍待 | 本批动 `AGENTS.md` 五处条文，但**未涉模板形态**；触发＝首次移植 |
| 071-2 | 自测句的铺开边界与覆盖口径 | 仍待（**同主题已核**） | 本批**改述了红线 1 的自测句**（随总括句同步，仍以「（自测：」起首、总数仍 8——断言 4 钉住），但**未改**铺开边界与覆盖口径 → 状态不变；触发＝出现「条文读了但不当场校验」实例 |
| 071-3 | 归档件机械修复的纪律例外（登记留待触碰 `施工机制` 时补） | 仍待 | 本批不动 `施工机制.md`；触发＝下批触碰该件时补一笔 |
| 071-4 | 自检信号的自校验（是否入核验请求模板） | 仍待 | 本批动 §七 ③ 的**指针**（F4），未动「信号是否入核验请求模板」这一题；触发＝两批后仍无人据此核对 |
| 070-3 | 评审 #14（膨胀后下沉 ADR）暂无动作 | 仍待 | 本批**改了 `AGENTS.md` 五处**但**行数仍 82**（断言 3 钉住）——未触发「膨胀」阈值；触发＝按 §四「内生必要性自问」评估 |
| 070-2 | 评审 #13 的「解冻触发条件主观」（解冻判据是否逐条**可测化**） | 仍待 | 其触发原文＝「留待**下次触碰 §四** 时评估」——本批 F5 改的正是 §四 末句，但 F5 只改「触碰→增删改」的**触发措辞**，未动「解冻判据可测化」这一题 → 状态不变 |
| 070-5 | `施工机制` §四 的零依赖纪律句（`施工机制.md`:82）未加例外指针 | 仍待 | **与 F6 同题**（本批只勘正三处 npx 例外指针）；`施工机制` 本批禁改 → 该句仍无指针，触发条件不变（原文＝「下次触碰 `施工机制` 时补同一指针」） |
| 070-6 | §六.3 例外面的扩张（070 只举「术语叫法」「域清单」两例） | 仍待 | 本批 F1 **只给 §六.3 加「不得抵触宪法级约束」的限定**，**未动其举例句**（§五 明禁）→ 状态不变；触发＝出现第三类「某事项以某件为准」的明文指定 |
| 064-6 | 加粗密度超阈值件分类留痕（070 批处置，备案；070-4 同挂本行） | **已履行（本批维持）** | 本批五处改动**不新增加粗**：`AGENTS.md` 的 `**` 计数改前改后均 **91**（断言 3 与 `文档写作标准`:108 双向钉住）——070 的处置继续成立 |

**汇总**：按条计 **12** 条：已履行 1｜作废 0｜本批处置 0｜**仍待 11**｜待作者 0 ＝ 12 ✓（逐格可复核）。**范围注**：072／069／068／066／065 的开放项落点在 `check_spec.py` 的判据面、`测试与验收标准` §4 缺口表、`规格写作标准` §1／§5、`施工机制`（本批只动 `测试与验收标准` 的 `§1.3` 指针串与 §4 缺口表面无同节交集，其余件未触及）——均以其各自核销表为索引（不重列）。

## 八、开放项

1. **评审「三项高优先级」的处置留痕**：高 2／高 3 判为「已在册」——若作者不认（例如仍主张锁版本、或主张术语合并），回退面很小：高 2 只涉 §六.3 的举例句、高 3 只涉 F6 的三处指针串（回退＝逐字还原 `§一.3`，无连带）。
2. **二级形／三级形指针的读法风险与残留**：本批把三处指针改正为三级形（`§一.1.3`），但全文仍混用二级形——**同件内即有残留**：`运行环境标准`:81（§三 机检锚点表「§1.3 Node 可用性」指同一条，与表内 `§1.4`／`§3.1`／`§5` 同体例，本批 §五 只许动 `:34` 故未改）；另 `触发与交互标准.md`:62 的「§1.3 域间不互调」读法**正确**（＝该件小节 1 第 3 条）。触发＝再出现一次「按错指针读到空节」的实例，届时考虑在 `文档写作标准` §一 立一句**引用写法**（如「引用须写到能唯一落点的那一級」）。
3. **F6 未覆盖的形态**：本次只勘正 `运行环境标准` 的指针；若将来出现指向别件「小节 x 第 y 条」的二级形误用，照本批同法处置。
4. **`AGENTS.md` §三 的 lint 行是否也需例外指针**（成稿审查的残留意见）：评审原句是「未在**常用命令**体现」，而 F6 只修了 §四 不做清单里的指针——§三 的 `npx markdownlint-cli2` 行仍无指针。**裁定倾向：不加**（§三 是「常用命令」清单、§四 的例外句已给出机制指针；在 §三 再挂指针属同题第二处）。触发＝出现「照 §三 命令跑、不明其环境前提」的实例。

## 九、审查记录

- 2026-09-12｜成稿审查（fresh 只读分身）｜findings 共 **13**：**P0:3** P1:3 P2:7｜结论 **修订后通过**。处置：**F-01（P0：§七 核销表必列面缺行——070 §八 实为六条，其 2／5／6 与本批 F5／F6／F1 同题）**→ 补三行、汇总改 12 条（规划方独立复核 070 §八 六条与 064 的开放项在 **§七** 成立）；**F-02（P0：本规格自身逐字引用链接形态，自引 2 条断链——链接基准为 `docs/specs/`）**→ 两处改**描述式引法**（实测 177／断链 2 → 修后 175／0）；**F-03（P0：F5 的目标文本自带 `**`，与「不加粗」及 `**`＝91 不变量互斥）**→ 目标短语去掉加粗、并在 §五 与口径要点写明「落文不得带 `**`」；**F-04（P1：断言 5 的 numstat 应为 `6 6` 而非 `5 5`）**→ 改正并统一「五处 F 项／六个行号」的计数口径；**F-05（P1：断言 5 把改动面 `AGENTS.md` 混进禁改面，施工后恒为 1）**→ 命令剔除 `AGENTS.md`、按 §五 补齐禁改件；**F-06（P1：F2 总括「凡改变 `.git/` 或工作区状态的命令」过度触发——会覆盖一切改文件的命令，且 `git status` 的索引刷新会被自测句读成写）**→ 总括改「**凡改变仓库状态的 git 命令**（改 `.git/` 中的引用／对象／索引，或改工作区文件；`fetch` 等只动 `.git/` 者同样在内；**只读命令的索引缓存刷新不计**）」，自测句同步去加粗；**F-07（P2：必读输入缺术语两件与 `施工机制` §四）**→ 补齐；**F-08（P2：064 的开放项在 §七 而非 §八）**→ 改正（本批主题即指针勘正，此误尤须改）；**F-09（P2：断言 6 的 lint 件数 43 实为 44；「[4] 段现『无在制规格』」在本规格在制时为假）**→ 改正并写明 `[4]` 段实跑产出 11 条预警＋4 条候选、须逐条判属；**F-10（P2：§六 ② 的载体 `§一.1.5` 疑误）**→ 改为四处载体（`AGENTS.md` §三 `:36`／`运行环境标准` `:12` 与 `:22`／`check.sh` [1] 段），编号按该件结构厘清（`:22` 属 **§一.2 第 5 条**）；**F-11（P2：`施工机制.md`:148 同题复述未留裁定）**→ §六 加「不改」裁定与触发；**F-12（P2：`运行环境标准`:81 的二级形残留未点名）**→ §八 开放项 2 点名；**F-13（P2：F3 出口未写呈报对象与执行者）**→ 改为「先呈报并取得裁定，由规划方修订规格后再改（呈报与分级见 `施工机制` §六）」；审查方另核通过项含「不采纳四项尽力反驳后成立」（其残留意见「`AGENTS.md` §三 是否需指针」已记 §八 开放项 4）。
- 2026-09-12｜**窄域复审**（P0＝3 ≥ 2，按 `施工机制` §五 止损条 2：修订后只核原 P0 修复项 ＋ 其直接影响面）｜findings 共 **9**：**P0:1** P1:3 P2:5｜结论 **修订后通过**。处置：**N-1（P0：断言 1 的第 2／3 项 token 仍是改前措辞——与 F2／F3 目标文本互斥，施工后必恒红；073 批同类）**→ 两项 token 改为 `凡改变仓库状态的 git 命令`／`先呈报并取得裁定`（并机器核过：五个新 token 皆落在对应 F 项的改动格内，见 §四.2）；**N-2（P1：§五「落文一律不得带 `**`」过宽——F2 的总括句必须带）**→ 收窄为「**新增文字**不得带 `**`（F2 的总括句系既有体例、对数不变）」；**N-3（P1：断言 5 的禁改面枚举漏 7 件标准件与 `.zcode`／`施工机制`）**→ 改**补集式**判据（以 `.` 为基、排除四个批次件与在制规格 → 0 等价于「除改动面外全仓零改动」）；**N-4（P1：§九 头计数 14／P1:5／P2:6 与实列 13 条不符）**→ 改「13：P0:3 P1:3 P2:7」；**N-5（P2：`[4]` 段条数写 11 实为 12）**→ 因该数**随规格文本每次修订而摆动**（实测 11↔12），改**走查判据**「每条都有归属」＋记录实测值（不设期望值——073 批 hunk 数同教训）；**N-6（P2：链接数括注自相矛盾）**→ 改「含／不含本规格同值（本规格自身无链接语法）」；**N-7（P2：F2 括注漏 `.git/config`／worktree）**→ 放宽为「改 `.git/` 内任何内容——引用／对象／索引／配置等」；**N-8（P2：链接判据缺命令出处）**→ 补「命令同归档 072 §四 断言 5 的一行式」；**N-9（P2：§七 头两条判据互斥）**→ 统一为「同节交集」为唯一判据并点名 `070-1`／`070-4` 为何不列；另修字形「三級→三级」。
- 2026-09-12｜核验三步（规划方执行）｜**通过**。① 改动面核对：恰四件（`AGENTS.md` `6 6`／`CHANGELOG.md` `1 0`／两份标准件各 `1 1`）＋本批规格未跟踪；`AGENTS.md` **六个 hunk 逐一对应六个行号**（`@@ -13`／`-16`／`-48`／`-61`／`-73`／`-81`，均单行），无表外行；补集式禁改判据 **0**。② 断言独立重跑（规划方自己的会话）：**断言 1–6 全绿**——1：`1／1／1／1／1／0`；2：旧串 `0／0／0` ＋ 新串 `1／1／1`；3：`**`＝**91**／行数＝**82**／`文档写作标准` 不含「计数不符」；4：`（自测：`＝**8**；5：numstat `6 6`／`1 1`／`1 1`；6：`CHANGELOG` 命中 1／`check.sh` 退出 0／lint **44 件 0 issue**／链接 **175／0**。③ 内容抽查：六行 diff 逐行读，三处指针**只动节号**（链接目标与 `〔出处〕` 未动），F1／F3 的落文以「——」吸收原句末句号（见下 P1-1）。
- 2026-09-12｜施工期 P1 三条 ＋ 一问的裁定｜**P1-1 照准**：F1／F3 的目标文本以「——」起首，若照「追加」字面接在句末句号后即落成「。——」（触 `文档写作标准` §一.2.3 标点不叠用）；施工方以插入的「——」**吸收**原句号、目标文本逐字保留——采纳，且此为「按方向自定实现」的正当裁量。**P1-2 照准并更正规格**：F2 的目标文本含 `**配置**`（第三对加粗，系**规划方在 N-7 修订时引入**，与同格「加粗对数不变／仍两处」、§一 不变量与断言 3 的 91 互斥）→ 施工方按**硬不变量优先**落为不加粗的「配置」（实跑 91 ✓）；**规格该处已同步去加粗**（本节修订）。**P1-3 照准、不改**：F2 落文出现相邻两处括注（限定 ＋ 列举）——二者角色不同（限定「什么算改仓库」／列举「典型命令」），合并会改引文边界；相邻括注不属「标点叠用」，保留。**问 4 无动作**：列举依规格引文取 `checkout`（覆盖原 `checkout -b` 的更宽形态），总括句已兜底，无需回退。

- 2026-09-12｜**双维产物审查**（fresh、只读；本批改规则 → `施工机制` §五 双维）｜findings 共 **7**：P0:0 P1:0 **P2:7**｜结论 **通过**（P2 按 `施工机制` §六 不阻塞）。审查方独立复跑六条断言**全部复现**、硬性不变量（82 行／`**` 91／`（自测：` 8）改前改后同值、三处指针以 `--word-diff` 逐字核**只动节号**（链接目标与 `〔出处〕` 零动）、四道机闸全绿（`check.sh` 0／lint 44 件 0 issue／链接 175・断链 0／`--static` 逐条可判属）；并**独立复核四项不采纳的现文依据**（术语两件互指与分工句、`运行环境标准` §一.1.3 四要素、`AGENTS.md` §四 门禁条四件式、11 件头注无版本日期）——**在册为真，规划方未读漏**。处置：**F-02（P2：红线 4 的自测句未随出口改述，合法路径上会误报）→ 就地修**（自测句补「动后三者＝已呈报并取得裁定了吗？」；仍以「（自测：」起首、不加粗、不增删行、不涉任何断言 token）；**F-04（P2：枚举尾「等，包括但不限于」两非穷举标记连用，系本批引入）→ 就地修**（删「等，」）；**F-01（本仓之外的 git 写〔`config --global`／凭据库〕不在新总括内）／F-03（`AGENTS.md`:73 一句三处「——」的折返句）／F-06（`施工机制.md`:148 的复述覆盖面较新总括微收）→ 记 §八 开放项**（触发均未到，且 §五 禁改 `施工机制`）；**F-05（`运行环境标准`:81 同件内 `§1.3` 与 `§一.1.3` 并存——属该表体例、非错）→ 由 §八 开放项 2 承载**；**F-07（核验记录未落断言 6 要求的 `[4]` 段逐条判属）→ 就此处补记**：施工后实跑 **15 条**（该数随规格文本修订而变，故不设期望值）：12 条**属预期**（＝断言 1／2／4／6 的期望值或归零型已达成、或 F7 的 `CHANGELOG` 新增），3 条为**工具口径假阳性**——① 段序那条 token（形状＝「段序见」＋反引号包住的脚本路径＋「头注」，规格自身以反斜杠转义其反引号；故字面比对落空——实跑 grep -cF ＝1）；② 三处 §一.1.3 新形同类（token 含 \. 正则转义，实跑各 1）；③ [A] 未落实或已漂移「见 运行环境标准 §一.3 @ AGENTS.md」（检查 C 对 [A] 行取**引号块**逐字核，该块引的是改前形态；三处新形由断言 2 独立钉住）。**无未判项**。审查方另自陈一处过程瑕疵（在仓库根落过一个临时重定向文件、同命令内即删；`git status` 确认无残留）——记录备查。
- 2026-09-12｜产物审查两条 P2 就地修后的**复验**（规划方）｜`AGENTS.md` 仍 **82 行／`**`＝91**（两处修正在原行内，未动加粗）；`（自测：` 仍 **8**；断言 1 六项 `1／1／1／1／1／0`、断言 2 六项 `0／0／0／1／1／1`、断言 3 三项绿、断言 5 numstat `6 6`／`1 1`／`1 1`、断言 6 `check.sh` 退出 0／lint 44 件 0 issue／链接 175・0——**全绿**。
- 2026-09-12｜过程注记（规划方）｜产物审查两条 P2 的**就地修**系以脚本改写 `AGENTS.md`，该脚本在 Windows 下把**全件行尾**写成了 CRLF（82 行）——已当场以二进制读写归一为 **LF**（`运行环境标准` §一.3.1）；归一对 `git diff --numstat`（仍 `6 6`）与六个 hunk 头（仍单行、行号不变）**零影响**，且 `* text=auto eol=lf` 本会在入库时归一。另注：本批两份标准件与 `CHANGELOG.md` 的改写**未**出现同类问题（复核均已是 LF）。
