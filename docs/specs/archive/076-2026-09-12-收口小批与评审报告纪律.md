# 076-2026-09-12-收口小批与评审报告纪律

> 定位：收口三件事——① **075 §八 的一行级遗留**（`extract()` 围栏语言大小写与 `FENCE_BLOCK` 口径不一；`check_pairs()` 无 `errors='replace'`；`_git_readonly()` 对 `--` 之后实参仍按选项判——**预验实证为误收窄**：`git grep -F -c -- "-delete" f` 现判 `False`，而 `--` 之后的是**搜索模式**不是选项）；② **评审报告纪律缺口**（「常驻文档超 100 行无节目录」这一**假阳性连续三轮**被提、每轮消耗一遍复核成本——根因是评审报告不受「计数类发现须附实测」约束，而 `规格写作标准` §5.3 只辖规格自身、`施工机制` §五 的四查里无此条）；③ **销一条老账**：`施工机制` §七 的 scope 清单缺「开发守卫」项（074／075 两批的 scope 因此都不贴主作用域），且 071 §九 第 3 条「归档件例外登记留待触碰该件时补」——本批正好触碰该件，一并落定。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**——含「新增／修改规范条目」（`施工机制` §五／§七／§八 与 `测试与验收标准` §1.1／§4）与跨文件，**不满足**轻行「≤3 文件且不涉规则改动」的豁免；不命中「重」行。**送审深度**：按作者裁定，只核改动项与其直接影响面（`施工机制` §五 止损条 2 的对称做法），不重开全文审。
> 自由度分布：[A]×0 [B]×13
> 必读输入：`scripts/check_spec.py`（`:156` `FENCE_BLOCK`／`:190` `extract()` 的围栏正则／`check_pairs()` 的 cache 读／`_git_readonly()`／`check_counts()` 的 `errors='replace'` 先例）；`docs/specs/施工机制.md`（`:91` 成稿审查行／`:152` scope 行／`:244` 交底必带六项／§七 归档件纪律句）；`docs/standards/测试与验收标准.md`（`:18` 段表前言的两半句／`:22` `[0]` 行／`:78` G16 行）；`docs/specs/archive/075-*.md` §八（核销对象）；`archive/071-*.md` §九 第 3 条；`docs/standards/规格写作标准.md` §1／§5
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（现场核过）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `extract()` 与 `FENCE_BLOCK` 的围栏语言口径不一 | `FENCE_BLOCK = re.compile(r'^\s*```[a-zA-Z]*\s*$')`（`:156`）认大小写；`extract()` 的 `re.findall(r'```[a-z]*\n(.*?)```', …)`（`:190`）**只认小写** → 规格写 ` ```Bash ` 时其内命令**不被提取**（`grep -c "a-zA-Z"` 现为 **1**，改后应为 **2**） |
| ② | `check_pairs()` 的目标件读取无容错 | 该函数的 `with open(path, encoding='utf-8')` 为**裸读**，而 `check_counts()` 已加 `errors='replace'`（075 F12）→ 引用二进制／非 UTF-8 目标件时**同型 `UnicodeDecodeError` 仍在**（`errors='replace'` 现计数 **5**，改后 **≥6**） |
| ③ | `_git_readonly()` 对 `--` 之后的实参仍按选项判（**误收窄**） | 预验实跑：`readonly('git grep -F -c -- "-delete" f')` → **False**（`-delete` 被当选项查、不在允许集）；`readonly('git grep -F -c -- pattern f')` → True；`readonly('git grep -O p f')` → False（安全面未损）。`--` 之后按通用语义皆路径／模式，**不应**再按选项形态判 |
| ④ | 评审报告的计数类发现无约束 | `规格写作标准` §5.3「计数必须实跑」的射程是**规格自身**；`施工机制` §五 成稿审查「审什么」列（`:91`）四查为「锚点真实性／改动清单完备可执行／验收断言可判定／内部矛盾」，**无**「计数发现须附实测」；§八 的交底必带**六项**（`:244`）亦无此条。**实证代价**：「目录缺失」这一假阳性在 070 成稿审查、075 成稿审查、二轮评审**各出现一次**，三轮的行数估计分别虚高（二轮点名四件实为 **82／68／66／89** 行，而全仓 12 件 >100 行常驻文档**每件都有目录**） |
| ⑤ | scope 清单缺「开发守卫」项 | `施工机制` §七（`:152`）的 scope 枚举＝`skill`（skill 资产）／域名（单域建设）／`governance`（治理文档）／`standards`（标准件）／`specs`（规格）——**无** `scripts` 类；074／075 两批的主作用域都是开发守卫脚本（`check_spec.py`／`check.sh`），只能用副改面的 `standards`／`governance` 顶替 |
| ⑥ | 071 §九 第 3 条的登记义务未履行 | 该条＝「**归档件机械修复的纪律例外**（070 批先例，经作者追认）……**登记留待下批触碰 `施工机制` 时补一笔**」；本批触碰 §七 的归档件纪律句（该句现文只写「原则只读（仅允许作废头注）」）→ 登记义务**触发已到** |

**不变量（施工前后须同）**：全语料静态输出**逐字节不变**（语料＝`docs/specs/archive/*.md` ＋ **九件标准件** ＋ `AGENTS.md` ＋ `README.md`（`docs/standards/` 共 11 件、本批改 **2 件**——`测试与验收标准.md` **已排除**、`规格写作标准.md` **未排除**；后者系 F11 三处为**段内同行替换**、实测未使该件静态输出的行号漂移，故语料保留该件仍逐字节不变——**产物审查 A-3 指出**：此与本批 F11① 所引 §一.5 第 10 条「须排除本批改动的每一件」字面不符，属既有实况的记录性偏差、无实际后果）——**语料必须排除本批改动的三个 `.md`：`施工机制.md`／`测试与验收标准.md`／`CHANGELOG.md`**，基准 **324 行／md5 `3b12bcaccd90903201bef67bd3d01933`**，即 `规格写作标准` §一.5 第 10 条 那条律的首次适用）；检查 A／B／C／D 判据与三态、`--verify` 逻辑、退出契约零改动；`check.sh` 零改动（本批不动它）；`docs/product/`、`skills/`、`docs/specs/archive/`（除本批规格自身归档）零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py` `:190`（`extract()` 的围栏正则） | 围栏语言由 `[a-z]*` 改 `[a-zA-Z]*`，与 `FENCE_BLOCK`（`:156`）**口径统一**（只改字符类，不动 `re.S` 与其余分组） | ① | [B] |
| F2 | 同件**三处目标件读取**：`check_swallow()`（`:580`，**逐行读、按行计 token**）／`check_pairs()`（`:789`，整读）／`check_counts()`（`:703`，已容错） | **抽 `_read_target(path)` 辅助函数**（`open(path, encoding='utf-8', errors='replace')` ＋ 返回**文本**），**三处共用**：`check_swallow()` 的行计数改在 `_read_target()` 返回的文本上，**以换行符为唯一分隔切行**（须与现文严格等价：现为 text 模式逐行迭代、只在换行符处切；**不得**用 splitlines——它会额外在垂直制表符／换页符／NEL／行分隔符处切行，窄域复核实测四例各差 1 行、跨垂直制表符的 token 命中由 True 变 False）；`check_pairs()` 由崩溃转容错；`check_counts()` 的既有容错行为不变。函数 docstring 写明「目标件读取口径的唯一落点」。**`verify_report()` 里读规格的那处（`:1004`）不在本项面内**（读的是规格自身、非目标件），记 §八 | ②（**完工实证**：`check_swallow()` `:580` 是本次夹具的崩点）＋ 075 §九（产物审查 A-01）的收口方向（原文建议「两处同加 errors=replace 或把头注『统一』收窄」；抽 helper 系本批在其方向上的收口） | [B] |
| F3 | 同件 `_git_readonly()`（`--` 的处置）＋ 其 docstring | **① 先把 `-e`／`--regexp` 移出 `git grep` 允许集；② `--` 之后不再判选项**（**安全关键**：次序不可颠倒**——须先移出取值型选项**——窄域复审实证 git 把 `-e --` 里的 `--` 当 `-e` 的**值**吃掉、其后实参**仍按选项解析**，故 `git grep -F -e -- -O <pager> f` 会被放行、**重开 075 P0-3 的 pager 面**；移出后允许集内再无消耗实参的选项，`--` 必为真分隔符）：循环中遇 `--` 即**跳出**（其余实参一律视作路径／模式放行）；docstring 由现文「`--` 为参数分隔符，恒允许」改为「`--` 之后一律放行（其后的实参按通用语义是路径／模式，不是选项）」——**不得**放宽 `--` 之前任何判据 | ③ | [B] |
| F4 | `docs/specs/施工机制.md` §五 成稿审查行（`:91`，「审什么」列）＋ §八 交底必带（`:244`）＋ **同节 `:144`（已知缺口表「协1」的「本文件只给必带六项，见 §八」）** | **评审报告纪律**：① 成稿审查「审什么」列末补一句——「**计数／行数／条数类发现须附实测命令与输出**（`wc -l`／`grep -c` 等）；未附者**标注「估计」且不计入 findings**（只能作观察项）」；② §八 的「施工交底必带六项」标题改为「施工交底**必带七项**」，并**新增第 7 项、格式与 1–6 项**逐字同构**（行首 `7.` ＋空格，后接说明句——**该节 1–6 项无加粗，故第 7 项亦不加粗**）——内容为「报告计数类发现时附实测命令与输出（未测者标「估计」）」（**不加粗**）；③ **同件 `:144`（已知缺口表「协1」）的「本文件只给必带六项，见 §八」须同步改「七项」**（窄域复核 N-7 实测：`grep -n "六项" docs/specs/施工机制.md` 命中 2 处 = `:144` ＋ `:244`）；④ §五 该列标题「规格本身**四查**」改述为「规格本身**四查 ＋ 报告纪律**」（窄域复核 P2 明示要求：新增项与「四查」措辞同步） | ④（三轮假阳性的实证） | [B] |
| F5 | 同件 §七 scope 行（`:152`） | scope 枚举**补一项**：「**开发守卫＝`scripts`**（`check.sh`／`check_spec.py` 等；其改动为主作用域的批次取 `scripts`）」——并在同句末补「（074／075 两批按历史用 `standards`／`governance`，不复改）」 | ⑤ | [B] |
| F6 | 同件 §七 归档件纪律句（`:154`） | **改写「唯一例外」半句**（**不得**只在句末追加——窄域复核 N-5：`:154` 现文为「……（**唯一例外是移入时的作废头注**，归档后不再修改）」，追加而不改即自相抵）：改为「……（**两处例外**：① 移入时的作废头注；② **归档位移造成的机械断链可只改路径**——被修件为归档 070 件的那条相对链接，经作者追认；对照先例见 071 §九 第 3 条所引 `064` §七 第 5 条）」 | ⑥（销 071 §九 第 3 条） | [B] |
| F7 | `docs/standards/测试与验收标准.md` `:18` 与 `:22` 两处半句 ＋ `:78` G16 行 | ① `:18` 的「本标准不复制段序列表」改述为「**不复述段名与顺序**」（与紧随其后的「下表列全部段（含段号）」不再字面相抵）；② `:22` `[0]` 行的「管什么」补「**存在但非目录形态亦置红**」；③ `:78` G16 行状态改为「**本批处置**」（**已实查**：六类只在内联 `unauditable()` docstring 的 `:222`–`:227`，模块头注只点名 `$` 与反引号一类——故 G16 的触发条件「补枚举随工具头注下次修订」**已到**，由 **F12** 补枚举后本批处置；**F7③ 与 F12 的取值必须一致**，两者任一改另一必同步） | 075 §八 13／14 ＋ 072 批先例 | [B] |
| F9 | `docs/specs/施工机制.md`:148 | **同题复述同步**：该句现为「commit／push／tag／分支等**一切 git 写操作**每次单独取得作者确认（AGENTS.md 红线 1）」——074 批已把红线 1 改为形态判据（「凡改变仓库状态的 git 命令」），此处复述**覆盖面微收且措辞过时**；075 §九 产物审查 F-06 记其「触发＝下次触碰该件」，**本批正触碰该件** → 按红线 1 新口径改述（保留其末的指针「（AGENTS.md 红线 1）」） | 075 §七「074 §九（产物审查 F-06）」行 ＋ 本批触碰该件 | [B] |
| F10 | `docs/specs/施工机制.md` §六 反模式清单（`:136` 的 11 行之后追加第 12 行）＋ §三「修复后自核验四条」（`:76`） | **① 反模式第 12 条**（要求：在 `施工机制` §六 反模式清单**表格内**追加**一行**，**首列字面为 编号列内容为 12（表体四列，与 1–11 行同构）；下列内容按四列落位——**本规格不复述管道符以免破表**：「错误做法」＝以为改了、实际没改——修订脚本失败未检查，且未回读核验文件实际状态；「正确方式」＝修订类脚本三段式（**全量校验 → 统一写盘 → 回读核验**），改后逐条 `grep`／read 核验目标文本确在文件中；「事故出处」＝2026-09-14 076 窄域复核：两次 `.tmp` 脚本末尾报错中止在写盘之前，12 处改动零落地，审查方逐字读文件发现）——把「修订脚本中止在写盘前、未回读核验、以为改了实际没改」登记为事故；**② §三 四条第①条**「修改已落实到位」补判据：「**逐条 `grep`／read 核验目标文本确在文件中**，不得凭脚本输出判『已改』」 | 元发现（076 窄域复核：两次 `.tmp` 脚本末尾报错中止、12 处改动零落地） | [B] |
| F11 | `docs/standards/规格写作标准.md` §4 附加项（`:54`）／§5 第 3 条（`:60`）／§5 第 5 条（`:62`） | **① §4 矛盾检查**下补：「**禁改面与改动面须逐件对账**——禁改面列举的每一件若同时出现在改动清单，必须在禁改面明示豁免」（P0-1 的出身）；**② §5.3** 补：「行数断言的计数口径为**换行符出现次数**，与 `wc -l` 一致；**禁**依赖 `splitlines()`」（F2 的出身）；**③ §5.5** 补：「修订既有条文时，判据是**新文本是否使旧文本失效**——失效即**改写**而非追加」（F6 的出身） | 三处实撞各一例 | [B] |
| F12 | `scripts/check_spec.py` 模块头注（`:10`／`:15` 一带）＋ `docs/standards/测试与验收标准.md`:78（G16 行） | **补枚举不可审计六类**：头注在现文「`$` 与反引号不论引号内外一律判不可审计」之外，补列六类——① shell 解释器／包执行器族；② 解释器任意代码入口（`-c`）；③ 裸解释器；④ 展开符 `$`／反引号；⑤ 引号外元字符；⑥ `sed` 非 `-n`（判据实现在内联 `unauditable()` docstring `:222`–`:227`，本项只把它们**枚举进头注**）；G16 行（该表行的首列字面为 编号列内容为 G16）状态列随之改为「**本批处置（075 F1⑤／076 F12 补枚举）**」（**现文实测**：`测试与验收标准.md`:78 为「维持现状＋理由（065-8 的自身约定：两批内无人补即恢复该行——本批恢复；补枚举随工具头注下次修订）」——窄域复核 N-3：初稿把现值误记为「触发已到，未处置」） | G16 触发条件原文（「补枚举随工具头注下次修订」）＋ 本批触碰该工具 | [B] |
| F13 | `docs/standards/测试与验收标准.md` §4 缺口表（G16 行后追加**一行**，**首列字面为 编号列内容为 G17、三列与全表同构） | **新增候选缺口 G17**：「规格内『某文档 §N』引用的节号未自动化核对（现只人判轨）」／触发＝「引用闭合」类检查器扩展时——**登记为候选扩展位、本批不实现**（`AGENTS.md` §四：引入此类机制须先登记事故出身；本轮 076 §七 来源列失真是其事故出身） | 076 窄域复核的锚点失真 | [B] |
| F8 | `CHANGELOG.md` | 本批一行（≤400 字符），落 `[Unreleased]` › `### Changed` 首条，以「收口小批与评审报告纪律（076）：…」起首；**末尾须带「核验：…」句**，且**不得引入新的计数声称**（073-3 口径：行内不得出现「数字 ＋ 行／处／字符」跟在实存路径的代码跨度之后） | 过程产物两项 ＋ 073／075 既有口径 | [B] |

**口径要点（易错）**：① F1 是**字符类**替换（`[a-z]*` → `[a-zA-Z]*`），不得顺手改围栏匹配的其它部分；② F2 抽函数后**两处调用点行为须等价**（`check_counts()` 的读法不变、`check_pairs()` 由崩溃转容错）；③ F3 **只放行 `--` 之后**，`--` 之前的判据逐字不动（安全面）；④ F4 的落文**不得复述**「目录缺失」那三个案例的细节（只写规则）；⑤ F7③ 的 G16 状态**按实况写**（先 `grep` 头注确认枚举是否存在，再决定「已履行」或「触发已到」）。

## 三、验收断言（含预验记录）

### 断言 1｜静态输出逐字节不变（语料已按 `规格写作标准` §一.5 第 10 条 排除本批改动件）

```bash
python scripts/check_spec.py --static docs/specs/archive/*.md docs/standards/命名标准.md docs/standards/文档写作标准.md docs/standards/产物契约标准.md docs/standards/触发与交互标准.md docs/standards/版本与分发标准.md docs/standards/规格写作标准.md docs/standards/术语标准.md docs/standards/运行环境标准.md docs/standards/安全与合规标准.md docs/standards/skill形态标准.md AGENTS.md README.md > .tmp/076/after.txt 2>&1
diff .tmp/076/before.txt .tmp/076/after.txt && echo "[逐字节相同]"
python -c "import hashlib;print(hashlib.md5(open('.tmp/076/after.txt','rb').read()).hexdigest())"
wc -l < .tmp/076/after.txt
```

期望：`diff` **无输出**；md5 ＝ **`3b12bcaccd90903201bef67bd3d01933`**；行数 ＝ **324**。**预验记录**：基准已冻结（324 行／md5 同上／「B：判定 9 条整件尺寸声称（另有 40 条非整件口径声称未判）」／末行「有预警 246 条」）。

### 断言 2｜F1 的口径统一与行为（大小写围栏现在能提取）

```bash
grep -c "a-zA-Z" scripts/check_spec.py
python -c "import importlib.util,io;spec=importlib.util.spec_from_file_location('cs','scripts/check_spec.py');cs=importlib.util.module_from_spec(spec);spec.loader.exec_module(cs);print(len(cs.extract(io.open('.tmp/076/fence.md',encoding='utf-8').read())))"
```

夹具 `.tmp/076/fence.md`（含两段围栏：` ```bash ` 与 ` ```Bash `，各含一条 `grep -c` 命令）——期望：第一项 **2**（预验 **1**）；第二项 **2**（预验 **1**，即大写围栏内的命令未被提取）。

### 断言 3｜F2 的容错与两处口径统一（含修前实证）

```bash
grep -c "errors='replace'" scripts/check_spec.py
grep -c "_read_target" scripts/check_spec.py
python scripts/check_spec.py --static .tmp/076/binary_ref.md
```

夹具 `binary_ref.md`（在改动清单行声明目标件 `.tmp/076/bin.dat`，并在断言里引用它）＋ `bin.dat`（含非 UTF-8 字节）。期望：第一项 **5**（**窄域复核 P1**：F2 是把 `:703` 的容错读**移入** helper，属移动非新增，该字面数**不变**——初稿「≥6」不可达）；第二项 **≥4**（定义 ＋ 三处调用）；第三项**退出 0 且不出现 `UnicodeDecodeError`**。**修前实证（已跑）**：同夹具在修前于 `check_swallow()`（`:581` 的 `token in line` 生成式）抛 `UnicodeDecodeError`——即 F12 只补 `check_counts()` 时留的**另两处**同型崩溃面，本项一并收口。

### 断言 4｜F3 的 `--` 语义（预验对比）

```bash
python -c "import importlib.util;spec=importlib.util.spec_from_file_location('cs','scripts/check_spec.py');cs=importlib.util.module_from_spec(spec);spec.loader.exec_module(cs);print([cs.readonly(c) for c in ['git grep -F -c -- \"-delete\" f','git grep -F -c -- pattern f','git grep -O p f','git grep -F -c -O x f']])"
```

期望：`[True, True, False, False]`（**预验**：`[False, True, False, False]`）——即 `--` 之后的 `-delete` 放行，而 `--` **之前**的 `-O` 仍判非只读 ✓。

### 断言 5｜F4／F5／F6 三处条文的 token 落位

```bash
grep -c "计数／行数／条数类发现须附实测命令与输出" docs/specs/施工机制.md
grep -c "开发守卫＝\`scripts\`" docs/specs/施工机制.md
grep -c "归档位移造成的机械断链可只改路径" docs/specs/施工机制.md
grep -c "^7\. 报告计数类发现时附实测命令与输出" docs/specs/施工机制.md
grep -c "凡改变仓库状态的 git 命令" docs/specs/施工机制.md
grep -c "^| 12 |" docs/specs/施工机制.md
grep -c "逐条 \`grep\`／read 核验目标文本确在文件中" docs/specs/施工机制.md
grep -c "禁改面与改动面须逐件对账" docs/standards/规格写作标准.md
grep -c "禁.*依赖 \`splitlines()\`" docs/standards/规格写作标准.md
grep -c "失效即\*\*改写\*\*而非追加" docs/standards/规格写作标准.md
grep -c "^| G17 |" docs/standards/测试与验收标准.md
grep -c "本批处置（075 F1⑤／076 F12 补枚举）" docs/standards/测试与验收标准.md
grep -c "裸解释器" scripts/check_spec.py
grep -c "必带七项" docs/specs/施工机制.md
```

期望各 **≥1**；**预验基线均 0**（另核 §八 的「六项」已改「七项」，且该节序号连续）。

### 断言 6｜F7 的三处落位

```bash
grep -c "不复述段名与顺序" docs/standards/测试与验收标准.md
grep -c "存在但非目录形态亦置红" docs/standards/测试与验收标准.md
grep -c "^| G16 |" docs/standards/测试与验收标准.md
```

期望前两项各 **≥1**；第三项 **1**、该行状态列须含「**本批处置**」、**不含**旧文「维持现状＋理由」；含**实况判据（两态）**——`sed -n '1,45p' scripts/check_spec.py | grep -c "包执行器"`：**预验为 0**、**施工后须 ≥1**（F12 把头注补上六类枚举后，「包执行器」即现于头注）——**窄域复核 N-2**：初稿写「应为 0」是把预验值当施工后期望，将与 F12 互斥而必假红；另注全件 `grep -c` 恒为 **2**（内联 docstring），故须限定行段。

### 断言 7｜禁改面、hunk 归属与全仓守卫

```bash
git status --porcelain . ':!scripts/check_spec.py' ':!docs/specs/施工机制.md' ':!docs/standards/测试与验收标准.md' ':!docs/standards/规格写作标准.md' ':!CHANGELOG.md' ':!docs/specs/076-2026-09-12-收口小批与评审报告纪律.md' | wc -l
git diff -U0 scripts/check_spec.py | grep -c "^@@"
bash scripts/check.sh
npx markdownlint-cli2 "**/*.md"
```

另核：全仓相对链接**断链 0** 且条数**不低于 175**（预验 175／0；命令同归档 072 规格 §四 断言 5 的一行式）；`check.sh` 的 `[4]` 段对本规格自身的发现须**逐条判属**（条数不设期望值）并记入核验结论。期望：第一项 **0**（补集式）；第二项只作记录，判定走**走查判据**（每个 hunk 须落在 F1／F2／F3 点名面内、无表外行）；`CHANGELOG` 命中 **≥1**、行长 **≤400**；`check.sh` 退出 0；lint **0 issue**（件数不设期望值）。**链接判据自带一行式命令**（见上第 4 条；不再引「同归档 072 §四 断言 5」——窄域复审指出该引用不可复现），期望 **`175 0`**。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 六条逐条实跑——`:156`／`:190` 现文与 `a-zA-Z` 计数 **1**、`errors='replace'` 计数 **5**；`readonly()` 三例实跑（`[False, True, False]`，其中第一例＝**误收窄实证**）；`施工机制` 三处锚点（`:91`／`:152`／`:244`）与 §七 归档件句、`测试与验收标准` 三处（`:18`／`:22`／`:78`）逐行读；「目录缺失」三轮假阳性的数字复核（点名四件 82／68／66／89 行、12 件 >100 行者全有目录）。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1 的基准已冻结（324 行／md5 `3b12bcac…`，**语料已排除本批三个改动 `.md`**，即 `规格写作标准` §一.5 第 10 条的首次适用）；断言 2／3／4 的**修前基线**已实跑（`a-zA-Z` 1／`errors='replace'` 5／`readonly()` 四例 `[False, True, False, False]`；夹具 `fence.md`／`binary_ref.md`／`bin.dat` 已就位）；断言 5／6 的八处 token 预验均 **0**。**逐条核断言 token 与目标文本互斥性**：断言 5／6 的 token 须落在 F4–F13 的目标文本内——**F7③ 与 F12 对 G16 的取值已同步为「本批处置」、断言 6 同步**（窄域复核 N-2：三者曾取值互斥）；断言 4 是**返回值的期望列表**（非文本互斥面）✓。**无未预验项**（断言 7 的 hunk 归属属施工后实核项）。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/` 与 `docs/standards/` **除 §二 点名者**外全部（含本批规格自身与 `archive/`）、`docs/product/`、`skills/`、`scripts/check.sh`、`scripts/hooks/`、`AGENTS.md`、`README.md`。**禁改面与改动面须逐件对账**：本批改动恰 **5 件**——`scripts/check_spec.py`／`docs/specs/施工机制.md`／`docs/standards/规格写作标准.md`／`docs/standards/测试与验收标准.md`／`CHANGELOG.md`（**窄域复核 P0-1／N-1 两次实撞：初稿只豁免 `施工机制.md`、次稿又漏 F11 新点的 `规格写作标准.md`——本句即 F11① 所立之律的自我适用**）。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具。
- 禁止越权：不改版本号与 tag；不新增文件（本批只改**五件**既有文件：`scripts/check_spec.py`／`docs/specs/施工机制.md`／`docs/standards/规格写作标准.md`／`docs/standards/测试与验收标准.md`／`CHANGELOG.md`）；不建台账／不建索引件。**（窄域复核 P2：初稿只写「四件」且未列件名，与 §二 的 F10–F13 扩面不自洽。）**
- 禁止改断言、改数字、删检查来变绿；**F3 只许放行 `--` 之后**（`--` 之前判据逐字不动）；**F2 抽函数后 `check_counts()` 的读法不得变**；**F1 不得动围栏匹配的其它部分**；**F4 的落文不得复述案例细节**。

## 六、被否决的替代方案

- **重写 074／075 两个提交以修正 scope** → 否决：需 `rebase` 既有历史、且红线 1 下每次 git 写都要单独确认，收益仅是 message 里两个词；正确收口＝F5 把 scope 口径补进 §七（对后续批次生效），两批按历史接受。
- **`extract()` 改 `re.I`（忽略大小写）** → 否决：字符类显式写 `[a-zA-Z]*` 与 `FENCE_BLOCK` 逐字一致，比加 flag 更可核（且 `re.I` 会连带影响 `.*?` 的其它匹配语义面）。
- **把 F2 写成「各自就地加 `errors='replace'`」** → 否决：三处读取口径正是漂移的来源（075 只改了一处即留两处），抽 `_read_target()` 是 075 §八 9 点名建议的收口方式。
- **评审纪律只写进 §五、不写 §八** → 否决：§五 管**审查位审什么**、§八 管**送审交底带什么**；两处是不同环节，只写一处会在另一环节漏（本轮假阳性正是「审查位报了未实测的数」）。
- **把「目录缺失」那条的判定写进 `施工机制`** → 否决：个案判定不入规则（规则只写「须附实测」）；个案留痕在 075／076 规格的审查记录里即可。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批（075）规格的全部开放项**（必列面 ①）＋**本批改动直接触及或可能变状态者**（必列面 ②）。判据＝**同节交集**；语料口径按 `规格写作标准` §一.5 第 10 条。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 075 §八 1 | 动态阶段的宿主无关语义（`shell=True` 两宿主行为不同） | 仍待 | 需先给「跨宿主可复现」的取舍；本批不动 `report()` 的执行方式 |
| 075 §八 2 | 执行面收窄的覆盖代价（`find`／`sed`／集外 git 选项／`$`·反引号 token） | **本批处置（部分）** | F3 把「`--` 之后的模式」从代价里移出（**误收窄**已实证）；其余代价（`find`／`sed` 不执行、集外选项）保留为设计取舍 |
| 075 §八 3 | `check_spec.py` 的崩溃面（目标件被删后抛 `FileNotFoundError`） | 仍待 | 非真实场景；触发＝出现真实自删／竞态实例 |
| 075 §八 4 | P3 四项（`_dir_split` 计数语义、`~~~` 围栏、`extract()` 语言大小写、`MD_LINK` 带 title） | **本批处置（1/4）** | **`extract()` 语言大小写＝F1**；`~~~`／`MD_LINK`／`_dir_split` 三项仍待（本仓规范使前二者不出现，后者只作布尔用） |
| 075 §八 5 | `术语标准` 补正例 | 仍待 | 本批不动该件；触发＝下次触碰 |
| 075 §八 6 | F7 的残留形式冲突（`check.sh` 头注「其他文档不复述列表」） | 仍待 | 本批不动 `check.sh`；触发＝下次触碰其头注 |
| 075 §九（施工期 P0-b 裁定） | 「逐字节不变」不变量与「改 `check.sh` 行数」的耦合 | 仍待 | 本批不动 `check.sh`；触发＝确需改其行数时按归因处置 |
| 074 §八 第 4 条 | `AGENTS.md` §三 的 lint 行指针（裁定倾向：不加）——075 §七 已顺延此条、不重列理由见范围注 | 仍待 | 触发＝出现「照 §三 命令跑、不明其环境前提」实例 |
| 075 §九（产物审查 A-01） | A-01：F12 的容错面只覆盖 `check_counts()` | **本批处置（扩面）** | **F2**（抽 `_read_target()` 并让**三处**共用）——执行期实证崩点在 `check_swallow()`（`check_pairs()` 同型），比原条目所述面**更大**，故一并收口 |
| 075 §九（产物审查 A-03） | A-03：`_git_readonly()` 对 `--` 之后仍过白名单 | **本批处置** | **F3**（并已实证其为**误收窄**而非保守取舍） |
| 075 §九（产物审查 A-04） | A-04：`MUTATING` 对 `find`／`sed` 已成前向防御 | 仍待 | 注释已自陈；本批不动 |
| 075 §九（产物审查 B-01） | B-01：`运行环境标准` 未同步 F6② 的主版本门 | 仍待 | 本批不动该件；触发＝下次触碰 |
| 075 §九（产物审查 B-02） | B-02：G16 触发已到未处置 | **本批处置** | **F7③ ＋ F12**（先核实况再落状态；**若**核实为「未处置」且需补头注枚举，属超出本批 F 面者，按 P1 申报） |
| 075 §九（产物审查 B-03＋B-05） | B-03／B-05：`测试与验收标准` §1.1 两处小改 | **本批处置** | **F7① ②** |
| 074 §九（产物审查 F-06） | `施工机制.md`:148 的同题复述覆盖面较新总括微收 | **本批处置** | **F9**（本批触碰该件，按红线 1 新口径改述） |
| 071 §九 第 3 条 | 归档件机械修复的纪律例外，登记留待触碰 `施工机制` 时补 | **本批处置** | **F6**（本批触碰该件 §七 的归档件纪律句，登记义务触发起效） |

**汇总**：按条计 **16** 条：已履行 0｜作废 0｜**本批处置 8**（含「（部分）」「（1/4）」「（扩面）」三格，字面精确等于「本批处置」者 5 格）｜仍待 8｜待作者 0 ＝ 16 ✓（逐格可复核；含末行「074 §九（产物审查 F-06）」与本批新增 F9）。**范围注**：074／073／072 及更早各批的开放项落点在 `AGENTS.md`／`docs/standards/` 其余件／`check_spec.py` 的另判据面，与本批五件**无同节交集**（`测试与验收标准` 本批只动 §1.1 两半句、§4 的 G16 行与新增 G17 行；改动件共 **5 件**，见 §五 的逐件列名），以其各自核销表为索引（不重列）。

## 八、开放项

1. **`shell=True` 的宿主语义取舍**（075 §八 1 顺延）：要么显式 `executable=bash`，要么在头注登记「Windows 下由 `cmd.exe` 解析」。触发＝出现跨宿主行为差异的实例，或作者要求守卫行为跨宿主可复现。
2. **`[0]` 段的「手动路径」与「收尾汇总」**（`测试与验收标准` §1.1 自陈缺口）：会改 `check.sh` 行数 → 与「逐字节不变」的耦合（075 §八 7）需要一并裁定。触发＝作者要求段输出规范收口。
3. **`~~~` 围栏与 `MD_LINK` 带 title／`_dir_split` 计数**（075 §八 4 的三项）：本仓规范使前二者不出现。触发＝规范放开相应写法。
4. **`verify_report()` 读规格那处（`:1004`）未纳入 `_read_target()`**：其角色是「读规格自身」而非「读目标件」，故本批不并入；若将来出现非 UTF-8 规格件（不应有）再议。触发＝出现该类实例。
5. **F4 的规则若仍被绕过**（审查位不实测）：触发＝再审出现同类假阳性时，把「须附实测」升级为送审交底的**门**（无实测即退回补测）。

## 九、审查记录

- 成稿审查分身（fresh、只读；按作者裁定的**窄域深度**：只核改动项与其直接影响面）：待回填。
- 施工后核验与双维产物审查：待回填（**本批改规则 → `施工机制` §五 双维**）。

**产物审查（各「低」项，按 `施工机制` §六 不阻塞提交，记此后处置）**

1. **A-1｜`check_spec.py` 头注 F3① 的括注措辞过实**：「故 `git grep -F -e -- -O <pager> f` **原会被放行**」不成立——修前代码实测判 `False`（`-O` 在集外），**只有「只做 ② 不做 ①」的中间态才会放行**；同句「允许集内已无取值型选项」亦应精确为「无**消耗独立实参**的选项」（`git diff`／`log`／`show`／`status` 仍有 `-U`／`--pretty=`／`-u`／`--untracked-files=`，但皆紧贴赋值、不吞后续 `--`，实测证实）。结论对、措辞错。触发＝下次触碰该头注。
2. **A-2｜`规格写作标准` §5.5 的实例称「现文」而该文本已随本批消失**：该实例引「现文作『唯一例外是移入时的作废头注』」，而 F6 落地后该件现文已改「两处例外」→ 应作「**修前那句**」。触发＝下次触碰该节（与 A-1 同批为宜）。
3. **B-1｜G17 行的准入依据未落半句**：`测试与验收标准` §4 的 G17 行缺「**候选扩展位，本批不实现**（`AGENTS.md` §四 事故出身见 076 §七 来源列失真）」——规格 F13 有此要求而落文未含。触发＝下次触碰该表。
4. **B-2｜报告纪律只落在成稿审查行**：`施工机制` §五 `:91` 的补句只进「成稿审查（规格侧）」行，**「产物审查（产物侧）」行未含**→ 产物审查位的计数类发现仍无「须附实测」约束（而三轮假阳性中有一轮出自该位）。触发＝下次触碰 §五 时在该行补同句，或明示该纪律对**三处审查位通用**。
5. **观察项（不计、仅记）**：① `测试与验收标准`:18 的「不复述段名与顺序」与紧随的「下表列全部段」仍有轻微字面相抵（表体逐行列出段名与顺序）→ 更彻底的改法是「不以本标准为段序真源」；② 头注六类枚举的 ②③ 只写 `python`，实现在 `unauditable()` 里含 `python3`／`py`；③ `_read_target()` 的「行数＝换行符出现次数（与 `wc -l` 一致）」在**独 CR 件**上与 `wc -l` 会差 1（本仓无此形态，新旧行为一致）。

- 2026-09-14｜核验三步（规划方执行）｜**通过**。① 改动面核对：恰五件（`CHANGELOG.md` `1 0`／`docs/specs/施工机制.md` `9 7`／`docs/standards/测试与验收标准.md` `4 3`／`docs/standards/规格写作标准.md` `3 3`／`scripts/check_spec.py` `42 15`）＋本批规格未跟踪；`git diff --name-only -- scripts/check.sh` ＝ **0 行**（零改动）；补集式禁改判据 **0**；`check_spec.py` **11 hunk** 逐条走查全落 F1／F2／F3／F12 前半点名面内、无表外行。② 断言独立重跑（规划方自己的会话）：断言 1 `diff` **无输出**／md5 **`3b12bcaccd90903201bef67bd3d01933`**／**324 行**；断言 2 `a-zA-Z`＝**2**、`extract(fence.md)`＝**2**；断言 3 `errors='replace'`＝**5**（移动非新增）、`_read_target`＝**7**、`binary_ref.md` `--static` 退出 **0**（修前于 `check_swallow()` 抛 `UnicodeDecodeError`）；断言 4 `[True, True, False, False]`（`--` 之后放行 ✓ 而 `-O` 仍拦 ✓）；断言 5／6 全部 token 在位（含**头注段**「包执行器」＝**1** ＝ F12 已落）；断言 7 补集 **0**／`check.sh` 退出 **0**／lint **46 件 0 issue**。③ 内容抽查：由下述双维产物审查独立覆盖。
- 2026-09-14｜施工期两条偏差的裁定｜**P1 照准**：施工方发现**断言 1 的语料含本批改动件 `docs/standards/规格写作标准.md`**，而该件的基线静态发现**逐行带行号**、F11 三处若新增行即漂移 → 其处置＝F11 三处改**段内同行替换**（不加行、不加链接），两次比对**逐字节相同** ✓；**该 P1 的成因在我**（语料排除清单少排一件——即产物审查 A-3），处置方式正确。**P2 照准**：F6 改写时保留并改述原纪律句为「除上述两处外，归档后不再修改」，正合 F11③「失效即改写而非追加」的判据，判为得当。
- 2026-09-14｜**双维产物审查**（fresh、只读）｜findings 共 **5**：P0:0 P1:0 **P2:0（全「低」）**｜结论 **通过**。A 维：`_read_target()` 三处**行为等价**（实测含垂直制表符／换页符／独 CR 三例；`check_pairs()` 由崩溃转容错；`check_counts()` 读参逐字相同）／`extract()` 只改字符类／`GIT_READONLY_OPTS['grep']` **已移出 `-e`／`--regexp`** 且余项皆无值／`--` 跳出**只放行其后**（逐例对比新旧，差异仅本批意图项）／头注六类与 `unauditable()` 判据**逐类对应**／`施工机制` 四处落位（含反模式第 12 行四列同构、表列数未破）／`规格写作标准` 三处带实例／`测试与验收标准` 三处属实（`[0]` 行补语与 `check.sh:23` 分支相符、G16 状态与头注实况相符）／`CHANGELOG` **359 字符**、无新增计数声称、带「核验：」句。B 维：「必带六项」非归档残留 **0**／「唯一例外是移入时的作废头注」残留 **0**／「规格本身四查」无第二载体／「一切 git 写操作」第二载体为纯指针（074 已裁定）／G17 准入半句缺（＝B-1）。四闸：`check.sh` 退出 0／lint 46 件 0 issue／链接 **175 0**／`--static` 对本规格 **16 条发现逐条判属全属预期**。**P1 独立复核成立**（实跑语料命令 → `diff` 空／md5 同／324 行、行号零漂移）。处置：**A-3（规格不变量句失实：标准件改 1 件实为 2 件、且语料含被改件）→ 就地订正**；**A-1（头注括注措辞过实）／A-2（`规格写作标准` §5.5 实例误称「现文」）／B-1（G17 准入半句缺）／B-2（报告纪律未落到产物审查行）→ 记 §八 1–4**（触发＝下次触碰对应件）；观察项三项 → §八 5。
