# 077-2026-09-14-对账机制化

> 定位：把 072–076 四轮审查里**反复咬人的一族规格缺陷**从「人判轨」搬进工具——**改动面 ↔ 禁改面 ↔ 断言排除集** 三方对账（076 的 P0 连撞两次：§五 禁改面覆盖不到新点名的件、断言排除集漏项），顺带做 `~~~` 围栏双认（075 §八 4 的遗留项之一）。本批**只落工具**，不改任何规则条文（相关写作律已在 076 立毕）。
> **范围裁定（2026-09-14，作者）**：原设计的第二族——**断言 token ↔ 改动清单「改动」单元格的模糊核（检查 F）**——**本批不落**。成稿审查的实测把它的判据否掉了：按现文落地报 **126／222 行（57%）**，其中**约 98% 是假报**（切不出 ≥8 字符片段的短 token 必然落进「否则报」）；而改成「无片段即跳过」又会丢掉那 3 条真阳性——**片段包含式判据两头不靠**。故本批只做检查 E 与 `~~~` 围栏，**族二连同其实测数据记入 §八 开放项**（触发＝出现更可靠的判据形态，如「token 的**完整规范化形态**出现在单元格内」）。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**——改工具判定行为（新增一类检查）并同步头注与维护入口；不命中「重」行。按 076 新立的 scope 口径，本批**主作用域＝开发守卫 → `scripts`**。
> 自由度分布：[A]×0 [B]×4
> 必读输入：`scripts/check_spec.py`（`:1`–`:58` 头注六行「四类」表述（含 `:8`／`:58` 同行两处）与 `:33` 维护入口段；`:120` `SECTION`；`:124` `CHANGE_SECTION`；`:133` `PATH_TOKEN`；`:163` `FENCE_BLOCK`；`:197` `extract()` 内联围栏正则；`:366` `_abs()`；`:371` `_section()`；`:406` `_change_rows()`；`:489` `_row_target_file()`；`:913` `static_report()`；`:946`–`:947` 逐件打印串；`:968` `findings` 收列表）；`docs/specs/archive/072-*.md`／`075-*.md` §八・§九／`076-*.md` §八・§九（核销表体例与开放项真源）；`docs/standards/规格写作标准.md` §5（断言写法，含第 9／10 条）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（逐条实测）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 改动件集可从规格机械取得 | `_change_rows()`（`:406`）解析「文件级改动清单」表 → 行 dict 键 `path`／`change`／`freedom`／`raw` ✓；路径列内 token 经 `PATH_TOKEN`（`:133`）＋ `_abs()`（`:366`）＋ `os.path.isfile` 判实存 ✓。**实测语料**：189 个改动行中 **113 行**有可解析路径；其中 **3 行**的路径列含 **≥2 个**实存 token（066／074／076 各一行）→ 现成助手 `_row_target_file()`（`:489`）**只返回首个** token，本判**须逐个 token 遍历**（不得复用它作取集口） |
| ② | 禁改面句的既有写法统一 | 075／076 等件 §五 均以「**- 禁止触碰**：…」起首一行列举 ✓；076 形态含「除 §二 点名者外」类豁免子句 ✓；072 形态含「只许改 F3／F4 点名两件」类**显式豁免** ✓ |
| ③ | 排除集可从规格机械取得，但**多数件没有** | `':!<path>'` 形态出现在 074／075／076 的验收断言节 ✓；**实测语料：有验收节的件 19 份，其中有补集式断言者仅 3 份**（其余 16 份 `C ⊄ ∅` 恒真）→ 该判**必须「E 为空即跳过」**，否则每份规格各出数行纯噪声（成稿审查实测 51 行） |
| ④ | 现四类检查的编排与**两处归并点** | `static_report()`（`:913`）依次调 `check_swallow`／`check_counts`／`check_pairs`／`check_writing`，逐件打印串在 `:946`–`:947`（`for line in a_lines + c_lines + d_lines + count_lines: print(line)`），三态计数收列表在 `:968`（`findings = warn + zero + undecided + sentinel + extra + counts`）✓ → **新检查须同时接入两处**：只挂打印串则「屏上有行、三态不计数」（工具自陈「漏计即假绿」的反面）；只挂 `findings` 则行不出现 |
| ⑤ | `四类` 字样共 **8 行**，其中 **6 行需改、2 行不得改** | 实测 `grep -c "四类"` ＝ **8**（本批目标为 **2**）。需改 6 行：`:1`「检出四类规格缺陷」／`:8`「机械检查四类（…）——四类一律」（**同一行内含两处，行号是 `:8` 而非 `:7`**）／`:43`「检查 A／B／C／D 四类无论报出多少条发现」／`:58`「计入 A／B／C／D 四类全部输出…只在四类全空时打印」（同句两处）／`:915`／`:917`（`static_report()` docstring 同义句）✓。**不得改**：`:711`「四类已知假阳性」（指检查 B 自身的四类误报）与 `:867`「检查 D：规格写作机械检查（四类…）」（指 D 的四个子判）——**不得整体替换** |
| ⑥ | 围栏正则**两处都只认三反引号围栏**（不认波浪号） | `FENCE_BLOCK`（`:163`＝`^\s*```[a-zA-Z]*\s*$`）与 `extract()` 内联 `re.findall(r'```[a-zA-Z]*\n(.*?)```', …)`（`:197`）✓ → 双认须**同改两处**（只改 `FENCE_BLOCK` 则动态阶段的命令提取仍漏 `~~~` 块） |
| ⑦ | 事故出身 | 072–076 四轮审查的 P0 集中在两族：改动面↔禁改面↔排除集（076 连撞两次）与断言 token 不可满足（076 窄域复审推翻自评）✓；**逐条可核出处见 §七** |

**不变量（施工前后须同）**：全语料 `--static` 输出的**差异面只许是新增的对账行 ＋ 三态「有预警」行**——即 `diff` 与 `.tmp/077/before.txt`（基准 **355 行／md5 `7abbec1d80a616b96dc3ef3fe6236376`**，2026-09-14 复验一致）相比，取差异行（以 `diff` 的 `<`／`>` 前缀起首者）后，排除 `· 对账` 起首的行、再排除 `· 有预警` 起首的汇总行，剩余行数**恒 0**；检查 A／B／C／D 四类的判据与输出、退出契约（静态发现恒 0）、`--verify` 逻辑零改动；`scripts/check.sh` 零改动；`docs/standards/`、`docs/specs/`、`AGENTS.md`、`README.md` 零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py`（新增 `def check_reconcile(text, root)` ＋ 接入 `static_report()` 的两处归并点） | 「本项新增判据函数 `def check_reconcile(text, root)`，候选行前缀 `· 对账①`」。**检查 E：三方对账（改动面 ↔ 禁改面 ↔ 断言排除集）**。**取集**：**C**＝全部改动行的 `path` 单元格内**逐个** `PATH_TOKEN` 判 `os.path.isfile`，取实存者为改动件（**不得**用只返回首个 token 的 `_row_target_file()`）。**判一（前缀命中而未豁免）**：禁改面检索面 **S**＝`## …禁止事项…` 节的**全节文本**（新常量 `BAN_SECTION`；**不限 `- 禁止` 起首行**——豁免句可能写在标题不含「禁止」的兄弟条目里，071 批的『归档件例外』即此类，只取 `- 禁止` 行会漏读并产假阳性）；**P**＝S 内**字面以斜杠结尾的独立目录 token**（正则两侧不得紧邻路径字符，**防从文件名反推目录**——073 批的 `scripts/check.sh` 曾被反推成 `scripts/`，那件实际只禁该件与 `hooks/` 两处）且 `os.path.isdir` 为真者；对**命中前缀**（`x` 以某 `p` ＋ 斜杠起首）的每件 `x`，**按子句**（以 `；`／`，`／`、`／`：` 切分 S；**不含圆括号**——括号会把授权语与其落点切开，065 批的『只在 [4] 处增改』即此类）判豁免：**存在一个子句**同时含 ① 豁免词（「除」／「除外」／「例外」／「不在禁改之列」／「不在此列」／「只许改」／「只改」／「只允许」／「只在」／「点名」）与 ② 落点（该件全路径或基名，或 `§`＋数字，或 `F`＋数字，或方括号包住的数字如 `[4]`）→ 已豁免 ✓；否则出候选行 `· 对账① 禁改面未豁免`（P 命中集为空或无 S 时不判）。**判二（排除集缺）**：**E**＝验收断言节内 `':!<path>'` 形态的路径集（归一化＝去首尾空白与尾随斜杠）；**E 为空 → 静默跳过**（实测 19 件有验收节的语料中仅 3 件有此形态）；否则对每件 `x ∈ C`，无 `e ∈ E` 使 `x == e` 或以 `e` ＋ 斜杠起首者 → 出候选行 `· 对账① 断言排除集缺 N 件`（列件名）。**汇总明示行**（`· 对账① 汇总: …`：改动面件数／两判命中数／跳过计数）——**发射面＝只对「含文件级改动清单节且 C ≠ ∅」的件打印**；无该节或 C＝∅ 的件**候选与汇总皆不输出**（完全静默）。**接入规格**：`check_reconcile(text, root)` 返回 **(候选行列表, 汇总行列表)**；候选行并入 `extra`（⇒ 计入 `findings`），汇总行**只进逐件打印串**（不计入）；`static_report()` 的逐件打印串追加这两个列表。 | ①②③④⑤⑦；成稿审查 P1-2／P1-6 与 P2-1／P2-2／P2-4／P2-5 | [B] |
| F2 | `scripts/check_spec.py`（`FENCE_BLOCK` `:163` ＋ `extract()` 内联围栏正则 `:197`） | **`~~~` 围栏双认**：两处正则各扩为**同时匹配三反引号与 `~~~`**，**且开合须按标记字符配对**（`FENCE_BLOCK` 捕获标记并按字符开合；`extract()` 用**反向引用**闭合）——**不得**让开合各写成独立的「三反引号或波浪号」分支：那样「三反引号块内一行孤立波浪号标记行」会提前翻转围栏态，令该件其余 check D 预警**全部失效**、且 `extract()` 静默丢掉该块内的命令断言（**本批新引入的假阴性类**，产物审查 P1-2 以夹具实证——奇数标记变体下修前静态吞并、动态丢命令，补修后两侧俱复原；现语料零触发，属潜伏型）（`extract()` 处按同样字符类改写 findall 模式，使动态阶段亦认 `~~~` 块）；`check_writing()` 的围栏感知随之覆盖两种形态。改写后 `grep -c "a-zA-Z" scripts/check_spec.py` 须仍为 **2**（两处正则各含一次——该计数进基准的静态输出，变则断言 1 第三项红） | ⑥；075 §八 4 的遗留项（该条由 076 F1 处置了 `extract()` 大小写一项） | [B] |
| F3 | `scripts/check_spec.py`（头注）：`:1`／`:7`–`:8`／`:43`／`:58`／`:915`／`:917` 六处「四类」→「五类」并补列检查 E ＋ `:33` 维护入口段 | **头注与新检查同步**：六行中含「四类」字样者按**指代**分两路处置——指**检查种类**者改「五类」（`:1`／`:8` 次处／`:43`／`:58` 两处／`:915`／`:917`），而 `:8` **首处**「检查 D 规格写作机械检查四类（…）」指的是 **D 自身的四个子判**（与不得改的 `:867` 同类），须改「**四项**」而**不得**改「五类」（否则括号内只列四项即成伪述）；改毕在枚举处补「检查 E 三方对账（改动面 ↔ 禁改面 ↔ 断言排除集）」；维护入口段补一行——「新增检查 E 的判据改 `check_reconcile()` 与 `BAN_SECTION`；呈报前缀与三态收列表改 `static_report()`」。**`:711`／`:867` 两处「四类」不属本判面，不得改** | ④⑤；076 立「头注须枚举维护入口」的同向要求 | [B] |
| F4 | `CHANGELOG.md`（`## [Unreleased]` → `### Changed` 段首） | 增一行『对账机制化（077）』登记（改了什么／核验结论），**≤400 字符**、末尾带「核验：」句，形态照 073 起各行的既有体例 | `规格写作标准`（CHANGELOG 体例）＋ 073-3 口径 | [B] |

**口径要点（易错）**：① 两个新产出面（候选行与汇总行）**一律非阻断**：只 `print`／只返回行列表，**不得**置退出码；② **候选行须计入 `findings`**（否则三态少计、屏上行数多于计数行），**汇总行不计入**（其角色同「· B：判定 …」明示行）；③ C 取集**逐 token 遍历**（3 行含 ≥2 token，见 §一①）；④ F1 判二**只认 `':!<path>'` 形态**（本仓补集式命令的既有写法），不猜其它排除写法；⑤ E 为空、无禁改面句两种情形**只进汇总行的跳过计数、不逐件明示**（「静默」的可见化落点是**汇总行**，不是逐件明示行——成稿审查实测逐件明示即 51 行噪声）；**C 为空（含无改动清单节）的件连汇总行亦不出**（完全静默，见 F1 发射面）；⑥ 新检查对**无改动清单节的件**不得报行。

## 三、验收断言（含预验记录）

### 断言 1｜差异面只许是新增的对账行与三态汇总行（最强一道）

```bash
python scripts/check_spec.py --static docs/specs/archive/*.md docs/specs/施工机制.md docs/standards/*.md AGENTS.md README.md > .tmp/077/after.txt 2>&1
diff .tmp/077/before.txt .tmp/077/after.txt | grep "^[<>]" | grep -c "^[<>] · 对账"
diff .tmp/077/before.txt .tmp/077/after.txt | grep "^[<>]" | grep -v "^[<>] · 对账" | grep -vc "^[<>] · 有预警"
python -c "import io;t=io.open('.tmp/077/after.txt',encoding='utf-8').read().split(chr(10));n=int(__import__('re').search(r'有预警 (\\d+) 条',[l for l in t if l.startswith('· 有预警')][0]).group(1));c=sum(1 for l in t if l.startswith('· 对账①') and '汇总' not in l);s=sum(1 for l in t if l.startswith('· 对账① 汇总'));print('候选',c,'汇总',s,'自洽',n==273+c)"
```

期望：第二项 **≥1 且 ≤40**（新增对账行数 ＝ 候选行 ＋ 汇总行；**上界 40 ＝ 实测预测 ＋ 余量**，超限即判因——要么判据过松、要么语料里禁改面句确有缺口，不得直接调高上界）；第三项 **0**；第四项输出 `自洽 True`（＝「候选行计入 `findings`、汇总行不计入」——**两个方向都被锁**：漏计则 n 小于 273＋候选数、多计则 n 大于之）。**判据为前缀锚定**（`^[<>] · 对账`／`^[<>] · 有预警`）——**不得**退回子串过滤：基线的 355 行里含「对账」或「有预警」字样者共 **8 行**，其中 **7 行不是汇总行**（实测：`:104`／`:105`／`:166`／`:176`／`:227` 是 A 类的自噬预警与件名行、`:173`／`:185` 是「跳过…有预警 121 条」的真发现行），子串过滤会把它们一并滤掉 → 对这些行的任何改动**不可检出**（成稿审查已实证：篡改其中一行后本判仍判「一致」）。**预验记录**：基准已冻结（355 行／md5 `7abbec1d80a616b96dc3ef3fe6236376`／2026-09-14 复跑逐字节一致／末行「有预警 273 条」）；**新增行数的下限侧亦已实跑预测**——原型 `.tmp/077/predict.py`（按 F1 现判据独立实现）在冻结语料上得**候选 0 行 ＋ 汇总 16 行 ＝ 16 行**（14 件无改动清单节或 C＝∅ 者完全静默），故上界 40 的余量为 24。

### 断言 2｜F1 两判的正控与两种负控（夹具驱动）

夹具 `.tmp/077/fixture-recon.md`（**正控**）：改动清单声明 `scripts/check_spec.py`；§五 写「禁止触碰：`scripts/` 全部」（**无豁免子句**）；断言块含 `':!CHANGELOG.md'`（**缺该件**）。夹具 `.tmp/077/fixture-recon-ok.md`（**负控一**：豁免成立）：§五 为「…`scripts/` 除 §二 点名者外全部」，排除集含该件。夹具 `.tmp/077/fixture-recon-noexc.md`（**负控二**：该判适用面不成立）：§五 禁止 `docs/standards/`（**不覆盖** C）；断言节**无** `':!` 形态。

```bash
python scripts/check_spec.py --static .tmp/077/fixture-recon.md
python scripts/check_spec.py --static .tmp/077/fixture-recon-ok.md
python scripts/check_spec.py --static .tmp/077/fixture-recon-noexc.md
```

```bash
python scripts/check_spec.py --static .tmp/077/fixture-recon.md | grep -c "^· 有预警 2 条"
```

期望：本项 **1**（＝2 行候选**确实**被计入三态；若候选未并入 `findings`，此处会打印「· 无发现」→ 本项 0 → 红）。**此项为产物审查 P1-1 所补**：冻结语料上候选恒 0 行，故断言 1 的第四项在真实跑法上只锁得住「多计」一侧，漏计一侧须由本件承担（审查方以反事实实证：候选不入 `findings` 时断言 1 四项**全绿**）。

期望：第一件**含两行候选**（「禁改面未豁免」含 `scripts/check_spec.py`；「断言排除集缺」含同一件）＋**一行汇总**；第二件**不含**候选行、**含**汇总行（豁免子句含「除」与落点 `§二`）；第三件**不含**候选行、**含**汇总行，且其跳过计数显示「无补集式断言 1」与「无禁改面句 0」。三件**均须有汇总行**（皆含改动清单节且 C ≠ ∅；**该发射面由 F1 写死**，实现成「逐件打印」即与此不符）。**预验基线**：现工具在五件夹具上皆 **0** 对账行（已实跑）。夹具属 `.tmp/`（红线 2，用完即删）。

### 断言 3｜C 取集须逐 token（多 token 行不得漏判）

夹具 `.tmp/077/fixture-multi.md`：改动清单**一行**的路径格同时写 `scripts/check_spec.py` 与 `scripts/check.sh`（二者均实存）；§五 写「禁止触碰：`scripts/` 全部」。

```bash
python scripts/check_spec.py --static .tmp/077/fixture-multi.md
```

期望：候选行**同时**列出 `scripts/check_spec.py` **与** `scripts/check.sh`（判据命令：输出中该两串各出现 ≥1 次）。**预验基线**：现工具 0 对账行。此判针对 §一① 实测的「3 行含 ≥2 token」——若实现退回 `_row_target_file()`（只取首个），本断言即红。

### 断言 4｜F2 的 `~~~` 围栏（修前实证）

夹具 `.tmp/077/fixture-fence.md`：含 `~~~` 围栏，围栏内一行是**加粗引导行紧跟列表标记**（检查 D 的既有预警形态）。

```bash
python scripts/check_spec.py --static .tmp/077/fixture-fence.md
```

期望：输出**不含**该行触发的「加粗引导行紧跟列表」预警（围栏感知已覆盖 `~~~`）。**预验基线**：现工具**含**该预警（`~~~` 未被 `FENCE_BLOCK` 认作围栏——修前实证）。

### 断言 5｜结构锚与头注同步

```bash
grep -c "def check_reconcile" scripts/check_spec.py
grep -c "对账①" scripts/check_spec.py
grep -c "检查 E 的判据改" scripts/check_spec.py
grep -c "五类" scripts/check_spec.py
grep -c "四类" scripts/check_spec.py
grep -c "a-zA-Z" scripts/check_spec.py
python -c "import ast,io;ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read());print('[语法自检通过]')"
```

期望依次 **1**／**≥2**／**1**／**≥3**／**2**（`:711`／`:867` 两处保留）／**2**（两处围栏正则各一处）／`[语法自检通过]`。**预验基线 0／0／0／0／8／2／通过**。

### 断言 6｜禁改面、hunk 归属与全仓守卫

```bash
git status --porcelain . ':!scripts/check_spec.py' ':!CHANGELOG.md' ':!docs/specs/077-2026-09-14-对账机制化.md' | wc -l
git diff --name-only -- scripts/check.sh | wc -l
bash scripts/check.sh
npx markdownlint-cli2 "**/*.md"
```

期望：第一项 **0**（补集式）；第二项 **0**（`check.sh` 零改动）；第三项退出 **0**；第四项 **0 issue**（件数不设期望值）。另核：全仓相对链接**断链 0** 且条数**不低于 175**（预验 175／0；判据命令见本规格 §三 末的链接一行式）。**预验基线** 0／0／0／0／175‧0。

链接判据（自带一行式，不再外引）：

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md')];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

### 断言 7｜过程产物两项

```bash
grep -cF '对账机制化（077）' CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '对账机制化（077）' in l][0].rstrip()))"
```

期望：第一项 ≥1、第二项 ≤400，且该行**末尾带「核验：」句**、**不含**「数字 ＋ 行／处／字符」跟在实存路径代码跨度之后（073-3 口径）。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 七条逐条实跑——`:1`／`:8`／`:33`／`:43`／`:58`／`:120`／`:124`／`:133`／`:163`／`:197`／`:366`／`:371`／`:406`／`:489`／`:711`／`:867`／`:913`／`:946`–`:947`／`:968` 逐行读原文并核行号（**§一 与必读输入的行号全部取自本批实跑 `grep -n` 输出，非记忆**——上稿五处错位即因凭记忆写号；本稿另自查纠正两处：`四类` 基线 6→**8**、逐件打印串 `:945`→**`:946`**）；189 改动行／113 可解析行／3 多 token 行、19 有验收节件／3 有补集式断言件、基线 8 行含「对账」字样（7 行非汇总）三组数字均由 `.tmp/077/measure.py` 实跑得出；基准 md5 于 2026-09-14 复跑复验。
2. **断言实跑过（与「替换为」侧文本不互斥）**：断言 1 的基准已复验；断言 2／3／4 的**修前基线**已实跑（四件夹具现为 0 对账行；`~~~` 夹具现**报**该预警——即 F2 的修前实证）；断言 5 的六项基线 0／0／0／0／8／通过；断言 6 的五项基线 0／0／0／0／175‧0。**逐条核断言 token 与本规格目标文本的互斥性**（断言 5 的 `def check_reconcile`／`对账①`／`检查 E 的判据改`／`五类` 均落在 F1–F3 的目标文本内 ✓；断言 2／3 的件名落在 C 取集面内 ✓）；**无未预验项**（断言 6 第二项属施工后实核项）。
3. **禁止事项已写明**：见 §五。**本规格自身的静态自检**：**修前实测 2 条**（`四类` 现行 8／目标文本内 2；`a-zA-Z` 现行 2／目标文本内 1）——两条都是**本批改动本身**所致（F3 要把 `四类` 由 8 处降为 2 处；F2 要改写含 `a-zA-Z` 的那两行），**已判因、不改**；修后预期输出 **7 条自噬预警**（`四类`／`五类`／`def check_reconcile`／`对账①`／`检查 E 的判据改`／`对账机制化（077）`／**`a-zA-Z`**（F2 改写该两行，现行仍 2、目标文本内 1）——F1–F4 落地后六者的「现行行数」与「目标文本内次数」同时大于 0，属本批立法的**预期**结果，逐条已判因；窄域复审 P1-4 指出上稿「仅 1 条」为**少计**，已按判据逐 token 重算更正）＋ **判一 0 候选**（本规格 §五 的豁免句写在兄弟条目「禁止越权」内，S＝§五全节故可读到——此即 P1-4 所指的作用域缺口，已由 S 取集放宽消解）＋ **汇总行 1 行**（本规格含改动清单节且 C ≠ ∅）。另有一族曾现的「目标文本未提及且现行为 0」预警（F2／F3 路径列写「同件」致 hit 门不过 ＋ 四处 token 未落引号块），已按检查 A 的机制（`_row_mentions()` 读写路径列、`_row_target_text()` 取引号块优先）**改写法规格而非改判据**——「路径列写『同件』」正是本仓反复咬人的同一错族（070 批起 ≥5 发），本批把它在施工现场就地清掉。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/` 全部（含本批规格自身与 `archive/`）、`docs/standards/` 全部、`docs/product/`、`skills/`、`scripts/check.sh`、`scripts/hooks/`、`AGENTS.md`、`README.md`。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具。
- 禁止越权：不改版本号与 tag；**除 §二 点名的两件（`scripts/check_spec.py`／`CHANGELOG.md`）外不新增不改动任何文件**（四件夹具属 `.tmp/` 探针，不入库）；不建台账／不建索引件。
- 禁止改断言、改数字、删检查来变绿；**两个新产出面一律非阻断**（不得置退出码）；**不得放宽**既有四类检查的任何判据；**不得**把 F1 的豁免判据改成「豁免词出现即放行」（该形态已实证在 076 上恒空转）或退回子串过滤（本批断言 1 已实证其盲区）；**不得**整体替换「四类」（`:711`／`:867` 两处须保留）；**不得**调高断言 1 的 40 行上界。

## 六、被否决的替代方案

- **本批一并落族二（断言 token ↔ 改动格）** → **否决（作者裁定缩范围）**：成稿审查实测**报 126／222 行、精确率约 2.4%**（切不出 ≥8 字符片段的短 token 被「否则报」全收），且规范化规则四种读法报 **125–136** 行不等、而断言 5 又不锁新检查行数 → **实现分歧不被任何断言抓住**；改「无片段即跳过」则丢掉 3 条真阳性。片段包含式判据两头不靠，故退 §八（触发＝「完整规范化形态」判据成形）。
- **F1 判二对 E 为空者仍逐件作「明示跳过」行** → 否决：实测 16／19 件无补集式断言 → 逐件明示即 51 行噪声；改为**不逐件输出、只进汇总行的跳过计数**（空转可见性由汇总行承担）。
- **F1 判一用「豁免词出现即放行」** → 否决：实测 076（**本判的事故出身件**）因句含「除」而被整句豁免 → 该判在立法对象上**恒空转**；072 的「只许改 F3／F4 点名两件」这种合法显式豁免**又不在词表内** → 反被误报。故取**子句级双条件**（豁免词 ＋ 落点），并把「写出『除…外』却无实质点名时漏报」如实登记（宁可漏报方向）。
- **C 取集复用 `_row_target_file()`** → 否决：该助手**只返回首个** token，而实测 3 行的路径列含 ≥2 个实存 token → 复用即漏判（断言 3 即为此设）。
- **新检查纳入 `--verify` 模式而非 `--static`** → 否决：`--verify` 不进 `check.sh` 门禁，而本族的价值正在「每次 `check.sh` 都过一遍」；纳入 `--static` 为主、`--verify` 不动。
- **顺手清 075 §八 其余遗留**（`shell=True` 宿主语义、`[0]` 段收尾汇总、`MD_LINK` 带 title、`_dir_split` 计数语义）→ 本批**不塞**：前二者需跨宿主取舍或改 `check.sh`（触发 075 §九（施工期 P0-b 裁定）的行数耦合，须先重冻那处基线）；后二者本仓规范使其不出现／只作布尔用。逐条记 §八 与 §七。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批（076）规格的全部开放项**（必列面 ①：§八 全 5 条）＋**本批改动直接触及或可能变状态者**（必列面 ②：076 §九 产物审查各项、075 §八・§九 中与 `scripts/check_spec.py`／`check.sh`／标准件有关者）。判据＝**同节交集**；**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 076 §八 1 | `shell=True` 的宿主语义取舍（顺延自 075 §八 1） | 仍待 | 本批不改执行方式（族门／参数白名单零改动）；触发＝出现跨宿主行为差异实例 |
| 076 §八 2 | `[0]` 段的「手动路径」与「收尾汇总」 | 仍待 | 本批不动 `check.sh`（且与 075 §九（施工期 P0-b 裁定）的行数耦合同题） |
| 076 §八 3 | `~~~` 围栏／`MD_LINK` 带 title／`_dir_split` 计数（三项） | **本批处置（1/3）** | **F2** 落 `~~~`；另两项仍待（见本表 075 §八 4 行与 §八 开放项 2） |
| 076 §八 4 | `verify_report()` 读规格处未纳入 `_read_target()` | 仍待 | 本批不改该处；角色是「读规格自身」非「读目标件」。**行号更正**：真源现为 `:1031`（076 写作时为 `:1004`，已随 076 自身加行位移——上稿逐字继承未重核） |
| 076 §八 5 | F4 的规则若仍被绕过（审查位不实测） | 仍待 | 监测型条目；触发＝再审出现同类假阳性时升级为送审门 |
| 076 §九 A-1 | `check_spec.py` 头注 F3① 的括注措辞过实 | 仍待 | 本批只动头注的**「四类」表述与维护入口段**，不动该括注所在的执行面段（`GIT_READONLY_OPTS` 注释）；触发＝下次触碰该段 |
| 076 §九 A-2 | `规格写作标准` §5.5 实例误称「现文」 | 仍待 | 本批不动标准件；触发＝下次触碰该节 |
| 076 §九 B-1 | G17 行缺「候选扩展位／本批不实现」半句与准入指针 | 仍待 | 本批不动 `测试与验收标准`；触发＝下次触碰该表 |
| 076 §九 B-2 | 报告纪律只落「成稿审查」行，产物审查行未含 | 仍待 | 本批不动 `施工机制`；触发＝下次触碰 §五 |
| 076 §九 观察项① | `测试与验收标准`:18 的「不复述段名与顺序」字面相抵 | 仍待 | 本批不动该件 |
| 076 §九 观察项② | 头注六类枚举的 ②③ 只写 `python`（实现含 `python3`／`py`） | 仍待 | 本批只动「四类」表述与维护入口段，不动不可审计类的枚举段；触发＝下次触碰该段 |
| 076 §九 观察项③ | `_read_target()` 在**独 CR 件**上行数与 `wc -l` 差 1 | 仍待 | 本批不动 `_read_target()` |
| 075 §八 2 | 执行面收窄的覆盖代价（`find`／`sed`／集外 git 选项／`$`·反引号 token） | 仍待 | 本批不改执行面（族门与参数白名单零改动） |
| 075 §八 3 | `check_spec.py` 的崩溃面（目标件被删后抛 `FileNotFoundError`） | 仍待 | 本批不改目标件读取路径；非真实场景 |
| 075 §八 4 | P3 四项（`_dir_split` 计数语义／`~~~` 围栏／`extract()` 围栏语言大小写／`MD_LINK` 带 title） | **本批处置（1/4）** | `~~~`＝本批 **F2**、`extract()` 语言大小写＝076 F1 已履行；`_dir_split`／`MD_LINK` 仍待（本仓规范使二者不出现／只作布尔用）——本行与 076 §八 3 行为同源交叉引用 |
| 075 §八 5 | `术语标准` 补正例 | 仍待 | 本批不动该件；触发＝下次触碰该件 |
| 075 §八 6 | F7 的残留形式冲突（`check.sh` 头注「其他文档不复述列表」） | 仍待 | 本批不动 `check.sh`；触发＝下次触碰其头注 |
| 075 §九（施工期 P0-b 裁定） | 「逐字节不变」不变量与「改 `check.sh` 行数」的耦合 | 仍待 | 本批不动 `check.sh`；触发＝确需改其行数时按归因处置 |
| 075 §九（产物审查 A-01） | F12 的容错面只覆盖 `check_counts()` | 已履行（076 F2 扩面） | 备查行：本批对 `_read_target()` 零改动（同行 A-02 已在 075 内修复，不立行） |
| 075 §九（产物审查 A-03） | `_git_readonly()` 对 `--` 之后仍过白名单 | 已履行（076 F3） | 备查行：本批对 `_git_readonly()` 零改动 |
| 075 §九（产物审查 A-04） | `MUTATING` 对 `find`／`sed` 已成前向防御 | 仍待 | 注释已自陈；本批不动 `MUTATING` |
| 075 §九（产物审查 B-01） | `运行环境标准` 未同步 F6② 的主版本门 | 仍待 | 本批不动该件；触发＝下次触碰 |
| 075 §九（产物审查 B-02） | G16 触发已到未处置 | 已履行（076 F12 补枚举） | 备查行：本批不动 `测试与验收标准` |
| 075 §九（产物审查 B-03＋B-05） | `测试与验收标准` §1.1 两处小改 | 已履行（076 F7① ②） | 备查行（同上一行同批） |

**汇总**：共 **24 行／按条 24**——**本批处置 2**（076 §八 3 与 075 §八 4，**同源同一事实**：`~~~` 由本批 F2 落；按行计 2、按事实计 1）｜**已履行 4**（075 §九 A-01／A-03／B-02／B-03＋B-05，均备查行）｜**仍待 18**｜作废 0｜待作者 0 ＝ 24 ✓（逐行可复核；每行恰一条目，「汇总＝行数」恒成立）。**范围注**：074／073／072 及更早各批的开放项落点在 `AGENTS.md`／`docs/specs/施工机制.md`／其余标准件／`check_spec.py` 的**其它判据面**，与本批所动的「新增检查 E ＋ 围栏双认 ＋ 头注六处表述 ＋ 维护入口段」**无同节交集**，以其各自核销表为索引（不重列）。

## 八、开放项

1. **族二（断言 token ↔ 改动清单「改动」单元格）本批未落工具**，其实测数据存档：以「双侧规范化 ＋ ≥8 字符片段，无片段即报」实现 → 语料报 **126／222** 行、其中真阳性 3 条（**精确率约 2.4%**）；「四种规范化读法」下报行数在 **125–136** 间摆动（**实现分歧不被断言约束**）；改「无片段即跳过」则 3 条真阳性**全失**。触发＝出现「把 token 的**完整规范化形态**（而非片段）与改动格比对」的可实现判据时（该形态须先解决正则／转义 token 的等价类问题），或族二复发一次且现有手段未报。
2. **`MD_LINK` 带 title 的链接**（075 §八 4 剩余）与 **`_dir_split()` 计数语义**：本仓规范使前者不出现、后者只作布尔用。触发＝规范放开相应写法时。
3. **F1 的判据仍是粗判（宁可漏报方向）**：判一只做「前缀命中 ＋ 子句级豁免双条件」，若某规格写出「除…外」却**无实质点名**（无件名、无 `§`／`F` 落点）——正常写法下该双条件不成立、会报出 ✓；但若写成「除 §二 点名者外」而 §二 实际未点名该件，本判会**漏报**。判二只认 `':!<path>'` 形态（其它补集写法不判）。触发＝上述两种漏报各自出现一次实例时，按实测收紧（如解析禁改面句的作用域、或扩认补集形态）。
4. **F1 汇总行的口径失真与观测缺口**（产物审查 P2-2／P2-3，**实测**）：汇总行报的「改动面 N 件」只数「路径列内可解析为实存件的 token」，与真实改动面可差一个量级（059：改动行 **33** 行而 N＝3）；且「判一无适用面」「有改动行但 C＝∅」与「对账通过」在输出上**同形**（下游读者易读成「全语料已对账」；实测 17 件中判一真有适用面者 8 件／21 例）。触发＝出现「以为已对账」而漏检的实例，或作者要求汇总口径收口时，按「改动行 M 行／可解析 N 件／判一适用 K 件」改口径，并对「有改动行而 C＝∅」补空转明示（现为完全静默，属本批写死的发射面，改动需一并改规格）。
5. **判一的产出方向登记**（产物审查 P2-4）：**落点形态**（件名／`§N`／`FN`／`[N]`）不全的**泛称豁免**（如「本批改动件除外」）会被**报出**——方向是**宁可多报**，规格写法须**逐件点名**；这不是判据缺陷（与上一条 3 的漏报方向并存，两向均已登记）。触发＝后续批次收到「看似假阳性」的候选行时按此登记判属。
6. **075 §八 1（宿主语义）／6（段表形式冲突）**与 **075 §九（施工期 P0-b 裁定：行数耦合）／075 §九 A-04（`MUTATING` 前向防御）**：均本批不动，触发见 §七 各行。
7. **`check_spec.py` 自带测试**：本批的检查 E 只由夹具（`.tmp/`）验证、**用完即删**——工具无仓内测试件（`scripts/` 下不建 `test_*`，红线 2）。触发＝出现「工具判据回归无人发现」的实例时，评估把夹具固化进 `--verify` 的只读自检。

## 九、审查记录

- 2026-09-14｜**成稿审查**（fresh 只读分身）｜findings 共 **15**：**P0:2** P1:7 P2:6｜结论 **修订后通过**。处置（本条落本批规格，**长文不入 CHANGELOG**）：
  - **P0-1（核销表来源列失真）**：原 §七 五行的来源标「076 §八 1–5」，而 **076 §八 实体只有 5 条且内容不同**（其行文实为 076 §九 的产物审查 A-1／A-2／B-1／B-2 与观察项），且「075 §八 7／10／11／12」是**悬空编号**（075 §八 只有 6 条）→ **已按实测真源重列 §七 全表**（076 §八 5 条逐条 ＋ 076 §九 各项 ＋ 075 §八・§九 各项），每行来源逐一核到条号。
  - **P0-2（必列面缺行）**：由 P0-1 连带——076 §八 2／4／5 与 075 §八 2／3／5 未列 → **已补齐**（见 §七）。
  - **P1-1（汇总算术破「一行一条」）**：原表把多项并入一格致汇总与行数不等 → **已拆为 23 行、一行一条**，汇总＝行数。
  - **P1-2（F1③ 未定义「E 为空」）**：实测 16／19 件无补集式断言 → 每件各出数行纯噪声（审查方实测 51 行）→ **已定「E 为空即静默跳过」**，跳过计数只进汇总行。
  - **P1-3／P1-4（族二判据不可实现／规范化不完备）**：报 126／222 行、精确率约 2.4%；四种读法 125–136 行而断言 5 不锁新检查行数 → **已按作者裁定整体移出本批**（§八 开放项 1 存档实测数据）。
  - **P1-5（核销行称「本批处置」而无落点）**：A-1 的落点（`GIT_READONLY_OPTS` 注释 `:84`）不在任何 F 项位置列内 → **已改「仍待」**并写明本批只动头注的另两段（§七 第 6 行）。
  - **P1-6（断言 1 判据有盲区）**：子串过滤 `对账\|有预警` 会滤掉基线中含该字样的 **7 行非汇总行**（审查方实证篡改其一仍判「一致」）→ **已改前缀锚定**（`^[<>] · 对账`／`^[<>] · 有预警`）并把该 7 行的行号写进断言（§三 断言 1）。
  - **P1-7（六处锚点行号错位）**：审查方以 `git show` 逐版本核出那组数字**不可能来自任何单一版本**（凭记忆写号）→ **已全部重取**（§一／必读输入的行号逐条出自本批 `grep -n` 实跑输出），并在 §四 自审第 1 条写明该纪律。
  - **P2-1（豁免词过松）** → 已改子句级双条件（§六 否决项 3）；**P2-2（`_row_target_file()` 只取首 token）** → 已写明逐 token 遍历并加断言 3；**P2-3（`extract()` 内联围栏正则）** → F2 已含两处同改；**P2-4（`static_report()` 两处归并点）** → §一④ 与 F1 已点名两处；**P2-5（新检查产出无上界）** → 断言 1 已加上界（先 30、后按窄域复审实测改 40）；**P2-6（族二事故出身的载体）** → 随族二一并移出，§七／§八 只引可核条号（076 §九 A-1 等）。
  - **审查方的独立判断**（F1 值当／族二按现判据不值当／建议「E 为空即跳过」）已由作者裁定采纳——即本批的范围缩定（见头注「范围裁定」）。
- 2026-09-14｜**窄域复审**（fresh 只读分身；按 `施工机制` §五 止损条 2——P0＝2 触发，只核原 P0 修复项 ＋ 缩范围后的直接影响面）｜findings 共 **9**：**P0:1** P1:4 P2:4｜结论 **不通过**（存在未处置 P0）。处置：
  - **P0-1（悬空编号未清）**：上稿只重列了 §七 表体，**§六 否决项／§七 理由列／§八 4 三处仍写着「075 §八 7」与「／11」**（075 §八 实体只 6 条，无 7–14）→ 三处已改为「**075 §九（施工期 P0-b 裁定）**」与「075 §九 A-04」，`／11` 删去；同时把 §四 自审 1 的「全部行号取自实跑」**限定为「§一 与必读输入的行号」**（原声称被 §七 理由列自身证伪）。
  - **P1-1（汇总行发射面未写死 ＋ 上界余量≈0）**：审查方按 F1 现文独立仿写判据、实测新增行数随三种读法为 **28／30／45**，而上界恰 30 且 §五 禁止调高 → 实现方无处可退 → **F1 已写死发射面**（「含文件级改动清单节且 C ≠ ∅」的件；无该节或 C＝∅ 者候选与汇总皆不输出），上界改 **40**，并在断言 1 的预验记录里落**下限侧实测**（原型 `.tmp/077/predict.py`：候选 0 ＋ 汇总 16 ＝ **16 行**，余量 24）。
  - **P1-2（「候选计入 findings／汇总不计入」无断言锁定，漏计与多计双绿）**：两处归并点（逐件打印串 `:946`–`:947`、`findings` `:968`）本互不相干，候选行只进打印串即与基准逐字节同、全绿 → **F1 已写死接入规格**（`check_reconcile()` 返回两列表；候选并入 `extra`、汇总只进打印串），**断言 1 新增第四项「三态自洽」**（`n == 273 + 候选行数`——漏计与多计各自可判）。
  - **P1-3（判一在本仓语料上产可判定的假阳性）**：审查方实测 12 行候选里 ≥3 行语义上已授权——066／067 写「**只允许** F4 的单点修改」（词表无「只允许」）、065 写「（**只在** [4] 处与头注／依赖行增改）」（圆括号被当切分符 ＋ `[4]` 不是合法落点）→ 已三项并改：**豁免词表补「只允许／只改／只在／例外」**、**落点补方括号数字**、**切分符去掉圆括号**；另修正**前缀反推**（073 的 `scripts/` 系从文件名 `scripts/check.sh` 反推——该件实际只禁该件与 `hooks/`）与 **S 取集**（071 的豁免句写在标题不含「禁止」的兄弟条目『归档件例外』里，取行式会漏读）。修后同一原型实测：**候选 0 行**（旧判据 12 行）。
  - **P1-4（§四 自审 3 的预警预期少计）**：上稿称「修后仅 1 条预警」，实测 F1–F4 落地后六处 token 的「现行行数」与「目标文本内次数」同时 >0 → 应为 **6 条自噬预警** → 已更正；该条同时暴露「判一只看首个 `- 禁止触碰` 行」的作用域缺口（本仓最新两批的豁免都写在兄弟条目里）→ 由 S 取集放宽消解（本规格自身判一 0 候选）。
  - **P2 四条**：`:1004` → **`:1031`**（076 自身加行后位移，上稿逐字继承未重核）已更正并注明；补 **075 §九 A-01 备查行**（同行 A-02 已在 075 内修复、不立行）；075 §八 4 状态改「**本批处置（1/4）**」与 076 §八 3 对齐并在汇总注明**同源不重复计数**；F2 补写「改写后 `grep -c "a-zA-Z"` 须仍为 2」并进断言 5。
  - **审查方独立判断**（摘要）：族二退 §八「完全正确」；`~~~` 围栏双认「值当」（唯一暗礁＝ `a-zA-Z` 计数）；检查 E「判据骨架可实施，但产出面定义欠一行」——该判断直接催生 P1-1／P1-2 的修法（本批上界与三态自洽两项判据即由此而来）。
- 2026-09-14｜**施工期偏差裁定（规划方；施工方申报 1 P1 ＋ 3 P2，逐条带实测）**｜**全部照准**，规格就地修订：
  - **P1（照准；缺陷在我）**：断言 1 第四项的一行式用 `split()[1]` 取数，实得「有预警」三字而非数字——**该命令在任何实现下都抛异常**。施工方**拒绝「改输出去迎合断言」**（正确：把「· 有预警」压成「·有预警」会让第二项的前缀锚定失效），如实呈报并另跑同义命令证明该项**意图**成立 → 规格改为 `re.search(r'有预警 (\d+) 条', …)`，原样命令即可通过。
  - **P2-1（照准）**：F3 原写「六处『四类』→『五类』」，但 `:8` **首处**指的是**检查 D 自身的四个子判**（与不得改的 `:867` 同类）→ 规格改写为「按指代分两路：`:8` 首处改『四项』、次处改『五类』」，以免产伪述。
  - **P2-2（照准）**：§四 自审 3 的修后预警预期**少计 1**（漏了修前已登记的 `a-zA-Z`）→ 改「7 条」并补列。
  - **P2-3（照准）**：口径要点 ⑤ 的「C 为空…只进汇总行的跳过计数」与 F1 发射面（C＝∅ 者**连汇总行亦不出**）互斥 → ⑤ 改述。
- 2026-09-14｜**双维产物审查**（fresh、只读；本批改工具判定行为 → 按 `施工机制` §五 双维）｜findings 共 **7**：**P0:0 P1:2 P2:5**｜结论 **修订后通过**。审查方**独立复跑**断言 1–7（含以 `git show HEAD:` 取改前版重测基准 → 355 行／md5 与 `before.txt` `cmp` 逐字节相同）、以 `ast` 抽出改前改后全部顶层函数源码段比对判「既有 A／B／C／D 四类**零放宽**」（函数集合差异只有 `added: ['check_reconcile']`）、另写一份 C 取集实现独立复算（17 件逐件件数一致，语料锚点 113／3 命中）。处置：
  - **P1-1（断言强度的洞；照准并补）**：审查方以**反事实实验**证明——「候选行不入 `findings`」时断言 1 四项**全绿**（因冻结语料候选恒 0，第四项退化为 `273 == 273`），即规格自称的「两个方向都被锁」在**漏计**一侧不成立 → **断言 2 补第五项**（`grep -c "^· 有预警 2 条"` 期望 1；漏计则打印「· 无发现」→ 0 → 红），已实测为 1 ✓。
  - **P1-2（照准并补修代码）**：F2 原文只规定「同时认两种围栏」**未规定开合配对**，施工按字面写成两个独立分支 `(?:```|~~~)` → 「三反引号块内一行孤立波浪号标记行」会提前翻转围栏态，令该件其余 check D 预警**全部失效**、`extract()` 静默丢命令——**本批新引入的假阴性类**（现语料零触发，属潜伏型）→ **F2 补写「开合须按标记字符配对」**（`FENCE_BLOCK` 捕获标记、`check_writing()` 按字符开合、`extract()` 反向引用闭合）并**派原施工分身补修**；补修后实证（奇数标记变体）：修前**静态吞并（0 条）＋动态丢命令（0 条）**、补修后**两侧俱复原（1／1）**，既有 `~~~` 夹具不回退，且**47 件语料的 `extract()` 与 `check_writing()` 输出与 HEAD 逐件相同（差异 0）**＝零回归。
  - **P2-1（照准）**：`:1062` 注释枚举陈旧（`检查 B／C／D 输出` 缺 E）→ 补修时一并改为 `检查 B／C／D／E 输出`。
  - **P2-2／P2-3（记 §八 4）**：汇总行「改动面 N 件」只数「可解析为实存件的 token」，与真实改动面可差一个量级（审查方实测 059：改动行 33 行而 N＝3），且「判一无适用面／有改动行但 C＝∅」与「对账通过」输出同形 → 按 P2 不阻塞，连同实测数据登记为开放项。
  - **P2-4（记 §八 5）**：判一对**落点形态不全的泛称豁免**（如「本批改动件除外」）会**报出**——方向是宁可多报，不是判据缺陷，已登记。
  - **P2-5（提交前处置）**：`.tmp/077/` 遗留探针（17 件）→ 归档前删净（红线 2）；基准哈希已写入 §三，件本身不再保留。
  - **审查方独立判断**：本检查**值得留门禁**——全语料 17 件产出 **16 行汇总、0 行候选**（每次 `check.sh` 约 +4% 输出、**零假阳性**），且**不是空转**：判一在 **8／17 件上有真实适用面（21 例）**全部正确放行、对本批事故出身（076 首稿形态）能**准确报出**，属「触发器型」判据；最该改的一处即上述 P1-1（已照改）。
- 2026-09-14｜**规划方核验（三步 ＋ 修复面自核验）**｜**通过**。① 改动面核对：恰**两件**（`CHANGELOG.md` `1 0`／`scripts/check_spec.py` `122 17`）＋本批规格未跟踪；`git diff -U0` 的 hunk 区间**全部落在**头注（`:1`／`:8`／`:36`–`:38`／`:45`／`:60`–`:61`）、常量区（`:128`／`:184`）、`extract()`（`:226`）、新函数与 `static_report()` 接线，**既有 A／B／C／D 四函数体内零 hunk**；`check.sh` 零改动、禁改面补集式 0。② 断言独立重跑（修复面）：断言 1 四项 `16／0／自洽 True`；断言 2 五件 `2＋1／0＋1／0＋1／两件各 1 行／不报该预警`；断言 3 两件各 ≥1；断言 4 `0` 且奇数混栏变体 `1`；断言 5 `1／3／1／8／2／2／通过`；断言 6 `0／0／exit 0／0 issue／175‧0`；断言 7 `1／275`。③ 内容抽查：`check_reconcile()` 两列表返回、静默发射面、C 逐 token、前缀边界、子句级双条件、E 为空即跳过**逐条与规格一致**；`check_writing()` 围栏态已改为标记字符配对（`None`＝栏外）。④ 回归：47 件语料 `extract()`／`check_writing()` 与 HEAD 差异 **0**。
- 施工后核验与双维产物审查：待回填（**本批改工具判定行为 → 按 `施工机制` §五 双维**）。
