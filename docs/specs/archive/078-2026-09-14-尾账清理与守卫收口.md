# 078-2026-09-14-尾账清理与守卫收口

> 定位：清 075／076／077 三期留存的**一行级尾账**——共八项：① `shell=True` 宿主语义**取舍落文**；② `check.sh` `[0]` 段的「手动路径」与「收尾汇总」；③ `MD_LINK` 认带 title 的链接；④ `_dir_split()` 计数语义收口；⑤ `规格写作标准` 的「现文」误称；⑥ `测试与验收标准` G17 准入半句；⑦ `施工机制` 报告纪律未落产物审查行；⑧ 检查 E 汇总行**口径失真与观测缺口**（077 §八 4）。八项皆**一行至数行**级，无一新机制、无一新文件。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**——跨 **6 件**（`scripts/check_spec.py`／`scripts/check.sh`／两份标准件／`施工机制`／`CHANGELOG`）、改工具判定行为（⑧）与规则条文（⑤⑥⑦）；不命中「重」行（不动 `skills/`、不动域内流程／产物契约）。scope 按 076 口径：**开发守卫＝`scripts`**、**标准件＝`standards`**、**治理文档＝`governance`**。
> 自由度分布：[A]×0 [B]×9
> 必读输入：`scripts/check_spec.py`（`:194` `MD_LINK`；`:361` 与 `:1106` 两处 `shell=True`；`:482` `_dir_split()` 与 `:502` 其唯一调用点 `_target_blocks()`；`:936`–`:1000` `check_reconcile()` 与其中的汇总 f-string；头注「段输出／执行面」段）；`scripts/check.sh`（头注段序、`:15` `[0]` 段、`:63`–`:66` 收尾）；`docs/standards/规格写作标准.md`:62；`docs/standards/测试与验收标准.md`:22／:31／:79；`docs/specs/施工机制.md`:89–:95（审查两表与两条纪律）与 §七 归档件纪律；`docs/specs/archive/060-2026-09-12-基座审计整改-审计报告附件.md`:44（陈旧声称，**只读**）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（逐条实测）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 检查 E 汇总行**口径失真** | 现文只报「改动面 N 件｜判一命中 X｜判二命中 Y｜跳过：无禁改面句 A｜无补集式断言 B」；N＝**路径列内可解析为实存件的 token 数**，非该件改动面。实测语料（17 件出汇总行）：**059 改动行 33 而 N＝3**；语料改动行合计 **193**，可解析件合计远小 |
| ② | 判一无适用面与「对账通过」**输出同形** | 实测「前缀命中件数」：063＝6／065＝2／066＝1／067＝1／071＝1／072＝2／074＝2／075＝3／076＝3（**9 件共 21 例**，全部正确放行）；另 8 件为 0 例——两者现都只显示「判一命中 0」，读者无法区分「对账通过」与「对账没跑到」 |
| ③ | 「有改动行但 C＝∅」的件**完全静默** | 实测全语料仅 **1 件**：`archive/062-…产品层内容第一批.md`（**8 改动行**）；`docs/specs/施工机制.md`（1 行）由 `NEVER_SCANNED` 排除。故补空转明示的代价＝**1 行** |
| ④ | `MD_LINK` 在带 title 的链接上**整条不匹配** | `:194` ＝ `r'\[[^\]\n]*\]\(([^)\s]+?)\)'`——`[x](p "t")` 形态因 `)` 前有空白而**不匹配** → 该链接既不计数、也不查断链（`check_writing()` ④ 用它）。**本仓语料带 title 链接 0 条**（现形式下无差异，属潜伏假阴性） |
| ⑤ | `_dir_split()` 的计数语义是**死语义** | `:482` 返回 `(标记数, 末标记结束位置)`；唯一调用点 `_target_blocks()`（`:502`）只把**标记数当布尔**用（`n` 的真假），无任何处用其数值 |
| ⑥ | `shell=True` 两处，本机形态可用 | `report()`（`:361`，动态阶段）与 `_git_changes()`（`:1106`，核验模式）；Python 语义下 Windows 宿主经 `cmd.exe` 解析、POSIX 宿主经 `/bin/sh`——**本机（Windows）动态阶段历批实证可用**（输出形如 `40 CHANGELOG.md`）；075 的「`$()`／反引号差异」在**执行面收窄后已不可达**（`$`／反引号／引号外元字符一律判不可审计） |
| ⑦ | `check.sh` 的两处缺口 ＋ 段序真源 | 66 行（`wc -l` 口径＝换行符出现次数；**先前写的「67 行」是 `split` 计数法的产物**，违 `规格写作标准` §5.3 的计数口径）；`[0]` 段跳过**只打原因、无手动路径**；末尾只有「== 检查全绿 ==」／「检查未全绿，禁止提交」＋ `exit`（**无收尾汇总**）；`[2]`／`[3]`／`[5]`（待建）**连行都不打**；**段序与段名真源＝其头注**（`测试与验收标准` §1.1 明文不复述）→ 本批**不得**新增段或改段名 |
| ⑧ | 060 的陈旧声称与归档件纪律 | 工具现报 `· 计数不符: scripts/check.sh 称「39 行」实测 66`（`archive/060-…附件.md`:44，该件属归档只读）→ 本批改 `check.sh` 行数**只使该行的「实测」值变化**（由 66 变为新值），**不得**去改 060（`施工机制` §七：归档件原则只读、两处例外均不适用） |
| ⑨ | 四处文档落点逐行核过 | `规格写作标准`:62「076 F6 的归档件纪律句**现文作**『唯一例外是移入时的作废头注』」——该文本已被 F6 改为「两处例外」，**「现文」是误称**；`测试与验收标准`:79 G17 只写触发条件、缺「候选扩展位／本批不实现」半句与 `AGENTS.md` §四 准入指针；`:22`／`:31` 的「缺手动路径与收尾汇总」现状句须随 ⑦ 同步；`施工机制`:91 成稿审查行有「计数类发现须附实测」，**`:92` 产物审查行无** |

**不变量（施工前后须同）**：全语料 `--static` 输出的**差异面只许是下列七类**——① 检查 E 的 **34 行差异行**＝17 行汇总行被**改写**（`diff` 对每条被改行输出 `<`／`>` 两侧各一行，故 17×2＝34；**不得**按 17 预期——那是把「改写」误当「纯新增」）；② `060` 陈旧声称的**两个载体**共 **4 行**差异——检查 B 的「计数不符」一对（`实测 66` → 改后实测值）＋「基线漂移」一对（同一声称的第二报法，施工期实证）；③ `062` 的**1 行**新增空转明示；④ `MD_LINK`／`_dir_split` 改动带来的差异**须为 0**（语料无带 title 链接、`_dir_split` 行为等价）；⑤ 末行三态 `· 有预警 281 条` → **282 条**（C＝∅ 空转行**计入** `findings`，见 F1）；⑥ `· 自噬预警: 对账① @ scripts/check_spec.py` **一对**（F1 向工具内新增字面 `对账①`，令归档 077 规格该断言的「现行」计数 3→4——「归档件的工具实测值随工具演化而变」的同族）；⑦ 其余差异 **0** → 即「以 `· 对账①`／`· 计数不符: scripts/check.sh`／`· 对账① 空转` 起首者外，`diff` 差异行数 **0**」。另：检查 A／B／C／D 四类判据与输出、退出契约（静态发现恒 0）、`--verify` 逻辑零改动；`docs/standards/` 与 `docs/specs/` 内**除 F6／F7 点名的两件与 `施工机制`**外零改动；`AGENTS.md`、`README.md`、`docs/product/`、`skills/` 零改动。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py`（`check_reconcile()` 的汇总 f-string ＋ 新增「空转明示」行） | **汇总行口径收口 ＋ C＝∅ 空转可见化**。汇总行改为四项显式计数：`· 对账① 汇总: 改动行 M 行｜可解析 N 件｜判一适用 K 件｜判二缺 P 件｜跳过：无禁改面句 A｜无补集式断言 B`（`M`＝该件改动清单表体行数；`N`＝C 的件数；`K`＝**命中前缀的件数**（无论是否豁免）；`P`＝**排除集缺项件数**＝旧字段 `判二命中` 之值 `len(missing)`，**只改名不改值**——**不得**读作「已覆盖」）；另对「**有改动清单节且有改动行、但 C＝∅**」的件产**一行空转明示**（`· 对账① 空转: 有改动行 M 行而无可解析实存件——对账未判`），该行**计入** `findings`（同候选行），仍**不**对无该节者输出任何行 | ①②③⑧；077 §八 4 | [B] |
| F2 | 同件（`:194` `MD_LINK`） | **认带 title 的链接**：正则改为「目标非空且不含空白／`)`，其后可跟一个由空白分隔的**引号包裹**的『title』段（**只认** `"…"`／`'…'` 两种 CommonMark title——**不得**扩认全角括号等形态，那会吞掉非法写法）」——使 `[x](p "t")`／`[x](p 't')` 形态**照常计数并查断链**；不得放宽到「`)` 前可有空白」（那会把 `[x](a b)` 这类**非法**形态也吞进来）。**本仓语料 0 条**（现形式下零差异） | ④；075 §八 4 的遗留项 | [B] |
| F3 | 同件（`:482` `_dir_split()` 与其唯一调用点 `:502`） | **计数语义收口**：返回由 `(标记数, 末标记结束位置)` 改为 `(是否含标记, 末标记结束位置)`，docstring 同步（写明「调用方只作布尔用，计数语义已在 078 批删除」）＋调用点同改——**行为等价**（旧 `n` 的真假 ≡ `n ≥ 1`） | ⑤；075 §八 4 的遗留项 | [B] |
| F4 | 同件（头注：执行面段） | **`shell=True` 宿主语义落文**：在头注「执行面白名单」段的相邻处补一句——本工具两处子进程经 `shell=True` 调用，宿主语义为「POSIX→`/bin/sh`、Windows→`cmd.exe`」（本机为后者，动态阶段历批实证可用）；并写明**差异面已由执行面收窄关闭**（`$`／反引号／引号外元字符一律判不可审计）。**不改调用方式**（见 §六 否决项） | ⑥；075 §八 1／076 §八 1 的取舍落文 | [B] |
| F5 | `scripts/check.sh`（`:15` `[0]` 段跳过分支 ＋ `:63`–`:66` 收尾） | **`[0]` 段补手动路径 ＋ 末尾补收尾汇总**：`[0]` 跳过时增打一行「手动路径：确认 `skills/` 为真实目录（`ls -ld skills`）且未被安装器符号链接化」；末尾在「== 检查全绿 ==」／置红之前增**收尾汇总**一行——「共 N 段：已跑 X｜跳过 Y（列跳过项）｜手动路径见各段」。**两行一律顶格（行首无缩进）**——既有 `[0]` 段输出带两空格缩进，而断言 6 的判据按行首锚定（`^手动路径：`／`^共 [0-9]* 段`），缩进即假红；跳过项的括注**须按实况拼**（如 `[2]`／`[3]`／`[5]` 待建、`[0]` 跳过），**不得**写死机器状态（`skills/` 一旦建起即变假陈述）。**不动段序与段名**（真源＝头注，禁漂移）；退出码与置红逻辑零改动 | ⑦；`测试与验收标准`:31 段输出规范 | [B] |
| F6 | `docs/standards/规格写作标准.md`（`:62` 一处） | 「现文」误称勘正：「076 F6 的归档件纪律句**现文作**『唯一例外是移入时的作废头注』」→「**修前那句**『唯一例外是移入时的作废头注』（该件现文已改为『两处例外』）」——**只改这一处称谓**，不追加新条文（本条与 §一.5 第 5 条「失效即改写而非追加」同向） | ⑨；076 §九 A-2 | [B] |
| F7 | `docs/standards/测试与验收标准.md`（`:79` G17 行；`:22`／`:31` 现状句） | G17 行补半句——「**候选扩展位，本批不实现**（准入依据见 `AGENTS.md` §四：事故出身 ＋ 可机械化为零外部依赖断言）」；`[0]` 行与段输出规范的「缺『手动路径』与收尾汇总」现状句按 F5 落地结果**同步**（改「已补（078 批 F5）」或删去缺口描述，二者择一，须与 `check.sh` 实况逐字相符） | ⑨⑦；076 §九 B-1 | [B] |
| F8 | `docs/specs/施工机制.md`（`:89`–`:95` 审查两表之后） | **报告纪律三处通用**：在审查两表后补一条纪律行——「**计数／行数／条数类发现须附实测命令与输出**（`wc -l`／`grep -c` 等）对**成稿审查／产物审查／单维核对三处审查位通用**，未附者标注『估计』且不计入 findings（只能作观察项）」；**同时删去 `:91` 行内那句同义语**，改写为指向该纪律行（**不得**两处并存——两载体复述正是漂移之源，与 `规格写作标准` §5.5「失效即改写而非追加」同向）。 | ⑨；076 §九 B-2 | [B] |
| F10 | `scripts/check_spec.py`（头注「不可审计类」枚举段 `:17`–`:18`） | **枚举补全**：该段现只写 `python -c` 与「`python` 单命令」，而判据实现 `unauditable()` 的正则是 `^(python3?\|py)\b`（含 `python3`／`py`）——枚举与实现不符（076 §九 观察项②）→ 两处各补 `python3`／`py`，使枚举与实现逐字对齐。**只改措辞，不改判据** | 076 §九 观察项②（本批新增） | [B] |
| F9 | `CHANGELOG.md`（`## [Unreleased]` → `### Changed` 段首） | 本批一行：九项各一句、**≤400 字符**、末尾带「核验：」句，形态照 073 起各行体例 | 过程产物两项 | [B] |

**口径要点（易错）**：① F1 的汇总行**字段名逐字照写**（`改动行`／`可解析`／`判一适用`／`判二缺`），断言按这些串查；② F1 的**空转明示只对「有清单节且有改动行而 C＝∅」**——无清单节者**仍完全静默**（否则逐件刷屏，077 已实证）；③ `K`（判一适用＝命中前缀的件数，无论是否豁免）与「判一命中」（出候选者数）**是两个不同的数**，不得合并成一个字段；④ `P`（判二缺）＝**排除集缺项件数**（旧字段 `判二命中` 之值，只改名不改值）；④ F5 **不得**新增段、不得改段名、不得改退出码路径；⑤ F2 **不得**放宽为允许 `)` 前空白（会让非法形态静默通过）；⑥ F3 的改动是**行为等价改写**，其判据是「新返回值的真值与旧 `(n≥1, pos)` 逐例相同」；⑧ 六件之外**零改动**（尤其 `docs/specs/archive/` 全部只读）。

## 三、验收断言（含预验记录）

### 断言 1｜差异面只许四类（最强一道）

```bash
python scripts/check_spec.py --static docs/specs/archive/*.md docs/specs/施工机制.md docs/standards/*.md AGENTS.md README.md > .tmp/078/after.txt 2>&1
diff .tmp/078/before.txt .tmp/078/after.txt | grep "^[<>]" | grep -c "^[<>] · 对账① 汇总"
diff .tmp/078/before.txt .tmp/078/after.txt | grep "^[<>]" | grep -c "^[<>] · 对账① 空转"
diff .tmp/078/before.txt .tmp/078/after.txt | grep "^[<>]" | grep -c "^[<>] · 计数不符: scripts/check.sh"
diff .tmp/078/before.txt .tmp/078/after.txt | grep "^[<>]" | grep -c "^[<>] · 基线漂移"
diff .tmp/078/before.txt .tmp/078/after.txt | grep "^[<>]" | grep -c "^[<>] · 自噬预警: 对账①"
diff .tmp/078/before.txt .tmp/078/after.txt | grep "^[<>]" | grep -v "^[<>] · 对账①" | grep -v "^[<>] · 计数不符: scripts/check.sh" | grep -v "^[<>] · 基线漂移" | grep -v "^[<>] · 自噬预警: 对账①" | grep -vc "^[<>] · 有预警"
```

期望依次：**34**（17 行汇总行被改写，`diff` 两侧各计一行——**与第三项同一算术**）／**1**（`062` 的空转明示；无清单节者不产）／**2**（`060` 的「计数不符」载体，`<`／`>` 各一）／**2**（`060` 的「基线漂移」载体，同上一对）／**2**（归档 077 规格的自噬预警值随 F1 新增字面而变，一对）／**0**（其余差异行——含检查 A／B／C／D 全类输出与 `MD_LINK`／`_dir_split` 改动带来的 0 差异）。**差异行合计 43**（34＋1＋2＋2＋2＋末行三态一对 2）＝施工期实测 43 ✓。**预验记录**：基准 `.tmp/078/before.txt` 已冻结——**381 行／md5 `93c76a041ac13dd9799d8e1dec5787b8`／末行「有预警 281 条」**；改前实测「汇总行件数 **17**」「有改动行而 C＝∅ 者 **1 件（062，8 改动行）**」「语料改动行 **193**」。

### 断言 2｜F1 的新口径在三件上可判（语料实测，逐件值）

```bash
python scripts/check_spec.py --static docs/specs/archive/*.md 2>&1 | grep "^· 对账① 汇总" | grep -c "改动行"
python scripts/check_spec.py --static docs/specs/archive/059-2026-09-12-底座与业务分层.md | grep "^· 对账① 汇总"
python scripts/check_spec.py --static docs/specs/archive/063-2026-09-12-大纲域设计迁移.md | grep "^· 对账① 汇总"
python scripts/check_spec.py --static docs/specs/archive/062-2026-09-12-产品层内容第一批.md | grep -c "^· 对账① 空转"
python scripts/check_spec.py --static .tmp/078/fixture-exclude.md | grep -c "^· 对账① 汇总.*判二缺 1 件"
```

期望：第一项 **17**（全部汇总行含「改动行」字段）；第二项含 `改动行 33 行` 与 `可解析 3 件` 与 `判一适用 0 件`；第三项含 `改动行 8 行`、`可解析 8 件`、`判一适用 6 件`；第四项 **1**；第五项 **1**（夹具 `.tmp/078/fixture-exclude.md`：改动面 2 件、排除集只列 1 件 → `判二缺 1 件`——锁 F1 第四字段的**取值语义**，改前该行作 `判二命中 1`、`判二缺` 计数 **0**）。**预验记录**：第一／四／五项改前＝**0**／**1**（现文为 `改动面 3 件…` 旧口径，无「改动行」字段）／**0**；第二／三项的期望值取自 `.tmp/078/measure.py` 实测（059＝33/3/0、063＝8/8/6）。

### 断言 3｜C＝∅ 空转明示的正负控（夹具）

夹具 `.tmp/078/fixture-zero-c.md`（**正控**：有改动清单节、有改动行，但路径列写**不可解析为实存件**的路径 `docs/不存在的件.md`）与 `.tmp/078/fixture-no-section.md`（**负控**：无改动清单节）。另附 `.tmp/078/fixture-zero-c-ok.md`（**负控二**：有清单节且 C≠∅ → 只出汇总、不出空转行）。

```bash
python scripts/check_spec.py --static .tmp/078/fixture-zero-c.md
python scripts/check_spec.py --static .tmp/078/fixture-no-section.md
python scripts/check_spec.py --static .tmp/078/fixture-zero-c-ok.md
```

期望：第一件**含**空转明示行且 `有预警` 计数把该行计入；第二件**无任何** `· 对账①` 行（完全静默）；第三件**无**空转行、**有**汇总行。**预验记录**：`fixture-zero-c` 与 `fixture-no-section` 改前 **0** 对账行；`fixture-zero-c-ok` 改前 **1**（**现有汇总行**——该行改后仍在、仅口径换新，故本批对该件只断言「无空转行、有汇总行」）。三件已实跑。

### 断言 4｜F2 的 `MD_LINK` 认 title（探针目录 ＋ 三行夹具）

> **判据语义先厘清**（`check_writing()` ④，现 `:933`–`:945`——`MD_LINK` 迭代在 `:939`、追加预警行在 `:945`）：该判**不是「断链」检查**，而是「**按本件所在目录能解析、而按归档位置不能解析**」的链接（隐含「该件会被归档」）——故探针必须自造 `docs/specs/` 两层结构，且锚点文件须实存。

探针目录 `.tmp/078/probe/`：`README.md`（**实存锚点**）＋ `docs/specs/fixture-title.md`（三行，均以「从 `docs/specs` 能解析到 `README.md`、从 `docs/specs/archive` 不能」为目标）：① 目标 ＋ 引号包裹 title（**正控：改后须被认作链接而报出**）；② 目标 ＋ 空格 ＋ **无引号**尾注（**负控：不得被认作链接**）；③ 目标不存在的真断链（**负控二：两种状态下都不报**——判据只看「能解析者会不会因归档而断」）。

```bash
python scripts/check_spec.py --static .tmp/078/probe/docs/specs/fixture-title.md
```

期望：报出**恰一行**「规格写作预警（规格内可解析相对链接）」，且该行**原文含正控那行**（其文本含「带 title」）；负控两行**均不出现**。**预验记录**：改前该夹具 → `· 无发现`（**正控那行被整条跳过——潜伏假阴性实证**；负控二本就应不报）。**写作纪律**：本规格描述该夹具时**只用文字**、不写以「方括号 ＋ 圆括号」形态出现的字面链接样例——该形态会被本批断言 10 的一行式（不跳代码跨度与围栏）计成假断链（本规格初稿即踩，已改述为文字）。探针与夹具属 `.tmp/`（红线 2，用完即删）。

### 断言 5｜F3 的 `_dir_split()` 行为等价

```bash
python -c "import importlib.util as u;m=u.module_from_spec(s:=u.spec_from_file_location('c','scripts/check_spec.py'));s.loader.exec_module(m);cases=['改作「甲」','无标记文本','甲→乙','『A』改述为『B』','（仅括注）'];print([(m._dir_split(c)[0],m._dir_split(c)[1]) for c in cases])"
```

期望：输出**第一元全为布尔**（`True`／`False`，**不再出现整数**）且 `type` 为 `bool`，第二元与改前相同。**判据命令一律不加 `bool()` 包裹**——包了则改前改后都打 `True`／`False`，断言**恒真**、F3 是否落地不可检（成稿审查 P1-1 实证）。**预验记录**：改前原样命令实测 `[(1, 2), (0, 0), (1, 2), (1, 6), (0, 0)]`（**第一元是整数**；五例覆盖 0／1 标记，无 2 标记例）；改后第一元应为 `True`／`False`，并与旧值满足 `新 == (旧 ≥ 1)`。

### 断言 6｜F5 的 `check.sh` 三面

```bash
bash scripts/check.sh 2>&1 | grep -c "^手动路径："
bash scripts/check.sh 2>&1 | grep -c "^共 [0-9]* 段"
bash scripts/check.sh 2>&1 | tail -3
git diff -- scripts/check.sh | grep -c "^[+-]# \\|^[+-]echo \"\\[[0-9]\\]"
```

期望：第一项 **≥1**（`[0]` 跳过时的手动路径；本机 `skills/` 未建 → 必现。**判据须锚定行首**——不锚定会被 `[4]` 段对**在制规格**的输出污染，实测未锚定得 1 而锚定得 0，污染源不是 `[0]` 段）；第二项 **1**（收尾汇总）；第三项末行仍为 `== 检查全绿 ==` 或置红句 ＋ `exit` 语义不变（**本机须为全绿、退出 0**）；第四项 **0**（**未动头注段序行与任何段标题行**——段序/段名真源不得漂移）。**预验记录**：改前锚定后 **0／0**（未锚定误得 1，来源如上）；`check.sh` 退出 **0**、末行 `== 检查全绿 ==`；头注段序行与段标题行 hunk **0**。

### 断言 7｜F6／F7／F8／F10 四处文档落点

```bash
grep -c "修前那句" docs/standards/规格写作标准.md
grep -c "现文作" docs/standards/规格写作标准.md
grep -c "候选扩展位，本批不实现" docs/standards/测试与验收标准.md
grep -c "三处审查位通用" docs/specs/施工机制.md
python -c "import io;print(io.open('scripts/check_spec.py',encoding='utf-8').read().split(chr(34)*3)[1].count('python3'))"
```

期望依次 **1**／**0**／**1**／**1**／**≥1**（末项＝**模块 docstring 内** `python3` 计数；用 `chr(34)*3` 切出首个三引号串，避开正文实现处的两处 `python3`——`grep -c` 全件查会得 2 而不可判）。**预验记录**：改前 **0**／**1**／**0**／**0**／**0**。

### 断言 8｜结构锚与头注同步

```bash
grep -c "判一适用" scripts/check_spec.py
grep -c "对账① 空转" scripts/check_spec.py
grep -c "cmd.exe" scripts/check_spec.py
grep -c "def _dir_split" scripts/check_spec.py
python -c "import ast,io;ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read());print('[语法自检通过]')"
```

期望依次 **≥1**／**≥1**／**≥1**／**1**／`[语法自检通过]`。**预验记录 0／0／0／1／通过**。（**不取 `改动行` 作 token**：该串现文已出现 2 次——`_change_rows()` docstring 即含，取之则基线非 0、涨落不可判。）

### 断言 9｜过程产物两项

```bash
grep -cF '尾账清理与守卫收口（078）' CHANGELOG.md
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '尾账清理与守卫收口（078）' in l][0].rstrip()))"
```

期望：第一项 ≥1（**唯一可预验项**）、第二项 ≤400（**施工后实核项**——改前该行不存在，命令抛 `IndexError`，成稿审查 P2-5 实证），且该行**末尾带「核验：」句**、**不含**「数字 ＋ 行／处／字符」跟在实存路径代码跨度之后（073-3 口径）。验收通过后本规格移入 `docs/specs/archive/`。

### 断言 10｜禁改面、hunk 归属与全仓守卫

```bash
git status --porcelain . ':!scripts/check_spec.py' ':!scripts/check.sh' ':!docs/standards/规格写作标准.md' ':!docs/standards/测试与验收标准.md' ':!docs/specs/施工机制.md' ':!CHANGELOG.md' ':!docs/specs/078-2026-09-14-尾账清理与守卫收口.md' | wc -l
bash scripts/check.sh
npx markdownlint-cli2 "**/*.md"
```

期望：第一项 **0**（补集式；**含 `docs/specs/archive/` 零改动**）；第二项退出 **0**；第三项 **0 issue**（件数不设期望值）。另核全仓相对链接**断链 0** 且条数**不低于 175**（**预验 175／0**；判据见本规格 §三 末一行式。**该一行式不跳代码跨度与围栏**——规格内出现目标指向不存在件的字面链接样例即产假断链，本规格初稿在断言 4 说明里踩过一次、改述为文字后复归 0）。**预验基线** 0／0／0／175‧0。

链接判据（自带一行式，不再外引）：

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md')];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

## 四、规格自审（三条）

1. **锚点现场核过**：§一 九条逐条实跑——`:194`／`:361`／`:1106`／`:482`／`:502`／`:949`–`:1011`（`check_reconcile()` 定义至 `return`）／`:933`–`:945`（`check_writing()` ④）／`:17`–`:18`（不可审计类枚举）与头注位置读原文核行号；`check.sh` **66 行**（`wc -l` 口径）与 `[0]`／收尾段实读；四处文档落点（`规格写作标准`:62、`测试与验收标准`:22／:31／:79、`施工机制`:89–:95）逐行读；060 的陈旧声称由工具实跑复现（`计数不符: …称「39 行」实测 66`）；三组语料数字（汇总行 **17** 件／C＝∅ 者 **1** 件（062）／语料改动行 **193**／判一适用 **21 例 9 件**）由 `.tmp/078/measure.py` 实跑得出。
2. **断言实跑过**：断言 1 的基准已冻结（`.tmp/078/before.txt`）；断言 3／4 的**修前基线**已实跑（夹具 0 对账行／0 断链报出）；断言 5 的改前返回值已实读；断言 6／7／8／9／10 的基线已实跑（0／0／全绿／…）。**逐条核断言 token 与本规格目标文本的互斥性**：断言 8 的四 token 为 `判一适用`／`对账① 空转`／`cmd.exe`／`def _dir_split`（**不取 `改动行`**——现文已有 2 处），前三条落在 F1／F10 的目标文本内 ✓；`def _dir_split` 为**结构锚**（锚「函数定义行仍在」），不在任何 F 格替换文本内，工具据此出「现行有值／目标文本未提及」候选行属**既有取数口径**（`_row_target_text()` 引号块优先），非互斥问题。断言 7 的四 token 落在 F6／F7／F8 目标文本内 ✓。**未预验项两处**（非「无」）：断言 9 的第二命令（改前不存在该行，抛 `IndexError`）与断言 10 的第二／三项，均属施工后实核。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` **全部**（含 060 那份陈旧声称——本批**只**让工具报出的实测值随动，**不改该件**）、`docs/product/`、`skills/`、`scripts/hooks/`、`AGENTS.md`、`README.md`、`docs/specs/施工机制.md` **除 F8 点名处之外的部分**。
- 禁止动作：联网、安装依赖、在源仓库内跑分发／安装工具。
- 禁止越权：不改版本号与 tag；**除 §二 点名的六件外不新增不改动任何文件**；不建台账／不建索引件。
- 禁止改断言、改数字、删检查来变绿；**不得放宽**既有 A／B／C／D 四类与检查 E 的既有判据；**不得**把 F2 放宽为允许 `)` 前空白、**不得**把 F1 的空转明示扩到「无清单节」的件（逐件刷屏）、**不得**在 F5 内新增段或改段名、**不得**改动 `check.sh` 的退出码路径。

## 六、被否决的替代方案

- **F4 改为显式 `executable='bash'`**（075 §八 1 的另一条路）→ **否决**：① 该路径要解决「bash 在哪」（Windows 下 `shutil.which('bash')` 未必命中、命中亦可能是 WSL 的 bash，语义与 Git Bash 不同）；② 075 的实测差异（`$()`／反引号在 `cmd.exe` 下不展开）**在执行面收窄后已不可达**——`$`／反引号／引号外元字符一律判不可审计 → 两宿主的**可达命令面**已是同一批简单 argv 形态，差异面已关闭；③ 本机动态阶段历批实证可用。故取「头注落文 ＋ 不追求跨宿主可复现」，并把「作者要求跨宿主可复现」留为解冻触发（触发条件原文即两分支，本条取其一）。
- **F1 的空转明示扩展到「无改动清单节」的件** → 否决：实测全语料该类件 16 件（有验收节而无清单节者），逐件明示即刷屏——077 已就此立「静默的可见化落点是汇总行／单行明示，不是逐件明示」的口径。
- **顺手清 077 §八 其余项**（`MD_LINK` 之外的两项、工具自带测试件、`shell=True` 的另一分支）→ 本批**不塞**：`MD_LINK`／`_dir_split` 已由 F2／F3 处置；工具测试件与跨宿主可复现各需独立决策，留 §八。
- **F5 把收尾汇总做成新段 `[6]`** → 否决：段序与段名真源＝`check.sh` 头注，新增段会连带 `测试与验收标准` §1.1 表与 `AGENTS.md` 的指针同步（六处漂移的老病）——故收尾汇总作为**末尾一行**并入既有结构，不动段序。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批（077）规格的全部开放项**（必列面 ①：§八 全 **7** 条）＋**本批改动直接触及或可能变状态者**（必列面 ②）。判据＝**同节交集**；**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 077 §八 1 | 族二（断言 token ↔ 改动格）未落工具＋实测数据存档 | 仍待 | 触发原文未到（须「完整规范化形态」判据成形或族二复发）；本批不动 |
| 077 §八 2 | `MD_LINK` 带 title ＋ `_dir_split()` 计数语义 | **本批处置** | **F2 ＋ F3**（两项同批清） |
| 077 §八 3 | 检查 E 判据仍是粗判（两向漏报） | 仍待 | 触发＝各自出现一次实例时按实测收紧；本批只改汇总口径，不动判据 |
| 077 §八 4 | 汇总行口径失真与观测缺口 | **本批处置** | **F1**（四项计数 ＋ C＝∅ 空转明示） |
| 077 §八 5 | 判一产出方向登记（泛称豁免会被报出） | 仍待 | 已登记即履行；本批不动判据 |
| 077 §八 6 | 075 侧四项（宿主语义／段表形式冲突／行数耦合／`MUTATING` 前向防御） | **本批处置（1/4）** | **F4** 落宿主语义；另三项仍待（段表形式冲突待触碰 `check.sh` 头注时评估——本批 F5 恰触该件但**不动头注段序**，故该条仍待） |
| 077 §八 7 | `check_spec.py` 自带测试件 | 仍待 | 触发＝出现「工具判据回归无人发现」实例 |
| 076 §八 1 | `shell=True` 的宿主语义取舍（顺延自 075 §八 1） | **本批处置** | **F4**（头注落文；与 077 §八 6 行**同源**，按行计两行、按事实计一） |
| 076 §八 2 | `[0]` 段的「手动路径」与「收尾汇总」（`测试与验收标准` §1.1 自陈缺口） | **本批处置** | **F5**（**触发已到**：原文＝「会改 `check.sh` 行数…须一并裁定」，作者本批点名要求收口；行数耦合按 **075 §九（施工期 P0-b 裁定）** 的「按归因处置」处理——归因见本表末行与 §八 4） |
| 076 §八 3 | `~~~` 围栏／`MD_LINK` 带 title／`_dir_split` 计数（三项） | **本批处置（2/3）** | **F2 ＋ F3** 落后两项；`~~~` 已由 **077 F2** 履行（与 077 §八 2 行**同源**，按行计两行、按事实计一） |
| 076 §九 A-2 | `规格写作标准` §5.5 实例误称「现文」 | **本批处置** | **F6** |
| 076 §九 B-1 | G17 行缺「候选扩展位／本批不实现」半句与准入指针 | **本批处置** | **F7** |
| 076 §九 B-2 | 报告纪律只落「成稿审查」行，产物审查行未含 | **本批处置** | **F8**（取「三处通用 ＋ 删原句改指向」形态） |
| 075 §九（施工期 P0-b 裁定） | 「逐字节不变」不变量与「改 `check.sh` 行数」的耦合 | **本批处置** | **F5 正改 `check.sh` 行数**（触发原文＝「确需改其行数时按归因处置」）→ 归因：060 的陈旧计数声称**保留不动**（归档件只读、两处例外均不适用），其因本批改动而变化的「实测」值作为**计划内差异行**列入 §一 不变量第 ② 类；代价与解冻条件记 §八 4 |
| 076 §九 观察项② | 头注六类枚举的 ②③ 只写 `python`（实现含 `python3`／`py`） | **本批处置** | **F10**（本批新增项：一行级、同头注、同属「尾账」） |

**汇总**：共 **15 行／按条 15**——**本批处置 11**（077 §八 2／4／6；076 §八 1／2／3／§九 A-2／B-1／B-2；075 §九 P0-b；076 §九 观察项②）｜**已履行 0**｜**仍待 4**（077 §八 1／3／5／7）｜作废 0｜待作者 0 ＝ 15 ✓（逐行可复核；「汇总＝行数」恒成立。**同源交叉**：077 §八 2 与 076 §八 3、077 §八 6 与 076 §八 1 各为同一事实的两行——按行计 15、按事实计 13，**不重复计入「本批处置」的动作面**）。**范围注**：076 §八 与 §九、075 §八／§九 的其它条目已由 077 §七 24 行逐条核销（状态见该表），本批只重列**可能因本批改动变状态者**；074 及更早各批的开放项落点在别处，以其各自核销表为索引。

## 八、开放项

1. **`check.sh` 段序真源的六处漂移风险**：本批 F5 只补「末尾一行 ＋ `[0]` 一行手动路径」，**未动段序段名**；但 `测试与验收标准` §1.1 表、`check.sh` 头注、`AGENTS.md` 指针三处仍各有一份「段」的表述。触发＝出现段序／段名不一致实例时，评估「表只留现状、段序只认头注」是否已在各处落实。
2. **`shell=True` 的跨宿主可复现**（077 §八 6 剩余里的一条）：本批取「头注落文」；触发＝作者要求守卫行为跨宿主可复现时，评估 `shell=False` ＋ 逐命令参数表（届时须一并解决 `.cmd` 载体与 token 反斜杠转义两题）。
3. **`check_spec.py` 自带测试件**（077 §八 7 顺延）：本批仍只由 `.tmp/` 夹具验证、用完即删（红线 2）。触发同原文。
4. **`060` 归档件的陈旧计数声称（`check.sh` 39 行）**：本批改 `check.sh` 行数（66→94）后该声称与实况差距更大，但归档件原则只读、两处例外均不适用 → **永久保留为「工具每次都报的候选」**（非阻断）。**该声称有两个载体**（检查 B 的「计数不符」＋「基线漂移」），故本批差异面按 4 行计（施工期实证）；同族的「归档件的工具实测值随工具演化而变」另有**一例**：归档 077 规格的断言 token `对账①` 因 F1 新增字面而「现行 3→4」（差异面第 ⑥ 类）。触发＝作者裁定为归档件开第三处例外时，或该类「归档件实测值随仓演化失效」积累到成噪声时，评估在归档件表头加「本件实测值截至成文时」的口径注。

5. **规格自身写「字面链接样例」会污染链接判据**：各批沿用的「链接一行式」**不跳代码跨度与围栏**，规格里一旦出现目标指向不存在件的字面链接样例（哪怕包在代码跨度里）即产假断链（本批断言 4 初稿即踩）。触发＝再踩一次时，评估给该一行式加跨度／围栏感知，或在 `规格写作标准` §5 补一条写作律。

## 九、审查记录

- 成稿审查分身（fresh、只读）：待回填。
- 2026-09-14｜**成稿审查**（fresh 只读分身）｜findings 共 **13**：**P0:1** P1:3 P2:9｜结论 **修订后通过**。审查方**逐条实跑**锚点（20 处核过为真、3 处不实）、断言 1–10 **全部自跑**（不照抄预验值）、并替本规格**模拟** F1 落地后的输出以验断言 1。处置：
  - **P0-1（核销表必列面②缺行）**：原 §七 只列 077 §八 七条，而本批 **F5／F6／F7／F8／F4／F10 实际处置的是 076 侧六条 ＋ 075 §九 P0-b** → **已补 8 行**（076 §八 1／2／3、076 §九 A-2／B-1／B-2、075 §九 P0-b、076 §九 观察项②），汇总重算为 **15 行／本批处置 11／仍待 4**，并注明两处同源交叉。**此为 077 成稿审查 P0-2 的同族复发**（核销表只核前一版、漏计本批实际处置的更早批次条目）。
  - **P1-1（断言 5 恒真）**：命令把首元包进 `bool()`，改前改后都打 `True`／`False` → 断言**无法检出 F3 是否落地**（审查方实跑两侧证明）→ **已删 `bool()` 包裹**，预验记录同步为「改前未包裹输出＝`[(1, 2), …]`」。
  - **P1-2（断言 1 首项恒假）**：17 行汇总行是**改写**而非纯新增，`diff` 两侧各计一行 → 应为 **34**；规格同一段却给 060 那行写「`>` 与 `<` 各一」（自相矛盾）→ **已改 34 并写明算术**。
  - **P1-3（F1 第四字段语义未定义）**：`判二排除集覆盖 P 件` 的 `P` 无定义、且两种读法相反 → **已改为 `判二缺 P 件`＝`len(missing)`（旧字段 `判二命中` 之值，只改名不改值）**，并补夹具断言（`fixture-exclude` → `判二缺 1 件`）。
  - **P2 九条**：不变量漏列第 5 类差异（`· 有预警` 281→282）→ 已补（P2-1）；三处锚点不实（`check_reconcile` 真址 `:949`–`:1011`、`check_writing()` ④ 真址 `:933`–`:945`、`check.sh` **66** 行而非「67 行（内容 66 行）」——后者是 `split` 计数法产物、违 `规格写作标准` §5.3 口径）→ 已改（P2-2）；F5 未规定两行**顶格**而断言 6 按 `^` 锚定、且括注写死机器状态 → 已改（P2-3）；§四 自审 2 的 token 列表陈旧（`改动行` → `判一适用`）且「无未预验项」不实 → 已改（P2-4／P2-5）；断言 5 说明句「0／1／2 标记」与五例实际只覆盖 0／1 矛盾 → 已改（P2-6）；F8 的「保留不复述」与「改为指向」互斥 → **已择一（删 `:91` 行内同句、改为指向）**（P2-7）；F2 多出无依据的**全角括号** title 支路 → **已删**（P2-8）；断言 2 预验记录引「058」（本仓无此件）→ 已改述（P2-9）。
  - **审查方独立判断**：两处取舍（F4 头注落文、060 不改）**均认**；并指出 076 §九 观察项②（头注枚举只写 `python`）是**廉价漏项** → **本批采纳入批，即 F10**。
- 2026-09-14｜**施工期偏差裁定（规划方；施工方申报 1 P1 ＋ 4 P2）**｜**全部照准**：
  - **P1（照准；缺陷在我）**：断言 1 第四项期望 0，实测 **4** —— 漏算两个载体：① 060 陈旧声称除「计数不符」外另有**「基线漂移」**报法（同一声称的第二载体，一对 2 行）；② 归档 077 规格的断言 token `对账①` 因 F1 新增字面而「现行 3→4」（「归档件的工具实测值随工具演化而变」同族，一对 2 行）→ **§一 不变量扩为七类**（新增第 ⑥ 类）、断言 1 补两项判定并把这两类并入第四项的过滤链，**总差异 43** 逐项闭合；施工方**未改断言**（正确）。
  - **P2-1（照准）**：F5 的收尾汇总施工方取**从头注派生**（`_seg_summary()` 读 `'# [N] '`／`'# TODO: [N] '`）而非写死段数与待建段——代价 28 行（非「一行级」），收益是**不写死机器状态**（`skills/` 建起后自动跟随）→ 判为**优于规格要求**，保留。
  - **P2-2／P2-3**：手动路径行按「」逐字口径含反引号、F7 现状句取「已补（078 批 F5）」——均照准。
- 2026-09-14｜**双维产物审查**（fresh、只读；改工具判定行为 ＋ 动标准条文 → 按 `施工机制` §五 双维）｜findings 共 **10**：**P0:0 P1:0 P2:0**、P3×7 ＋ 观察项 3｜结论 **通过**。审查方**独立复现基准**（用 `git archive HEAD` 展纯 HEAD 树自跑 → md5 与 `.tmp/078/before.txt` 逐字节相同）、**自造夹具与变异体**做实 8 组**反事实**（字段名改回旧／删空转返回／空转行不计入 findings／删 `_seg_summary` 调用／删手动路径行／汇总行加缩进／`_dir_split` 退回计数／`判一适用` 改取候选数）证明断言**真能报红**、以 **5727 个表格单元格**逐例证明 `_dir_split` 行为等价、以 `ast` 抽函数源码段证明 **38 个函数中仅 3 个（`_dir_split`／`_target_blocks`／`check_reconcile`）有改动、无增删函数**。处置（**七条 P3 全部照做，列入施工方补修**）：
  - **P-1** `hit_one` 死变量（本批新引入）→ 删两处并改写注释（**须避开 `对账①` 字面**——它被归档 077 规格的断言监视，引之即多一行差异；施工方自行发现并绕开）✓；**P-2** `check.sh` 段号模式只认一位数（第 10 段起静默漏计）→ 两处各加 `*` ✓；**P-3** 解析面是整件而非头注块（正文列 0 的 `# [N]` 注释会成幻影段）→ 加「遇 `set -u` 行即 break」✓（含**反证**：去掉该行即复现幻影段）；**P-4** `${BASH_SOURCE[0]}` 退化时静默打印「共 0 段」（错数且仍绿）→ 改 `_self="${BASH_SOURCE[0]:-$0}"` ＋ 读不到时打显式标记并 return（**不打错数、不置红**）✓；**P-5** `CHANGELOG` 核验句未覆盖第 ⑥ 类差异 → 补正（**397 字符**，首稿 410 被长度守卫拦下）✓；**P-6** 头注 F10 引文精度 → 改「正则族」并注明两判据各自的接续（**源码内反斜杠双写**：非 raw docstring 直写 `\s` 会每次运行打 `SyntaxWarning`，污染 `check.sh` 的 stderr）✓；**P-7** `scripts/__pycache__/` → 删净并置末（`import` 会重建）✓。
  - **补修期 P1（照准）**：等价性**不可能逐字节相同**——P-2／P-3／P-4 必然给 `check.sh` 增行，而 060 那两行的「实测」值**就是**该件行数（94→**100**）→ 归因属 §一 不变量第 ② 类的**值随动**，非判据变化；等价性口径按「除 060 两行的实测值随动外逐字节相同」核（实测 **380／382 行逐字节相同**）。
  - **审查方独立判断**：收益最低＝F3（零可观测增量，价值在防未来误用）、风险最高＝F5（本批唯一改到提交门禁执行体的项）；「派生式实现」认（段序唯一真源已定为头注）并给出可执行加固（即 P-2／P-3／P-4，已落）。
- 2026-09-14｜**规划方核验（三步 ＋ 修复面自核验）**｜**通过**。① 改动面核对：恰**六件**＋本批规格未跟踪（`CHANGELOG` `1 0`／`施工机制` `2 1`／`测试与验收标准` `3 3`／`规格写作标准` `1 1`／`check.sh` `34 0`／`check_spec.py` `44 22`）；`git diff -U0` 的 hunk 全落在头注、`MD_LINK` 常量、`_dir_split`／`_target_blocks`、`check_reconcile` 四处；**函数级逐字节比对：38 个函数中 35 个相同，有改动者恰为点名的三个，无增删**；禁改面补集式 0（含 `docs/specs/archive/` 零改动）。② 断言独立重跑（修复面）：断言 1 六项 `34／1／2／2／2／0`、总差异 **43**；断言 2 五项 `17／33-3-0／8-8-6／1／1`；断言 3 三夹具 `1／0／1`；断言 4 恰 1 行且含正控；断言 5 首元全 `bool`；断言 6 `1／1／全绿 exit 0／hunk 0`；断言 7 `1／0／1／1／3`；断言 8 `2／1／1／1／通过`；断言 9 `1／397`；断言 10 `0／exit 0／0 issue／175‧0`。③ 内容抽查：F1 四字段与空转发射面、F2 只认 ASCII 引号 title、F3 布尔化、F5 顶格与派生式收尾汇总**逐条与规格一致**；`check.sh` 段序段名与退出码零改动。④ 等价性：除 060 两行的实测值随动外，**380／382 行逐字节相同**。
- 施工后核验与双维产物审查：待回填（**本批改工具判定行为 ＋ 动标准条文 → 按 `施工机制` §五 双维**）。
