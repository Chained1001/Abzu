# 080-2026-09-14-产品层按域分层

> 定位：把 `docs/product/` 按域分成 `outline/`／`volume/`／`write/` 三个子文件夹（**文件名一律不变，只动位置**），共享件留顶层——作者裁定（2026-09-14）：书目录契约留顶层（实测其 **17** 条入链仅 3 条来自大纲域件，跨域消费为主）；文件夹名用 `outline`／`volume`／`write`。勘察时发现的「`docs/product/总纲.md`」引用（**9 处**）经逐处判型**全部为 `main` 存档沿革引用、无活指针**，不随本批处置。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**——多文件结构迁移＋改 `命名标准` §1；**按 `施工机制` §五 走双维产物审查**（结构类批次）。scope：**产品层＝product**、**标准件＝standards**、**门面＝README**。
> 自由度分布：[A]×0 [B]×4
> 必读输入：本规格 §一（勘察结论即锚点表）；`docs/standards/命名标准.md` §1 目录树（`:27` 一带）与 `:57`「产品文档」表行与 `:74` 口径句；`README.md:23`（产品索引行）；§一所列 10 件的链接行；`docs/specs/archive/070-*.md` 的 `docs/product/产品定义.md` 链接（唯一归档入链，目标留顶层故不断）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（勘察实测，2026-09-14；经成稿审查复核修订）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `docs/product/` 现有 **12 件**，全在顶层 | 产品定义／架构设计／术语表／验收场景／书目录契约（**5 件留顶层**）＋ 大纲域设计及其规格与资料步／草稿步／打磨定稿步／附录（**5 件 → outline/**）＋ 卷纲域设计（**→ volume/**）＋ 正文域设计（**→ write/**）＝ 7 件移动 ✓ |
| ② | 全仓指向 `docs/product/` 的**文件链接**共 **100 条**（活 99＋归档 1），另有 2 条**目录链**（`README:23` 与 `产物契约标准` 的 `../product/`——目录仍在，迁移后有效，不计入改写面） | 归档唯一 1 条＝`archive/070-…md → docs/product/产品定义.md`——**目标留顶层，迁移后仍有效**（归档零改动、零例外追认）✓ |
| ③ | 需改写的链接共 **31 条、10 件**（成稿审查 P0-1 复核后总数；初稿勘察漏 9 条） | **入链（他件→移动件）20 条**：README **7**（`:23` 一行内）＋测试与验收标准 **3**（`:46`）＋触发与交互标准 **2**（`:36`／`:66`）＋验收场景 **3**（`:21`）＋**书目录契约 5**（`:49` 附录 ×1、`:50`–`:53` 大纲域设计 ×4）。**出链（移动件→外）11 条**：指回顶层共享件 **7**（大纲域设计 `:22`→书目录契约；规格与资料步 `:19`／`:135`→书目录契约；打磨定稿步 `:30`→架构设计；卷纲 `:8`／`:16`→书目录契约；正文 `:16`→书目录契约——各改 `../` 前缀）＋**跨层加深 4**（大纲域设计 `:73`→`../standards/触发与交互标准.md`；规格与资料步 `:4`→`../standards/安全与合规标准.md`；打磨定稿步 `:4` 与 `:30`→`../specs/施工机制.md`——从子文件夹解析须 `../../`）。**大纲五件互链（同子文件夹）一律不动** ✓ |
| ④ | 零改动件（链接目标全在留顶层五件或仓外） | `AGENTS.md`（链术语表／架构设计）、`施工机制`（术语表）、`skill形态标准`（术语表）、`产物契约标准`（书目录契约 ×3＋1 条目录链）、`文档写作标准`（术语表／书目录契约）、`术语标准`（术语表 6 链／架构设计 2 链／书目录契约）、`安全与合规标准`、`运行环境标准`、`规格写作标准`——**九件零改动** ✓ |
| ⑤ | 两件纯移动件（无跨层出链、无指回顶层出链） | `大纲域设计-草稿步.md`（出链全为域内）md5 `3d65aceded9a593ce49a3903d5fe9fcb`；`大纲域设计-附录.md`（同）md5 `336ec6d7da83d65496e33d60b687e00d`——**迁移后须逐字节不变** ✓ |
| ⑥ | 基线 | 链接一行式 **175／断链 0**；`bash scripts/check.sh` exit **0**；markdownlint **0 issue**；`命名标准` 内涉产品件**位置口径**的共三处：§1 目录树（`:27`）＋`:57`「产品文档」表行的位置列＋`:74`「中文名」口径句 ✓ |
| ⑦ | `docs/product/总纲.md` 引用（**9 处**，活件面）判型 | 架构设计`:4`、产品定义`:4`／`:50`、安全与合规`:4`／`:16`／`:17`、触发与交互`:4`、运行环境`:14`、术语标准`:4`——逐处带「`main` 存档」限定词＝**旧仓沿革出处引用**，无一处当现存文件指向——**不改**；立此备查防后续评审重报 ✓ |

**不变量（施工前后须同）**：链接一行式**条数 175、断链 0**（改写不增减链接数；**文件列表须按磁盘存在性过滤**——文件系统 `mv` 后 `git ls-files` 仍列旧路径，不过滤则一行式在核验态崩溃，成稿审查 P0-2 实证）；两件纯移动件 md5 不变；`docs/specs/`（含 `archive/`）与 §一④ 九件**零改动**；文件名零变化；`skills/`、`.claude-plugin/` 不建；lint **0 issue**（不设件数期望——扫描面含在制规格，件数随批内状态浮动）。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `docs/product/`（新建 `outline/`／`volume/`／`write/` 三目录 ＋ **7 件移动**） | **文件系统 `mv`**（**禁 `git mv`**——git 写操作须作者单点确认，施工分身无权；rename 语义由提交时暂存检出承载）：大纲域设计.md／大纲域设计-规格与资料步.md／大纲域设计-草稿步.md／大纲域设计-打磨定稿步.md／大纲域设计-附录.md → `outline/`；卷纲域设计.md → `volume/`；正文域设计.md → `write/`。文件名一律不变 | ①② | [B] |
| F2 | **10 件**（README＋测试与验收标准＋触发与交互标准＋验收场景＋书目录契约＋5 个移动件） | **31 条链接改写**，九类（执行判据＝**字符串形态**——每形在其件内的出现次数已实测：五形多次（书目录契约的主件形 4 次、触发与交互标准的大纲形 2 次、规格与资料步的书目录契约形 2 次、打磨定稿步的施工机制形 2 次、卷纲的书目录契约形 2 次），余 19 形各 1 次；行号仅参考，作者若在定稿与施工间继续打磨，以字符串形态为准）：① README `:23`——`docs/product/大纲域设计.md` 及 `-规格与资料步`／`-草稿步`／`-打磨定稿步`／`-附录` 四件前插 `outline/`、`卷纲域设计.md` 前插 `volume/`、`正文域设计.md` 前插 `write/`（7 条；同行的 5 条顶层链接不动）；② 测试与验收标准 `:46`——`../product/大纲域设计.md`→`../product/outline/大纲域设计.md`、卷纲→`volume/`、正文→`write/`（3 条）；③ 触发与交互标准 `:36`／`:66`——`../product/大纲域设计.md` ×2 前插 `outline/`（2 条）；④ 验收场景 `:21`——`大纲域设计.md`→`outline/大纲域设计.md`、卷纲→`volume/`、正文→`write/`（3 条；`:15` 书目录契约同目录链接**不动**）；⑤ **书目录契约 `:49`–`:53`——`](大纲域设计-附录.md)`→`](outline/大纲域设计-附录.md)`、`](大纲域设计.md)` ×4→`](outline/大纲域设计.md)`（5 条；成稿审查 P0-1 补）**；⑥ 移动件指回顶层——`](书目录契约.md)`→`](../书目录契约.md)`（大纲域设计 `:22` ×1＋规格与资料步 ×2＋卷纲 ×2＋正文 ×1＝6 条）、`](架构设计.md)`→`](../架构设计.md)`（打磨定稿步 `:30` ×1）；⑦ **跨层加深——`](../standards/触发与交互标准.md)`（大纲域设计 `:73`）、`](../standards/安全与合规标准.md)`（规格与资料步 `:4`）、`](../specs/施工机制.md)`（打磨定稿步 `:4`／`:30`）各改 `../../` 前缀（4 条；成稿审查 P0-1 补）**。**大纲五件互链一律不动** | ③ | [B] |
| F3 | `docs/standards/命名标准.md`（§1 目录树 `:27` ＋ `:57` 表行 ＋ `:74` 口径句） | 目录树 `product/` 行展开为四行（product/ 共享件注释 ＋ `outline/`／`volume/`／`write/` 三子行）；`:57`「产品文档」行「位置」列改「`docs/product/`（共享件在根；域件在 `outline/`／`volume/`／`write/` 子文件夹）」；`:74`「中文名」句的产品文档路径口径同步为「`docs/product/{主题}.md`（共享件）或 `docs/product/{域}/{主题}.md`（域件）」 | ⑥ | [B] |
| F4 | `CHANGELOG.md`（`## [Unreleased]` → `### Changed` 段首） | 一行：『产品层按域分层（080）：…』——迁移了什么／31 条链接与 `命名标准` 三处同步／核验结论，**≤400 字符**、末尾「核验：」句 | 过程产物 | [B] |

**口径要点（易错）**：① **先 F1 后 F2**（先移后改）；② 31 条**逐条按 §一③ 的字符串形态落位，不得多改一条**（同行顶层链接不动）；③ `mv` 用文件系统命令，**全程零 git 写**；④ 新目录只建三个；⑤ `验收场景:15` 的 `](书目录契约.md)` **保留**（同目录链接仍成立）——与移动件内同形链接（必须变 `../`）严格区分；⑥ **施工形态＝一次性迁移脚本**（`.tmp/080-build/migrate.py`，红线 2 合规、用完即删）：对 31 条逐条做**精确字符串替换并按形断言期望次数**（五形为 4／2／2／2／2，余 19 形为 1——多一次少一次即中止报错；「每形恰 1 次」的初稿口径已被窄域重审 R1 推翻，不得回退）→ 建３目录与 `mv` → 输出 31 行「件｜旧形｜新形」diff 表作核验记录——**本规格 §二 F2 清单仍是唯一真源，脚本只是执行载体**（`AGENTS.md` §五.1：脚本做确定性）；⑦ 改写后链接**总数不变**（每条只改路径段）。

## 三、验收断言（含预验记录；逐命令一期望）

### 断言 1｜outline 子目录件数

```bash
ls docs/product/outline/ | wc -l
```

期望 **5**。**预验基线**：目录不存在（迁移前实跑）。

### 断言 2｜volume 与 write 子目录件数

```bash
ls docs/product/volume/ | wc -l
```

期望 **1**；同式 `write/` 期望 **1**（合并记录）。**预验基线**：两目录不存在。

### 断言 3｜顶层恰余 5 件且旧路径清零

```bash
ls docs/product/*.md | wc -l
```

期望 **5**（产品定义／架构设计／术语表／验收场景／书目录契约）；另以 `test ! -f` 逐验 7 个旧路径（大纲域设计及其四伴件／卷纲域设计／正文域设计），输出无「旧路径残留」字样。**预验基线**：顶层 **12**。

### 断言 4｜链接一行式（**按磁盘存在性过滤**）

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**（与 §一⑥ 基线同值——`os.path.exists` 过滤使本命令在「已 mv、未提交」的核验态照常可跑；成稿审查 P0-2 修）。**预验基线 175‧0**（迁移前用**同一条**命令实跑）。

### 断言 5｜README 的入链前缀

```bash
grep -o "docs/product/outline/" README.md | wc -l
```

期望 **5**；同法 volume／write 前缀各期望 **1**（用 `-o` 计**出现次数**——README 7 条链接同行，`grep -c` 计行会恒得 1、判别力尽失，窄域重审 R2 修）。**预验基线 0**。

### 断言 6｜标准件与验收场景的入链

```bash
grep -c "product/outline/大纲域设计.md" docs/standards/测试与验收标准.md
```

期望 **1**；`grep -c "product/outline/大纲域设计.md" docs/standards/触发与交互标准.md` 期望 **2**；`grep -c "](outline/大纲域设计.md)" docs/product/验收场景.md` 期望 **1**（同法卷纲／正文前缀各 1，合并记录）。**预验基线**：全 0。

### 断言 7｜书目录契约的 5 条入链（成稿审查 P0-1 补）

```bash
grep -c "](outline/大纲域设计" docs/product/书目录契约.md
```

期望 **5**（附录 ×1＋主件 ×4，`:49`–`:53`）。**预验基线 0**。

### 断言 8｜移动件指回顶层的出链

```bash
grep -c "](\.\./书目录契约.md)" docs/product/outline/大纲域设计.md
```

期望 **1**；同法：规格与资料步期望 **2**、卷纲域设计期望 **2**、正文域设计期望 **1**（合计 6）；`grep -c "](\.\./架构设计.md)" docs/product/outline/大纲域设计-打磨定稿步.md` 期望 **1**。**预验基线**：全 0（目录未建）。

### 断言 9｜跨层加深 4 条（成稿审查 P0-1 补）

```bash
grep -c "](\.\./\.\./standards/触发与交互标准.md)" docs/product/outline/大纲域设计.md
```

期望 **1**；同法：规格与资料步的 `../../standards/安全与合规标准.md` 期望 **1**；打磨定稿步的 `../../specs/施工机制.md` 期望 **2**。**预验基线**：全 0（目录未建）。

### 断言 10｜旧形态清零（活件面；**自排除本规格**）

```bash
grep -rc "(docs/product/大纲域设计\|(docs/product/卷纲域设计\|(docs/product/正文域设计" --include="*.md" . | grep -v ":0" | grep -v "^\.tmp" | grep -v "080-2026-09-14-产品层按域分层"; echo "扫毕"
```

期望：仅「扫毕」（无输出行——旧仓根形态清零；**自排除**＝本规格断言命令文本含旧形态字样，预验实测命中自身 1 行，不排除则本断言恒假——此即 078 §八 第 5 条的实撞处置）。

```bash
grep -rn "](大纲域设计" docs/product/*.md; echo "扫毕2"
```

期望：仅「扫毕2」（顶层五件不再有指向移动件的裸形链接——覆盖书目录契约 5 条与验收场景 1 条的旧形态）。

```bash
grep -rn "](\.\./standards/\|](\.\./specs/" docs/product/outline docs/product/volume docs/product/write; echo "扫毕3"
```

期望：仅「扫毕3」（子文件夹件内不再有单层 `../` 跨层链接）。**预验基线**：第一项现行命中 README 1 行＋本规格 1 行；第二项现行命中 **30 行**（书目录契约 5＋验收场景 1＋移动件互链 24——后者现居顶层被 glob 命中，迁移后随件离顶层不再命中）；第三项现行命中 **7 行**（须加深的 4＋留顶层**正确保留**的 3：书目录契约 `:22`／`:41`、验收场景 `:20`——单层 `../` 对顶层件是对的）。窄域重审 R4／R5 修。

### 断言 11｜两件纯移动件逐字节不变

```bash
md5sum docs/product/outline/大纲域设计-草稿步.md docs/product/outline/大纲域设计-附录.md
```

期望 `3d65aceded9a593ce49a3903d5fe9fcb`／`336ec6d7da83d65496e33d60b687e00d`（§一⑤ 冻结值逐一相同）。**预验记录**：冻结值即迁移前实测。

### 断言 12｜F3 落位（成稿审查 P2-6 补）

```bash
grep -c "outline/" docs/standards/命名标准.md
```

期望 **3**（`:87` 表例 `references/outline/outline-workflow.md` **既有 1 处**非 F3 触点＋目录树 `outline/` 子行 1＋`:57` 表行 1）；`grep -c "docs/product/{域}/{主题}.md" docs/standards/命名标准.md` 期望 **1**（`:74` 口径句）。**预验基线 1／0**（首值即 `:87` 既有命中——初稿误记 0，窄域重审 R3 修）。

### 断言 13｜改动面与归档零改动

```bash
git status --porcelain -- docs/specs/archive | wc -l
```

期望 **0**（**归档零改动**——070 那条链接因目标留顶层而天然不断）。

```bash
git status --porcelain . ':!docs/product' ':!README.md' ':!docs/standards/命名标准.md' ':!docs/standards/测试与验收标准.md' ':!docs/standards/触发与交互标准.md' ':!CHANGELOG.md' ':!docs/specs/080-2026-09-14-产品层按域分层.md' | wc -l
```

期望 **0**（补集式——排除集含两份改链接的标准件，成稿审查 P1-1 修）。改动面清单预期：`docs/product/`（7 删＋7 未跟踪＋书目录契约与验收场景 2 改）＋README＋命名标准＋测试与验收标准＋触发与交互标准＋CHANGELOG＋080 规格自身，逐行列入核验记录。**预验基线**：仅 080 规格未跟踪。

### 断言 14｜门禁

```bash
bash scripts/check.sh
```

期望 exit **0**（不设件数期望）。

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**（不设件数期望——扫描面含在制规格，件数随批内状态浮动，成稿审查 P2-4 修）。**预验基线 0／0 issue**。

### 断言 15｜过程产物两项

```bash
grep -cF '产品层按域分层（080）' CHANGELOG.md
```

期望 ≥1；`python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '产品层按域分层（080）' in l][0].rstrip()))"` 期望 **≤400**（施工后实核项），末尾「核验：」句、无「数字＋行／处／字符」跟在实存路径代码跨度之后。验收通过后本规格移入 `docs/specs/archive/`。

链接判据即断言 4（自带一行式，不再外引）。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 七条出自本批勘察脚本实跑（`.tmp/080/survey.py`）**并经成稿审查独立复核修订**（③ 22→31 条／9→10 件——书目录契约 5 条与跨层 4 条为审查方查出、我独立复跑 `grep -n` 确认；⑦ 10→9 处；② 补 2 条目录链口径）；10 件的链接行号逐行实读；`命名标准` 三处（`:27`／`:57`／`:74`）实读；两 md5 实测。
2. **断言实跑过**：断言 1–3 迁移前侧、断言 4 基线（175‧0，同条命令）、断言 5–10 预验基线（新前缀全 0／旧形态现行命中面逐项实跑）、断言 11 冻结值、断言 12 基线 0／0、断言 13 预验（仅 080 未跟踪）、断言 14 基线——均已实跑。**逐条核断言 token 与目标文本互斥性**：断言 5–9 的新前缀形态即 F2 目标文本 ✓、断言 12 的两 token 即 F3 目标文本 ✓、断言 15 条目名即 F4 目标文本 ✓。**未预验项**＝断言 15 第二命令（改前抛 `IndexError`）与各断言的施工后侧（施工后实核）。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/` 全部（**含 `archive/`——070 那条链接不许改**）、§一④ 所列**九件零改动件**、`docs/product/` 顶层五件的**非链接内容**（书目录契约与验收场景仅按 F2⑤④ 改链接行）、`scripts/`、`skills/`、`.claude-plugin/`（不建）。
- 禁止动作：**任何 git 写命令**（`git mv` 亦禁）；联网、安装依赖、跑分发／安装工具。
- 禁止越权：**不改任何文件名**；不建三个子文件夹之外的任何目录；F2 不得多改一条链接（31 条外的同行链接不动）；不改 `验收场景:15` 的同目录形链接。
- 禁止改断言、改数字、删检查来变绿；迁移脚本的「替换次数恰为 1」断言失败时**中止报错**，不得改清单迁就。

## 六、被否决的替代方案

- **用 `git mv` 迁移** → 否决：属 git 写操作（红线 1——须作者单点确认），施工分身无权；文件系统 `mv` ＋ 提交时暂存即承载 rename 语义。
- **文件名加域前缀**（如 `outline-大纲域设计.md`）→ 否决：目录已表达域归属，前缀即双载体；且裸件名引用会大面积碎。
- **书目录契约放 `outline/`** → 否决（作者裁定留顶层）：**17** 条入链仅 3 条来自大纲域件（成稿审查逐源复核数；初稿误记 16），卷纲／正文／验收场景／架构设计／产品定义与五份标准件均链它。
- **顺带清「`docs/product/总纲.md`」引用** → 否决：9 处逐处带「`main` 存档」限定词＝沿革引用，无活指针（§一⑦），无可清之物。
- **建域时再做本批** → 否决：目录先定，作者的产品层打磨直接落新位，免二次搬迁。
- **F2 手改 31 处** → 否决（采纳成稿审查建议）：一次性脚本逐条精确替换＋次数断言＝1＋diff 表（口径要点⑥），把「同行多改／漏改」的最易错面交给脚本（`AGENTS.md` §五.1）。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批规格（078）的全部开放项**（必列面 ①：§八 全 **5** 条——初稿误记 4 条并漏列第 5 条，成稿审查 P0-3 查出，同族第三发）＋**本批改动直接触及或可能变状态者**（必列面 ②）。判据＝同节交集；**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 078 §八 1 | `check.sh` 段序真源的六处漂移风险 | 仍待 | 本批不动 `check.sh` |
| 078 §八 2 | `shell=True` 跨宿主可复现 | 仍待 | 本批不动 `check_spec.py` |
| 078 §八 3 | `check_spec.py` 自带测试件 | 仍待 | 触发＝「工具判据回归无人发现」实例 |
| 078 §八 4 | `060` 陈旧计数声称（永久候选） | 仍待 | 本批不动 `check.sh` 行数与归档件 |
| 078 §八 5 | 规格自身写「字面链接样例」污染链接判据 | **本批处置（当次）** | **触发已到**：本规格断言 10 初稿即实撞——断言命令文本含旧形态字样、**被断言 10 自身扫法命中**（现行链接一行式经实证**不**计该行——命令行无链接形态；窄域重审 R6 勘正）→ 当次处置＝断言 10 **自排除**；「一行式加跨度／围栏感知或补写作律」仍待（触发＝再踩一次时） |

**汇总**：共 **5 行／按条 5**——**本批处置 1**（当次自排除；机制化仍待）｜已履行 0｜仍待 **4**｜作废 0｜待作者 0 ＝ 5 ✓（「汇总＝行数」恒成立）。**范围注**：079 为轻路径微调、无规格，其处置见 CHANGELOG 行；本批改动与 075–077 各批开放项**无同节交集**，以其各自核销表为索引。

## 八、开放项

1. **域子文件夹名与 skill 域名的对应**（`outline` ↔ `abzu-outline` 等）：现由目录树注释表达，未立映射表。触发＝建域时若出现命名歧义再评估。
2. **工作树中间态的 rename 可见性**：施工后、提交前 `git status` 呈「D＋??」而非 `R`——**预期行为**，提交时由规划方按作者确认统一暂存；立此防误报。
3. **链接一行式的跨度／围栏感知**（078 §八 5 机制化部分顺延）：本批以断言自排除应对当次；触发＝再踩一次时，评估一行式加感知或补写作律。
4. **顺手清单顺延**：`_dir_split` docstring 首句（下次触碰 `check_spec.py`）、术语标准 §二 补正例（下次触碰该件）。
5. **大纲四分件 H1 与文件名的分隔符不一致**（产物审查 F-2，批外既有）：H1 用「·」、文件名用「-」（063 批创建即如此）；现行无「H1＝文件名」规。触发＝建域前统一裁定（改 4 个 H1 或在标准记形）。
6. **「断言命令含被扫字面须自排除」的写作律**（产物审查建议，接 078 §八 5 机制化部分）：本批已是第二次逼近。触发＝下次触碰 `规格写作标准` §5 时补一条（断言命令含被扫旧形态字样时须自排除或变量拼接）。

## 九、审查记录

- 2026-09-14｜**成稿审查**（fresh 只读分身）｜findings 共 **13**：**P0:3** P1:2 P2:6｜结论 **不通过**（按止损条 2，修订后**窄域重审 P0 修复面**）。处置（本条落本规格，长文不入 CHANGELOG）：
  - **P0-1（F2 漏 9 条链接）**：初稿勘察的普查脚本只查「指向 `docs/product/` 的入链」，漏「product 内互链（顶层件→移动件）」与「移动件→`../standards/`、`../specs/` 跨层出链」两个方向 → 审查方查出**书目录契约 5 条**（`:49`–`:53`）与**跨层加深 4 条**，我独立复跑 `grep -n` 确认 → **F2 扩为 31 条／10 件**（九类），§一③ 重写（入链 20＋出链 11），断言 7／9 补书目录契约与跨层计数，断言 10 第二／三项扩扫法。
  - **P0-2（断言 2 核验态崩溃）**：一行式用 `git ls-files` 列文件——mv 后索引仍列旧路径、磁盘无此件 → `io.open` 抛 `FileNotFoundError`（审查方沙盘实证）→ 断言 4 改为**按磁盘存在性过滤**（`os.path.exists`），基线与核验同一条命令。
  - **P0-3（核销表漏行，同族第三发）**：078 §八 实有 **5** 条，初稿凭记忆写「全 4 条」并漏第 5 条（「字面链接样例污染判据」）→ **补第 5 行**；且本批断言 10 初稿即实撞该条——状态定「**本批处置（当次）**」（自排除），机制化部分顺延 §八 3。**教训**：核销面① 的条数今后一律 `grep -c "^[0-9]+\. "` 实数，不凭记忆。
  - **P1-1（断言补集排除集漏两标准件→恒假）**：排除集补 `测试与验收标准`／`触发与交互标准`（断言 13 第二项）。**P1-2（§一③ 分项标签错）**：入链／出链分项重标（20／11）。
  - **P2 六条**：16→**17** 入链（§六）；10→**9** 处（§一⑦）；`命名标准` 补**第三处** `:74`（F3）；断言 14 删「48 件」虚期望；断言 1／3 违 `规格写作标准` §5.9 → **全部拆为逐命令一期望**（断言 1–15 重排）；F3 补断言 12。
  - **审查方独立判断（已采纳两条）**：① F2 执行判据由「件：行」改**唯一字符串形态**（作者在打磨产品件，行号会漂移）；② 施工形态改**一次性迁移脚本**（逐条替换断言次数＝1＋diff 表，口径要点⑥）。另：「修订后**立即施工**」——行号快照会随作者打磨漂移。
- 2026-09-14｜**窄域重审**（fresh 只读分身；止损条 2——只核 P0 修复面＋直接影响面）｜findings 共 **6**：**P0:1** P1:2 P2:3｜结论 **不通过（机械修订后可进施工——审查方明示「修订后无需再全审」）**。它**确认**了 P0-1（独立全向普查：入链 20＋出链 11 逐条逐行号吻合、跨子文件夹互链＝0、引用式链接定义全仓 0 条无盲区）与 P0-2（沙盘证明过滤版一行式在 mv 态输出 175／0，并给出链级对账：旧路径 7 件携 41 条不计／新路径 7 件携 41 条计入，差恰为 0）；P0-3 行数修复成立。抓出的 **R1（P0）系修订版自引**：我写「每条旧形在其件内唯一，逐条验过」却**未验**——5 形实为 2–4 次（F2 自身即写着 ×2／×4，自相矛盾），按该口径施工脚本会在第③类中止。处置（全部照准，机械修订）：
  - **R1**：F2 判据句改「实测次数」（五形 4／2／2／2／2、余 19 形各 1）；口径⑥ 改「按形断言期望次数」并注明初稿口径已被推翻、不得回退。
  - **R2**：断言 5 三命令改 `grep -o | wc -l` 计出现次数（`grep -c` 计行，README 7 条同行会恒得 1——期望 5 恒假）。
  - **R3**：断言 12 基线订正 0→**1**（`:87` 表例 `references/outline/outline-workflow.md` 既有命中，非 F3 触点）、期望 ≥4→**3**。
  - **R4／R5**：断言 10 扫 2／扫 3 预验基线订正（6→**30** 行：含移动件互链 24 行迁移后自然不再命中；4→**7** 行：含留顶层**正确保留**的 3 条单层 `../`）。
  - **R6**：§七 第 5 行理由句勘正——自污染是「断言 10 自身扫法命中」而非「链接一行式计假断链」（现行一行式经实证不计该行）。
  - **审查方一句判断**：31 条清单本身完备、P0-1／P0-2 修复成立；修掉 R1 与 R2／R3 两个恒假期望（均机械、不动清单）即可进施工，无需再全审。
- 2026-09-14｜**规划方核验（三步）**｜**通过**。① 改动面：恰 18 行 porcelain（6 M＋7 D＋3 新目录未跟踪＋080 规格自身），与断言 13 预期清单逐行一致；归档 `git status --porcelain -- docs/specs/archive` ＝ 0。② 断言独立重跑：断言 4＝**175／0**（过滤版在 mv 态跑通）、断言 10 三扫全 0、断言 11 两 md5 逐一相同、断言 5＝5/1/1、断言 7＝5、断言 13＝0/0。③ 内容抽查：README:23 七条新前缀链接、书目录契约 :49–:53、验收场景 :15 保留／:21 改写、移动件 `../` 与 `../../` 落点逐条相符；两纯移动件 diff 无输出（逐字节相同）。
- 2026-09-14｜**双维产物审查**（fresh 只读；结构类批次 → `施工机制` §五 双维）｜findings 共 **2**：P0:0 P1:0 **P2:2（均不属本批缺陷）**｜结论 **通过**。审查方**独立重跑断言 1–15 全过**（自建全向普查：总链 177−2 外链＝175 与断言 4 对账；入链 20＋出链 11 逐条枚举与 §一③ 一致；互链 30 次／24 行未动、残留旧形态 0、README 同行 5 条与三处单层 `../` 与 HEAD 逐字相同）；**内容完整性**以 `git show HEAD:旧路径` 对新路径逐文件 diff——5 个改写移动件差异全为链接行、2 纯移动件逐字节相同；**反向构造两项**（去过滤即崩 `FileNotFoundError`；内存回退 1 条入链即三处断言齐红）证断言网闭合；B 维：全仓旧全路径文本引用活件面 0（仅存归档历史记录）、`命名标准` 三处各司其职无第二载体、gitignore／lint 实测无需跟随、`[4]` 段 3 条预警逐条判「属预期」。处置：
  - **F-1（P2）**：`check_spec.py` [4] 段对断言表散文产出 6 条「跳过」解析残片（候选类不阻断）→ 顺延至 078 §八 3（工具自带测试／判据改进）既有开放项，本批不动 `scripts/`。
  - **F-2（P2，批外既有）**：大纲四分件 H1 用「·」而文件名用「-」（063 批创建即如此、本批逐字节未动；现行无「H1＝文件名」规）→ 记入本规格 §八 开放项，建域前裁定。
  - **审查方独立判断**：迁移**值**（时机正确——产品层将进入逐域打磨期，此刻分层免二次搬迁；书目录契约留顶层避开了全仓最高入链件的风险面；断言 4 的磁盘过滤把链接一行式升级为「mv 未提交态可用」，此后结构类批次受益）。**最该改的一处**：把 080 断言 10 的「自排除」机制化——本批已是「断言命令含被扫字面」的第二次逼近，建议下次触碰 `规格写作标准` 时补一句写作律（§八 5 已挂，触发已半 armed）。
