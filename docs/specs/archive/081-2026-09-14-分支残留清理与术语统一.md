# 081-2026-09-14-分支残留清理与术语统一

> 定位：**两合一**（作者裁定 2026-09-14）——① 清 `main` 残留 **19 处／13 件**（四类：**件级依据 12**／**逐条〔出处〕5**／工具头注 1／**活规则 1**——第 4 类非旧仓残留而是现行规则写了 `main`，改「直推**当前工作分支**」）；② 术语统一 **60 处／8 件**：「分身」51→`subagent`、「fresh」7→「独立上下文」、「子代理」2→`subagent`，`术语标准` 的「子代理」「分身」两条词条**合并为一条 `subagent`**。替换形态须守 `文档写作标准` **§一.2 第 1 条**（中英文之间加空格；中文之间**不加**空格）。归档件（`分身` **144** 处／`fresh` 55 处）与 `CHANGELOG` 历史行**一律不动**。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径**——跨 **17 件**（15 件 `.md` ＋ `scripts/check_spec.py` ＋ `CHANGELOG`）、动 `术语标准` 条文；不命中「重」行。**按 `施工机制` §五 走双维产物审查**（动标准条文）。scope：**标准件＝standards**、**产品层＝product**、**治理文档＝governance**、**开发守卫＝scripts**。
> 自由度分布：[A]×0 [B]×8
> 必读输入：本规格 §一；`docs/standards/文档写作标准.md` §一.2 第 1 条（排版规则，§一④ 引全）；`docs/standards/术语标准.md` §一 两条词条（§一⑤ 引逐字）与 §4／§三 锚点；`docs/specs/施工机制.md:150`（活规则）与 `:168`（§八 模板内围栏占位）
> 施工形态：分身（规划方 spawn 的施工分身；无 git 写权限）

## 一、现状锚点（勘察实测，2026-09-14；经成稿审查复核修订）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `main` 残留 **19 处／13 件**，四类 | **件级依据 12 处**＝**11 个 `:4` 头注行**（大纲五件（「迁移自 `main` 分支存档《大纲阶段设计文档》v0.9.6…」）＋产品定义 `:4`／架构设计 `:4`（「条文摘要自 `main` 存档 `docs/product/总纲.md` §三／§四」）＋安全与合规 `:4`／术语标准 `:4`／触发与交互 `:4`／运行环境 `:4`，均「`main` 存档 `X`」形态）**＋ `产品定义:50` 节内依据句**（§七「路线」内「摘要自 `main` 存档…」，非 `:4` 行）。**逐条〔出处〕5 处**：安全与合规 `:16`／`:17`、运行环境 `:11`／`:12`／`:14`（均带「〔出处：…〕」前缀）。**工具头注 1**：`check_spec.py:49`。**活规则 1**：`施工机制.md:150`。**13 件**＝12 `.md`（大纲五件＋产品定义／架构设计／安全与合规／术语标准／触发与交互／运行环境／施工机制）＋`scripts/check_spec.py` ✓（成稿审查订正：初稿误记「9 件」与类目 8／7，实为 12／5） |
| ② | 术语 **60 处／8 件** | 「分身」51／「fresh」7／「子代理」2。**逐件（行数/处数）**：`施工机制` 19/27、`大纲域设计-打磨定稿步` 7/12、`大纲域设计` 3/3、`术语标准` 2/5、`术语表` 1/1、`架构设计` 1/1、`大纲域设计-规格与资料步` 1/1、`skill形态标准` 1/1 → 「分身」合计 35 行/51 处；另 `fresh`＋`子代理` 8 行/9 处（`施工机制` 3、`打磨定稿步` 3、`术语标准` 1、`架构设计` 1）✓ |
| ③ | 归档件与 `CHANGELOG` 历史行不动 | 归档件含「分身」**144 处**（另有 `fresh` 55 处、`子代理` 1 处——**199 是这三者中两者的合计，非「分身」单计**，窄域重审 P1-1 勘正）、`CHANGELOG` 含「分身」**1 处**（历史行）——零改动；本批**新增**的 CHANGELOG 行用新术语并**承载旧称对照**（见 F4）✓ |
| ④ | 排版规则（替换形态的硬约束） | `文档写作标准` **§一.2 第 1 条**（原文）：「中英文之间加空格（如：在 Claude Code 中运行）；中文与数字之间加空格（如：第 3 章）；**行内代码与全角标点邻接不加空格**（如：运行 `npm install`）。」第 2 条：「全角标点与其他字符之间不加空格。」→ 替换须在 `subagent` 与相邻 CJK 之间**补空格**；替换为**中文词**（独立上下文）时与左侧 CJK 之间**删空格**；与全角标点／行内代码邻接不补 ✓ |
| ⑤ | 术语标准两条词条逐字 | 「\| 子代理 \| 宿主中由主会话派生的 AI 分身（独立上下文）。**两种用途**：工序角色（施工分身／独立审查分身／产物审查分身，现行默认工序）与实体 agent（运行时物化，待 T1 触发，触发档位定义见 `docs/product/架构设计.md` §四）；区别于浏览器自动化 CLI 这类工具 \| subagent \|」／「\| 分身 \| 工序角色轨的**单称**——由规划方在会话内 spawn、无物化文件、不进用户书项目 \| process-role subagent \|」——新词条须**保住这四要素**（单称／会话内 spawn／无物化文件不进用户书项目／英文对应）✓ |
| ⑥ | 基线 | 链接一行式 **175／断链 0**；`bash scripts/check.sh` exit **0**；markdownlint **0 issue**；`规格写作标准` §5 现 **10** 条；`check_spec.py` 的 `__main__` 计数 **2**（`:45` 头注散文 ＋ `:1199` 语句；**`main()` 全仓不存在**）；活件面既有 `subagent` **5 处**（`术语标准` 2＋`大纲域设计-规格与资料步` 2＋`大纲域设计-附录` 1）✓ |
| ⑦ | 旧词不涉脚本与运行资产 | 全仓 `.py`／`.sh` 内无「分身」「fresh」「子代理」；`check_spec.py` 仅类③那 1 处 `main:` 形态（`__main__` 与 `if __name__` 系代码标识符，**不得**触碰）✓ |
| ⑧ | 围栏内的模板占位 | `施工机制.md:168` 的 §八 规格模板含 `> 施工形态：{分身／外部窗口}（{模型标识}）`——**在围栏内**，亦属替换面（否则断言 3a 命中）✓ |

**不变量（施工前后须同）**：链接一行式 **175／断链 0**；`docs/specs/archive/`、`CHANGELOG` 历史行、`.py`／`.sh` 代码标识符（`__main__` 等）**零改动**；替换后活件面「分身」「fresh」「子代理」在 **8 件扫描面内归零**（本规格自身不在扫描面——断言用**路径白名单枚举**）；`bash scripts/check.sh` exit **0**；lint **0 issue**。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | **13 件**（§一① 全列） | **`main` 残留清理（19 处，三类落法）**：**落法①（件级依据／出处，17 处）**——「迁移自 `main` 分支存档《…》」→「迁移自**存档**《…》」、「…摘要自 `main` 存档 `docs/product/总纲.md`」→「…摘要自**存档** `docs/product/总纲.md`」、「〔出处：`main` 存档 `X`…〕」→「〔出处：**存档** `X`…〕」（只摘分支指称、保留「存档」）；**落法②（`check_spec.py:49`）**——「由旧仓 `main:scripts/check_spec_assertions.py`」→「由旧仓 `scripts/check_spec_assertions.py`」；**落法③（`施工机制:150` 活规则）**——「直推 main（作者明确要求除外）」→「直推**当前工作分支**（作者明确要求除外）」 | ① | [B] |
| F2 | **8 件**（§一②） | **术语替换 60 处**：`分身`→`subagent`、`子代理`→`subagent`、`fresh`→`独立上下文`。**形态三条**：① `subagent` 与相邻 CJK 之间**补空格**（`施工分身`→`施工 subagent`、`分身纪律`→`subagent 纪律`）；② 替换为**中文词**时与左侧 CJK 之间**删空格**（`两个 fresh 分身位`→`两个独立上下文 subagent 位`——**不得**留「两个 独立上下文」）；③ **词组 `fresh context` 整体替换为「独立上下文」**（`打磨定稿步:32` 源文「（fresh context 各跑一维度，防同视角偏袒）」→「（独立上下文各跑一维度，防同视角偏袒）」，**删去 `context`**）；与全角标点／行内代码邻接不补。**点名两处边界**（成稿审查 P2-2）：`skill形态标准:90`「交接、子分身派发」→不得写「子 subagent」（改「（交接、**下派 subagent**）」形态）；`架构设计:54` 一句内「hooks／子代理…评审分身」两词同归 `subagent` → 前半作「hooks／subagent 进书项目」、后半保留「评审 subagent」。**点名第三组（窄域重审 P0，同 P0-3 族）**：`施工机制:92`「单维核对由独立的 fresh 只读分身执行」→「单维核对由**独立上下文的只读 subagent** 执行」、`:95`「必须是两个独立 fresh 分身」→「必须是**两个独立上下文 subagent**」——**不得**产出「独立的独立上下文…」叠字形态（机械落字即此，施工方无权重写、故须点名）。**不得**借机改写叙述句 | ②④ | [B] |
| F3 | `docs/standards/术语标准.md`（§一 词表两条） | **两条词条合并为一条**（**首格纯文本**不加反引号、**列分隔用半角竖线**，与该表同体例）：`subagent` \| 宿主中由主会话派生的 AI 执行体（**独立上下文**＝全新会话、不复用施工期记忆）；**工序角色轨的单称**——由规划方在**会话内 spawn**、**无物化文件、不进用户书项目**（英文对应另有 `process-role subagent`）。**两种用途**：工序角色（施工／独立审查／产物审查，现行默认工序）与实体 agent（运行时物化，待 T1 触发，触发档位定义见 `docs/product/架构设计.md` §四）；区别于浏览器自动化 CLI 这类工具。**曾用名与英文对应对照见 `CHANGELOG` 081 条目** \| `subagent`（产物审查 P2-1：「081 行」易被读成行号且随条目增删漂移，改「条目」；P2-5：`独立上下文` 的英文对应 `fresh context` 在此处一并留痕，免其只存活件与归档）（**逐字落盘时用 `\|` 转义字面竖线**——与 §一⑤ 引旧词条同法；写成全角竖线或加反引号会使断言 5b 恒红，窄域重审 P1-3）。**旧称字面不得进本件**（否则归零断言恒红——成稿审查 P0-1） | ⑤ | [B] |
| F4 | `CHANGELOG.md`（`## [Unreleased]` → `### Changed` 段首） | 一行：『分支残留清理与术语统一（081）：…』——19 处 `main` 残留（13 件）＋60 处术语替换（8 件，**写出旧称与英文对应对照**：分身／fresh／子代理 → subagent／独立上下文，并含字面 `fresh context`——`术语标准` 新词条指向本条目称「曾用名与英文对应对照见…」，故本条目**须真的含**该字面，否则标准件过实）＋`术语标准` 词条合并 ＋ 核验结论；**≤400 字符**、末尾「核验：」句。**字符预算紧**（现存最长 399——**075** 条；078 条为 397），优先保「19／60／词条合并／核验」四要素 | 过程产物 | [B] |
| F5 | `docs/standards/术语标准.md`（§二 正例与反例） | **补一条正例**（080 §八 4 顺延，触发已到）：在该节**反例之前**增 `- ✅ 术语用平台标准词并随行中文解释（如：`subagent`（独立上下文）、`skill 资产`（skill asset））`——**正例在前**、标记用 `✅`（与该族标准件 12 处体例一致）；**示例第二项须用既有词条 `skill 资产`**（产物审查 P1-1：初稿写「技能资产」，是给既有术语新造第二种叫法，与本批主题相抵） | 080 §八 4 | [B] |
| F6 | `docs/standards/规格写作标准.md`（§5，追加为第 11 条） | **新条「扫描类断言须排除本规格」**：逐字目标文本——「11. **扫描类断言须排除本规格**：以 `grep -r`／`grep -rc` 扫全仓的断言，若其**命令文本自身**含被扫字面（如替换表里的旧词），须**按路径白名单枚举扫描面**（只列活件面所属路径，归档与本规格天然不在面内）；次选在管道内按**裸文件名子串**排除本规格（如 `grep -v "081-…"`）——**禁用首路径前缀式**（`^docs/` 之类形态敏感，反斜杠环境静默失效）。否则该断言恒假——本规格既是被扫面又是扫描者。**过滤归档件不得依赖首路径前缀格式**（部分环境输出反斜杠路径，前缀式过滤会静默失效）。实例：080 断言 10、081 断言 1／3a／4c。」 | 080 §八 6；本批实撞 | [B] |
| F7 | `scripts/check_spec.py`（`_dir_split()` docstring 首句） | **顺延项落地**（080 §八 4 触发已到——本批 F1 即触碰 `check_spec.py`）：docstring 首句「按**方向标记**切分单元格」→「**检测**单元格内的**方向标记**（本函数不做切分）」，其余原样 | 080 §八 4 | [B] |
| F8 | `docs/product/术语表.md`（表头或 §一 首行） | **指向行收口**（产品层词条的落点链）：把**依据行**（`:4`）尾句「维护纪律与治理／资产类词条见该标准」扩写为「…维护纪律与治理／资产类词条（`subagent`／`skill 资产`／仓库治理文档等）见 `docs/standards/术语标准.md` §一.3」——**不新增行**（与该行既有指向句合并＝单一载体，产物审查 P2-3）；**锚点带子条号**（词条表在 §一.3，P2-4）；**不得**留「CJK＋半角空格＋CJK」形态（P2-2） | 成稿审查独立判断③ | [B] |

**口径要点（易错）**：① 替换**按词计数**（脚本对每件的每词先 `count` 断言、替换后再断言归零）；② 中英混排空格是硬约束（§一④），**不得**留「施工subagent」形态、**也不得**留「两个 独立上下文」这类中文间空格；③ **不得**触碰 `.py`／`.sh` 的 `__main__` 等代码标识符；④ **不得**改归档件与 `CHANGELOG` 历史行；⑤ 替换面**含围栏内的模板占位**（`施工机制:168` 的 `{分身／外部窗口}`，§一⑧）与引用块、表格单元格；⑥ 本规格自身含替换表与旧词，**不进**任何归零断言——断言用**路径白名单枚举**（只列活件面路径），不得用「管道自排除」（本机 ugrep 输出反斜杠路径，前缀式过滤会失效）；⑦ F3 的旧称**只落 `CHANGELOG` 081 行**（该件不在任何归零断言的扫描面）；⑧ F5／F7／F8 属 080 §八 顺延项的**触发已到**，形态逐字照 F 行；⑨ **施工前规划方现场重跑三基线**（`main` 19 行／术语 35 行＋8 行）——作者正在打磨产品层件，基线漂移时按偏差申报处理，**不得**改清单迁就。

## 三、验收断言（含预验记录；**一条断言一命令一期望**）

### 断言 1｜`main` 残留归零（路径白名单枚举）

```bash
grep -rn "存档 \`main\`\|\`main\` 分支\|直推 main\|\`main:\|main\` 存档" docs/product docs/standards scripts README.md AGENTS.md docs/specs/施工机制.md; echo "扫毕"
```

期望：仅「扫毕」（19 行全清）。**预验基线 19 行**（诊断：初测 43 行＝19 真命中 ＋ 24 行归档件漏滤，成因是前缀式过滤在本机 ugrep 的反斜杠路径上失效）。

### 断言 2｜活规则落位

```bash
grep -c "直推当前工作分支" docs/specs/施工机制.md
```

期望 **1**。**预验基线 0**。

### 断言 3a｜「分身」归零（8 件扫描面）

```bash
grep -rc "分身" docs/specs/施工机制.md docs/standards/术语标准.md docs/standards/skill形态标准.md docs/product/术语表.md docs/product/架构设计.md docs/product/outline/大纲域设计.md docs/product/outline/大纲域设计-规格与资料步.md docs/product/outline/大纲域设计-打磨定稿步.md | grep -v ":0$"; echo "扫毕"
```

期望：仅「扫毕」。**预验基线（行数/处数）**：施工机制 **19/27**、打磨定稿步 **7/12**、大纲域设计 **3/3**、术语标准 **2/5**、术语表／架构设计／规格与资料步／skill形态标准 各 **1/1**（合计 **35 行／51 处**）。

### 断言 3b｜「fresh」「子代理」归零

```bash
grep -rc "fresh\|子代理" docs/specs/施工机制.md docs/standards/术语标准.md docs/product/架构设计.md docs/product/outline/大纲域设计-打磨定稿步.md | grep -v ":0$"; echo "扫毕2"
```

期望：仅「扫毕2」。**预验基线**：施工机制 **3**、打磨定稿步 **3**、术语标准 **1**、架构设计 **1**（合计 **8 行／9 处**）。

### 断言 4a｜新词落位（三件）

```bash
grep -rc "subagent" docs/specs/施工机制.md docs/product/outline/大纲域设计-打磨定稿步.md docs/standards/术语标准.md | grep -v ":0$"
```

期望三件均 ≥1。**预验基线 0／0／2**（活件面既有 `subagent` 共 **5 处**：`术语标准` 2＋`大纲域设计-规格与资料步` 2＋`大纲域设计-附录` 1——`术语标准` 那 2 处随 F3 词条合并重排、净增由断言 3a／3b 的归零锁定）。

### 断言 4b｜空格形态（补空格方向）

```bash
grep -rc "施工 subagent\|独立上下文 subagent\|subagent 纪律\|独立上下文各跑一维度" docs/specs/施工机制.md docs/product/outline/大纲域设计-打磨定稿步.md | grep -v ":0$"
```

期望 ≥1 行命中。**预验基线 0**。

### 断言 4c｜空格形态（**无空格方向**归零——硬约束的网）

```bash
grep -rn "[一-龥]subagent\|subagent[一-龥]" docs/specs/施工机制.md docs/product/outline/大纲域设计-打磨定稿步.md docs/product/outline/大纲域设计.md docs/product/outline/大纲域设计-规格与资料步.md docs/product/术语表.md docs/product/架构设计.md docs/standards/术语标准.md docs/standards/skill形态标准.md | grep -v ":0$"; echo "扫毕4"
```

期望：仅「扫毕4」（该式**不误伤**「（subagent」「subagent，」等全角标点／行内代码邻接形态——全角标点不在 `一-龥` 内，与 §一④ 边界精确吻合）。**预验基线 0**。

### 断言 4d｜叠字形态归零（窄域重审 P0 的网）

```bash
grep -rn "独立独立\|独立的独立" docs/specs/施工机制.md docs/product/outline/大纲域设计-打磨定稿步.md docs/standards/术语标准.md docs/standards/skill形态标准.md docs/product/术语表.md docs/product/架构设计.md docs/product/outline/大纲域设计.md docs/product/outline/大纲域设计-规格与资料步.md; echo "扫毕5"
```

期望：仅「扫毕5」（替换若落成「独立的独立上下文…」即命中）。**预验基线 0**。

### 断言 5｜旧词条行归零

```bash
grep -c "^| 子代理 \|^| 分身 " docs/standards/术语标准.md
```

期望 **0**。**预验基线 2**。

### 断言 5b｜新词条行恰 1

```bash
grep -c "^| subagent " docs/standards/术语标准.md
```

期望 **1**（**行首半角竖线 ＋ 半角空格**——F3 的单元格不得带反引号，否则本断言恒红）。**预验基线 0**。

### 断言 5c｜F5 正例落位

```bash
grep -c "^- ✅ 术语用平台标准词" docs/standards/术语标准.md
```

期望 **1**。**预验基线 0**。

### 断言 6a｜归档零改动

```bash
git status --porcelain -- docs/specs/archive | wc -l
```

期望 **0**。**预验基线 0**。

### 断言 6b｜`CHANGELOG` 只增行不改行

```bash
git diff HEAD --numstat -- CHANGELOG.md | awk '{s+=$2} END {print s+0}'
```

期望 **0**（`--numstat` 的**删除列**求和；用 `diff HEAD` 覆盖「已暂存 ＋ 未暂存」两态——`git diff` 单用时不带 `HEAD`，暂存后恒空、假绿，窄域重审 P2-1 加固）。**预验基线 0**（改前无 diff）。

### 断言 7a｜链接不变

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**。**预验基线 175‧0**。

### 断言 7b｜代码标识符未受牵连

```bash
grep -c "__main__" scripts/check_spec.py
```

期望 **2**（`:45` 头注散文 ＋ `:1199` 语句）。**预验基线 2**。

### 断言 8｜门禁

```bash
bash scripts/check.sh
```

期望 exit **0**。**预验基线 0**。

### 断言 9｜lint

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**。**预验基线 0 issue**。

### 断言 10｜F6 落位

```bash
grep -c "扫描类断言须排除本规格" docs/standards/规格写作标准.md
```

期望 **1**（§5 由 10 条增为 11 条）。**预验基线 0**。

### 断言 11｜F7 落位

```bash
grep -c "本函数不做切分" scripts/check_spec.py
```

期望 **1**。**预验基线 0**。

### 断言 12｜F8 落位

```bash
grep -c "见 \`docs/standards/术语标准.md\` §一.3" docs/product/术语表.md
```

期望 **1**。**预验基线 0**。

### 断言 13｜过程产物（条目名）

```bash
grep -cF '分支残留清理与术语统一（081）' CHANGELOG.md
```

期望 ≥1。**预验基线 0**。

### 断言 14｜过程产物（行长）

```bash
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '分支残留清理与术语统一（081）' in l][0].rstrip()))"
```

期望 **≤400**（施工后实核项——改前抛 `IndexError`），末尾「核验：」句、无「数字＋行／处／字符」跟在实存路径代码跨度之后。验收通过后本规格移入 `docs/specs/archive/`。

链接判据即断言 7a（自带一行式，不再外引）。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 八条出自本批勘察实跑**并经成稿审查复核修订**（① 件数 9→**13**、类目 8／7→**12／5**；② 8 件；③ 归档 199＋CHANGELOG 1；④ 节号 **§一.2 第 1 条**——初稿误记 §一.3，实跑核过该节是「术语一致／示例优先」；⑥ `__main__` **2**——初稿误记 1、活件面 `subagent` **5** 处——初稿称仅 2；⑧ 围栏占位）。`术语标准` 两条词条逐字实读 ✓。
2. **断言实跑过**：断言 1–14 的**改前侧全部实跑**（19 行／0／35 行／8 行／0-0-2／0／0／2／0／0／0／0／175‧0／2／exit 0／0 issue／0／0／0／0／0；断言 14 改前 `IndexError` 属**未预验项**）；**逐条核断言 token 与目标文本互斥性**：断言 2／10／11／12 的 token 即 F1③／F6／F7／F8 目标文本 ✓、断言 5b 的行首形态即 F3 规定的体例 ✓、断言 13 条目名即 F4 目标文本 ✓。
3. **禁止事项已写明**：见 §五。**本规格自身的静态自检**：`check.sh` 的 `[4]` 段对本规格报 **4 条「目标文本未提及」候选（与改前同形）＋ 3 条状态迁移为自噬预警**（`扫描类断言须排除本规格`／`本函数不做切分`／`分支残留清理与术语统一（081）`——施工后 token 落盘，原「零命中」项转自噬；候选类不阻断）。其中「目标文本未提及」的一条——（断言 5b 的 token 以 `^| subagent` 起首且**末带一个半角空格**，而 F3 的表格行目标文本按体例用 `\|` 转义，二者字面不同形）→ **已判因、属预期**；不为此把 token 改成不可判形态。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/archive/` **全部**、`CHANGELOG.md` **历史行**（只许段首增一行）、`README.md`、`AGENTS.md`、`.py`／`.sh` 的代码标识符、`skills/`、`.claude-plugin/`、`scripts/check.sh`。
- 禁止动作：任何 git 写命令；联网、安装依赖、跑分发／安装工具。
- 禁止越权：不改文件名、不建文件（F8 只增一行文本）；不动表格列结构；不得留无空格形态或中文间多余空格；不得顺手替换归档件与 `CHANGELOG` 历史行里的旧词；**不得**借机改写产品层叙述句。
- 禁止改断言、改数字、删检查来变绿；替换脚本「按词 count 断言」失败时**中止报错**。

## 六、被否决的替代方案

- **术语保持中文自造词** → 否决（作者裁定）：宿主唯一是 Claude Code，`subagent` 是平台标准词；平台名词本仓本就用英文（skill／hook／commit）。
- **`fresh` 保留原词** → 否决（作者裁定换「独立上下文」）。
- **`main` 引用改为「旧仓」** → 否决（作者裁定取「全删」）：同句已有「旧仓《项目宪法》」等叫法，再加「`main` 存档」是杂音；**保「存档」而删分支指称**。
- **`施工机制:150` 改写死 `rebuild`** → 否决：分支切换即再陈旧；改「当前工作分支」恒定。
- **旧称对照留在 `术语标准` 词条格内** → 否决（成稿审查 P0-1 实证）：词条含旧词字面会使断言 3a／3b **恒红**（自伤），且与 `术语标准` §4「退役术语删除」相抵；改落 `CHANGELOG` 081 行（不在任何归零断言的扫描面）。
- **断言用「管道自排除本规格」** → 否决（成稿审查实证）：本机 ugrep 输出反斜杠路径，前缀式过滤**静默失效**（19 真命中被误计为 43）；改用**路径白名单枚举**，并把该经验写入 F6 新条。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批规格（080）的全部开放项**（必列面 ①：§八 全 **6** 条，`grep -c "^[0-9]\+\. "` 实数）＋**本批改动直接触及或可能变状态者**（必列面 ②）。判据＝同节交集；**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 080 §八 1 | 域子文件夹名与 skill 域名的对应 | 仍待 | 本批不动目录结构 |
| 080 §八 2 | 工作树中间态 rename 可见性（说明性） | 仍待 | 本批无文件移动 |
| 080 §八 3 | 链接一行式的跨度／围栏感知（078 §八 5 机制化部分） | 仍待 | 本批以白名单枚举应对；机制化（一行式加感知）仍未做 |
| 080 §八 4 | 顺手清单顺延（`_dir_split` docstring／术语标准 §二 正例） | **本批处置** | **F7**（docstring 首句，触发已到——F1 触碰该件）＋**F5**（§二 正例）——两项全落 |
| 080 §八 5 | 大纲四分件 H1 与文件名分隔符不一致 | 仍待 | 本批不动产品件正文叙述；触发＝建域前裁定 |
| 080 §八 6 | 「断言命令含被扫字面须排除本规格」的写作律 | **本批处置** | **F6** 落 §5 第 11 条；本批断言 1／3a／4c 即其实例 |
| 078 §八 5 | 规格自身写「字面链接样例」污染链接判据（上批记「机制化仍待」） | **本批处置（机制化）** | **F6** 即该条的机制化落点（写作律）；「一行式加感知」仍待（见 080 §八 3） |

**汇总**：共 **7 行／按条 7**——**本批处置 3**（080 §八 4／6、078 §八 5）｜已履行 0｜仍待 **4**｜作废 0｜待作者 0 ＝ 7 ✓（「汇总＝行数」恒成立；成稿审查 P1-10 补 078 §八 5 行）。

## 八、开放项

1. **链接／文本扫描类断言的机械检出**：本批以「路径白名单枚举」化解（并写入 F6 写作律），但「断言命中规格自身」仍靠人写对。触发＝再踩一次时，评估在工具侧产候选行。
2. **`术语标准` 旧称的归档可读性**：旧称对照只落 `CHANGELOG` 081 行。触发＝出现「按现行术语读归档件」的实际误读时，评估归档件表头加口径注。
3. **产品层术语表与术语标准的分工**：本批以 F8 加一条指向行衔接。触发＝产品层词条增多、两表出现重复条目时，评估合并或明确边界。
4. **顺手清单顺延**：无（080 §八 4 两项本批全落）。
5. **检查 A 的两处工具缺口**（产物审查观察-1，**不属本批**——本批判据面零改动）：① 断言行的**位置格只写件数**（如「13 件」）时，`check_swallow()` 的 hit 门判「目标件不出现在任何改动行」→ 本批 4 条断言的零命中核对**静默跳过**（靠人工实跑兜住）；② token 提取对转义反引号不解反转义（断言 12 的 token 含字面反斜杠）→ 恒产「目标文本未提及」候选。触发＝再遇同类静默跳过时，按 `AGENTS.md` §七「工具报得出的与应当报出的一致」立项（须先走规格）。

## 九、审查记录

- 2026-09-14｜**成稿审查**（fresh 只读分身）｜findings 共 **18**：**P0:3** P1:10 P2:5｜结论 **修订后通过**（按止损条 2，修订后**窄域重审 P0 修复面**）。审查方**独立复跑**本规格全部预验值并**确认刚订正的四组基线全为真**（19 行／35-51 与 8-9 双口径／0-0-2／`__main__`=2），且精确复现「43＝19＋24」的归档漏滤诊断。处置（全部照准；本条落本规格，长文不入 CHANGELOG）：
  - **P0-1（F3 与自身断言互斥→恒红）**：F3 原写词条含字面「旧称『子代理／分身／fresh』」，而断言 3a／3b 扫 `术语标准` 期望 0 → **自伤**。改：**旧称只落 `CHANGELOG` 081 行**（不在任何归零断言扫描面），词条内仅留无旧词字面的指向句「本词曾用名对照见 `CHANGELOG` 081 行」。
  - **P0-2（断言 6b 恒红）**：`git diff | grep "^-"` 会命中 diff 头行 `--- a/…`（审查方以 `--no-index` 实测得 1）→ 改 `git diff --numstat | awk '{s+=$2}'`（删列求和＝0）并注明须在未暂存态跑。
  - **P0-3（F2 落法产出错文本）**：`打磨定稿步:32` 源文为「（fresh **context** 各跑一维度…）」，按逐词替换得「独立上下文 context」→ F2 补「词组 `fresh context` 整体替换为『独立上下文』」。
  - **P1-1（§一① 件数与类目错）**：件数 9→**13**；类目 8／7／1／1（＝17≠19）→ **12（全部 `:4` 头注）／5（带〔出处：前缀者）／1／1 ＝ 19**；F1 件数列表补 `施工机制.md`、删「有重叠」句。**该分类错有实害**：类② 原含 4 个无〔出处〕前缀的 `:4` 行，按类目映射会用错落法。
  - **P1-2（F2 抬头件数）**：9→**8 件**（三旧词的文件并集）。**P1-3**：`subagent` 既有基线非「仅 2 处」→ 活件面 **5 处**（`术语标准` 2／`大纲域设计-规格与资料步` 2／`大纲域设计-附录` 1），并删除 §一 不变量的算式句（无断言承载、且 F3 词条自身含 `subagent` 使算式必假）。
  - **P1-4（节号错）**：§一④／F2／口径② 引的排版规则实为 **`文档写作标准` §一.2 第 1 条**（初稿误记 §一.3——该节是「术语一致／示例优先」）→ 全部改，并补引原文两处括注；§四.1 的「实读」声称同步订正。
  - **P1-5（缺删空格方向）**：替换为中文词时须与左侧 CJK **删空格**（否则得「两个 独立上下文」）→ F2 形态栏补第 ② 条。**P1-6（硬约束无网）**：新增**断言 4c**（`[一-龥]subagent|subagent[一-龥]` 扫 8 件，期望空——审查方已验证该式不误伤全角标点邻接形态）。**P1-7（词条丢四要素）**：F3 定义补回「工序角色轨的单称／会话内 spawn／无物化文件不进用户书项目／`process-role subagent`」。**P1-8（词条行不得照抄反引号）**：F3 写明首格纯文本、半角竖线，否则断言 5b 恒红。**P1-9（违 §5.9）**：断言**全部拆为一条命令一期望**（原断言 5 两命令同 bullet 已致 `[4]` 段把目标件解析成散文）。**P1-10（核销缺行）**：§七 补 **078 §八 5** 行（本批 F6 即其机制化落点）→ 7 行、汇总同步。
  - **P2 五条**：§四.1 的 `__main__`「实测 1」改 **2** 且 `main()` 加「全仓不存在」限定；F2 点名两处边界（`skill形态标准:90`「子分身」、`架构设计:54` 同句两词）；全文「自排除」措辞统一改「**路径白名单枚举**」；**顺手落 `_dir_split` docstring 首句**（原记「未触判据面」，而 080 触发语只写「下次触碰该件」——触发实已到，故新增 **F7**）；F6 的 ugrep 环境细节下沉为括注。
  - **审查方独立判断（已采纳三条）**：① 产品层**该动但只动词层**，且须防「逐件行数基线随作者打磨漂移」→ 口径要点⑨ 加「施工前规划方现场重跑三基线」；② 词条合并的**更好形态**（旧称落 `CHANGELOG`）→ 即 P0-1 的修法；③ 三项该做未做 → 全部纳入：**F8**（`术语表` 加指向行）、断言 4c、口径要点⑤ 补围栏内模板占位。
- 2026-09-14｜**窄域重审**（fresh 只读分身；止损条 2——只核 P0 修复面＋直接影响面）｜findings 共 **7**：**P0:1** P1:3 P2:3｜结论 **修订后通过（不可直接进施工，须先修补）**。它**确认 P0 三项修复全部有效**（F3 词条已无旧词字面、四要素齐；断言 6b 新形态在「只增行」样本得 0、在「改行」样本得 1；词组 `fresh context` 已被 F2 覆盖），并确认 §一① 的件数 13 与类目 12／5／1／1＝19、§七 7 行与汇总、断言 1–14 预验值**全部实跑吻合**。抓出的成分为：
  - **P0（同 P0-3 族，未闭合）**：F2 的机械落在 `施工机制:92`／`:95` 产出**叠字**——「独立的 fresh 只读分身」→「独立的**独立上下文**只读 subagent」、「两个独立 fresh 分身」→「两个独立独立上下文 subagent」→ **F2 补第三组点名**（逐字落法：「单维核对由**独立上下文的只读 subagent** 执行」／「必须是**两个独立上下文 subagent**」）并新增**断言 4d**（叠字形态归零）。
  - **P1-1**：归档件「分身」实为 **144**（初稿的 199 是「分身＋fresh」的合计）→ 定位与 §一③ 两处同改。**P1-2**：类① 措辞「全部 `:4` 头注行」不实（12 处含 `产品定义:50` 节内依据句）→ 改「11 个 `:4` 头注行 ＋ `产品定义:50`」。**P1-3**：F3 示例用全角竖线且首格带反引号，与其自身括注／断言 5b 不齐（工具已报「目标文本未提及 `^| subagent`」）→ F3 改为**逐字落盘用 `\|` 转义**（与 §一⑤ 同法）。
  - **P2 三条**：断言 6b 加固为 `git diff HEAD --numstat`（覆盖已暂存＋未暂存）；F6 次选句与 §六 否决口径的张力收紧；`skill形态标准:90` 落法去叠「派」。另核出 F4 引「现存最长 399——078 条」实为 **075 条** → 改。
- 2026-09-14｜**施工期偏差裁定（规划方；施工方申报 2 P2）**｜**全部照准**：
  - **P2-1（照准；缺陷在我）**：F1 落法① 机械执行后凡分支指称前带空格者得「迁移自 存档」「摘要自 存档」（中文间空格，违 `文档写作标准` §一.2）共 **8 处** → 施工方按 F1 **目标文本逐字**（无空格）就地补一步「自 存档」→「自存档」并加收尾零命中断言 ✓（规格 §九 记录；F1 未写该复核句属我疏漏）。
  - **P2-2（照准）**：§四.3 原称「修后报 1 条『目标文本未提及』」不实——实为 **4 条（与改前同形）＋ 3 条状态迁移为自噬预警** → 已就地订正。
  - **过程披露（施工方主动申报，采纳）**：首次脚本在 F5 处中止（其 `rep()` 对**追加式**改写误判「替换后旧串归零」），施工方以**只读** `git show HEAD:<件>` 取回原字节经 Python 写回工作区（**全程零 git 写**）后重跑 → 末态 `git diff HEAD` 恰 17 件，与规格清单逐件吻合。
- 2026-09-14｜**规划方核验（三步）**｜**通过**。① 改动面：恰 **17 件**（15 `.md` ＋ `check_spec.py` ＋ `CHANGELOG`，+59/−55 前后）、`docs/specs/archive` **零改动**；② 断言独立重跑：断言 1–14＋4c／4d 全绿（A1＝0／A2＝1／A3a＝0／A3b＝0／A4c＝0／A4d＝0／A5／5b／5c＝0/1/1／A6b＝0／A7a＝175‧0／A7b＝2／A10／11／12＝1／A14＝389）；③ 内容抽查：`术语标准` 新词条四要素齐＋首格纯文本、`施工机制:92` 已无叠字、`:150` 直推当前工作分支、`大纲域设计:4` 为「迁移自存档《…》」无残留空格、`术语表` 指向行按新体例。
- 2026-09-14｜**双维产物审查**（fresh、只读；动标准条文 → `施工机制` §五 双维）｜findings 共 **9**：P0:0 **P1:1** P2:6 ＋ 观察 2｜结论 **修订后通过**（P0＜2，不触发窄域重审）。审查方**独立重跑断言 1–14＋4c／4d 全绿**、以字符级段比对核实「除替换面外**零顺手动**」、以 `ast.get_docstring()` 证明 `check_spec.py` **仅 2 行改动且两处均在 docstring**（判据面零放宽）、以 **8 项反向构造**证明归零断言非空转（含「回退一处替换→3a 红」「改成叠字→4d 红」「首格加反引号→5b 归零」）。处置：
  - **P1-1（本批主题自伤）**：F5 的正例写「skill（**技能资产**）」——给既有术语 `skill 资产` **新造第二种叫法**（全仓唯一实例），与本批「术语统一」相抵 → 改为 `skill 资产`（skill asset），并按该族体例**正例在前**、标记 `✓`→`✅`（全仓 12 处用 `✅`）；断言 5c 的 token 同步改。
  - **P2 六条**：① 词条内「见 `CHANGELOG` 081 **行**」易读成行号且随条目漂移 → 改「081 **条目**」；② `术语表` 新增行有「仓库治理文档 等」中文间半角空格 → 删；③ `术语表:4`／`:5` **同一指向两行并存**（第二载体）→ 合并为一行；④ 指向「§一」精度不足（词条表在 **§一.3**）→ 带子条号，断言 12 的 token 同步；⑤ 「独立上下文」**未回写词条**（`术语标准` §一.4 要求记英文对应）→ 借 ① 的条目一并留痕 `fresh context`；⑥ 正例标记／次序体例 → 见 P1-1。
  - **观察 2**：F6 次选句「禁依赖路径形态」与括注「按形态并列两式」相抵 → 改「按**裸文件名子串**排除、禁用首路径前缀式」。**观察 1（不属本批，已登记）**：检查 A 对「位置格只写件数」的聚合行 hit 门不过致 **4 条断言的零命中核对静默跳过**（本批靠人工实跑兜住）＋ 转义反引号 token 恒假阴性 → 记 §八 5，触发＝再遇同类静默跳过时立项。
  - **审查方独立判断**：读感**整体优于改前**（平台词＋中文解释比自造词清爽、无机翻感），唯一可优化为个别行「独立上下文」短距重复（不阻塞）；**最该改的一处即 P1-1**。
- 2026-09-14｜**补修两轮（施工方执行，规划方独立复核）**｜**通过**。第一轮 4 处：`术语标准:23` 末句（「081 行」→「081 条目」＋英文对应留痕）、`术语标准` §二（正例前移 ＋ `✅` ＋ 改用既有词条 `skill 资产`）、`术语表`（`:5` 并入 `:4` 尾句、带 `§一.3` 锚点、去 CJK 间空格）、`规格写作标准:69` 次选句。第二轮 1 处：`CHANGELOG` 081 条目补入字面 `fresh context`——**因 `术语标准` 新词条指向该条目称「与英文对应对照见…」，该条目须真的含该字面，否则标准件过实**（施工方申报、我裁定照准）。复核实测：5b／5c＝1／1、旧 `✓` 与「技能资产」各 0、`术语表` 行数未增、A12＝1、A14＝**389** ≤400、A6b 删列 0、A7a＝175‧0、`check.sh` exit 0、lint 0 issue、改动面仍 17 件。
