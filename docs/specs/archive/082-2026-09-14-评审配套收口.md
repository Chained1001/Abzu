# 082-2026-09-14-评审配套收口

> 定位：清第七轮评审**成立的三条**（另四条已驳回，驳回证据见 §九）——① `术语标准` §1「每条＝中文名＋…」与 `subagent` 词条名自相抵（**方案一：加例外**，不改回「子代理」——工作用词与词条名用平台标准词是作者 081 裁定方向）；② `规格写作标准` §5.11 两种修法的适用面差异（「归档与本规格天然不在面内」只对白名单形态成立）；③ `施工机制:150`「当前工作分支」定义半句。外加一条可选同步（④ `产物契约标准` §3 的目录级表述精确化——080 批当时判「目录级指针仍成立不改」，本批顺手精确化，无害）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径（微批）**——4 件正文各改半句至一句＋`CHANGELOG` 一行；**动标准条文 → 按 `施工机制` §五 双维产物审查**。scope：**标准件＝standards**、**治理文档＝governance**。
> 自由度分布：[A]×4 [B]×2
> 必读输入：本规格 §一（锚点即现文）；`docs/standards/术语标准.md:13` 与 §4「新术语」条；`docs/standards/规格写作标准.md:69`；`docs/specs/施工机制.md:150`；`docs/standards/产物契约标准.md` §3
> 施工形态：subagent（规划方 spawn 的施工 subagent；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-14）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `术语标准:13` 现文 | 「每条 = 中文名 + 一句话定义 + 英文对应（英文用于 skill 内文件与代码命名）。」——与 §3 首行词条名 `subagent`（英文）**字面相抵**（第七轮 P2-A 属实）；§4 维护节另有「新术语：先查本标准；没有 → 定义后回写本标准（中文 + 定义 + 英文对应），再在其他文档使用。」——两处同款例外 ✓ |
| ② | `规格写作标准:69`（§5 第 11 条）现文 | 「须**按路径白名单枚举扫描面**（只列活件面所属路径，归档与本规格天然不在面内）；次选在管道内按**裸文件名子串**排除本规格（如 `grep -v "081-…"`）」——「天然不在面内」只对**白名单形态**成立；裸文件名形态只排本规格、**归档件仍在扫描面内**（第七轮 P2-B 属实）✓ |
| ③ | `施工机制:150` 现文 | 「AI 会话不自建分支——批次为原子单元直推当前工作分支（作者明确要求除外）。」——「当前工作分支」无定义（第七轮 P2-C 半条属实；另半「无出处无留痕」已驳回：出处＝081 归档规格 F1 落法③，`CHANGELOG` 081 条目明文「活规则改『直推当前工作分支』」）✓ |
| ④ | `产物契约标准` §3 现文 | 「同层的域设计见 [`docs/product/`](../../product/) 下各域设计件。」——目录级指针，080 批产物审查判「仍成立不改」；本批顺手精确化 ✓ |
| ⑤ | 基线 | 链接一行式 **178／断链 0**（178＝跟踪件基线 175 ＋ **本规格引文自带的 3 条链接**——§一④ 锚点行与 §二 F5 新旧文各含一条指向 `../product/` 的真链接；一行式含未跟踪件，本规格验收前恒在文件集内。此为「字面链接样例污染判据」的**第四次逼近**，078 §八 5 的机制化只覆盖 grep 扫描类、链接一行式感知仍待＝080 §八 3）；`bash scripts/check.sh` exit **0**；markdownlint **0 issue**；**F1–F4 四个目标串全仓 0 命中**；F5 目标串在 `命名标准:60` 已有同款（副本问题见 F-3 处置——本批改指针形态）✓ |

**不变量（施工前后须同）**：链接一行式 **178／断链 0**（含本规格引文 3 条，见 §一⑤；**归档后全仓基线由 175 变 178**——后续批次沿用 178，勿再取 175）；`docs/specs/`（含 `archive/`）零改动；除 §二 点名的 **5 件**外零改动；`check.sh` exit 0；lint 0 issue。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `docs/standards/术语标准.md`（`:13` 一行） | 「每条 = 中文名 + 一句话定义 + 英文对应（英文用于 skill 内文件与代码命名）。」→「每条 = 中文名 + 一句话定义 + 英文对应（英文用于 skill 内文件与代码命名）；**例外**：平台标准词可径作词条名（如 `subagent`——宿主生态的既定叫法，中文名反而生造），此时英文对应列与词条名同形。」 | ① | [A] |
| F2 | 同件（§4「新术语」条） | 「新术语：先查本标准；没有 → 定义后回写本标准（中文 + 定义 + 英文对应），再在其他文档使用。」→「新术语：先查本标准；没有 → 定义后回写本标准（中文 + 定义 + 英文对应；平台标准词可径作词条名，见 §1 例外），再在其他文档使用。」 | ① | [A] |
| F3 | `docs/standards/规格写作标准.md`（`:69`，§5 第 11 条中段） | 「须**按路径白名单枚举扫描面**（只列活件面所属路径，归档与本规格天然不在面内）；次选在管道内按**裸文件名子串**排除本规格（如 `grep -v "081-…"`）」→「须**按路径白名单枚举扫描面**（只列活件面所属路径——**此形态下归档与本规格天然不在面内**）；次选在管道内按**裸文件名子串**排除本规格（如 `grep -v "081-…"`；**此形态只排除本规格，归档件仍在扫描面内、须另行排除**）」 | ② | [A] |
| F4 | `docs/specs/施工机制.md`（`:150`） | 「批次为原子单元直推当前工作分支（作者明确要求除外）」→「批次为原子单元直推当前工作分支（`git branch --show-current` 返回值；作者明确要求除外）」 | ③ | [A] |
| F5 | `docs/standards/产物契约标准.md`（§3 末句） | 「同层的域设计见 [`docs/product/`](../../product/) 下各域设计件。」→「同层的域设计见 [`docs/product/`](../../product/) 下各域设计件（目录口径见 `命名标准` §1）。」——**指针形态、不复制目录口径原文**（成稿审查 F-3：初稿拟逐字复写「共享件在根；域件在…」与 `命名标准:60` 逐字同款，违 `AGENTS.md` §四「shared-assets 多副本同步不做、他处只引用」；指针即该逃生口的正用） | ④ | [A] |
| F6 | `CHANGELOG.md`（`## [Unreleased]` → `### Changed` 段首） | 一行：『评审配套收口（082）：…』——四项各半句＋核验结论，**≤400 字符**、末尾「核验：」句 | 过程产物 | [B] |

**口径要点（易错）**：① F1–F5 的替换**逐字照目标文本**（[A] 级），锚点唯一（§一所引现文在其件内恰 1 处）；② 不得顺手改四件里的其他措辞；③ F3 改后的括号**嵌套不超过一层**（原文一层，新增限定以分号并列入同一层）。

## 三、验收断言（含预验记录；一条断言一命令一期望）

### 断言 1｜F1/F2 落位

```bash
grep -c "平台标准词可径作词条名" docs/standards/术语标准.md
```

期望 **2**（用法节与维护节各一——期望句不写节号数字，防工具把数字当期望值致「零命中核对未判」）。**预验基线 0**。

### 断言 2a｜F3 落位（白名单形态限定语）

```bash
grep -c "此形态下归档与本规格天然不在面内" docs/standards/规格写作标准.md
```

期望 **1**。**预验基线 0**。

### 断言 2b｜F3 落位（裸文件名形态限定语）

```bash
grep -c "归档件仍在扫描面内、须另行排除" docs/standards/规格写作标准.md
```

期望 **1**。**预验基线 0**。

### 断言 3｜F4 落位

```bash
grep -c "git branch --show-current" docs/specs/施工机制.md
```

期望 **1**。**预验基线 0**。

### 断言 4｜F5 落位

```bash
grep -c "目录口径见 \`命名标准\` §1" docs/standards/产物契约标准.md
```

期望 **1**。**预验基线 0**。

### 断言 5a｜改动面

```bash
git status --porcelain | wc -l
```

期望 **6**（5 件改动＋082 规格未跟踪）。**预验基线 1**（仅 082 未跟踪）。

### 断言 5b｜归档零改动

```bash
git status --porcelain -- docs/specs/archive | wc -l
```

期望 **0**。**预验基线 0**。

### 断言 6｜链接不变

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **178 0**（含本规格引文 3 条链接，见 §一⑤；F5 改指针形态后不增链接）。**预验基线 178‧0**（规格原命令实跑）。

### 断言 7a｜门禁

```bash
bash scripts/check.sh
```

期望 exit **0**。**预验基线 0**。

### 断言 7b｜lint

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**。**预验基线 0 issue**。

### 断言 8｜过程产物（条目名）

```bash
grep -cF '评审配套收口（082）' CHANGELOG.md
```

期望 ≥1。**预验基线 0**。

### 断言 9｜过程产物（行长）

```bash
python -c "import io;print(len([l for l in io.open('CHANGELOG.md',encoding='utf-8') if '评审配套收口（082）' in l][0].rstrip()))"
```

期望 **≤400**（施工后实核项——改前抛 `IndexError`），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 五条的四处现文与基线均为本批实跑取证（`:13`／§4／`:69`／`:150`／§3 逐字读盘；四目标串全仓 0 命中）。
2. **断言实跑过**：断言 1–9 的改前侧全部实跑（0／0／0／0／0／1 行 porcelain／0／0／178‧0／exit 0／0 issue／0）；断言 9 第二命令属未预验项（改前 `IndexError`）。**互斥性**：四目标串均不在锚点现文内（新增文本）✓。
3. **禁止事项已写明**：见 §五。
4. **本规格自身的静态自检**：`[4]` 段对本规格报 **11 条预警**，逐类判「属预期」——4 条「[A] 未落实或已漂移」＋2 条「零命中待复核」＝施工前常态（token 尚未落盘）；1 条「零命中核对未判（期望子句多值）」＝断言 1 期望句的数字误判（已改写规避，见断言 1 括注）；1 条「方向标记后无引号块」＝F2 行的正常形态；3 条「规格内可解析相对链接」＝§一⑤ 所记的引文链接（F-1 同因）；1 条「B：未提取到可判定尺寸声称」＝本批无整件尺寸断言。无未核项。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/` 全部（含 `archive/`）、`README.md`、`AGENTS.md`、`scripts/`、`docs/product/`、其余标准件。
- 禁止动作：任何 git 写命令；联网、安装依赖。
- 禁止越权：只改 §二 点名的 5 件；不得顺手改四件里的其他措辞；`CHANGELOG` 历史行不动。
- 禁止改断言、改数字、删检查来变绿。

## 六、被否决的替代方案

- **词条名改回「子代理」（评审倾向的方案二）** → 否决：会把作者 081 刚裁定废弃的显示名请回来（裁定原意是「不标准名词应换为 subagent」）；取 §1 加例外（方案一），叫法与词条名同用平台标准词。
- **四条成立项各自记开放项、本批不开** → 否决（作者裁甲案）：三条同属「081 配套收口」，一次清完免再挂账。
- **`施工机制:150` 补 merge 时机（评审建议的完整形态）** → 否决：本仓无多分支工作流（单分支直推），写了是伪需求；只补定义半句。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批规格（081）的全部开放项**（必列面 ①：§八 全 **5** 条，**节内围栏** `sed -n '/^## 八、/,/^## 九、/p' | grep -c "^[0-9]\+\. "` 实数——不加围栏会连 §四 自审的编号行一并计入）＋**本批改动直接触及或可能变状态者**（必列面 ②）。判据＝同节交集；**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 081 §八 1 | 扫描类断言的机械检出（工具侧产候选行） | 仍待 | 本批 F3 只是措辞限定，不涉工具；触发＝再踩一次 |
| 081 §八 2 | 旧称的归档可读性 | 仍待 | 本批不动归档；触发＝出现实际误读 |
| 081 §八 3 | 产品术语表与术语标准的分工 | 仍待 | 本批 F1/F2 的例外让分工更清晰但未处置该条；触发＝两表出现重复条目 |
| 081 §八 4 | 顺手清单顺延 | 已履行（空） | 原文即「无」——空条目，标记已履行以闭合 |
| 081 §八 5 | 检查 A 两处工具缺口（聚合行 hit 门／转义 token） | 仍待 | 本批不动 `scripts/`；触发＝再遇同类静默跳过 |

**汇总**：共 **5 行／按条 5**——本批处置 0｜已履行 **1**（空条目闭合）｜仍待 **4**｜作废 0｜待作者 0 ＝ 5 ✓（「汇总＝行数」恒成立）。

## 八、开放项

1. **全仓链接基线 175→178（本批引文所致）**：082 规格归档后其 3 条引文链接永久计入全仓基线，后续批次的链接断言沿用 **178**（勿再取 175）。此为 078 §八 5「字面链接样例污染判据」的第四次逼近——前三次以自排除化解，本批是**基线吸收**（引文链接真实有效、不断链），处置差异记此；机制化（链接一行式的跨度／围栏感知）仍待＝080 §八 3。**归档时实证**：本规格移入 archive/ 后这 3 条链接即「归档位移断链」——已按 `施工机制` §七 例外②（机械断链只改路径）改为 `../../product/`（3 处），终检复归 178／0。
2. 第七轮评审的四条驳回项证据固化在 §九，不另立条目。

## 九、审查记录

- **第七轮评审的驳回记录（规划方复核，2026-09-14）**：①「直推当前工作分支无出处无留痕」——出处＝081 归档规格 F1 落法③（四道把关在档），`CHANGELOG` 081 条目明文「活规则改『直推当前工作分支』」；②「grep 分身 涟漪面未留痕」——留痕在 081 归档规格 §九（归档 144／fresh 55／子代理 1 逐件计数＋活件面零命中复核），`AGENTS.md`／`测试与验收标准` 勘察即零命中（故不在 081 改动面）；③「存档指代不明」——`CHANGELOG` 081 条目明文「摘去分支指称」，追溯链完整；④「检查 E 的 E 为空未明示取舍」（**第三次提出**）——`check_reconcile()` docstring 已有两处申明（「E 为空即跳过（实测 19 件…仅 3 件有此形态，逐件报即纯噪声）」＋「『跳过』的可见化落点由汇总行与空转明示承担——16/19 件…51 行噪声」），且信号逐件可见（实测输出 `跳过：…｜无补集式断言 1`），登 G19＝给已有信息再立机制——维持驳回。
- 2026-09-14｜**成稿审查**（独立上下文只读 subagent）｜findings 共 **6**：**P0:1** P1:2 P2:3｜结论 **修订后通过**（「一轮微修后即可放行施工」）。审查方**独立复核为真**：六处锚点各恰 1 处、§九 四条驳回证据全部属实（自己取了 081 归档／CHANGELOG／docstring／059 输出原文）、F1 例外与 §1 其余三条相容、§七 五行逐条对上。处置（全部照准）：
  - **F-1（P0）**：断言 6 期望 175 **必假**——本规格引文自带 3 条真链接（一行式含未跟踪件），实测 **178 0**；施工后仍 178≠175 → 四处同步改 178，§八 留「全仓基线 175→178」漂移注（「字面链接样例污染判据」**第四次逼近**，本批以基线吸收化解，机制化仍待）。
  - **F-2（P1）**：§一⑤「四目标串全仓 0 命中」不实——F5 目标串在 `命名标准:60` 已有同款 → 改精确表述。
  - **F-3（P1）**：F5 拟逐字复写「共享件在根；域件在…」与 `命名标准:60` **逐字同款**，违 `AGENTS.md` §四「他处只引用」→ **改指针形态**「（目录口径见 `命名标准` §1）」，断言 4 token 同步。
  - **F-4（P2）**：§七 的 grep 计数命令须**节内围栏**（不加围栏会连 §四 自审编号行，081 全件得 8 而节内 5）→ 括注补围栏命令。
  - **F-5（P2）**：§四 缺静态自检记录 → 补第 4 条（11 条预警逐类判属预期）；顺带把断言 1 期望句的「§1／§4」改「用法节／维护节」（数字被工具当期望值致「未判」）。
  - **F-6（P2）**：断言 2／5／7 各含两命令 → 拆 2a/2b、5a/5b、7a/7b，基线注移位。
- 2026-09-14｜**规划方核验（三步）**｜**通过**。① 改动面：恰 **5 件 M**＋082 规格未跟踪（numstat：CHANGELOG `2 0`、术语标准 `2 2`（F1＋F2 两 hunk）、施工机制／产物契约标准／规格写作标准各 `1 1`）；归档零改动。② 断言独立重跑：1／2a／2b／3／4＝2／1／1／1／1、一行式 **178 0**、7a exit 0、7b 0 issue、8／9＝1／288——全中。③ 内容抽查：F3 整行读回——两处限定语落位、首尾段（含「禁用首路径前缀式」与实例句）逐字原样；F5 为指针形态（未复写 `命名标准` 原文）。
- 2026-09-14｜**双维产物审查**（独立上下文只读 subagent；动标准条文 → `施工机制` §五 双维）｜findings 共 **3**：P0:0 P1:0 **P2:1** P3:2｜结论 **通过（可定案提交）**。审查方**逐字比对**五处替换 vs 规格 [A] 目标文本（含 hunk 形态核对：`术语标准` 恰 2 hunks、无第三处）、**独立重跑断言 1–9 全绿**、**反向构造**证明 2a／2b 各自独立检出对应限定语（拆条设计实证有效）、涟漪面 grep 四个新短语**无冲突双载体**（「平台标准词」与 081 正例同义复用）、`[4]` 段 12 条预警逐条判属。处置：
  - **P2-1**：「目录口径见…」的零命中预警在施工后仍报——真实成因＝断言 4 命令的双引号＋转义反引号使工具按**字面**提取 token（含反斜杠）而永不命中（断言 4 的 bash 实跑为绿、落位＝1）→ **081 §八 5「检查 A 转义 token 缺口」的又一实例**，已有挂账，本批禁改 `scripts/` 不新开。
  - **P3-1**：本规格 §九 原有两行重复的「待回填」→ 本回填已合并消化。
  - **P3-2（观察）**：「平台标准词」判定边界未形式化（`skill`／`hook` 是否可径作词条名）——许可式条款＋081 先例＋「中文名反而生造」实用判据可兜底；触发＝实际出现第二个候选词条时再议。
  - **审查方一句判断**：五件产物零缺陷、九条断言独立全绿、门禁全绿、涟漪面干净——可定案提交。
