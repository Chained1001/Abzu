# 083-2026-09-14-断言token反转义与写作律

> 定位：修检查 A 的「转义 token」缺口（**触发已到**：081 断言 12、082 断言 4 两例——断言命令里的 markdown 转义形态 `` \` `` 使工具按字面提取 token、永不命中，恒产「目标文本未提及」假候选；修前实测全语料假候选 **15 条**（断言出处全部为归档规格，目标件横跨施工机制／AGENTS 与多份标准件——每批审查都在人工判属））＋落一条配套写作律（「断言目标件写具体路径」——聚合行 hit 门缺口的便宜修法）。修法经**猴补实验全语料验证**：差异面 36 行、六类、全部可解释。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径（小批）**——`scripts/check_spec.py` 一行判据改 ＋ `规格写作标准` §5 一条 ＋ `CHANGELOG`；**改工具判定行为 → `施工机制` §五 双维产物审查**。scope：**开发守卫＝scripts**、**标准件＝standards**。
> 自由度分布：[A]×2 [B]×1
> 必读输入：本规格 §一（实验数据即锚点）；`scripts/check_spec.py` 的 `_assert_refs()`（`:561`–`:567` 一带，append 行 `:567`）；`docs/standards/规格写作标准.md` §5（现 11 条）；`.tmp/083/`（基准 `before.txt` 与猴补 `monkey2.py`——预验载体，验收后删）
> 施工形态：subagent（规划方 spawn 的施工 subagent；无 git 写权限）

## 一、现状锚点（猴补实验实测，2026-09-14）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 缺口形态可复现 | `_assert_refs()` 对 082 归档件断言 4 提取的 token 为「目录口径见＋\`命名标准\`＋§1」（字面含**反斜杠＋反引号两字符**，程序内 repr 实证）；081 断言 12 同形。成因＝断言命令写在规格表格／围栏内时，markdown 转义（反引号／方括号前的反斜杠）进入命令原文，工具不反转义 → token 永不命中 ✓ |
| ② | 修前全语料假候选 | **15 条**「目标文本未提及且现行为 0」的 token 含转义字面（另有 1 条「跳过」行同形——**断言出处全部为归档规格**（065×2／069×1／074×3／075×4／076×3／078×1／081×1／082×1），**目标件**横跨施工机制／AGENTS／测试与验收标准／skill形态标准／规格写作标准／术语表／产物契约标准／check.sh（按件分解合计 16＝15 候选＋1 跳过）；成稿审查 F-1′ 勘正：初稿的「23」与「横跨」系目视估计，不可复算）✓ |
| ③ | 猴补实验（token 三步反转义）差异面**完全闭合** | 全语料（**37 件**，§一⑤ 口径）静态输出 **36 行差异、六类**：**消失假候选 15**（`<`「目标文本未提及且现行为 0」）；**新增自噬预警 7**（假候选→真核对——含两条事故出身件：「见＋转义反引号＋`docs/standards/术语标准.md`＋§一.3」@ 术语表与「目录口径见＋转义反引号＋`命名标准`＋§1」@ 产物契约标准）；**中性汇总 ±4 行**（两块「现行有值／目标文本未提及」5→6 个——15 条消失候选的构成＝7 转自噬＋**2 转中性**＋6 净化改写，成稿审查 F-1 补全的机制）；**净化后仍候选 6**（一进一出：token 形态干净但确不在目标文本，如 `^# [4]`——合理候选保留）；**跳过行净化 1**（`^# \[4\]`→`^# [4]`）；**三态计数 305→303**（数学自洽：净消失候选 9（15−6 一进一出）、其中 2 转中性行不入 findings → findings −9＋7＝−2 → 305−2＝**303** ✓）✓ |
| ④ | 反转义边界（**不**修的形态） | `\.`（regex 转义点号——语义非字面，反转义会把 grep 正则改坏）、`\|`（grep BRE 的 OR——多模式 token，无单字面等价）、`.*` 等（正则模式本身）。只修 **markdown 表格转义三形**：`` \` ``→`` ` ``、`\[`→`[`、`\]`→`]` ✓ |
| ⑤ | 基线 | 基准 `.tmp/083/before.txt`＝**425 行／md5 `35d429a656e7e189ff4a74b94bfc78a1`／末行「有预警 305 条」**——语料 **37 件**（archive 24＋施工机制＋标准 **10**（**剔除 `规格写作标准`——本批 F2 改动件，按 `规格写作标准` §5.10 等价性语料律排除**）＋AGENTS＋README；成稿审查 F-2：不剔除则 F2 插行使该件 4 条链接预警行号漂移、差异面爆到 44 行）；`bash scripts/check.sh` exit **0**；markdownlint **0 issue**；`规格写作标准` §5 现 **11** 条 ✓ |

**不变量（施工前后须同）**：除 §一③ 所列**六类差异**外，全语料静态输出**逐字节相同**（同序语料、同一命令）；`_assert_refs()` 的返回结构与 `(token, 目标件)` 对齐关系不变（`check_swallow()` 的 `zip(refs, located)` 不受影响——`located` 不涉 token）；A／B／C／D／E 五类判据与退出契约零改动；`--verify` 零改动；语料内 docs 件与 `AGENTS.md`／`README.md` 零改动（`docs/` 全域中仅 §二 点名的 `规格写作标准` 例外）。

## 二、文件级改动清单

| # | 文件:位置 | 改动 | 依据 | 自由度 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py`（`_assert_refs()` 的 append 行 `:567`） | **token 三步反转义**：`m.group('token')` → `m.group('token').replace('\\`', '`').replace('\\[', '[').replace('\\]', ']')`（源码用**双反斜杠或原始串**——单反斜杠会产 `SyntaxWarning` 污染 `check.sh` stderr，078 补修先例）；行旁补一句注释：「token 反转义（083）：规格表格／围栏内的 markdown 转义（`` \` ``／`\[`／`\]`）进入命令原文，不还原则永不命中（081 断言 12／082 断言 4 两例）；**不**反转义 `\.`／`\|`（regex 语义，非字面）」。其余零改动 | ①②③④ | [A] |
| F2 | `docs/standards/规格写作标准.md`（§5，追加为第 12 条） | 逐字目标文本——「12. **断言目标件须写具体文件路径**：断言命令里的目标件须写到具体文件（如 `docs/standards/术语标准.md`），不得只写件数聚合（如「8 件」）——检查 A 的 hit 门按「目标件出现在改动行」判，聚合写法使该断言的零命中核对静默失守（081 实例：4 条断言靠人工实跑兜住）。」 | 聚合行 hit 门缺口的写作律修法（比改工具便宜——082 已自然规避） | [A] |
| F3 | `CHANGELOG.md`（`## [Unreleased]` → `### Changed` 段首） | 一行：『断言 token 反转义与写作律（083）：…』——修了什么／差异面六类与三态 305→303／核验结论，**≤400 字符**、末尾「核验：」句 | 过程产物 | [B] |

**口径要点（易错）**：① F1 的替换**逐字**（含三步顺序）；② 不得顺手反转义 `\.`／`\|`（§一④ 边界——写坏即改判据）；③ 夹具放 `.tmp/083/`（红线 2）；④ 本规格引目标行一律用**行内代码**、不写 markdown 链接（082 教训：引文带真链接会污染一行式基线）。

## 三、验收断言（含预验记录；一条断言一命令一期望）

### 断言 1｜全语料差异面封闭（最强一道）

```bash
python scripts/check_spec.py --static docs/specs/archive/*.md docs/specs/施工机制.md AGENTS.md README.md $(ls docs/standards/*.md | grep -v 规格写作标准) > .tmp/083/after-final.txt 2>&1
diff .tmp/083/before.txt .tmp/083/after-final.txt | grep "^[<>]" | grep -vc "目标文本未提及且现行为 0\|自噬预警\|跳过（目标件不出现在任何改动行）\|现行有值／目标文本未提及\|有预警"
```

期望 **0**（差异行只许五类：候选行／自噬行／**中性汇总行**／跳过行净化／三态计数行——成稿审查 F-1 补的中性类）。**逐类判据**（第二条命令）：

```bash
diff .tmp/083/before.txt .tmp/083/after-final.txt | grep "^<" | grep -c "目标文本未提及且现行为 0"
```

期望 **15**。

```bash
diff .tmp/083/before.txt .tmp/083/after-final.txt | grep "^>" | grep -c "自噬预警"
```

期望 **7**。

```bash
diff .tmp/083/before.txt .tmp/083/after-final.txt | grep "^>" | grep -c "目标文本未提及且现行为 0"
```

期望 **6**。

```bash
tail -1 .tmp/083/after-final.txt
```

期望含「有预警 **303** 条」。**预验记录**：以上期望值全部经猴补实验实得（中性 ±4 与跳过 1 两类由主管道 0 覆盖）（对 §一⑤ 重冻基准实跑）（`.tmp/083/monkey2.py`，语料序取自基准输出本身——含「跳过（非规格件）」行的件名提取，实验第二轮曾因漏提取该件致 1 行假差异，已修正闭合）。

### 断言 2｜夹具正控（转义 token 恢复命中）

夹具 `.tmp/083/fixture-esc.md`：改动清单 F1 行写「新增句『指标 \`甲\` 落位』」（引号块内带反引号包裹）；断言节含 `grep -c "指标 \`甲\` 落位" scripts/check_spec.py`。

```bash
python scripts/check_spec.py --static .tmp/083/fixture-esc.md | grep -c "目标文本未提及.*指标"
```

期望 **0**（修前该断言产假候选——token 字面含转义反引号；修后反转义命中目标文本，转自噬／中性／静默）。**预验基线**：以**未修工具**跑本夹具需得 1（猴补实验前的行为）——已由 081／082 两例代证，本夹具复验。

```bash
python scripts/check_spec.py --static .tmp/083/fixture-esc.md | grep -c "自噬预警.*指标"
```

期望 **0**（修复的本义即假候选**消失**——该夹具句不在 `check_spec.py` 现文、cur＝0，故修后转**静默**而非自噬；成稿审查 F-3 勘正：初稿期望「自噬预警 1」与自噬分支的 cur＞0 前提不符）。

### 断言 3｜夹具负控（`\|` 不受影响）

夹具 `.tmp/083/fixture-pipe.md`：断言节含 `grep -c "甲\|乙" scripts/check_spec.py`（grep OR 形态）。

```bash
python scripts/check_spec.py --static .tmp/083/fixture-pipe.md | grep -c "甲\\\\|乙"
```

期望 **1**（token 仍按字面含 `\|`——边界不被越界反转义；该候选行本身属预期形态，非缺陷）。

### 断言 4｜F1 结构锚

```bash
grep -cF "replace('\\\\[', '[')" scripts/check_spec.py
```

期望 **1**（取三步反转义的**第二步**作字面锚——反引号步在 bash 双引号内必然嵌套崩坏，成稿审查 F-4 勘正；第一、三步由本断言的 ast 与 `-W error` 两命令及断言 1 的全语料差异面共同钉住）；`grep -c "token 反转义（083）" scripts/check_spec.py` 期望 **1**；`python -c "import ast,io;ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read());print('[语法自检通过]')"` 期望通过；`python -W error::SyntaxWarning -c "import ast,io;ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read())"` **不产** `SyntaxWarning`（反转义串须双反斜杠形态）。**预验基线 0／0／通过／无警告**。

### 断言 5｜F2 落位

```bash
grep -c "断言目标件须写具体文件路径" docs/standards/规格写作标准.md
```

期望 **1**（§5 由 11 条增为 12 条）。**预验基线 0**。

### 断言 6｜改动面与门禁

```bash
git status --porcelain | wc -l
```

期望 **4**（3 件改动＋083 规格未跟踪）。

```bash
git status --porcelain -- docs/specs/archive | wc -l
```

期望 **0**。

```bash
bash scripts/check.sh
```

期望 exit **0**；`npx markdownlint-cli2 "**/*.md"` 期望 **0 issue**。**预验基线**：1 行 porcelain／0／exit 0／0 issue。

### 断言 7｜链接基线不变

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **178 0**（本规格引文一律行内代码、零链接——口径要点④）。**预验基线 178‧0**。

### 断言 8｜过程产物

```bash
grep -cF '断言 token 反转义与写作律（083）' CHANGELOG.md
```

期望 ≥1；行长一行式期望 **≤400**（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一① token 逐字 repr 复现；② 23 条假候选逐行列出（`grep '\\\\`'` 计数）；③ 猴补实验两轮（第一轮语料序漏「跳过（非规格件）」件——从基准输出提取件名修正后闭合）；④ 边界两形态实证不修；⑤ 基准冻结。
2. **断言实跑过**：断言 1 的六值全由猴补实验实得；断言 2／3 夹具**修前侧**以两例代证（081／082）；断言 4–7 基线实跑。**互斥性**：断言 4／5 的 token 即 F1／F2 目标文本 ✓。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`LICENSE`、`.zcode/`、`docs/specs/` 全部（含 `archive/`）、`AGENTS.md`、`README.md`、`docs/product/`、其余标准件、`scripts/check.sh`。
- 禁止动作：任何 git 写命令；联网、安装依赖。
- 禁止越权：F1 只改 append 行＋加一行注释；**不得**反转义 `\.`／`\|`；不得改 A–E 任何判据；`.tmp/083/before.txt` 基准不得修改。
- 禁止改断言、改数字、删检查来变绿。

## 六、被否决的替代方案

- **token 全量反转义（含 `\.`／`\|`）** → 否决：二者是 regex 语义（点号转义／BRE 的 OR），反转义会把合法 grep 正则改坏——宁可留 6 条「净化后仍候选」（它们本就是合理候选）。
- **改聚合行 hit 门（工具面承认件数聚合）** → 否决：写作律（F2）更便宜且已自然规避（082 起断言都写具体路径）；工具改判据面须更多夹具，收益不成比例。
- **顺手清 6 条「净化后仍候选」** → 否决：它们是各归档规格的真实候选（token 确不在目标文本），归档件只读。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**前一批规格（082）的全部开放项**（必列面 ①：§八 全 **2** 条，节内围栏实数）＋**本批改动直接触及或可能变状态者**（必列面 ②）。**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 082 §八 1 | 全仓链接基线 175→178（本批引文所致） | 已履行（登记性） | 基线漂移注本身即履行；本批规格零链接（口径要点④）不再动基线 |
| 082 §八 2 | 第七轮驳回项证据固化（登记性） | 已履行（登记性） | 同上 |
| 081 §八 5 | 检查 A 两处工具缺口（聚合行 hit 门／转义 token） | **本批处置** | 转义 token＝**F1**（两例复发、触发已到）；聚合行 hit 门＝**F2 写作律**（便宜修法）；另 082 双维 P2-1 的「token 字面化」实例随 F1 一并消解 |
| 080 §八 3 | 链接一行式的跨度／围栏感知 | 仍待 | 本批不涉链接判据；触发＝再踩（082 是基线吸收、未触发感知化） |

**汇总**：共 **4 行／按条 4**——**本批处置 1**｜**已履行 2**（登记性）｜仍待 **1**｜作废 0｜待作者 0 ＝ 4 ✓（「汇总＝行数」恒成立）。**范围注**：081 §八 其余三条（机械检出／旧称可读性／术语表分工）与本批无同节交集，仍待以其原触发走。

## 八、开放项

1. **六条「净化后仍候选」**：token 形态已干净但确不在目标文本（如 `^# [4]` @ check.sh——该 token 期望的是 `[4]` 段头注行而现文无此形态）。属各归档规格的历史候选，归档只读不处置；触发＝某条被误当缺陷重报时，在该批规格内判属。
2. **头注维护入口补 `_assert_refs`**（双维 F-2）：token 提取与后处理的入口现经「检查 A 判据改 check_swallow()」间接路由，头注「载体形态说明」①未提反转义——触发＝下次触碰 `check_spec.py` 头注时补一句。
3. **`check_spec.py` 自带测试件**（081 §八 5 余项、多批顺延）：本批夹具仍 `.tmp/` 用完即删。触发＝「工具判据回归无人发现」实例。

## 九、审查记录

- 2026-09-14｜**成稿审查**（独立上下文只读 subagent）｜findings 共 **6**：**P0:4** P1:1 P2:1｜结论 **修订后通过**（「修订规格文本（含重冻语料基准）后即可进施工」）。审查方**独立重跑猴补实验并与作者输出逐字节一致**，随后在作者自证数据上打红四道断言：**F-1** 差异面实为**六类**（漏「中性汇总 ±4 行」——15 消失候选＝7 转自噬＋2 转中性＋6 净化改写）；**F-2** 语料含本批改动件 `规格写作标准`（违 §5.10 等价性语料律——073 P0 的老坑），F2 插行会使差异面爆到 44 行 → **剔除该件并重冻两基准**（425 行／37 件／md5 `35d429a6…`）；**F-3** 断言 2 修后期望错（夹具句不在工具现文、cur＝0 → 转**静默**非自噬）；**F-4** 断言 4 第一命令 bash 反引号嵌套崩坏 → 改 `-cF` 第二步字面锚。另 **F-1′**：§一② 的「23」系目视估计（真值 15 候选＋1 跳过，「横跨」混淆出处与目标件）；**F-6**：45 件错（真 37）。修订全部落位（生成命令的替换曾被脚本内 join 覆盖、三轮才齐——已回读核验）。
- 2026-09-14｜**窄域重审**（独立上下文只读 subagent；止损条 2——P0＝4）｜findings 共 **5**：P0:0 P1:0 **P2:2** P3:3｜结论 **可进施工**。六项修复面全部实测复现（生成命令重跑与重冻基准**逐字节一致**、六类值全中、断言 2 修后「转静默」用 patched 模块实证、断言 4 命令经脚本文件原字面在真 bash 执行通过）；并**主动排除**猴补未覆盖的残留风险（F1 注释行＋1 对检查 B 的 9 条整件尺寸声称零牵连、检查 C 只受删文本影响——施工后差异面＝猴补面）。五条文字收尾（「五类/六类」两处口径未传导、不变量「docs/ 零改动」与 F2 字面相抵、「六值」指涉、括注「或中性」、分解合计 16）已全部照改。
- 2026-09-14｜**规划方核验（三步）**｜**通过**。① 改动面：恰 3 件 M＋083 未跟踪（check_spec.py `2 1`／规格写作标准 `1 0`／CHANGELOG `2 0`）；归档零改动。② 断言独立重跑：六值（**0／15／7／6／303**）全中、夹具正负控 **0/0 与 1**、结构锚齐（注释 1／F2 1／语法 ✓／无 SyntaxWarning ✓）、链接 **178 0**。③ 内容抽查：F1 改后整行**双反斜杠形态**（od 级）＋注释逐字；`_assert_refs` 返回结构与 `zip` 对齐不变。
- 2026-09-14｜**双维产物审查**（独立上下文只读 subagent）｜findings 共 **3**：P0:0 **P1:1** P3:2｜结论 **修订后通过（修掉一行数字即可定案提交）**。审查方**独立重跑六类值全中**（其产物与施工方输出 md5 逐字节一致）；以 **ast 函数级比对**证 38 函数中仅 `_assert_refs` 有差异（判据零放宽）；**反向构造**将 `_assert_refs` monkeypatch 回 HEAD 版逐字实现后跑同语料 → 与 before.txt **逐字节全等**（既证 F1 非空转、又证基准可由「HEAD 工具×当前语料」复现）；**反转义边界实证**（`\.`／`\|` 字面保持；36 行差异逐对配对**零负面反转**——无一条从命中态翻为假候选；`` \` `` 归一在 `_strip_wrap()` 有 050 批先例，属策略对齐）；**涟漪面封闭**（`_assert_refs` 唯一调用方 `check_swallow`）。处置：
  - **F-1（P1）**：CHANGELOG 行「三态 312→310」失实——312/310 系**重冻前旧语料（38 件）**口径，真值 **305→303**；成因＝成稿审查 F-2 改语料时更新了 §一③⑤／断言 1 而**漏改 F3 行**（「五类」同漏），施工方按 [B] 照写传导 → **两处已勘正**（CHANGELOG 行与本规格 F3 行，后者归档前完成——归档件纪律的「作废头注／机械断链」两例外不适用于此，本勘正发生在**归档动作之前**，属规格定稿的一部分）。
  - **F-2／F-3（P3）**：头注维护入口未点名 `_assert_refs`（token 提取后处理）——记 §八 2，下次触碰该件补一句；CHANGELOG 与规格的类目分法差异（数字全实测吻合）——仅表述口径，不改。
