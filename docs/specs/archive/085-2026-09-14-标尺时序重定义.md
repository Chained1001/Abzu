# 085-2026-09-14-标尺时序重定义

> 定位：按作者裁定（2026-09-14 设计讨论）重定义**参考标尺在草稿期的角色与时点**——① **约束集收窄**：进草稿步时约束集＝**基本规格**（其根本属性如题材／基调是生成的**硬约束**——写玄幻不出科幻大纲），**实测标尺移出约束集、不进生成**；② **十组对照与三态判定统一集中到 3.5 草稿验收的量级查**（草稿期唯一定考点——治「几百万字从头到尾只对抗一两个敌人组织」的密度失衡：生成时 LLM 自己感觉不到，生成完拿标尺一照才现形）；③ 生成期各处只持**方法论默认密度感**（撑体量／压节奏的常识量级）。改动覆盖**装载表／过关条件／停靠／依赖图／2.3 分装句／附录默认值**全层（成稿审查 F-1／F-2／F-5：初稿只改拍三侧，装载层仍命令「十组全装·最大消费方」，重定义不会生效）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径**——产品层设计批，跨 **4 件产品层件**＋`CHANGELOG`；产物审查走**单维核对**（产品层纯文档内容批）。scope：**产品层＝product**。
> 自由度分布：[A]×0 [B]×6
> 必读输入：本规格 §一（锚点即现文）；`大纲域设计.md`（§二 `:30`／§三.2 `:53`／§四.13 `:110`–`:118`／§十一）；`大纲域设计-草稿步.md`（§一④ 所列各行）；`大纲域设计-规格与资料步.md:112`；`大纲域设计-附录.md`（`:93`／`:112`／`:115`）
> 施工形态：subagent（规划方 spawn 的施工 subagent；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-14；基线均为 python count 次数口径）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 主件 §二 `:30` | 「进草稿步时，规格与标尺就是全部创作决策的约束集。」——标尺被列为生成时约束 ✓ |
| ② | 主件 §四.13 两处 | 三层分工句（`:112`）无限定时点；成形即判段（`:114` 末「**判定对象须在本步已成形**——……判定随其成形步补（条目量＝3.2／卷数＝3.3）」）＝分散判定 ✓ |
| ③ | 主件另两处 | 依赖图 `:53`「└→ 3.1-3.4 各模块拍三」（标尺喂生成）；`:110` 不可数组括注「（写的时候对一对量级感觉）」——新政下「写的时候」歧义 ✓ |
| ④ | 草稿步**生成期标尺消费句全量**（拍三侧 8 处＋装载表 4 行＋过关 4 行＋停靠 2 处＋拍一 2 处＋交互总纲 1 处＋表注 1 处＋异常 1 处） | 拍三侧：`:30`（总表「量级校验」）／`:67`（对照主角压力组）／`:90`（量级校验并重）／`:95`（标尺结构组对照＋三级判定＋标尺节奏组）／`:123`／`:124`／`:126`（对标尺×2）／`:152`。装载表（「跨阶段资产｜参考标尺」**4 行**）：`:48`（3.1·2 组）／`:86`（3.2·2 组）／`:115`（3.3·**全十组·本步是标尺最大消费方**）／`:142`（3.4·1 组）。过关：`:72`（标尺组已对照＋成形步补）／`:99`（已判定并记录三态）／`:130`（量级已判定×2）／`:156`（量级已判定）。停靠：`:38`（条件停靠含参考标尺红灯）／`:131`（参考标尺红灯停靠＋算式链接）。拍一：`:56`（标尺压力组对表）／`:150`（标尺梯队组对表）。表注 `:52`（标尺十组全装例）。异常 `:158`（超**标尺**）。产物层 1 处：`:129` 行尾「**每节点标尺量级对照标注**」（初稿漏列；v2 曾误勘正为「无标尺字样」——系**截断阅读**致错，窄域复审 F-B 复核原文确含该短语，成稿审查 F-1 原列**正确**）✓ |
| ⑤ | 3.5 量级查 `:166` | 「标尺全组对照……逐项出三态判定……须带处置记录」——统一校验落点**已存在**；但 `:162` ②收集未装参考标尺（对照原料缺位，既有缺口）✓ |
| ⑥ | 2.3 `:112` 后半 | 「**十组分装**：3.1 消费③结构要点+④主角压力（装 2 组）；3.2 消费③结构要点+①节奏目标（装 2 组）；3.3 消费全部十组（含新增⑧⑨⑩……）；4.1 评委消费①-⑦（……）。消费端对照：⑧由 3.3 分卷五件套的情绪弧线拍三对标、⑨由 3.3 卷末钩子拍三对标、⑩由 3.3 伏笔速记排位对标」——直接指挥 3.x 生成期装载与拍三（初稿误划禁改面，成稿审查 F-2）✓ |
| ⑦ | 附录模板层 | `:93`「节点量级默认对标尺①区间」／`:112`「地理单元数量默认取标尺③结构要点区间」／`:115`「条目量已按参考标尺可数组判定并记录三态」（成稿审查 F-5）✓ |
| ⑧ | 基线（python count 次数） | 草稿步：对标尺 **5**／跨阶段资产｜参考标尺 **4**／参考标尺红灯 **2**／已判定并记录三态 **1**／量级已判定 **3**／标尺压力组对表 **1**／标尺梯队组对表 **1**／本步是标尺最大消费方 **1**／量级校验 **2**（`:30`／`:90`）／密度感 **0**／`](大纲域设计` 链接 **8**。主件：写的时候 **1**／3.1-3.4 各模块拍三 **1**／判定对象须在本步已成形 **1**／所在侧边界值 **1**（判定算式集中一处）／密度感 **0**。规格资料：十组分装 **1**。附录：三个旧锚各 **1**、密度感 **0**。链接一行式 **178／断链 0**（本规格自身零链接）；`check.sh` exit 0；lint 0 issue ✓ |

**不变量（施工前后须同）**：判定法逐字保留——三级判定算式（主件「所在侧边界值」计数**恒 1**）、处置四选、反机械红线、「已处置」四态记录用词、十组二分本身；3.5 量级查的十组／三态／处置原文（只加「唯一定考点」限定与②收集补装）；「跨阶段资产——卷纲/正文继续对表」定性；`:158` 尾部链接保留；**链接一行式 178→175**（明认删 3 条：`:72`／`:95`／`:131`——均在本批删除段内）；`docs/specs/` 与其余产品层件、治理文档、`scripts/` 零改动。

## 二、文件级改动清单

### F1｜主件 `docs/product/outline/大纲域设计.md`（4 处＋开放项 2 条）

| 落点 | 旧形 → 新形 |
| --- | --- |
| M1 §二 `:30` | 「进草稿步时，规格与标尺就是全部创作决策的约束集。」→「进草稿步时，**规格就是全部创作决策的约束集**——其**根本属性（题材／基调等）是生成的硬约束**（写玄幻不出科幻大纲）；**实测标尺不进生成**——它是大纲 v1 成形后（3.5 量级查）的**校验器**，治生成期 LLM 自己感觉不到的密度失衡（百万字只对抗一两个敌人组织这类病，生成完拿标尺一照才现形）；生成期各处只持**方法论默认密度感**。」 |
| M2 §三.2 依赖图 `:53` | 「└→ 3.1-3.4 各模块拍三」→「└→ **3.5 量级查**（大纲 v1 成形后十组对照——生成期不装）」 |
| M3 §四.13 `:114` 成形即判段 | 末句「**判定对象须在本步已成形**——本步尚未产出该组字段时（如 3.1 的 ③ 结构要点）只做对照、不出三态，判定随其成形步补（条目量＝3.2／卷数＝3.3）。」→「**判定统一落 3.5 量级查**（大纲 v1 成形后十组全对照，草稿期唯一定考点）；3.x 各模块拍三**不对照实测标尺**、只持**方法论默认密度感**（撑体量／压节奏的常识量级）——实测区间在生成期进场会诱发「对着区间写」，密度失衡的病从源头防（判定对象未成形的问题随之消失：3.5 时一切已成形）。」 |
| M4 §四.13 `:110` | 不可数组括注「（写的时候对一对量级感觉）」→「（**3.5 量级查时**对一对量级感觉）」 |
| M5 §十一 开放项（2 条，按该节体例追加） | ①「**结构组提前照**」——条目量／卷数／反派层级属结构位、v1 后才发现密度失衡的返工最痛（动 3.2/3.3 承重梁），是否在 3.3 成形时对结构组提前照一次——触发＝首批实机走查发现 3.5 密度判定返工成本过高时评估；②「**统一修订工单机制**」——任意时点（草稿期/打磨期/定稿后）修订走同构机制：提案（作者/LLM）→影响面（按 §三.2 依赖图倒推）→作者确认→执行＋级联标记→产物状态表落行；草稿期轻工单、打磨期即 4.2 工单、定稿后即变更记录——**落点＝卷纲域设计批升架构层（三域通用）**，本域记册 |

### F2｜草稿步 `docs/product/outline/大纲域设计-草稿步.md`——装载表 4 行（整行换）

| 行 | 新形（四列） |
| --- | --- |
| C1 `:48`（3.1） | 跨阶段资产｜**方法论默认密度感**｜体量与阻力配置的**常识秤**｜**实测标尺全十组**（生成期不装——对照统一在 3.5 量级查） |
| C2 `:86`（3.2） | 跨阶段资产｜**方法论默认密度感**｜世界复杂度的**常识秤**｜**实测标尺全十组**（生成期不装——对照统一在 3.5 量级查） |
| C3 `:115`（3.3） | 跨阶段资产｜**方法论默认密度感**｜节点/反派/伏笔的**常识量级**｜**实测标尺全十组**（不装——十组对照统一在 3.5 量级查，**草稿期最大标尺消费方是 3.5**） |
| C4 `:142`（3.4） | 跨阶段资产｜**方法论默认密度感**｜配角量级与认知负荷的**常识秤**｜**实测标尺全十组**（生成期不装——对照统一在 3.5 量级查） |

### F3｜草稿步——拍三与拍一 10 处

| 落点 | 旧形 → 新形 |
| --- | --- |
| C5 `:30` | 总表 3.2 行「体系设计问句与量级校验并重」→「体系设计问句与**密度感**并重」 |
| C6 `:56` | 「后两层知方向——标尺压力组对表」→「后两层知方向——**按主角压力常识**」 |
| C7 `:67` | 「金手指强度对照主角压力组（太强=阻力全空转，太弱=白设）」→「金手指强度按**主角压力常识**排（太强=阻力全空转，太弱=白设）」 |
| C8 `:90` | 「体系设计的问句密度与量级校验并重」→「体系设计的问句密度与**密度感**并重」 |
| C9 `:95` | 整句重写——「底盘判定——**按方法论默认的世界复杂度密度感**排条目量级（太薄撑不起体量、太厚压垮节奏；实测标尺的对照与三态判定统一在 3.5 量级查）；等级阶梯跨度对标**本书体量**（高潮周期需要升级节奏支撑——梯子太短=后期无级可升）」（删原「标尺结构组对照」「三级判定处置（含链接）」「标尺节奏组」三段） |
| C10 `:123` | 「张力曲线（节点强度递进，对标尺节奏组）」→「张力曲线（节点强度按**节奏常识**递进）」 |
| C11 `:124` | 「这层反派在梯队里的层级定位（对标尺反派层级组）」→「这层反派在梯队里的**层级定位按反派梯队常识**」 |
| C12 `:126` | 「节点数量级对标尺（百万字节点区间）」→「节点数量级按**体量常识**排（百万字级）」（留常识带）；「卷数对标尺与规格体量」→「卷数按**规格体量**定」 |
| C13 `:150` | 「群像认知负荷（同时在场有名有姓的角色数——标尺梯队组对表）」→「群像认知负荷（同时在场有名有姓的角色数——**按认知负荷常识**）」 |
| C14 `:152` | 「人物量对标尺（重要人物数/功能位覆盖率）」→「人物量按**群像密度常识**排（重要人物数/功能位覆盖）」 |

### F4｜草稿步——过关／停靠／表注／异常 8 处

| 落点 | 旧形 → 新形 |
| --- | --- |
| C15 `:72` | 删「本步装载的标尺组已对照——③ 结构要点的判定随其成形步补：条目量在 3.2、卷数在 3.3，见〔链接〕」整段（含链接，**明认删链 1**）；该行其余过关项逐字不动 |
| C16 `:99` | 「本步装载的标尺可数组（③结构要点）已判定并记录三态」→「**量级判定留待 3.5 量级查**（本步只持密度感）」 |
| C17 `:130` | 「量级已判定（本步装载的可数组三态）」→「量级判定留待 3.5」；「卷数量级已判定（本步装载的可数组三态）」→「卷数量级判定留待 3.5」 |
| C18 `:156` | 「量级已判定（本步装载的可数组三态——本步只装 ⑤人物梯队组）」→「量级判定留待 3.5 量级查」 |
| C19 `:38` | 「条件停靠（决策分叉/规格缺口/参考标尺红灯）不变」→「条件停靠（决策分叉/规格缺口——**标尺红灯移至 3.5 量级查后**）」 |
| C20 `:131` | 删「参考标尺红灯停靠（可数组偏离≥偏离幅度（所在侧边界值÷3，见〔链接〕）且处置未落 → 通报作者确认）」整句（含链接，**明认删链 2**）；该行其余（静默轻通报/完成停靠）逐字不动 |
| C21 `:52` | 表注「（标尺十组全装进一个模块=什么都对照不过来）」→「（全部参考装进一个模块=什么都对照不过来）」 |
| C22 `:158` | 「同时在场角色超标尺」→「同时在场角色**超常识上限**」（句尾链接**保留**） |
| C23 `:129` | 行尾「每节点标尺量级对照标注。」→「每节点**量级常识带**标注。」（窄域复审 F-B 补——产物字段层的标尺对照句；附录 B-4 节点表列无标尺列，删改无涟漪） |
| C24 `:47` | 3.1 装载表·上游产物行·刻意不装列「④标尺段（占位态，用参考标尺更准）」→「④标尺段（实测态——实测区间不进生成，对照归 3.5）」（F-C——装载层理由句同步，非指令但防误导域建设批） |

### F5｜草稿步 3.5 节（2 处）＋规格与资料步（1 处）＋附录（3 处）

| 落点 | 旧形 → 新形 |
| --- | --- |
| C25 `:162` | 「②收集：全部草稿产物（大纲六节含伏笔速记/底盘含预置地名/人物卡全群）」→「②收集：全部草稿产物（大纲六节含伏笔速记/底盘含预置地名/人物卡全群）＋**参考标尺.md**（量级查的对照原料）」 |
| C26 `:166` | 「**量级**：标尺全组对照…」→「**量级（标尺在草稿期唯一定考点**——生成期只持密度感，实测对照与三态判定全部集中在此）：标尺全组对照…」（其后原文逐字不动；无「的」字） |
| S1 2.3 `:112` 后半 | 「**十组分装**：……消费端对照：……」→「**十组消费时点**：草稿期全部十组由 **3.5 量级查**对照（生成期不装——3.x 各模块只持方法论默认密度感，含⑧⑨⑩的对照也集中于此）；4.1 评委消费①-⑦（打磨期第二道网；新增⑧⑨⑩评委不消费——评委只看读者可感量级，新三组是设计层施工参数，档案⑧⑨⑩字段为 3.5 对照的数据源）。」 |
| A1 附录 `:93` | 「节点量级默认对标尺①区间」→「节点量级默认按**方法论密度感**（百万字级常识区间）」 |
| A2 附录 `:112` | 「地理单元数量默认取标尺③结构要点区间」→「地理单元数量默认按**世界复杂度密度感**」 |
| A3 附录 `:115` | 「条目量已按参考标尺可数组判定并记录三态」→「条目量**密度感**在案（实测判定统一在 3.5 量级查）」 |

### F6｜`CHANGELOG.md` 段首一行

『标尺时序重定义（085）：…』——约束集收窄／实测标尺不进生成（装载表/过关/停靠/依赖图/2.3 分装/附录默认值全层改）／判定集中 3.5（唯一定考点）＋②收集补装标尺／开放项两条，**≤400 字符**、末尾「核验：」句。

**口径要点（易错）**：① **判定法零改动**（算式/四选/红线/记录用词/十组二分逐字保留）；② 3.5 `:166` 除句首限定外逐字不动；③ **删链恰 3 条**（`:72`／`:95`／`:131`），`:158` 保留；④ 「密度感」统一词；⑤ 开放项按 §十一 体例追加、既有条目零改动；⑥ `:166` 锚「草稿期唯一定考点」**无「的」字**（与断言一致）。

## 三、验收断言（一条命令一期望；基线见 §一⑧）

### 断言 1｜草稿步旧形态归零（4 命令）

```bash
grep -c "对标尺" docs/product/outline/大纲域设计-草稿步.md
```

期望 **0**（基线 **5 次**）。**预验基线 5**。

```bash
grep -c "跨阶段资产 | 参考标尺" docs/product/outline/大纲域设计-草稿步.md
```

期望 **0**。**预验基线 4**。

```bash
grep -c "参考标尺红灯\|已判定并记录三态\|量级已判定\|标尺压力组对表\|标尺梯队组对表\|本步是标尺最大消费方\|量级校验" docs/product/outline/大纲域设计-草稿步.md
```

期望 **0**（基线按行口径合计——实跑记录于施工核验）。**预验基线＝施工前实跑（各词次数见 §一⑧）**。

```bash
grep -c "超标尺" docs/product/outline/大纲域设计-草稿步.md
```

期望 **0**。**预验基线 1**。

### 断言 2｜新形态落位（4 命令，逐件密度感）

```bash
grep -c "密度感" docs/product/outline/大纲域设计-草稿步.md
```

期望 **≥8**（装载表 4＋C5/C8/C9＋C16＋C26 各 1——下限按 F2–F5 新文清单，模拟实跑 **9**；窄域复审 F-F 补 C26 计数）。**预验基线 0**。

```bash
grep -c "密度感" docs/product/outline/大纲域设计.md
```

期望 **≥2**（M1＋M3）。**预验基线 0**。

```bash
grep -c "密度感" docs/product/outline/大纲域设计-规格与资料步.md
```

期望 **1**（S1）。**预验基线 0**。

```bash
grep -c "密度感" docs/product/outline/大纲域设计-附录.md
```

期望 **3**（A1/A2/A3——A2「世界复杂度密度感」同样落词；窄域复审 F-A：v2 期望 2 恒假，模拟实跑得 3）。**预验基线 0**。

### 断言 3｜主件四锚

```bash
grep -c "实测标尺不进生成" docs/product/outline/大纲域设计.md
```

期望 **1**。**预验基线 0**。

```bash
grep -c "判定统一落 3.5 量级查" docs/product/outline/大纲域设计.md
```

期望 **1**。**预验基线 0**。

```bash
grep -c "3.1-3.4 各模块拍三" docs/product/outline/大纲域设计.md
```

期望 **0**。**预验基线 1**。

```bash
grep -c "写的时候" docs/product/outline/大纲域设计.md
```

期望 **0**。**预验基线 1**。

### 断言 4｜3.5 与判定法保留

```bash
grep -c "草稿期唯一定考点" docs/product/outline/大纲域设计-草稿步.md
```

期望 **1**（无「的」字）。**预验基线 0**。

```bash
grep -c "量级查的对照原料" docs/product/outline/大纲域设计-草稿步.md
```

期望 **1**。**预验基线 0**。

```bash
grep -c "所在侧边界值" docs/product/outline/大纲域设计.md
```

期望 **1**（改前改后同数——判定算式集中一处）。**预验基线 1**。

```bash
grep -c "标尺全组对照" docs/product/outline/大纲域设计-草稿步.md
```

期望 **1**（`:166` 原文保留）。**预验基线 1**。

### 断言 5｜规格资料与附录

```bash
grep -c "十组分装" docs/product/outline/大纲域设计-规格与资料步.md
```

期望 **0**。**预验基线 1**。

```bash
grep -c "十组消费时点" docs/product/outline/大纲域设计-规格与资料步.md
```

期望 **1**。**预验基线 0**。

```bash
grep -c "对标尺①区间\|标尺③结构要点区间\|按参考标尺可数组判定" docs/product/outline/大纲域设计-附录.md
```

期望 **0**（三旧锚）。**预验基线 3**（`grep -c` 行口径，各占一行）。

### 断言 6｜开放项

```bash
grep -c "结构组提前照\|统一修订工单机制" docs/product/outline/大纲域设计.md
```

期望 **≥2**（两词各行 ≥1）。**预验基线 0**。

### 断言 7｜链接一行式（明认删 3）

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**（＝基线 178 − 删链 3：`:72`／`:95`／`:131`）。**预验基线 178‧0**（本规格零链接）。

### 断言 8｜门禁与改动面

```bash
bash scripts/check.sh
```

期望 exit **0**；`npx markdownlint-cli2 "**/*.md"` 期望 **0 issue**。**预验基线 0／0 issue**。

```bash
git status --porcelain | wc -l
```

期望 **6**（5 件 M＋085 规格）。**预验基线 1**；`git status --porcelain -- docs/specs/archive | wc -l` 期望 **0**。

### 断言 9｜过程产物

```bash
grep -cF '标尺时序重定义（085）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一①–⑧ 逐行读原文（草稿步 24 行、2.3 `:112` 后半逐字、附录 3 行、主件 4 处）；**`:129` 的教训**——v2 曾以「截断阅读」误勘正「无标尺字样」，窄域复审以全行复读推翻（成稿审查原列正确）——**引文核对必须全行，不得以截断输出下结论**；基线全部 python count 实数（§一⑧）。
2. **断言实跑过**：断言 1–9 改前侧全部实跑；断言 1 第三命令的行口径基线施工时先记；断言 9 第二命令属未预验项。**互斥性**：新锚均不在锚点现文内 ✓；**本规格零链接**（引文一律行内代码——082 教训前移）。
3. **禁止事项已写明**：见 §五。**[4] 段对本规格的候选预警**（若报）随施工报告逐条判属——新锚词「目标文本未提及且现行为 0」型属预期（施工前常态）；「期望子句多值」型属断言 1 第三命令的多词基线句，人工已核。

## 五、禁止事项

- 禁止触碰：`.git/`、`docs/specs/` 全部、`docs/product/` 其余各件、治理文档、`scripts/`、`AGENTS.md`、`README.md`。
- 禁止动作：任何 git 写命令；联网、安装依赖。
- 禁止越权：**判定法五件套逐字保留**；3.5 `:166` 除句首限定外逐字不动；C15/C20 删除时同段其余逐字保留；`:158` 链接不得删；**删链不得扩出 `:72`/`:95`/`:131` 之外**；开放项既有条目零改动。
- 禁止改断言、改数字、删检查来变绿。

## 六、被否决的替代方案

- **只改拍三侧（初稿形态）** → 否决（成稿审查 F-1）：装载表/过关/停靠层仍命令生成期消费标尺（「十组全装·最大消费方」「红灯停靠」「过关即判三态」）——重定义在装载层不生效；v2 扩为全层。
- **2.3 不动（初稿形态）** → 否决（F-2）：`:112` 后半正是指挥 3.x 装载与拍三对标的条文，不改则跨件口径分裂（初稿引文截半句得出「资产定位不涉消费时点」的全称结论——引文须引全）。
- **拍三对标尺组整体砍除** → 否决（作者裁定「保留但换参照源」）：生成期持密度感比没有好；C12 留「（百万字级）」常识带压长程漂移（成稿审查建议）。
- **结构组 3.3 成形时提前照** → 否决本批落文：与「实测不进生成」相抵——记开放项（M5①）。
- **本批落统一修订工单条文** → 否决：横切机制须三域通用——记开放项（M5②）指向卷纲域设计批升架构层。
- **附录 `:93`/`:112` 登记不改** → 否决（F-5）：模板层默认值直接指挥生成，三处全改成本为零。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**本批改动直接触及或可能变状态者**（必列面 ②；前一批 084 为轻路径无规格，必列面 ① 为空）。**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 083 §八 3 | `check_spec.py` 自带测试件 | 仍待 | 本批不动 `scripts/` |
| 大纲域设计 §十一（本批触碰该节） | 既有五条（方法论萃取／附录分流／灵感库两项／域建设） | 仍待 | 本批只**新增**两条（M5），既有条目零改动 |

**汇总**：共 **2 行／按条 2**——本批处置 0｜已履行 0｜仍待 **2** ＝ 2 ✓。**范围注**：075–083 各批开放项与本批改动面无同节交集。

## 八、开放项

1. M5 两条已落主件 §十一（不重复登记）；「结构组提前照」的实测随首批实机走查记录。
2. **密度感的词源落地**——「方法论默认密度感」目前无独立载体（隐含 LLM 常识＋2.3 的「方法论默认表」）：触发＝首批实机走查发现生成期量级失控时，评估把 2.3 既有「十组默认值表」显式挂给 3.x 装载（仍不装实测值，只装默认值）。

## 九、审查记录

- 2026-09-14｜**成稿审查**（独立上下文只读 subagent）｜findings 共 **6**：**P0:3** P1:2 P2:1｜结论 **修订后通过**（「按现稿施工，重定义在装载层不会发生」）。审查方逐行核出**生成期标尺消费句全量**（拍三侧之外另有装载表 4 行／过关／停靠／拍一／表注／异常约 15 处——含 `:115`「本步是标尺最大消费方，十组全装」）、2.3 `:112` 后半「十组分装／消费端对照」的指挥句（初稿误划禁改面——**引文截半句得出全称结论**）、附录模板层 3 处默认值、断言组四处恒假（密度感 ≥5 实落 4／「唯一定考点」多一「的」字／所在侧边界值 ≥3 实 1／链接 178 与删链事实矛盾）与基线四处错记；其 `:129` 条经复核**误列**（本规格 §一④ 勘正）。处置（全部照准，v2 整稿重写）：F-1→F2–F4 全层扩面；F-2→F5.S1；F-5→F5.A1–A3；F-3→断言组重排（逐件密度感四命令、锚去「的」字、算式计数改「恒 1」、链接明认 178→175）；F-4→基线全数实测回填（§一⑧）；F-6→M4＋锚统一＋[4] 段判属进 §四.3。审查方**独立判断**：重定义在产品逻辑上**成立且是对 LLM 生成特性的正确让步**——标尺区间对 LLM 是「配额」不是「秤」，生成时装着区间会朝中段凑数，正是反机械红线 downstream 打的病，如今 upstream 摘诱因；约束集收窄与规格字段准入律同一立法精神；密度感部分可行（短程不失控、长程漂移由开放项挂起＋4.1 评委第二道网＋处置四选柔性兜底）；建议 C12 留「（百万字级）」常识带——已采纳。
- 2026-09-14｜**窄域复审**（独立上下文只读 subagent；止损条 2——P0＝3）｜findings 共 **6**：**P0:2** P1:1 P2:3｜结论 **修订后通过（两处窄修后可进施工，无需再全量重审）**。它以**模拟施工**核验——副本施加全部 34 处替换后逐条跑断言 1–8（全绿除一条）、链接算术实证（草稿步 8→5、其余三件零变动 → 175）、ugrep 方言核（`\|` alternation 语义）。抓出两条新 P0：**F-A** 断言 2-4 附录期望 2 恒假（A2「世界复杂度密度感」同样落词，实得 3）→ 期望改 3；**F-B** `:129` 行尾「每节点标尺量级对照标注」漏列且 **v2 勘正与事实相反**（成稿审查原列正确，我的「复核」是截断阅读致错）→ 补 C23 落点（改「每节点量级常识带标注」）、撤回两处勘正、§四.1 立教训「引文核对必须全行」。另 F-C（`:47` 装载表理由句同步）→ C24；F-D（M4 引文粗体与原文不符）→ 去；F-E（「同上」指代悬空）→ 展开；F-F（断言 2-1 括注漏计 C26）→ 补。
- 2026-09-14｜**规划方核验（三步）**｜**通过**。① 改动面：恰 5 件 M＋085 未跟踪（主件 `6 4`／草稿步 `26 26`／规格与资料步 `1 1`／附录 `3 3`／CHANGELOG `2 0`）；归档零改动。② 断言独立重跑：断言 1（4 命令）全 0；断言 2 密度感 **9／2／1／3**；断言 3–6 全符（含「开放项 ≥2」实测 3，落在 §十一 内两条）；断言 7 **175 0**。③ 内容抽查：`:166` 句首限定在位且其余原文保留、装载表 C3 整行新文、C15 删段后括注收口干净（无孤儿标点）。
- 2026-09-14｜**单维核对**（独立上下文只读 subagent；产品层纯文档内容批 → `施工机制` §五 单维）｜findings 共 **4**：P0:0 P1:0 **P2:4**｜结论 **通过（可定案提交）**。核对面四项逐项过：**主线一致**（五件全部「标尺」行逐行读过，无一处残留生成期装标尺/判三态语气——装载表/过关/停靠/依赖图/2.3/附录默认值全层同一口径）、**替换边界**（D1–D3 三处保守读数确读得通：`:72` 括注收口、`:131` 句号收尾、`:38` 语义自足、`:95` 括注降层闭合正确）、**数字与指代**、**遗留矛盾**（唯一残留 `:185`「标尺压力组」判不相抵）。删链算术实证「恰删 3 条零新增」。处置：
  - **F-1（P2，本批新引入）**：依赖图新行处在 ```` ```text ```` 围栏内、`**` 渲染为裸星号（同块其余行无加粗）→ **已去星号**。
  - **F-4（P2）**：表注「全部参考」指代略泛 → 改「全部参考**资产**」（本批面内）。
  - **F-2（P2）**：`:185` 挂载总表萃取源提示列以「标尺压力组」为源（全仓唯一残留，判**不相抵**——该列＝萃取源非装载指令）→ **登记为主件 §十一 开放项**（触发＝域建设批触碰该表时统一体例），本批不改（非本批面）。
  - **F-3（P2）**：`:126`「体量常识」与「密度感」两族词并存 → **不改**（规格 C12 明文，语义同指非实测默认档，无行为差异）。
  - 观察项（不计）：「大纲 v1」为批内新指代（四处用法一致、可推断）；附录 B-9 未同步 3.5 限定（字段说明书，无冲突）；依赖图模块链未含 3.5 节点（新增「标尺→3.5」边后图形自洽）。
