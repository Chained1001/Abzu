# 086-2026-09-14-skill资产结构对齐

> 定位：把 skill 资产结构从现行「**目录三件**（模板混在 `references/`）」对齐为 Agent Skills 标准形态「**`SKILL.md` 必需 ＋ `references/`／`assets/`／`scripts/` 三可选**」——作者 2026-09-14 裁定（附标准结构图）：① **引入 `assets/`**（模板、资源文件），② **模板从 `references/` 迁 `assets/`**，③ **`references/` 去 `{skill 名}/` 子目录层、改平铺**。顺手修一处**现有矛盾**：`skill形态标准` §1 称「三件」、条目 3 说 `scripts/`「可选」、机检锚点断言「三件齐」——三处自相抵，本批统一为「一必需 ＋ 三可选」。**本批不建 `skills/`、不落任何域资产**（那是 087 建域批的事），只对齐规则。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径**——改 **3 件标准件**（17 处）＋`CHANGELOG`；动标准条文 → **按 `施工机制` §五 走双维产物审查**。scope：**标准件＝standards**。
> 自由度分布：[A]×0 [B]×4
> 必读输入：本规格 §一（锚点即现文）；`docs/standards/skill形态标准.md`（`:3`／§1 `:14`–`:20`／§5.1 `:58`／§6 `:69`／§9.1 `:94`／§13 `:142`／机检锚点 `:168`／`:173`）；`docs/standards/命名标准.md`（§2 表 `:47`／`:48`／`:51`／§5 `:90`／`:94`／§二 `:105`／`:110`／机检锚点 `:128`）；`docs/standards/术语标准.md:24`
> 施工形态：subagent（规划方 spawn 的施工 subagent；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-14）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `skill形态标准` §1 现文（`:14`–`:20`） | 标题「### 1. 目录三件（`skills/{skill 名}/`，现状预留）」；条目 1 `SKILL.md`（必需语义）、2「`references/{skill 名}/`——skill 内方法论与模板；`references` 之间不互相链接（**一层深**：`SKILL.md` → `references` 为止…）」、3「`scripts/`——可选，运行时脚本与共享库；无脚本的域不建该目录」；末句「现状：`skills/` 尚未建立，本条为规则先行」✓ |
| ② | **三处自相抵** | §1 标题「**三件**」（含必须齐之意）↔ 条目 3「`scripts/` **可选**…不建该目录」↔ 机检锚点 `:168`「清单断言（`skills/{skill 名}/` **三件齐**）」——三者不能同真 ✓ |
| ③ | `skill形态标准` **另六处** | `:3` 头注定位「…目录三件、`SKILL.md` 四节骨架…」；`:58` §5.1 预算（SKILL.md <500／目标 <300／单 reference <300）；`:69` §6「**产物模板（`{域}-{产物名}-template.md`）**：①定位注记…」；**`:94` §9.1 引用示例「（如 `references/outline/outline-workflow.md`「草稿步」节、`scripts/init-book.js`）」——子目录层形态（成稿审查 F-3 查出：初稿漏列）**；`:142` §13 矩阵「产物模板 `{域}-{产物名}-template.md`」；机检锚点 `:173`「§9 引用写法…｜扫描 `references/` 内相对链接」✓ |
| ④ | `命名标准` §2 表 skill 资产行 | `:46` skill 壳；`:47` **references 子目录**（`skills/example-skill/references/example-skill/`）；`:48` 域工作流（位置 `skills/{skill 名}/references/{skill 名}/`）；`:49` 阶段方法论（「同上」）；`:50` 域内参考文件（「同上」）；`:51` **产物模板**（位置「同上」）；`:52` 运行时脚本（`scripts/`）✓ |
| ⑤ | `命名标准` §5／§二／§三 | `:90` 引用例「`references/outline/outline-workflow.md`「草稿步」节」；`:94` 分区规则「skill 运行时资产（`references/`、`scripts/`）禁跨目录链接…references 之间不互相链接（一层深纪律…）」；`:105` 正例「`skills/example-skill/references/example-skill/example-skill-workflow.md`」；`:110` 反例「`skills/example-skill/references/example-skill/工作流.md`」；`:128` 机检锚点「扫描 `references/` 内相对链接」✓ |
| ⑥ | **`术语标准:24`（新概念的权威定义处，F-4 查出）** | 「skill 资产 \| 随安装分发到用户机器的 skill 运行时文件（**references／scripts**），引用规则受部署自包含约束 \| skill asset」——同型括注在 `命名标准:94` 本批更新、此处留旧则**术语定义与新形态相抵** ✓ |
| ⑦ | 「目录三件」全仓出现面 | 仅 `skill形态标准` 三处（`:3`／`:14`／`:168`；**本规格自身除外**——其命中属引述）；`书目录契约:52` 的「大纲产物三件」是**产品层产物齐备断言**（六节齐／非空／一类一文件），与 skill 资产目录**无关，不动**（成稿审查独立复核对）✓ |
| ⑧ | 基线 | 链接一行式 **175／断链 0**（**本规格自身零链接**——引文一律行内代码／描述式）；`bash scripts/check.sh` exit **0**；markdownlint **0 issue**；`assets` 在标准件与产品层 **0 命中**；`一层深` ＝ `skill形态标准` 3（`:3`／`:17`／`:160`）／`命名标准` 1（`:94`）✓ |

**不变量（施工前后须同）**：`skill形态标准` 的**四节骨架**（§2）、frontmatter 五键（§3）、description 三段式（§4）、§5 预算的**数值**（500／300／300）、§6 其余两段模板（workflow／方法论）、**§8／§10–§12 各节与 §9 除 `:94` 示例外的内容**、§13 其余行**逐字不动**；`命名标准` 的 §2 表其余行、§1 目录树、§3／§4／§6、§二 其余正反例**逐字不动**；`术语标准` 除 `:24` 括注外**逐字不动**；链接一行式 **175／断链 0**；`docs/specs/`（含 `archive/`）与产品层、`scripts/` 零改动；**不建 `skills/`**。

## 二、文件级改动清单

### F1｜`docs/standards/skill形态标准.md`（8 处）

| 落点 | 旧形 → 新形 |
| --- | --- |
| S1 `:3` 头注定位 | 「…约束 skill 组本身：目录三件、`SKILL.md` 四节骨架、frontmatter 键（含调用控制键）、`references` 一层深与体积上限。」→「…约束 skill 组本身：**目录形态（`SKILL.md` 必需；`references`／`assets`／`scripts` 三可选）**、`SKILL.md` 四节骨架、frontmatter 键（含调用控制键）、`references` 一层深与体积上限。」 |
| S2 §1 整节（`:14`–`:18`） | 标题改「### 1. 目录形态（`skills/{skill 名}/`，现状预留）」；条目改**四条**——1 `SKILL.md`（照旧）；2 「`references/`——**按需加载的文档**（方法论／话术／规范／查询表）；**平铺**（不设 `{skill 名}/` 子目录层）；`references` 之间不互相链接（**一层深**：`SKILL.md` → `references` 为止，同层互引读者可能看不到对方）。」；3 新「`assets/`——**模板与资源**（产物模板等需被填充／复制的骨架）；平铺。与 `references/` 的分工判据＝**被「读」的说明归 `references/`，被「填／复制」的骨架归 `assets/`**。」；4 原条目 3 **删首「可选，」三字**（「无脚本的域不建该目录」已含可选语义） |
| S3 `:20` 现状句 | 其后追加「其中 2／3／4 为**按需建**——域无该类资产时不建该目录；仅 1 必需。**原「三件」表述与本标准机检锚点的「三件齐」断言一并废止**——「三件」含「可选项必须存在」之歧义。」（**注：「三件」不带「目录」二字**——成稿审查 F-1：带则与断言 1 的归零期望互斥） |
| S4 `:58` §5.1 预算 | 该条末追加「**`assets/` 的模板与资源不计行数预算**——它们不参与「触发即全量加载」（由运行时按需取用），长度由产物字段数决定；头部两行声明照旧（属 skill 资产，见 §9.3）。」 |
| S5 `:69` §6 产物模板条 | 「**产物模板（`{域}-{产物名}-template.md`）**」→「**产物模板（`assets/{域}-{产物名}-template.md`）**」 |
| S6 `:94` §9.1 引用示例 | 「（如 `references/outline/outline-workflow.md`「草稿步」节、`scripts/init-book.js`）」→「（如 `references/outline-workflow.md`「草稿步」节、`assets/outline-基本规格-template.md`、`scripts/init-book.js`）」——**去子目录层**＋**补 assets 一例**（F-3） |
| S7 `:142` §13 矩阵行 | 「\| 产物模板 `{域}-{产物名}-template.md` \|」→「\| 产物模板 `assets/{域}-{产物名}-template.md` \|」 |
| S8 机检锚点两行（`:168`／`:173`） | `:168`「§1 目录三件形态（SKILL.md 存在、references 同名子目录）」／「清单断言（`skills/{skill 名}/` 三件齐）」→「§1 目录形态（`SKILL.md` 存在；`references/`／`assets/` 按需建，**无 `{skill 名}` 子目录层**）」／「清单断言（`SKILL.md` 存在 ＋ 目录层数断言）」；`:173`「扫描 `references/` 内相对链接」→「扫描 `references/`／`assets/`／`scripts/` 内相对链接」 |

### F2｜`docs/standards/命名标准.md`（8 处）

| 落点 | 旧形 → 新形 |
| --- | --- |
| N1 `:47` | **删整行**（「references 子目录 \| 取 skill 名 kebab-case \| …」——去子目录层后该行无对象） |
| N2 `:48` | 位置列「`skills/{skill 名}/references/{skill 名}/`（预留）」→「`skills/{skill 名}/references/`（预留）」 |
| N3 **产物模板行**（原 `:51`；N1 删行后为 `:50`） | 位置列「同上（预留）」→「**`skills/{skill 名}/assets/`（预留）**」——**落点判据＝「产物模板」四字所在行**（「\| 同上（预留） \|」同形有 **4 处**：`:49`／`:50`／`:51`／`:53`，**不得按行号下手**）；`:49`／`:50`／`:53` 三行**不动**（F-6） |
| N4 `:90` | 示例「`references/outline/outline-workflow.md`「草稿步」节」→「`references/outline-workflow.md`「草稿步」节」 |
| N5 `:94` | 「skill 运行时资产（`references/`、`scripts/`）禁跨目录链接」→「skill 运行时资产（`references/`、`assets/`、`scripts/`）禁跨目录链接」；该条句末「references 之间不互相链接（一层深纪律）」**保留原样**（原文含指向 `skill形态标准` 的相对链接——改后原样保留，**不加不减**） |
| N6 `:105` | 正例「`skills/example-skill/references/example-skill/example-skill-workflow.md`」→「`skills/example-skill/references/example-skill-workflow.md`」 |
| N7 `:110` | 反例「`skills/example-skill/references/example-skill/工作流.md`」→「`skills/example-skill/references/工作流.md`」 |
| N8 `:128` | 「扫描 `references/` 内相对链接」→「扫描 `references/`／`assets/`／`scripts/` 内相对链接」（与 F1.S8 后者**同面**——F-5） |

### F3｜`docs/standards/术语标准.md`（1 处）

| 落点 | 旧形 → 新形 |
| --- | --- |
| T1 `:24` | 「随安装分发到用户机器的 skill 运行时文件（references／scripts），引用规则受部署自包含约束」→「随安装分发到用户机器的 skill 运行时文件（**references／assets／scripts**），引用规则受部署自包含约束」（F-4：新概念的**权威定义处**须同步，否则 087 建域批无据） |

### F4｜`CHANGELOG.md`（`## [Unreleased]` → `### Changed` 段首）

一行：『skill 资产结构对齐（086）：…』——引入 `assets/`（模板与资源）／模板迁 assets／`references` 平铺去子目录层／修「三件」三处自相抵／`术语标准` 定义同步／不建 `skills/`，**≤400 字符**、末尾「核验：」句。

**口径要点（易错）**：① S2 是**整节替换**——原三条目全换新，**不得**留旧条目残句；② **N3 以行内容（「产物模板」）定位，不以行号**；③ 判据句（被读／被填）逐字照 F1.S2；④ 改动面**不含** `skills/`、不含产品层；⑤ 本规格 §一 引用的行号仅作定位参考，施工以**内容锚点**为准；⑥ **S3 的废止句用「三件」不带「目录」**；⑦ **施工中如需引用他处，一律行内代码／描述式**（本规格自身零 markdown 链接）。

## 三、验收断言（一条命令一期望）

### 断言 1｜「目录三件」归零（`skill形态标准`）

```bash
grep -c "目录三件" docs/standards/skill形态标准.md
```

期望 **0**（基线 3：`:3`／`:14`／`:168`）。

### 断言 2｜子目录层形态归零（两件各查）

```bash
grep -c "references/{skill 名}/" docs/standards/skill形态标准.md
```

期望 **0**（基线 **1**：`:17` §1 条目 2）。

```bash
grep -c "references/{skill 名}/" docs/standards/命名标准.md
```

期望 **0**（基线 **1**：`:48`「域工作流」行的位置列）。

### 断言 3｜旧行与旧例归零

```bash
grep -c "| references 子目录 |" docs/standards/命名标准.md
```

期望 **0**（基线 **1**：`:47`——N1 删行的判别项）。

```bash
grep -c "references/outline/" docs/standards/skill形态标准.md
```

期望 **0**（基线 **1**：`:94` §9.1 示例——F-3 的判别项）。

### 断言 4｜新形态落位（`assets`）

```bash
grep -c "assets" docs/standards/skill形态标准.md
```

期望 **≥7**（S1／S2 条目 3／S4／S5／S6／S7／S8 各含）。**预验基线 0**。

```bash
grep -c "assets" docs/standards/命名标准.md
```

期望 **≥3**（N3／N5／N8）。**预验基线 0**。

```bash
grep -c "assets" docs/standards/术语标准.md
```

期望 **1**（T1）。**预验基线 0**。

### 断言 5｜判据句与矛盾修除

```bash
grep -c "被「读」的说明归" docs/standards/skill形态标准.md
```

期望 **1**（S2 分工判据）。**预验基线 0**。

```bash
grep -c "三件齐」断言一并废止" docs/standards/skill形态标准.md
```

期望 **1**（S3）。**预验基线 0**。

```bash
grep -c "模板与资源不计行数预算" docs/standards/skill形态标准.md
```

期望 **1**（S4）。**预验基线 0**。

```bash
grep -c "assets/{域}-{产物名}-template.md" docs/standards/skill形态标准.md
```

期望 **2**（S5／S7）。**预验基线 0**。

### 断言 6｜不变量保留

```bash
grep -c "一层深" docs/standards/skill形态标准.md
```

期望 **3**（`:3`／`:17`／`:160`）。**预验基线 3**。

```bash
grep -c "一层深" docs/standards/命名标准.md
```

期望 **1**（`:94`）。**预验基线 1**。

```bash
python -c "import io,re;t=io.open('docs/standards/skill形态标准.md',encoding='utf-8').read();print(len(re.findall(r'^### \d+\. ',t,re.M)))"
```

期望 **13**（§1–§13 节数不变）。**预验基线 13**。

```bash
grep -c "无脚本的域不建该目录" docs/standards/skill形态标准.md
```

期望 **1**（S2 条目 4 保留该语义）。**预验基线 1**。

### 断言 7｜链接与门禁

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**（**本规格零链接**——含在制规格时此口径成立；F-2 修）。**预验基线 175‧0**。

```bash
bash scripts/check.sh
```

期望 exit **0**；`npx markdownlint-cli2 "**/*.md"` 期望 **0 issue**。**预验基线 0／0 issue**。

### 断言 8｜改动面

```bash
git status --porcelain | wc -l
```

期望 **5**（3 件标准件 M＋`CHANGELOG`＋086 规格未跟踪）；`git status --porcelain | grep -c "skills/"` 期望 **0**（**未建 `skills/`**）。**预验基线 1**。

### 断言 9｜过程产物

```bash
grep -cF 'skill 资产结构对齐（086）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 八条全部本批实跑取证（`skill形态标准` 八处含 `:94`、`命名标准` 八处、`术语标准:24` 逐行读原文）；「目录三件」在标准件与产品层面 3 处（本规格自身除外）；`书目录契约:52` 判另一概念（成稿审查独立复核确认）；`一层深` 双件计数（3／1）实数。
2. **断言实跑过**：断言 1／3（两命令）／4／5／6（四命令）／7／8 改前侧实跑；断言 2 两命令实跑（各 1）；断言 9 第二命令属未预验项。**互斥性**：新锚（assets／判据句／废止句「三件」／预算句）均不在锚点现文内 ✓；**本规格零链接**（F-2 的修因）。
3. **禁止事项已写明**：见 §五。

## 五、禁止事项

- 禁止触碰：`.git/`、`docs/specs/` 全部（含 `archive/`）、`docs/product/`、`scripts/`、`AGENTS.md`、`README.md`、`测试与验收标准`（本批不触发其段表同步——`[2]` 段建设归 087）。
- **不得创建 `skills/` 目录或任何域资产**（本批只改规则）。
- 禁止动作：任何 git 写命令；联网、安装依赖。
- 禁止越权：S2 整节替换不得留旧条目残句；**N3 以内容定位、不得按行号**；`命名标准` `:49`／`:50`／`:53` 三行与 `:94` 句末链接不动；判据句逐字照 F1.S2；S3 废止句不得带「目录」二字。
- 禁止改断言、改数字、删检查来变绿。

## 六、被否决的替代方案

- **保留 `references/{skill 名}/` 子目录层** → 否决（作者裁定对齐标准结构）：单域 skill 根已隔离，同名子目录冗余；消歧由文件名域前缀承担。
- **模板留在 `references/`** → 否决：标准结构明确 `assets/`＝模板与资源；references（被读）与 assets（被填）性质不同，混放让「行数预算」口径含混。
- **只改 `命名标准:90` 示例、不改 `skill形态标准:94`** → 否决（F-3）：改后同型示例一平铺一子目录，正是本批要消的自相抵；且目标③在自家标准件里留反例。
- **`术语标准:24` 登记为 087 同步点、本批不改** → 否决（F-4）：该处是 `assets/` 的**权威定义处**，留旧则术语定义与 §1 新形态相抵；改一处成本为零。
- **S3 废止句照原样带「目录」二字** → 否决（F-1）：与断言 1 归零期望互斥、断言恒不可满足。
- **本规格引文照写 markdown 链接** → 否决（F-2）：一行式含在制规格 → 自带断链、断言 7 永不满足（082 批同型教训的第五次）。
- **顺带改 `测试与验收标准` §1.1 `[2]` 段** → 否决：该段尚未建设，定义归 087。

## 七、本批核销（相关归档规格的开放项）

> 核销对象＝**本批改动直接触及或可能变状态者**（必列面 ②；前一批 085 为产品层设计批，其 §八 两条属产品域内、与本批无同节交集）。**一行一条**，汇总＝行数。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 083 §八 3 | `check_spec.py` 自带测试件 | 仍待 | 本批不动 `scripts/` |
| 085 §七 第 2 行 | 附录模板与情绪词表分流待裁定 | **本批处置（部分）** | 本批确立**分流判据**（被读的说明归 `references/`、被填的骨架归 `assets/`）→ 模板归 `assets/`；**情绪词表 A-2 按判据归 `references/`**（查询表性质）；实际迁移归 **087** |

**汇总**：共 **2 行／按条 2**——本批处置 1（部分）｜仍待 1 ＝ 2 ✓。**另记**：产品层三处文字（`大纲域设计:197`「部分进 `skills/abzu-outline/references/`」、`:200`「运行资产（域壳、references、脚本）」、`大纲域设计-附录:3`「随 `skills/abzu-outline/references/` 分流」）在判据确立后已过时，**本批禁触碰**（§五）→ **同步归 087**（成稿审查观察项）。

## 八、开放项

1. **`assets/` 内资源类细分**：本批只立「模板与资源」一类，未细分子目录／索引。触发＝首个域 `assets/` 内文件超 15 件、或出现非模板类资源需分区时评估。
2. **`[2]` 段的目录形态校验**：本批只改规则，机检落地归 087（`agentskills validate` 是否覆盖目录形态待实测——若只校 frontmatter，须在 `[2]` 段补本地断言）。
3. **`references` 平铺后的单目录容量**：首个域预计 ≤6 件；触发＝单域 reference 超 10 件时评估分区。
4. **产品层三处过时文字的同步**（§七 另记）：随 087 建域批一并改（届时产品层设计文档的分流段与运行资产表述须按 `assets/` 新形态重写）。
5. **变体表头使检查 A／C 空转（双维审查 F-2，P1）**：本规格 §二 三表用「落点｜旧形→新形」两列——`_change_rows()` 无「文件」／「自由度」列别名即**整表跳过**，检查 A 的 hit 门取不到改动行（`A：提取 16 条断言、命中 0 条`），检查 C 逐字核对同因跳过；**085 批同形**（实测「跳过整表 5 张／提取 21 条、命中 0 条」，`git merge-base` 证同版工具）→ 该形态**自 085 起连续两批**使守卫空转（`AGENTS.md` §七 信号位）。**处置（审查方三选一，取首选）**：此后写规格**回归 `施工机制` §八 模板表头**（`文件:位置｜改动（现状→目标）｜自由度｜依据`）——零工具改动即恢复 A／C 全覆盖，且 070–083 实践已证该形态可解析；不取「扩工具认变体表」（须新增「路径在节标题而非列内」的语义，无事故出身，违 §四）。触发＝下一批规格写作时即执行；连带 **F-5**（A 的零命中明示行不计入 findings 计数）随此法自然消解。
6. **机检锚点措辞两处待收口（双维审查 F-3）**：`skill形态标准:169` 的摘要列已在本批顺手补 `scripts`；`:174` 条文列写「references 内无相对 `.md` 链接」而强制方式列已扫三目录——该措辞低估扫描面，随 087 建 `[2]`／内容轨检查器时一并收口。
7. **断言 4 只锁计数下限（双维审查 F-4）**：反向构造可构造「assets 字面在表内漂移而全绿」；087 建内容轨检查器时补「产物模板行的位置列含 `assets/`」断言。

## 九、审查记录

- 2026-09-14｜**成稿审查**（独立上下文只读 subagent）｜findings 共 **9**：**P0:2** P1:5 P2:2｜结论 **修订后通过**（「当前形态下施工方按 §五 逐字施工必在断言 1、断言 7 上停摆，故不得带 P0 进施工」）。审查方**按 F1.S1–S8／F2.N1–N8 逐字推演改后文本并跑全部断言**（推演件已删净、全树复原）。处置（全部照准）：
  - **F-1（P0）**：断言 1「目录三件」归零 vs S2 废止句含同一字面 → **恒不可满足** → S3 废止句改「原**「三件」**表述」（去「目录」二字）。
  - **F-2（P0）**：断言 7 命令实测 **177／2**——本规格自带 2 条真链接（引 `命名标准`／`skill形态标准` 现文时把链接也抄了进来，从 `docs/specs/` 解析落空）→ **两处引文改描述式**，规格归零链接、期望 175／0 成立。此为 082「引文带真链接污染一行式」的**第五次逼近**——已在 §二 口径要点⑦ 前置声明。
  - **F-3（P1）**：`skill形态标准:94` §9.1 引用示例仍子目录层形态（初稿漏列；全仓同型仅 2 处，本批只改另一处）→ **补 F1.S6**；不变量句同步收窄（§9 除 `:94` 外不动）。
  - **F-4（P1）**：`术语标准:24` 的「skill 资产」定义括注——`assets/` 的**权威定义处**漏改 → **新增 F3.T1**。
  - **F-5（P1）**：F1.S8 后者与 F2.N8 同源机检锚点行口径不一（N8 缺 `scripts/`）→ N8 补齐三目录。
  - **F-6（P1）**：N3 落点唯一性不足（「\| 同上（预留） \|」同形 **4 处**；且 N1 删行后原行号 `:51` 漂到 `:50`）→ N3 改为**内容定位**（「产物模板」行）并注明「不得按行号下手」，`:49`／`:50`／`:53` 明列不动。
  - **F-7（P1）**：断言 2 第一命令为空转（行尾锚对目标行不匹配——该行以「（预留） \|」结尾）→ 改**两件各查无锚形态**（`skill形态标准` 基线 1 新发现）。
  - **F-8／F-9（P2）**：头注「12 处」→ **17 处**；断言 6 原第一命令为恒真哨兵 → 换为「`| references 子目录 |` 行归零」（断言 3 第一命令）。
  - **审查方独立判断**：「目录三件」面判定正确（`书目录契约:52` 的「大纲产物三件」确为产品层产物齐备断言）；改后六处口径一致（§1／§5／§6／§9.1／§13／机检锚点）；`[2]` 段「不改」不造成悬空（已由 §八 开放项 2 指向 087）；与 `AGENTS.md` §四 无冲突（`shared-assets` 属同串异义）。
- 2026-09-14｜**规划方核验（三步）**｜**通过**。① 改动面：恰 5 行 porcelain（3 件标准件 M＋`CHANGELOG`＋086 规格未跟踪），**`skills/` 不存在**；numstat 与 §二 点名面逐处对应（`skill形态标准` 12/11／`命名标准` 7/8／`术语标准` 1/1）。② 断言独立重跑：19 项（断言 1–9＋不变量）全符——`目录三件` 0／`references/{skill 名}/` 0·0／`assets` 9·3·1／判据句·废止句·预算句各 1／模板路径 2／`一层深` 3·1／§节数 13／N3 落点 1／「同上」余 3 行／§5 数值 500·300·300 俱在／一行式 **175 0**。③ 内容抽查：`skill形态标准:16`–`:19` 四条目（必需／references 平铺／assets＋判据句／scripts）落位正确、`:95` §9.1 示例已平铺并补 assets 例、`:21` S3 废止句用「三件」不带「目录」（F-1 修因生效）。
- 2026-09-14｜**双维产物审查**（独立上下文只读 subagent；动标准条文 → `施工机制` §五 双维）｜findings 共 **5**：P0:0 **P1:1** P2:4｜结论 **修订后通过**。审查方**程序化比对 17 处落盘 vs 规格新形**（16 处逐字命中，1 处见 F-1）、**独立重跑断言 1–9 全过**、**反向构造四项**（回退 S2 标题→断言 1 红／N3→断言 4 红／删 S4 句→断言 5 红／复原 N1 行→断言 2＋3 红，证非空转）、**改后自洽六处**（§1／§5／§6／§9.1／§13／机检锚点口径一致；`命名标准` §2 表 21 行 pipe 恒 4；N3 只动产物模板行、另三处「同上」未动；全仓无其余标准件写旧形态）；独立判断「新形态在产品逻辑上成立」三理由（与宿主标准同构＋判据句与预算律同源且一举解决 085 的分流开放项＋平铺代价被既有纪律吸收）。处置：
  - **F-1（P2）**：`命名标准:93` 落盘多出 `**`（规格 N5 新形无加粗）——**裁定**：N5 系 [B] 项内的**强调补充**（该行新增项以加粗标出，渲染合理、不影响断言取值），**本批不返工**；同时确认「新形含 `**` 即文本内容」的判断正确（081 F3／085 C16 先例独立复核一致）——**但注意 `**` 的逐字性只有纪律、无机检**（断言一律取裸 token），故此类偏差只能靠审查发现。
  - **F-2（P1）**：**变体表头使检查 A／C 对 085／086 连续两批空转**（判因经读码＋实跑双证，`git merge-base` 证 085 同版工具）→ **登记 §八 5**，处置取审查方首选＝**写法回归 `施工机制` §八 模板表头**（下一批规格起执行），不取扩工具别名。
  - **F-3（P2）**：机检锚点两处措辞未同步 → `:169` 摘要列**本批顺手补 `scripts`**（属本批「一必需三可选」立法目标内）；`:174` 条文列措辞随 087 收口。
  - **F-4（P2）**：断言 4 只锁计数下限 → 087 建检查器时补位置列断言。
  - **F-5（P2）**：A 的零命中明示行不计入 findings 计数 → 随 F-2 的「回归模板」自然消解。
