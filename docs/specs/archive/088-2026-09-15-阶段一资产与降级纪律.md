# 088-2026-09-15-阶段一资产与降级纪律

> 定位：**阶段一（规格步 1.0–1.2）的资产批**——出四件运行资产（域工作流／阶段文件／情绪词表／基本规格模板），并把 087 留的三条账（壳的降级纪律／菜单刷新纪律／实测环境前提）一并落掉；**建设状态通报归位到域工作流**（壳与阶段文件只引用，不复制）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**重路径**——动 `skills/` 域资产（架构级）→ **成稿审查 ＋ 核验三步 ＋ 双维产物审查 ＋ 真机走查**。scope：**skill 资产＝skill**、**标准件＝standards**、**产品层＝product**、**开发守卫＝scripts**（本批不动 `scripts/`）。
> 自由度分布：[A]×5 [B]×8
> 必读输入：本规格 §一；`docs/standards/skill形态标准.md` §1／§2／§3／§5／§6／§8／§9／§10／§11／§13；`docs/standards/触发与交互标准.md` §1／§2／§3；`docs/standards/命名标准.md` §1／§2／§5；`docs/standards/测试与验收标准.md` §3／§4；`docs/standards/版本与分发标准.md` §3／§7；`docs/product/outline/大纲域设计.md` §三／§四；`docs/product/outline/大纲域设计-规格与资料步.md`（1.0–1.2 为 F2 真源）；`docs/product/outline/大纲域设计-附录.md`（A 六件套／A-2 词表／B-1 模板）；`docs/product/书目录契约.md` §1／§2／§3／§4
> 施工形态：subagent（规划方 spawn 的施工 subagent；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 壳与资产目录现状 | `skills/abzu-outline/SKILL.md` **35 行**；`references/`／`assets/`／`scripts/` **均不存在**（三处 `ls -d` 皆 No such file）✓ |
| ② | 壳的建设状态现状 | 建设状态全文**两处**落壳：`:16` 回显句（「…（全文见本壳末建设状态段）」）与 `:35` 段正文（含「未建阶段不模拟、不顶替、不产出占位内容」）——本批按「只落一处、另两处引用」改为**只落工作流** ✓ |
| ③ | 阶段一真源体量与范围 | `大纲域设计-规格与资料步.md` **141 行**：`:12` `## 一、规格步（书的规格）`／`:14` 1.0／`:24` 1.1／`:50` 1.2／`:54` `## 二、资料收集步`；**1.0–1.2 连父标题的正确范围＝`:12`–`:52`**（`:14`–`:52` 不含父标题，成稿审查 F-02 订正）。`大纲域设计-附录.md` **206 行**（A 六件套 `:14`／A-2 词表 `:16`–`:27`，四轴计 **17 词**——8+4+2+3，成稿审查 F-08 订正）／`大纲域设计.md` **207 行** ✓ |
| ④ | **资产命名与 087 登记相抵（本批裁定）** | `命名标准` §一 反例（`:115`）明文禁「skill 资产用中文名」；`命名标准` §5 示例与 `skill形态标准` §9.1 示例用的都是 `references/outline-workflow.md` 这种 **`outline-` 前缀**形态，`skill形态标准` §6 定 `assets/{域}-{产物名}-template.md`。按 `AGENTS.md` §六 权威顺序（标准 ＞ 规格）**从标准取英文**，并把 087 §八 4 登记的 `outline-stage-规格与资料.md`（**含中文**）与 `assets/outline-基本规格-template.md`（**含中文**）分别改为 `outline-stage-spec.md` 与 `assets/outline-basic-spec-template.md`；**口径**：本仓 `{域}` token 取 `outline`（同 §5／§9.1 示例）；**作者 2026-09-15 裁定：skill 资产用英文名——照此办，`命名标准` 不改** ✓ |
| ⑤ | 壳的四处缺口（087 §八 7／9 与走查三则） | ①入口判定**无降级纪律**（`grep -o "读不到"` → **0**）②**无跨域意图告知**（`skill形态标准` §2.1 要求）③`## 工作流` 节（`:20`）仍写「随 088–091 落地——本节暂一句指认」④建设状态全文落壳两处（见 ②） ✓ |
| ⑥ | 目标件改前基线（除末条外全为 0，逐条口径＝`grep -o … \| wc -l`） | `SKILL.md` 内 `references/outline-workflow.md` **0**；`SKILL.md` 内 `读不到` **0**；`触发与交互标准` 内 `未建阶段` **0**；`版本与分发标准` 内 `重载窗口` **0**；`测试与验收标准` 内 `走查环境前提` **0**；`书目录契约` 内 `本阶段用得到` **0**；`大纲域设计` 内 `阶段一（规格步）` **0**；`命名标准` 内 `阶段一资产已落` **0** ✓ |
| ⑦ | `命名标准` 内 `最小壳` | **2 处**（`:34`／`:45`，087 落下）——本批 F11 **只改 `:45`**；`:34`（目录树注）不在点名落点内，留 §八 10 ✓ |
| ⑧ | 门禁现状 | `bash scripts/check.sh` exit **0**（`[2]` 对现有唯一壳「逐壳六项全过」；新目录 `references`／`assets` 在该段一层子目录白名单内，成稿审查已核）；lint **57 件** 0 issue（**含在制规格**——绝对数随在制规格浮动，本处只记当次值、不作断言）；链接一行式 **175／断链 0** ✓ |
| ⑨ | 087 §八 存量 ＋ 086 残余 | 087 §八 共 **11 条**；另 **086 §八 1／3／6** 三条的状态因本批首建 `assets/`／`references/` 而变化（086 §八 3 原文「本批只建壳，`references/` 未建（**088 起**）」）→ 一并入 §七 ✓ |

**不变量（施工前后须同）**：**改动面与禁改面逐件对账**——本批改动**全部落点**：`skills/abzu-outline/SKILL.md`（改）／`skills/abzu-outline/references/outline-workflow.md`（新建）／`skills/abzu-outline/references/outline-stage-spec.md`（新建）／`skills/abzu-outline/references/outline-emotion-words.md`（新建）／`skills/abzu-outline/assets/outline-basic-spec-template.md`（新建）／`docs/standards/触发与交互标准.md`（§2 补一款）／`docs/standards/版本与分发标准.md`（§3 第 6 步＋§7）／`docs/standards/测试与验收标准.md`（§3 走查做法）／`docs/standards/命名标准.md`（§1 现状句＋`:34` 目录树注与两行标注）／`docs/standards/skill形态标准.md`（§1 现状句）／`docs/product/书目录契约.md`（§1 正文＋§2 正例＋**§三 机检锚点行**＋**§4 机检契约行**）／`docs/product/outline/大纲域设计.md`（`:4`／`:22`／`:26`／`:197`／`:200`）／`docs/product/架构设计.md`（`:10` 状态列）／`docs/product/产品定义.md`（`:10`）／`CHANGELOG.md`；**除此之外零改动**（`AGENTS.md` 不动；`scripts/` 零改动；`docs/specs/` 除本规格归档外零改动；产品层**设计判据**零改动）。资产内**零 markdown 链接**（`命名标准` §5）；链接一行式 **175／断链 0** 不因本批变化（新增资产不含链接；F2 迁入的三条链接按 §5 改写为路径文字——见 F2）。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `skills/abzu-outline/references/outline-workflow.md`（新建） | **域工作流**，按 `skill形态标准` §6 workflow 模板出件：①**头部两行声明**（**逐字符格式**＝首行 `> 消费点：…`、次行 `> 边界：…`；F2／F3／F4 三件同此格式；消费点＝壳「工作流」节与各阶段文件，必读；边界＝本件只写 Stage 一览与挂载表，阶段细则归各阶段文件）②域角色定位一句＋核心信念（**不设流程衔接表**）③**Stage 1..4 各一条**，每条含「做什么／读什么／产出／异常分支／停靠点」五件——**本批只详写 Stage 1**（引阶段文件），Stage 2–4 各记一行「未建（随 089–091）」④参考资料与脚本挂载表（三列：文件 × 作用 × 加载时机）⑤**副作用声明**（`skill形态标准` §11：本域写用户书目录的 `创作进度.md`／`基本规格.md` 等产物；不调外部服务；无不可逆操作）。**另含**：**中断契约**（`触发与交互标准` §2.5 标准句式；句内 `/{skill 名}` 占位符**自包含解析**为「对应域命令（本套件三域：`/abzu-outline`／`/abzu-volume`／`/abzu-write`）」——**不得解析成本域命令**（核验 R1），**亦不得指向包外治理文档**（产物审查 V-05：装进宿主后包内不存在））／**「建设状态」节**（通报全文的**唯一落点**，文案**逐字钉死**见下）。行数目标 <300 行（实计约 70–100） | [B] | `skill形态标准` §6／§9.3／§11；`触发与交互标准` §2.5；087 §八 4 |
| F2 | `skills/abzu-outline/references/outline-stage-spec.md`（新建） | **Stage 1（规格步）阶段文件**：1.0 开书与脚手架／1.1 规格收集／1.2 规格门——**内容逐字迁 `大纲域设计-规格与资料步.md` `:12`–`:52`**（**含 `:12` 父标题**——成稿审查 F-02），产品层七字段单元序固定、禁压缩。**映射说明**（写进头部声明）：七字段与 §6 五件的对应＝作用→做什么／收集→读什么／产物→产出／异常与降级→异常分支／交互（门控停靠）→停靠点；`③怎么想` 与 `⑤过关标准` 为本域**增补字段**，不删。**头部声明另须含**：「建设状态通报引用 `references/outline-workflow.md`『建设状态』节，不复制」（成稿审查 F-12）。 | [B] | 087 §八 4；`大纲域设计` §四.1；`命名标准` §5 |
| F3 | `skills/abzu-outline/references/outline-emotion-words.md`（新建） | **统一情绪词表**（独立成文——作者 087 期裁定「词表 A-2 拆出来，这个例外」）：①头部两行声明（消费点＝1.1 规格收集的基调字段——**本批唯一上线消费点**；边界＝其余四处消费点随批次上线）②词表**逐字**迁 `大纲域设计-附录.md` `:20`–`:27`（四轴｜情绪感受／风格基调／深度／节奏，**共 17 词**）③取值规则逐字（选 2-4 词、词序即主次、跨轴整体主次与同轴局部主次、表外补充句、**开放集增词记变更**）④**待上线消费点清单**（参考标尺②情绪基调组／灵感库情绪爽点标签槽／评委问句的情绪判断／分卷五件套情绪弧线——各标「随 089–091」），**不写未建阶段的做法** | [B] | 087 §八 4；`大纲域设计` §四.9 |
| F4 | `skills/abzu-outline/assets/outline-basic-spec-template.md`（新建） | **基本规格产物模板**，按 `skill形态标准` §6 四段式出件（①定位注记：产物是什么／何时创建／生产者／消费方 ②字段定义表：字段／说明／生产来源 Stage／必填性 ③默认值与完整填写示例 ④模板正文，`{____}` 填空与 `{A/B}` 枚举）＋**三要素齐**（字段定义／示例行／约束）。内容真源＝附录 **B-1 五段式**（`:31`）＋附录 **A 六件套**（`:14`），**六件套逐项装进四段式**：字段定义表并入枚举与必填；默认值及推导入③；填写引导话术入④的对应段；示例行入③；联动校验规则入④的约束位。占位符按 `skill形态标准` §10.3（`{____}`／`{A/B}`／业务变量中文如 `{书名}`）。**不设行数上限**（`assets/` 不计预算） | [B] | 087 §八 4；`skill形态标准` §6／§10.3 |
| F5 | `skills/abzu-outline/SKILL.md`（4 处） | ①**建设状态全文移出壳**：`:16` 回显句改「进域先向作者报出一行建设状态（作者据此判断壳是否被加载）；**该行文案的真源在 `references/outline-workflow.md`『建设状态』节**——按该节取当前档位，本壳不复制其正文」，`:35` 段正文**删**（并入 F1）②`## 工作流` 节（`:20`）改「四阶段流程（规格、资料、草稿、打磨定稿）。Stage 一览与各 Stage 的做什么／读什么／产出／异常分支／停靠点见 `references/outline-workflow.md`；本批 Stage 1（规格步）细则见 `references/outline-stage-spec.md`。**本壳不复制其内容。**」③**入口判定补降级纪律**（逐字）：「**读不到项目文件时（本会话无文件工具或无读取权限）：先报建设状态行 → 如实说明读不到 → 停下请作者换带文件工具的会话；不得以作者口述替代文件证据，不得用联网工具探测本地路径。**」④**入口判定补跨域意图告知**（逐字；域名**自包含**内联、不指向包外文档——产物审查 V-05）：「作者表达其他域意图时，如实告知对应命令与建设状态（本套件三域：`/abzu-outline`／`/abzu-volume`／`/abzu-write`），不强行走完本域流程。」 ——改后行数仍 <300（现 35 行） | [A] | 087 §八 7／9；`skill形态标准` §2.1 |
| F6 | `docs/standards/触发与交互标准.md`（§2 补一款） | §2 末补第 7 款（逐字）：「**未建阶段如实告知**：域已建但阶段未建时，进域先报该域建设状态；未建阶段不模拟、不顶替、不产出占位内容。建设状态通报只落**域工作流**一处，壳与阶段文件引用、不复制。」 | [A] | 087 §八 2（触发＝本批） |
| F7 | `docs/standards/版本与分发标准.md`（2 处） | ①§3 第 6 步（`:52`）句尾补（逐字）：「（**装后须在宿主中重载窗口**——`/` 菜单在窗口激活时取一次命令快照，同窗口内新装或新删的 skill 不补入）」②§7 发布渠道分层（`:74`）句尾补（逐字）：「**装后须在宿主中重载窗口**，`/` 菜单才会列出新装的 skill」 | [A] | 087 §八 10（走查发现） |
| F8 | `docs/standards/测试与验收标准.md`（§3 走查做法） | §3 补**实测环境前提**两条（逐字）：「**走查环境前提**：①用**会直接调用核心工具**（读文件／执行命令类）的模型会话——已知有模型会自述「本会话没有文件工具」而实际具备（其只检索延迟加载面即下断言），此时应换模型重试并记录 ②`-p` 无头模式**不展开**斜杠命令（当路径处理），真机走查只在**交互面板**内做。」 | [A] | 087 §八 11（走查发现） |
| F9 | `docs/product/书目录契约.md`（3 处） | ①§1 正文（`:22`）「立项步建齐目录与空文件」→「立项步建齐**本阶段用得到**的目录与空文件（阶段一完成时含 `创作进度.md`／`基本规格.md`），其余随各自阶段批建齐」②§2 正例行（`:72`）→「✅ 立项门控确认前：`创作进度.md` 与 `基本规格.md` 存在且为空模板（**本阶段用得到**的落点；不越阶段）」③§三 机检锚点行（`:87`）「§1 书目录骨架齐备（**五件**目录与空文件）」→「§1 书目录骨架齐备（**本阶段用得到**的目录与空文件）」④§4 机检契约行（`:49`）「立项步建齐目录与空文件：…五件…」→「立项步建齐**本阶段用得到**的目录与空文件（阶段一＝`创作进度.md`／`基本规格.md`）」（③④ 皆**条目失效同批更新**；④ 由产物审查观察转入本批） | [B] | F2 的阶段化交付；`测试与验收标准` §3 记录约定 |
| F10 | 产品层三件（7 处） | `docs/product/outline/大纲域设计.md`（`:4`／`:22`／`:26`／`:197`／`:200`）与 `docs/product/架构设计.md`（`:10` 状态列「**最小壳（087）**」）与 `docs/product/产品定义.md`（`:10`「**大纲域最小壳已建（087，试验性）**」）的状态句统一改「**阶段一（规格步）资产已建（088）**」（`大纲域设计:197` 的「分流待裁定」按本批既已执行的分流改述：模板进 `assets/`、词表与方法论进 `references/`） | [B] | ④ 状态真源同批同步；成稿审查 F-05 |
| F11 | `docs/standards/命名标准.md`（§1 现状句） | §1「现状与预留」段（`:45`）**只换前两句**、**第三句原样保留**（「下表 skill 资产类目与插件清单类目为预留——规则先行…」）：前两句 →「`skills/` 已建（`abzu-outline`——**阶段一资产已落**：工作流／阶段文件／情绪词表／产物模板）；`.claude-plugin/`（分发清单）尚未建立」；另 `:34` 目录树注改「首个域——阶段一资产已建（088）」（**纯文本、不带加粗标记**——树在 text 围栏内，加粗会渲染成字面星号且与兄弟行体例不一；补修偏差 a 裁定支。串与 §1 不同，保断言 8f 仍为 1），树内 `references/`／`assets/` 两行**去「随各批落地」**、`scripts/` 行保留 | [A] | ④；成稿审查 F-09；产物审查观察④ |
| F13 | `docs/standards/skill形态标准.md`（§1 现状句） | §1 现状句（`:21`）「`skills/` 已建（`abzu-outline` 最小壳）」→「`skills/` 已建（`abzu-outline`——**阶段一（规格步）资产已建（088）**）」（§八 12 由本批转入） | [A] | ④；产物审查观察邻域收口 |
| F12 | `CHANGELOG.md`（`### Added` 段首） | 一行：『阶段一资产与降级纪律（088）：…』——四件资产／建设状态通报归位／087 三条账；**≤400 字符**、末尾「核验：」句 | [B] | 过程产物 |

**F1『建设状态』节逐字**（通报的**唯一落点**；产物审查 V-03 裁定**拆两句**——作者可见句不得用批号与内部术语，模型纪律句不报给作者）：

- ① **报给作者的一行**（建设期诊断行，作者据此判断壳是否被加载）：「大纲域现在只能走**规格**这一段——把这本书的规格聊清楚、定下来；采资料、写大纲、打磨定稿这几步还没做，我不会假装能做。」
- ② **模型侧纪律**（不报给作者）：「**未建阶段不模拟、不顶替、不产出占位内容**；规格门确认后**只落阶段值并报该步尚未落地，不进入资料收集**。」（「尚未落地」刻意不与 Stage 行的「未建」同串，见 口径要点⑤）

**F2 五处必改**（不属「逐字」范围，改法已定）：① **源件自带 3 条 markdown 链接**（`:19` 书目录契约／`:39` 附录 B-1／`:45` 附录 A-2）按 `命名标准` §5 改写为 **skill 根相对路径文字＋节名**（如「见 `references/outline-emotion-words.md`」），**逐条改写、不保留链接语法**，改写项记入 §九差异 ② **1.0 ④产物**补 `创作进度.md` 的**最小格式**（头部两行＝当前阶段＋产物状态表标题；三列表＝产物名｜状态｜备注；阶段值取五值枚举——真源 `书目录契约` §2，**内联为 ≤6 行骨架**，成稿审查 F-18）③ **1.0 交付物落点**按 F9 的阶段化口径（本阶段＝`创作进度.md`＋`基本规格.md`）④ **用户问句的模态标注按 `触发与交互标准` §3.5／§3.6 收口**（实为 `:26`／`:48`／`:50`——规格原写 `:46` 对不到唯一行，补修偏差 c 订正；**并外延至 1.2 `:57` 同族一处，已照准**：前缀式「选项列表「…」」→ 问句后置「（选项列表）」；「（对话）」→「（对话式）」；「（选项列表≤3+对话≤3）」的混合标注按单一模态拆开）——**话术文字仍逐字，只动模态标注与选项呈现形态**，差异记 §九（产物审查 V-04）⑤ **1.1④ 完备校验的例外清单补一项**：「`读者避雷点` 1.1 留位、**不计入本步完备校验**（随采风补）」——否则照章办事的模型会在 1.1 反复追问或编造该值（产物审查 V-07；2.x 本批未建）

**口径要点（易错）**：① **F5 四处与 F6／F7／F8／F11 逐字**；② **建设状态全文只落一处**（`skills/` 内该串出现次数＝**1**，在 F1；壳内为 **0**）——断言 4 的判据**分文件计数**（壳内 0／全目录 1），F1 目标文本自带该串不构成自噬；③ 资产命名**从标准取英文**（§一 ④，与 087 §八 4 登记不同——核销行已记理由）；④ 资产内**零 markdown 链接**（`命名标准` §5）；⑤ **F1 建设状态节的模型纪律句用「尚未落地」（不带括注）、Stage 2–4 行用「未建（随 089–091）」**——两串互不包含，断言 7b 才可安全计数（成稿审查 F-14 同族防御；V-03 拆句后括注已去，补修偏差 b 订正）；⑥ 计数断言逐条标注口径（行 vs 出现次数 vs 文件数）。

## 三、验收断言（一条命令一期望；计数口径逐条标注）

> **真空基线说明**：断言 1／2／3／6／7 的「预验基线 0」皆因**目标文件尚不存在**，不构成「模式正确」的证据——这五条的**改后侧**由核验方逐条独立重跑（`规格写作标准` §5.1）。

### 断言 1｜四件资产就位与头部两行声明

```bash
for f in references/outline-workflow.md references/outline-stage-spec.md references/outline-emotion-words.md assets/outline-basic-spec-template.md; do test -f "skills/abzu-outline/$f" && echo ok; done | wc -l
```

期望 **4**（**计行**）。**预验基线 0**（两目录均不存在）。

```bash
grep -rl "^> 消费点：" skills/abzu-outline/references/ skills/abzu-outline/assets/ | wc -l
```

期望 **4**（**计文件数**——漏写者不计入，故用 `-l` 计数而非 `-rc` 过滤）。**预验基线**：目录不存在。

```bash
grep -rl "^> 边界：" skills/abzu-outline/references/ skills/abzu-outline/assets/ | wc -l
```

期望 **4**（**计文件数**）。**预验基线**：目录不存在。

### 断言 2｜阶段文件为逐字迁移（抽查签名句）

```bash
grep -o "作家翻开新笔记本拍掉封膜\|先把你想写的这本书聊聊\|规格定了吗？定了就开始采风。" skills/abzu-outline/references/outline-stage-spec.md | wc -l
```

期望 **3**（**计出现次数**——1.0／1.1／1.2 各一句签名）。**预验基线 0**。

```bash
grep -c "^## 一、规格步\|^### 1.0 \|^### 1.1 \|^### 1.2 " skills/abzu-outline/references/outline-stage-spec.md
```

期望 **4**（**计行**——含 `:12` 父标题，范围 `:12`–`:52` 自洽）。**预验基线 0**。

```bash
grep -c "^### 2\.\|^## 二、资料收集步" skills/abzu-outline/references/outline-stage-spec.md
```

期望 **0**（**计行**——2.x **不在本批**，禁提前迁入）。**预验基线 0**。

```bash
grep -c "](" skills/abzu-outline/references/outline-stage-spec.md
```

期望 **0**（**计行**——源件 3 条链接已按 `命名标准` §5 改写为路径文字）。**预验基线**：文件不存在。

### 断言 3｜词表逐字与消费点清单

```bash
grep -o "热血燃、爽快、解压、轻松、搞笑沙雕、治愈、悲壮、致郁\|白爽直给、正剧、史诗厚重、暗黑\|深度内核、烧脑\|快节奏强钩、张弛有度、慢热\|词序即主次" skills/abzu-outline/references/outline-emotion-words.md | wc -l
```

期望 **5**（**计出现次数**——**四轴词串全列**＋取值规则；成稿审查 F-07 补足两轴）。**预验基线 0**。

```bash
grep -c "^| 情绪感受 \|^| 风格基调 \|^| 深度 \|^| 节奏 " skills/abzu-outline/references/outline-emotion-words.md
```

期望 **4**（**计行**——四轴表行齐）。**预验基线 0**。

```bash
grep -o "随 089–091" skills/abzu-outline/references/outline-emotion-words.md | wc -l
```

期望 **≥1**（未上线消费点标注；**计出现次数**）。**预验基线 0**。

### 断言 4｜建设状态全文只落一处

```bash
grep -o "未建阶段不模拟、不顶替、不产出占位内容" skills/abzu-outline/SKILL.md | wc -l
```

期望 **0**（**计出现次数**——壳内已改为引用；改前 **1**）。**预验基线 1**。

```bash
grep -rlo "未建阶段不模拟、不顶替、不产出占位内容\|只落阶段值并报该步尚未落地" skills/abzu-outline/ | wc -l
```

期望 **1**（**计文件数**——唯一落点＝工作流件）。**预验基线 1**。

### 断言 5｜壳的三处新句与一根指针

```bash
grep -o "references/outline-workflow.md" skills/abzu-outline/SKILL.md | wc -l
```

期望 **≥2**（**计出现次数**——工作流节＋建设状态指针）。**预验基线 0**。

```bash
grep -o "不得以作者口述替代文件证据，不得用联网工具探测本地路径" skills/abzu-outline/SKILL.md | wc -l
```

期望 **1**（降级纪律；**计出现次数**）。**预验基线 0**。

```bash
grep -o "如实告知对应命令与建设状态" skills/abzu-outline/SKILL.md | wc -l
```

期望 **1**（跨域意图告知；**计出现次数**）。**预验基线 0**。

```bash
wc -l < skills/abzu-outline/SKILL.md
```

期望 **<300**（`skill形态标准` §5.1 目标）。**预验基线 35**。

### 断言 6｜模板四段式与五段式

```bash
grep -c "^## " skills/abzu-outline/assets/outline-basic-spec-template.md
```

期望 **≥4**（四段式；**计行**）。**预验基线 0**。

```bash
grep -o "基础参数\|基调风格\|参考偏好\|参考标尺\|故事直觉" skills/abzu-outline/assets/outline-basic-spec-template.md | wc -l
```

期望 **≥5**（五段式段名齐；**计出现次数**）。**预验基线 0**。

### 断言 7｜工作流五件、中断契约与 Stage 计数

```bash
grep -o "用户中途要求停止或表达其他域意图时，确认当前进度已落盘后移交对应域" skills/abzu-outline/references/outline-workflow.md | wc -l
```

期望 **1**（中断契约逐字自 `触发与交互标准` §2.5；**计出现次数**）。**预验基线 0**。

```bash
grep -c "未建（随 089–091）" skills/abzu-outline/references/outline-workflow.md
```

期望 **3**（**计行**——Stage 2／3／4 各一行，本批只详写 Stage 1；本件「建设状态」节按 §二 口径要点⑤ 用「尚未落地」，不撞此串）。**预验基线 0**。

```bash
grep -o "不调外部服务；无不可逆操作" skills/abzu-outline/references/outline-workflow.md | wc -l
```

期望 **1**（副作用声明；**计出现次数**）。**预验基线 0**。

```bash
grep -o "建议使用对应域命令（本套件三域：" skills/abzu-outline/references/outline-workflow.md | wc -l
```

期望 **1**（中断契约的 `/{skill 名}` 占位符已**自包含**解析为对应域命令，且不指向包外文档；**计出现次数**）。**预验基线 0**（现形态写作本域命令，须由补修轮改）。

### 断言 8｜四处标准件与产品层三件

```bash
grep -o "未建阶段不模拟、不顶替、不产出占位内容" docs/standards/触发与交互标准.md | wc -l
```

期望 **1**（§2 新款；**计出现次数**）。**预验基线 0**。

```bash
grep -o "重载窗口" docs/standards/版本与分发标准.md | wc -l
```

期望 **2**（§3 第 6 步＋§7 各一，两处均**逐字**给定；**计出现次数**）。**预验基线 0**。

```bash
grep -o "走查环境前提" docs/standards/测试与验收标准.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "本阶段用得到" docs/product/书目录契约.md | wc -l
```

期望 **4**（§1 正文＋§2 正例＋§三 机检锚点行＋§4 机检契约行各一；**计出现次数**）。**预验基线 0**。

```bash
grep -o "阶段一（规格步）资产已建（088）" docs/product/outline/大纲域设计.md docs/product/架构设计.md docs/product/产品定义.md | wc -l
```

期望 **≥3**（三件各有；**计出现次数**）。**预验基线 0**。

```bash
grep -o "阶段一资产已落" docs/standards/命名标准.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

### 断言 9｜零外部依赖、零资产内链接与门禁

```bash
grep -rc "](" skills/abzu-outline/ | grep -v ":0$"; echo "扫毕"
```

期望：仅「扫毕」（**资产内零 markdown 链接**）。**预验基线**：仅壳（无链接）→「扫毕」。

```bash
bash scripts/check.sh
```

期望 exit **0**（含 `[2]` 段对 `references`／`assets` 目录形态的校验）。**预验基线 0**。

```bash
bash scripts/check.sh 2>&1 | grep -c "^\[2\] "
```

期望 **1**（**恰段标题行**）。**预验基线 1**。

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**（件数不设期望——**含在制规格**，随批内状态浮动）。**预验基线 0 issue**。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**（新增资产零链接，故不增）。**预验基线 175‧0**。

### 断言 10｜改动面

```bash
git status --porcelain | wc -l
```

期望 **14**（**逐类列举**：**11 件 tracked M**＝`skills/abzu-outline/SKILL.md`／`触发与交互标准`／`版本与分发标准`／`测试与验收标准`／`命名标准`／`书目录契约`／`skill形态标准`／`大纲域设计`／`架构设计`／`产品定义`／`CHANGELOG` ＋ **3 行未跟踪**＝`?? skills/abzu-outline/references/`／`?? skills/abzu-outline/assets/`（git 对未跟踪目录各记 1 行，不展开其下文件）／`?? docs/specs/088-…md`）。**预验基线 1**（仅本规格未跟踪）；施工后**逐行列核验记录**（行数与分组若与实况不符，**以实况为准并在核验记录里订正本行**——口径歧义先例见 082／087）。

```bash
ls skills/abzu-outline/scripts 2>&1 | grep -c "No such file"
```

期望 **1**（本批**不建** `scripts/`）。**预验基线 1**。

### 断言 11｜真机走查（作者执行）

**判据**（阶段一主流程一遍走完）：① 敲 `/abzu-outline` → 报建设状态行（且该行文案与 `references/outline-workflow.md`『建设状态』节一致）② 空目录下走 **1.0**：建骨架（本批口径＝`创作进度.md` ＋ `基本规格.md`，**后者格式须合 F2 内联的最小骨架**）并通报 ③ 走 **1.1**：三段式（段一对话／段二回读／段三段≤3 选项列表）问齐字段，基调字段**给的是词表里的词** ④ **1.2 规格门**停住等确认；**确认后只落阶段值并报「资料收集尚未落地」，不得进入 2.x**。**环境前提**：按 F8 落进 `测试与验收标准` §3 的两条（换会直接调核心工具的模型；只在交互面板跑）。**负结果预案**：若某步与产品层设计不符 → 落 §八，本批或下批处置。

**走查记录模板**（落 §九）：宿主版本｜走查日期｜四步逐项结论｜发现的不一致｜安装落点。

### 断言 12｜过程产物

```bash
grep -cF '阶段一资产与降级纪律（088）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（三条）

1. **锚点现场核过**：§一 九条实跑取证（①三处目录缺失；②两处建设状态；③四件行数与 `:12`–`:52` 范围、四轴 17 词逐轴点算；⑥ 八条改前基线全 0；⑦ `最小壳` 2 处；⑧ 门禁三命令；⑨ 087 §八 11 条＋086 §八 3 条逐条读原文；④ 命名标准 `:54`／`:56`／`:115` 与 §5 示例、`skill形态标准` §9.1 示例实读）。
2. **断言实跑过**（改前侧）：断言 1–12 的 0 侧全部实跑（资产不存在／八处目标串 0／`未建阶段…` 壳内 1／porcelain 1／exit 0／lint 0／链接 175‧0）。**互斥性**：断言 4 分文件计数（壳 0／全目录 1）与 F1 目标文本已互斥；断言 7 的 `未建（随 089–091）` 与 F1『建设状态』节的「尚未落地（随 089–091）」**刻意不同串** ✓；**计数口径逐条标注** ✓；**真空基线五条已标注**（见 §三 前言）✓。
3. **禁止事项已写明**：见 §五。**[4] 段预警**：本规格在制时实跑有候选（新文件未建所致），逐条属预期。

## 五、禁止事项

- 禁止触碰：`.git/`、`AGENTS.md`、`README.md`、`scripts/`（含 `check.sh` 与 `check_spec.py`）、`docs/specs/` 除本规格归档外全部（含 `archive/`）、产品层**设计判据**、`docs/product/` 与 `docs/standards/` 除 F6–F11 点名面外的内容（**点名面＝F9 的 `书目录契约` 三处／F10 的 `大纲域设计` 五处与 `架构设计:10` 与 `产品定义:10`／F11 的 `命名标准` §1 两句**）。
- **不得**把产品层设计文档的「设计语言」（设计理由、旧仓教训）搬进 skill 资产。**例外（产物审查 V-01 裁定）**：步骤单元 `①作用` 内的**心流锚句**属产品层七字段的**格式部件**（`大纲域设计` §四.1 定其格式），**不在「设计语言」之列**——随 F2 逐字迁入。
- **不得**把 2.x（资料收集步）内容提前迁入 F2 阶段文件；**不得**为未建阶段写做法（只写「未建（随 089–091）」一行）。
- **不得**在资产内写 markdown 链接或裸文件名（`命名标准` §5：skill 根相对路径 + 节名）；F2 的 3 条链接改写项**须记入 §九差异**，不得静默改写。
- **不得**复制建设状态正文到壳或阶段文件（唯一落点＝F1）。
- **不得**改动 F11 目标段的**第三句**（「下表 skill 资产类目与插件清单类目为预留——规则先行…」原样保留）。
- 禁止动作：任何 git 写命令；联网；安装依赖；在仓库内跑安装器或把 `skills/` 复制到宿主（走查由作者在**仓库外**做）。
- 禁止改断言、改数字、删检查来变绿。

## 六、被否决的替代方案

- **沿用 087 §八 4 登记的中文资产名**（`outline-stage-规格与资料.md` 等）→ 否决：与 `命名标准` §一 反例相抵，按 `AGENTS.md` §六 权威顺序从标准；同时「一件覆盖两阶段」与 `skill形态标准` §8.1（Stage 整数编号、一 Stage 一名）不合。
- **F2 照搬 3 条链接**（真「逐字」）→ 否决：资产内链接必断且违 `命名标准` §5／`skill形态标准` §9.2；改「逐字迁内容 ＋ 3 条链接按 §5 改写并留痕」。
- **088 一次做完「规格＋资料」整步（含 2.x）**→ 否决（作者 087 期既定节奏：先做第一个阶段、实测后再铺下一阶段；且阶段一收窄后四件资产自然变成「三 references ＋ 一 assets」）。
- **1.0 建齐契约要求的五件空模板** → 否决：与壳的「未建阶段不产出占位内容」相抵；改走 F9 的**阶段化**（契约同步改述）。
- **把三条账另立轻批** → 否决：F5 本就在改壳与工作流，三条账与 F5／F6／F8 同面，合并即一次施工一次审查。
- **建 `skills/abzu-outline/scripts/` 域级守卫**（`书目录契约` §4 的机检点）→ 本批否决：守卫属「无事故出身的注册表类」邻域，须先登记事故出身（`AGENTS.md` §四）；本批只出资产，守卫另议（§八 3）。

## 七、本批核销（必列面：前一批全量 ＋ 本批触及者）

> **必列面**（`施工机制` §六）：① 087 §八 **全 11 条**；② 本批触及或状态可能变化者——**086 §八 1／3／6**（本批首建 `assets/`／`references/`、并做链接改写）。**不再重列**：086 §八 2／4／5／7（已由既往核销表承载、本批未使其状态变化——索引指针＝`087` §七）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 087 §八 1 | `AGENTS.md` 两处状态句（`:5`／`:9`） | **待作者** | 须作者确认；本批不动 `AGENTS.md`（§五 禁改面） |
| 087 §八 2 | 「未建阶段如实告知」缺标准锚 | **本批处置** | **F6**（`触发与交互标准` §2 补第 7 款） |
| 087 §八 3 | 走查的负结果路径 | **已履行** | 087 走查判据③：宿主接受该键，负路径未触发 |
| 087 §八 4 | 088 的四件资产要求 | **本批处置** | **F1–F4**；命名按 `命名标准` 改英文（§一 ④） |
| 087 §八 5 | `运行环境标准` §4 与 `AGENTS.md` §四 例外句 | **已履行** | 087 内：`[2]` 段改纯本地后无新例外（已销） |
| 087 §八 6 | 四处安装命令装旧壳 | **本批处置** | A 案推送后远端 `main` ＝新仓，命令装到的即新壳（087 走查实证）→ 命令本身**无需改**；只补「装后重载窗口」（**F7**）。**残余**（是否补 `-a claude-code`／`-g`）见 §八 8 |
| 087 §八 7 | `skill形态标准` §2.1「跨域意图告知」未落壳 | **本批处置** | **F5④** |
| 087 §八 8 | F3 行为要求的验收落点 | **已履行** | 087 真机走查完成（空目录端到端跑通） |
| 087 §八 9 | 壳的降级纪律缺口 | **本批处置** | **F5③** |
| 087 §八 10 | 菜单刷新纪律未落真源 | **本批处置** | **F7** |
| 087 §八 11 | 实测环境前提未成文 | **本批处置** | **F8** |
| 086 §八 1 | `assets/` 内资源类细分 | **仍待** | 本批只落 1 件模板，未到触发线（>15 件或出现非模板类资源） |
| 086 §八 3 | `references` 平铺后的单目录容量 | **仍待** | 本批落 3 件，未到触发线（单域 reference >10 件） |
| 086 §八 6 | 机检锚点措辞两处待收口 | **仍待** | `:169` 已随 087 收口；`:174`（references／assets 内相对链接扫描）本批有断言 9 覆盖、**未入守卫**——随 `[3]` 内容轨检查器建设 |

- 汇总（恒列五档、无该档计 0）：`已履行 3｜作废 0｜本批处置 7｜仍待 3｜待作者 1 ＝ 14` ✓

## 八、开放项

1. **`AGENTS.md` 两处状态句**（087 §八 1 转记）：`:5`／`:9` 与实况（已建一个域的阶段一资产）不符——触发＝作者裁定（`AGENTS.md` 须作者确认）。
2. **词表另四处消费点**（F3④）：随 089–091 各批上线时补入该件消费点清单（并删「随 089–091」标注）。
3. **域级守卫未建**（`书目录契约` §4 五条机检点全标「待建守卫」）：建守卫前须先登记事故出身（`AGENTS.md` §四），触发＝作者裁定或同类事故复发。
4. **`验收场景.md` 的真机步骤**：本批 F8 只把环境前提落在 `测试与验收标准` §3，`验收场景` §三 的逐步命令未同步——触发＝下批触碰该件时同批补。
5. **2.x 阶段文件**：`references/outline-stage-materials.md` 类名与内容随 089 定（本批 F2 只到 1.2）。
6. **`skill形态标准` §13 矩阵**未列「阶段文件」类目行（现由「方法论 `{域}-{主题}.md`」行承载）：触发＝矩阵下次修订时评估加行。
7. **B-2…B-10 九件模板**未落 `assets/`：随各自阶段批（089–091）出件。
8. **仓内安装命令的措辞精确化**（087 §八 6 残余）：`README.md:8`／`docs/product/验收场景.md:19`／`docs/standards/版本与分发标准.md:52`／`:74` 四处是否补 `-a claude-code`／`-g`——本批只补「装后重载窗口」（F7）；措辞精确化触发＝作者裁定（`README.md` 属禁改面）。
9. **`大纲域设计` 「常见问题」节**（`:204`）仍为零条留位：触发＝首批真机走查后补录。
10. ~~`命名标准` `:34` 与目录树三行~~ —— **已履行**（本批补修处置：F11 第二处改 `:34` 与目录树两行；规格原文写「触发＝下批收口」，双维产物审查标出后**转入本批补修**，见 §九）。
11. ~~`书目录契约` §4 机检契约行（`:49`）~~ —— **已履行**（本批补修处置：F9④，断言 8d 期望同步为 4；同上转入本批补修）。
12. ~~`skill形态标准` §1 现状句（`:21`）~~ —— **已履行**（本批补修处置：新 F13；同上转入本批补修）。
13. **标准缺款：运行时资产引用治理文档的格式口径**（产物审查 V-05）：`命名标准` §5 与 `skill形态标准` §9.1 只规定「skill 根相对路径＋节名」，未规定引用**仓库治理文档**（`书目录契约`／`架构设计`／`命名标准`）时怎么写——此类指针在装进用户项目后包内不存在。本批已把**可执行**的一处（中断契约）改自包含；其余四处为**追溯性指针**（设计真源标注），待立规。触发＝下批触碰上述两标准件时补款（口径建议：可执行事实须自包含；追溯指针写「设计真源：X §N」，并注明运行时不必加载）。
14. **1.1 段一/段二对模板字段的覆盖**（产物审查观察②）：`凭什么火`／`借鉴侧重`／`读者定位`／`故事直觉` 四个必填字段在模板里标「生产来源＝1.1 段一／默认推导」，但段一话术无专属问句、靠 `:51` 收拢式重问兜底——不同模型完整度会分叉。触发＝下批（089）在 1.1⑥ 明写「段二回读后按模板字段表盘点一次缺项清单」（属产品层上游缺口，本批不发明话术）。
15. **迁移件的数字来源标注与情态词**（产物审查观察⑦⑧）：四件资产的数字全部逐字来自产品层（含平台默认值），未逐个加〔书证／实测／自定〕；模板 `:30`「禁百分比」的「禁」不在 `skill形态标准` §10.1 三级词表；`SKILL.md:31` 087 遗留的「须」同类。触发＝立「迁移件是否豁免 §10.2／§10.1」的口径时一并处置。
16. **088 真机走查未执行**（收口时登记）：断言 11（1.0→1.1→1.2 四步）未跑；已有真机证据仅一条（V-05 现场确认，见 §九）。触发＝**089 之后或下一个域建成之前**必须补跑（`测试与验收标准` §3 第 ④ 条：走查欠账不得超过下一个域建成）。

## 九、审查记录

- 2026-09-15｜**成稿审查·第一轮**（独立上下文只读 subagent）｜findings 共 **18**：**P0:1** P1:5 P2:12 ＋观察 7｜结论 **修订后通过**。审查方**独立实跑全部断言改前侧与全部 §一 锚点**（含逐轴点算词数、复算链接后果、共 51 次工具调用），并据 `施工机制` §六 核销规则（五档／汇总恒列／不得与表列不符）判 §七 不合格。处置（**全部照改**）：
  - **F-01（P0）**：F2「逐字迁」的源范围自带 **3 条 markdown 链接**（`:19`／`:39`／`:45`），照迁即违「资产内零链接」并致断言 9 预期由 `175 0` 变 `178 3`（3 条必断）→ 改「逐字迁内容 ＋ 3 条链接按 `命名标准` §5 改写为路径文字 ＋ 留痕 §九」。
  - **F-02（P1）**：F2 范围 `:14`–`:52` **不含** `:12` 父标题，而断言 2 期望 4 是按含 `:12` 的源件算的 → 范围改 `:12`–`:52`（锚点与期望同源）。
  - **F-03（P1）**：§七 汇总算式 `1+0+7+1+1＝10≠11`，且「已销」不在五档内 → 两行改**已履行**，汇总按恒列五档重写。
  - **F-04（P1）**：核销**必列面②**漏 **086 §八 1／3／6**（`references` 容量条的原文触发即写着「088 起」）→ 补三行 ＋ 给既往核销表索引指针。
  - **F-05（P1）**：`架构设计:10` 与 `产品定义:10` 承载同一状态事实而未同步、亦未落 §八 → **纳入 F10**（状态真源同批同步）。
  - **F-06（P1）**：F1 漏 `skill形态标准` §11 的**副作用声明**硬约束（首个 workflow 件落地即 live）→ F1 补 ⑤ ＋ 必读输入补 §11 ＋ 断言 7 补一条。
  - **P2 十二条**：F-07（断言 3 漏两轴 → 四轴全列＋表行计数）／F-08（词数 16 → **17**）／F-09（F11 替换范围未限定 → 明写「只换前两句、第三句原样保留」并入禁改面）／F-10（F9②③ 无目标文本 → 给逐字＋断言 8 改计 3）／F-11（`书目录契约` §三 机检锚点行未同步 → 纳入 F9③）／F-12（F6 的「阶段文件引用」义务无落点 → 写入 F2 头部声明）／F-13（断言 1 第二条无定量期望且静默过滤 → 改 `grep -rl` 计文件数）／F-14（断言 7 判据串可能与 F1『建设状态』节撞车 → F1 该节**逐字钉死**为「尚未落地（随 089–091）」）／F-15（F7② 无逐字致期望 2 不稳 → 两处均给逐字）／F-16（lint 件数 56 → **57**，并注明随在制规格浮动）／F-17（§一 ④ 引 087 §八 4 的第二个文件名失真 → 引用原文名）／F-18（`创作进度.md` 格式无承载件 → F2 1.0 内联最小骨架）。
  - **观察采纳三条**：① 命名裁定改引 `命名标准` §5 与 `skill形态标准` §9.1 示例 ＋ 存「本仓 `{域}` token 取 `outline`」口径（§一 ④）② 1.2 门后行为无处安 → 写进 F1『建设状态』节逐字（「只落阶段值并报该步尚未落地，不进入资料收集」）＋断言 11④ ③ §二 表头**回归 `施工机制` §八 模板列序**（本轮起止连续两批偏离的纠正）。
  - **登记一条**：087 §八 6 的残余（四措辞精确化）→ §八 8。
- 2026-09-15｜**作者裁定**｜① **命名：skill 资产用英文名**（作者原话「我不坚持使用中文名，在 skill 中应该用英文才对吧」）——§一 ④ 的「从标准取英文」获确认，`命名标准` 与 `skill形态标准` 均**不改** ② 三处默认口径（只做阶段一／1.0 只建本阶段落点／087 三条账并入本批）作者未提异议，照此施工。
- 2026-09-15｜**施工**（subagent）｜**完成**：四件资产（`skills/abzu-outline/references/outline-workflow.md` 52 行／`references/outline-stage-spec.md` 57 行／`references/outline-emotion-words.md` 24 行／`assets/outline-basic-spec-template.md` 99 行）＋壳 4 处改写（37 行）＋标准件四处（`触发与交互标准` §2 第 7 款／`版本与分发标准` §3 第 6 步与 §7／`测试与验收标准` §3 环境前提两条／`命名标准` §1 现状句前两句）＋`书目录契约` 3 处＋产品层 7 处（`大纲域设计` 5／`架构设计:10`／`产品定义:10`）＋`CHANGELOG` 一行（284 字符）。**F2 三条链接改写留痕**（断言 2d 归零；按**描述式**记目标、不写链接语法——写了即成本批断链，见规划方核验 R2）：`:19` 源件为链接（目标 `书目录契约.md`、显示「书目录契约」＋「§1」）→ 改写为「`书目录契约` §1」；`:39` 源件为链接（目标 `大纲域设计-附录.md`、显示「附录」＋「B-1」）→ 改写为「`assets/outline-basic-spec-template.md`」；`:45` 源件为链接（同目标＋「A-2」）→ 改写为「`references/outline-emotion-words.md`「取值规则」节」。**迁移实核**：与源件 `:12`–`:52` 逐行 diff，差异仅上述三条＋1.0 ④ 的阶段化落点与 `创作进度.md` 最小骨架（4 行，≤6 行）＋文末换行；其余逐字未动。**断言实测**：1–12 全过（1 四件就位 4／消费点 4／边界 4；2 签名句 3／标题行 4／2.x 行 0／链接 0；3 词串 5／四轴行 4／随 089–091 出现 4；4 壳内 0／全目录文件数 1；5 指针 2／降级句 1／跨域句 1／行数 37；6 四段式 4／五段名 32；7 中断契约 1／未建 3 行／副作用 1；8 四处标准件 1／2／1／3＋产品层 7＋`命名标准` 1；9 资产内零链接／`check.sh` rc=0／`[2]` 段标题 1 行／lint 0 issue（61 件）／链接一行式 175／断链 0（**§九 追加前测得**——追加后本条留痕一度自带 3 条链接、复测 178／3，已改描述式并复测 175／0）；10 porcelain **13 行**（10 M＋3 未跟踪，与期望逐行一致、**无需订正**）／`scripts/` 未建 1；12 `CHANGELOG` 1 行）。**偏差申报两条**（均未构造绕过、未改断言）：**a** §二 改动清单**表头与数据行列序不一致**——表头为「…｜自由度｜依据／裁定」（＝`施工机制` §八 模板序），而 12 行数据格序一律为「依据｜自由度」→ `check_spec.py` 的 `[4]` 段 A-check 按表头取「自由度」格（实得依据文本）→ `[A]`／`[B]` 过滤后 `a_rows＝∅` → 10 条断言全「跳过（目标件不出现在任何改动行）」、汇总「A：提取 10 条断言、命中 0 条」——**非阻断、exit 0 不变**，但本批 `[4]` 段 A／B 判据空转（B 段「未提取到可判定尺寸声称」同源）。建议：把 12 行数据格按表头列序对调，或由规划方裁定表头列序；**施工方未擅改规格表**。**b** §一 ⑦ 记「`命名标准` 内 `最小壳` **2 处**（`:34`／`:45`）——本批随 F11 改成阶段一口径」，而 F11 只点名 §1 现状句（`:45`）且断言 8f 要求 `阶段一资产已落` 恰 1 处 → 施工按 F11 只改 `:45`（`:34` 目录树注「首个域——最小壳已建（087）」与树内三行「随各批落地」**未改**，不在点名落点内）；建议补记 `:34` 的改写口径或更正 §一 ⑦。**连带改述说明**（属 F10 状态句改动的直接后果，非新增裁量）：`大纲域设计:4` 的「随 088–091」→「随 089–091」；`:197`／`:200` 的过时句按 F10 改述（「分流待裁定」→「已按分流落位」、「随域建设批落地」→「阶段一已建、其余随 089–091」）。**本批未做**：断言 11 真机走查与 §九 走查记录（作者执行）。
- 2026-09-15｜**规划方核验（三步）**｜**通过（附两处缺陷，交补修轮）**。① **改动面核对（逐 hunk）**：porcelain **13 行**（10 M ＋ 3 未跟踪），逐 hunk 读 `git diff` 均在 §一 不变量逐件列举之内，**零越界**；四件新资产行数复测＝workflow **52**／stage-spec **57**／emotion **24**／template **99**（施工记录原写 97，已按复测订正为 99——产物审查 V-06）。② **断言独立重跑**：1–12 逐条自跑，**28 个子项全中**，与施工记录同值（唯 9e 见 R2）。③ **内容抽查**：四件逐读——迁移保真（与源件 `:12`–`:52` 逐行 diff 仅规格点名的 5 处差异）、头部两行格式齐、零链接、四段式与五段式齐、24 个填空位、枚举与必填在案；**查出缺陷一处（R1）**。**另修规格缺陷三处**（核验副产）：F1 补 `/{skill 名}` 占位符解析口径、新增**断言 7d**、§一 ⑦ 更正为「F11 只改 `:45`」（偏差 b）。
  - **R1（P1，内容错）**：`references/outline-workflow.md:44` 中断契约把 `/{skill 名}` 解析成 **`/abzu-outline`（本域自己）**——该占位符在 `触发与交互标准` §2.5 指**「对应域」的命令**（表「表达其他域意图」时移交谁），写成本域即语义反转。**判因＝规格缺陷**：F1 只写「§2.5 标准句式逐字」，而该句自带占位符，「逐字」与「可执行」相抵。处置：规格已补口径＋断言 7d，**资产改句交补修轮**。
  - **R2（P1，规范性）**：断言 9e 独立重跑得 **`178 3`**（期望 `175 0`）——多出的 3 条链接**不在资产**（资产零链接 ✓），而在**本规格 §九 施工记录自身的留痕**（写了 markdown 链接语法，相对 `docs/specs/` 必断）；施工方测得 175／0 系在追加 §九 **之前**。处置：核验当场改**描述式留痕**，复测 **175／0** 通过。**教训**：断言须在**全部写入之后**再跑；此为「判据／记录自带被扫串」同族**第四次**（前三次 082／086／087）。
  - **[4] 段预警逐条判定**（`AGENTS.md` §七）：表头列序修复后判别力恢复（改动行 12／可解析 11／判一适用 10），13 条候选**逐条属预期、无待处置项**——8 条「目标文本未提及且现行为 0」＝判据指向新建件（0 侧即「不存在」，构建期常态）；2 条「自噬预警」（`未建（随 089–091）`／`阶段一资产与降级纪律（088）`）＝判据串与目标串同现于规格、而扫描面是**目标件**，非真自噬；3 条「A／B 未提取」＝非整件口径声称，工具明示不判。
  - **偏差 a／b 处置**：**a**（§二 表头与数据格列序不一致致 `[4]` 空转）→ 规格 12 行数据格已按表头列序对调（规划方修订），判别力复测恢复；**b**（§一 ⑦ 与 F11 口径不一）→ §一 ⑦ 已更正，`:34` 留 §八 10。
- 2026-09-15｜**双维产物审查**（独立上下文只读 subagent；重路径＋改标准条文，故**双维**）｜findings 共 **7**：**P0:0** P1:1 P2:6 ＋观察 8｜结论 **修订后通过**。审查方**独立复核**：迁移保真（源件 `:12`–`:52` ↔ 产物逐行 diff＝**3 hunk**，与 F2 声明一致；词表与 1.2 行**逐字符相同**）、**断言 1–12 全条复跑**（与核验同值；7d＝0 即 R1 现态）、**改动面逐 hunk**（零越界）、**判别力实测三探针**（四节名降 H3／`references/{skill 名}/` 同名层／一级非法子目录名——**三构造全被 `[2]` 段报出**，跑完均删净、porcelain 复原 **13**）。处置：
  - **V-01（P1，条文互斥）**：§五 禁「心流锚」而 F2 命逐字迁含心流锚的 `①作用`——**裁定**：心流锚属产品层七字段的**格式部件**（`大纲域设计` §四.1 定其格式），不在「设计语言」之列；§五 已写明例外。
  - **V-03（P2，受众未定）**：建设状态通报被断言 11① 钉成「报给作者」，而原句含批号与内部纪律，与壳「语言」节零黑话相抵 → **裁定拆两句**：作者可见句去批号去术语、模型纪律句不报给作者（F1 逐字已改，**补修轮执行**）。
  - **V-05（P2，标准缺款）**：四件资产有 **6 处**引用包外治理文档（装进用户项目后包内不存在）。**可执行**的两处（中断契约、壳跨域告知）改**自包含**（内联三域命令）；其余为追溯性指针，立规落 §八 13。本批**未**擅改标准件。
  - **V-02（P2，覆盖面）**：断言 4 只钉建设状态节第 1 句 → 判据改**模型侧两句并集**（已更新）。
  - **V-04（P2，规范形态）**：阶段文件用户问句的模态标注为产品层旧形态（前缀式／「（对话）」／混合标注），与 `触发与交互标准` §3.5／§3.6 不合 → **按标准收口**（话术仍逐字，只动模态标注与选项呈现），入 F2 第 ④ 项。
  - **V-06（P2，计数互斥）**：施工记录写 template **97** 行、复测 **99** → 施工记录已订正。
  - **V-07（P2，会卡门）**：模板 `读者避雷点` 标「必填（随采风）」而 1.1 完备校验例外清单未列它（2.x 本批未建）→ 模型会反复追问或编造 → 入 F2 第 ⑤ 项。
  - **观察登记三条**（落 §八 14／15）：段一对四个必填字段无专属问句（靠收拢式重问兜底）／迁移件的数字来源标注与情态词对 §10.1／§10.2 的适用口径未立／挂载表末行「`scripts/`（本域暂无）」为空信息行。
  - **范围扩展**：审查标出的三处同族残留（`命名标准:34` 与目录树两行／`书目录契约 §4:49`／`skill形态标准:21`）**转入本批补修**（F9④／F11 第二处／新 F13），断言 8d 期望 3→**4**、断言 10 期望 13→**14** 已同步。
- 2026-09-15｜**补修**（施工方）｜八处全落、门禁绿（rc=0／lint 61 件 0 issue／链接一行式 175／断链 0）、断言 1–12 复跑**全中**（重点：**7d＝1**、**4b＝1**、**8d＝4**、**8f＝1**、**10a＝14**；其余同核验值）。**改动（改前 → 后）**：**①** `references/outline-workflow.md` 中断契约 →「…建议使用对应域命令（本套件三域：`/abzu-outline`／`/abzu-volume`／`/abzu-write`）…」（R1＋V-05，逐字自 F1 现文）；**②** 同件『建设状态』节**拆两句**（V-03）：报给作者的一行（逐字「大纲域现在只能走**规格**这一段——把这本书的规格聊清楚、定下来；采资料、写大纲、打磨定稿这几步还没做，我不会假装能做。」，无批号无内部术语）＋模型侧纪律（**不报给作者**，含原两句判据串）；**③** `SKILL.md` 跨域意图告知 →「（本套件三域：`/abzu-outline`／`/abzu-volume`／`/abzu-write`）」（V-05）；**④** 阶段文件用户问句模态收口（V-04）：1.0⑥ 前缀式「选项列表「在当前目录开新书吗？」」→ 问句后置「（选项列表）」；段一「（对话）」→「（对话式）」；段三混合标注「（选项列表≤3+对话≤3）」→「（逐项单模态：选项列表≤3、对话式≤3）」且书名／基调／避雷三处各挂单一模态（**话术原文逐字未动**，含「**受控词表选 2-4 词，词序即主次**」与三条问句原文）；**并**同族第 4 处——1.2 交互的前缀式「选项列表「规格定了吗？定了就开始采风。」」→ 问句后置（V-04 未点此行，按 `触发与交互标准` §3.5 通例一并收口）；段二「（静默+回读）」属过程性动作标注、非问句模态标注，**未动**；**⑤** 阶段文件 1.1④ 完备校验例外清单补「`读者避雷点` 1.1 留位、**不计入本步完备校验**（随采风补）」（V-07）；**⑥** 模板 `:25` 必填性「必填（随采风）」→「留位（不计入 1.1 完备校验）」（**与 F2⑤ 同源**；规格只点名 1.1④，模板侧按补修轮指示一并改）；**⑦** `书目录契约 §4:49` →「立项步建齐**本阶段用得到**的目录与空文件（阶段一＝`创作进度.md`／`基本规格.md`）；门控确认前为**空模板**」（F9④；该串 3 → **4**）；**⑧** `命名标准:34` 目录树注 →「首个域——**阶段一资产已建（088）**」（**含字面 `**`**，理由见下）、树内 `references/`／`assets/` 两行「随各批落地」→「已建（阶段一资产）」、`scripts/` 行保留（F11 第二处）；**⑨** `skill形态标准 §1:21` →「`skills/` 已建（`abzu-outline`——**阶段一（规格步）资产已建（088）**）」（F13）。**「[4]」段预警逐条判定**（`AGENTS.md` §七；共 **10** 条，**非阻断、exit 0 不变**）：**9 条**「目标文本未提及且现行为 0」皆因断言 token 为**正则／模式形态**（含 `^`／`\|`／括号）而工具按**字面子串**取数——结构性为 0，实际值由断言 1–12 直接复跑确认（2b＝4／2c＝0／2d＝0／3b＝4／6a＝4／9a＝「扫毕」／9c＝1／10b＝1）；**2 条**「自噬预警」为**规格目标文本自身含该串**（F1 描述 Stage 2–4 行的「未建（随 089–091）」→预测 4、到位期望 **3**；F12 条目开头的「阶段一资产与降级纪律（088）」→预测 2、到位期望 **1**）；**逐条属预期，无待处置项**。**观察申报三条（不阻断，交规划方）**：**a** `命名标准:34` 的 [A] 目标文本含 `**`，而该行位于 text 代码围栏内（强调标记在围栏内只作字面字符显示）——施工按 [A] **字面**判据落 `**`，否则 「[4]」 报「`[A]` 未落实或已漂移」；若规划方偏树注纯文本，建议删规格引号块内的 `**`（`[4]` 该行随之消失）。**b** §二 口径要点⑤ 仍写「F1 建设状态节用「尚未落地（随 089–091）」」，而 V-03 改后的 F1 逐字文本已无该括注（② 仅保留「尚未落地」字样）——断言 7b 不受影响（仍 3），建议口径要点⑤ 同步删括注。**c** V-04 所点 `:46` 对不到唯一行（段一「（对话）」实于 `:48`、混合标注实于 `:50`），按 §3.5 通例对段一／段三一并收口并外延至 1.2 同族一处，逐处留痕于上。

- 2026-09-15｜**规划方复核（修复面）**｜九处逐处读原文核对＝**全部到位**（中断契约改「对应域命令＋内联三域」／建设状态节拆两句且作者句无批号无术语／壳跨域句自包含／模态标注四处收口／例外清单与模板同源改述／`书目录契约 §4` 行阶段化／`命名标准:34` 与目录树两行／`技能形态标准 §1`）；`check.sh` rc=0、lint **0 issue**（61 件）、链接一行式 **175／0**、porcelain **14**（11 M＋3 未跟踪，逐行在 §一 不变量之内）；关键断言自跑复核 **7a／7d／4b＝1／1／1、8d＝4、8f＝1、10a＝14**。
- 2026-09-15｜**补修三条申报的裁定**｜**a（纯文本支）**：`命名标准:34` 树注去加粗标记，规格 F11 引号块同步去标——该两字符体例修正由规划方收尾并记录（施工轮已收口，且须同改规格引号块）。**b（照准）**：口径要点⑤ 已与 V-03 改后文本同步。**c（照准）**：F2④ 行号订正为 `:26`／`:48`／`:50`，**1.2 `:57` 的外延照准**（同族形态缺陷，一致性优先）。
- 待填：**真机走查（作者，仓库外）** → 归档 → 呈作者确认提交。

- 2026-09-15｜**收口（作者裁定）**｜① **§八 三处订正入档**：10／11／12 由「触发＝下批收口」订正为**已履行**（088 补修轮已处置：F11 第二处／F9④／新 F13）——不订正会使 089 的核销表档位算错。② **真机走查（断言 11）未执行**：作者裁定直接收口提交；已有真机证据一条＝**V-05 的现场确认**（test2-2 安装会话读完壳即报「缺依赖」——`SKILL.md:33` 引包外标准，装进用户项目后包内不存在），该指针属 §八 13 的实体 → 走查欠账登记为 **§八 16**（触发＝089 之后或下一个域建成前补跑，依 `测试与验收标准` §3 第 ④ 条）。③ **本规格归档**（验收通过；走查欠账按上述约束挂账）。
