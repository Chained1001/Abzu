# 089-2026-09-15-施工效率批

> 定位：**降批内成本**——今天（088 批）实测：4 个独立上下文 subagent、**205 次工具往返**、约 **23.5 M token**、~24 分钟墙钟；两个可量化的浪费＝**必读输入 190 KB／批**（每个 agent 各付一次）与**断言逐条单跑**（每次往返重发整段上下文）。本批把五条纪律写进流程真源：**① 实跑批式化 ② 必读输入按节点名 ③ 新增检查 F（禁改面禁词 ↔ 目标文本）④ 审查跳过机器已覆盖面 ⑤ 小改动攒批**；外加一条流程发现（施工 subagent 一律后台 spawn）与一条规格写作律（断言范围与期望同源）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径**（跨文件、改规则、动开发守卫）＋ **双维产物审查**（改标准条文）；**不触 `skills/`**，故**免真机走查**（`施工机制` §二 重路径行的豁免判据）。
> 自由度分布：[A]×7 [B]×4
> 必读输入：本规格 §一；`docs/specs/施工机制.md`（改面：§一 `:29`／§二 `:45`–`:53`／§三 `:57`–`:66`／§五 `:85`–`:99`／§八 `:186`／`:217`／`:253`–`:254`）；`docs/standards/规格写作标准.md`（§4 `:46`–`:54`／§5 `:56`–`:70`）；`docs/standards/测试与验收标准.md`（§1.1 `:26`／§3 `:106`）；`scripts/check_spec.py`（头注 `:3`–`:8`／维护入口 `:39`–`:43`／检查 E 取集常量与 `check_reconcile()`／派发处 `:1060`–`:1066`）
> 施工形态：subagent（后台 spawn——本批 F1 即把该条写成默认；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 088 批成本实测（本批的动机；该数取自对话记录，**不可复跑**） | 成稿审查 ~3.0 M／51 次往返；施工 ~9.9 M／82 次；双维 ~2.5 M／39 次；补修 ~8.1 M／33 次——**合计 ~23.5 M／205 次往返**；必读输入十件合计 **190 044 字节**（`大纲域设计.md` 24 790／`skill形态标准.md` 19 922 最肥） ✓ |
| ② | 后台 spawn 条现文 | `施工机制:29`＝「预期后续整改的施工 subagent 必须后台 spawn——前台 subagent 结束后不可续，整改只能重新 spawn。」（`grep -o "预期后续整改的施工 subagent 必须后台 spawn"` → **1**）——088 期施工方走前台，补修只能靠 agent id 恢复 ✓ |
| ③ | 自审三条的两处载体 | `施工机制:49`「规格自审三条（规格「规格自审」节按此写；缺任一条不得产出施工交底）」＋`:186` 模板节标题「## 四、规格自审（三条）」；`规格写作标准:46`「### 4. 规格自审（三条；各规格的「规格自审」节按此写）」（**该件声明与 `施工机制` §二 同源、不复制细则**）。实测：`grep -c "规格自审（三条"` → 施工机制 **1**／规格写作标准 **1**；`grep -c "规格自审三条"` → 施工机制 **1** ✓ |
| ④ | 分流表后无攒批条 | `施工机制:43`–`:45` 三条 bullet 依次为「判据来源」「产品层纯文档内容批」「重路径真机走查」——无攒批纪律 ✓ |
| ⑤ | §三 无实跑纪律节 | `施工机制:57`–`:66`＝核验三步／断言预验；`:67` 起为过程产物两项——**无任何「批式实跑」条文** ✓ |
| ⑥ | §五 无「机器已覆盖面」条 | `:85`–`:99`＝判据核心／表格两行／报告纪律／subagent 不复用／报告落点／无裁量／止损三条——**无去重复条**；088 期成稿审查独立重跑了计数与 `[A]` 对照（与 `[4]` 段重复） ✓ |
| ⑦ | 必读输入与交底项现文 | `:167` 模板头注「> 必读输入：{本批需要读的既有文件与节；不涉业务写「无」}」；`:217` 头注说明「规格模板头注四行：定位／自由度分布／必读输入／施工形态；…」；`:253`–`:254` 交底第 6 项「必读输入清单（逐条列本批该读的文件与节）。」／第 7 项「报告计数类发现时附实测命令与输出（未测者标「估计」）。」 ✓ |
| ⑧ | `check_spec.py` 现状 | **1238 行**；头注声明**五类**检查（A 自噬／B 计数重算／C `[A]` 逐字／D 写作机械／E 三方对账），`grep -c "五类"` → **8 行**；派发处 `:1060`–`:1066` 五个 `check_*()` 顺序调用；**无检查 F**（`grep -c "检查 F"` → **0**）✓ |
| ⑨ | `[4]` 段的枚举已过时 | `测试与验收标准:26`（§1.1）只列四项（自噬／计数重算／`[A]` 逐字／写作检查）——**检查 E 未列**（实测该行 `grep -c "三方对账\|检查 E"` → **0**）；`:106`（§3 机检锚点）同形漏列 ✓ |
| ⑩ | 七个目标串改前基线 | `施工 subagent 一律后台 spawn`／`小改动攒批`／`实跑纪律（批式`／`机器已覆盖面`／`未点名的节不读`／`范围与期望同源`／`规格自审四条` —— 三件内 `grep -o` 全 **0** ✓ |
| ⑪ | 基线 | `bash scripts/check.sh` exit **0**（`[4]` 段对 088 与 089 两份在制规格实跑，候选逐条属预期）；lint **0 issue**；链接一行式 **175／断链 0**；porcelain **2**（他批未提交件 1 件 M ＋ 本规格 1 行未跟踪，见下 ⑫） ✓ |
| ⑫ | **他批未提交件（申明）** | `docs/specs/088-…md` 现为 **M**——该件 §八 10／11／12 三行原文写「触发＝下批收口」，而三处已由 088 补修处置（F11 第二处／F9④／F13）→ 规划方已就地订正为**已履行**。**该件尚未归档**（088 收口＝真机走查 ＋ 归档 ＋ 本次订正，尚未发生）；本批核验时它仍会是 M，**不属本批改动面**（`测试与验收标准` §4 G4 已认「他批未提交改动件」这一既有条件）。**前提**：本批施工在 088 收口提交**之后**（§八 5）——届时该 M 行消失，porcelain 只剩「5 件本批 M ＋ 1 行本规格未跟踪」＝**6**（断言 10 定期望 6） ✓ |

**不变量（施工前后须同）**：本批改动**全部落点**：`docs/specs/施工机制.md`（§一／§二／§三／§五／§八）／`docs/standards/规格写作标准.md`（§三 锚点表两行 ＋ §4 标题与正文第 4 项 ＋ §5 第 13 条）／`docs/standards/测试与验收标准.md`（§1.1 `:26`＋§3 `:106` 两行枚举）／`scripts/check_spec.py`（头注／维护入口／新增检查 F／派发处）／`CHANGELOG.md`；**除此之外零改动**（`AGENTS.md`／`README.md`／`skills/`／`docs/product/`／`docs/specs/archive/` 零改动；`docs/specs/088-…md` 的 M 属他批未提交件，本批**不动**）。链接一行式 **175／断链 0** 不因本批变化。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md:29`（§一 subagent 纪律第 3 条） | 「预期后续整改的施工 subagent 必须后台 spawn——前台 subagent 结束后不可续，整改只能重新 spawn。」→「**施工 subagent 一律后台 spawn**——前台结束后不可续、整改只能重新 spawn；后台 spawn 不阻塞且可续（补修／整改直接对同一 agent 续发指令，无需重派）；**规划方须在交底约定回报点、完成后主动取回报**（不阻塞不等于不管）。**对外部窗口形态**（§八 备选形态）：无 subagent，本条不适用」 | [A] | 作者裁定丁；088 期实例 |
| F2 | `docs/specs/施工机制.md`（§二 `:49`＋`:51`–`:53`／§八 模板 `:186`＋`:188`–`:190`）＋`docs/standards/规格写作标准.md`（`:46`＋`:50`–`:52`／§三 机检锚点 `:121`） | **自审三条 → 四条**（**三处标题 ＋ 三处正文列表 ＋ 一处锚点行**，缺一即同件标题与正文互斥）：① 标题——`施工机制:49`「规格自审三条（…）」→「**规格自审四条**（…）」／`施工机制:186`「## 四、规格自审（三条）」→「（四条）」／`规格写作标准:46`「### 4. 规格自审（三条；…）」→「（四条；…）」——**三处标题与两处锚点句原文本无粗体，故目标亦不带粗体**（产物审查 V-01：规格侧曾多写 `**`，致 `检查 C` 常驻一条假漂移候选；判「粗体非字面」以就实件风格）② **三处正文列表各追加第 4 项**（`施工机制:51`–`:53`／`施工机制:188`–`:190` 模板正文／`规格写作标准:50`–`:52`），**三处同文**（逐字）③ `规格写作标准:121`「§4 自审三条齐备」→「§4 自审四条齐备」（**不带粗体**，同 V-01）。**第 4 条逐字**：「4. **范围与期望同源**——断言若建立在某段范围（文件行段／节／清单）之上，其期望值须由**该范围**实跑得出（范围与期望取自不同侧即失守）。实例：088 成稿审查 F-02——迁移范围写 `:14`–`:52`（不含父标题 `:12`），而期望 4 是按**含** `:12` 的源件算的，施工后必红。」——**该短语**（`范围与期望同源`）在改后**两件内恰 5 处**（`施工机制` 2＝§二 ＋ 模板；`规格写作标准` 3＝§4 ＋ §5.13 ＋ §三 锚点表行；**全仓另含 `CHANGELOG` 1 处**，故全仓为 6；产物审查 V-03 订正指代与口径——此前「该串」指代不清，所引 154 字块实为 3 处），断言 2 的该条命令按此定期望 5 | [A] | 作者裁定乙；088 F-02 事故；窄域复审 P0-1 订正 |
| F3 | `docs/specs/施工机制.md`（§二 `:45` 之后） | 新增一条 bullet（逐字）：「- **小改动攒批**：发现的零散欠账与同族残留，优先**夹带进邻近批次**（写进该批改动清单与核销表），不零散开批——一批＝一次施工＋一次审查；只有触发条件独立且紧迫时才单独立批。实例：088 夹带 087 三条账与审查标出的三处同族残留。」 | [A] | 作者裁定；088 实践 |
| F4 | `docs/specs/施工机制.md`（§三 `:66` 之后新增一段；**并同步 §三 标题 `:57` 与目录行 `:11`：`（核验三步／过程产物两项）`→`（核验三步／实跑纪律／过程产物两项）`**） | （现状：无）→ 新增（逐字）：「**实跑纪律（批式）**：断言与门禁的实跑一律**批式**——断言用一个脚本一次跑完并一次输出（每行三列：命令／实测／期望）；门禁命令取尾（`bash scripts/check.sh > .tmp/<任务>/chk.out 2>&1; tail -5`）；大输出先 `head`／`tail` 收敛。**理由**：每次工具往返都要重发整段上下文，逐条单跑是批内成本的主要来源（088 实测 205 次往返／~23.5 M token）。**适用范围**：施工方自跑、规划方核验重跑、审查方抽样实跑三处同一口径。」 | [A] | 作者裁定甲（真源落 §三）；088 实测 |
| F5 | `docs/specs/施工机制.md`（§五 表格之后） | （现状：无）→ 新增一条 bullet（逐字；**插在「报告纪律」条之前**，置于 §五 表格之后）：「**机器已覆盖面不重复**：派审查 subagent 的交底须附『本批 `[4]` 段已覆盖的面』（**面清单不复述，口径见 `测试与验收标准` §1.1**）；审查方**跳过该面**、只做机器做不到的语义面（锚点真实性／改动清单可执行性／内部矛盾／内容质量）。**不删审查面，只删重复**。」 | [A] | 作者裁定丙；088 期重复实见 |
| F6 | `docs/specs/施工机制.md:217`（§八 头注说明段） | 句尾追加（逐字）：「**必读输入按「文件:节」点名**（不得只写整件）；交底与施工默认**只读点名节**，未点名的节不读——**整件点名（如 `AGENTS.md`）即读整件**；装载量是每个 subagent 的成本主项（088 实测定名十件共 190 KB，实际只用到少数节）。」 | [A] | 作者裁定；088 实测 |
| F7 | `docs/specs/施工机制.md:253`–`:254`（§八 交底七项 6／7） | 第 6 项 →「6. 必读输入清单（逐条列本批该读的文件与**节**；按节点名规则见 §八 头注说明）。」；第 7 项 →「7. **实跑须批式**（规则见 §三 实跑纪律）；报告计数类发现时附实测命令与输出（未测者标「估计」）。」 | [A] | 同 F4／F6（**引用不复述**） |
| F8 | `scripts/check_spec.py`（新增检查 F ＋ 头注 ＋ 维护入口 ＋ 派发处 `:1066` 后） | **检查 F：禁改面禁词 ↔ 改动清单目标文本的词面命中**（候选非阻断）：取集源＝检查 E 已解析的**禁改面**节文本（`BAN_SECTION`＝`^##…禁止事项…$`）；**禁词表**＝该节内**「不得把 … 搬进 skill 资产」／「不得把 … 写入 skill 资产」类述谓句**中的**顿号分隔中文词组**各成员——**排除反引号包裹的路径／命令 token**（路径出现在目标文本里属正当引用，非「搬入禁语」））；**无该类句则禁词表为空——此时本检查完全静默**（不打汇总行、不打空转明示，输出不含「禁改面词」字样；否则断言 7e 会因字样出现而假红，窄域复审 P1-b）。核对对象＝ §二 改动清单各行的**目标侧引号块**（`_target_blocks()`，不读整行——整行含位置列路径会误报）；命中即呈报候选行「禁改面词 X 出现在改动行 N」。**分工与命名**：与检查 E 的「判一」（按**目录前缀**）互补不重叠，本条按**词面**；本检查＝**顶层第六类「检查 F」**，与正文 `（F1）`／`（F2）` 这些**子判据标签**不同族（头注须写明，免维护者误读）。头注「五类」→「**六类**」（**8 行 8 处**同口径），**并同步 4 处字母枚举** `A／B／C／D／E`→`A／B／C／D／E／F`（`:51`／`:66`／`:1039`／`:1041`）＋ `:66` 括注「含 B 的计数行与 E 的候选行」→「含 B 的计数行与 **E／F** 的候选行」——**`:51` 属退出契约行**（「静态发现恒 0」的适用面），不列 F 即把新检查留在契约外；维护入口补检查 F 判据函数名（**函数名钉为 `check_ban_words()`**——原由 `[B]` 交施工方定名致断言无从精确判定，据评审 P1-2 改钉）、派发处按 A–F 顺序追加调用 `check_ban_words(text, root)`。**判别力自证夹具骨架**（落 `.tmp/089-probe/fixture.md`、自证后删净）：① `## 六、禁止事项` 节（命中 `BAN_SECTION`）② 节内一句「不得把（心流锚、设计理由）类的设计语言搬进 skill 资产」③ §二 改动清单一行（**节标题须写成 `## 二、文件级改动清单`**——命中 `CHANGE_SECTION`；写成「## 二、改动清单」会使 `_change_rows()` 返空、检查走空转分支），**位置列写实存路径**（`.tmp/089-probe/fixture.md` 自身——`check_reconcile()` 对无实存件只产空转明示并 `return`）④ 该行目标侧引号块含「心流锚」。**判别力自证（随施工交付）**：以 `.tmp/` 下合成夹具（禁改面列「（心流锚、设计理由）」＋改动行目标文本含「心流锚」）实跑须**报出**；以本规格自身实跑须**不误报** | [B] | 作者裁定乙；088 V-01 事故出身（`AGENTS.md` §四 准入：有出身＋可机械化＋零外部依赖） |
| F9 | `docs/standards/测试与验收标准.md`（§1.1 `:26` ＋ §3 `:106`） | 两行的 `[4]` 枚举由四项**补全为六项**（补**三方对账**与**禁改面禁词核对**）——`:26` 现文漏列检查 E（新增 F 后同漏）→ 改为「…断言自噬预警（`[A]`／`[B]` 面，含零命中待复核与零命中核对未判）、计数重算、`[A]` 项目标文本（表格形态）逐字核对、规格写作机械检查、**三方对账（改动面 ↔ 禁改面 ↔ 断言排除集）**、**禁改面禁词核对**」；`:106` 同口径 | [B] | 现状锚点 ⑨（真缺口，顺手收口） |
| F10 | `docs/standards/规格写作标准.md`（§5 `:70` 之后） | 新增第 **13** 条（逐字）：「13. **范围与期望同源**：断言若建立在某段范围（文件行段／节／清单）之上，期望值须由**该范围**实跑得出——范围与期望取自不同侧必假红（实例：088 成稿审查 F-02，范围 `:14`–`:52` 不含 `:12` 而期望 4 按含 `:12` 算）。与 §4 自审第 4 条同源，本条是**写法**侧、自审条是**自检**侧。」**另**：给该件 §三 机检锚点表补两行——「§5.13 范围与期望同源｜自审第 4 条（人判但强制）｜无守卫（人判）」与「§4 附加项·禁改面禁词｜静态自检的禁改面禁词核对（`check_spec.py --static`，接入 `check.sh` [4]）｜已实现」**（该行条文列由补修轮 **F12** 改为「§4 附加项（禁改面对账·词面侧）」并新增前缀侧一行——以 F12 为准；本处保留施工当时的原样，供逐处对照）****（落盘形态注**：引号块内以全角 ｜ 表示单元格分隔，是**表格形态占位**——实件按 markdown 表格落为半角竖线，故 `检查 C` 对该两行必产「`[A]` 未落实或已漂移」候选，属预期；施工偏差②） | [A] | 作者裁定乙（F2 的写法侧）；产物审查 F-16 |
| F12 | `docs/standards/规格写作标准.md`（§三 机检锚点表的两行） | （现状：F 行条文列为「§4 附加项·禁改面禁词」——**§4 附加项并无此子项**，指针不可回指）→ **改**：「§4 附加项（禁改面对账·词面侧）」；**并新增一行**（补齐与检查 E 的对称记法，此前 E 无行）：条文列「§4 附加项（禁改面对账·前缀侧）」／强制方式列「静态自检的三方对账（`check_spec.py --static`，接入 `check.sh` [4]）」／状态列「已实现」 | [B] | 产物审查 V-04（条文指针指向不存在的子项；E 无行／F 有行不对称） |
| F13 | `docs/standards/测试与验收标准.md`（§4 已知缺口表末尾） | （现状：无 G19 行）→ 新增一行（三列：编号｜缺口｜触发条件，**落盘为半角竖线分隔**）：「G19｜检查 F 的取词面＝顿号分隔中文词组，**通用词**（断言／数字／锚点类）易误报——实测夹具「不得把（断言、数字）搬进 skill 资产」＋目标文本含该词即报 2 条｜触发＝上线后出现大批「人工逐条判为预期」的候选时，按类收窄取集源（如只取反引号 token，或要求词组为专有名词）」——**债跟着主题走**：该缺口原只登记在 089 §八 3，随规格归档即沉底 | [B] | 产物审查 V-05 |
| F11 | `CHANGELOG.md`（`### Added` 段首） | 一行：『施工效率批（089）：…』——五条效率纪律＋检查 F＋规格写作第 13 条；**≤400 字符**、末尾「核验：」句 | [B] | 过程产物 |

**口径要点（易错）**：① **F1–F7 逐字**（引号内文字原样落）；② **单一真源**：实跑纪律只在 §三（F7 第 7 项只引用）、必读按节规则只在 §八 头注说明（F7 第 6 项只引用）、「范围与期望同源」的**规则文本**在 `规格写作标准` §5.13、**自检条**在 `施工机制` §二第 4 条（F2 那段的括注已写明两侧分工）；③ 计数口径逐条标注（行 vs 出现次数）；④ `check_spec.py` 的「五类→六类」须**8 处同口径**替换，不得只改头注；⑤ 本批**不动** `skills/`、不动 `docs/specs/088-…md`（他批未提交件）。

## 三、验收断言（一条命令一期望；计数口径逐条标注）

### 断言 1｜后台 spawn 默认（F1）

```bash
grep -o "施工 subagent 一律后台 spawn" docs/specs/施工机制.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "预期后续整改的施工 subagent 必须后台 spawn" docs/specs/施工机制.md | wc -l
```

期望 **0**（旧句归零）。**预验基线 1**。

### 断言 2｜自审四条（F2／F10）

```bash
grep -c "规格自审（三条" docs/specs/施工机制.md docs/standards/规格写作标准.md | grep -v ":0$" | wc -l
```

期望 **0**（**计文件数**——两件都不得再含旧标题；改前 2）。**预验基线 2**。

```bash
grep -c "规格自审（四条" docs/specs/施工机制.md docs/standards/规格写作标准.md | grep -v ":0$" | wc -l
```

期望 **2**（**计文件数**——`施工机制:186` 模板节标题 ＋ `规格写作标准:46` 各 1）。**预验基线 0**。

```bash
grep -c "规格自审三条" docs/specs/施工机制.md
```

期望 **0**（`:49` 的引导句同步；**计行**）。**预验基线 1**。

```bash
grep -c "自审三条" docs/standards/规格写作标准.md
```

期望 **0**（**计行**——§三 锚点行同步；**预验基线 1**）。

```bash
grep -o "范围与期望同源" docs/specs/施工机制.md docs/standards/规格写作标准.md | wc -l
```

期望 **5**（**计出现次数**——**五处源逐处列举**：`施工机制` §二第 4 条 1 ＋ §八 模板第 4 项 1；`规格写作标准` §4 第 4 条 1 ＋ §5 第 13 条 1 ＋ **§三 机检锚点表新行 1**（F10 同批补入，窄域复审 P0-1））。**预验基线 0**。

```bash
grep -o "4. \*\*范围与期望同源" docs/specs/施工机制.md docs/standards/规格写作标准.md | wc -l
```

期望 **3**（**计出现次数**——**三处正文列表各一**：`施工机制` §二 `:51`–`:53`／§八 模板 `:188`–`:190`／`规格写作标准:50`–`:52`；**编号前缀 `4.` 使该条与 §5.13（编号 13）及 §三 表行区分开**，故上一条的「5」与本条「3」互补——评审 P1-1 采纳）。**预验基线 0**。

### 断言 3｜攒批纪律（F3）

```bash
grep -o "小改动攒批" docs/specs/施工机制.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

### 断言 4｜实跑纪律真源唯一（F4／F7）

```bash
grep -o "实跑纪律（批式）" docs/specs/施工机制.md | wc -l
```

期望 **1**（**计出现次数**——**唯一落点**＝§三 那段；交底项只写「§三 实跑纪律」）。**预验基线 0**。

```bash
grep -o "核验三步／实跑纪律／过程产物两项" docs/specs/施工机制.md | wc -l
```

期望 **2**（**计出现次数**——§三 标题 `:57` ＋ 目录行 `:11` 同步；评审 P2-2）。**预验基线 0**。

```bash
grep -c "§三 实跑纪律" docs/specs/施工机制.md
```

期望 **1**（**计行**——交底第 7 项的引用）。**预验基线 0**。

### 断言 5｜机器已覆盖面（F5）

```bash
grep -o "机器已覆盖面" docs/specs/施工机制.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

### 断言 6｜必读输入按节（F6／F7）

```bash
grep -o "未点名的节不读" docs/specs/施工机制.md | wc -l
```

期望 **1**（**计出现次数**——**唯一落点**＝§八 头注说明段）。**预验基线 0**。

```bash
grep -o "按节点名规则见 §八 头注说明" docs/specs/施工机制.md | wc -l
```

期望 **1**（交底第 6 项的引用句）。**预验基线 0**。

### 断言 7｜检查 F（F8）

```bash
grep -c "检查 F" scripts/check_spec.py
```

期望 **≥1**（**计行**）。**预验基线 0**。

```bash
grep -c "五类" scripts/check_spec.py
```

期望 **0**（头注与相关行同口径改「六类」；**计行**）。**预验基线 8**。

```bash
grep -o "六类" scripts/check_spec.py | wc -l
```

期望 **≥8**（**计出现次数**——与改前「五类」同口径）。**预验基线 0**。

```bash
grep -c "A／B／C／D／E 六类" scripts/check_spec.py
```

期望 **0**（**计行**——字母枚举须同步为 `A／B／C／D／E／F`，禁「A–E 六类」这类自相抵）。**预验基线 0**。

```bash
grep -o "A／B／C／D／E／F" scripts/check_spec.py | wc -l
```

期望 **≥4**（**计出现次数**——`:51`／`:66`／`:1039`／`:1041` 四处字母枚举同口径）。**预验基线 0**。

```bash
grep -c "^def check_ban_words(" scripts/check_spec.py
```

期望 **1**（**计行**——检查 F 判据函数已定义；评审 P1-2）。**预验基线 0**。

```bash
grep -c "= check_ban_words(text, root)" scripts/check_spec.py
```

期望 **1**（**计行**——派发处已按 A–F 顺序追加调用；评审 P1-2。**判据含等号前缀（反引号内 `=` 后接一空格）以排除 `def` 行自身的形参串**——施工偏差①实测：全串形态为 **2**（`def` 行 ＋ 派发行），与本断言互斥故不可达；施工方**未**改 def 形参去凑 1，处置正确）。**预验基线 0**。

```bash
python scripts/check_spec.py --static .tmp/089-probe/fixture.md 2>&1 | grep -c "禁改面词"
```

期望 **≥1**（**判别力自证**：合成夹具＝禁改面节含「（心流锚、设计理由）」＋改动清单目标文本含「心流锚」；**该夹具由施工方落 `.tmp/089-probe/`、自证后删净**）。**预验基线**：夹具不存在（命令输出 0）。

```bash
python scripts/check_spec.py --static docs/specs/089-2026-09-15-施工效率批.md 2>&1 | grep -c "禁改面词"
```

期望 **0**（**不误报**自证——**本规格 §五 无可抽取述谓句（禁词表为空）故完全静默**，与 F8 的静默硬条件对齐。**原括注误写为「禁改面与目标文本无词面交集」**——实测两者**有**交集（§五 含「不得改断言、数字、删检查」，§二 目标文本含「断言」等），真因是禁词表空；产物审查 V-02 订正，防后人沿错误方向「把禁改面改成无交集」）。**预验基线 0**。

### 断言 8｜`[4]` 段枚举补全（F9）

```bash
grep -o "三方对账" docs/standards/测试与验收标准.md | wc -l
```

期望 **≥2**（§1.1 与 §3 各 1；**计出现次数**）。**预验基线 0**。

```bash
grep -o "禁改面禁词" docs/standards/测试与验收标准.md | wc -l
```

期望 **≥2**（同上）。**预验基线 0**。

### 断言 9｜门禁、链接与零外部依赖

```bash
bash scripts/check.sh
```

期望 exit **0**（`[4]` 段须能跑通在制规格——**088 收口归档后只剩 089 一份**）。**预验基线 0**。

```bash
bash scripts/check.sh 2>&1 | grep -c "^\[2\] "
```

期望 **1**（**恰段标题行**）。**预验基线 1**。

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**（件数不设期望）。**预验基线 0 issue**。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**。**预验基线 175‧0**。

### 断言 10｜改动面

```bash
git status --porcelain | wc -l
```

期望 **6**（**前提＝088 已收口提交**，见 §一 ⑫／§八 5；**逐类列举**：**5 件 tracked M**＝`docs/specs/施工机制.md`／`docs/standards/规格写作标准.md`／`docs/standards/测试与验收标准.md`／`scripts/check_spec.py`／`CHANGELOG.md` ＋ **1 行未跟踪**＝`?? docs/specs/089-…md`）。**预验基线 2**（当前含他批未提交件 1 行；**088 收口提交后归 1**，届时本期望 6 成立）。**处置句（评审 P2-5）**：若 088 因故未收口而施工，**以实况为准并在核验记录里订正本行**，不因此判红（088 规格 M ＋ 089 规格未跟踪）；施工后**逐行列核验记录**（与实况不符以实况为准并订正本行）。

```bash
ls skills/abzu-outline | wc -l
```

期望 **3**（`SKILL.md`＋`assets`＋`references`——本批**不建** `scripts/`；**计行**。本断言意在确认本批**未触 `skills/`**）。**预验基线 3**。

### 断言 12｜补修轮两处（F12／F13）

```bash
grep -o "禁改面对账·词面侧" docs/standards/规格写作标准.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "禁改面对账·前缀侧" docs/standards/规格写作标准.md | wc -l
```

期望 **1**（**计出现次数**——检查 E 的对称行）。**预验基线 0**。

```bash
grep -c "^| G19 " docs/standards/测试与验收标准.md
```

期望 **1**（**计行**）。**预验基线 0**。

### 断言 11｜过程产物

```bash
grep -cF '施工效率批（089）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 十二条实跑取证（①成本数取自本会话实测；②③④⑤⑥⑦ 逐处读 `施工机制` 原文并核行号；⑧ `check_spec.py` 行数／五类计数／派发行号；⑨ `测试与验收标准:26` 漏列检查 E 实测；⑩ 七个目标串全 0；⑪ 门禁三命令；⑫ 088 规格 M 的来因）。
2. **断言实跑过**（改前侧）：断言 1–11 的 0 侧全部实跑（七个目标串 0／旧句 1／旧标题 2 文件／`五类` 8 行／porcelain 2／exit 0／lint 0／链接 175‧0）；**夹具类断言（7d／7e）属施工后侧**，夹具由施工方落 `.tmp/` 并删净。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**（本批新立条的自适用）：每条断言的范围与期望同源——断言 2 的「计文件数」期望 2 由**两件各 1** 得出（已实跑改前侧核 2 文件含旧标题）；断言 4 的「唯一落点」期望 1 由**真源唯一**口径得出（交底项改引「§三 实跑纪律」故不含该串，已核）；断言 10 期望 **6** 由**逐类列举**得出（含他批未提交件 1 行）。

## 五、禁止事项

- 禁止触碰：`.git/`、`AGENTS.md`、`README.md`、`skills/`、`docs/product/`、`docs/specs/archive/`、**`docs/specs/088-…md`**（他批未提交件，本批不动）、`scripts/check.sh`、`scripts/hooks/`。
- **不得**把 F4／F6／F10 的规则文本复制到第二个落点（单一真源：引用处只写节名）。
- **不得**在 `check_spec.py` 里做通过/失败判定——检查 F 一律**呈报候选**，`退出契约`「静态发现恒 0」不得破坏。
- **不得**改断言、改数字、删检查来变绿；`.tmp/089-probe/` 夹具**用后删净**（红线 2）。
- 禁止动作：任何 git 写命令；联网；安装依赖。

## 六、被否决的替代方案

- **① 的真源放 §八 交底七项** → 否决（作者裁定甲）：交底离施工方最近，但会与 §三 形成第二载体；§三 为题中义（核验／实跑），交底只引用。
- **成稿审查收窄审查面**（只审可施工性）→ 否决（作者裁定丙）：088 期成稿审查 18 条中 P1 五条全真；只做**去重复**（机器已覆盖面跳过），不删面。
- **把三类规格病全机械化** → 否决：判据自带被扫串已有检查 A（088 期属**判断失误**非工具缺口）；「范围与期望同源」机械化不可行（需解语义）→ 落规格写作 §5.13 ＋ 自审第 4 条（人判但强制）；只有「禁改面禁词」可机械化为检查 F。
- **施工 subagent 维持「预期整改才后台」** → 否决（作者裁定丁）：088 期未能预判整改，前台 spawn 致补修需靠 id 恢复；后台 spawn 不阻塞、可续，默认化成本为零。

## 七、本批核销（前一批 088 §八 全部开放项）

> **必列面**：088 §八 **全 16 条**（前一批的全部开放项；第 16 条＝088 收口时登记的走查欠账，**在成稿审查之后补入**——见 §九）＋本批触及者。**不必重列**：**086 §八 1／3／6**（088 §七 已承载、本批未使其状态变化——索引指针＝`088` §七）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 088 §八 1 | `AGENTS.md` 两处状态句（`:5`／`:9`） | **待作者** | 须作者确认；本批不动 `AGENTS.md`（§五 禁改面） |
| 088 §八 2 | 词表另四处消费点 | **仍待** | 随 089–091 各批上线时补（**注**：本条所指 089 为原批序的「资料收集」批，本批改为效率批后顺延） |
| 088 §八 3 | 域级守卫未建（`书目录契约` §4 五条机检点） | **仍待** | 建守卫前须先登记事故出身；本批只动开发守卫 `check_spec.py`，不建域级守卫 |
| 088 §八 4 | `验收场景.md` 真机步骤未同步环境前提 | **仍待** | 触发＝下批触碰该件时同批补 |
| 088 §八 5 | 2.x 阶段文件（`outline-stage-materials.md` 类名与内容） | **仍待** | 随资料收集批（现顺延为 090） |
| 088 §八 6 | `skill形态标准` §13 矩阵未列「阶段文件」类目行 | **仍待** | 触发＝矩阵下次修订 |
| 088 §八 7 | B-2…B-10 九件模板未落 `assets/` | **仍待** | 随各自阶段批 |
| 088 §八 8 | 仓内安装命令措辞精确化（`README.md` 等四处） | **仍待** | `README.md` 属禁改面，须作者确认（含 088 期新发现的「装后重载」提示是否上 README） |
| 088 §八 9 | `大纲域设计`「常见问题」节零条留位 | **仍待** | 触发＝首批真机走查后补录 |
| 088 §八 10 | `命名标准:34` 与目录树三行 | **已履行** | 088 补修处置（F11 第二处）；原「触发＝下批收口」句已由规划方就地订正 |
| 088 §八 11 | `书目录契约` §4 机检契约行 | **已履行** | 088 补修处置（F9④，断言 8d 期望同步为 4） |
| 088 §八 12 | `skill形态标准` §1 现状句 | **已履行** | 088 补修处置（新 F13） |
| 088 §八 13 | 标准缺款：运行时资产引用治理文档的格式口径 | **仍待** | 该条实体是**运行时资产**怎么引用治理文档（落 `命名标准` §5／`skill形态标准` §9.1），与本批的审查去重复（F5）／开发守卫加检查（F8）**主题零交集** → **不构成本批处置**；触发＝下批触碰该两件时补款（真机已证：装后模型把 `SKILL.md:33` 的包外指针读成「缺依赖」）。落点＝§八 开放项 1 |
| 088 §八 14 | 1.1 段一/段二对模板字段的覆盖 | **仍待** | 触发＝资料收集批（090）在 1.1⑥ 明写缺项盘点 |
| 088 §八 15 | 迁移件数字来源标注与情态词对 §10.1／§10.2 的适用口径 | **仍待** | 触发＝立「迁移件是否豁免」口径时一并处置 |
| 088 §八 16 | 088 真机走查未执行（088 收口时登记） | **仍待** | 触发＝**089 之后或下一个域建成前**必须补跑（`测试与验收标准` §3 第 ④ 条）；本批为开发侧效率批，不构成该约束的「下一个域」 |

- 汇总（恒列五档、无该档计 0）：`已履行 3｜作废 0｜本批处置 0｜仍待 12｜待作者 1 ＝ 16` ✓

## 八、开放项

1. **运行时资产引用治理文档的格式口径**（088 §八 13 转记）：可执行事实须自包含、追溯指针须标注「运行时不必加载」——落 `命名标准` §5／`skill形态标准` §9.1，触发＝下批触碰该两件。**真机证据**：088 走查安装会话读完壳即报「缺依赖」（`SKILL.md:33` 引包外标准）。
2. **批序顺延**：原「089＝资料收集」顺延为 **090**（本批插队）；`087`／`088` 规格内的批号指称不复改（归档件历史记录）。
3. **检查 F 的假阳性观察**：上线后若报出大量候选而人工逐条判「属预期」，触发＝按类收窄取集源（如只取反引号 token、不取顿号词组）。
4. **② 的进一步压降**：本批只立「按节点名＋未点名不读」纪律；若装载量仍高，下一步可评估「规格把真源关键段落**摘录**进规格本文」（触发＝连续两批实测装载量未降）。
5. ~~088 收口待办（他批）~~ —— **已履行**（088 已于本批施工前完成收口并提交推送：`1ff612f`，含 §八 三处订正、走查欠账登记与归档；本批 porcelain 因此为 **6** 而非 7，与 §一 ⑫／断言 10 的前提一致）。
6. **`scripts/check.sh` 的 `[4]` 段标题行枚举仍为四项**（施工方登记，属第三载体漂移）：`:10`／`:111` 现文「断言自噬／计数重算／`[A]` 逐字／写作检查」——F9 只修了 `测试与验收标准` 两处，`check.sh` 属本批 §五 禁改面。触发＝**下批触碰 `check.sh` 时**补「三方对账／禁改面禁词核对」。
7. **反模式清单补两条**（**作者 2026-09-15 会话内确认**；本批不落，落下一批——`施工机制` 在本批改动面内，且本批已过核验、正过产物审查，此处插入会使「零越界」结论失效）：本会话 **MD038 六次**（代码跨度首尾带空格）／**MD060 一次**（表格单元内裸竖线，**即便在反引号内**也被当列分隔）／**MD029 两次**（作者追加：以锚点行前插的方式增补有序列表项致序号顶翻——088 §八 15／16 与 089 §八 6／7 两见）。**草稿（落 `施工机制` §六 反模式清单 #13／#14／#15，措辞已避开三个陷阱本身）**：「**13**｜在反引号片段内写首尾带空格的串｜改述为文字（如「井号加一空格」），或改用无边缘空格的等价写法｜2026-09-15 088–089 批（本会话同型六次）」「**14**｜在 markdown 表格单元内写裸竖线（含反引号内）｜单元内竖线一律转义或改述为文字（如「半角竖线」）｜2026-09-15 089 批」「**15**｜以锚点行前插的方式增补有序列表项（新项落到锚点项之前，序号顶翻）｜锚点取插入点**之前**的那一项，或插入后逐项核对序号｜2026-09-15 088 §八 15／16 与 089 §八 6／7 两次同型（MD029）」。

## 九、审查记录

- 2026-09-15｜**成稿审查·第一轮**（独立上下文只读 subagent）｜findings 共 **16**：**P0:3** P1:7 P2:6 ＋观察 9｜结论 **修订后通过**。审查方独立实跑 §一 十二条锚点与全部断言改前侧（含断言 2 四条命令、禁改面反引号 token 与 §二 行文本求交、读核 `BAN_SECTION`／`_target_blocks()`／`check_reconcile()` 源码）。**处置（16 条全落）**：
  - **F-01（P0，范围与期望不同源）**：F2 只写「三处标题」，三处**正文列表**（`施工机制:51`–`:53`／`:188`–`:190`／`规格写作标准:50`–`:52`）未入改动清单 → 标题改四条而正文仍三条＝同件互斥，cmd4 期望 3 无对应产处。**处置**：F2 写全「三标题 ＋ 三正文（三处同文）＋ `:121` 锚点行」，四处都产该串 → 期望改 **4** 并逐处列举。
  - **F-02（P0，断言与判据互斥）**：F8 原判据取禁改面**全部**反引号 token，而 F4 目标文本含 `scripts/check.sh`（禁改面明列）→ 断言 7e「不误报」必红。**根因＝判据过宽**（路径出现在目标文本里属正当引用）。**处置**：禁词表收窄为**「不得把 … 搬进／写入 skill 资产」类述谓句内的顿号词组**、排除反引号路径 token，无该类句即空表；核对面同步收窄（只读目标侧引号块，不读整行）。
  - **F-03（P0，范围与期望不同源）**：断言 10 期望 7 与 §八 5「施工待 088 收口提交」互斥（收口后 088 的 M 行消失 → 实况 6）。**处置**：期望改 **6** 并写明前提；§一 ⑫ 补收口后状态。
  - **F-04（P1）**：`规格写作标准:121`（§三 锚点行）是「自审三条」第三处活件位置 → 入 F2 ＋ 新增断言（该件内 `自审三条` 归零）。
  - **F-05（P1）**：F8 漏 **4 处字母枚举** `A／B／C／D／E` 与 `:66` 括注；`:51` 是**退出契约行**，不列 F 即把新检查留在契约外 → F8 补明，断言 7 加两条（「A–E 六类」归零／`A／B／C／D／E／F` ≥4）。
  - **F-06（P1）**：F5 的 [A] 逐字枚举五项，同批 F8 加检查 F → 落笔即过时 → 改**指向式**（引 `测试与验收标准` §1.1）。
  - **F-07（P1）**：§一 ⑪ porcelain 写 1、实测 **2** → 改 2 并注明来源。
  - **F-08（P1）**：必读输入改面清单列 `:167`（无 F 项）而漏 `:186`（F2 目标）→ 换为 `:186`；§五 区间订正为 `:85`–`:99`。
  - **F-09（P1）**：§七 088 §八 13 标「本批处置（部分）」而理由自述「触发＝下批」→ 档位与理由相抵 → 改 **仍待**；汇总改 `已履行 3｜作废 0｜本批处置 0｜仍待 11｜待作者 1 ＝ 15`。
  - **F-10（P1）**：断言 7d 夹具描述不足（节标题须命中 `BAN_SECTION`／句干形态／路径须实存）→ F8 补**夹具骨架四条**。
  - **F-11（P2）**：顶层「检查 F」与正文子判据标签 `（F1）`／`（F2）` 字面撞车 → 头注写明两者不同族。
  - **F-12（P2）**：「未点名的节不读」与交底第 1 项「先读 `AGENTS.md`」冲突 → 补「整件点名即读整件」。
  - **F-13（P2）**：F4 使 §三 多一块而标题／目录未同步；F5 落点未定 → F4 补「同步 §三 标题 `:57` 与目录行 `:11`」，F5 补「插在『报告纪律』条之前」。
  - **F-14（P2）**：§一 ⑥ 区间 `:85`–`:97` 不含其枚举末项（止损三条在 `:99`）→ 改 `:85`–`:99`。
  - **F-15（P2）**：§七 必列面注称「不必重列：无」而 088 §七 实载 086 §八 1／3／6 → 补索引指针。
  - **F-16（P2）**：`规格写作标准` §三 机检锚点表缺 §5.13 与检查 F 的行 → 入 F10 同批补两行。
  - **观察采纳四条**：① §一 ① 的成本数标「**不可复跑**」② F1 补「外部窗口形态无 subagent，本条不适用」③ F1 补「规划方须约定回报点、主动取回报」④ F8 写明「与检查 E 判一**互补不重叠**」（防后人误删其一）。
  - **未采纳**：观察 3 的后半（后台 spawn 的停摆受理纪律需宿主侧证据）——本仓无运行时证据，留 §八 观察。
- 待填：**窄域复审**（P0≥2，按 `施工机制` §五 止损条 2：只核原 P0 修复项 ＋ 其直接影响面）→ 作者确认 → 施工（subagent，**后台 spawn**）→ 核验三步 → 双维产物审查 → 归档。

- 2026-09-15｜**窄域复审**（P0≥2，按 `施工机制` §五 止损条 2：只核原 P0 修复项 ＋ 直接影响面）｜结论 **仍有问题**（残留 1 条 P0 ＋ P1×2 ＋ P2×3）——**已全部处置**：**P0-1**（cmd5 期望 4 与 F10 强制的第五产处「`规格写作标准` §三 表行」互斥，实为 **5**）→ 期望改 5、五处逐处列举、F2 末句同步（**本批新立「范围与期望同源」条在自己身上的第二次违例**）；**P1-a**（§四 自审仍写旧值 7）→ 改 6；**P1-b**（断言 7e 要求屏面无「禁改面词」字样，F8 只要求不产候选）→ F8 补「禁词表为空时完全静默」；**P2-a**（夹具 ③ 未写明节标题须命中 `CHANGE_SECTION`）→ 补；**P2-b**（断言 9 括注「两份在制规格」在 088 归档后不成立）→ 改述；**P2-c**（不变量对 `规格写作标准.md` 的节级描述漏 §三）→ 补全。**另**：088 于本规格送审后完成**收口**（§八 新增 16＝走查欠账、归档提交）→ §七 补该行、汇总由 15 改 **16**；该行未在成稿审查与窄域复审范围内，此处明示。
- 2026-09-15｜**双维产物审查**（独立上下文只读 subagent；改规则／结构／契约批 → **双维**）｜findings 共 **5**：**P0:0** P1:1 P2:4｜结论 **修订后通过**。审查方**独立批式实跑**断言 1–11，**与核验同值**；逐 hunk 核改动面（porcelain **6**、禁改面零 diff）；锚点对 `git show HEAD:` 改前件逐处复核；逐字落实扫全部 `[A]` 引号块（**唯一 MISS 即 V-01**）；残留扫描（`自审三条`／`五类`／`A／B／C／D／E`／`[4]` 枚举载体）**无未登记残留**；**判别力实测 8 个夹具**（含反引号**中文** token 排除、通用词误报面、完全静默对照、本规格注入述谓句）——全部删净。处置：
  - **V-01（P1，粗体漂移）**：F2①③ 的 `[A]` 目标多写了 `**`，而两处原文（`施工机制:49` 引导句、`规格写作标准:121` 锚点行）**本就无粗体** → 实件无粗体、`检查 C` 常驻一条假漂移候选（且该候选曾由施工方以一句**不成立的取证**登记为「属预期」——放宽后的 grep 才命中）。**裁定：粗体非字面**（就实件风格），规格 F2 两处目标去 `**`（**规格侧修正，实件不动**）→ 候选随之消失。判据：088／087 的 `[A]` 粗体落地属**原件本有粗体**，与本例情形不同。
  - **V-02（P2）**：断言 7e 括注误写为「禁改面与目标文本无词面交集」（实测**有**交集）→ 改述为**真因**「§五 无可抽取述谓句（禁词表为空）故完全静默」。
  - **V-03（P2）**：F2 末句「该串…全仓恰 5 处」指代不清（所引 154 字块实为 3 处；5 是**短语**在两件内的次数，全仓含 `CHANGELOG` 为 6）→ 改述为「**该短语**在两件内恰 5 处（全仓 6）」。
  - **V-04（P2）→ F12**：`规格写作标准` §三 的 F 行条文列「§4 附加项·禁改面禁词」**指向不存在的子项**；且检查 E 无行／F 有行不对称 → 补修轮改条文列并新增 E 行（前缀侧）。
  - **V-05（P2）→ F13**：检查 F 的已知误报面只登记在 089 §八 3（随归档沉底）→ 按「债跟着主题走」补 `测试与验收标准` §4 的 **G19** 行。
  - **观察采纳**：① `施工机制:29` 新 bullet 句末无「。」（属 `[A]` 逐字落地，不改）② §八 6 的第三载体漂移实测复现、处置到位（下批补 `check.sh`）③ `CHANGELOG` 与规格 §定位 的「五条」分组口径不同（各自自洽，不构成冲突）。
- 2026-09-15｜**补修**（施工方）｜三处落地：`规格写作标准` §三 的 F 行条文列改「§4 附加项（禁改面对账·词面侧）」（旧指针串 `§4 附加项·禁改面禁词` 全件归零）＋ 新增相邻一行「§4 附加项（禁改面对账·前缀侧）」（检查 E 的对称记法）＋ `测试与验收标准` §4 新增 **G19**（检查 F 取词面的通用词误报面）。断言 12 三条实测 1／1／1，受影响面（8a／8b／9／10／7g）复测一致；porcelain **6**；过程自纠一次（补修记录首稿在代码跨度内写带尾随空格的 token → MD038 ×3 → 改整条命令形态后 lint 0 issue、门禁 rc=0）。
- 2026-09-15｜**规划方复核（修复面）｜通过**：三处改动逐处读原文核对＝到位（`规格写作标准:132`／`:133` 相邻两行、`测试与验收标准:83` 的 G19 三列齐）；断言 12 **独立重跑** 1／1／1、旧指针串 0；门禁 rc=0、lint 0 issue、porcelain 6。**另修规格两处不同步**：① **F10 与 F12 相抵**——F10 仍写补修前的旧行文「§4 附加项·禁改面禁词」，已加指向 F12 的同步注（保留施工当时原样供对照）② **F12 的两处目标粗体**按 V-01 裁定（粗体非字面）去掉。**[4] 段残留候选 2 条，逐条判「属预期」**：① `§4 自审四条齐备 @ docs/specs/施工机制.md`＝F2 行跨两件而 `检查 C` 只认该行**首个可解析路径 token**（该串实存于 `规格写作标准.md`，实测 1）② `` `[A]` 未落实或已漂移 @ docs/standards/规格写作标准.md ``＝F10「落盘形态注」**自引分析器消息串**。**无待处置项**。
- 2026-09-15｜**收口**｜本规格**验收通过**（成稿审查 16 ＋ 窄域复审 6 ＋ 外部评审 7 ＋ 双维产物审查 5，共 34 条意见全处置；P0 三条均在规格侧、无产物缺陷）；本批**不触用户可观测行为**，按 `施工机制` §二 重路径行**免真机走查** ✓。规格移入 `docs/specs/archive/`，呈作者确认提交。

- 2026-09-15｜**成稿审查·第二轮（外部评审）**（作者转呈的独立评审报告）｜findings 共 **7**：**P0:0** P1:2 P2:5｜结论 **修订后通过**。规划方**逐条取证后**处置：
  - **P1-1（采纳，改进）**：F2 的「三处正文列表各追加第 4 项」原仅由 cmd5 的计数（5）间接覆盖——该计数实为**双射**（五处源各贡献 1，缺一即 4 必红），故原判据**并未失守**；但采纳其建议**单列一条**：新增 `grep -o "4. \*\*范围与期望同源"` → 期望 **3**（以编号前缀 `4.` 与 §5.13（13.）及 §三 表行区分）。
  - **P1-2（采纳）**：F8 的维护入口函数名原为 `[B]`（施工方定名）→ 断言无从精确判定 → **改钉 `check_ban_words()`**，并新增两条断言（`^def check_ban_words(` ＝1／`check_ban_words(text, root)` ＝1）。
  - **P2-1（驳回，附证据）**：评审称「§五 表格后无『报告纪律』条，实为『报告落点』」——**与实况相抵**：`施工机制:94` 现文即「- **报告纪律**：**计数／行数／条数类发现须附实测命令与输出**…」（实测 `grep -c "报告纪律"` ＝ **2 行**／`报告落点` ＝ 1 行，两者并存）。F5 的「插在『报告纪律』条之前」**表述正确、不改**。驳回依据＝`施工机制` §五 报告纪律条要求「计数类发现须附实测命令与输出」——本条即因其未附实测而不成立。
  - **P2-2（采纳）**：F4 的 §三 标题／目录同步原无断言 → 新增 `grep -o "核验三步／实跑纪律／过程产物两项"` → 期望 **2**（实测现文 `:11`＋`:57` 各 1，改后仍 2）。
  - **P2-3（已覆盖）**：F8 现文已写明「**无该类句则禁词表为空——此时本检查完全静默**（不打汇总行、不打空转明示，输出不含「禁改面词」字样；否则断言 7e 会因字样出现而假红）」——即评审所求的「空表静默与断言 7e 的关系」，无需再改。
  - **P2-4（不采纳，附理由）**：评审建议把断言 9 的链接核对一行式改为 `.tmp/089-probe/link-check.py`。**不采纳**：① 该命令是 087／088 起沿用的**既有断言形态**（同一命令跨批复用，便于对照）② 断言须**独立可跑**，改为临时脚本会引入「脚本须先存在」的外部依赖，反使断言不自足。判据说明按需在核验记录中口头给出。
  - **P2-5（采纳）**：断言 10 补处置句「若 088 因故未收口而施工，以实况为准并在核验记录里订正本行，不因此判红」。
  - **评审述评（§五 表第 4 项）更正**：评审称「§七 核销 088 §八 16 条 ⚠️ 无法独立核验（088 规格未提供）」——**088 规格在库**（`docs/specs/archive/088-2026-09-15-阶段一资产与降级纪律.md`，本批收口提交 `1ff612f` 时归档），可随时独立核验。

- 2026-09-15｜**施工**（subagent，后台 spawn）｜完成：**F1–F11 全落**（`施工机制.md` 12 处＝diff 12 hunk：目录行 `:11`／§一 `:29`／§二 三条 bullet 后新 bullet／`:49`＋§二第 4 项／§三 标题／§三 实跑纪律段／§五 机器已覆盖面条／§八 模板标题＋第 4 项／§八 头注说明／§八 交底 6／7；`规格写作标准.md` 5 处＝diff 5 hunk：`:46` 标题／§4 第 4 项／`:121` 锚点行／§5 第 13 条／§三 表两行；`测试与验收标准.md` 2 行＝§1.1 `:26`＋§3 `:106` 枚举补全为六项；`check_spec.py`＝新增检查 F（`check_ban_words()`／`_ban_words()`＋常量 `BAN_WORD_CLAUSE`／`BAN_WORD_TICK`／`BAN_WORD_RUN`）＋头注 8 处「五类→六类」＋4 处字母枚举补 F＋维护入口＋派发处 A–F 顺序；`CHANGELOG.md` 一行，条目限长内）。
  **检查 F 判别力自证**（`.tmp/089-probe/fixture.md`，四条结构条件齐备：`## 六、禁止事项` 节／「不得把（心流锚、设计理由）类的设计语言搬进 skill 资产」述谓句／`## 二、文件级改动清单` 节＋实存路径列／目标侧引号块含「心流锚」）：`python scripts/check_spec.py --static .tmp/089-probe/fixture.md` → 报出 `· 检查 F 禁改面词 心流锚 出现在改动行 1` ＋ `· 检查 F 汇总: 禁改面词 2 个｜核对改动行 1 行｜命中 1 条`（**禁词表恰取「心流锚」「设计理由」**——分类语「类的设计语言」不紧邻顿号故不入表）；对本规格自身实跑 → `grep -c "禁改面词"` 实测 **0**（完全静默）。**夹具已删净**（`rm -rf .tmp/089-probe`，实测 `gone`）。
  **实跑（批式，脚本一次跑完）**：断言 1–11 逐条实测全部达期望（命令→实测→期望 逐行见核验请求）；`bash scripts/check.sh` exit **0**（尾「共 6 段：已跑 4｜跳过 2」／「== 检查全绿 ==」）、`npx markdownlint-cli2 "**/*.md"` **0 issue**、链接一行式 **175‧0**、porcelain **6**＝5 件 M（`CHANGELOG.md`／`施工机制.md`／`测试与验收标准.md`／`规格写作标准.md`／`check_spec.py`）＋1 行未跟踪（本规格）——**与规格所写逐个吻合**，代码件 `ast.parse` 通过。
  **偏差申报（P1×2，均在核验请求中详列）**：① **断言 7g 与 7f 互斥**——`grep -c "check_ban_words(text, root)"` 期望 1 **不可达**：任一满足 7f（`^def check_ban_words(`）的实现，其 `def check_ban_words(text, root):` 行本身即含该调用串，故实测恒 **2**（def 行 ＋ 派发行各 1）。已按「以实况为准、不改断言期望」处置，**建议期望改 2 或判据收窄为 `= check_ban_words(text, root)`（后者实测 1）**；② **F10 两行引号块用全角 `｜` 作表行分隔符**（规格内表格不能含裸 `|`），落盘为 markdown 表格后字面必不相等 → check C 对这两行必产「[A] 未落实或已漂移」候选（另 1 条同族：F2 行跨两件而 check C 只认首个可解析路径 token，故 `:121` 锚点文本被记在 `施工机制.md` 名下核）；三条候选中两条即时自证为 **属预期**（`grep` 实测目标文本在 `规格写作标准.md` 内实存）。**未改任何断言／数字／检查求绿。**
  **观察（非本批可动，交规划方）**：`scripts/check.sh` `:10`／`:111` 的 `[4]` 段标题行枚举（`断言自噬／计数重算／[A] 逐字／写作检查`）同样漏列检查 E／F——`check.sh` 属本批 §五 禁改面故未改，**建议与 F9 同口径收口**（免第三载体漂移）。

- 2026-09-15｜**规划方核验（三步）**｜**通过**。① **改动面核对（逐 hunk）**：porcelain **6 行**（5 件 M ＋ 1 行未跟踪），逐 hunk 读 `git diff` 均在 §一 不变量逐件列举内；**禁改面零 diff 实测**（`AGENTS.md`／`README.md`／`scripts/check.sh`／`skills/`／`docs/product/`／`docs/specs/archive/` 各 0 行）；diffstat＝5 件 ＋107／−24。② **断言独立重跑（批式）**：§三 断言 1–11 全部子项自跑，**与施工方报值逐条一致**（1a 1／1b 0／2a 0 文件／2b 2 文件／2c 0／2d 0／2e 5／2f 3／3 1／4a 1／4b 2／4c 1／5 1／6a 1／6b 1／7a 10／7b 0／7c 11／7d 0／7e 4／7f 1／7g **1**（收窄后）／8a 2／8b 2／10a 6／10b 3／11 1 且行长 **289** ≤400）；门禁 `check.sh` **rc=0**、lint **0 issue**、链接 **175／0**。③ **内容抽查 ＋ 判别力自证**：**独立重建夹具**（`.tmp/089v/fixture.md`，四条结构条件齐）实跑 → `check_ban_words()` **报出**「禁改面词 心流锚 出现在改动行 1」＋汇总行；**对本规格自身实跑 → 0 条（完全静默）**；夹具用后删净（实测目录已不存在）。读核实现：`check_ban_words()` 的判据、静默硬条件、「顶层第六类 vs 子判据标签不同族」注、`_ban_words()` 取词与 `_target_blocks()` 核对面**与规格逐条吻合**。
  - **施工方两条偏差申报的处置**：**①（照准并订正规格）**断言 7g 原判据（全串）实测 2 与期望 1 互斥——根因是 `def check_ban_words(text, root):` 行自身含该串；判据收窄为 `= check_ban_words(text, root)`（实测 1）。**施工方未改 def 形参去凑期望，处置正确**。**②（照准并订正规格）**F10 两行的 `[A]` 引号块以全角 ｜ 表表格形态、实件落半角 `|`，`检查 C` 必产 2 条候选——已在 F10 补「落盘形态注」，判属预期。
  - **登记一条**：`scripts/check.sh` 的 `[4]` 段标题行枚举仍四项（第三载体漂移）→ §八 6。
- 2026-09-15｜**补修**（subagent）｜处置产物审查 F12／F13（V-04／V-05）＋重跑受影响面。**改动两件三处**：① `规格写作标准.md` §三 F 行「条文」列：「§4 附加项·禁改面禁词」→「§4 附加项（禁改面对账·词面侧）」（旧指针指向 §4 附加项并不存在的子项，不可回指）；② 同表**新增相邻一行**（置 F 行**上一行**，循 `检查 E`→`检查 F` 的检查序）：条文列「§4 附加项（禁改面对账·前缀侧）」／强制方式列「静态自检的三方对账（`check_spec.py --static`，接入 `check.sh` [4]）」／状态列「已实现」——实测两行落 `:132`／`:133` 相邻、旧指针串全件归零（`grep -c "§4 附加项·禁改面禁词"` 实测 **0**）；③ `测试与验收标准.md` §一.4 已知缺口表末尾新增 **G19**（三列半角竖线）：缺口＝检查 F 取词面为顿号分隔中文词组、通用词（断言／数字／锚点类）易误报（实测夹具「不得把（断言、数字）搬进 skill 资产」＋目标文本含该词即报 **2** 条），触发＝出现大批「人工逐条判为预期」候选时按类收窄取集源——债跟着主题走，补上原只登记在 §八 3 的落点。**口径说明**：F12 两处目标串在规格内含 `**`，而该表「条文」列无粗体先例 → 按 V-01 裁定「粗体非字面（就实件风格）」落为**无粗体**（F12／F13 均为 `[B]`，`检查 C` 只核 `[A]` 行，故不生假漂移）；此点列为观察交规划方——若须带粗体，请在规格侧标注后同轮改落。
  **实跑（批式）**：断言 12 三条 → `禁改面对账·词面侧` **1**／`禁改面对账·前缀侧` **1**／`grep -c "^| G19 "` **1**，全达期望；受影响面 → 断言 8 两条仍 **≥2**、`bash scripts/check.sh` **exit 0**（「== 检查全绿 ==」）、`grep -c "^\[2\] "` **1**、`npx markdownlint-cli2 "**/*.md"` **0 issue**（未用 `--fix`）、链接一行式 **175‧0**、porcelain **6**（逐行＝`CHANGELOG.md`／`docs/specs/施工机制.md`／`docs/standards/测试与验收标准.md`／`docs/standards/规格写作标准.md`／`scripts/check_spec.py` 五件 M ＋ 本规格 1 行未跟踪）、`ls skills/abzu-outline` **3**、断言 7g 收窄后判据复测 **1**（与规格修订一致）。
  **`[4]` 段候选 12 条逐条判属预期**（较上轮 10 条增 2 条，新增两条来源已逐条查明）：④「目标文本未提及且现行为 0」的 token＝断言 12 第三条的命令模式（`grep -c "^| G19 "`）——`^` 与 `|` 是正则元字符而工具按**字面**搜，故 cur 恒 0；该命令实测 **1** ✓，与既有条目的 `^def check_ban_words(`／`grep -c "^\[2\] "` 同族（同一取 token 局限）。⑤ `检查 C` 报出的「\[A\] 未落实或已漂移」候选，其核对面是 F10 新增「落盘形态注」**自身的引号块**（该引号块字面即该候选语）——注内已登记「必产…候选，属预期」，**非漏改**。**V-01 的粗体类假漂移候选已消失**（规格侧去 `**` 后字面失配消除）；现存同 token 候选的真因＝**F2 行跨两件而 `检查 C` 只认首个可解析路径 token**（`施工机制.md`），而该文本实落 `规格写作标准.md`（`grep -c "§4 自审四条齐备"` 实测 **1**）——上一轮我以「同族跨件」为据登记「属预期」的取证**当时不成立**（真因是粗体失配，V-01 已裁定），本补修轮如实订正该口径。
  **偏差申报：无新增**；`.tmp/089-fix/` 批式脚本与输出用后删净。
