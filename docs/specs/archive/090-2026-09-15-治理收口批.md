# 090-2026-09-15-治理收口批（G20／G21）

> 定位：清掉外部评审留在 `测试与验收标准` §4 的两笔待收口——**G20**（守卫与工具文档的**批次叙事**收敛：`check_spec.py` docstring 与 `check.sh` 段注）与 **G21**（两处标准件的**数字来源与依据强度**：`安全与合规标准` §4.1 限速值、`运行环境标准` §2.6 ugrep 条款）；顺带把 089 §八 6 的 `check.sh` `[4]` 段枚举漂移一并收口（本批已动该件）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径**——**四件**跨文件（`scripts/check_spec.py`／`scripts/check.sh`／`docs/standards/安全与合规标准.md`／`docs/standards/运行环境标准.md`），**超过轻行「复盘驱动的治理微调 ≤ 3 文件」上限**（故不适用免规格），且改规则 → 走 **双维产物审查**；不触 `skills/`，**免真机走查**。
> 自由度分布：[B]×5（五行 F1–F5/F6 **全为 [B]**——外部评审 F-03 订正；该族**第四次**复发：`archive/068` F-01、`archive/069` 同族第三次，见 §二 F6 的 G22 登记）
> 必读输入：本规格 §一；`scripts/check_spec.py`（docstring `:1`–`:71`；**§一 ④ 列出的不可动技术清单**）；`scripts/check.sh`（头注 `:1`–`:12`；`[2]` 段 `:41`–`:110`；`[4]` 段标题行）；`docs/standards/安全与合规标准.md`（§4 `:25`–`:35`／§三 机检锚点 `:64`）；`docs/standards/运行环境标准.md`（§2 `:18`–`:26`）；`docs/standards/测试与验收标准.md` §4（G19／G20／G21 三行）
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `check_spec.py` docstring 的批次叙事密度 | docstring **70 行**（`ast.get_docstring` 实取）；**收敛面＝批号 3 处 ＋「本批…」类裁定括注 2 处 ＝ 5 处**（`075 批 F1`／`078 批 F4`／`083 批` 各 1；「本批（065）」「本批改为」各 1）——**另有通用词 2 处**（「历批实证」「同一批」）**落在须逐字保留的子进程宿主语义句内、不属收敛面**。**再另**：docstring **之外**的代码注释另有 **8 行**含批号（`:84`／`:90`／`:112`／`:122`／`:266`／`:309`／`:336`／`:784`）→ 按 §八 1 登记为**下批残余**、本批不动（外部评审 F-01／F-07 订正：初稿「3＋5＝约 8」重复计数、且把通用词误列收敛面） ✓ |
| ② | **docstring 的技术承载（不可动面）** | 该节**自称「不可审计类（六类，枚举的唯一落点＝本节；判据实现见 `unauditable()`）」**——即它同时是**执行面白名单**（可执行族 **7 族**：`e?grep` 计一族 ＋ `fgrep`／`wc`／`head`／`tail`／`ls`／`cat`；git 子命令白名单 **5 个**——外部评审 F-02 订正：初稿「9 个」无出处）与**不可审计六类枚举**的单一真源，并载有子进程宿主语义、检查 A–F 口径要点、**维护入口**与**退出契约**；`施工机制` §四 的段序真源虽为 `check.sh` 头注，但本节是维护者的唯一入口 ✓ |
| ③ | `check.sh` `[2]` 段 ⑦ 注释 | `:45`–`:46`「另**段行一条**：⑦头注段行（本段已由头注改列 0 落地，非逐壳判据；段状态由 `_seg_summary` 按头注取）」——外部评审 P2-2 判「难懂」 ✓ |
| ④ | `check.sh` `[4]` 段标题行枚举（089 §八 6 登记） | `:10`／`:111` 现文枚举**四项**（`断言自噬／计数重算／[A] 逐字／写作检查`）——`check_spec.py` 早已是**六类**（A–F），此处是**第三载体漂移** ✓ |
| ⑤ | `安全与合规标准` §4.1 | `:28`「限速：对外部网站的请求间隔 ≥ 2 秒；失败退避重试 ≤ 3 次（2s／4s／8s）；部分成功时在 stderr 明示缺失规模。〔出处：旧仓《文件规范》脚本编写规范「限速」行；旧仓域采集脚本契约〕」——有〔出处〕但**未按 `skill形态标准` §10.2 标〔自定〕**、未声明可否被覆盖（外部评审 P2-6） ✓ |
| ⑥ | `运行环境标准` §2.6 | `:23` 一条含**三层**：规范句（「守卫与断言实现不得依赖转义差异」）／实测事实（「本机 `grep` 可为 ugrep 的函数封装（实测 `ugrep 7.8.4`）」「未稳定复现」）／出处注——实测与规范混排，评审 P2-7 判「依据偏弱」 ✓ |
| ⑦ | 基线 | `bash scripts/check.sh` exit **0**；lint **0 issue**；链接一行式 **175／断链 0**；porcelain **1**（仅本规格未跟踪；外部评审 F-04 订正） ✓ |

**不变量（施工前后须同）**：本批改动**全部落点**：`scripts/check_spec.py`（仅 docstring 的**批次叙事与裁定标记**）／`scripts/check.sh`（`[2]` 段 ⑦ 段注 ＋ `[4]` 段标题行枚举）／`docs/standards/安全与合规标准.md`（§4.1 一行的来源与可覆盖声明；**条目失效时同批更新 §三 机检锚点 `:64` 行**）／`docs/standards/运行环境标准.md`（§2.6 一条的重排）／`docs/standards/测试与验收标准.md`（§4 新增 **G22** ＋ **G20／G21 两行同批改标**）／`CHANGELOG.md`；**除此之外零改动**（`AGENTS.md`／`README.md`／`skills/`／`docs/product/`／`docs/specs/`（含 `archive/`）零改动）。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/check_spec.py:1`–`:71`（docstring） | **收敛批次叙事与裁定标记**：**收敛面 5 处**（批号 3 ＋「本批…」裁定括注 2）→ **保留**「事故出身」语义但改为**指向式**（形如「事故出身：见 `docs/specs/archive/065-2026-09-12-规格静态自检器搬回.md`（事故四型）」——初稿示例有乱码且误指 `075 条`（该条不含事故四型），外部评审 F-08 订正），并**顶部加一行索引**：「事故出身与历次裁定：见 `CHANGELOG.md` 对应条目与 `docs/specs/archive/` 各批规格」。**不动**（外部评审 F-06 补全三段）：执行面白名单（可执行族 **7 族** ＋ git 子命令白名单 5 个）、**不可审计六类枚举**（该节自称「枚举的唯一落点＝本节」，逐字保留）、**默认跑法**（三态呈报／不做通过失败判定／两种断言载体的提取口径）、**载体形态说明**（两种载体形态／表格列角色表头别名／检查 B 对象口径／检查 C 逐字与方向标记）、**呈报守恒**（三态判据计入六类、E／F 汇总行不计入、「· 无发现」条件）、子进程宿主语义、检查 A–F 的口径要点、**维护入口**、**退出契约**、用法与参数、依赖与前置 | [B] | 外部评审 P1-3／P2-5；「债跟着主题走」 |
| F2 | `scripts/check.sh`（`[2]` 段 `:45`–`:46` 段注 ＋ `[4]` 段标题行 `:10`／`:111`） | ① ⑦ 段注改述为：「另**段行一条**：⑦**头注段行校验由 `_seg_summary` 统一处理，不在本段逐壳判**（**段序与段状态按头注取**；本段已由头注改列 0 落地——`，` 非 `、`：施工偏差① 指出初稿引文用顿号、与断言 4a 及 G20 登记文的逗号不一致，**判据以验收断言为准**，规格已同步）」② `[4]` 段标题行枚举由四项**补全为六项**（补「三方对账／禁改面禁词核对」），**与 `check_spec.py` 头注的六类同口径**——**两处载体同批同步**（实测确认：`:10` 的头注行 ＋ `:111` 的段标题 `echo` 行，两处现文皆为四项） | [B] | 外部评审 P2-2；089 §八 6（第三载体漂移，本批收口） |
| F3 | `docs/standards/安全与合规标准.md`（§4.1 `:28` ＋ §三 `:64`） | §4.1 行**补来源档与可覆盖声明**（逐字）：「限速：对外部网站的请求间隔 ≥ 2 秒；失败退避重试 ≤ 3 次（2s／4s／8s）；部分成功时在 stderr 明示缺失规模。〔出处：旧仓《文件规范》脚本编写规范「限速」行；旧仓域采集脚本契约〕**〔自定〕**——本阈值为**自定阈值**（**非外部书证**、非实测；上列〔出处〕为该条款的**沿革来源**，阈值取值本身判自定——双维审查 V-03 订正措辞以免与〔出处〕相抵），**可被域配置或 CLI 参数覆盖**（覆盖时在脚本文档头四要素的「依赖与前置」行声明实际取值）。」；§三 机检锚点 `:64` 行同步（条文列加「〔自定〕可覆盖」） | [B] | `skill形态标准` §10.2；外部评审 P2-6 |
| F4 | `docs/standards/运行环境标准.md`（§2.6 `:23`） | 该条**重排为「规范句在前、实测与未复现入出处注」**（逐字；**规范句照原样**——原句为「守卫与断言实现不得依赖转义差异」，无 `grep` 反引号，不得改动）：「6. 命令行工具语义不可假定为 GNU 版：**守卫与断言实现不得依赖转义差异**——固定串用 `-F`、模式匹配优先用脚本语言（Python／Node）；核验复核以脚本语言实测为准。〔出处：063 批规格 §八 工具假阳性记录；**辅助事实**（个案、未稳定复现）：本机 `grep` 为 ugrep 函数封装（实测 `ugrep 7.8.4`），`\*` 等转义语义与 GNU grep 不一致〕」 | [B] | 外部评审 P2-7（规范选择为主、本机实测为附注） |
| F6 | `docs/standards/测试与验收标准.md`（§4 已知缺口表末尾） | （现状：无 G22 行）→ 新增一行（三列半角竖线）：「G22｜规格头注「自由度分布」与 §二 改动清单的**自由度列**不自洽（本仓**第四次**复发：`archive/068` F-01、`archive/069` 同族第三次、**090 规格初稿**）——该族**可机械化**（两侧都是可数结构）｜触发＝出现第五次，或下一批为 `check_spec.py` 增检查 G（头注分布 ↔ 清单自由度列自洽核对，候选非阻断）时一并实现」（**落盘形态注**：引号块内以全角 ｜ 表单元格分隔，系**表格形态占位**；实件按该表既有行风格落**半角竖线**——施工偏差② 照准，判属预期。**另**：编号列**不加粗**（全表其余行皆不加粗，双维审查 V-04）） | [B] | 外部评审 F-03；本批同族第四次的教训 |
| F7 | `docs/standards/测试与验收标准.md`（§4 的 **G20／G21 两行**） | （现状：两行触发列写**未来时**——「触发＝下一次触碰该两件时…」／「触发＝下批触碰该两件时…」）→ **本批即那次触碰、两笔已执行完毕**，照同表 **G16 体例**在触发列前补「**本批处置（090 F1／F2①）**」／「**本批处置（090 F3／F4）**」，触发列**各按本行实况**改述——G20：「**残余**＝`check_spec.py` 代码注释批号、`check.sh` 其余批号（见 090 §八 1）；下次触发＝残余收敛时」；G21：「**残余＝无**（标来源与重排两笔均已落）；下次触发＝出现同类来源／依据缺口时」（**补修偏差①的裁定**：交底原给两行同一句尾串属误——G21 的残余为**无**；该句为 `[B]` 级措辞、在 F7 方向内，由**规划方就地改**并记于 §九） | [B] | 双维产物审查 **V-01**（P1）；`施工机制` §三「本批内被处置的开放项须同批改标」＋同表 G16 先例 |
| F5 | `CHANGELOG.md`（`### Changed` 段首） | 一行：『治理收口（090）：…』——G20／G21 两笔 ＋ 089 §八 6 收口；≤400 字符、末尾「核验：」句 | [B] | 过程产物 |

**口径要点（易错）**：① **F1 的铁律**：能删的只有**批次号与裁定括注**，「不可审计六类枚举」「执行面白名单」「维护入口」「退出契约」**逐字不动**——收敛后须 grep 复核这些串仍在（断言 2）；② F2② 的枚举补全须**两处同口径**（`:10` 头注 ＋ `:111` 段标题 echo **两处**，实测两处现文皆为四项）；③ F3／F4 的改写**不动规范句本身**（只补来源档／重排次序）。

## 三、验收断言（一条命令一期望；计数口径逐条标注）

### 断言 1｜基线

```bash
bash scripts/check.sh
```

期望 exit **0**。**预验基线 0**。

### 断言 2｜F1：叙事收敛、技术清单逐字不动

```bash
python -c "import ast,io;d=ast.get_docstring(ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read()));print(d.count('075 批 F1')+d.count('078 批 F4')+d.count('083 批'))"
```

期望 **0**（**计出现次数**——**扫描面＝docstring**（`ast.get_docstring` 取，不依赖行号，F1 改动后仍准）；**改前 docstring 内实测 3**。**全件**为 11（另 8 处在代码注释里、按 §八 1 属下批残余）——**判据必须限 docstring**，否则施工后必剩 8、断言恒假（外部评审 F-01，本批 P0））。**预验基线 3**。

```bash
grep -o "事故出身与历次裁定" scripts/check_spec.py | wc -l
```

期望 **≥1**（**计出现次数**——**顶部索引行**在位）。**预验基线 0**。（**不用 `事故出身` 作判据**：该串改前已有 1 行，用它即恒真——自审第 2 条实跑查出后订正。）

```bash
grep -o "不可审计类" scripts/check_spec.py | wc -l
```

期望 **≥1**（**计出现次数**——枚举唯一落点标识仍在）。**预验基线 1**。

```bash
grep -c "^def unauditable(" scripts/check_spec.py
```

期望 **1**（**计行**——判据实现未动）。**预验基线 1**。

```bash
python -c "import ast,io;ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read());print('ok')"
```

期望 **ok**（**语法自检**）。**预验基线 ok**。

```bash
grep -c "维护入口" scripts/check_spec.py
```

期望 **≥1**（**计行**——维护入口一节未丢）。**预验基线 1**。

```bash
python -c "import ast,io;d=ast.get_docstring(ast.parse(io.open('scripts/check_spec.py',encoding='utf-8').read()));print(sum(k in d for k in ['执行面白名单','退出契约','载体形态','呈报守恒','不可审计类']))"
```

期望 **5**（**计出现次数**——**五个技术承载锚串逐条在 docstring 内**；外部评审 F-06：原「逐字保留」只落自觉、无断言守护）。**预验基线 5**。

### 断言 3｜F1 判别力未降（回归）

```bash
mkdir -p .tmp/090-probe && printf '%s\n' '## 六、禁止事项' '' '- 不得把（心流锚、设计理由）类的设计语言搬进 skill 资产。' '' '## 二、文件级改动清单' '' '| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |' '| --- | --- | --- | --- | --- |' '| 1 | `.tmp/090-probe/f.md` | （现状：无）→ 新增：「正文出现心流锚」 | [A] | 夹具 |' > .tmp/090-probe/f.md && python scripts/check_spec.py --static .tmp/090-probe/f.md 2>&1 | grep -c "禁改面词" ; rm -rf .tmp/090-probe
```

期望 **≥1**（**计行**——docstring 收敛后检查 F 仍**报出**；夹具跑完删净）。**预验基线 ≥1**（收敛前同夹具亦报）。

### 断言 4｜F2：段注改述与枚举补全

```bash
grep -o "头注段行校验由 \`_seg_summary\` 统一处理，不在本段逐壳判" scripts/check.sh | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "三方对账／禁改面禁词核对" scripts/check.sh | wc -l
```

期望 **≥2**（**计出现次数**——`:10` 头注与 `:111` 段标题 echo **两处**同口径）。**预验基线 0**。（**不写并集判据**：旧四项尾巴「计数重算／[A] 逐字／写作检查」改前已有 2 处，并集形态会恒真——自审第 2 条实跑查出后订正。）

### 断言 5｜F3：来源档与可覆盖声明

```bash
grep -o "本阈值为\*\*自定阈值\*\*" docs/standards/安全与合规标准.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "可被域配置或 CLI 参数覆盖" docs/standards/安全与合规标准.md | wc -l
```

期望 **≥1**（**计出现次数**——**仅 §4.1 行一处**；§三 `:64` 按 F3 只加「〔自定〕可覆盖」短串、不含本长串，外部评审 F-11 订正口径）。**预验基线 0**。

### 断言 6｜F4：规范句前置、实测入出处注

```bash
grep -o "守卫与断言实现不得依赖转义差异" docs/standards/运行环境标准.md | wc -l
```

期望 **1**（**计出现次数**——规范句仍在且**仅一处**）。**预验基线 1**。

```bash
grep -o "辅助事实" docs/standards/运行环境标准.md | wc -l
```

期望 **1**（**计出现次数**——实测被降为出处注内的附注）。**预验基线 0**。

### 断言 7｜门禁、链接与改动面

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**。**预验基线 0 issue**。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**。**预验基线 175‧0**。

```bash
git status --porcelain | wc -l
```

期望 **7**（**逐类列举**：**6 件 tracked M**＝`scripts/check_spec.py`／`scripts/check.sh`／`安全与合规标准.md`／`运行环境标准.md`／`测试与验收标准.md`／`CHANGELOG.md` ＋ **1 行未跟踪**＝`?? docs/specs/090-…md`）。**预验基线 1**（仅本规格）；施工后逐行列核验记录（不符以实况为准并订正）。

```bash
ls scripts/hooks | wc -l
```

期望 **≥1**（本批**不动** `scripts/hooks/`；行数以实况记）。**预验基线**：实况。

### 断言 9｜F7：缺口行同批改标（双维审查 V-01）

```bash
grep -o "本批处置（090 F1／F2①）" docs/standards/测试与验收标准.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "本批处置（090 F3／F4）" docs/standards/测试与验收标准.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

### 断言 8｜过程产物

```bash
grep -cF '治理收口（090）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 七条实跑取证（①docstring 71 行与约 8 处批次标记、②技术承载的自我声明句原文、③`:45`–`:46` 段注原文、④`:10`／`:111` 枚举四项、⑤`:28` 原文、⑥`:23` 三段混排原文、⑦门禁三命令与 porcelain 0）。
2. **断言实跑过**（改前侧）：断言 1／2（**docstring 内批号 3**／0 事故出身与历次裁定／1 不可审计类／1 unauditable／ok／1 维护入口／5 技术锚串）／3（夹具报 ≥1）／4（0／0）／5（0／0）／6（1／0）／7（0 issue／175‧0／porcelain 1）逐条实跑。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**（`施工机制` §二 自审条第 4 条）：断言 2 的「期望 0」由**所列三个批次标签逐串实跑**得出（改前 3 行）；断言 4 的「≥2」由**该枚举在 `check.sh` 的两处载体**（`:10` 头注 ＋ `:111` echo）得出；断言 6 的「1」由**规范句仅出现于 `:23` 一处**（实测）得出；断言 7 的 porcelain 由**逐类列举**得出。

## 五、禁止事项

- 禁止触碰：`.git/`、`AGENTS.md`、`README.md`、`skills/`、`docs/product/`、`docs/specs/`（含 `archive/`）、`scripts/hooks/`、`docs/standards/` 除 F3／F4 点名两件外的内容。
- **F1 不得动技术清单**：执行面白名单、**不可审计六类枚举**、子进程宿主语义、检查 A–F 口径、维护入口、退出契约、用法与参数、依赖与前置——**逐字保留**（断言 2 复核）。
- **不得为收敛叙事而改** `check_spec.py` 的**任何代码**（只动 docstring；`ast.parse` 与断言 3 的判别力回归须过）。
- **不得改** `安全与合规标准` §4.1 与 `运行环境标准` §2.6 的**规范句本身**（只补来源档／重排次序）。
- 禁止改断言、改数字、删检查来变绿；`.tmp/090-probe/` 夹具用后删净（红线 2）。
- 禁止动作：任何 git 写命令；联网；安装依赖。

## 六、被否决的替代方案

- **只做 G21、不做 G20**（或反之）→ 否决：两笔同属外部评审留下的待收口、同批一次施工一次审查更省（**089 F3**「小改动攒批」条落于 `施工机制` §二 的同理）。
- **把 F1 的批次叙事全部删除**（不留指向式索引）→ 否决：守卫的**事故出身**是 `AGENTS.md` §四 的准入要求，删净即丢失出身；改指向式（保留语义、去掉会过时的细节）是正解。
- **给 `check_spec.py` 单独立「文档批」**→ 否决：G20 与 G21 是同一份评审的两笔，且 F2 顺带收口 089 §八 6，合并成一批最省。

## 七、本批核销（089 §八 全部开放项）

> **必列面**：089 §八 **全 7 条**（前一批的全部开放项）＋本批触及者。**不必重列**：088 §八（089 §七 已承载——索引指针＝`089` §七）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 089 §八 1 | 运行时资产引用治理文档的格式口径 | **仍待** | 落 `命名标准` §5／`skill形态标准` §9.1；触发＝下批触碰该两件 |
| 089 §八 2 | 批序顺延（资料收集顺延为 090） | **作废** | 已由 089 收口确立（本批为治理收口，非资料收集；批号不复改，归档件历史记录不动） |
| 089 §八 3 | 检查 F 假阳性观察 | **已履行** | 等价登记于 `测试与验收标准` §4 的 **G19**（089 补修轮落） |
| 089 §八 4 | ② 的进一步压降（规格内摘录真源段落） | **仍待** | 触发＝连续两批实测装载量未降 |
| 089 §八 5 | 088 收口待办 | **已履行** | 088 已于 089 施工前收口提交（`1ff612f`） |
| 089 §八 6 | `check.sh` `[4]` 段标题行枚举仍四项 | **本批处置** | **F2②**（本批动该件，顺带收口） |
| 089 §八 7 | 反模式清单补两条（后扩为三条） | **已履行** | 已由 2026-09-15 轻批落（`施工机制` §六 #13–#15，提交 `27b95dc`） |
| `测试与验收标准` §4 | G20（守卫与工具文档的批次叙事收敛） | **本批处置** | **F1／F2①** |
| `测试与验收标准` §4 | G21（两处标准件的数字来源与依据强度） | **本批处置** | **F3／F4** |

- 汇总（恒列五档、无该档计 0）：`已履行 3｜作废 1｜本批处置 3｜仍待 2｜待作者 0 ＝ 9` ✓

## 八、开放项

1. **G20 的残余**：① `check_spec.py` 的**代码内注释**（8 行含批号；**落地后实况行号**＝`:87`／`:93`／`:115`／`:125`／`:269`／`:312`／`:339`／`:787`——§一① 记的是**落地前**值，因 docstring 净增 3 行而后移，双维审查 V-02 订正）——本批只收敛 docstring；② `scripts/check.sh` 的其余批号（**落地后实况**＝`:14`／`:26`／`:48`／`:136`／`:142`／`:147`——F2① 把 `:45`–`:46` 并作一行致其后各点前移 1，双维审查 V-02 订正）——触发＝下批触碰该两件时同口径收敛。
2. **`命名标准` §5／`skill形态标准` §9.1 的补款**（089 §八 1 转记）：运行时资产引用治理文档的格式口径（可执行事实须自包含／追溯指针须标注「运行时不必加载」）——触发＝下批触碰该两件。
3. **真机走查欠账仍在**（`088` §八 16 转记）：触发＝089 之后或下一个域建成前必须补跑。
4. **大纲阶段二（资料收集／采风）** 尚未开工——本批清完治理账后即为下一批候选。

## 九、审查记录

- 2026-09-15｜**成稿审查·第一轮**（独立上下文只读 subagent）｜findings 共 **14**：**P0:1** P1:8 P2:5｜结论 **修订后通过**。**P0（F-01，本批最致命）**：断言 2① 原扫**全件**数批号（改前 11 行），而 F1 只动 docstring、§五 又禁改代码 → 施工后必剩 8、**断言恒假**，施工方三条路（违禁改代码／违红线改断言／停摆）全违 → **判据改为 `ast.get_docstring()` 只取 docstring**（不依赖行号、F1 改后仍准）、期望 0、预验 3；代码注释 8 行按 §八 1 登记为下批残余。**P1 八条（择要）**：F-02「执行面白名单可执行族 9 个」无出处（实测 **7 族**——`e?grep` 计一族）**且已抄进 F1 禁改清单**；F-03 头注 `[A]×2 [B]×4` 与五行全 [B] 不符（**该族第四次复发** → 新增 F6／G22 登记）；F-04 §一⑦ porcelain 0 与断言 7 的基线 1 互斥（改 1）；F-05 §四 自审 2 留旧值 3；**F-06 F1 禁改清单漏三段技术承载**（默认跑法／载体形态说明／呈报守恒）且原「逐字保留」**只落自觉** → 补清单 ＋ 新增 5 锚串断言；F-07 §一①「3＋5＝约 8」重复计数且把通用词「历批实证」误列收敛面；F-08 F1 示例串乱码且误指 `CHANGELOG 075 条`（不含事故四型）→ 改指 `archive/065`；F-09 `check.sh` 其余批号未登记。**P2 五条**：F-10（⑦ 段注丢「按头注取」）／F-11（断言 5② 口径括注与 F3 不符）／F-12（§二 口径要点② 悬空括注）／F-13（§六 引「`施工机制` §二 F3」属悬空指称 → 改「089 F3」）／F-09 的 `check.sh` 侧。**14 条全部照改**（规格侧），并在 §五 禁改清单与断言 2 同步加固。
- 2026-09-15｜**规划方核验（三步）｜通过**。① **改动面**：porcelain **7**（6 M ＋ 1 未跟踪），逐 hunk 均在 §一 不变量内；**禁改面零 diff**（`AGENTS.md`／`README.md`／`skills/`／`docs/product/`／`docs/specs/archive/`／`scripts/hooks/` 各 0 行）。② **断言独立重跑（批式）**：断言 1–8 全部子项自跑，**与施工方报值逐条一致**（2a 0／2b 1／2c 1／2d 1／2e ok／2f 1／2g 5／4a 1／4b 2／5a 1／5b 1／6a 1／6b 1／lint 0 issue／porcelain 7／行长 380）；门禁 rc=0、链接 175／0。③ **内容抽查 ＋ 关键自证**：**独立复跑**「剥去 docstring 后 AST 相等」→ **True**（F1 只动 docstring、代码零改动）；docstring 逐行 diff ＝ **6 hunk 全为「去批号／改指向」**（**订正**：原记「8 hunk」系目视误数——双维审查独立复算 `-U0`／`-U3`／difflib 三口径皆为 6，即 5 处收敛 ＋ 顶部索引 1 处），技术承载（执行面白名单 7 族／不可审计六类／宿主语义／维护入口／呈报前缀）**逐字未动**、事故**四型语义保留在指针内** ✓。
  - **施工两条偏差裁定**：**① 照准（逗号）**——⑧段注引文在规格内不自洽（F2① 顿号 vs 断言 4a 与 G20 登记逗号），施工方按「两载体优先」落逗号，**规格 F2 引文已同步**（判据以验收断言为准）。**② 照准（半角竖线）**——F6 引号块以全角 ｜ 表分隔、口径句又要求半角，实件落半角，规格侧补**落盘形态注**。
  - **施工观察四条处置**：② **§九 补记成稿审查条目**（本条）③ 规划方 `.tmp/*.out` 遗留**已删净**（红线 2）——**补记**：该次核验后又落 `.tmp/v090b.out`，已删（双维审查 V-05 指出记录与实况不符，如实订正）④ 行尾 CRLF **非阻塞**（依 `运行环境标准` §3.3「工作树 CRLF 不折腾」）⑤ §八 残余（代码注释批号）登记在位 ✓。
- 待填：**双维产物审查**（独立上下文）→ 归档 → 呈作者确认提交。

- 2026-09-15｜**施工**（subagent，后台 spawn）｜完成：**F1–F6 全落**，六件落点与 §二 逐条对齐——① `scripts/check_spec.py`：docstring 顶部加索引行（「事故出身与历次裁定：见 `CHANGELOG.md` 对应条目与 `docs/specs/archive/` 各批规格」）＋收敛面 **5 处**降为指向式（批号三处 `075 批 F1`／`078 批 F4`／`083 批`，裁定括注两处「本批（065）」／「本批改为」；事故出身段改指 `archive/065` 并把 020–025 六发事故四型语义留在括注内，`scripts/check_spec_assertions.py` 与两种载体形态字面保留），**代码零改动**；② `scripts/check.sh`：`[2]` 段 ⑦ 段注改述（含「段序与段状态按头注取」）＋`[4]` 段标题行枚举补全为**六项**（`:10` 头注与 `:111` echo 两处同口径，与 `check_spec.py` 头注 A–F 同序）；③ `docs/standards/安全与合规标准.md`：§4.1 补〔自定〕与可覆盖声明（规范句与限速值未动）、§三 `:64` 加「〔自定〕可覆盖」短串；④ `docs/standards/运行环境标准.md`：§2.6 重排（规范句「守卫与断言实现不得依赖转义差异」逐字前置、未被改字，本机 ugrep 实测降为出处注内的辅助事实）；⑤ `docs/standards/测试与验收标准.md`：§4 表末新增 G22 一行；⑥ `CHANGELOG.md`：`### Changed` 段首一行（实测 **380** 字符 ≤400，末句「核验：」）。
  **代码面等价自证**：`scripts/check_spec.py` 与 `git show HEAD:scripts/check_spec.py`（副本落 `.tmp/090-run/`）逐节点比对——剥去 docstring 后 AST 串相等（实跑 True），即除 docstring 外零改动；`ast.parse` 通过；检查 F 判别力回归夹具报 **2** 条（断言 3）。
  **实跑（批式，一个脚本一次跑完；命令→实测→期望 逐行见核验请求）**：断言 1–8 逐条达期望——**1** `bash scripts/check.sh` rc **0**（尾「== 检查全绿 ==」）；**2a 0**／**2b 1**／**2c 1**／**2d 1**／**2e ok**／**2f 1**／**2g 5**；**3 ≥1（实测 2）**；**4a 1**／**4b 2**；**5a 1**／**5b 1**；**6a 1**／**6b 1**；**7a** `npx markdownlint-cli2 "**/*.md"` **0 issue**（63 文件）／**7b** 链接一行式 **175‧0**／**7c** porcelain **7**（6 件 M：`CHANGELOG.md`＋三件标准件＋`check.sh`＋`check_spec.py`；1 行未跟踪＝本规格，逐行与 §三 断言 7 同）／**7d** `scripts/hooks` **1** 项（本批未动）；**8a 1**／**8b 380**（≤400）。
  **`[4]` 段候选逐条判（均属**预期**）**：①「自噬预警 `治理收口（090）` @ `CHANGELOG.md` 现行 1｜目标文本内 1｜近似预测 2」——断言 8 是**计行**命令且目标件是 `CHANGELOG.md`（非规格本体），工具把 F5 行目标侧的字面串也计入预测故高估；实跑 1 ≥ 期望 1，该串施工前在 `CHANGELOG.md` 中为 **0**（本批新增即产物本身），无恒真风险。②「目标文本未提及且现行为 0: `^def unauditable(` @ `scripts/check_spec.py`」——该 token 为带行锚的**计行**判据，工具按字面串比对（`^` 不计正则）故字面命中 0；实跑 1＝期望 1，判据实现未动。③「现行有值／目标文本未提及: 1 个」——**实证所计为 `维护入口` @ `scripts/check_spec.py`**（把在制规格文本中的该串整体替换后本行消失、改报同型「目标文本未提及且现行为 0」）；实跑 1＝期望 ≥1，工具的「目标文本」提取口径未含 F1 行正文故降为只计数呈报。④「跳过（目标件不出现在任何改动行）: 禁改面词 @ `.tmp/090-probe`」——该串取自 §五 的夹具路径句、不在改动清单各行内故按跳过明示（跳过类不计入「有预警 3 条」）；夹具与施工期探针已删净（`rm -rf .tmp/090-probe .tmp/090-run`，实测不存在）。另「对账① 汇总：改动行 6 行｜可解析 6 件｜判一适用 3 件｜判二缺 0 件｜跳过：无禁改面句 0｜无补集式断言 1」与「B：未提取到可判定的尺寸声称（1 条非整件口径未判）」皆既有口径常态。**判据见下条偏差 ①（工具代码 AST 等价 ⇒ 逻辑未动，候选非本批引入）。**
  **偏差申报（P1×2，均按最合理理解执行、未改任何断言／数字／检查）**：① **F2① 的 ⑦ 段注引文在规格内不自洽**——F2 行目标文写「统一处理**、**不在本段逐壳判」（顿号），而 §三 断言 4a 与 `测试与验收标准` §4 的 G20 登记文写「统一处理**，**不在本段逐壳判」（逗号），二者不可同时满足；按「两载体（验收断言＋G20 登记）优先于改动清单引文」取**逗号**落盘，段注其余成分（含「段序与段状态按头注取」）逐字照 F2。② **F6 行目标文以全角 `｜` 作表行分隔符**，而同句口径明写「三列**半角**竖线」且该表既有行皆半角——按半角落盘为三列（`| 编号 | 缺口 | 触发条件 |` 形态），缺口列与触发列字面（`archive/068` F-01、`archive/069` 同族第三次、**090 规格初稿**、检查 G、候选非阻断）逐字未改。
  **观察（交规划方）**：① §九 **缺成稿审查条目**（本规格 §九 原仅一行「待填：成稿审查…」）——施工记录按交底追加于此，成稿审查／窄域复审的 findings 与处置待规划方补；② `scripts/check.sh` 代码注释内仍存批号（`:14`／`:26`／`:49`／`:137`／`:143`／`:148`）与 `check_spec.py` 代码注释内 **8** 行批号，按 §八 1 属下批残余、本批未动。

- 2026-09-15｜**双维产物审查**（独立上下文只读 subagent；改规则／结构批 → **双维**）｜findings 共 **5**：**P0:0** P1:1 P2:4｜结论 **修订后通过**。审查方**独立复算**：剥去 docstring 后 AST 相等＝True（len 157486≡157486）；逐串锚核（执行面 7 族／git 5 子命令／六类枚举／三态／呈报守恒／维护入口／退出契约／用法与参数／依赖与前置／「历批实证」「同一批」／`shell=True`…）HEAD≡WORK **全 True**；夹具实跑 → 检查 F **仍报出**、删净；禁改面零 diff（porcelain 7）；残留载体扫描无漏改、无新第二载体。处置：
  - **V-01（P1，规范健康度）**：**G20／G21 两行的触发列写未来时，而本批即那次触碰**（两笔已执行完毕）→ 照同表 **G16 体例**同批改标为「本批处置（090 F1／F2①）」／「（090 F3／F4）」并把触发列改述为**残余＋下次触发**。审查方**同时指出这会长出本批不变量的声明边界**（§一 明写「除 §4 新增 G22 外零改动」）→ **规划方先修订不变量与 §二 范围（新增 F7）**，再落盘（**这正是「本批内被处置的开放项须同批改标」在 090 身上的自适用**——该纪律是本会话早先那个轻批刚落进 `施工机制` §三 的）。
  - **V-02（P2，行号漂移）**：§八 1 登记的两组点位皆**落地前**值（`check.sh` 因 F2① 并行使其后各点前移 1；`check_spec.py` 因 docstring 净增 3 行而后移）→ 已改**落地后实况**并加注。**同因另指出**：规划方核验记录写「docstring diff ＝ 8 hunk」**不可复算**（三口径皆 6）→ 已订正（目视误数）。
  - **V-03（P2）**：`安全与合规标准` §4.1 的「**非书证**」与同一行的〔出处〕措辞相抵（且 `skill形态标准` §10.2 要求来源标签取**之一**）→ 措辞改「**非外部书证**；上列〔出处〕为该条款的**沿革来源**」（规格 F3 目标文本同步）。
  - **V-04（P2）**：G22 行的编号列**加粗**而全表其余行皆不加粗 → 规格 F6 目标去粗体（实件同改）。
  - **V-05（P2）**：`.tmp/v090b.out` 为规划方核验后新落的遗留，与核验记录「已删净」不符 → **已删**、记录如实订正（审查方**未**删他人件，纪律正确）。
  - **观察一条**：`AGENTS.md:5`／`:9` 的现状句与实况不符（「`skills/` 尚未创建」而实存）——**非本批所致**、亦在禁改面；该笔在 **088 §八 1**（待作者）与 089 §七 的索引链上 ✓ 未丢。
- 2026-09-15｜**补修**（施工方）｜三处全落：F7（G20／G21 两行照 G16 体例同批改标）／V-03（§4.1 改「非外部书证；〔出处〕为沿革来源」）／V-04（G22 编号列去粗体）。断言 9 两条实测 1／1，受影响面（5a／5b／6a／6b／porcelain 7）全绿，门禁 rc=0、lint 0 issue（63 件）、链接 175／0。**两条申报**：**①（规划方裁定后就地改）** G21 行的「残余」误用 G20 的尾串（交底原给两行同一句）——G21 自身两笔已全部处置，残余应为**无**；核为 `[B]` 级措辞、在 F7 方向内，**由规划方就地改**（`测试与验收标准:84` 与规格 F7 目标文本同步），并如实记于本条。**②（明示未落）** 规格 F3 引号块内的「——双维审查 V-03 订正措辞…」属**规格侧注**，施工方按交底止于「阈值取值本身判自定）」落盘 ✓ 正确（规格侧注不入标准件条文）。
- 2026-09-15｜**规划方复核（修复面）｜通过**：三处逐处读原文核对＝到位（G20／G21 两行只动第三列、编号列与缺口列逐字未动；§4.1 限速值与规范句字面未动；G22 编号列已去粗体）；断言 9 **独立重跑** 1／1；门禁 rc=0、lint 0 issue、链接 175／0、porcelain **7**。**另修**：G21 残余归属（见上）；`.tmp/x.o`（规划方遗留）已删净。
- 2026-09-15｜**收口**｜本规格**验收通过**——成稿审查 14 ＋ 双维产物审查 5，共 **19 条全处置**（P0 一条在规格侧、产物零缺陷）；本批不触用户可观测行为，按 `施工机制` §二 **免真机走查** ✓。规格移入 `docs/specs/archive/`，呈作者确认提交。

- 2026-09-15｜**补修轮**（施工方，subagent，后台 spawn）｜**三处全落**（同两件文件，零超面改动）：① **F7（V-01，P1）** `docs/standards/测试与验收标准.md` §4 的 **G20／G21 两行**——**只改第三列（触发列）**，编号列与缺口列**逐字未动**：G20 行触发列＝「**本批处置（090 F1／F2①）**：**残余**＝`check_spec.py` 代码注释批号、`check.sh` 其余批号（见 090 §八 1）；下次触发＝残余收敛时」，G21 行同式作「**本批处置（090 F3／F4）**：**残余**＝…」（照同表 G16「**本批处置（…）**：…；」体例）；② **V-03（P2）** `docs/standards/安全与合规标准.md` §4.1——「（非书证、非实测）」→「（**非外部书证**、非实测；上列〔出处〕为该条款的**沿革来源**，阈值取值本身判自定）」（**限速值与规范句字面未动**；旧式整串 `（非书证、非实测）` 实测 **0**）；③ **V-04（P2）** 同表 **G22 行编号列** `| **G22** |` → `| G22 |`（去粗体、同表风格；加粗残留实测 **0**）。
  **断言实跑（批式，脚本一次跑完；命令→实测→期望 逐行见核验请求）**：**断言 9a** 计出现次数 **1** ＝ 期望 1；**9b** 计出现次数 **1** ＝ 期望 1。**受影响面**：5a **1**／5b **1**／6a **1**／6b **1**（F3／F4 两笔的断言全数保持）；7b 链接一行式 **175‧0**；7c porcelain **7**（6 件 M ＋ 1 行未跟踪，逐行同前轮）。**门禁**：`bash scripts/check.sh` rc **0**（尾「== 检查全绿 ==」）、`npx markdownlint-cli2 "**/*.md"` **0 issue**（63 文件）。**`[4]` 段候选**仍为 **3 预警 ＋ 1 跳过**，与上轮**同源同判据**、逐条判**属预期**（自噬预警 `治理收口（090）` @ `CHANGELOG.md`／`^def unauditable(` 现行为 0／「现行有值／目标文本未提及 1 个」＝`维护入口` @ `check_spec.py`／跳过项 `禁改面词 @ .tmp/090-probe`）；唯一变动＝汇总行「改动行 **7** 行｜可解析 **6** 件」（本批改动清单增 F7 行、而 F7 与 F6 同件）——属预期。
  **偏差申报（P1×1，未改任何断言／数字／检查、未构造绕过）**：**G21 行触发列尾串的归属**——F7 与交底给出的是**同一句**尾串（「**残余**＝`check_spec.py` 代码注释批号、`check.sh` 其余批号（见 090 §八 1）；下次触发＝残余收敛时」），而 §八 1 标题为「**G20 的残余**」、G21 侧无残余登记；已按两载体**逐字落盘、未发明 G21 专属尾串**，即按**批级残余指针**读（两行本批处置完毕，下次审视随残余收敛）。若作者裁定 G21 行尾串应另作「残余＝无」，改一处即可。
  **另（规格侧注未落盘，明示）**：F3 行引号块内含「——双维审查 V-03 订正措辞以免与〔出处〕相抵」一句，而交底给出的替换串止于「阈值取值本身判自定）」（与 F6 行的「落盘形态注」同属**规格侧注**）→ 按**交底**落盘，未把审查批注写进标准件条文。
  **临时件**：`.tmp/090-fix/`（批式脚本）用后删净，实测不存在。
