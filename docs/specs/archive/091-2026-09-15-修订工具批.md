# 091-2026-09-15-修订工具批

> 定位：把「**修订类脚本三段式**」（反模式 #12：全量校验 → 统一写盘 → 回读核验）从**每次手写的纪律**固化为**仓内工具** `scripts/patch_file.py`——根因不是「记性差」，而是**编辑动作的形态依赖记忆**（匹配键靠重建、而文件可能已被 lint／自己／subagent 改过），故把「锚点由磁盘校验、命中 0 或 >1 即中止并**打印实际命中行**」做成机制；并同批立一条纪律（仓内文本修订一律经该工具）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径**——**新增文件**（判定表 ✅ 第 1 行）＋ 跨件 → 需规格；改流程纪律 → 走 **双维产物审查**；不触 `skills/` → **免真机走查**。
> 自由度分布：[A]×2 [B]×2
> 必读输入：本规格 §一；`AGENTS.md`（§一 红线 2 临时产物／§二 路由／§五 分工原则一）；`docs/specs/施工机制.md`（§三 `:80` 修复后自核验四条／§六 反模式 #12 `:143`／§四 机器闸 `:78`–`:83`）；`docs/standards/命名标准.md`（§2 表 `:57`／`:59`–`:61` ＋ 表注 `:73`）；`docs/standards/skill形态标准.md` §13 矩阵的「开发守卫」行（脚本文档头四要素）；`scripts/check_spec.py`（头注的**文档头四要素范式**）
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 命名表无「仓级开发工具」格 | `命名标准` §2 现有相关行：`:57` 运行时脚本 `{动词}-{对象}.{sh,py,js}`／`:59` 一键入口 `check.sh`（固定名）／`:60` 开发守卫 `check-{对象}.{sh,py,js}`／`:61` 开发测试 `test-{对象}.{sh,py,js}`；**表注 `:73`（扩展名口径）：「`.py` 文件用 snake_case，`.sh`／`.js` 用 kebab-case；命名总表运行时脚本、开发守卫、开发测试三行的 `.py` 后缀以本表注为准」 ✓ |
| ② | 修订纪律的落点现文 | `施工机制:80`「修复后自核验四条（谁修谁核）：① 修改已落实到位（**逐条 `grep`／read 核验目标文本确在文件中，不得凭脚本输出判「已改」**）② 无遗漏点 ③ 无衍生新问题 ④ **断言对账**…」——现为**事后核验**纪律，**无「怎么改」的形态约束** ✓ |
| ③ | 反模式 #12 现文 | `施工机制:143`「以为改了、实际没改——修订脚本失败未检查，且未回读核验文件实际状态 ｜ 修订类脚本**三段式**（全量校验 → 统一写盘 → 回读核验），改后逐条 `grep`／read 核验目标文本确在文件中 ｜ 2026-09-14 076 窄域复核…」——**要求已立，实现靠每次手写** ✓ |
| ④ | 本会话该族的实际代价 | 三次同型中止：① **可核面**——089 内该汇总串命中 **2 行**（`archive/089-…md:335` 汇总行 ＋ `:358` §九记录引旧值，`grep -c` 实测 2），致计数断言写盘前中止、**零误改**；② **本会话规划方操作记录**——090 期两次 `count=0`（凭记忆写的匹配键与盘上原文不符），**无仓内落点、不可复跑**（成稿审查 F-16 要求如实标注）。每次代价＝一轮白跑，且中止信息只有数字、**须再猜一轮**才能取到真值 ✓ |
| ⑤ | 仓内现有开发件与脚本语法守卫 | `scripts/` ＝`check.sh`／`check_spec.py`／`hooks/`；`check.sh:11` 头注「`# TODO: [5] 脚本语法（node --check 逐域脚本）——待落 contracts 脚本后加入`」＝**`[5]` 段待建**，故新增 `.py` **无语法守卫**（本批须登记，不接线） ✓ |
| ⑥ | 090 §八 1 残余的口径核对（本批核销面） | 090 §八 1 记「`check_spec.py` 代码注释 **8 行**含批号（`:87`／…）＋`check.sh` 其余批号 **6** 处」。**实测**：按单串 `075 批 F1` 计＝8 行 ✓；按宽口径 `[0-9]{3} 批` 计＝**37 行**（`check_spec.py`）／**6 行**（`check.sh`）。**性质判定**：其括注是**跨批指针**（指向已归档的 075／076／077／078／089 条目号，**不随时间失效**），与 090 收敛的「本批（065）」类**自指时态**不同 → **判为非缺陷、保留**（核销见 §七） ✓ |
| ⑦ | 基线 | `bash scripts/check.sh` exit **0**；lint **0 issue**（**64 件**——含本规格；原写 63 系抄 090 批旧值，成稿审查 F-09 订正）；链接一行式 **175／断链 0**；porcelain **1**（仅本规格未跟踪；成稿审查 F-02 订正） ✓ |

**不变量（施工前后须同）**：本批改动**全部落点**：`scripts/patch_file.py`（新建）／`docs/specs/施工机制.md`（§三 `:80` 一条纪律 ＋ **§六 反模式清单新增 #20**）／`docs/standards/命名标准.md`（§2 新增一行 ＋ **§1 目录树** ＋ 表注同步）／`docs/standards/测试与验收标准.md`（§4 的 G20 行触发列同批改标）／`CHANGELOG.md`；**除此之外零改动**（`AGENTS.md`／`README.md`／`skills/`／`docs/product/`／`docs/specs/archive/`／`scripts/check.sh`／`scripts/check_spec.py`／`scripts/hooks/` 零改动）。链接一行式 **175／断链 0** 不因本批变化（新件不含 markdown 链接）。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `scripts/patch_file.py`（新建） | **修订工具**（零外部依赖／不联网／不跑 LLM／不写目标件以外的文件）。**文档头四要素**（用途／用法与参数／依赖与前置／维护入口，范式照 `check_spec.py` 头注）。**三种用法**：① `--at <锚片段> --set <新文本>`（单行替换）② `--at <锚片段> --append <文本>`（**锚行之后**追加一行）③ `--at <锚片段> --insert-before <文本>`（**锚行之前**前插一行——F2① 要的正是前插）④ `--batch <edits.json>`（多处批量，JSON 形状：`[{"path":…,"at":…,"set"/"append"/"insert-before":…}, …]`）。**语义钉死（成稿审查 F-08）**：`--at` 是**行内片段**（命中即替换该片段，非整行）、`--set` 的新文本**可含换行**；**目标文件以位置参数传入**（末位，成稿审查 F-12）。**另三项语义（F-15）**：`--batch` 同件多编辑按**逐条应用后文本**判唯一性；回读判据＝新文本在位**且**（新文本 ≠ 锚串时）锚串不再于该行出现；`--at` 为空串即**参数错**（退出 2）。**三段式（硬要求）**：**全量校验**——每条编辑先在内存解析，锚片段须**唯一命中**（**命中 0 或 >1 即中止**，并**打印实际命中行及其行号**；命中 0 时另打印**最接近的一行**供取真值）；**统一写盘**——全过才写（写临时件后整体替换，或一次性写回）；**回读核验**——写后重读，逐条确认目标文本在位，不符即报。**输出**：逐处「`文件:行` 改前 → 改后」＋汇总（`N 处／M 件`）。**退出码**：0 全成功／2 参数错或校验失败（照 `check_spec.py` 约定）。`--help` 可用。**编码与行尾（硬要求，成稿审查 F-06）**：读写一律 **`newline=''`**（不作换行翻译）、**保持目标件原有行尾**（新插入行沿用该件行尾）、**UTF-8 无 BOM**——仓内 `.md` 目标件通体 CRLF 而 `.py`／`.sh` 为 LF，朴素文本模式读写会静默把 CRLF 夹具改成 CRLF 或把目标件整体改行尾（后者经 `.gitattributes` 归一后在 `git diff` 里**不可见**）。 | [B] | 反模式 #12；§一 ③④（本会话三次同型中止） |
| F2 | `docs/specs/施工机制.md`（§三 `:80` ＋ §六 反模式清单） | ① §三 在「修复后自核验四条」**之前**新增一条纪律（逐字）：「**修订工具律**：仓内文本修订一律经 `scripts/patch_file.py`（或其等价的三段式实现）——**禁手写长串匹配的临时脚本**；该工具把反模式 #12 的三段式固化为机制（锚点由**磁盘**校验、命中 0 或 >1 即中止并打印实际命中行、统一写盘、回读核验）。触发条件：**以脚本／命令行方式做批量或长串匹配的仓内文本改动**时本条适用；**宿主自带的一次一处、要求唯一匹配的编辑工具（Edit）不受本条约束**，但改前须 `grep` 取原文（#18）。**〔规格侧注，不入落盘文本〕**：施工方呈报本条与断言 5b 互斥——原触发条件句重述了禁则、致该串在目标件内出现 2 次；裁定**取去重**，使其在 `施工机制.md` 内**恰 1 处**，条文亦更干净。」② §六 反模式清单新增一行（逐字）：「**20**｜以「手写临时脚本 ＋ 凭记忆写的匹配键」改仓内文本（中止信息只有计数、须再猜一轮）｜改用 `scripts/patch_file.py`：锚点由磁盘校验、命中 0 或 >1 即中止并打印实际命中行｜2026-09-15 089–090 批：同型中止三次（089 内该串命中 2 行；090 两次 `count=0`）」**（落盘形态注**：表行四列以**半角**竖线分列（反模式 #14 禁表内裸竖线），其余字面照落——成稿审查 F-18，同 090 F6 先例）**（**关系注**：本行**取代 #18 的手写脚本路径**；#18 保留为事故记录——成稿审查 F-20 防两行并置产生相反指引）** | [A] | 作者 2026-09-15 会话内确认「开」；与 #12 呼应（纪律 ＋ 机制双轨） |
| F3 | `docs/standards/命名标准.md`（§2 表 ＋ 表注 `:73`） | §2 在「开发测试」行后新增一行（逐字）：「\| 仓级开发工具 \| `{动词}-{对象}.{sh,py,js}`（`.py` 按表注用 snake_case） \| `patch_file.py` \| `scripts/`（仓库级） \|」；**§1 目录树同步**（`:39`–`:42`）：`scripts/` 下**在 `check_spec.py` 之后、`hooks/pre-commit` 之前**新增一行 `├── patch_file.py`（`└──` 字形不动），`:39` 的注释「开发守卫」→「开发守卫与工具」（**不带粗体**——本无粗体，双维审查 V-01 指出初稿多写 `**` 致逐字漂移；同 090 V-01 裁定「粗体非字面」）；**表注 `:73` 逐字改**：「命名总表运行时脚本、开发守卫、开发测试、仓级开发工具四行的 `.py` 后缀以本表注为准」（**点名四行**，因而「仓级开发工具」在该件内由 1 处变 **2 处**——断言 6a 期望按此定 2） | [A] | §一 ①（现表无通用工具格）；表注既有口径 |
| F5 | `docs/standards/测试与验收标准.md`（§4 的 **G20 行**触发列） | （现状：「**本批处置（090 F1／F2①）**：**残余**＝`check_spec.py` 代码注释批号…；下次触发＝**残余收敛时**」）→ 本批将其**残余判为不存在**（§一 ⑥／§七），故触发列改述（逐字）：「**本批处置（090 F1／F2①；091 复核判残余不存在）**：下次触发＝**同类自指时态再现时**」（**同批改标**，照 `施工机制` §三「本批内被处置的开放项须同批改标」——090 F7 即同法） | [B] | 成稿审查 **F-04**（P1）：原 §七 写「见 §五」属悬空（该件不在禁改面） |
| F4 | `CHANGELOG.md`（`### Added` 段首） | 一行：『修订工具批（091）：…』——工具落地 ＋ 纪律入真源 ＋ 命名补行；≤400 字符、末尾「核验：」句 | [B] | 过程产物 |

**口径要点（易错）**：① **F1 的零依赖**（只用标准库；不联网、不跑 LLM）；② **F2／F3 逐字**；③ 本批**不碰** `check_spec.py`／`check.sh`（§一 ⑥ 已判其批号括注为**跨批指针、非缺陷**，核销见 §七）；④ `[5]` 段（脚本语法）待建 → 新 `.py` 无语法守卫，**登记 §八**，不接线。

## 三、验收断言（一条命令一期望；计数口径逐条标注）

### 断言 1｜工具可用与零依赖

```bash
python scripts/patch_file.py --help > /dev/null 2>&1; echo $?
```

期望 **0**（`--help` 可用）。**预验基线**：文件不存在（命令输出非 0）。

```bash
python -c "import ast,io;m={n.split('.')[0] for x in ast.walk(ast.parse(io.open('scripts/patch_file.py',encoding='utf-8').read())) if isinstance(x,(ast.Import,ast.ImportFrom)) for n in ([a.name for a in x.names] if isinstance(x,ast.Import) else [x.module or ''])};print(sorted(m - {'argparse','json','os','sys','io','re','ast'}))"
```

期望 **`[]`**（**ast 遍历**取顶层模块名并减去白名单 `argparse／json／os／sys／io／re／ast`，余集须为空——**成稿审查 F-10**：原 `grep -cE "^import …"` 只覆盖顶格单模块导入，`import os, subprocess` 与缩进导入均漏）。**预验基线**：文件不存在（命令**报错**、无 stdout）。

```bash
python -c "import ast,io;ast.parse(io.open('scripts/patch_file.py',encoding='utf-8').read());print('ok')"
```

期望 **ok**（**语法自检**）。**预验基线**：文件不存在。

### 断言 2｜三段式：唯一命中即改、0 命中中止且**零写盘**

```bash
mkdir -p .tmp/091-probe && printf 'alpha\nbeta\ngamma\n' > .tmp/091-probe/t.txt && python scripts/patch_file.py --at "beta" --set "BETA" .tmp/091-probe/t.txt > /dev/null 2>&1; rc=$?; a=$(cat .tmp/091-probe/t.txt | tr '\n' '/'); printf '\n' > .tmp/091-probe/u.txt; printf 'alpha\ngamma\n' > .tmp/091-probe/u.txt && python scripts/patch_file.py --at "not-there" --set "X" .tmp/091-probe/u.txt > /dev/null 2>&1; rc2=$?; b=$(cat .tmp/091-probe/u.txt | tr '\n' '/'); echo "rc=$rc rc2=$rc2 a=$a b=$b"; rm -rf .tmp/091-probe
```

期望 `rc=0 rc2=2 a=alpha/BETA/gamma/ b=alpha/gamma/`（**唯一命中→改**；**0 命中→退出 2 且文件**逐字节未变**）。**加硬（成稿审查 F-14）**：另记 `stat -c '%Y %s'` 前后比对，把「未写盘」与「写回同一内容」分开——内容串相同不足以证未写盘。**预验基线**：文件不存在（无法构成）。

### 断言 3｜三段式：>1 命中中止，且**打印实际命中行**

```bash
mkdir -p .tmp/091-probe && printf 'dup\ndup\n' > .tmp/091-probe/d.txt && out=$(python scripts/patch_file.py --at "dup" --set "X" .tmp/091-probe/d.txt 2>&1); rc=$?; c=$(cat .tmp/091-probe/d.txt | tr '\n' '/'); n=$(printf '%s' "$out" | grep -c "dup"); echo "rc=$rc n=$n c=$c"; rm -rf .tmp/091-probe
```

期望 `rc=2 n=≥2 c=dup/dup/`（**判据已加硬**：`n≥2` 要求输出含**两行**命中行原文——只回显锚串的实现 `n=1` 会被判红。成稿审查 F-05 用假实现实证：原判据 `≥1` 对「只打印『锚片段 dup 命中 2 处，中止』」的实现同样成立、**测不出本工具的核心改进**）。**预验基线**：文件不存在。

### 断言 3b｜0 命中时输出「最接近的一行」（F-13）

```bash
mkdir -p .tmp/091-probe && printf 'alpha
gamma
' > .tmp/091-probe/n.txt && out=$(python scripts/patch_file.py --at "gammb" --set "X" .tmp/091-probe/n.txt 2>&1); rc=$?; n=$(printf '%s' "$out" | grep -c "gamma"); echo "rc=$rc near=$n"; rm -rf .tmp/091-probe
```

期望 `rc=2 near=≥1`（**0 命中时须打印最接近的一行**——中止即给真值；成稿审查 F-13 指出原断言 2 把该侧 stdout 丢进 `/dev/null`、此项无覆盖）。**预验基线**：文件不存在。

### 断言 3c｜`--append` 与前插（F-13／F-08）

```bash
mkdir -p .tmp/091-probe && printf 'head
tail
' > .tmp/091-probe/p.txt && python scripts/patch_file.py --at "head" --append "MID" .tmp/091-probe/p.txt > /dev/null 2>&1; a=$(tr '
' '/' < .tmp/091-probe/p.txt); python scripts/patch_file.py --at "tail" --insert-before "PRE" .tmp/091-probe/p.txt > /dev/null 2>&1; b=$(tr '
' '/' < .tmp/091-probe/p.txt); echo "a=$a b=$b"; rm -rf .tmp/091-probe
```

期望 `a=head/MID/tail/ b=head/MID/PRE/tail/`（**after 追加**与**before 前插**各一击——F2① 依赖前者、F3 依赖后者）。**预验基线**：文件不存在。

### 断言 4｜批量模式与输出形态

```bash
mkdir -p .tmp/091-probe && printf 'one\ntwo\n' > .tmp/091-probe/b.txt && printf '[{"path":".tmp/091-probe/b.txt","at":"one","set":"ONE"}]' > .tmp/091-probe/e.json && out=$(python scripts/patch_file.py --batch .tmp/091-probe/e.json 2>&1); rc=$?; d=$(cat .tmp/091-probe/b.txt | tr '\n' '/'); printf '%s' "$out" | grep -c "→" | sed 's/^/arrows=/'; echo "rc=$rc d=$d"; rm -rf .tmp/091-probe
```

期望 `arrows=≥1 rc=0 d=ONE/two/`（**批量可用** ＋ 输出含「改前 → 改后」形态）。**预验基线**：文件不存在。

### 断言 5｜F2 纪律与反模式 #20

```bash
grep -o "修订工具律" docs/specs/施工机制.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "禁手写长串匹配的临时脚本" docs/specs/施工机制.md | wc -l
```

期望 **1**（**计出现次数**——**该件内恰一处**：§三 纪律句的禁则；触发条件句**不重述**，见 F2① 的去重说明）。**预验基线 0**。

```bash
grep -o "改用 \`scripts/patch_file.py\`：锚点由磁盘校验" docs/specs/施工机制.md | wc -l
```

期望 **1**（反模式 #20 的「正确方式」格；**计出现次数**）。**预验基线 0**。

### 断言 6｜F3 命名行与表注

```bash
grep -o "仓级开发工具" docs/standards/命名标准.md | wc -l
```

期望 **2**（**计出现次数**——**1＝§2 表行 ＋ 1＝表注行**；施工方复核指出：类目名「仓级开发工具」**不由目录树行贡献**（该行写的是 `patch_file.py`）；成稿审查 F-01 订正后归因又偏，今按实况订正）。**预验基线 0**。

```bash
grep -o "patch_file.py" docs/standards/命名标准.md | wc -l
```

期望 **2**（**计出现次数**——**§2 表行 1 ＋ §1 目录树行 1**；**表注不计**：F3 的逐字表注只点名四个**类目**、不含 `patch_file.py`——施工方动工前停摆呈报的本条即此：原写「＋ 表注行 1」致期望 3 与 F3 逐字互斥，**判据以目标文本为准**，裁定取「期望改 2、归因改两处」，不往表注里塞字）。**预验基线 0**。

```bash
grep -o "运行时脚本、开发守卫、开发测试三行" docs/standards/命名标准.md | wc -l
```

期望 **0**（表注适用面已改为四行，旧措辞归零）。**预验基线 1**。

### 断言 7｜门禁、链接与改动面

```bash
bash scripts/check.sh
```

期望 exit **0**。**预验基线 0**。

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**。**预验基线 0 issue**。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **175 0**。**预验基线 175‧0**。

```bash
git status --porcelain | wc -l
```

期望 **6**（**逐类列举**：**4 件 tracked M**＝`docs/specs/施工机制.md`／`docs/standards/命名标准.md`／`docs/standards/测试与验收标准.md`／`CHANGELOG.md` ＋ **1 行未跟踪**＝`?? scripts/patch_file.py` ＋ **1 行未跟踪**＝`?? docs/specs/091-…md`）。**预验基线 1**（仅本规格）；施工后逐行列核验记录（不符以实况为准并订正）。

### 断言 8｜过程产物

```bash
grep -cF '修订工具批（091）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 七条实跑取证（①命名表四行与表注 `:73` 原文；②`:80` 现文；③`:143` 现文；④三次中止的实例（取自本会话 089 §九／090 §九 记录）；⑤`scripts/` 实况与 `check.sh:11` 的 `TODO: [5]`；⑥**宽口径实测 37／6 行**（对 090 §八 1 的「8 行」单串口径作订正与性质判定）；⑦门禁三命令与 porcelain 0）。
2. **断言实跑过**（改前侧）：断言 1／5／6／7 的 0 侧全部实跑（工具不存在／四个目标串 0／表注旧措辞 1／门禁 0／链接 175‧0／porcelain 1）；**断言 2／3／4 属**施工后侧**（依赖工具存在），其**夹具骨架**已在本规格内写死（`.tmp/091-probe/` 下三行文本，跑完删净）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**（`施工机制` §二 自审第 4 条）：断言 7 的 porcelain **6** 由**逐类列举**得出（**4 M**：`施工机制`／`命名标准`／`测试与验收标准`／`CHANGELOG` ＋ **2 未跟踪**：`scripts/patch_file.py`／本规格；原写 5 系施工方 P2 观察指出后订正）；断言 2／3／4 的期望由**夹具内容**直接推得（唯一命中→改；0 命中→退出 2；>1 命中→退出 2 且输出含命中行）；断言 6 的「期望 0」由**表注旧措辞现仅 1 处**（实测）得出。

## 五、禁止事项

- 禁止触碰：`.git/`、`AGENTS.md`、`README.md`、`skills/`、`docs/product/`、`docs/specs/archive/`、**`scripts/check.sh`**、**`scripts/check_spec.py`**、`scripts/hooks/`。
- **F1 不得引入任何非标准库依赖**、不得联网、不得跑 LLM；**不得读写改动清单所列件之外的仓内既有件**（若写盘用临时件，须落**同目录**、写后即整体替换、并保证异常路径删除——成稿审查 F-11 订正原「不得读写目标件以外的文件」与 F1 的临时件策略相抵）。
- **不得**为「让断言过」而弱化三段式（例如把「命中 0 或 >1 即中止」改成「取首个命中」）。
- **不得**手写临时替换脚本改本批四件（本批正是立这条律的批——**自适用**：四处改动一律经新工具或等价的带计数断言实现）。
- 禁止改断言、改数字、删检查来变绿；`.tmp/091-probe/` 夹具用后删净（红线 2）。
- 禁止动作：任何 git 写命令；安装依赖。

## 六、被否决的替代方案

- **不新立工具，只把反模式 #12 的写法要求写详细** → 否决：本会话三次同型中止已证「纪律靠自觉」不够（反模式 #9：写完纪律就以为会自动执行）；机制化才有确定性。
- **把 090 §八 1 的 43 个批号点位一并改写** → 否决（§一 ⑥ 判因）：那些括注是**跨批指针**（指向已归档条目，不随时间失效），与 090 收敛的**自指时态**（「本批（065）」）不同类；改写会丢追溯性，且把本批扩成「大扫除」。
- **把工具做成 `check-` 前缀的守卫** → 否决：它不是检查器（不报红、不进 `check.sh` 段），是**写入工具**；按 `命名标准` §2 的 `{动词}-{对象}` 形态另立「仓级开发工具」类目，且 `.py` 依表注用 snake_case（`patch_file.py`）。

## 七、本批核销（090 §八 全部开放项）

> **必列面**：090 §八 **全 4 条**（前一批的全部开放项）＋本批触及者。**不必重列**：088 §八（089 §七 已承载——索引指针＝`089` §七；其 16 条走查欠账见 §八 3）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 090 §八 1 | G20 的残余（`check_spec.py` 代码注释批号 ＋ `check.sh` 其余批号） | **本批处置（判为非缺陷、保留）** | 本批实核并**订正口径**：单串 `075 批 F1`＝8 行／**宽口径 `[3 位] 批`＝37 行**（`check_spec.py`）与 6 行（`check.sh`）；性质＝**跨批指针**（指向已归档条目号、不随时间失效），非 090 所类的自指时态 → **保留**，不修改（§一 ⑥）。据此 `测试与验收标准` §4 的 G20 行「下次触发＝残余收敛时」**不再需要**——残余不存在；**由下批触碰该件时把 G20 行的下次触发改述为「同类自指时态再现时」**（**已由本批 F5 履行**——初稿「本批不碰该件，见 §五」系悬空指针，双维审查 V-05 订正） |
| 090 §八 2 | `命名标准` §5／`skill形态标准` §9.1 的补款（运行时资产引用治理文档的格式口径） | **仍待** | 触发＝下批触碰该两件时补款（可挟带进大纲阶段二批） |
| 090 §八 3 | 真机走查欠账（`088` §八 16 转记） | **仍待** | 触发＝089 之后或下一个域建成前必须补跑（作者 2026-09-15：先不着急） |
| 090 §八 4 | 大纲阶段二（资料收集／采风）待开工 | **仍待** | 本批清完工具账后即为下一批候选 |

- 汇总（恒列五档、无该档计 0）：`已履行 0｜作废 0｜本批处置 1｜仍待 3｜待作者 0 ＝ 4` ✓

## 八、开放项

1. **`[5]` 段（脚本语法）待建**：`check.sh:11` 头注仍为 `TODO`，本批新增的 `.py` 无语法守卫（本批以断言 1 的 `ast.parse` 兜一次，但那是**当批人工**、非常驻闸）。触发＝`[5]` 段落建时把 `scripts/*.py` 纳入 `py_compile`／`ast.parse` 逐件。
2. **工具的假阴性面**：锚片段若在文中出现多次但语义唯一（如两处同名标题），工具按「>1 即中止」拒绝——需人工改用更长锚或 `--batch` 逐处锚。触发＝实际撞见时评估「按行号 ＋ 上下文双键」的补强。
3. ~~`测试与验收标准` §4 的 G20 行改述~~ —— **本批已履行**（F5；双维审查 V-05：原列「触发＝下批」与本批实况相抵）。
4. **`skill形态标准` §13 矩阵缺「仓级开发工具」类目行**（成稿审查 F-19）：该表自称「管分类全覆盖」，新类目无行 → 触发＝矩阵下次修订时补行（机检列写「`[5]` 段接入 `ast.parse` 前＝人工断言 1」）。
5. **命名标准 §2 的「仓级开发工具」类目**：本批先立 `patch_file.py` 一实例；若后续再增同类工具（如批量 lint 修复器），需评估是否并为「开发工具族」并补族级判据。

## 九、审查记录

- 2026-09-15｜**施工**（subagent，后台 spawn）｜**五件全落**（1 新建 ＋ 4 改）：`scripts/patch_file.py`（**385 行**／14173 字符（补修后实测；原记 367／13277 系补修前值——施工方 ⑤ 指出），LF 无 BOM；四用法 ＋ 三段式 ＋ 编码行尾三项 ＋ 退出码 0／2）／`施工机制` §三「修订工具律」＋ §六 反模式 **#20**（半角竖线四列 ＋ 关系注）／`命名标准` §1 目录树 ＋ §2 表 ＋ 表注（三处）／`测试与验收标准` §4 G20 行触发列改述／`CHANGELOG` 359 字符。**自跑 10 项 F1 硬要求探针**（CRLF 件插行保持 CRLF／无 BOM／`--set` 含换行与空行／`--append` 值以「- 」起首／参数错三项／批量唯一性与**批内不唯一零写盘**）。**两条 P0 呈报**（**均为规格之错、均为施工方动工前或落盘中主动发现**，详见下）。
- 2026-09-15｜**两条 P0 呈报的裁定（同族：期望值未从目标文本重推）**｜**① 断言 6b**（`patch_file.py` 期望原写 3，含「表注行 1」）与 F3 的逐字表注互斥（表注只点名四个**类目**、不含该文件名）→ 裁定**期望改 2**、归因「§2 表行 ＋ §1 目录树行」（**成稿审查 F-01 的订正本身又写偏，本次按实况再订**）。**② 断言 5b**（`禁手写长串匹配的临时脚本` 期望 1）与 F2① 的首版逐字互斥（该串在逐字块内出现 **2 次**——触发条件句重述了禁则）→ 裁定**取去重**：触发条件句改「…时本条适用」、删重述，条文更干净且使该串**恰 1 处**。**施工方两次都未改断言、未删字凑数、未构造绕过**（照 `施工机制` §六 停摆条与红线 4）——**本会话同族第三次，而三次全由施工方先发现**。
- 2026-09-15｜**规划方核验（三步）｜通过**。① **改动面**：porcelain **6**（4 M ＋ 2 未跟踪），逐 hunk 在 §一 不变量内；**八个禁改面零 diff**（`AGENTS.md`／`README.md`／`skills/`／`docs/product/`／`docs/specs/archive/`／`scripts/check.sh`／`scripts/check_spec.py`／`scripts/hooks/`）。② **断言独立重跑（批式）**：5a＝1／5b＝1／5c＝1／6a＝2／6b＝2／6c＝0／CHANGELOG 1 行 359 字符；门禁 rc=0、lint 0 issue（64 件）、链接 175／0。③ **内容抽查（规划方亲手跑工具，不看自报）**：唯一命中→`b'aa BETA cc\r\nsecond\r\n'`（**CRLF 原样保留**）／0 命中→rc=2 且 `stat` 前后同（**真零写盘**）＋打印最接近行／>1 命中→rc=2、输出含两行命中原文、文件未变／`--append` ＋ `--insert-before` → `head/MID/PRE/tail/`／空锚→rc=2——**与 F1 的每条硬要求逐条吻合**。

- **施工方四条观察的处置**：**③** F2① 引号块尾的规格侧注**未落盘**——**正确**（侧注不入标准件条文，同 090 F3 先例）；规格侧已把该注移出引号块（标「〔规格侧注，不入落盘文本〕」），使 `检查 C` 不再常驻该候选。**④** #20 关系注落「正确方式格」末——**采纳**（形式与位置合理）。**⑤** 工具两处回读放宽（`--set` 新文本自带锚串时免「锚串不再出现」子项；`--batch` 同槽位重写按终态判）——**采纳**（均为语义自洽的必要放宽，已在工具 docstring 明载、非静默，且不涉本批 9 条编辑）。**⑥** §四 自审 4 的 porcelain 陈旧值——**已订正为 6**。

- 待填：**双维产物审查**（独立上下文）→ 归档 → 呈作者确认提交。（独立上下文只读 subagent）→ 作者确认 → 施工（subagent，**后台 spawn**）→ 核验三步 → 双维产物审查 → 归档 → 呈作者确认提交。

- 2026-09-15｜**施工**（subagent，后台 spawn）｜完成：**F1–F5 全落**，五件落点与 §二 逐条对齐——① `scripts/patch_file.py`（**新建**，仓级开发工具）：文档头四要素齐（用途／用法与参数／依赖与前置／维护入口，范式照 `check_spec.py`）＋四种用法（`--set` 行内片段替换／`--append` 锚行后追加／`--insert-before` 锚行前插／`--batch` 批量 JSON），**目标件以末位位置参数传入**；语义钉死：`--at` 为行内片段、`--set` 新文本可含换行、`--at` 空串＝参数错（退出 2）；**三段式**（全量校验 → 统一写盘 → 回读核验）为硬要求——命中 0 或 >1 即中止并打印**实际命中行及其行号**（0 命中另给**最接近的一行**）、校验不过**零写盘**（写盘经同目录临时件 ＋ 整体替换、异常路径删临时件）；编码行尾三项（读写 `newline=''`／保持目标件原有行尾、新插入行沿用该件行尾／UTF-8 无 BOM，目标件带 BOM 即拒绝改动）；退出码 0／2；输出「`文件:行` 改前 → 改后」＋汇总（`N 处／M 件`）；`--help` 可用；零外部依赖（仅 argparse／json／os／re／sys）、不联网、不跑 LLM、不写目标件以外的文件。② `docs/specs/施工机制.md`：§三 `:80` 在「修复后自核验四条」**之前**新增「**修订工具律**」段（逐字）＋§六 反模式清单 `:153` 新增 **#20** 行（半角竖线四列 ＋ 关系注）。③ `docs/standards/命名标准.md`：§1 目录树 `:42` 新增 `├── patch_file.py`（在 `check_spec.py` 之后、`hooks/pre-commit` 之前，`└──` 字形未动、注释列位同为列 32）＋`:39` 注释「开发守卫」→「开发守卫与工具」＋§2 表 `:63` 新增「仓级开发工具」行（在「开发测试」行后）＋表注 `:75` 逐字点名四行。④ `docs/standards/测试与验收标准.md` §4 G20 行触发列改述（**只动第三列**，编号列与缺口列逐字未动）。⑤ `CHANGELOG.md` `### Added` 段首一行（实测 **359** 字符 ≤400、末句「核验：」）。**本批四处文本改动一律经新工具落盘**（一次 `--batch` 8 条 ＋ 裁定后一次 `--set` 1 条；`edits.json` 的锚串与逐字文本由脚本从规格／目标件**程序化提取**，不手打长串；输出逐处「文件:行 改前 → 改后」＋汇总「8 处／4 件」）。
  **实跑（批式：两脚本各一次跑完；命令→实测→期望 逐行见核验请求）**：断言 **1** `--help` rc **0**／白名单外模块 **[]**／`ast.parse` **ok**；**2** rc **0** ＋ `alpha/BETA/gamma/`；0 命中 rc **2** ＋ 内容逐字未变 ＋ `stat -c '%Y %s'` 前后**同**（真未写盘）；**3** rc **2** ＋ 命中行回显 **3** 行（判据 ≥2）＋ 文件未变 ＋ stat 同；**3b** rc **2** ＋ 最接近的一行 `gamma` **1**；**3c** `head/MID/tail/` → `head/MID/PRE/tail/`；**4** rc **0** ＋ `ONE/two/` ＋ 箭头行 ≥1；**5a 1**／**5b 1**（另 **5d** #20 行头 **1**）／**5c 1**；**6a 2**／**6b 2**（按裁定①的新期望）／**6c 0**；**7a** `bash scripts/check.sh` rc **0**（尾「== 检查全绿 ==」）／**7b** `npx markdownlint-cli2 "**/*.md"` **0 issue**（64 文件）／**7c** 链接一行式 **175‧0**／**7d** porcelain **6**（4 件 M ＋ 2 行未跟踪，逐行与断言 7 的逐类列举同）；**8a 命中 1**／**8b 359**。**F1 硬要求另跑自备探针十项（F1a–F1e ＋ P1–P5）**：CRLF 件插行后 CRLF=**4**／裸 LF=**0**／无 BOM；LF 件 CRLF=0／裸 LF=3；`--set` 含换行／含空行；`--append` 值以「- 」起首（经批量形态）；参数错三项（`--at` 空串／缺操作开关／batch ＋位置参数）rc **2**；批量同件多编辑按逐条应用后文本判唯一（rc 0）与批内不唯一**零写盘**。**改动件行尾完整性**：四件重读均 CRLF／裸 LF=0／无 BOM（工具未把 CRLF 写坏）。
  **`[4]` 段候选逐条判**：①「自噬预警 `修订工具批（091）` @ `CHANGELOG.md` 现行 1｜目标文本内 1｜近似预测 2」——断言 8 是**计行**命令且目标件为 `CHANGELOG.md`（非规格本体），工具按 [B] 行（F4）目标侧字面串并入预测故高估（其头注自注「`[B]` 行的目标文本内含改动说明文字，故近似预测为上限」）；实跑命中 1 ≥ 期望 1，该串施工前为 0（本批新增即产物本身）→ **属预期**。②「[A] 未落实或已漂移: `**修订工具律**：…——**禁` @ `docs/specs/施工机制.md`」——检查 C 按 F2 [A] 行的**整块逐字**比对，而该引号块末尾含**规格侧注**（去重说明）未落盘（见偏差②）→ **属预期**（规格侧注不入标准件条文，090 F3 先例；该规格归档后本候选不再产生）。③「对账① 汇总：改动行 5 行｜可解析 5 件｜判一适用 0 件｜判二缺 0 件｜跳过：无补集式断言 1」与「B：未提取到可判定的尺寸声称（2 条非整件口径未判）」——既有口径常态 → **属预期**。
  **偏差申报（P0×2：规格自相矛盾两处；均先呈报、后落 [A] 逐字、未改断言／数字／未构造绕过；两处均经规划方裁定）**：**① 断言 6b ↔ F3 表注逐字互斥**（动工前停摆呈报）：断言 6b 原期望 **3** 且口径注称「＋ 表注行 1」，而 F3 表注 `:73` 的逐字文本只点名四个**类目**、不含 `patch_file.py` → 逐字落盘后必为 **2**（§2 表行 ＋ §1 目录树行）。裁定**取①**：期望改 **2**、归因改「§2 表行 1 ＋ §1 目录树行 1」、表注不计（规格 §三 已同步）；落盘实测 **2** ✓。**② 断言 5b ↔ F2① 逐字互斥**（呈报后经裁定）：断言 5b 期望 `禁手写长串匹配的临时脚本` **1**，而 F2① 首版逐字文本内该串出现 **2 次**（首句禁则 ＋ 触发条件句重述）→ 逐字落盘后实测 **2**。裁定**取去重**：F2① 触发条件句改为「…时**本条适用**；**宿主自带…**」并删去重述 → 已按**新逐字**再落一次（`--set` 一处，实测该串 **1**、旧句残留 **0**）。**规格侧注未落盘（明示）**：新逐字引号块末尾的「（**去重说明**：施工方呈报本条与断言 5b 互斥…）」属**规格侧注**（过程叙述、且点名施工方与裁定来因），按 090 F3 先例**不入标准件条文**；若裁定意图含落盘，一条 `--set` 即可补。
  **观察（交规划方，P1／P2）**：①（P1）**#20 行「关系注」的落格未由规格指定**——落**正确方式格**末、括注形式「（**本行取代 #18 的手写脚本路径**；#18 保留为事故记录——防两行并置产生相反指引）」（该注修饰的正是 #18 的处方；尾部「成稿审查 F-20」属过程指针、未入条文）。②（P2）**§四 自审 4 的 porcelain 值陈旧**：写「porcelain **5**（3 M ＋ 2 未跟踪）」，与 §三 断言 7 的逐类列举 **6**（4 M ＋ 2 未跟踪）不一致——实况 **6** ✓（与断言 7 一致），§四 该句待规划方按实况订正。③（P1）**工具两处回读放宽（已在 docstring 明载、非静默）**：(a) `--set` 新文本自身含锚串时免「锚串不再在该行出现」子项（该子项与其字面自相矛盾）；(b) `--batch` 内**同槽位被后条重写**时前条记录改判该槽位**终态**（其产出被后条消费、不可能仍在位）——由自跑探针 P4 实证（未修前该形态必误报回读不符）。两处均不涉本批 9 条编辑（槽位互不重叠）。④（P2）**`[5]` 段（脚本语法）未建**：新 `.py` 无语法守卫，本批以断言 1 的 `ast.parse` 兜一次（登记 §八 1、不接线）。

- 2026-09-15｜**补修轮**（施工方，subagent，后台 spawn）｜**V-03／V-06 两处全落**（同件 `scripts/patch_file.py`，零超面改动）：
  **① V-03（P1，硬要求被破）——`--batch` 同件归并只按字符串键**：新增 `_ident()`（优先 `os.stat` 的 `(st_dev, st_ino)`——Windows 下即卷序列号 ＋ 文件索引，可识大小写 alias／8.3 短名／符号链接／硬链接；`st_ino` 为 0 或 stat 失败时退 `normcase(realpath(...))`；**只读**，只 stat 不开文件）；`main()` 内载入前按 ident 分组判 alias，**命中即 `Fail` → 退出 2、零写盘**，错误行 `x` 起首并说明「同件被多个路径写法引用——…指向同一物理件（`--batch` 同件多编辑须用同一路径写法）」；头注「用法与参数」的 `--batch` 条同批载明该限制、「维护入口」补 `_ident()`。
  **② V-06（P2，头注与实现相抵）**：删去不可达的「（同一行内出现多处时一并替换）」，改述为「把锚行内的锚片段**整片段**替换为新文本（同一行内出现多处＝命中不唯一、中止）」；`_hits()` docstring 的同族措辞同步改述为「故 `--set` 的替换面对**恒为单处**（`str.replace` 与单处替换等价），无「同行多处」形态」。
  **实跑（批式，一脚本一次跑完；命令→实测→期望 见核验请求）**：**alias 探针**——大小写 alias（`.tmp/091-fix/case.txt` ＋ `.tmp/091-fix/CASE.txt`）rc **2**、文件逐字节未变（`aa/bb/`）、`stat -c '%Y %s %i'` **前后全同**（真零写盘）、首行 `x 参数错：同件被多个路径写法引用…`（归因已改对，不再是误导性的「回读不符」）；硬链接 alias（`hard.txt`）rc **2**、零写盘、stat 同；**反向核**单条正常批量仍 rc **0**（补修未把正常批量挡死）。**回归**：断言 **1**（`--help` rc 0／白名单外模块 `[]`／`ast.parse` ok）／**2**（rc 0 ＋ `alpha/BETA/gamma/`；0 命中 rc 2 ＋ 逐字未变 ＋ stat 同）／**3**（rc 2 ＋ 命中行回显 ≥2 ＋ 未变）／**3b**（rc 2 ＋ 最接近行 1）／**3c**（`head/MID/PRE/tail/`）／**4**（rc 0 ＋ `ONE/two/` ＋ 箭头 ≥1）＋ 同件同写法多编辑（rc 0 ＋ `yy/bb/cc/`）全绿、**零红**。门禁 `bash scripts/check.sh` rc **0**（尾「== 检查全绿 ==」）、`npx markdownlint-cli2 "**/*.md"` **0 issue**、porcelain **6**（4 M ＋ 2 未跟踪，逐行同前）；工具件 **LF／无 BOM**（补修后实测 **385 行／14173 字符**）。
  **偏差记录（P1×1，未自行处置、报请规划方）**：§九 `:222` 记「（**385 行**／14173 字符（补修后实测；原记 367／13277 系补修前值——施工方 ⑤ 指出）…）」为**补修前**值，补修后实测 **385 行／14173 字符**——该行为规划方本轮所改、属其施工面，故**未擅动**（反模式 #1「文档数字与实测对齐」：改一位数字即可）。`[4]` 段「基线漂移（本批改动件，不阻断）: scripts/patch_file.py 称「13277 字符」实测 14173」即此项——**判「待处置」（规划方面）**，不按「属预期」登记。**另观察（P1，规划方面）**：§九 `:225`（内容为 `second`）与 `:226`（以 `'` 起、以 `"` 止的残句）疑为本轮 §九 改写的**中间态残留**；该行另产出两条「规格写作预警（代码跨度边缘空格）」（其首字符 `'` 后的反引号参与配对所致）——两者同属规划方面，一并报请核处，本施工方未动该两行。
