# 092-2026-09-15-真源面收口批

> 定位：清掉基座**真源面**的三笔——① **运行时资产引用仓库治理文档的格式口径缺款**（`090` §八 2／`089` §八 1 转记；**真机已证害**：安装会话读完壳即报「缺依赖」）；② `skill形态标准` §13 矩阵缺**「阶段文件」**与**「仓级开发工具」**两类目行；③ **`AGENTS.md` 两处状态句与实况不符**（作者 2026-09-15 会话内确认逐字）。并**补登记** `091` §八 漏登记的一条（`check_spec.py` 的 `\|` 静默盲区）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**重路径**——**动 `skills/` 域资产**（4 处指针标注）。→ 标准路径 ＋ **双维产物审查**（改标准条文）＋ **真机走查豁免**——**豁免理由**：本批在 `skills/` 内只改**出处指针的措辞**（加「维护出处／运行时不必加载」标注），**不新增／删除行为指令、不改门控与话术**（新增文字仅为止损标注——本批修的正是真机可观测之害，修法本身不含行为变更），按 `施工机制` §二 重路径行的豁免判据不跑走查。
> 自由度分布：[A]×5 [B]×2
> 必读输入：本规格 §一；`docs/standards/命名标准.md`（§5 表 `:93`–`:99` ＋ `:101` 规则段）；`docs/standards/skill形态标准.md`（§13 矩阵 `:138`–`:148`；§9.1 `:93`–`:98`）；`skills/abzu-outline/SKILL.md:33`；`skills/abzu-outline/references/outline-stage-spec.md:7`／`:16`；`skills/abzu-outline/references/outline-workflow.md:48`；`AGENTS.md:5`／`:9`
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | 跨层引用**实为 4 处**（088 双维审查记「**6 处**」＝**修前**总数，其中 2 处已由 088 当场**内联自包含**（088 §九 V-05／§八 13）；现存 **4 处**，本次 `grep -rn` 复核＝4——成稿审查 F-08 订正框定） | 见 ②–⑤ 逐处原文 ✓ |
| ② | 壳内指针 | `SKILL.md:33`「细则引 `触发与交互标准` §2／§3。」——**该指向不可达**（包内无此件）；而壳**已自包含**门控／停靠／模态的三条要点 ✓ |
| ③ | 阶段文件·沿革指针 | `outline-stage-spec.md:7`「> 出处：内容逐字迁自产品层《大纲域设计·规格与资料步》§一（1.0–1.2）；源件自带 3 条 markdown 链接已按 `命名标准` §5 改写为路径文字（088 §九 差异有记）。」 ✓ |
| ④ | 阶段文件·目录树指针 | `outline-stage-spec.md:16`「④产物：书目录树（见 `书目录契约` §1）；`创作进度.md` 初始化…」——**同一行已把本阶段用得到的落点列明** ✓，故该指针属补充 ✓ |
| ⑤ | 工作流·目录树指针 | `outline-workflow.md:48`「…（`创作进度.md`／`基本规格.md`；其余随各自阶段批建齐，目录树见 `书目录契约` §1）。不调外部服务；无不可逆操作。」 ✓ |
| ⑥ | `命名标准` §5 规则段现文 | `:101`「规则（按资产域分区）：skill 运行时资产（`references/`、**`assets/`**、`scripts/`）禁跨目录链接——部署副本会断链，必须自包含；仓库治理文档（根目录、`docs/`）允许跨目录 Markdown 链接——它们在编辑器与 GitHub 中阅读，链接是可用性加分。references 之间不互相链接（一层深纪律…）。」——**只管「链接」，未管「按名指向」**（本批补的正是这一格） ✓ |
| ⑦ | §13 矩阵两缺口 | 现有行：域工作流 `:141`／方法论 `:142`／开发守卫 `:145`／治理与门面文档 `:146`——**无「阶段文件」行**（088 §八 6 登记）／**无「仓级开发工具」行**（091 §八 4 登记） ✓ |
| ⑧ | `AGENTS.md` 两处状态句 | `:5`「**运行**＝`skills/` 下的 skill 组（**尚未创建**）…」；`:9`「**当前状态：运行侧（`skills/`）与产品层各 skill 均尚未创建——本仓库只有开发侧基座**」——与实况（`skills/abzu-outline/` 已建最小壳与阶段一资产）不符 ✓ |
| ⑨ | 基线 | `bash scripts/check.sh` exit **0**；lint **0 issue**（**65 件**——含本规格；原写 64 系 091 批旧值，成稿审查 F-07 订正）；链接一行式 **176／断链 0**（**175 ＋ 1**：`skill形态标准` §12.4 括注新增同目录链接——窄域复审 P1-1 订正）；porcelain **1**（仅本规格） ✓ |

**不变量（施工前后须同）**：本批改动**全部落点**：`docs/standards/命名标准.md`（**§2 行名 ＋ §5 表新行 ＋ §5 规则段追加**）／`docs/standards/skill形态标准.md`（**§9.1 半句 ＋ §13 两行**）／`skills/abzu-outline/SKILL.md`（`:33`）／`skills/abzu-outline/references/outline-stage-spec.md`（`:7`／`:16`）／`skills/abzu-outline/references/outline-workflow.md`（`:48`）／`AGENTS.md`（`:5`／`:9`）／`CHANGELOG.md`；**除此之外零改动**（`README.md`／`docs/product/`／`docs/specs/`（含 `archive/`）／`scripts/` 零改动）。**资产内零 markdown 链接**（本批只加文字标注）；链接一行式 **176／断链 0**（**＋1 vs 预验基线 175**——增量＝`skill形态标准` §12.4 括注新增的一处同目录链接；窄域复审 P1-1 订正）；断链 0 不因本批变化。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `docs/standards/命名标准.md`（**§5 表新行**——插在 `:99` 之后（§5 表 `:95`–`:99`）／`:101` §5 规则段末）／`docs/standards/skill形态标准.md:95` | **三处落点**（成稿审查 F-03／F-11）：① §5 **表新增一行**（正例，同时解 F-03 的「禁裸文件名」相抵）：「\| 指向仓库治理文档（**部署副本内不存在**） \| **维护出处：{件名} §N；运行时不必加载** \| 维护出处：`书目录契约` §1；运行时不必加载 \|」② §5 规则段末追加（逐字）：「**运行时资产引用治理文档**：skill 资产内**不得裸指向**仓库治理文档（**无标注地指向**）（`docs/` 下各件）——部署副本内不存在。凡**模型执行时必须知道**的事实（命令名、字段名、阈值），一律**自包含**写在资产内；凡**仅供维护追溯**的出处，写「**维护出处：{件名} §N**」并**明示「运行时不必加载」**。」 ③ `docs/standards/skill形态标准.md:95`（**§9.1 第 1 条末**——`:95` 是该条收尾「格式总表见…」；§9.1 末实为 `:98`，窄域复审 N-6 订正）加半句：「**指向仓库治理文档的写法见 `命名标准` §5**」（消 §9.1「禁裸文件名」与本条款相抵）。 | [A] | **090 §八 2**／089 §八 1；作者 2026-09-15 裁定「甲」；真机证据（088 走查安装会话报「缺依赖」） |
| F2 | `docs/standards/skill形态标准.md`（§13 矩阵：**阶段文件行插在 `:142` 之后**（邻「方法论」）、**仓级开发工具行插在 `:145` 之后**（邻「开发守卫」）；**原行号口径、施工时自下而上插**，免行号漂移） | 新增两行：①「\| 阶段文件 `{域}-stage-{阶段名}.md`（**同批把 `命名标准` §2 行名「阶段方法论」统一为「阶段文件」**——F-10；**该括注与下文 F-09 说明均为规格侧注，不入落盘行文**） \| §6 方法论底线（头部与分节）＋ 阶段细则**七字段单元**（格式真源＝`大纲域设计` §四.1——§6 不含七字段，成稿审查 F-09 订正） \| 头部两行声明（§9.3） \| 同上 \| 内容轨检查器（待建） \|」②「\| 仓级开发工具（`scripts/` 下非守卫脚本） \| 无统一正文模板 \| 四要素（`#` 注释块／模块 docstring）——**写实不复用「同上」**（落位在 `:145` 之后，指向上文方法论行会失真） \| `.py` 遵循 PEP 8 与 PEP 257 \| `[5]` 段落建前＝当批人工 `ast.parse` \|」 | [B] | 088 §八 6／**091 §八 4** |
| F3 | `skills/abzu-outline/SKILL.md:33` | 「细则引 `触发与交互标准` §2／§3。」→「细则引 `触发与交互标准` §2／§3（**维护出处：`触发与交互标准` §2／§3；运行时不必加载**——本壳已自包含其要点）。」 | [A] | F1 新条款的落地；§一 ② |
| F4 | `skills/abzu-outline/references/outline-stage-spec.md:7`／`:16` | `:7` 句尾补「——**维护出处：`命名标准` §5；运行时不必加载**」；`:16`「（见 `书目录契约` §1）」→「（**维护出处：`书目录契约` §1；运行时不必加载**；本阶段用得到的落点见下文）」 | [A] | F1；§一 ③④ |
| F5 | `skills/abzu-outline/references/outline-workflow.md:48` | 「目录树见 `书目录契约` §1）。」→「目录树**维护出处：`书目录契约` §1；运行时不必加载**）。」 | [A] | F1；§一 ⑤ |
| F6 | `AGENTS.md:5`／`:9` | `:5`「（**尚未创建**）」→「（**已建首个域的最小壳与阶段一资产**，其余域未建）」；`:9`「**当前状态：运行侧（`skills/`）与产品层各 skill 均尚未创建——本仓库只有开发侧基座**」→「**当前状态：大纲域已建最小壳与阶段一资产，卷纲／正文两域未建——本仓库以开发侧基座为主**」 | [A] | **作者 2026-09-15 会话内确认逐字**（宪法件修改）；088 §八 1 |
| F7 | `CHANGELOG.md`（`### Changed` 段首） | 一行：『真源面收口（092）：…』——运行时资产引用治理文档口径入 §5／矩阵补两类目行／四处指针标注／`AGENTS.md` 状态句；≤400 字符、末尾「核验：」句 | [B] | 过程产物 |

**口径要点（易错）**：① **F3–F5 只加文字标注、不动任何指令与门控**（真机走查豁免的前提，见头部豁免理由）；② **F2 两行以半角竖线分列**（矩阵既有行体例；表内不得出现裸竖线）；③ **F1 的「维护出处：」形态**是该件内新引入的写法，须与既有「见 `X` §N」措辞区分（**本批不改 `命名标准` 内既有措辞**——如其 `:72`「见 `docs/product/架构设计.md` §四」不动，只新增本条款；而 F4／F5 对 `skills/` 内那两处「见 `书目录契约` §1」的改写**属本批范围**，窄域复审 N-7 明写范围）；④ 本批**不改** `skills/` 内的其余文句（仅加标注，零行为指令变动）。

## 三、验收断言（一条命令一期望；计数口径逐条标注）

### 断言 1｜F1 新条款

```bash
grep -o "运行时资产引用治理文档" docs/standards/命名标准.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "维护出处：{件名} §N" docs/standards/命名标准.md | wc -l
```

期望 **2**（**计出现次数**——**§5 表正例行样板 1 ＋ 规则段样板 1**；窄域复审 N-1 曾报此项未随 F1 扩容重算——**已修**（经复核方独立复算：§5 两处落点各 1 次、合计恰好 2））。**预验基线 0**。

### 断言 2｜F2 矩阵两行 ＋ **F1 另两处落点**（§5 表新行／§9.1 半句）

```bash
grep -c "^| 阶段文件 \|^| 仓级开发工具" docs/standards/skill形态标准.md
```

期望 **2**（**计行**）。**预验基线 0**。

```bash
grep -c '^| 指向仓库治理文档' docs/standards/命名标准.md
```

期望 **1**（**计行**——§5 表正例行；窄域复审 N-2 补，基线实测 0）。**预验基线 0**。

```bash
grep -c '指向仓库治理文档的写法见' docs/standards/skill形态标准.md
```

期望 **1**（**计行**——§9.1 半句；基线实测 0）。**预验基线 0**。

```bash
grep -c '阶段方法论' docs/standards/命名标准.md
```

期望 **0**（**计行**——§2 行名已统一为「阶段文件」；基线实测 **1**）。**预验基线 1**。

```bash
grep -c '^| 阶段文件 ' docs/standards/命名标准.md
```

期望 **1**（**计行**——改名后的行在位；基线实测 0）。**预验基线 0**。

### 断言 3｜F3–F5 四处标注（正向计数 ＋ **负向齐备**）

```bash
grep -ro '维护出处：[^；]*；运行时不必加载' skills/abzu-outline/ | wc -l
```

期望 **4**（**计出现次数**——壳 1 ＋ 阶段文件 2 ＋ 工作流 1）。**预验基线 0**。

```bash
grep -c "触发与交互标准" skills/abzu-outline/SKILL.md
```

期望 **1**（**计行**——指针仍在，只是带了「运行时不必加载」标注；**不得删指针**：维护追溯要用）。**预验基线 1**。

```bash
grep -rn '见 `\(触发与交互标准\|命名标准\|书目录契约\|架构设计\|术语表\|skill形态标准\)' skills/ | wc -l
```

期望 **0**（**计行**——**负向：「见〈件名〉」形态的裸指向已归零**——**命令只覆盖该形态**，另两处「引／按」形态（`SKILL.md:33`／`outline-stage-spec.md:7`）由本断言第一条的「四处标注计数＝4」覆盖；基线实测 **2**。窄域复审 N-4：原标题的「不得出现裸形态」无对应命令）。**预验基线 2**。

### 断言 4｜F6 两处状态句

```bash
grep -o "已建首个域的最小壳与阶段一资产" AGENTS.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -o "大纲域已建最小壳与阶段一资产，卷纲／正文两域未建" AGENTS.md | wc -l
```

期望 **1**（**计出现次数**）。**预验基线 0**。

```bash
grep -c "尚未创建" AGENTS.md
```

期望 **0**（**计行**——两处旧句归零；改前 **2 行**）。**预验基线 2**。

### 断言 5｜门禁、链接与改动面

```bash
bash scripts/check.sh
```

期望 exit **0**（含 `[2]` 段对 `skills/abzu-outline/` 的逐壳六项）。**预验基线 0**。

```bash
npx markdownlint-cli2 "**/*.md"
```

期望 **0 issue**。**预验基线 0 issue**。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **176 0**（**预验基线 175‧0**——增量 ＋1 来自 §12.4 括注新增的同目录链接；窄域复审 P1-1 订正）。

```bash
git status --porcelain | wc -l
```

期望 **8**（**逐类列举**：**7 件 tracked M**＝`docs/standards/命名标准.md`／`docs/standards/skill形态标准.md`／`skills/abzu-outline/SKILL.md`／`skills/abzu-outline/references/outline-stage-spec.md`／`skills/abzu-outline/references/outline-workflow.md`／`AGENTS.md`／`CHANGELOG.md` ＋ **1 行未跟踪**＝`?? docs/specs/092-…md`）。**预验基线 1**；施工后逐行列核验记录（不符以实况为准并订正）。

```bash
grep -rc "](" skills/abzu-outline/ | grep -v ":0$"; echo "扫毕"
```

期望：仅「扫毕」（**资产内仍零 markdown 链接**）。**预验基线**：仅「扫毕」。

### 断言 6｜过程产物

```bash
grep -cF '真源面收口（092）' CHANGELOG.md
```

期望 ≥1；行长 ≤400（施工后实核项），末尾「核验：」句。验收通过后本规格移入 `docs/specs/archive/`。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 九条实跑取证（①②–⑤ 四处原文逐处 python 读取；⑥ §5 规则段原文；⑦ 矩阵四行原文；⑧ `AGENTS.md:5`／`:9` 原文；⑨ 门禁三命令与 porcelain 1）。
2. **断言实跑过**（改前侧）：断言 1（0／0 → **期望 1／2**）／2（**0／0／1／0**——含窄域复审 N-2 补的四条）／3（0／1 ＋ **负向 2**，N-4 补）／4（0／0／2）／5（exit 0／lint 0／175‧0／porcelain 1／资产零链接）逐条实跑；N-1～N-7 修订后五条新基线（`0／0／1／0／2`）由规划方同轮重跑复核。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：断言 4 第三条「期望 0」由**实测旧句 2 行**（`grep -c "尚未创建"` ＝2）得出；断言 5 的 porcelain **8** 由**逐类列举**得出（7 M ＋ 1 未跟踪）；断言 3 的「期望 4」由**四处落点逐处列举**得出（壳 1／阶段文件 2／工作流 1）。 断言 1 第二条的「期望 2」由 F1 与 §5 两处落点（表正例行 ＋ 规则段）逐处列举得出——N-1 的病因正是此处未同源。

## 五、禁止事项

- 禁止触碰：`.git/`、`README.md`、`docs/product/`、`docs/specs/`（含 `archive/`）、`scripts/`（含 `patch_file.py`／`check.sh`／`check_spec.py`／`hooks/`）。
- **F3–F5 不得改动任何指令、门控、话术或产物契约**——只加「维护出处／运行时不必加载」标注；**不得删除原指针**（维护追溯要用）。
- **F1① 与 F2 两行的表格样本须落为真表格行**——样本内的 `\|` 是为写进本规格表格而转义的，**落盘时去转义为分列符**；但**单元格内不得再出现裸竖线**（含 `<br>` 等内联 HTML——MD033 亦禁）（半角分列）。
- 不得改断言、改数字、删检查来变绿。
- 禁止动作：任何 git 写命令；联网；安装依赖。

## 六、被否决的替代方案

- **只立规则、不改 `skills/` 那 4 处实例**（拆分）→ 否决（作者 2026-09-15 裁定「乙同批」）：真机已证那 4 处形态会让模型读成「缺依赖」，只立规则会让缺口继续活着。
- **把 4 处指针直接删掉**（而非标注）→ 否决：它们是**维护追溯**的唯一线索（哪一节迁来的、模板真源在哪），删了等于丢溯源；F1 新条款本身就区分了「须自包含的**可执行事实**」与「须标注的**追溯指针**」。
- **把新条款放在 `skill形态标准` §9.1** → 否决：§9.1 末尾已写「格式总表见 `命名标准` §5」，引用格式的**单点权威在 §5**（单一真源）。
- **`AGENTS.md` 状态句改为「已建成」** → 否决：只建了**一个域的部分资产**，写「已建成」同样失真；F6 的逐字按实况细述（大纲域已建最小壳＋阶段一资产、两域未建）。

## 七、本批核销（091 §八 ＋ 090 §八 2）

> **必列面**：091 §八 **全 5 条**（前一批的全部开放项）＋ 090 §八 2（本批的专项目标）＋ 本批触及者。**不必重列**：088 §八（089 §七 已承载——索引指针＝`089` §七）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 090 §八 2 | `命名标准` §5／`skill形态标准` §9.1 的补款（运行时资产引用治理文档的口径） | **本批处置** | **F1**（落 §5，单点权威）；§9.1 不复制（其已指 §5） |
| 090 §八 1 | G20 的残余（代码注释批号） | **已履行**（091 §七 判「非缺陷、保留」） | 跨批指针不随时间失效；索引指针＝`091` §七 |
| 090 §八 3 | 真机走查欠账（`088` §八 16 转记） | **仍待** | 触发＝089 之后或下一个域建成前必须补跑（作者：先不着急） |
| 090 §八 4 | 大纲阶段二（资料收集／采风）未开工 | **仍待** | 本批清完真源账后仍非下一批（作者定：先守内容轨检查器） |
| 091 §八 1 | `[5]` 段（脚本语法）待建 | **仍待** | 触发＝内容轨检查器批（下一批）合建；F2 的机检列已写「`[5]` 段落建前＝当批人工 `ast.parse`」 |
| 091 §八 2 | 工具的假阴性面（语义唯一但字面多处） | **仍待** | 触发＝实际撞见时评估「按行号 ＋ 上下文双键」 |
| 091 §八 3 | ~~G20 行改述~~ | **已履行**（091 内） | 091 F5 已办 |
| 091 §八 4 | `skill形态标准` §13 矩阵缺「仓级开发工具」行 | **本批处置** | **F2②** |
| 091 §八 5 | `命名标准` §2 的「仓级开发工具」类目（族级判据） | **仍待** | 触发＝再增同类工具时评估族级判据 |
| **091 §八 漏登记（本批补记）** | `check_spec.py` 的 `\|` 静默盲区（091 双维审查 V-02：含转义竖线的表格行被 `_cells()` 当分隔符 → **该行既不核逐字也不报，静默跳过**；091 的加粗漂移即因此漏网） | **仍待（高优先）** | 091 §八 因此条**未落盘**（当时规格脚本一次中止、该登记随之中止，091 归档后才发觉）→ **本批补登记**：触发＝**内容轨检查器批**（下一批）修 `_cells()` 认 `\|` 转义，或对含转义竖线的行明示「不核逐字」 |
| 088 §八 1 | `AGENTS.md` 两处状态句（原「待作者」） | **本批处置** | **F6**（作者 2026-09-15 会话内确认逐字） |
| 088 §八 6 | `skill形态标准` §13 矩阵缺「阶段文件」类目行 | **本批处置** | **F2①**（成稿审查 F-02：该行原缺，而 F2 依据列已明引 088 §八 6，自相不一） |

- 汇总（恒列五档、无该档计 0）：`已履行 2｜作废 0｜本批处置 4｜仍待 6｜待作者 0 ＝ 12` ✓

## 八、开放项

1. **`[5]` 段与 `check_spec.py` 的 `\|` 盲区** → **下一批（内容轨检查器批）**一并处置（`[3]` 内容轨检查器 ＋ `[5]` 脚本语法 ＋ V-02 盲区）。
1b. **1.2 的落行式样未内联也未指向**（成稿审查 F-14 独立判断查出）：资产要求「门控与停靠须落产物状态表行」，但**式样**（`书目录契约` §2 的「{产物名}-待确认」）既未内联、也未被指向（全 grep 无「待确认」）——**正是 F1 新条款所谓「模型执行时必须知道的事实」那一类**。触发＝**内容轨批**同批内联进 `outline-stage-spec.md` 1.2 的落行句（一行）。
2. **`命名标准` §5 的「维护出处：」形态**：本批只立样板（`{件名} §N`），仓内既有措辞（「见 `X` §N」）未统一——**本批已统一为单一形态**（「维护出处：`X` §N；运行时不必加载」×4，成稿审查 F-13）——原「两种写法并存」的担心解除；仓内**既有**旧措辞（「见 `X` §N」）的收口触发不变。
3. **真机走查欠账**（`088` §八 16）：仍挂，触发不变。
4. **本批的真机验证缺位**：`skills/` 内 4 处标注**未跑真机**（按豁免理由：不新增／删除行为指令与门控话术；**残余**：本批修的是真机可观测之害（088 走查报「缺依赖」）却未在原观测路径复验——触发＝内容轨批若跑真机则顺带冒烟）——触发＝下一批若跑真机走查（内容轨批亦不触用户行为，故可能继续挂账到阶段二批）。
5. **091 双维审查 findings 未落 091 §九**（成稿审查 F-05 查出）：091 归档件 `:228` 仍为「待填：双维产物审查…」，该报告（V-01／V-03／V-05／V-06）**无落点**——违 `施工机制` §五 报告落点。091 已归档只读，故本批登记；触发＝**下批（内容轨批）评估补记或列为本批之外的历史欠账**。

6. **术语表登记「维护出处」**（窄域复审 ②）：本批定名的标注词按 `术语标准` §4 须立词条（建议 `术语标准` §3 末行：中文 ＋ 定义 ＋ 英文对应）——因立词条须**扩本批改动面**（＋1 件）并重算 porcelain 与逐类列举（本批已因补修重算过一次链接数），风险大于收益 → **下一批首项**。同批审视两件事：① 既有术语「设计真源」（义＝域设计件，`docs/product/`／`docs/standards/` 内 8 处）是否也需入表；② `088` §八 13 的追溯链（该件「口径建议」用的是「设计真源：X §N」，与本批实定词不同——归档件只读，不动其文，靠本批 `CHANGELOG` 的「标注词定名」句承接）。
7. **新条款的机检锚点**（产物审查 P2-6）：「资产内治理件名出现而无『维护出处／运行时不必加载』标注」的可机械化检查，`命名标准` §3 机检锚点表未登记；该条款**有事故出身**（088 走查真机报「缺依赖」＋ `088` §八 13／`090` §八 2），按 `AGENTS.md` §四 具备建立资格（不建则须留一句口径）。触发＝**内容轨检查器批**（与该批 `[3]` 段、`\|` 盲区一并处置）。
8. **条款与批次号型包外标记的边界**（产物审查 P2-7）：条款「skill 资产内不得裸指向仓库治理文档」为**全称**，而资产内仍有**批次号型**包外标记（`skills/` 内 11 行，如「随 089–091 未建」「本批唯一上线消费点」）——两者危害面不同（批次号不构成可加载的包外文件，真机不会据此报缺依赖），按 `088` §八 2 的到期删除制处理。触发＝**下一批裁定**「纳入条款明示／维持到期制」。
9. **`SKILL.md:33` 同句指针重复**（产物审查 P2-4）：**不采纳**——同一句内「`触发与交互标准` §2／§3」出现两次（指针本身 ＋ 标注内），但**形态统一优先于行文精简**（成稿审查 F-13 已立「统一为单一形态」）。若下批立「紧邻已有指针时只写『运行时不必加载』」的简写形态，须**四处同改**。
10. **`[5]` 段范围的四载体**（窄域复审 P2-4 延伸）：`check.sh` 头注（现只写 `node --check` 逐域 `.js`）／`测试与验收标准` §1.1 表行／`运行环境标准` §5 机检锚点行（含 `.py`）／`091` §八 1——本批 `skill形态标准` §13 已改为只写现状、不再承诺，但**四载体仍各说一份**。触发＝**建 `[5]` 段时以 `check.sh` 头注为唯一真源并收口另三处**。

## 九、审查记录

- 2026-09-15｜**成稿审查·第一轮**（独立上下文只读 subagent）｜findings 共 **14**：**P0:2** P1:3 P2:9｜结论 **修订后通过**。**两条 P0**：**F-01** 断言 3 的命令双引号内含反引号 → 被 bash 当**命令替换**执行 → 第二分支**永不可能命中**（期望 4 最多给 2；审查方实跑复现 `bash: 书目录契约: command not found`，并给单引号对照 3/3 命中证明**病因在引号不在模式**）→ 改单引号形态并**统一四处标注为单一形态**「设计真源：`X` §N；运行时不必加载」；**F-02** 核销**漏 088 §八 6**（`skill形态标准` §13「阶段文件」行＝F2① 的落点，本批使其状态变化故必列；同批却列了同为 088 的 §八 1，自相不一）→ 补行、汇总改 12。**P1 三条**：**F-03** 新条款使 `命名标准` §5 表第三行与 `skill形态标准` §9.1 的「禁裸文件名」**失效**，按 `规格写作标准` §5.5 应改写而非追加 → **F1 扩为三处落点**（§5 表加正例行 ＋ 规则段追加 ＋ §9.1 加指引半句）；**F-04** 走查豁免理由写「不新增／删除指令」而 F3 新增的正是指令式括注 → 改「不新增／删除**行为指令**，新增文字仅为止损标注」；**F-05** 所引「091 双维 V-02」**仓内不可核**（091 §九 从未落该报告）→ 改可复现实证 ＋ §八 5 登记。**P2 九条**：F-06（自审基线 `0／1／2`→`0／0／2`）／F-07（lint 64→**65** 件）／F-08（「订正 088 的 6 处」框定失实——6 是**修前**数、2 处已由 088 内联）／F-09（F2① 结构真源格指向 §6 而七字段不在那）／F-10（「阶段文件」vs `命名标准` §2「阶段方法论」两名并存 → 同批统一）／F-11（新规范条目缺正反例）／F-12（**F1 首句「不得指向」与后两句「带标注地指向」自相抵** → 改「不得**裸**指向」）／F-13（本批自产两种标注形态 → 统一）／F-14（**审查方独立判断四处分类成立**，但查出真缺口：1.2 的落行**式样**既未内联也未指向 → §八 1b 登记）。**14 条全部处置**（全部规格侧；F-14 属登记）。**审查方另核**：四处指针独立重数＝4 ✓、F6 的作者确认边界合规 ✓、真机走查豁免判据成立 ✓、porcelain 8 与逐类列举同源 ✓、`check_spec.py` **不覆盖核销表**（F-02 无机器兜底） ✓。
- 待填：**窄域复审**（P0＝2，按 `施工机制` §五 止损条 2：只核两条 P0 修复面 ＋ 直接影响面）→ 施工（subagent，后台 spawn）→ 核验三步 → 双维产物审查 → 归档 → 呈作者确认提交。

- **核验结论（规划方，2026-09-15）**：**第 1 步·改动面核对**——`git status --short`／`git diff` 逐 hunk 对照 §二 改动清单：7 件 tracked `M`（`AGENTS.md`／`CHANGELOG.md`／`命名标准`／`skill形态标准`／`SKILL.md`／`outline-stage-spec`／`outline-workflow`）＋ 1 件未跟踪（本规格），清单外零文件；`[A]` 九项与规格给出文本**逐字一致**（F1①／F1②／F2①／F2②／F3／F4①／F4②／F5／F6×2）。**第 2 步·断言独立重跑**——§三 断言 1–6 全部命令以规划方会话重跑、逐条命中期望（含改名后两条模式与订正后的 176 链接数）；退出码不经管道。**第 3 步·内容抽查**——触发产物审查条件（动标准条文）→ 派双维产物审查 subagent（见下条）。**[4] 段预警逐条判定**（`python scripts/check_spec.py --static`，exit 0）：「自噬预警 `触发与交互标准`＠`SKILL.md`」（现行 1｜目标 3｜近似预测 4）＝**属预期**（口径差：断言计行、预测计出现次数且未计删除）；「自噬预警 `真源面收口（092）`＠`CHANGELOG`」＝**属预期**（断言 6 为 ≥1 下界，预测取上界）；「目标文本未提及且现行为 0：`](`＠`skills/abzu-outline/`」＝**属预期**（即断言 3 第三条的归零型已达成）；5 条「跳过（目标件不出现在任何改动行）」＝**属预期且已挂账**（`\|` 静默盲区，见 §七 补登记与 §八 1）——**无未核项**。
- 2026-09-15｜**产物审查·双维**（独立上下文只读 subagent）｜findings 共 **9**：**P0:0** P1:2 P2:7｜结论 **有 finding（无 P0）**。**P1-1（术语相抵）**：本批新立的标注词「设计真源：」与仓内**既有术语**「设计真源」（义＝域设计件，HEAD 中 `docs/product/`／`docs/standards/` 8 处）**同形异义**；且 `outline-stage-spec.md:7` 同行行首「出处：…《大纲域设计》§一」与行尾标注并置，整行读下来会把语义**读反** → 规划方裁定**改名「维护出处：」**（避二义；`:7` 语义随之读正），四处资产 ＋ 标准件 ＋ `CHANGELOG` ＋ 规格同批同步。**P1-2（条文未同步）**：`skill形态标准` §12.4「改 skill 资产后必跑」与 `测试与验收标准:36`／`:61`「真机走查是否必跑按 `施工机制` §二 判据」相抵 → §12.4 补指引（指向由窄域复审再订正为真源）。**P2 七条**：P2-1 示例格代码跨度包住标签本体（→ 只包件名）／P2-2 §13 `:147` 机检列越出 `[5]` 唯一真源（→ 去承诺、只写现状）／P2-3 自造词「包外」未立词条（→ 去词，改「部署副本内不存在」）／P2-4 `SKILL.md:33` 同句指针重复（→ **不采纳**，见 §八 9）／P2-5 `CHANGELOG`「零指令变动」与 F-04 口径不一致（→ 改「仅加标注，零行为指令变动」）／P2-6 新条款无机检锚点（→ §八 7）／P2-7 条款全称覆盖批次号型包外标记（→ §八 8）。**审查方另核**：内容质量五面通过（四处标注形态统一、§5 新行职责清晰、矩阵「同上」链追溯成立、`AGENTS.md` 状态句与实况相符（现场 `find` 核）、`CHANGELOG` 逐项在 diff）✓；**真机走查豁免独立判断成立**（附两条限定：头部豁免理由措辞已改与 F-04 同口径；§12.4 未对接即 P1-2）✓；[4] 段三条预警逐条判「属预期」✓。
- 2026-09-15｜**窄域复审·补修面**（独立上下文只读 subagent）｜findings 共 **6**：**P0:0** P1:1 P2:5｜结论 **仍有问题（补修主体成立）**。**P1-1**：补修在 §12.4 新增的**一处同目录链接**使仓级「链接一行式」**175→176**，而规格断言 5／§一⑨／不变量与 `CHANGELOG` 仍写 175——**本批自己的核验数字被自己的补修打假**（`check_spec.py` 的 B 段对「非整件口径声称」不判，无机器兜底）→ 规划方四处订正为 **176** 并写明增量来源（＝`175 ＋ 1`）。**P2 五条**：§12.4 指向「§3 判据」而非判据真源（→ 改指 `施工机制` §二，与 `测试与验收标准:36`／`:61` 同源）／§5 新表行与 `:97` 行职责重叠且无优先判据（→ `:97` 格式格补半句「指向仓库治理文档时按本表末行」）／表行两格各只对齐实落形态的一半（→ 格式格与示例格补全「整串加粗 ＋ 件名行内代码」）／§13 `:147` 的 `.py` 纳入承诺使 `[5]` 范围成四载体（→ 去承诺，见 §八 10）／「维护出处」须按 `术语标准` §4 立词条（→ 本批不扩改动面，见 §八 6）。**审查方另核**：改名完备（资产与标准件零旧词；2 处命中分别为本规格历史记录与归档 `088` 件，均属应留）✓、23 处新词全在本批语境零误伤 ✓、两条改名断言逐字实跑＝期望 ✓、门禁全绿（`check.sh` exit 0／lint 65 件 0 issue／porcelain 8）✓、[4] 段预警逐条判「属预期」✓。
