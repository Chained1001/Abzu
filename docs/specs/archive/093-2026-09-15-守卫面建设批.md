# 093-2026-09-15-守卫面建设批

> 定位：把 `check.sh` 的两道待建段建成——`[3]` 内容轨（新建 `scripts/check_content.py`）与 `[5]` 脚本语法；修掉 `check_spec.py` 的 `\|` 静默盲区（`091` 双维 V-02，`092` §七 补登记项）；同批收口由此牵出的真源面（`CHANGELOG` 条目限长的规范真源、`[5]` 范围四载体、`命名标准` §3 机检锚点、术语表「维护出处」词条）。
> 路径判定（`施工机制` §二／`规格写作标准` §1）：**标准路径**（跨多文件、增删文件、改规则）＋ **双维产物审查**。**真机走查豁免**——本批只动**开发侧守卫与治理文档**，`skills/` 域资产**零改动**，无用户可观测行为（判据：`施工机制` §二 重路径行；豁免理由的适用范围仅到「不改用户可观测行为」）。
> 自由度分布：[A]×17 [B]×7 ＋ **混合×2**（F6／F12——其单元格内 [A]／[B] 并存），共 **26 行**——**逐行实测得出**（成稿审查 P1-6 订正原写 8＋4）；**F24／F25／F26 各增行后三次同步**（G22 族第五·六·七次复发，均由本批 F22 新建的检查 G 当场抓出，候选非阻断）
> 必读输入：本规格 §一；`scripts/check.sh`（全文）；`scripts/check_spec.py` 的 `:57`–`:61`（退出契约头注）／`:197`–`:201`（常量）／`:437`–`:490`（`_cells`／`_tables`／`_change_rows`）；`docs/standards/测试与验收标准.md` §1.1／§1.2／§4；`docs/standards/文档写作标准.md` §一.1.3／§一.3.8；`docs/standards/skill形态标准.md` §5.1／§6／§9／§13；`docs/standards/命名标准.md` §1／§3／§5；`docs/standards/术语标准.md` §3／§4；`docs/standards/运行环境标准.md` §5；`docs/standards/版本与分发标准.md` §3
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（取证实测，2026-09-15）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `scripts/check_content.py` **不存在**（`[3]` 段为 TODO 占位）；件名已在 `命名标准` `:119` 立法为正例 | `wc -l scripts/check_content.py` → No such file |
| ② | `check.sh` 头注段序：`# [0]`／`[1]`／`[2]`／`# TODO: [3]`／`# [4]`／`# TODO: [5]`，头注止于恰为 `set -u` 的行（`:12`） | 逐行读 `:6`–`:12` |
| ③ | `.js` 目标件**零个**（`skills/` 下只有 `assets/`／`references/`，无 `scripts/`）；`.py` 目标件 **2 个**（`check_spec.py`／`patch_file.py`）；`.sh` 目标件 **1 个**（`check.sh`） | `find scripts skills -type f`／`find skills -type d` |
| ④ | `node` v25.8.0 ✓／`python` 3.12.8 ✓／**`python3` 不可用**／`py` ✓ → `[5]` 若跑 Python **必须复用 `[4]` 段的三级链**（`check.sh:111`–`:119`），且 `[3]` 段序在 `[4]` 之前，探测须**上移** | `command -v` 四命令 |
| ⑤ | `_cells()`（`:437`–`:444`）用朴素按单竖线字符切分，**不认转义**：含 `\|` 的行切出格数多于表头 → `[A]` 落到错列 → `check_pairs()` 的 `if '[A]' not in row['freedom']: continue` **静默跳过**（实证：含竖线转义的样本行其 freedom 列被顶掉，变为「b 形态」而不含 `[A]`） | 只读 import 直调（`_cells` 行为四例） |
| ⑥ | 含 `\|` 的表格行**面很宽**：`archive/083` 11 行、`081` 10 行、`088` 9 行、`092` 10 行、`085` 4 行……（非边角） | `grep -c '\\\|'` 逐件 |
| ⑦ | 链接基线：全仓相对链接 **176 条／断链 0**（`092` 断言 5 命令实跑） | 该一行式命令 |
| ⑧ | `CHANGELOG` 共 **36** 条，字符数最长 **399**（`:44` 075、`:25` 086），第二 **398**（`:15` 087）——**余量 1 字符**；阈值「≤ 400 字符」的**唯一定义在 `docs/specs/施工机制.md:74`**，`docs/standards/` 十一件**字面不含 400** | python `len` 逐条 ＋ `grep -rn '400' docs/standards/ AGENTS.md` → 无输出 |
| ⑨ | 行数实测：`SKILL.md` **37**（硬上限 500／目标 <300）、references 最长 **57**（<300）、`assets/` 件 **99**（不计预算）；`docs/specs/施工机制.md` **272** 行 | `wc -l` 六件 |
| ⑩ | TOC 存在性：全仓唯一「有目录而条目与 H2 不符」者为 `docs/specs/施工机制.md`（§八 内嵌规格模板的 9 个 H2）——该件**正落在「规格与归档件不适用」的例外内**，无需特例豁免；其余 >100 行件目录条目与 H2 逐字一致 | python 遍历比对 |
| ⑪ | 加粗密度（`文档写作标准` §一.3.8：`**` 数 ≤ 行数 ÷ 3，**适用范围限叙事型文档**）——实测非 `docs/specs/**` 的 `.md` 共 **31 件**、**超阈 12 件／未超 19 件**（例：`AGENTS.md` 91‧82 超；`命名标准` 32‧46 不超）；**按 §一.3.8 的适用范围句，仓内目标件全属「各标准件／产品层域设计／清单型文档」故不计兜底 → 该检查当前零适用目标件**（成稿审查 P1-5 订正：原写「20 件全部超阈」失实） | `grep -o '\*\*' \| wc -l` ＋ `wc -l` 逐件 |
| ⑫ | `[5]` 范围**四载体各说一份**：`check.sh:11`（只 `node --check` 逐域）／`测试与验收标准:27`（明写 `.js`）／`运行环境标准:85`（含 `py_compile`，未限对象）／`091` §八 1（明写 `scripts/*.py`） | 四处逐字 |
| ⑬ | `测试与验收标准` §1.2「段内分档」句的枚举**只列 `[0]`／`[1]`／`[2]`／`[4]`**，未含 `[3]`／`[5]`——新段的档位须本批裁定 | `:38` 逐字 |
| ⑭ | 术语表 §3 为**三列**表（`术语｜定义｜英文对应`，`:21`–`:30`，末行 `:30` 负向流程守卫）；**无**「维护出处」也**无**「设计真源」词条 | 逐行读 |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | 新建 `scripts/check_content.py` | 内容轨检查器（`[3]` 段调用）。**五项检查**：① 引用闭合（治理文档相对 Markdown 链接实存；skill 资产内 `references/`／`assets/`／`scripts/` 路径引用实存）② TOC 存在性（常驻文档 >100 行须有 `## 目录` 且条目与 H2 逐字一致；`docs/specs/**` 不适用）③ `SKILL.md` ≤500 行 ＋ 单 reference <300 行 ④ `CHANGELOG` 条目 ≤400 字符 ⑤ 「维护出处」标注（资产行内出现「见／按／引 ＋ 反引号治理件名」形态而无标注者） | [B] | ⑪ 排除加粗密度（阈值未立法 ＋ 零目标件，见 §八 2）；①＝G13 认领；②③④ 的定义真源见 §一 ⑨⑩⑧；⑤＝`命名标准` §5 条款的机检面 |
| F2 | `check_content.py` 的**输出契约与分档** | 调用形态 `python scripts/check_content.py [--static]`——**`--static` 与无参数同义**（保留该写法只因 `[4]` 段既有调用惯例）；两者均扫全仓（本工具无「在制」概念）。候选行＝`·` ＋ 空格起首；置红行＝`x` ＋ 空格起首；信息行＝两空格缩进（沿用 `check.sh` 段输出体例）。**退出码**：`0` 无置红项（候选不影响）／`1` **有置红项**／`2` 参数或环境错。**分档**：**可置红**＝③④（阈值无歧义、现状全绿）；**候选只呈报**＝①②⑤（存量债面宽、判据含近似） | [A] | `AGENTS.md` §五.2 候选永不拦截；`[4]` 段先例（静态发现恒 0）与 `[1]`／`[2]` 先例（发现即置红）之间的**第三档**，须同批写进 `测试与验收标准` §1.2（F11）。成稿审查 P2-1：补 `--static` 口径 |
| F3 | `scripts/check_content.py` 工具头注 | 四要素（用途／用法与参数／依赖与前置／维护入口）＋「只读不写盘**任何**文件」声明（`ast.parse` 而非 `py_compile`：后者会落 `__pycache__`——守卫不得有写盘副作用） | [A] | `skill形态标准` §13「仓级开发工具」行；`:147` 四要素 |
| F4 | `scripts/check.sh:9`（头注 TODO 行 → 实现行） | 「`# TODO: [3] 内容轨（引用闭合 + 加粗密度 + TOC + 行数 + 条目限长）——待建 scripts/check_content.py 后加入`」→「`# [3] 内容轨（引用闭合／TOC／行数与条目限长／「维护出处」标注）——scripts/check_content.py；分档见测试与验收标准 §1.2`」 | [A] | ② 段序唯一真源；同时删 TODO 行（同段号并存会被计两次、`共 N 段` 失真） |
| F5 | `scripts/check.sh:11`（头注 TODO 行 → 实现行） | 「`# TODO: [5] 脚本语法（node --check 逐域脚本）——待落 contracts 脚本后加入`」→「`# [5] 脚本语法（node --check 逐域 .js／ast.parse 仓级 .py／bash -n 各 .sh）`」 | [A] | ⑫ 四载体收口：**本行即唯一真源**，另三处改引用／同步 |
| F6 | `scripts/check.sh`：`[3]`／`[5]` 两段实现 ＋ Python 探测上移 | ① 三级链探测（`python3 → python → py`，逐字照搬 `:111`–`:119`）**上移到 `[3]` 之前**；`[4]` 段改为复用该变量（不得自带第二份探测链）。② `[3]` 段：调 `check_content.py`；前置缺失或**整段零目标件**时按段输出规范顶格打「原因 ＋ 手动路径」两行并 `seg_note_rt[3]` 登记。③ `[5]` 段：逐 `skills/*/scripts/*.js` 跑 `node --check`（**本批零 `.js` 目标件——不探 Node、只顶格打一行信息**）；逐 `scripts/*.py` 跑 `ast.parse`；逐 `.sh`（含 `scripts/hooks/pre-commit`）跑 `bash -n`；**仅当三类目标件全空时才明示跳过并登记 `seg_note_rt[5]`**。④ 接线：两段的置红（exit 1）与参数／环境错（exit 2）均须接到 `fail` 置红（bash 短路写法，`[4]` 段为范式）；输出体例沿用「段标题行以 [N] 起首、x 起首＝失败、两空格缩进＝信息」 | [B]／[A]（探测链逐字照搬） | `check.sh:4` 头注依赖行须同批改述（Python 由「[4] 段需」改为「[3]／[4] 段需」）；`运行环境标准` §一.4「逐脚本不重复实现」。**成稿审查 P0-1**：原 ③ 写「无 `.js` 即跳过」与断言 2／口径要点③ 的「跳过 0」相抵——**裁定：`[5]` 对 `.py`／`.sh` 有目标件，不跳过**；P2-1 补接线句 |
| F7 | `scripts/check_spec.py:437`–`:444`（`_cells()`） | 认转义：按**未转义**竖线切分（负向后视形态的 `re.split`），并**把切出的单元格内的转义竖线还原为竖线字符**——不得再用朴素按字符切分 | [B] | ⑤ 盲区根因；`092` §七 补登记条。成稿审查 P1-7：原标 `[A]` 却给举例式实现 → 改 `[B]`（行为由断言 3 判） |
| F8 | `scripts/check_spec.py`：`_change_rows()`（`:483` 附近） | **新增格数校验**：`len(cells) != len(header)` 时产出一条候选（语义＝「疑似转义竖线或列数错」）——即「认不出也不静默」。**落点边界**：判定落 `_change_rows()`，但**件名与行号由调用方补**（该函数只入参 `text`、无件名上下文；其 **5 处调用点**（`:631`／`:851`／`:1004`／`:1091`／`:1240`）签名与既有行为**不得变更**），输出随调用方既有候选体例 | [B] | ⑤ 的第二道防线（该盲区**已实际漏网一次**：091 加粗漂移）。成稿审查 P1-8：原「打印 件:行」在该函数内不可得 → 补落点边界 |
| F9 | `docs/standards/测试与验收标准.md:25`（§1.1 `[3]` 行） | 管什么列 → 「引用闭合（治理文档相对链接与 skill 资产路径引用须实存）＋TOC 存在性（规格与归档件不适用）＋`SKILL.md`／reference 行数＋`CHANGELOG` 条目限长＋『维护出处』标注形式；**加粗密度本批不实现**（阈值未立法，见 §4 G1）」；现状列「待建」→「**已实现**（分档：行数与条目限长可置红；余项候选只呈报）」 | [A] | ⑬；F2 分档 |
| F10 | `docs/standards/测试与验收标准.md:27`（§1.1 `[5]` 行） | 管什么列 → 「逐 `skills/{skill 名}/scripts/*.js` 跑 `node --check`；逐 `scripts/*.py` 跑 `ast.parse`；逐 `.sh` 跑 `bash -n`」；现状列「待建」→「已实现」 | [A] | ⑫ 收口 |
| F11 | `docs/standards/测试与验收标准.md:38`（§1.2 段内分档句） | **改写 ＋ 追加**（`规格写作标准` §5.5：新文本使旧文本失效即改写，不得只追加）：① 枚举段「**已实现段（现为 `[0]`／`[1]`／`[2]`／`[4]`）的置红一律计红**」→「**已实现段（现为 `[0]`–`[5]`）的置红一律计红**」 ② 句末追加 → 「`[3]` 的置红＝环境缺失或工具崩溃 ＋ 其**可置红项**（`SKILL.md`／reference 行数、`CHANGELOG` 条目限长）超标；其余三项（引用闭合／TOC／「维护出处」标注）为候选类，只呈报、恒不置红。`[5]` 的置红＝语法错或前置缺失。」 | [A] | ⑬：新段档位入分档真源。成稿审查 P1-4：原只追加，与同段旧枚举相抵 |
| F12 | `docs/standards/测试与验收标准.md` §4 四行（表列＝编号｜缺口｜触发条件） | ① **G13** → 触发条件列改「**仍待（本批不认领）**：G13 是**禁止型**扫描（不许出现相对 `.md` 链接），与本批 F1① 的**实存型**闭合扫描**异题**」 ② **G1** 触发条件列追加 → 「**本批不实现**（093：`[3]` 段未含加粗密度——阈值未立法且仓内零**适用**目标件）；「入口型文档」无独立定义（`文档写作标准` §一.3.8 讲的是「以表格与编号条目为主要结构者（各标准件、产品层域设计、清单型文档）」，其机检锚点行用「入口与清单型文档」），扩展时须一并立法」 ③ **G22** 触发条件列 → 「**本批处置（F22）**（第五次复发已到：本批自查即其一）」 ④ 新增 **G23** 行，**追加于 G22 行之后**（**样本内竖线为转义，落盘去转义**）：「\| G23 \| 内容轨「维护出处」标注检查的判据依赖正则近似（「见／按／引 ＋ 反引号治理件名」形态），无标注的等价写法（如裸文件名字符串）漏报 \| 出现漏报实例时按类扩取集 \|」 | [A]／[B]（① ② ④ 为 [A]） | `施工机制` §三「债跟着主题走」；§4 落点分工。成稿审查 P2-3／P2-4／P2-7：列名订正／G13 异题裁定／引文改逐字 |
| F13 | `docs/standards/版本与分发标准.md` §3 第 2 步 | 句末补括注 → 「（**条目写作**：一行结论、**≤ 400 字符**、只写「改了什么 ＋ 核验结论」，细节留规格——`CHANGELOG` 条目限长的**规范真源在本行**，`施工机制` §三 本条引此）」 | [A] | ⑧ 真源摆正（机检阈值的唯一定义原在 `docs/specs/施工机制.md:74`）。成稿审查 P2-2：删原括注里的「`check.sh` `[3]` 段引此」（与 F4 落盘文本不同源） |
| F14 | `docs/specs/施工机制.md:74`（§三 2） | 「一行结论，条目 ≤ 400 字符，只写「改了什么 + 核验结论」，细节留在规格。」→「一行结论，只写「改了什么 + 核验结论」，细节留在规格；**条目限长的规范真源**见 `版本与分发标准` §3。」 | [A] | ⑧ 去第二载体（单一真源） |
| F15 | `docs/standards/运行环境标准.md:85` | 「\| §5 脚本语法可解析 \| `node --check`／`py_compile`（接入 `check.sh` [5]） \| 待建 \|」→「\| §5 脚本语法可解析 \| `node --check`／`ast.parse`／`bash -n`（`check.sh` [5]，已接入） \| 已实现 \|」 | [A] | ⑫ 收口（并去 `py_compile`——见 F3 的写盘副作用理由） |
| F16 | `docs/standards/skill形态标准.md:145`／`:147`（§13 矩阵机检列） | `:145`（运行时脚本与共享库）机检列 → 「`node --check`／`ast.parse`（`check.sh` [5]，已接入）」；`:147`（仓级开发工具）机检列 → 「`ast.parse`（`check.sh` [5]，已接入）」 | [A] | ⑫ 收口；`:147` 原「当批人工」措辞随段建成而失效 |
| F17 | `docs/standards/命名标准.md:138` 之后（§3 机检锚点表） | 新增一行（**样本内竖线为转义，落盘去转义**）：「\| §5 「维护出处」标注（资产内指向治理文档须带标注） \| 内容轨检查器（接入 `check.sh` [3]） \| 已实现 \|」 | [A] | ⑭ 表列名三列；`092` §八 7。成稿审查 P1-2：引号统一为「」（原用『』，与断言 4 的 token 不同形） |
| F18 | `docs/standards/命名标准.md:104` 句末（§5 条款） | 追加（逐字）：`（**批次号型包外标记**——如「随某批未建」——**不属本条**；该类按各批上线时删除的到期制处理）` | [A] | `092` §八 8 裁定：明示边界，不把批次号标记纳入条款 |
| F19 | `docs/standards/命名标准.md` §1 目录树 `scripts/` 段 | 在 `├── check_spec.py` 行**之后**补一行——**引号块为逐字目标**：「`├── check_content.py`」；缩进与该行一致；**不带注释**（若置注释须对齐该件注释列位＝列 32） | [A] | 新文件须入目录树真源。成稿审查 P2-8：补引号块（原仅行内代码，`[4]` 段逐字核对不覆盖）＋注释列裁定 |
| F20 | `docs/standards/术语标准.md:30` 之后（§3 治理与资产类） | 新增一行（**样本内 `\|` 为转义，落盘去转义**）：「\| 维护出处 \| skill 资产内指向**仓库治理文档**的标注形态（「维护出处：{件名} §N；运行时不必加载」）——标明该出处仅供维护追溯、部署副本内不存在、运行时不必加载；可执行事实须自包含（写法与正例行见 `命名标准` §5） \| maintenance pointer \|」 | [A] | `092` §八 6（`术语标准` §4 新术语须立词条）；共 3 列 |
| F21 | `CHANGELOG.md`（`### Changed` 段首） | 一行：『守卫面建设（093）：…』——`[3]` 内容轨与 `[5]` 脚本语法两段落地／`check_spec.py` 认转义竖线＋格数校验／`CHANGELOG` 条目限长真源入 `版本与分发标准`／`[5]` 四载体收口；≤400 字符、末尾「核验：」句 | [B] | 过程产物 |
| F22 | `scripts/check_spec.py`：**新增检查 G** | 规格头注「自由度分布」（形如 `[A]×n [B]×m`，可含「混合×k」）↔ §二 改动清单**自由度列**的逐行统计——两侧都是可数结构；不一致即产出候选行并附二者实测值。**候选非阻断、恒不置红**；**不得变更既有六检查（A–F）的行为与退出契约** | [B] | `测试与验收标准` §4 **G22 触发已到**（本仓第五次复发，本批头注即其一——成稿审查 P1-6 抓出）＋ 本批正改该文件（`施工机制` §三「债跟着主题走」） |
| F23 | **同族「待建」状态行收口**（九行；**只改状态列与括注，不改条文正文**） | ① `测试与验收标准` 的 §三 `[3]` 行（`:109` 附近）→ 状态改「已实现（分档见 §1.2）」＋管什么改述同 F9 ② 同件 §三 `[5]` 行（`:111` 附近）→ 强制方式补 `ast.parse`／`bash -n`、状态改「已实现」 ③ `skill形态标准` 的 §5.1 行数行 → 「已实现（`check.sh` `[3]`）」 ④ 同件 §5.3 TOC 行 → 同 ⑤ 同件 §11 脚本语法与契约行 → 强制方式补 `ast.parse`／`bash -n`、状态改「部分已实现（语法已接入 `[5]`；契约实跑待建）」 ⑥ 同件 §9 引用闭合行 → 「已实现」 ⑦ `文档写作标准` 的 §一.1.3 TOC 行 → 「已实现」 ⑧ 同件同行括注（「旧仓 `check_content.py` 只扫 `skills/`」）→ 改述（`[3]` 扫全仓常驻文档；**加粗密度仍不实现**） ⑨ `运行环境标准` 的 §1.4 Python 探测链行（`:82` 附近）→「`[4]` 段内三级探测」改述为「`[3]`／`[4]` **共用前置探测**」（F6① 上移所致） | [B] | 成稿审查 P1-3（同族状态行漏改 ≥10 处，其中两处在同一件内与本批直接相抵）。**逐行现状以施工期实文为准**（行号可能因前序改动漂移——按状态列／括注**语义**定位）；`命名标准` `:138` **不在此列**（其判据属 G13 的禁止型扫描，见 F12①） |
| F24 | `scripts/check.sh` 的 `[4]` 枚举措辞（2 处） | 头注 `:10` 的段名枚举与 `:118` 附近的段标题 `echo` 行，均补第七项 → 「断言自噬／计数重算／[A] 逐字／写作检查／三方对账／禁改面禁词核对／**自由度分布核对**」（F22 新增检查 G 后，两处枚举与 `check_spec.py` 头注的「七类」须同口径） | [A] | 成稿审查未列、施工方按「不动清单外文本」申报——**裁定：同批补**（新增检查不改列举载体即留第二口径，与 `施工机制` §四「段序真源」同精神）；枚举字样变、段序与段数不变 |
| F25 | 四处同族收口（产物审查 P2-1／P2-2／P2-5／P2-6） | ① `scripts/check_spec.py`：「六类」口径 → 「七类」**共 4 处**（`:1` 用途句／`:10` 用途枚举末／`:74`–`:75` 呈报守恒段）——**余 3 处「六类」**（「不可审计类（六类）」与「顶层第六类」）**为正解、不得动** ② `docs/standards/skill形态标准.md` §13 矩阵 `:141`／`:143` 两行的「内容轨检查器（待建）」→ 按同口径改述（**区分「检查器已建」与「该具体检查仍待建」**：`:141` 的行数部分已由 `[3]` 覆盖、`:143` 的七字段单元格式检查确未建——**不得直接改状态列以免失实**） ③ `scripts/check.sh` 的 `.js` 零目标件信息行 → 改**两空格缩进**（其性质是信息、不是跳过；顶格体例只留给「跳过」两行与「手动路径」） ④ 引号统一：`测试与验收标准`（`:25`／`:38`）与 `术语标准`（`:31`）的『』 → 「」（F17 成稿审查裁定即「统一为「」」；`文档写作标准` §一 第 6 条的引号三分未立『』） | [A] | 产物审查 P2-1／P2-2／P2-5／P2-6（四条均为本批自身造成的口径不一致） |
| F26 | `docs/standards/测试与验收标准.md` §4 新增两行（工具近似面的落点） | ① **G24**：检查 C 的「嵌套引号取目标文本」近似——`_row_target_text()` 被内层引号截断（实测 F17 → `维护出处`、F13 → `改了什么 ＋ 核验结论`），致检查 A 的中性汇总误计、检查 C 核错文本；触发＝修 `_quoted_blocks()` 的嵌套切分，或出现误报实例 ② **G25**：检查 G 的两型误报——**非表格形态**改动清单（无别名列，实测 `archive/085`／`086` 头注分布正确却被判「清单 0 行」）与**头注「混合×k」写在续行**（11 条候选中已确证 2 条假阳性）；触发＝同族误报再现时按「降级为『不可判』明示行」改造 | [B] | 产物审查 P2-3／P2-4（机检／工具类缺口落 §4，`施工机制` §三「债跟着主题走」） |

**口径要点（易错）**：① **`[3]`／`[5]` 的段序位次不变**（`[3]` 在 `[4]` 前、`[5]` 末），头注行按段序插入原位；② **头注止读锚**＝恰为 `set -u` 的行，`set -u` 之后**不得**新增列 0 的 `# [N]` 注释；③ `[3]`／`[5]` 落地后 `共 N 段` 仍为 **6 段**（两道 TODO 行在原地换成实现行，段总数不变），且「跳过」应为 **0**（`[5]` 对 `.py`／`.sh` 有目标件故不跳过；仅 `.js` 面为空时在段内打一行信息）；④ 新工具**只读**——**不得**用 `py_compile`（落 `__pycache__`）、**不得**写任何文件；⑥ **本规格自身的 `[4]` 段预警（施工前后均可能报）须逐条判「属预期／待处置」并记入核验结论**（`072` 先例）；其中「跳过（目标件不出现在任何改动行）」属**预期**——该形态的根治由 F7／F8 承担。⑤ F7 的正则须自证：`\|` 不得再被当分隔符，同时**本规格 §二 内以 `\|` 写就的样本行**（F4／F5／F17／F20——为写进本规格表格而转义）应能被正确切分；**这些样本落盘时须去转义为真分列符**（行首尾各一收尾竖线、单元格内零裸竖线）。

## 三、验收断言

### 断言 1｜新建件与工具契约

```bash
python scripts/check_content.py --static; echo "exit=$?"
```

期望末行为 `exit=0`（无置红项）。**预验基线**：无此件（F1 为新建）。**未预验**（件不存在，施工后方可跑）——交成稿审查重点核验。

```bash
python scripts/check_content.py --bogus; echo "exit=$?"
```

期望 `exit=2`（参数错）。**未预验**。

### 断言 2｜`check.sh` 段落地

```bash
grep -c '^# \[3\] \|^# \[5\] ' scripts/check.sh
```

期望 **2**（两条 TODO 行已换成实现行）；**预验基线 0**。

```bash
grep -c '^# TODO: \[[35]\] ' scripts/check.sh
```

期望 **0**；**预验基线 2**。

```bash
bash scripts/check.sh > /dev/null 2>&1; echo "exit=$?"
```

期望 **exit=0**。**预验基线**：基线亦为 `exit=0`。

```bash
bash scripts/check.sh 2>&1 | grep -c '^共 6 段：已跑 6｜跳过 0'
```

期望 **1**（**计行**——收尾汇总行；**该行恒为倒数第二行**，末行是 `== 检查全绿 ==`）。**预验基线 0**（基线汇总为「已跑 4｜跳过 2」）。**裁定（施工方 P1-1 判因申报）**：原断言写「末行含 共 6 段…」——`_seg_summary` 在 fail 判定前执行，该行在基线与施工后两侧都**不是末行**，原判据不成立（规划方预验时门禁恰为红、末行是失败横幅，故未暴露）；改为按行 grep ＋ 计数。

### 断言 3｜`\|` 盲区修复（定点实证）

```bash
python -c "import importlib.util as u;BT=chr(96);BS=chr(92);s=u.spec_from_file_location('c','scripts/check_spec.py');m=u.module_from_spec(s);s.loader.exec_module(m);c=m._cells('| '+BT+'a'+BS+'|b.md'+BT+' | 改 X | [A] |');print(len(c),c)"
```

期望输出 **3 格**的列表（`['`a|b.md`', '改 X', '[A]']` 形态，即 `\|` 未被当分隔符、且已还原为 `|`）。**预验基线（修前）**：4 格且 `[A]` 错位（实测 `['`a\\', 'b.md`', '改 X', '[A]']`）。

```bash
python -c "import re,io;s=io.open('scripts/check_spec.py',encoding='utf-8').read();print(len(re.findall(r'格数与表头不符', s)))"
```

期望 **≥1**（F8 的格数校验码在位）；**预验基线 0**。

### 断言 4｜标准件同步（逐条定点）

```bash
grep -c '\*\*已实现\*\*（分档：行数与条目限长可置红' docs/standards/测试与验收标准.md
```

期望 **1**（**计行**——模式中的 `**` 须写成 `\*\*`；**裸 `**` 在 grep 里是空表达式、模式恒零**——施工方 P1-2 判因申报：`grep -c` 裸形 0／`grep -cF` 1／转义形态 1，落文侧另有 `grep -cF` 与 check_spec 检查 C 自噬预警「现行 1」双证）。**预验基线 0**。

```bash
grep -c 'ast.parse`；逐 `.sh` 跑 `bash -n' docs/standards/测试与验收标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '§5 「维护出处」标注' docs/standards/命名标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '^| 维护出处 |' docs/standards/术语标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '≤ 400 字符' docs/standards/版本与分发标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '≤ 400 字符' docs/specs/施工机制.md
```

期望 **0**（改引用后旧数字离场）；**预验基线 1**。

```bash
grep -c 'check_content.py' docs/standards/命名标准.md
```

期望 **5**（**计行**——§1 目录树新增 1 ＋ `:61`／`:75`／`:119`／`:122` 既有 4）；**预验基线 4**。

```bash
grep -c '自由度分布' scripts/check.sh
```

期望 **2**（**计行**——`[4]` 头注枚举行 ＋ 段标题 `echo` 行各 1）；**预验基线 0**。

```bash
grep -c '六类' scripts/check_spec.py
```

期望 **3**（**计行**——余 3 处为「不可审计类（六类）」与「顶层第六类」的正解；本批把其中 4 处改为「七类」）。**预验基线 7**。

```bash
grep -c '内容轨检查器（待建）' docs/standards/skill形态标准.md
```

期望 **0**；**预验基线 2**。

```bash
grep -c '『' docs/standards/测试与验收标准.md
```

期望 **0**；**预验基线 2**。

```bash
grep -c '『' docs/standards/术语标准.md
```

期望 **0**；**预验基线 1**。

```bash
grep -c 'G24\|G25' docs/standards/测试与验收标准.md
```

期望 **2**；**预验基线 0**。

```bash
grep -c '|| echo "  skills/ 下暂无' scripts/check.sh
```

期望 **1**（两空格缩进的 `.js` 信息行）；**预验基线 0**。

### 断言 5｜门禁、链接与改动面

```bash
npx markdownlint-cli2 "**/*.md" 2>&1 | tail -1
```

期望 **0 issues**（件数随新增件变化，不写死）。

```bash
git status --porcelain | wc -l
```

期望 **13**（**逐类列举**：`M` **11 件**＝`CHANGELOG.md`／`docs/specs/施工机制.md`／`docs/standards/` 下**七件**（`测试与验收标准.md`／`版本与分发标准.md`／`运行环境标准.md`／`skill形态标准.md`／`命名标准.md`／`术语标准.md`／**`文档写作标准.md`**——F23⑦⑧ 新增）／`scripts/check.sh`／`scripts/check_spec.py`；**未跟踪 2 件**＝本规格 ＋ **本批预期新增**的 `scripts/check_content.py`）。**预验基线 1**（仅本规格未跟踪）。**施工方须现场逐行列举**并与上列 13 件逐名核对；**若列举与此不符即停摆申报**（不得自行改数字）。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **0**（**断链数**；本批**不新增任何 markdown 链接**——F20 的指向按 `命名标准` §5 `:99`「指定小节」形态写为文件名 ＋ 节名，故**总链接数与基线同为 176**；仍**只断言断链为 0**，总数以施工后实跑值记入核验结论）。**预验基线 0**。

### 断言 6｜过程产物

```bash
grep -cF '守卫面建设（093）' CHANGELOG.md
```

期望 **≥1**；**预验基线 0**；条目行长 ≤400 字符（`python len` 实测并记入核验结论）。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 十四条全部实跑取证（① `wc -l` 缺件／② 逐行读头注／③ `find`／④ `command -v` 四命令／⑤ 只读 import 直调 `_cells` 四例／⑥ `grep -c` 逐件／⑦ 链接一行式／⑧ python `len` 逐条 ＋ `grep -rn '400'`／⑨ `wc -l` 六件／⑩ python 遍历比对／⑪ `grep -o` 逐件／⑫ 四处逐字／⑬ `:38` 逐字／⑭ 逐行读）。
2. **断言实跑过（改前侧）**：断言 1（**未预验**——`check_content.py` 尚不存在，交成稿审查重点核验）／2（0／2；`check.sh` `exit=0` 且末行 `共 6 段：已跑 4｜跳过 2（[3] 待建／[5] 待建）`）／3（修前 `_cells` 得 **4** 格且 `[A]` 错位；格数校验码 0）／4（`测试与验收标准` 三处 0／`命名标准` 0／`术语标准` 0／`版本与分发标准` 0／`施工机制` **1**／`命名标准` 计 **4**——**原写「基线 1」经预验订正为 4、期望改为 5**）／5（lint **66 件 0 issue**／porcelain **1**／链接 **176‧断链 0**）／6（0）逐条实跑。**预验期本规格自身经 6 轮 lint 订正**（MD038 跨度边缘空格 4 例／MD056 表内裸竖线 2 例／MD060 竖线间距 2 例／MD031 围栏空行 1 例）——**均属本批 F7／F8 所治的同族形态**（表格构造与转义处理），已全部清零、现 0 issue。 **成稿审查后全量重跑复核**（2026-09-15）：本件经审查 **P0×1／P1×8／P2×11** 修订——P0-1（`[5]` 段跳过与断言 2 相抵，裁定「不跳过」）／P1-1·P1-2（两条断言 token 与落文不同形 → 已订正，本条基线仍 0）／P1-3（同族「待建」状态行漏改 9 行 → 新增 **F23**）／P1-4（§1.2 分档句须改写枚举）／P1-5（§一 ⑪ 计数失实 → 按实测订正）／P1-6（**头注自由度分布与 §二 不符——G22 第五次复发 → 新增 F22 实现检查 G**）／P1-7·P1-8（F7 改 `[B]`、F8 补落点边界）／P2×11（含 G13 认领错题 → 改「仍待」、§七 补 `088` §八 2 行、porcelain 12→**13** 重算）。**修订后 断言 1–6 的改前侧基线逐条重跑，与规格所写一致**（2-1＝0／2-2＝2／porcelain 1／`_cells` 修前 4 格／格数校验 0／断言 4 七项＝0·0·0·0·0·1·4／lint 66 件 0 issue／链接 176‧断链 0／断言 6＝0）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：断言 4 各条的「期望 1」均由「该行在本批新增」逐条对应（非目视估计）；断言 5 的 porcelain 期望由**逐类列举**得出；断言 3 的「修前 4 格」为实测输出逐字；断言 5 的断链「期望 0」由**基线 0**（176／0）得出、并**只断言断链面**（总链接数随新增链接上升，不作断言——`092` 窄域复审 P1-1 的同类教训）。 断言 4 的 `check_content.py` 期望 **5** 由「§1 目录树新增 1 ＋ 既有 4」逐处列举得出（`grep -n` 实测四行：`:61`／`:75`／`:119`／`:122`）；断言 2 的 porcelain **13** 由逐类列举得出（11 M ＋ 2 未跟踪）。**成稿审查 P1-6**（头注分布）与 **P1-5**（⑪ 计数）均系「未与实测同源」的同族——本批 F22 即以检查 G 机械兜住前者。

## 五、禁止事项

- 禁止触碰：`skills/`（**全目录零改动**——本批不改任何运行时资产）、`docs/product/`、`docs/specs/archive/`、`AGENTS.md`、`README.md`、`scripts/hooks/`、`scripts/patch_file.py`。
- **禁止使用 `py_compile`**（落 `__pycache__` ＝ 守卫产生写盘副作用）。
- 禁止新增列 0 的 `# [N]` 注释于 `set -u` 之后（段计数污染）。
- **F4／F5 的目标文本含 `\|` 转义**（为写进本规格表格）——落盘时须**去转义为真分列符**，且单元格内不得再出现裸竖线。
- 不得改断言、改数字、删检查来变绿。
- 禁止动作：任何 git 写命令；联网；安装依赖。

## 六、被否决的替代方案

- **加粗密度同批实现** → 否决：`文档写作标准` §一.3.8 的兜底阈值**只量叙事型文档**，按 §一.3.8 的适用范围句，仓内 31 件目标件全属「各标准件／产品层域设计／清单型文档」**不计兜底**（实测超阈 12 件／未超 19 件——即便把阈值挪到全类型也会当场 12 红）——现在建即**零适用目标件的死码**；阈值扩展与「入口型文档」定义须先立法（G1 触发未到）。
- **`py_compile` 而非 `ast.parse`** → 否决：`py_compile` 会落 `__pycache__/*.pyc`，守卫不得有写盘副作用（`check_spec.py` 旁已有 `__pycache__` 被 `.gitignore` 排除，属既有噪声，不再增）。
- **`_cells()` 只加「格数校验」而不改切分** → 否决：格数校验只能「不再静默」，`\|` 行仍无法核逐字；两件同做才是「修根因 ＋ 加防线」。
- **`CHANGELOG` 条目限长仍以 `施工机制` 为真源** → 否决：机检阈值的规范真源不应落在流程件（`docs/specs/`），否则 `[3]` 段要引一份流程文档作判据；改落 `版本与分发标准` §3（该件已管 CHANGELOG 版本段）。

## 七、本批核销（092 §八 全部 ＋ 091 §八；`091` §九 一事由 092 §八 5 行承载）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 092 §八 1 | `[5]` 段与 `check_spec.py` 的 `\|` 盲区 → 内容轨批一并处置 | **本批处置** | F5／F6（`[5]` 段）＋ F7／F8（盲区）＋ F1（`[3]` 段） |
| 092 §八 1b | 1.2 落行式样未内联（`outline-stage-spec.md`） | **仍待**（触发修订） | 该改动动 `skills/` **资产正文**、可能触真机走查判据，与守卫批（纯开发侧）不同路径 → 触发改为**阶段一资产内容批**（`092` 原触发「内容轨批同批内联」经本批勘察修订） |
| 092 §八 2 | 既有「见 `X` §N」旧措辞收口 | **仍待** | 属 `skills/` 内容改动，同上；本批只建其**机检面**（F1⑤／F17） |
| 092 §八 3 | 真机走查欠账（`088` §八 16） | **仍待** | 触发不变 |
| 092 §八 4 | 092 的真机验证缺位（残余） | **仍待** | 触发不变（本批不跑真机，理由见头部豁免） |
| 092 §八 5 | 091 双维 findings 未落 091 §九 | **本批处置** | 裁定：**V-02**（`\|` 盲区）由本批 F7／F8 处置，以 `092` §七 补登记行为落点；**V-01／V-03／V-05／V-06** 按「本批之外的历史欠账」结案——`092` §八 5 只登记了其缺落点、未载条目内容，故不复现其结论（「不凭记忆写」纪律；成稿审查 P2-6 订正原「以 §七／§八 登记为准」的整体表述） |
| 092 §八 6 | 术语表登记「维护出处」 | **本批处置** | F20；②「设计真源」入表一事：经本批勘察，该词在 `docs/product/`／`docs/standards/` 内 8 处**义一致**（域设计件），（其中 2 处在治理侧标准件）**用法一致**（＝域设计件／设计权威），与本批新词「维护出处」不再同名、无二义可歧 → **不入表** |
| 092 §八 7 | 新条款的机检锚点未登记 | **本批处置** | F17（`命名标准` §3 行）＋ F1⑤（检查实现） |
| 092 §八 8 | 条款与批次号型包外标记的边界 | **本批处置** | F18（裁定：**维持到期制**，条款明示「不属本条」） |
| 092 §八 9 | `SKILL.md:33` 同句指针重复 | **作废** | 该条在 092 内已裁定「不采纳」（形态统一优先），无后续动作 |
| 092 §八 10 | `[5]` 段范围的四载体 | **本批处置** | F5（头注＝唯一真源）＋ F10＋ F15 ＋ F16 |
| 091 §八 1 | `[5]` 段待建，`.py` 无语法守卫 | **本批处置** | F6③（`ast.parse` 逐 `scripts/*.py`） |
| 091 §八 2 | 工具假阴性面（锚片段多次而语义唯一） | **仍待** | 触发不变（撞见时评估行号＋上下文双键） |
| 091 §八 3 | `测试与验收标准` §4 G20 行改述 | **已履行** | 091 内已履行（该行已划掉标「本批已履行」） |
| 091 §八 4 | `skill形态标准` §13 缺「仓级开发工具」行 | **已履行** | 092 F2② 已落该行；本批 F16 仅改其机检列 |
| 091 §八 5 | `命名标准` §2 仓级开发工具族级判据 | **已履行** | 本批新增 `check_content.py` 即触发条件（再增同类工具）——评估结论：`命名标准` §2 既有「仓级开发工具」行已覆盖（`{动词}-{对象}.py`），不另立族级判据 |
| 088 §八 2 | 「随 089–091」等批次号标注的到期删除（词表另四处消费点） | **仍待** | 属 `skills/` 正文标注，本批为纯开发侧（F18 只明示条款边界、不改资产）；触发＝各批上线时（成稿审查 P2-5 补列） |

- 汇总（恒列五档、无该档计 0）：`已履行 3｜作废 1｜本批处置 7｜仍待 6｜待作者 0 ＝ 17` ✓

## 八、开放项

1. **`[3]` 加粗密度项未实现**（本批排除）→ 触发＝`文档写作标准` §一.3.8 的阈值扩展立法（含「入口型文档」定义——该词无独立真源）；并须先裁定「超阈 12 件」的处置；已并入 §4 G1 触发列。
2. **「维护出处」标注检查的判据近似** → G23（无标注的等价写法漏报）；触发＝出现漏报实例。
3. **1.2 落行式样内联 ＋ 既有「见 `X` §N」旧措辞收口** → 触发＝**阶段一资产内容批**（动 `skills/` 正文，按重路径判真机走查）。
4. **`docs/product/` 等叙事型文档出现后**，加粗密度与 TOC 的适用面须重新核对（本批 TOC 只扫常驻文档、排除 `docs/specs/**`）。
5. **`docs/specs/施工机制.md` 272 行**（本批实测）：其中 §八 内嵌规格模板占约 40 行——若该件触及行数上限话题，模板是否下沉为独立件，留待体积预算讨论（G12 未触发）。

## 九、审查记录

- **核验结论（规划方，2026-09-15）**：**第 1 步·改动面核对**——`git status --short`／`git diff` 逐 hunk 对照 §二 清单：**13 件**（11 `M`＝`CHANGELOG.md`／`docs/specs/施工机制.md`／`docs/standards/` 下七件／`scripts/check.sh`／`scripts/check_spec.py`；2 未跟踪＝本规格 ＋ `scripts/check_content.py`），与断言 5 的逐类列举**逐名一致**、清单外零文件；`skills/` **零改动**（`git diff --quiet -- skills` 一致）。**第 2 步·断言独立重跑**——断言 1–6 全部命令以规划方会话重跑逐条命中（1-1 `exit=0`／1-2 `exit=2`／2-1＝2／2-2＝0／2-3 `exit=0`／2-4＝1／3-1 三格／3-2＝4／4 八项＝1‧1‧1‧1‧1‧0‧5‧2‧0／5 lint 66 件 0 issue／porcelain 13／断链 0／6＝1）；退出码不经管道。**第 3 步·内容抽查**——触发产物审查条件（改规则＋新增守卫件）→ 派双维产物审查（见下条）。**[4] 段预警逐条判定**（核验期 12 条、补修后 18 条，**全部非阻断**）：**属预期 10 条**——4 条自噬预警＝本批新增文本已到位（工具「近似预测＝现行＋新增」多计了规格自身一处，标签已声明「未计删除」）；6 条哨兵＝`^# TODO: [[35]]` 与 `≤ 400 字符@施工机制` 为**归零型已达成**，`^共 6 段…`／`^| 维护出处 |`／`^# [3]`／`^# [5]`／`\*\*已实现\*\*…` 的「现行 0」系 **token 含正则元字符而工具 cur 恒 0**（既有设计面，非本批引入）；**待处置 2 条**——`现行有值／目标文本未提及: 2 个`＝检查 C 的嵌套引号取目标文本近似（已登记 **G24**）、`C：1 行无核对面`＝F23 行路径列未写仓根路径（多文件合并行，属预期）。**12 条中无一条指向本批交付缺陷**。
- **施工期判因申报（施工方，四条全为规格侧缺陷；施工方未动断言、未改落文）**：① 断言 2-3 原写「**末行**含 共 6 段…」——`_seg_summary` 在 fail 判定前执行，该行**恒为倒数第二行**（规划方预验时门禁恰为红、末行是失败横幅，**假前提未被暴露**）→ 改按行 grep ＋ 计数；② 断言 4-1 的 token 在处置成稿审查 P1-1 时被写成**裸 `**`**（grep 视为空表达式、模式恒零）→ 改转义形态；③ 断言 2-4 未按行首锚定，`[4]` 段**回显**同 token 致恒 2 → 改 `^` 锚定；④ F24 断言命令误用反引号（＝命令替换、恒零）→ 改单引号。**四条经规划方修订后重跑复核通过**——本批规格自身引入的缺陷共 4 处，全部由施工方判因申报抓出（未改断言求绿，处置正确）。
- **成稿审查·一轮**（独立上下文只读 subagent）｜findings 共 **20**：**P0:1** P1:8 P2:11｜结论 **修订后通过**。**P0-1**：`[5]` 段「无 `.js` 即跳过」与断言 2 的「已跑 6｜跳过 0」**同规格内相抵且期望不可达** → 裁定「`[5]` 对 `.py`／`.sh` 有目标件故**不跳过**」。**P1 八条**：同族「待建」状态行**漏改 9 行**（其中两处在同一件内与本批直接相抵）→ 新增 **F23**；§1.2 分档句须**改写枚举**而非只追加 → F11 扩为改写＋追加；§一 ⑪ 的「20 件全部超阈」**计数失实**（实测 31 件／超阈 12）→ 按实测订正；**头注自由度分布与 §二 不符（G22 第五次复发）→ 新增 F22 实现检查 G**；F7 标 `[A]` 却给举例式实现 → 改 `[B]`；F8 的输出契约在该函数内不可得 → 补落点边界；两条断言 token 与落文不同形 → 订正。**P2 十一条**：含 **G13 认领错题**（禁止型扫描 vs 本批实存型闭合＝**异题** → 改「仍待」）、§七 补 `088` §八 2 行与 porcelain 12→**13** 重算、G23 新行落点、引文逐字订正等（全部处置入规格）。
- **产物审查·双维**（独立上下文只读 subagent）｜findings 共 **6**：**P0:0 P1:0 P2:6**｜结论 **有 finding（均不阻断）**。**处置**：**F25**（四条同族口径——`check_spec.py` 的「六类」→「七类」**4 处**（余 3 处「不可审计类六类」／「顶层第六类」为正解不动）／`skill形态标准` §13 两行括注（区分「检查器已建」与「该具体检查仍待建」）／`.js` 信息行改两空格缩进／『』统一为「」）；**F26**（两条工具近似面登记为 §4 **G24**＝检查 C 嵌套引号取目标文本、**G25**＝检查 G 的两型误报）。**审查方独立实证**（未采信规格自述）：五项检查**逐项真报出**（阈值边界 500‧501／299‧300／400‧401 精确）；跑前跑后**全件 md5 清单零差异**（无写盘副作用）；退出码 0／1／2 三分如契约；`_cells()` 修复在**全仓 34 件规格 0 误报**；检查 G 的静默面与误报面已量化（11 候选含 2 假阳性）；`skills/` 零改动实测；**真机走查豁免独立判断成立**（13 件全为开发侧守卫与治理文档，无用户可观测行为）。
- **本批的闭环实证（另记）**：F24 增行后**本批新建的检查 G 当场报出「头注 23 行 ≠ 清单 24 行」**——即 **G22 族第六次复发被新检查首次实战抓出**；头注同步为 24 行后自愈（现 0 命中）。这同时是对 F22 立项理由的现场验证（该族**可机械化**）。
