# 095-2026-09-15-守卫回写批

> 定位：处置 **综合评审（外审，五层交叉）** 的 findings——**P1 三项同属一族**（近期守卫变更**未回写上层文档**：`测试与验收标准` §1.1 表、`AGENTS.md` §七 自检信号、`check_content.py` 头注三处与守卫实况漂移），**P2 六项**逐条裁定（采纳 5／拒绝 1），并落地该评审的**结构性产出**：一条「守卫变更回写清单」纪律句。
> 路径判定（`规格写作标准` §1／`施工机制` §二）：**外审驱动 → 标准路径**（判定表「外审报告或外部参考研究驱动的修改」行：✅，**报告转为规格附件、逐条标注采纳／拒绝及理由——禁止拿到外审直接改文件**）＋ **产物审查（双维）**；**真机走查豁免**——本批只动治理文档与开发工具头注，不改用户可观测行为（判据：`施工机制` §二 重路径行）。**唯一触及 `skills/` 的条目（F9）已改为 §八 登记、不在本批改动面**（避免扰动 `094` 待执行的走查版本）。
> 自由度分布：[A]×13 [B]×1
> 必读输入：本规格 §一；`AGENTS.md` §二（路由表）／§七（自检信号）；`docs/standards/测试与验收标准.md` §1.1（`:20`–`:27`）／§4（G22 行）；`scripts/check_content.py` 的 docstring `:10` 与内联注释 `:45`／正则 `:46`；`docs/specs/施工机制.md` §四（机器闸）；`docs/standards/skill形态标准.md` §9.3（`:97`）
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-15）

| # | 评审条目 | 实测（我方复核） | 判定 |
| --- | --- | --- | --- |
| ① | P1-1 §七 第二条「机检段 `[3]` 待建，尚无常驻闸」 | `AGENTS.md:80` 逐字含该句；而 `[3]` 段**已实现**（`check.sh` 头注 `:9` 为实现行、`check_content.py` 的 `_check_refs()` 即引用闭合常驻检查） | **属实**（宪法层事实性错误） |
| ② | P1-2 §1.1 表 [4] 行未同步检查 G | `测试与验收标准.md:26` 的「管什么」列止于「禁改面禁词核对」；全件「自由度分布」仅 **1 处**（在 §4 G22 行） | **属实** |
| ③ | P1-3 `check_content.py` 判据两处不同步 | **docstring `:10`** 写「出现「见／按／引 ＋ 反引号治理件名」形态」；而**内联注释 `:45`** 写「「见／按／引／依照／据 ＋ 可选空格与左括号 ＋ 反引号治理件名」」；正则 `:46` 为 **五词交替式**（「见／按／引／依照／据」＋可选空格与左括号＋反引号治理件名）——**三个词 vs 五个词** | **属实**（病因比评审所述更精确：是头注与同件内联注释不一致） |
| ④ | P2-1 §七 第三条未覆盖七类 | `AGENTS.md:81` 逐字「`[4]` 段的每条预警都已逐条判为…」 | **属实** |
| ⑤ | P2-2 §二 路由 8 类简写 vs 11 份完整列举 | 同格内两处列举不一致（前半括号 8 项、后半完整 11 份） | **属实** |
| ⑥ | P2-3 命名标准 §1 目录树产品层列举不合实际 | `命名标准.md:27` 该行注释**已含**「域件在子文件夹（…）」——评审所指缺口**并不存在** | **拒绝**（现状已覆盖；见 §六） |
| ⑦ | P2-4 §9.3「两行声明」未明示可否增行 | `skill形态标准.md:97` 仅写「阶段文件头部两行声明：消费点…／边界…」；而 `outline-stage-spec.md` 头部实为 **5 行**（含映射说明／建设状态通报引用／出处） | **属实** |
| ⑧ | P2-5 §1.1 表 [2] 行未提 ⑦段行校验 | `测试与验收标准.md:24` 的「管什么」列止于「目录形态」 | **属实** |
| ⑨ | P2-6 `SKILL.md` 用『建设状态』 | `skills/abzu-outline/SKILL.md:16` 逐字含「『建设状态』」；`文档写作标准` §一 第 6 条的引号三分只立 `「」`／`""`／代码惯例 | **属实**（但**本批不落**——见 §八 1） |
| ⑩ | 基线 | `check.sh` exit **0**（`共 6 段：已跑 6｜跳过 0`）／lint **68 件 0 issue**（含本规格）／链接 **176‧断链 0**／工作区 **2 行**（`094` 规格的「待填：真机走查」行 ＋ 本规格未跟踪） | 实测 |

**族级判断（采纳评审的族系分析）**：三项 P1 是同一族——**「下层守卫变更 → 上层文档回写」依赖人记，而该回写面不在任何机检锚点内**（`测试与验收标准` §1.1 的「段序变更时须同步本表」纪律只管段序、不管**检查项**）。族级处置＝立一条**回写清单**纪律句（F6）。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `AGENTS.md:80`（§七 第二条信号末句） | 「（**机检段 `[3]` 待建，尚无常驻闸**）」→ 「（**引用闭合的常驻检查现状见 `scripts/check.sh` 头注与 `测试与验收标准` §1.1**——候选类只呈报）」 | [A] | ① 宪法层事实性错误；**改为动态指认**（不硬编码守卫状态——同族风险的根治方向） |
| F2 | `AGENTS.md:81`（§七 第三条信号） | 「`[4]` 段的每条预警」→ 「**`[4]` 段的七类检查（A–G）的每条发现**」 | [A] | ④；与 §1.1 表的七类列举呼应 |
| F3 | `AGENTS.md` §二 路由表首行 | 「（命名、格式、skill 形态、产物契约、触发、测试、版本、规格写作）」→ **删该 8 项括注**，**其余逐字保留**（后半句的 11 份完整列举不动）——简写与完整列举同格并列即第二处易漂移的枚举 | [A] | ⑤；**改法是删简写而非补全**（11 项简写进同一括号会挤成一团，且简写本身就是漂移源） |
| F4 | `docs/standards/测试与验收标准.md` 的**两处**：`:26`（§1.1 [4] 行）＋ `:113`（§三 机检锚点行） | 两处的枚举均末补「／**自由度分布核对**」（`[4]` 段的枚举载体在本件共两处，**同批并改**——`089` F9 的同型先例即为两处并改；与 `check.sh` 头注 `:10`／`:137` 及 `check_spec.py` 的七类一致） | [A] | ②；`[4]` 段是审查交底「机器已覆盖面」的依据（`施工机制` §五），缺项会导致重复审或漏审 |
| F5 | `docs/standards/测试与验收标准.md` §4 的 **G22** 行 | **触发条件列**（该表三列为「编号｜缺口｜触发条件」，**无「现状」列**）末尾追加核销注：「**（093 F22 已实现；095 F4 同步 §1.1 表）**」 | [A] | ②；核销落点：G22 的处置已记「本批处置」但未记 §1.1 回写——本批补；成稿审查 P2-1（原写「现状列」与实表列名不符）。**落盘形态注**：该格内层引号块「编号｜缺口｜触发条件」以**全角 ｜** 表单元格分隔，系**表格形态占位**；实件按该表既有行风格落半角竖线（同 `090` F6 先例）——故 `[4]` 段的「[A] 未落实或已漂移: 编号｜缺口｜触发条件」属**预期**；**产物审查 P1-D**：原路径列写「同件 §4 的 G22 行」不解析为仓根路径 → 检查 A 中性汇总误计、检查 C 报「无核对面」、**该 [A] 行的逐字核对被静默跳过**（同族第 3 例：`093` F23／`094` F5／本行）→ 改**仓根全路径**（`069` 对 `068` F-02 的既有裁决形态）；**施工方 P0 呈报（断言 2-2 的 token 与 [A] 目标文本互斥）→ 规划方裁定「乙」**：去批次号反引号，按该表兄弟行（G16「075 F1⑤／076 F12」／G20／G21）的**裸形**取齐，断言 2-2 不动 |
| F6 | `docs/specs/施工机制.md` §四（机器闸） | 新增一条纪律句（**本批的结构性产出**）：「**守卫变更回写清单**：改 `check.sh` 段或 `check_*.py` 的检查项时，须**同批核三处**——① `测试与验收标准` §1.1 表（该段的「管什么」与「现状」——**本项的义务真源是该表 `:18` 的既有口径注「段序变更时须同步本表」，本条扩充其触发面为「检查项变更」并引之，不另立第二载体**）② `AGENTS.md` §七 自检信号（凡述及该守卫状态者）③ 工具自身的**头注 docstring**（与同件内联注释、正则实现三者一致）。触发条件：**任何检查项的新增／删除／改判据**。本清单不设守卫（回写面含语义判断，机械化成本高于收益）；漏回写按 P1 观察型处置（§六 分级）。」 | [A] | 族级处置（评审 §四 族系分析）；`AGENTS.md` §四「不做清单」的准入依据——**本条为纪律句、非新守卫** ✓ |
| F7 | `docs/standards/测试与验收标准.md:24`（§1.1 [2] 行） | 「管什么」列末补「（**⑦段行校验由收尾汇总统一处理**）」 | [A] | ⑧；与 `check.sh` `[2]` 段注释 `:45` 同口径 |
| F8 | `docs/standards/skill形态标准.md:97`（§9.3） | 「阶段文件头部两行声明：消费点…／边界…。」→ **该句句号之前**补「**（两行为必含项；可另加补充行——如映射说明、建设状态通报引用、出处）**」 | [A] | ⑦；`outline-stage-spec.md` 头部 5 行即该形态的实例；**产物审查 P2-7 订正**：原两对 `**` 被「；」切碎（渲染成两段残缺加粗）→ 整段括注一并加粗（另一路径「整体去粗」可一句话切回） |
| F9 | `CHANGELOG.md`（`### Changed` 段首） | 一行：『守卫回写（095）：…』 | [B] | 过程产物 |
| F10 | `scripts/check_content.py` 的**两处**：`:10`（模块 docstring 的 ⑤ 项）＋ `:227`（`_check_maint_mark()` 的函数 docstring——**同件第三载体**） | 「出现『见／按／引 ＋ 反引号治理件名』形态」→ 「出现『**见／按／引／依照／据** ＋ 反引号治理件名』形态」——**与同件内联注释 `:45` 及正则 `:46` 的五词同形**（**不收窄正则**：「依照」「据」在中文里确为等效引用动词） | [A] | ③；成稿审查 P1-2（原缺 F 行，致该断言的零命中核对被机检静默跳过）；**施工方 P1-1 呈报 → 规划方裁定：补 `:227`**（该处即反模式 #22「同载体多副本漏改」所指的第三载体；F10 只落 `:10` 时同件内 ⑤ 形态描述仍不齐）。**引号形态维持原文的「」**（规格 F10 行以『』引该句系规格侧笔误——施工方按最小改动保留「」，规划方认之） |
| F11 | `docs/specs/施工机制.md` §六 反模式清单：**回填 5 条 ＋ 表头注加一句** | ① 表头注（`> 格式：…` 那句之后）加上：「**每批复盘 findings 时须判「本批发现是否构成新族」**——构成即按本表格式回填（错误做法 → 正确方式 ＋ 事故出处），并同步评估可否机械化为零外部依赖断言（`AGENTS.md` §四 解冻句）；**只修实例不回填族＝本项失守**。」 ② 回填条目（接末号顺序，逐条含事故出处）：**#21 改一处、忘了它使自家断言／数字失效**（正确：改动落地后逐条重算受影响的断言与计数——含本批新增的样本串与新增链接数；事故：`092` 窄域 N-1、`092` 窄域复审·补修面 P1-1 链接数 175→176、`094` 成稿 P0-1 断言 4-1 期望 4→5——**三次同族**）｜**#22 同载体多副本漏改**（正确：改一处枚举／判据前先 `grep` 该形态的**全部载体**，同批并改；事故：`089` F9、`093` P1-3 九行状态收口、`095` 成稿 P1-1 同件第二处枚举／P1-2 改动清单漏 F 行）｜**#23 文档里的状态句与计数句随实现漂移**（正确：状态句**指真源**（不写死「待建／已实现」）、计数句随批重测；事故：`093` P1-5「20 件全部超阈」实为 31 件／`095` P1-1「`[3]` 待建」而 `[3]` 已实现）｜**#24 工具判据遇自家产物即失效**（正确：判据的样本来源与判据实现分离——转义行、括注复述旧值、嵌套引号都会让判据读错自家文本；事故：`093` 的 `\|` 盲区（检查 C 静默跳过含转义竖线的自家样本行）、`093`／`094` 批头注括注复述旧值被检查 G 读作声明值）｜**#25 自检与自评同源**（正确：**不得以自己写的检查断言自己的成品并据此宣布「无问题」**——汇报全绿须**声明已检范围**（既有条文明文，`施工机制` §四 边界声明），用语止于「已检 X／未检 Y」，不出现「没问题」类总评；事故：`092`–`095` 四批的独立上下文审查各挑出语义级问题（逐批可核）） | [A] | 作者 2026-09-15 会话内指出：第三方指正须落成可复用经验（否则同类在批间复发）；`AGENTS.md` §四 解冻句（同类复发 ＋ 可机械化）为机械化的准入依据 |
| F12 | `docs/standards/规格写作标准.md:87`（§4 的「无实例条文标预立法」条） | 句末补（**预立法的到期机制**，设计层评审建议 2 的采纳形态）：「**预立法标记须附「关联实例判据」**——即该条文一旦有实例时应出现的可判形态（如路径通配、字段名、串）；**判据写入该标记旁**。**每批核销时逐条过一遍预立法标记**：判据命中即**同批校准或删除该标记**（不得只留标记不校准）。**不采「超 N 批未校准自动升为待复核」**——那需要预立法登记台账与计数守卫，属**无事故出身的新机制**（`AGENTS.md` §四），且「N」无依据；本句为纪律句、不建台账。」 | [A] | 设计层评审 §三.6（预立法可能永不校准）；现状 **8 处**预立法标记（**产物审查 P1-C 按实测订正**：`文档写作标准:34`／`:55`、`命名标准:63`、`施工机制:44`、`skill形态标准:59`、`规格写作标准:87` 规则句自身、**`docs/product/volume/卷纲域设计.md:21`**、**`docs/product/write/正文域设计.md:21`**——原列的 `触发与交互标准:14`／`:41` **全文无「预立法」**，系误指「域外休眠」规则名；数目凑巧同为 8）——**本批不逐条校准**（那需逐条判「实例是否已现」，属勘察量级），**只立机制**；逐条复核入 §八 |
| F13 | `scripts/check.sh` 末尾的收尾行 | 「== 检查全绿 ==」→ 「== 检查全绿（已检：真目录／lint／skill 格式／内容轨／规格静态自检／脚本语法；候选类只呈报，见各段输出）==」 | [A] | 设计层评审建议 3（输出形态上阻断「全绿＝没问题」的误读——**人读输出、不读文档**）；与 `测试与验收标准` §1.4 边界声明同精神；**`fail` 分支的横幅不动**（未全绿时不列已检面，避免弱化告警）；**施工方 P1-4 呈报 → 规划方裁定：echo 串内不带 markdown 强调**（`**` 是文档侧强调，落进 `echo` 会在控制台打出一对星号）——**文档侧各处（F4／F5／F8／F11）的 `**` 保留**（markdown 强调，正当） |
| F14 | `docs/standards/测试与验收标准.md:87`（§4 的 **G23** 行） | 「（「见／按／引 ＋ 反引号治理件名」形态）」→ 「（「**见／按／引／依照／据** ＋ 反引号治理件名」形态）」——**五载体收口**（工具四处 `check_content.py:10`／`:45`／`:227`／正则 `:46` 已五词，唯该行仍三词）；**产物审查 P1-B**：本批改过同一文件同一节（G22 行）却未同批收口该行 | [A] | ③／反模式 #22（同载体多副本漏改）；`[4]` 段已把该形态报为「[A] 未落实或已漂移」，可作核对锚 |

**口径要点（易错）**：① **F1 是「改述为动态指认」、不是「改成另一个硬编码状态」**——不得写成「`[3]` 已实现」这类同样会漂移的陈述，须指向真源（`check.sh` 头注／§1.1 表）；② F3 **只删前半句括号**，后半句完整列举逐字保留；③ F6 新增句**插在 §四 现有 bullet 之后**（该节为 bullet 列表），**不得**另起小标题（§四 现有**四个** bullet（段序真源／何时必须跑／守卫纪律／边界声明），无子节）。 ⑥ **F11 #24 单元格内的竖线取转义形**（施工方 P1-3 呈报 → 规划方裁定维持）：`#24` 内述及转义竖线时以 `\|` 形落盘（**与反模式 #14「单元格内不得出现裸竖线」一致**）；规格 F11 原文「去转义…不得出现裸竖线」**自相抵**，以本条为准。

## 三、验收断言

### 断言 1｜宪法层回写（F1／F2／F3）

```bash
grep -c '待建，尚无常驻闸' AGENTS.md
```

期望 **0**；**预验基线 1**。

```bash
grep -c '引用闭合的常驻检查现状见' AGENTS.md
```

期望 **1**（**计行**——改纯真源指针后的新句；**施工方 P0 呈报 → 规划方裁定「甲」**：P2-3 的落盘使原 token「引用闭合已由 」失效，取新句形态，判别力仍在）；**预验基线 0**。

```bash
grep -c '七类检查（A–G）的每条发现' AGENTS.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c 'skill 形态、产物契约' AGENTS.md
```

期望 **0**（简写括号已删）；**预验基线 1**。

### 断言 2｜标准件回写（F4／F5／F7／F8）

```bash
grep -c '自由度分布核对' docs/standards/测试与验收标准.md
```

期望 **2**（**计行**——§1.1 [4] 行 ＋ §三 机检锚点行；成稿审查 P1-1 订正原只写一处）；**预验基线 0**（该 token 带「核对」二字，全件现 0 处——基线 1 的是不带「核对」的「自由度分布」，成稿审查已实测）。

```bash
grep -c '095 F4 同步 §1.1 表' docs/standards/测试与验收标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '⑦段行校验由收尾汇总统一处理' docs/standards/测试与验收标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '两行为必含项' docs/standards/skill形态标准.md
```

期望 **1**；**预验基线 0**。

### 断言 3｜族级纪律句（F6）

```bash
grep -c '守卫变更回写清单' docs/specs/施工机制.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '构成新族' docs/specs/施工机制.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '自检与自评同源' docs/specs/施工机制.md
```

期望 **1**；**预验基线 0**。

走查型：新句须**指到三处落点**（`测试与验收标准` §1.1 表／`AGENTS.md` §七 信号／工具头注 docstring）——由核验第 1 步逐读该句确认（**不作计数断言**：所指数串「`测试与验收标准`」在该件现有 5 处、计数无判别力；本断言的判别力由 3-1 的 `守卫变更回写清单` 0→1 承担）。

### 断言 4｜判据同步（P1-3 的第二断面）

```bash
grep -c '依照／据' scripts/check_content.py
```

期望 **3**（**计行**——`:10` 模块 docstring ＋ `:45` 内联注释 ＋ `:227` 函数 docstring **三处并含五词**；F10 扩为两处后）；**预验基线 1**（本批前仅 `:45` 含五词）。

```bash
grep -c '关联实例判据' docs/standards/规格写作标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '已检：真目录' scripts/check.sh
```

期望 **1**；**预验基线 0**。

### 断言 5｜门禁、链接与改动面

```bash
bash scripts/check.sh > /dev/null 2>&1; echo "exit=$?"
```

期望 **exit=0**；**预验基线 exit=0**。

```bash
npx markdownlint-cli2 "**/*.md" 2>&1 | tail -1
```

期望 **0 issues**；**预验基线 0 issue**。

```bash
git status --porcelain | wc -l
```

期望 **10**（**逐类列举**：`M` **9 件**＝`AGENTS.md`／`docs/specs/施工机制.md`／`docs/standards/测试与验收标准.md`／`docs/standards/skill形态标准.md`／`docs/standards/规格写作标准.md`／`CHANGELOG.md`／`scripts/check_content.py`／`scripts/check.sh`／**`docs/specs/094-2026-09-15-阶段一资产内容批.md`**（其 §九「待填：真机走查」行尚未提交——**与 `094` 在制状态交叠**）；**未跟踪 1 件**＝本规格）。**预验基线 2**（`094` 规格 1 行 ＋ 本规格未跟踪）。**施工方须现场逐行列举**并与上列逐名核对；**若不符即停摆申报**（不得自行改数字）。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **断链 0**（总数与基线 176 同；本批不新增链接）。**预验基线 176‧0**。

### 断言 6｜过程产物

```bash
grep -cF '守卫回写（095）' CHANGELOG.md
```

期望 **≥1**；**预验基线 0**；行长 ≤400 字符（`python len` 实测并记入核验结论）。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 十条全部实跑（①③ 四文件 `grep`／② 全件计数／④–⑨ 逐条 `grep`／⑩ 三命令）。
2. **断言实跑过（改前侧）**：断言 1（1／0／0／1）／2（0／0／0／0）／3（0；第二项改走查型）／4（1）／5（`exit=0`／0 issue／**porcelain 2**／176‧0）／6（0）逐条实跑。**预验期本规格自身经 1 轮 lint 订正**（MD060：§一 ③ 行引正则时含裸竖线与嵌套反引号 → 改述为「五词交替式」）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：断言 1 的四条期望均由「该串在本批新增／旧串离场」逐条对应；断言 5 的 porcelain 期望由**逐类列举**得出（并为 `094` 待填行的状态留了施工期实况判据——**本批与 `094` 收口存在交叠，须以实况列举为准**）；断言 4 的「≥2」由「docstring ＋ 内联注释两处并含」的裁定（§一 ③）得出。

## 五、禁止事项

- 禁止触碰：`skills/`（**全目录零改动**——F9 已移入 §八）、`docs/product/`、`docs/specs/archive/`、`scripts/check.sh`／`scripts/check_spec.py`／`scripts/patch_file.py`（本批只改 `check_content.py` 的 **docstring**，不改其判据实现）、`README.md`。
- **F1 不得写成另一个硬编码状态**（「已实现」之类同样会漂移）；**F3 只删前半句括号**；**F6 不得另起小标题**。
- **F4／F6 的落点若与 `094` 的收口动作（规格归档）交叠**，以**不破坏 `094` 在制状态**为准（`094` 未归档前，其规格仍留在 `docs/specs` 目录）。
- 不得改断言、改数字、删检查来变绿。禁止动作：任何 git 写命令；联网；安装依赖。

## 六、被否决／拒绝的条目（逐条标注，评审纪律）

| 评审条目 | 裁定 | 理由 |
| --- | --- | --- |
| P1-1 §七 `[3]` 待建 | **采纳** | F1（改动态指认） |
| P1-2 §1.1 [4] 行缺 G | **采纳** | F4＋F5 |
| P1-3 头注与正则不同步 | **采纳（改头注）** | F→ 断言 4；**病因按我方复核更精确**：不是头注与正则不一致，而是**头注与同件内联注释不一致**（注释 `:45` 已列五词）——故改法是**补 docstring 至五词**，**不收窄正则**（「依照／据」在中文里确为等效引用动词，收窄会把合法形态判成无标注） |
| P2-1 §七 第三条措辞 | **采纳** | F2 |
| P2-2 §二 路由 8 vs 11 | **采纳（删简写）** | F3；删而非补——简写本身即第二处漂移源 |
| P2-3 命名标准 §1 目录树列举 | **拒绝** | **§一 ⑥ 实测**：`命名标准.md:27` 该行注释已含「域件在子文件夹（…）」——评审所指缺口不存在（该行亦已注明共享件在根） |
| P2-4 §9.3 两行声明 | **采纳** | F8 |
| P2-5 §1.1 [2] 行 ⑦项 | **采纳** | F7 |
| P2-6 『建设状态』引号 | **采纳但缓落** | **§八 1**：该条动 `skills/` 资产、而 `094` 的真机走查**尚未执行**——若此刻改 `SKILL.md`，走查测到的版本将与本批 commit 不一致；**触发＝`094` 走查收口后**（改动面仅 1 字符，随下次触碰 `skills/` 一并落） |
| 族级建议（回写清单） | **采纳** | F6（本批结构性产出）；**不新增守卫**（评审同judgement：回写面含语义判断） |

**被否决的替代方案**（`规格写作标准` §10 的落点）：① **F1 写成「`[3]` 已实现」** → 否决：那是另一个会漂移的硬编码状态，须指向真源；② **F3 补全 8 项简写为 11 项** → 否决：11 项挤进同一括号既难读，且简写本身即漂移源（本批的族级病因）；③ **为回写清单新建守卫** → 否决：回写面含语义判断，机械化成本高于收益（评审同判）；纪律句即可，符合「事故驱动」准入；④ **P2-6 的引号本批即落** → 否决：会扰动 `094` 待执行的真机走查版本（缓落，见 §八 1）。

## 七、本批核销（094 §八 全部 7 条；以本批规格写作时点为准）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 094 §八 1 | `assets/` 无状态表示例（触发＝走查观察到形态偏差） | **仍待** | `094` 走查未执行，触发未到 |
| 094 §八 2 | `SKILL.md:31` 与 `outline-workflow.md:18` 的义务句重复 | **仍待** | 触发＝任一义务句变更时一并收口 |
| 094 §八 3 | `088` 施工记录的行数误记（52 实为 56） | **仍待** | 归档件只读，不回填（登记即可） |
| 094 §八 4 | 残余的批次号型标记（`SKILL.md:24`／`outline-emotion-words.md:3`） | **仍待** | 触发＝下一次触碰 `skills/` 正文 |
| 094 §八 5 | `:7` 的差异清单未含本批增补 | **仍待** | 触发＝下一次触碰该件 |
| 094 §八 6 | `命名标准` §5 末句类目名与实物脱节 | **仍待** | 触发＝下次触碰 `命名标准` §5 |
| 094 §八 7 | 「随对应阶段批」与「随各自阶段批」两措辞并存 | **仍待** | 触发＝下一次触碰 `skills/` 正文 |

- **必列面**：① 前一批（`094`）§八 全部开放项（上表 7 条）——**但 `094` 尚未收口**（真机走查待执行、规格未归档）；故本表按其**写作时点**的 §八 逐条核销，**`094` 收口后若有新增开放项，由下批核销**；② 本批改动面直接触及者（`AGENTS.md` §七／§二、`测试与验收标准` §1.1／§4、`check_content.py` 头注、`施工机制` §四、`skill形态标准` §9.3）——均无对应开放项待销。
- 汇总（恒列五档、无该档计 0）：`已履行 0｜作废 0｜本批处置 0｜仍待 7｜待作者 0 ＝ 7` ✓

## 八、开放项

1. **`SKILL.md:16` 的『建设状态』→「建设状态」**（评审 P2-6，缓落）：触发＝**`094` 真机走查收口后**（本批不动 `skills/`，以免走查版本与提交不一致）；改动面＝**该形态 2 处 ×2 字符**（`skills/abzu-outline/SKILL.md:16` ＋ `references/outline-stage-spec.md:6`——**成稿审查 P2-4 订正原记 1 处**）。
2. **回写清单的后续维护**（F6 的配套）：若今后出现「同层两处判据不同步」（如 `check_content.py` 的头注与内联注释这类**同件内**双载体），是否把 F6 的第③项细化为「同件内所有述及判据的载体」——触发＝该形态再现。
3. **`094` 的收口动作**（不在本批面内）：真机走查（作者）→ 走查记录回填 §九 → 规格移入 `archive/`；`094` §八 5／6／7 三条触发均等「下一次触碰 `skills/` 正文」。
4. **预立法标记的逐条校准**（产物审查 P1-C）：实测 **8 处**标记**均无「关联实例判据」**，而 F12 新增的要求即刻对现存 7 处（除规则句自身）构成相抵——触发＝**下一批**逐条过一遍（判「仍适用／可删／须补判据」），本批只立机制。
5. **`check.sh` 收尾行是静态清单**（产物审查 P2-1）：该串为字面量、不读 `seg_note_rt`，故当某段跳过时仍声明「已检」该面（过度声明）——与同件注释「不得写死机器状态」同型；触发＝**出现段跳过而收尾行仍称已检**的实撞，或下一批顺手改为读段状态。

## 九、审查记录

（待填：成稿审查 → 施工 → 核验三步 → 双维产物审查 → 归档 → 呈作者确认提交）
