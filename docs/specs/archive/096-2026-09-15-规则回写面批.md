# 096-2026-09-15-规则回写面批

> 定位：处置三份评审的公共发现——**「新规立完、其射程面没被回扫」这一族**：① `AGENTS.md` §六.3 的一处语义互斥（作者 2026-09-15 裁定「意图 A」）；② `施工机制` §四 的回写清单**只覆盖守卫变更、不覆盖规则变更**（三份评审独立同提）；③ `规格写作标准` §8 立的「预立法须附关联实例判据」**自身落地为零**（全仓 8 处预立法标记无一有判据）＋ 其执行面未定义；④ 反模式 #9 未回填本轮两笔自有事故。
> 路径判定（`规格写作标准` §1／`施工机制` §二）：**标准路径**——外审驱动（判定表该行：✅ 报告转附件、逐条标注采纳／拒绝、**禁止拿到外审直接改文件**）＋ 改规范条文（含宪法件 `AGENTS.md`，其改动经作者 §六.1 确认）＋ 跨 5 件。**真机走查豁免**（纯治理／流程件，不改用户可观测行为）。**双维产物审查**（改规则）。
> 自由度分布：[A]×4 [B]×2
> 必读输入：本规格 §一；`AGENTS.md` §六（`:68`–`:74`）；`docs/specs/施工机制.md` §四（`:84`–`:90`）／§六 反模式清单（表头注 ＋ `#9` 行 ＋ `#25` 行）；`docs/standards/规格写作标准.md` §8（预立法判据条，约 `:87`）／§4 附加项；8 处预立法标记所在件（本规格 §二 F4 逐条列出）
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（现场核过）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `AGENTS.md` §六.3 为**一整行**（`:73`，LF／无 BOM），尾句为「…不得抵触 §一 红线／§四 不做清单／§五 分工原则；**冲突时按 `施工机制` §六 升级**，升级结果同样不得违反宪法级约束。」 | `grep` 三串各 1 次（`从其指定`／`冲突时按`／`升级结果同样不得违反宪法级约束`）；逐字读 `:73` |
| ② | 「从其指定」（常规取用）与「冲突时升级」（无答案）**在同一分号两侧方向相反**，字面可读成互斥 | 逐字读；评审 P2-1 同判 |
| ③ | `施工机制` §四 现为**四条 bullet ＋ 一条**（095 批新增的「守卫变更回写清单」）＝ 五条；回写清单的触发面写「改 `check.sh` 段或 `check_*.py` 的**检查项**」——**不含「改任一标准件的硬约束条文」** | 读 §四 全文 |
| ④ | 该清单的「同批核**三处**」为 ① §1.1 表 ② §七 ③ 工具头注 docstring——**`check.sh` 头注段行**（段序真源）未列，读者按字面会漏 | 同前；评审 P2-B 复现两轮 |
| ⑤ | `规格写作标准` §8（095 批 F12 落）已立「预立法标记**须附**关联实例判据…**每批核销时逐条过一遍**」——但**全仓 8 处预立法标记无一附判据**，且「每批核销」的**执行面未定义**（全仓扫 or 只核本批触及？） | 评审 P1-A／P1-C 同判；8 处清单见 F4 |
| ⑥ | 反模式清单**末行仍 #25**；`#9`（纪律靠自觉不靠机制）的事故出处仍为「2026-09-06 全天多次…」，**未含本轮两笔自有事故**（`规格写作标准` §8 新规未回扫存量、`施工机制` §八 补丁未回填族） | 读 §六 清单 |
| ⑦ | `施工机制` §六 表头注（095 批落）已含「每批复盘 findings 时须判『本批发现是否构成新族』…**只修实例不回填族＝本项失守**」——**本批事故即违此句** | 读表头注 |
| ⑧ | 基线：`check.sh` exit **0**（`共 6 段：已跑 6｜跳过 0`）／lint **69 件 0 issue**（**含本规格**；不含则 68）／链接 **176‧断链 0**／工作区 **1 行**（**仅本规格未跟踪**） | 三命令实跑 |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `AGENTS.md:73`（§六.3 尾句） | 「…；**冲突时按 `施工机制` §六 升级**，升级结果同样不得违反宪法级约束。」→「；该指定本身若与宪法级约束相抵，或两处指定互相相抵，按 `施工机制` §六 升级——升级结果同样不得违反宪法级约束。」（**只动这一分句**，该行其余逐字保留；**落盘不加粗**——`AGENTS.md` §六 通篇无加粗体例，此为 `074` F1 明记的体例决定） | [A] | **作者 2026-09-15 会话内裁定「意图 A」**＋评审 P2-1；消歧依据：条款先立「该指定只在宪法级约束未覆盖的范围内有效」＝**边界**，后句管**越界**；① 指定越界 ② 两处指定互抵（例外条自身判不了，层级序又被本条排除 → 唯一出口是升级） |
| F2 | `docs/specs/施工机制.md` §四 回写清单 | ① **明写四处、其余三处括注逐字保留**：现「须同批核三处——① … ② … ③ 工具自身的头注 docstring」→「须**同批核四处**——① `测试与验收标准` §1.1 表 ② `AGENTS.md` §七 自检信号 ③ **`check.sh` 头注的段行与段名**（段序真源）④ 工具自身的**模块 docstring**」——**改后落文须与现文逐字对齐**：① 的括注（「该段的「管什么」与「现状」…不另立第二载体」）、② 的括注（「凡述及该守卫状态者」）、③ 的原括注（「与同件内联注释、正则实现三者一致」）**逐字保留**（[A] 引文不覆盖括注，落文以现文为准） ② **并列增一条**：「**规则新增回写清单**：新增／修改任一**硬约束类条文**（`须`／`不得`／`禁止`级）时，须同批核**该条文的射程面**（**由条文自身描述限定**——如新增反模式行须核该族全部入口是否已回填；新增标准件条文须核该标准件内射程面；新增术语规则须核术语引用面）。**发现射程面内有不满足者，同批补齐**；**本句为回写纪律的最后一层**——不得再为「回写纪律本身」立回写规则（续立即违本句），其失效由**独立上下文审查**发现。（**正例**：本条自身即 F5 `#9` 事故的直接产物；**反例**：`095` 批 `规格写作标准` §8 立规未回扫存量——见 `施工机制` §六 `#9` 行事故出处）」 | [A] | 评审（三份独立同提）＋ `AGENTS.md` §四 准入（纪律句非机制）；**终止句**为本批新增（防无限递归：承认最后一层由审查兜底） |
| F3 | `docs/standards/规格写作标准.md` §8（预立法判据条） | **改写旧句 ＋ 补执行面**（`规格写作标准` §5.5：新文本使旧文本失效即**改写**）：① 旧句「每批核销时逐条过一遍预立法标记」→「每批核销时逐条过一遍**本批改动面触及的**预立法标记」 ② 句末补：「**执行面**：① 本批改动面**触及**的标准件内的预立法标记必核；② 改动面**未触及**的存量标记，**由任一后续批次认领清理**（**本条的存量 7 处已于 `096` 批一次清账**）——**不得以「本批未触及」为由长期搁置**。」 | [A] | 评审 P1-C（执行面未定义）；**采纳其方案 B ＋ 规划方加「不得长期搁置」兜底** |
| F4 | **7 处预立法标记**（逐条补「关联实例判据」；**写入形态统一为**「（预立法，随首批实例校准；**关联实例判据：…**）」——**判据字面须含「关联实例判据」五字**，供断言 3-1 计数） | ① `docs/standards/skill形态标准.md:59`（目标 <300 行）→ 判据「**首个接近 280 行的 `SKILL.md` 出现**即校准」（**非**「已达 280」——现仓唯一 `SKILL.md` 仅 37 行，该条仍是休眠态 ✓）　② `docs/standards/文档写作标准.md:34`（移植豁免休眠）→ 「**文件头两行含来源标注「移植自」**者出现即校准」（**不得**用 `grep -rl '移植自'` 全域计数——规则句自身与本规格引文即命中，判据恒真 ✓）　③ 同件 `:55`（符号型状态清单尚未建立）→ 观测面＝「**该件机检锚点表「状态」列出现符号型取值**」（如 ✅／🚧 类）　④ `docs/standards/命名标准.md:63`（开发测试类目预留）→ 「`ls scripts/test-*` 命中 ≥1」　⑤ `docs/standards/规格写作标准.md:87` —— **非标记**（该行是**定义「预立法标记」的规则句**，不属标记本体）→ **不计入本批清账**（`095` F12 的清单口径把该定义句计为第 8 处，本批订正为 **7 处标记**）　⑥ `docs/specs/施工机制.md:44`（单维核对尚无实例）→ 「出现首批产品层纯文档内容批」　⑦ `docs/product/volume/卷纲域设计.md:21`　⑧ `docs/product/write/正文域设计.md:21`（清单其余条目待该域建设时补）→ 「该域建设批落地」 | [B] | 评审 P1-A（新规自身落地为零）；**判据措辞按方向自定**（形态＝可判的具体信号，不写成散文） |
| F5 | `docs/specs/施工机制.md` §六 反模式清单 `#9` 行 | 事故出处列末追加：「**＋ 2026-09-15 两笔自有事故**：`规格写作标准` §8 新规未回扫存量预立法标记（8 处＝`095` 口径含定义句；`096` 订正为 **7 处标记**）、`施工机制` §八 交底装载路径补丁未回填族——**均为 `#9` 的实例化**」 | [A] | 评审 P1-A 的方案 A（**并入 #9，不新立 #26**——#9 已是该族，另立即同族拆行）；依 `AGENTS.md` §四 反膨胀倾向 |
| F6 | `CHANGELOG.md`（`### Changed` 段首） | 一行：『规则回写面（096）：…』 | [B] | 过程产物 |

**口径要点（易错）**：① **F1 只动 `AGENTS.md:73` 的那一分句**，该行 §六.3 其余（含三处指定举例、`不得抵触…` 三串）**逐字保留**；**不得**顺手改宪法件其他任何字（作者确认范围仅此一分句）。② **F2 是把现有「三处」改「四处」＋ 并列新增一条**——**不是**替换现有清单（守卫变更那条**保留**）。③ **F4 是 8 处逐条就地补判据**：判据写在**该标记旁**（同一括号内或紧随半句），**不得**另立清单件（`AGENTS.md` §四 不做平行台账）。④ §五 的「禁止触碰」含 `skills/` 全目录 ✓。

## 三、验收断言

### 断言 1｜§六.3 消歧（F1）

```bash
grep -c '或两处指定互相相抵' AGENTS.md

```

期望 **1**；**预验基线 0**。

```bash
grep -c '冲突时按' AGENTS.md

```

期望 **0**（旧措辞离场）；**预验基线 1**。

### 断言 2｜回写清单扩面（F2）

```bash
grep -c '规则新增回写清单' docs/specs/施工机制.md

```

期望 **1**；**预验基线 0**。

```bash
grep -c '本句为回写纪律的最后一层' docs/specs/施工机制.md

```

期望 **1**；**预验基线 0**。

```bash
grep -c '`check.sh` 头注的段行与段名' docs/specs/施工机制.md

```

期望 **1**；**预验基线 0**。

### 断言 3｜预立法判据与执行面（F3／F4）

```bash
grep -rc '关联实例判据' docs/standards/*.md docs/specs/施工机制.md docs/product/volume/卷纲域设计.md docs/product/write/正文域设计.md | grep -v ':0$' | wc -l

```

期望 **7**（**计文件数**——6 件标记所在件（`skill形态标准`／`文档写作标准`／`命名标准`／`施工机制`／`卷纲域设计`／`正文域设计`）＋ `规格写作标准` 的定义句件）；**预验基线 1**（095 批落规则句的 `规格写作标准`）。

```bash
grep -c '执行面' docs/standards/规格写作标准.md

```

期望 **≥1**；**预验基线 0**。

### 断言 4｜反模式回填（F5）

```bash
grep -c '均为 `#9` 的实例化' docs/specs/施工机制.md

```

期望 **1**；**预验基线 0**。

```bash
grep -c '^| 26 |' docs/specs/施工机制.md

```

期望 **0**（**不新立 #26**——并入 #9 是本批裁定）；**预验基线 0**。

### 断言 5｜门禁、链接与改动面

```bash
bash scripts/check.sh > /dev/null 2>&1; echo "exit=$?"

```

期望 **exit=0**；**预验基线 exit=0**。

```bash
npx markdownlint-cli2 "**/*.md" 2>&1 | tail -1

```

期望 **0 issues**；**预验基线 0 issue**。

```bash
git status --porcelain | wc -l

```

期望 **exit=0**／**0 issues**／porcelain **10**（**逐类列举**：`M` **9 件**＝8 件改动面（`AGENTS.md`／`docs/specs/施工机制.md`／`docs/standards/skill形态标准.md`／`docs/standards/文档写作标准.md`／`docs/standards/命名标准.md`／`docs/standards/规格写作标准.md`／`docs/product/volume/卷纲域设计.md`／`docs/product/write/正文域设计.md`）＋ `CHANGELOG.md`；**未跟踪 1 件**＝本规格）。**预验基线 1**（仅本规格未跟踪）。**施工方须现场逐行列举**并与上列逐名核对；**若不符即停摆申报**。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(len(L),sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"

```

期望 **断链 0**（总数与基线 176 同；本批不新增链接）。**预验基线 176‧0**。

### 断言 6｜过程产物

```bash
grep -cF '规则回写面（096）' CHANGELOG.md

```

期望 **≥1**；**预验基线 0**；行长 ≤400 字符（`python len` 实测并记入核验结论）。

过程产物第二项：**验收通过后本规格移入 `docs/specs/archive/`**（移动不留副本）——走查型，归档时核。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 八条（① `:73` 逐字 ＋ 三串各 1 次／③④ 读 §四 全文／⑤⑥⑦ 读 §六＋`规格写作标准` §8／⑧ 三命令）。
2. **断言实跑过（改前侧）**：断言 1（0／1）／2（0／0／0）／3（1 件／0）／4（0／0）／5（`exit=0`／0 issue／porcelain **1**／176‧0）／6（0）逐条实跑。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：断言 5 的 porcelain 期望由**逐类列举**得出（8 ＋ 1 ＋ 1）；断言 3 第一条的「≥6 件」由 F4 的 8 处**逐件列举**得出（减 `规格写作标准` 规则句自身、减可能同件两处的合并计数口径——**施工方按实跑值记入核验结论，若与 ≥6 不符须停摆申报**）。

## 五、禁止事项

- 禁止触碰：`skills/`（全目录）、`docs/specs/archive/`、`README.md`、`scripts/`（本批**不动任何守卫与工具**）。
- **F1 只改那一分句**（宪法件的确认范围仅此）；**F2 不得替换**现有守卫变更清单；**F4 不得另立清单件**。
- 不得改断言、改数字、删检查来变绿。禁止动作：任何 git 写命令；联网；安装依赖。

## 五b、评审 findings ↔ 采纳／拒绝／落位（外审驱动批的强制对照表）

| 来源＋finding | 裁定 | 落位 |
| --- | --- | --- |
| `AGENTS.md` 单件 P2-1（§六.3 语义互斥） | 采纳「意图 A」 | **F1**（作者会话内裁定） |
| `施工机制` 单件 P1-A（新规未回填族）＋ 三份评审同提（回写清单扩面） | 采纳 | **F2**（扩四处＋规则新增回写＋终止句）／**F5**（并入 `#9`，不新立 #26） |
| 新规落地检查 P1-A（存量无判据） | 采纳（口径订正为 7 处标记） | **F4** |
| 同批 P1-C（执行面未定义） | 采纳方案 B ＋ 加「不得长期搁置」 | **F3** |
| 同批 P2-B（「三处」歧义） | 采纳（改四处） | **F2①** |
| 守卫工具层 P1-1／P2-1／P2-2；`skill形态标准` 5 条；`AGENTS.md` 其余 3 条 | **拒绝本批落位（转 `097`）** | `097`（本 §八 3 已登记） |

- **F5 新立 #26**（「规则条文已立但执行入口从未点名」）→ 否决：#9 已是该族（纪律靠自觉不靠机制），另立即同族拆两行，违 `AGENTS.md` §四 反膨胀倾向（评审同判，倾向 A）。
- **F3 采「每批全仓扫预立法标记」** → 否决：每批固定成本，且属预防性动作；改为「触及必核 ＋ 存量不得长期搁置」＋ **本批一次清账**（8 处清完，债即消）。
- **F2 只写触发面、不写终止句** → 否决：照「每上一级回写面就多一层」的逻辑推，通用句自己也需要回写句（无限递归）→ 明写**最后一层**、由独立审查兜底。
- **F1 取意图 B（「冲突」泛指一切分歧）** → 否决：会使「从其指定」成为空头承诺（指定了还要每次升级），且无法解释末句为何要约束「升级结果」——**作者裁定意图 A**。

## 七、本批核销（095 §八 全部 ＋ 094 §八 相关条）

**必列面**：① 前一批（`095`）§八 全部开放项；② 本批改动面直接触及者。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 095 §八 1 | `SKILL.md:16` 的『建设状态』→「建设状态」（缓落：不扰动 `094` 走查版本） | **仍待** | `094` 走查仍未执行（本批不动 `skills/`） |
| 095 §八 2 | 回写清单的后续维护（同件内双载体是否纳入 F6 ③） | **仍待（转 `097`）** | **F2① 明写四处但不动同件双载体条**（其 ③ 的「与同件内联注释、正则实现三者一致」括注**逐字保留** ✓）；「同件内双载体」面**转 `097`** 处置 |
| 095 §八 3 | `094` 的收口动作（走查 → 记录 → 归档） | **仍待** | 作者执行走查后由 `094` 收口批处理 |
| 095 §八 4 | 预立法标记的逐条校准（8 处均无判据） | **本批处置** | **F3＋F4**（执行面 ＋ 8 处一次清账） |
| 095 §八 5 | `check.sh` 收尾行是静态清单（段跳过时过度声明） | **仍待** | 触发＝出现「段跳过而收尾行仍称已检」的实撞 |
| 094 §八 全部 7 条 | —（**见 `095` §七 表索引**——该表已逐条列出 094 §八 1–7 并皆标「仍待」；本节不重列，免两处口径） | **仍待** | 触发均未到（走查未执行／未再触碰 `skills/` 正文） |

- 汇总（恒列五档、无该档计 0；**按条计**）：`已履行 0｜作废 0｜本批处置 2｜仍待 4｜待作者 0 ＝ 6` ✓（`094` 的 7 条经索引指针整行承载，计 1 条。**未触碰 `命名标准` §5，故不销 `094` §八 6**）

## 八、开放项

1. **`AGENTS.md` 的「形态自审」**（评审观察）：头注列举与节序一致／四条信号平行／括注方向单一／红线自测句齐——**触发＝宪法层出现形态类事故**（届时按事故驱动加「形态自审四条」）。
2. **交底装载路径的触发面**（评审观察）：本轮补丁只覆盖 `skills/` 资产正文；产品层／治理文档批次的同型问题**触发＝另两类资产出现同型事故**。
3. **回写清单的执行面扩展**（评审观察）：跨工具同概念常量（`MD_LINK` 等）与「改标准条文」两面的回写在 F2 中已部分覆盖；**具体「跨工具同概念」是否单列一项 → 归 `097`**（本批不扩）。

## 九、审查记录

（待填：成稿审查 → 施工 → 核验三步 → 双维产物审查 → 归档 → 呈作者确认提交）
