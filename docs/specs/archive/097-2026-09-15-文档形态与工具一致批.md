# 097-2026-09-15-文档形态与工具一致批

> 定位：处置六份评审中**尚未落位**的 findings——① `skill形态标准` 的形态不齐四处（批次叙事／过程数字／机检列口径／[3] 段头注缺项）；② `AGENTS.md` 六处（头注列举不全／§七 主体句与注反向／四信号不平行／§四 末句并列／红线 1 括注方向叠加／红线 4 句式歧义）；③ 工具层三处（`MD_LINK` 双定义／`set -u` 哨兵契约未声明／CHANGELOG 条目判据隐性化）；④ `096` 转办五条（③④边界／执行面认领落点／「四处」未含两载体／判据措辞／**`G22` 族回填**）；⑤ 归档件 `070-6` 的核销与**假前提订正**；⑥ 「预留 vs 预立法」分野句。
> 路径判定（`规格写作标准` §1／`施工机制` §二）：**标准路径**——外审驱动（判定表该行：✅ 报告转附件、逐条标注采纳／拒绝、**禁止拿到外审直接改文件**）＋ 改规范条文（含宪法件 `AGENTS.md`，其改动经作者 §六.1 会话内确认）＋ 跨 8 件。**真机走查豁免**（纯治理／工具件，不改用户可观测行为）。**双维产物审查**（改规则 ＋ 改守卫实现）。
> 自由度分布：[A]×18 [B]×4
> 必读输入：本规格 §一（全部锚点）；`docs/standards/skill形态标准.md` §1／§3／§13；`AGENTS.md` 头注／§一 红线 1・4／§四／§七／§六；`scripts/check_content.py:42`；`scripts/check_spec.py:225`；`scripts/check.sh` 头注（`:2`–`:11`）；`docs/standards/版本与分发标准.md` §3（`:48`）；`docs/specs/施工机制.md` §四（回写清单）／§六（反模式清单表头注与 `#21`／`#22` 行）／§八（交底七项之 6）；`docs/specs/archive/071-*.md:178`／`archive/074-*.md:146`；`docs/standards/命名标准.md` §1（`:47` 现状与预留段）
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `skill形态标准:21`（§1 现状段尾）含批次叙事：「…**原「三件」表述与本标准机检锚点的「三件齐」断言一并废止**——「三件」含「可选项必须存在」之歧义。」 | 逐字读 `:21`；违 `规格写作标准` §9（治理文档不写批次叙事） |
| ② | 同件 `:46`（§3）含过程数字：「同批实测中技能附件计数 **13→14** 的增量只来自未带该键的探针」 | 逐字读 `:46`；同段「宿主 Claude Code 2.1.270｜2026-09-15｜两判据均成立」属**可核判定事实**（保留） |
| ③ | 同件 §13 机检列 **5 格**为「已接入 ＋ 仍待建」两态并存（`:141`／`:143`／`:145`／`:147`／`:177`） | `grep -c '已接入'` ＝ **5**；而 §三 机检锚点表状态列为**单值**（已实现／待建／无守卫） |
| ④ | `scripts/check.sh:9`（[3] 段头注）列四项（引用闭合／TOC／行数与条目限长／「维护出处」标注）——**未提「加粗密度不实现」** | 逐字读 `:9`；工具 docstring 列**五项**（①②③④⑤，**不含**加粗密度）——加粗密度是紧随的**分列「不实现」声明** |
| ⑤ | `AGENTS.md` 头注列举 **4 项**（项目一句／红线／去哪读／常用命令），而文件有 **7 节** | 逐字读头注；`grep -c '^## '` ＝ 7（漏 §四 不做清单／§五 分工原则／§六 权威顺序／§七 自检信号） |
| ⑥ | `AGENTS.md:80`（§七 第二条）主体写「链接若被改动，**断链按当批规格的链接命令现场核**」，同句括号注却指「常驻检查现状见…候选类只呈报」——**主体与注反向** | 逐字读 `:80` |
| ⑦ | 同件 `:79`–`:82` §七 **四条**信号形态**不平行**（第二条夹长括号注，实测四条字符数 **53／141／127／71**——第二条对第 1 条 **2.66×**、对第 3 条仅 **1.11×**（**原「约两倍长」表述不实，按实测订正**）） | 逐行读四条 |
| ⑧ | 同件 `:61`（§四 末句）：「…先修订本节并取得作者确认；**解冻一律由作者裁决并记入 `CHANGELOG`**」——分号并列，后句实为前句的**具体化** | 逐字读 `:61` |
| ⑨ | 同件 `:13`（红线 1 括注）三段方向叠加：正面列举（改 `.git/` 或工作区文件）＋ 并入（`fetch` 等）＋ **否定例外**（只读命令的索引缓存刷新不计） | 逐字读 `:13` |
| ⑩ | 同件 `:16`（红线 4）「…不在此禁列」句式歧义（读法 A 该程序为例外允许／B 归其他条款管辖） | 逐字读 `:16` |
| ⑪ | `MD_LINK` **同概念双定义**：`check_content.py:42` 为短形（不认 title 链接），`check_spec.py:225` 为 078 F2 的认 title 形 | 两行逐字；**语料带 title 的相对链接实测 3 条**（全在 `docs/specs/archive/078-*.md` 的 `:16`／`:30` **示例串文本**内，非真链接）——**成稿审查 P1-1 订正**：规划方原写「0 条」系转抄外审转述、未自跑 |
| ⑫ | `check.sh` 的 `_seg_summary` 以**恰为 `set -u` 的行**作头注扫描终止哨兵，而该契约仅在**函数内注释**里；头注未声明 | 读 `_seg_summary` 与头注 `:2`–`:11` |
| ⑬ | `版本与分发标准:48`（§3 第 2 步）只写 CHANGELOG 定稿动作——「条目**列 0、以 `-` 加空格起首**」这条隐性假设**未成文** | 逐字读 `:48`；`check_content.py` 用严格判据（守卫）／`check_spec.py` 用宽松判据（核验） |
| ⑭ | `施工机制` §四 回写清单 ③④ 边界待收窄：③「`check.sh` 头注的段行与段名」未含**用法／依赖与前置行**，④「工具自身的模块 docstring」未写明对象是 `check_*.py` | 逐字读该 bullet |
| ⑮ | `规格写作标准:87` 执行面 ②「由任一后续批次认领清理」**未绑定认领落点**（`施工机制` §三 要求核销落「本批核销」节；`AGENTS.md` §五.3 文件账本律） | 逐字读 `:87` |
| ⑯ | 「四处」**未含**两个同样承载守卫状态的载体：`测试与验收标准` §三 机检锚点表状态列（`:107` 表头「条文｜强制方式｜状态」）与同件 §4 缺口表（G1–G25） | 逐行读两处；095／096 两批的 P1 均出在这两处 |
| ⑰ | 反模式清单 **`G22` 族无对应行**（`施工机制` §六 反模式段内 `G22`／`自由度` 计数 ＝ 0），而 `测试与验收标准` §4 的 `G22` 行明文记「**第五次**复发已到」 | `grep` 计数；同表 `#21`／`#22` 已有三次同族事故记录 |
| ⑱ | 归档 `070-6` 现存两行（`archive/071:178`／`archive/074:146`），且 `074` 那行的**前提记载有误**：写「070 只举『术语叫法』『域清单』两…」——逐提交回放证 `070` 原文**即三例**（`协作与流程类以 施工机制 为准` 自 `070` 起已在） | 读两行；评审方逐提交回放（9 个改动提交全测） |
| ⑲ | `命名标准:47`（§1 现状与预留段）已有「下表 skill 资产类目与插件清单类目为**预留**——规则先立，首个域落地时按本表命名」，但**未与「预立法」作分野**；`命名标准:63` 开发测试行却带预立法标记＋判据 | 逐字读 `:47`；`grep -c '（预留）'` ＝ 5 |
| ⑳ | 基线：`check.sh` exit **0**（`共 6 段：已跑 6｜跳过 0`）／lint **69 件 0 issue**／链接 **176‧断链 0**／工作区 **1 行**（仅 `094` 规格） | 三命令实跑 |

**口径裁定（规划方，2026-09-15；承 P2-9）**：**「预留」不属 `规格写作标准` §8 预立法标记的辖域**——分野在**判据／对象／触发**三面：预立法＝「条文有效、缺**实例**来校准」（配「随首批实例校准」＝**校准承诺**，触发＝首个实例出现，到点**删标记**）；预留＝「该类目／位置**尚未启用**」（配「**启用前不得新增实例**」＝**启用门控**，触发＝某**事件**：T1 触发／首个发布批／user-invoked 层启用，到点**启用该位置**）。故 096 的「一次清账（7 处）」句**未过实**，P2-9 维持 P2，修法为**补一句分野**（F20），**不补预留类目的判据**（实测 `grep -c '（预留）' docs/standards/命名标准.md` ＝ **5**，另 `:63` 的「预留类目」1 处 ＝ **6 处**预留形态）。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `docs/standards/skill形态标准.md:21`（§1 现状段尾句） | 「…。**原「三件」表述与本标准机检锚点的「三件齐」断言一并废止**——「三件」含「可选项必须存在」之歧义。」→ 「…。**目录形态**：`SKILL.md` 必需；`references`／`assets`／`scripts` **三可选、按需建**——三者不必齐备。」（**状态陈述取代历史陈述**，规则实质不变） | [A] | ①；`规格写作标准` §9（治理文档不写批次叙事，沿革记 `CHANGELOG` 与 git 历史） |
| F2 | 同件 `:46`（§3） | 「…同批实测中技能附件计数 **13→14** 的增量只来自未带该键的探针」→「…同批实测中技能附件计数的**增量只来自未带该键的探针**」（**删数字、留可核结论**） | [A] | ②；判定事实（宿主版本／日期／结论）保留 |
| F3 | 同件 `:136` 的现有表头注（`>` 行）句末**追加**（**不新起行、不入表内**——表内插句必破列数） | 表头注补一句：「**本表「机检」列说明该文件类别的机检覆盖范围，非状态断言**——同一格内可同时述「已接入」与该类文件的具体检查是否待建。」 | [A] | ③；**裁定：加口径句而非改 5 格**（「已接入＋仍待建」是**精确**描述，统一成单值会失真） |
| F4 | `scripts/check.sh:9`（[3] 段头注） | 段名末补「（**加粗密度不实现**——阈值未立法，见 `测试与验收标准` §4 G1）」 | [A] | ④；与工具 docstring 的五项口径一致 |
| F5 | `AGENTS.md` 头注（第二行末） | 「本文件是**入口与路由**：项目一句、红线、去哪读、常用命令。」→ 「本文件是**入口与路由**：项目一句、红线、路由、常用命令、不做清单、分工原则、权威顺序、自检信号。」（**8 项，与 7 节对应**） | [A] | ⑤；宪法件改动经作者会话内确认 |
| F6 | `AGENTS.md:80`（§七 第二条**整句**：主体句 ＋ 括号注一并改写） | 「链接若被改动，**断链按当批规格的链接命令现场核**（**引用闭合的常驻检查现状见 …**——候选类只呈报）」→ 「链接若被改动，**断链由常驻检查报候选——无需现场人工核**（见 `scripts/check.sh` 头注与 `测试与验收标准` §1.1；**候选类只呈报，但须在本批核验结论中逐条判「属预期／待处置」**）」 | [A] | ⑥；主体与注同向（皆指机器已覆盖） |
| F7 | 同件 `:79`–`:82`（§七 **四条**） | 第二条的长括号注**并入句式**，使四条平行（长度量级相当、皆「信号句 ＋ 判据指针」两段式） | [B] | ⑦；`文档写作标准` §一.9.2 的自检信号须可一眼对比。**成稿审查 P1-2**：原标 `[A]` 却只给方向、无逐字落文 → 改 `[B]`（形态由走查型断言判；**「量级相当」须限定为「去掉了独占整段的长括号注、与最长条持平」**——实测改后 53／**133**／127／71，第二条仍为第 1 条的 2.5×，**不得称「已拉平」**） |
| F8 | 同件 `:61`（§四 末句） | 「…先修订本节并取得作者确认；**解冻一律由作者裁决并记入 `CHANGELOG`**」→ 「…先修订本节并取得作者确认——**解冻一律由作者裁决并记入 `CHANGELOG`**」（分号→破折号，示后句为前句的具体化） | [A] | ⑧ |
| F9 | 同件 `:13`（红线 1 括注） | 括注拆为**方向一致**的两段（「算」的一面与「不计」的例外分开陈述，各自成句），不改任何实质判据 | [B] | ⑨；**只改陈述形态，不改红线语义**。**成稿审查 P1-2**：原标 `[A]` 却只给方向 → 改 `[B]`（红线 1 的实质判据**一字不变**，由走查型断言 ＋ 核验第 1 步逐 hunk 判） |
| F10 | 同件 `:16`（红线 4） | 「…不在此禁列」→ 「…**此程序为改断言的合法路径**」（消读法 B 的歧义） | [A] | ⑩ |
| F11 | `scripts/check_content.py:42` | 常量 `MD_LINK` 的**正则定义**：**同步为 `check_spec.py:225` 的认 title 形**（该行**逐字照搬**——含可选的引号包裹 title 段；本规格**不内联该正则**，施工方现场读 `check_spec.py:225` 取真值）。**落地后 `[3]` 段将新增 3 条断链候选**（三条均指 `archive/078` 的示例串、目标不实存）——**属预期**（候选只呈报、不置红；归档件只读故不改其示例串） | [A] | ⑪（**实测 3 条**，全为 `archive/078` 示例串、非真链接）；同概念同语义；**只改常量定义**，不改任何检查逻辑 |
| F12 | `scripts/check.sh` 头注末（`:11` 之后新增一行注释） | 补：「`# 段序区止于恰为「set -u」的行——_seg_summary 以该行为扫描终止哨兵；改此行须同步改其终止判据`」 | [A] | ⑫；**在头注内**（`set -u` 行之前），不改 `_seg_summary` 实现 |
| F13 | `docs/standards/版本与分发标准.md:48`（§3 第 2 步） | 句末补：「（**条目格式**：**列 0、以 `-` 加空格起首**——`check_content.py` 的 `[3]` 段按此严格守卫（**不匹配该形态的行不入限长核对**——既不入红也不出候选），`check_spec.py` 的核验按宽松形态识别缩进与符号差异；两判据各司其职）」 | [A] | ⑬；把隐性假设显性化（两判据**不统一**是有意的） |
| F14 | `docs/specs/施工机制.md` §四 回写清单 ③④ | ③ → 「**`check.sh` 头注（段行与段名为主，含用法／依赖与前置行）**」；④ → 「**`check_*.py` 工具自身的模块 docstring**（与同件内联注释、正则实现三者一致）」 | [A] | ⑭；两处边界写死，免读者并成同一物 |
| F15 | `docs/standards/规格写作标准.md:87`（§8 执行面 ②） | ② 末补：「**认领须在本批规格的「本批核销」节记一行**（核销落本批规格，不另建台账）。」 | [A] | ⑮；`施工机制` §三 核销落点 ＋ `AGENTS.md` §五.3 文件账本律 |
| F16 | `docs/specs/施工机制.md` §四 回写清单（「四处」→「五处 ＋ 一类」） | 「四处」扩为：「**五处**——① `测试与验收标准` §1.1 表 ② `AGENTS.md` §七 自检信号 ③④ **见 F14 落文**（逐字以 F14 为准，本处不复述，免两处字面不一） ⑤ **各标准件 §三 机检锚点表的状态列**（**含 `测试与验收标准` 自身**——11 件标准各 1 张，与该件 §1.1 表、§一.4 缺口表为**三张不同的表**）」；另并列一类：「**缺口表**：`测试与验收标准` §4（G1–G25）逐行描述各检查的实现状态，检查项变更时须同步。——⑤ 与「缺口表」两类为何并入：`095`／`096` 两批的 P1 均发生在该两处。」 | [A] | ⑯；**规划方裁定：并入**（同族漂移现场，且两批 P1 实证） |
| F17 | 判据措辞两处 **＋ F17② 的标记校准**（**产物审查 P1-2**：其判据按字面**已命中**——`archive/085-*.md:299` 逐字含「单维核对」，另有 `archive/062:230`／`063:212`；按 `规格写作标准` §8「判据命中即**同批校准或删除该标记**」→ 本批履行） | ① `docs/standards/文档写作标准.md:55` 的「**该件**机检锚点表『状态』列…」→「**任一标准件** §三 机检锚点表（或产品层清单）『状态』列出现符号型取值」（射程面与该条条文自身一致） ② `docs/specs/施工机制.md:44` 的判据与条文原触发同义 → 改指更明确（措辞按方向自定） | [B] | ⑰ 邻近项；096 转办 D 组第 4 条 |
| F18 | `docs/specs/施工机制.md` §六 反模式清单 **`#21` 行**事故出处列 | 追加「**＋ `G22` 族第五次复发**（`测试与验收标准` §4 `G22`：规格头注「自由度分布」与 §二 清单不自洽；`068`／`069`／`090`／`093` 已有记录；**本批无 G22 实例**——检查 G 汇总实测「不符 0 项」✓）——**本族无独立反模式行，并入本行**」 | [A] | ⑰；不新立行（并入 #21/#22 已是同族，依 `AGENTS.md` §四 反膨胀倾向） |
| F19 | 本规格 §七（`070-6` 的核销与假前提订正） | 核销 `070-6`（`archive/071:178`／`archive/074:146`）：标 **已履行**，理由记「**该条前提记载有误**——`074` 记『070 只举两例』，逐提交回放证 `070` 原文**即三例**（`协作与流程类以 施工机制 为准` 自 `070` 起已在）；且该例外面自 `076` 后无批次核销，本次一并结案（归档件只读，订正记于本规格）」 | [A] | ⑱；评审 P1-1 |
| F20 | `docs/standards/命名标准.md:47`（§1 现状与预留段） | 段末补分野句：「**「预留」与「预立法」是两类「尚未」**：预留＝该类目／位置**尚未启用**（「启用前不得新增实例」，触发为**事件**：T1 触发／首个发布批／user-invoked 层启用，到点**启用该位置**）；预立法＝条文有效但**缺实例来校准**（「随首批实例校准」，触发为**首个实例出现**，到点**校准并删标记**）——两者判据／对象／触发三面不同，**不得互推**。**同一类目可两态并存**——如 `命名标准:63` 开发测试：**类目预留**（`scripts/` 下 `test-*` 类目尚未启用，见该行关联实例判据）＋ **条文预立法**（该类目下无实例可校准）——并存时**各按所属面判**，互不取消。」 | [A] | ⑲；本规格 §一 口径裁定 |
| F21 | `CHANGELOG.md`（`### Changed` 段首） | 一行：『文档形态与工具一致（097）：…』 | [B] | 过程产物 |
| F22 | 三处**同族批次表述**（产物审查 P2-3）：`docs/standards/测试与验收标准.md:25`（§1.1 [3] 行）／同件 `:112`（§三 [3] 行）／`scripts/check_content.py:10`（模块 docstring） | 「**加粗密度本批不实现**」→ 「**加粗密度不实现**」（**去「本批」**——该词无指代；与 F4 在 `check.sh:9` 落的新措辞同形）。**注**：前两处正是本批 F16 新纳入回写清单的载体（① §1.1 表／⑤ §三 锚点表），须同批收口 | [A] | 产物审查 P2-3（同族残留：批次相对表述）；F1 同族（历史陈述→状态陈述） |

**口径要点（易错）**：① **F5／F6／F7／F8／F9／F10 六处均在宪法件 `AGENTS.md`**——只动 F 行指明的那一段，该件其余**一字不改**；六处的作者确认范围为这六处（会话内逐项 ✓）。② **F9 只改陈述形态**（拆成方向一致的两段），**红线 1 的实质判据一字不变**。③ **F11 只改常量定义**（正则逐字照搬 `check_spec.py:225`），**不改任何检查逻辑**。④ **F12 是头注内新增注释行**（`set -u` 行**之前**），**不改 `_seg_summary` 实现**。⑤ **F16 是扩面不是替换**——现有「守卫变更回写清单」的 ①②③④ 全部保留。⑥ `docs/specs/archive/` **只读**（F19 只在本规格内记录，不回填归档件）。

## 三、验收断言

### 断言 1｜`skill形态标准` 四处（F1–F4）

```bash
grep -c '一并废止' docs/standards/skill形态标准.md
```

期望 **0**；**预验基线 1**。

```bash
grep -c '13→14' docs/standards/skill形态标准.md
```

期望 **0**；**预验基线 1**。

```bash
grep -c '本表「机检」列说明该文件类别的机检覆盖范围' docs/standards/skill形态标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '加粗密度不实现' scripts/check.sh
```

期望 **1**；**预验基线 0**。

### 断言 2｜宪法件六处（F5–F10）

```bash
grep -c '项目一句、红线、路由、常用命令、不做清单、分工原则、权威顺序、自检信号' AGENTS.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '断链由常驻检查报候选' AGENTS.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '断链按当批规格的链接命令现场核' AGENTS.md
```

期望 **0**（旧措辞离场）；**预验基线 1**。

```bash
grep -c '此程序为改断言的合法路径' AGENTS.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '不在此禁列' AGENTS.md
```

期望 **0**；**预验基线 1**。

### 断言 3｜工具层三处（F11–F13）

```bash
python -c "import io;a=[l for l in io.open('scripts/check_content.py',encoding='utf-8') if l.startswith('MD_LINK')][0];b=[l for l in io.open('scripts/check_spec.py',encoding='utf-8') if l.startswith('MD_LINK')][0];print(a==b)"
```

期望 **True**（**比对式**——两件的 `MD_LINK` 定义行逐字相等；成稿审查 P0-1：原用 `grep -c "…r''''"` 判「同形」，而目标形态是 3 个单引号、模式写成 4 个 → **恒 0、到位期望不可达**）；**预验基线 False**。

```bash
grep -c '段序区止于恰为' scripts/check.sh
```

期望 **1**；**预验基线 0**。

```bash
grep -c '列 0、' docs/standards/版本与分发标准.md
```

期望 **1**；**预验基线 0**。

### 断言 4｜回写面扩面与认领落点（F14–F16）

```bash
grep -c '工具自身的模块 docstring' docs/specs/施工机制.md
```

期望 **1**（**计行**——成稿审查 P0-2：原模式 `check_*.py` 在 BRE 下 `_*` 为量词 → **恒 0**；改取不含元字符的锚）；**预验基线 0**。

```bash
grep -c '五处' docs/specs/施工机制.md
```

期望 **1**（回写清单改「五处」）；**预验基线 0**。

```bash
grep -c '认领须在本批规格的「本批核销」节记一行' docs/standards/规格写作标准.md
```

期望 **1**；**预验基线 0**。

### 断言 5｜族回填与分野句（F18–F20）

```bash
grep -c 'G22` 族第五次复发' docs/specs/施工机制.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '「预留」与「预立法」是两类' docs/standards/命名标准.md
```

期望 **1**；**预验基线 0**。

```bash
grep -c '均为 `#9` 的实例化' docs/specs/施工机制.md
```

期望 **1**（096 落的那条**不动**）；**预验基线 1**。

### 断言 5b｜F7／F9 的形态判据（**走查型**）

**走查型（两条，由核验第 1 步逐 hunk ＋ 规划方通读判）**：① **F7**——`AGENTS.md` §七 四条改造后**各为「信号句 ＋ 判据指针」两段式、长度量级相当**（判据：**去掉了独占整段的长括号注、与最长条持平**——实测改后 53／**133**／127／71；**不得表述为「长度已拉长」**：第二条仍为第 1 条的 2.5×）；② **F9**——红线 1 的括注拆句后**两段方向一致**（「算」的一面与「不计」的例外各自成句），且**其实质判据逐字未变**（三串仍在：改 `.git/` 内内容或工作区文件／`fetch` 等只动 `.git/` 者同样在内／只读命令的索引缓存刷新不计）——**实质判据一字之差即 P0**。

### 断言 6｜门禁、链接与改动面

```bash
bash scripts/check.sh > /dev/null 2>&1; echo "exit=$?"
```

期望 **exit=0**；**预验基线 exit=0**。

```bash
npx markdownlint-cli2 "**/*.md" 2>&1 | tail -1
```

期望 **0 issues**；**预验基线 0 issue**（70 件含本规格）。

```bash
git status --porcelain | wc -l
```

期望 **11**（**逐类列举**：`M` **10 件**＝`AGENTS.md`／`CHANGELOG.md`／`docs/specs/施工机制.md`／`docs/standards/skill形态标准.md`／`docs/standards/命名标准.md`／`docs/standards/文档写作标准.md`／`docs/standards/规格写作标准.md`／`docs/standards/版本与分发标准.md`／`scripts/check_content.py`／`scripts/check.sh`）；`??` **1 件**＝本规格。**预验基线 1**（仅本规格——`094` 规格的「待填」行已于 `6c65767` 随其规格入库）。**施工方须现场逐行列举**并与上列逐名核对；**若不符即停摆申报**（不得自行改数字）。

```bash
python -c "import io,os,re,subprocess;g=lambda a:subprocess.run(['git','ls-files','-z']+a,capture_output=True).stdout.decode().split(chr(0));f=[x for x in g([])+g(['--others','--exclude-standard']) if x.endswith('.md') and os.path.exists(x)];L=[(p,u) for p in f for u in re.findall(r'\[[^\]\n]*\]\(([^)\s]+?)\)',io.open(p,encoding='utf-8').read()) if not u.startswith(('http','#','mailto'))];print(sum(1 for p,u in L if not os.path.exists(os.path.normpath(os.path.join(os.path.dirname(p),u.split('#')[0])))))"
```

期望 **0**（断链数）；**预验基线 0**（总数 176，本批不新增链接）。

### 断言 7｜过程产物

```bash
grep -cF '文档形态与工具一致（097）' CHANGELOG.md
```

期望 **≥1**；**预验基线 0**；行长 ≤400 字符（`python len` 实测并记入核验结论）。

过程产物第二项：**验收通过后本规格移入 `docs/specs/archive/`**（移动不留副本）——走查型，归档时核。

## 四、规格自审（四条）

1. **锚点现场核过**：§一 二十条逐条实跑（`sed`／`grep -n`／`grep -c`／三命令基线）。
2. **断言实跑过（改前侧）**：断言 1–7 逐条实跑（见 §四 附）。
3. **禁止事项已写明**：见 §五。
4. **范围与期望同源**：断言 6 的 porcelain 期望由**逐类列举**得出（10 件 M ＋ 1 件未跟踪）；断言 2 的两条零命中项（旧措辞）由「现状 1 → 目标 0」逐条对应；断言 3-1 的 `MD_LINK` 期望由**与 `check_spec.py:225` 逐字同形**得出。

**预验摘要（改前侧实测）**：1-1＝**1**／1-2＝**1**／1-3＝**0**／1-4＝**0**／2-1＝**0**／2-2＝**0**／2-3＝**1**／2-4＝**0**／2-5＝**1**／3-1＝**0**／3-2＝**0**／3-3＝**0**／4-1＝**0**／4-2＝**0**／4-3＝**0**／5-1＝**0**／5-2＝**0**／5-3＝**1**／6＝`exit=0`／0 issue／**porcelain 1**／断链 0／7-1＝**0**。

## 五、禁止事项

- 禁止触碰：`skills/`（全目录）、`docs/product/`、`docs/specs/archive/`（**只读**——`070-6` 的订正记在本规格内）、`README.md`、`scripts/check_spec.py`／`scripts/patch_file.py`（本批只改 `check_content.py` 的**常量定义**）。
- **F5–F10 六处均在宪法件**：只动 F 行指定的段落，该件其余一字不改；**F9 不得改红线 1 的实质判据**；**F12 不得改 `_seg_summary` 实现**；**F16 是扩面不得替换**既有 ①②③④。
- 不得改断言、改数字、删检查来变绿。禁止动作：任何 git 写命令；联网；安装依赖。

## 六、被否决的替代方案

- **F3 把 §13 的 5 格统一为单值**（如「部分已实现」）→ 否决：「已接入 ＋ 该具体检查仍待建」是**精确**描述，压成单值即**失真**；改为表头加口径句（`095` 的「表格形态占位」同例）。
- **F11 引入共享模块（`scripts/lib/`）消除双定义** → 否决：`scripts/` 下无 `lib/` 形态，新增目录违 `命名标准` §1 平铺；语料 0 条，属潜在面，**同步常量**即足。
- **F12 改 `_seg_summary` 用更鲁棒判据（`case 'set -u'*`）或显式终止标记行** → 否决本批做：那是**结构改动**（改段序真源与扫描器），而触发（有人把首行改成 `set -euo pipefail`）**是假设不是事故** ✗ → 按事故驱动**先声明契约**；结构改动**登记待触发**（§八 1）。
- **F16 不并入 §三 锚点表与 §4 缺口表** → 否决：`095`／`096` 两批的 P1 均发生在该两处（同族漂移现场有实证），不并入即留缺口。
- **F18 新立 `#26`（`G22` 族独立行）** → 否决：`G22` 族与 `#21`（改一处忘了它使自家断言失效）／`#22`（同载体多副本漏改）同族，并入其事故出处列即足（`AGENTS.md` §四 反膨胀倾向）。

## 七、本批核销（096 §八 全部 ＋ `070-6`）

**必列面**：① 前一批（`096`）§八 全部开放项；② 本批改动面直接触及或使其状态变化者（含 `070-6`——本批改 `AGENTS.md` §七／§四／§一，**正是其射程面**）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| 096 §八 1 | `AGENTS.md` 的「形态自审」（头注与节序一致等四条） | **本批处置（部分）** | **F5**（头注列举补全）／**F7**（四信号平行）——即该四条中的两条已落；**余两条**（括注方向单一／红线自测句齐）由 **F9** 与「现状已齐」覆盖 → 该条**结案** |
| 096 §八 2 | 交底装载路径的触发面（只覆盖 `skills/` 资产） | **仍待** | 触发＝产品层／治理文档批出现同型事故 |
| 096 §八 3 | 回写清单的执行面扩展（跨工具同概念常量） | **本批处置** | **F11**（`MD_LINK` 同步）＋ **F16**（五处＋缺口表） |
| 095 §八 2（承 `096` §七 表「仍待（转 `097`）」） | 回写清单的后续维护（同件内双载体是否纳入） | **本批处置** | **F12**（`check.sh` 头注 ↔ `_seg_summary` 函数内注释的契约声明）＋ **F14④**（docstring ↔ 内联注释 ↔ 正则实现「三者一致」）——本批改动面直接触及该条形态 |
| 095 §八 4（经 `096` §七 表承载） | 预立法标记的逐条校准（7 处，096 已清账） | **已履行** | `096` F4 已逐条补判据（实测 7 件 ✓）；本批 **F20** 补「预留 vs 预立法」分野 |
| 095 §八 5（经 `096` §七 表承载） | `check.sh` 收尾行是静态清单（段跳过时过度声明） | **仍待** | 触发＝出现「段跳过而收尾行仍称已检」的实撞 |
| `archive/074:146`（`070-6`） | §六.3 例外面的扩张（**前提记载有误**） | **本批处置（结案）** | **F19**：核销并订正前提（`070` 原文即三例，非两例）；归档件只读，订正记本规格 |
| `archive/071:178`（`070-6`） | 权威顺序的例外面（补第三类明文真源指定） | **本批处置（结案）** | 同上（同一条目的另一承载行） |
| `097` 本批自产 | —（无） | — | — |

- 汇总（恒列五档、无该档计 0；**按条计**）：`已履行 1｜作废 0｜本批处置 5｜仍待 2｜待作者 0 ＝ 8` ✓

## 八、开放项

1. **`_seg_summary` 的终止判据结构改动**（F12 登记）：头注显式终止标记行（`# --- 头注止 ---`）或 `case 'set -u'*` 鲁棒化——触发＝**首行 shell 选项被改**的实撞。
2. **`070-6` 的第四类指定**：该条原始诉求（补第三类明文真源指定）实为**已由 `070` 满足**（三例并存）；若将来新增第四类真源，触发＝出现时按例外面程序处置（届时须作者裁定）。
3. **`docs/product/` 三域设计件的清单其余条目**：其预立法判据（`卷纲域设计:21`／`正文域设计:21`＝「该域建设批落地」）在该域建设批落地时兑现——触发＝卷纲／正文域开工。
4. **评审节奏**：本轮六份评审的 findings 经 `096`／`097` 两批落位后**应暂停接收新评审**，先做 `094` 的实测（末次）并清账至仅余挂起项——触发＝作者裁定恢复评审。
5. **`CHANGELOG.md` 行尾混淆**（**核验第 1 步实测**：CRLF **77** ＋ 裸 LF **2** ✗——该件同时存在两种行尾；其余四件干净：`命名标准`／`规格写作标准` 全 CRLF、`AGENTS`／`施工机制` 全 LF ✓）。**非本批引入**（三点强推断：本批 `+1/-0` 且新增行为 CRLF／仓内改动必经 `patch_file.py`（逐行各存行尾、不改既有）／两条裸 LF 行是 diff 的上下文行）。**且入库合规**：`git cat-file blob HEAD:CHANGELOG.md`＝**78 行纯 LF**（`.gitattributes` 的 `* text=auto eol=lf` 已在入库时归一）——**故本条不立案**：工作树的 CRLF 属本地配置（`core.autocrlf`）现象，正是 `运行环境标准` §一.3.3「工作树出现 CRLF 属本地配置差异，**不折腾**」所辖面。**收口对象只能是工作树**（如需一致，本地 `core.autocrlf` 置 false 即可）；仓内无缺陷。

**核验结论（规划方，2026-09-15）要点**：① 改动面 11 行逐名一致、未动面零 diff ✓；② 断言 1–7 独立重跑全中，**红线 1 实质判据三串 `True／True／True`** ✓；③ 独立复核方（产物审查）对三条裁定 A／B／C **均判成立** ✓、对「行尾非本批引入」给出三点强推断并证**入库已合规**（`HEAD` blob 为纯 LF）→ 该条**不立案** ✓；④ `[4]` 段对 097 的 **23 条候选**（含 094 段）已由审查方逐条判为**属预期**：归零型 3 条（删旧串到位）、自噬预警 7 条（现行各 1＝断言期望）、`[A]` 未落实或已漂移 2 条（F1 的引文省略号前缀／F16 目标栏的自指措辞）、方向标记后无引号块 1 条（**F8——工具 `_dir_split()` 把括注内的 `→` 当方向标记 → 该 `[A]` 行未被逐字核，属检查 C 的假阴性，与 #24 同族**）、无逐字目标文本 1 条（F11 明写不内联正则）、`C：4 行无核对面`（语义指代式路径列）、汇总行若干 —— **无待处置项** ✓。

> **登记（承上条第 ④ 项的 F8 面）**：检查 C 的「方向标记后无引号块」对**括注内含 `→`** 的 [A] 行**假阴性**（该行不被逐字核且不报警）——触发＝`测试与验收标准` §4 增列缺口时，或该形态再现时按类修 `_dir_split()`（**本批不扩改动面**）。

## 九、审查记录

（待填：成稿审查 → 施工 → 核验三步 → 双维产物审查 → 归档 → 呈作者确认提交）
