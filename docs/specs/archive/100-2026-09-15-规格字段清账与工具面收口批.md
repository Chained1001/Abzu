# 100-2026-09-15-规格字段清账与工具面收口批

> 定位：两块内容——**面一·规格字段清账**：按**同一把尺子**（「**有没有消费者**」）处置两个字段——`每章字数` **删**（大纲／卷纲侧零读点，真正的用户是正文阶段）／`卷数` **移出规格、只住标尺**（有消费者：草稿步 3.3 分卷，而标尺现已进生成期，读得到）；**面二·`scripts/` 工具面收口**：作者 2026-09-16「scripts 都看看」触发的一轮清账（依赖声明补漏／批次时态／批次引用三分／`G20` 台账订正／补登两条真缺口）。
> **范围收窄（窄域复核 P1-5）**：原拟给标尺加**⑬作品体量**、给 B-2 加采「作品体量」——**经核该组无消费点**（唯一候选消费点「开书定位」在**规格阶段**，而标尺 2.3 才产出，**时间上读不到**），**恰是本批判据的反例**，故**撤销**；标尺**组数仍十二、命名面不动**。同类体量／单章字数作为**待建消费点的可采项**登记 §八。
> 路径判定（`规格写作标准` §1／`施工机制` §二）：**重路径**——动 `skills/` 运行资产 ＋ 改产品层设计件 ＋ 改 `scripts/` 工具与标准件 → 标准 ＋ **双维产物审查** ＋ **真机走查**（随批尾合并）。
> 自由度分布：[A]×6 [B]×2 ＋ 混合×1（F1：删行 [A]／改述 [B]）
> 必读输入：本规格 §一（全部锚点）；`docs/product/outline/大纲域设计-规格与资料步.md` §二；`docs/product/outline/大纲域设计-附录.md` B-1／B-3／B-4；`docs/product/outline/大纲域设计.md` §四 写法纪律 13；`docs/product/outline/大纲域设计-草稿步.md`（四处装载表／拍三）；`docs/product/书目录契约.md`；`skills/abzu-outline/assets/outline-basic-spec-template.md`；`skills/abzu-outline/references/outline-stage-spec.md`；`scripts/check.sh`；`scripts/check_content.py`；`docs/standards/测试与验收标准.md`
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（**2026-09-16 重锚——099 落地后逐条实测**）

> **重锚说明**：本规格初稿锚点系 **098/099 落地前**所取；成稿审查 **P0-1** 实测指出全部行号已偏移（098 给 B-2 加两行、给 2.3 装载表加两行；099 又给装载表加一行、给 B-2 加一字段、删了模板正文的书名行）。下表为 099 落地后真实行号，**第一轮与窄域复核两度逐条复验通过（无偏移）**。

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `docs/product/outline/大纲域设计-附录.md`：`:33`＝B-2 标题／`:35`＝表头／`:37`–`:52`＝B-2 表体（**16 行**）／`:54`＝枚举／`:55`＝默认值与推导／`:56`＝填写引导／`:57`＝示例行／`:58`＝联动校验——**B-2 无「作品体量」** | 逐行 read |
| ② | 同件：`:60`＝B-3 标题／`:62`＝表头／**`:64`–`:75`＝B-3 表体（①–⑫ 共 12 行，组数＝十二）**／`:77`＝标尺分类句／`:79`＝枚举／`:80`＝默认值与推导／`:81`＝填写引导（「这**十二组**…」）／`:82`＝示例行／`:83`＝联动校验 | 逐行 read；**`grep -c '^\| [①-⑫] '` ＝ 12** |
| ③ | `docs/product/outline/大纲域设计-规格与资料步.md`：`:97`＝2.2④ 产物字段串／`:105`＝2.3 装载图谱标题／`:110`＝「**十二组**默认值」行／`:111`＝⑪ 默认行／`:112`＝⑫ 默认行／`:114`＝③怎么想 统计口径句（含「数什么——**十二组**」）／`:115`＝④产物 标尺句（含「十组消费时点」；**同句含「卷数」两处**——①待删的「卷数按标尺派生区间回填…」半句 ②**保留的标尺侧派生项表述「派生体量项（节点数/卷数）」**）／`:116`＝依据强度／`:117`＝⑤过关／`:118`＝⑥交互／`:119`＝⑦异常 | 逐行 read |
| ④ | `skills/abzu-outline/assets/outline-basic-spec-template.md`：**`:22`＝`卷数` 定义表行**／**`:23`＝`每章字数` 定义表行**／`:41`＝卷数推导句／`:42`＝每章字数推导句／`:48`＝卷数示例行／**`:62`＝模板正文 `卷数` 行**／**`:63`＝模板正文 `每章字数` 行**／**`:86`＝`### 约束（联动校验四规则＋区间外处置话术）`**（约束节标题）／**`:91`＝「参考性字段（不符＝软提示，不拦）：字数↔卷数量级。」** | 逐行 read |
| ⑤ | 同件表头（`:16`）＝段｜字段｜**说明**｜**生产来源 Stage**｜必填性 | 逐行 read |
| ⑥ | **平台常量**（串 `3000-4000`）落点＝**4 处**：`docs/product/outline/大纲域设计-规格与资料步.md:36`／`assets:23`／`assets:42`／`skills/abzu-outline/references/outline-stage-spec.md:41` | `grep -rc` 实跑 |
| ⑦ | **「四规则」落点＝2 处**：`skills/abzu-outline/assets/outline-basic-spec-template.md:86`（约束节标题）／`docs/product/outline/大纲域设计-附录.md:31`（B-1 摘要内「联动校验四规则+区间外处置话术」）。**098 先例已实证「四规则」之数为「三角三边＋字数↔卷量级」**——删该条后「四」失准 | `grep -rn` 实跑 |
| ⑧ | `docs/product/outline/大纲域设计.md:110`＝§四 写法纪律 13 的「**十组二分**…另设**决策参照两组**（⑪结局形态分布／⑫感情线处理分布…）」——标尺分类的**真源**（`附录:77` 明写「判定与处置见 大纲域设计 §四 写法纪律 13」） | 逐行 read |
| ⑨ | `docs/product/outline/大纲域设计-草稿步.md`：**`:48`／`:86`／`:115`／`:142`＝四处装载表的「刻意不装」栏均含「⑬⑭（体量／单章字数）不进草稿（消费点是开书定位——规格阶段）」**——**⑬⑭ 两组本批均不增**，该括注**整体失效**；`:114`＝3.3 上游产物行（含 099 补入的「＋基本规格.md（**卷数与**基调的约束源）」）／`:126`＝拍三（「卷数按**规格卷数**（`2.3` 标尺回填值）定」）／`:130`＝⑤过关（「卷数量级判定留待 3.5」） | 逐行 read |
| ⑩ | `docs/product/书目录契约.md:62`＝正文域机检级契约点 4「章节字数落在 `基本规格.md` 声明的**章节字数目标区间**内」——**该契约把每章字数当硬契约**，本批删该字段；`:28`＝基本规格行／`:31`＝参考标尺行（「**十二组**量级画像」）／`:53`＝标尺**十二组**机检点 | 逐行 read |
| ⑪ | `scripts/check.sh:4`＝依赖行（**段号不带反引号**）／`:9`＝`[3]` 段行（含「加粗密度不实现」）；`scripts/check_content.py:10`＝「**加粗密度本批不实现**」 | 逐行 read |
| ⑫ | 批次引用三类：**状态型**（`check_content.py:10` 的「本批」）／**出处型**（`check_spec.py:811`–`:813`「本批 F12／F3／F11」）／**运行态与示例串**（`check_spec.py:806`＝运行态措辞／`:852`＝**代码内 f-string**／`:787`＝**示例串，改述即毁**） | `grep -n '本批' scripts/*` 实跑 |
| ⑬ | `docs/standards/测试与验收标准.md:83`＝`G20` 行（现文含「091 复核判残余不存在」）；**§4 表止于 `G25`** | 逐行 read |

## 二、文件级改动清单

### 面一·规格字段清账（F1–F4）

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F1 | `skills/abzu-outline/assets/outline-basic-spec-template.md`（`:22`／`:41`／`:48`／`:62`／**`:91`** 五行）＋ `docs/product/outline/大纲域设计-规格与资料步.md`（`:36`／`:37`／`:39`／`:115`／`:129`）＋ `skills/abzu-outline/references/outline-stage-spec.md`（`:41`／`:42`／`:44`）＋ `docs/product/outline/大纲域设计-附录.md`（`:31`／`:97`）＋ `docs/product/outline/大纲域设计-草稿步.md`（`:114`／`:126`） | **`卷数` 移出规格、只住标尺**（作者 2026-09-16 裁定）。**删**：`assets` 的 `:22`／`:41`／`:48`／`:62` 四处（定义行／推导句／示例行／模板正文行）与 `:91` 的**「参考性字段（字数↔卷数量级）」分句**（**该句两个操作数本批双双离开规格**——每章字数删、卷数移出，校验**失去操作数**）；`规格与资料步` 的 `:36`（卷数半句）／`:39`（完备校验句内卷数分句）／`:115`（「卷数按标尺派生区间回填——作者已给值则保留作者值…」整半句）／`:129`（验收基准内「卷数／」）；`stage-spec` 的 `:41`／`:42`／`:44` 同构三处；`附录` 的 `:31`（B-1 摘要内「卷数（资料收集后按标尺回填）／」）。**改述**：`附录:97`「分卷数默认取基本规格**卷数**（`2.3` 按标尺回填值）」→「分卷数默认取**参考标尺**的卷数（③派生项）」；`草稿步:114` 装载括注「基本规格.md（**卷数与**基调的约束源）」→「基本规格.md（**基调**的约束源）」；`草稿步:126`「卷数按**规格卷数**（`2.3` 标尺回填值）定」→「卷数按**参考标尺**卷数定」。**「四规则」两处同批改述**（锚点⑦——删该条后「四」失准）：`assets:86` 节标题与 `附录:31` 摘要内「联动校验**四规则**」→「联动校验**三规则**」（承诺性三角三条边）。**注（产物审查 P2-3）**：产物落盘为**无加粗**形态（`联动校验三规则`）——本行「三规则」的 `**` 系规格强调标记，**不属目标文本**，机器闸据引文形态报的该条 `[A]` 漂移预警**属预期** | [A]／[B]（删行 [A]；三处改述 ＋ 四规则两处 [B]） | **作者裁定**（2026-09-16）；判据＝「**有没有消费者**」；**窄域复核 P1-2**（四规则失准）／**P1-1 类**（状态词） |
| F2 | `skills/abzu-outline/assets/outline-basic-spec-template.md`（`:23`／`:42`／`:63` 三行）＋ `docs/product/outline/大纲域设计-规格与资料步.md`（`:36` 内该半句／`:129`）＋ `skills/abzu-outline/references/outline-stage-spec.md:41` ＋ `docs/product/outline/大纲域设计-附录.md:31` | **删 `每章字数`**（作者 2026-09-16 裁定：「压根没用，起码要等正文阶段才使用」）。**依据（规划方实证，窄域复核复验通过）**：全仓 `每章字数` **7 处落点**（`assets:23`／`:42`／`:63`／`规格与资料步:36`／`:129`／`stage-spec:41`／`附录:31`）——**无一处是读点**（定义／推导／模板／摘要／验收基准／副本）；`卷纲域设计.md` 与 `打磨定稿步.md` 对「字数」**零命中**。**连带删平台常量 4 处**（`规格与资料步:36`／`assets:23`／`assets:42`／`stage-spec:41`）——**不搬进标尺**（裁定「删除」），正文域建设时按需重建（登记 §八 1）。**唯一语义消费者**是正文域契约点 4（`书目录契约:62`），登记 §八 2 | [A] | **作者裁定**（2026-09-16）；判据同 F1；窄域复核 **C-10** 实证通过 |
| F3 | `docs/product/outline/大纲域设计-草稿步.md`（`:48`／`:86`／`:115`／`:142` 四处装载表的「刻意不装」栏） | **删失效括注**：「**⑬⑭（体量／单章字数）不进草稿**（消费点是开书定位——规格阶段）；」→ **整段删**（保留其后「**⑪⑫决策参照两组装**（形态选项、不含量级区间，不诱发『对着区间写』）」原样）。**依据**：⑬⑭ 两组本批**均不增**（`⑬` 因无消费点撤销、`⑭` 随每章字数一并取消），该括注**指代两个不存在的组**；且其自述「消费点是开书定位——**规格阶段**」**时间上不成立**（标尺 2.3 才产出，规格阶段 1.1 在其前）。**:115` 该栏另含「草稿期最大标尺消费方是 3.5」半句——**保留** | [A] | **窄域复核 P1-3／P1-5**（残留指代不存在的组＋消费点时间倒置）。**注（产物审查 P2-6）**：删后半句后该栏以**正向陈述**起首（「**⑪⑫决策参照两组装**（…）」）而列名为「刻意不装」——**系有意保留**（其后内容为既有口径，不在本批改动面） |
| F4 | `CHANGELOG.md`（`[Unreleased]` 段） | **过程产物**：新增本批条目一行（列 0、以 `-` 加空格起首、≤400 字符；条首串写死为「规格字段清账与工具面收口（100）」；**子段＝`### Changed`**——主体是改结构而非加能力） | [B] | `施工机制` §三 过程产物两项；成稿审查 P2-8 |

### 面二·`scripts/` 工具面收口（F5–F9）

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| F5 | `scripts/check.sh:4`（依赖与前置行） | **依赖声明补漏段**：现文（**逐字**）`# 依赖与前置：Node.js（[1] 段需 npx，未找到即置红）；Python 3（[3]／[4] 段需 python3／python／py，三级链均缺即置红）；命令行按 Git Bash 语义执行（Windows）。` → 其中 `[3]／[4] 段需` 改 `[3]／[4]／[5] 段需`。依据：`[5]` 段用 `$PY` 跑 `ast.parse`（`scripts/check.sh:176`／`:180`），且 `scripts/*.py` 恒存在；093 批把 `[4]` 改述为「`[3]`／`[4]`」时**同批新加的 `[5]` 加漏** | [A] | 作者 2026-09-16 提问触发；规划方逐段实测；成稿审查 P2-6（引文形态订正） |
| F6 | `scripts/check_content.py:10`（文档头·用途段） | 去批次时态：「**加粗密度本批不实现**」→「**加粗密度不实现**」（与 `scripts/check.sh:9` 头注同口径；「本批」指 093，非状态声明） | [A] | 规划方实测（该件「本批」仅 `:10` 属状态型） |
| F7 | `scripts/check.sh`／`scripts/check_content.py`／`scripts/check_spec.py`／`scripts/patch_file.py` 四件的文档头 | **批次引用的三分处置**（成稿审查 P1-5 订正：初稿两分法漏第三类）：① **状态型**（把某批运行态写成恒常陈述者，如 `check_content.py:10`）**改述为状态无关**；② **出处型**（批次号用作「该判据的事故来由」，如 `check_spec.py:811`–`:813`）**保留原文**；③ **运行态与示例串**（`:806`＝运行态措辞／`:852`＝**代码内 f-string**／`:787`＝**示例串，改述即毁示例**）**保留不动**。②③ 由四件文档头**各补一句口径**：「行内出现的批次号分两种——**判据的事故出处**（provenance，非状态声明）与**运行时措辞／示例串**（**不得**按批次叙事改写）。」 | [B] | `G20` 族（台账「残余不存在」与实况不符，见 F8）；**窄域复核复核通过**（三处确属第三类）；**范围判据**：以施工方现场 `grep -n '本批' scripts/*` 逐处列举分类为准（**不得沿用任何预列处数**——规划方正则粗测不可靠） |
| F8 | `docs/standards/测试与验收标准.md:83`（§4 `G20` 行） | **台账状态订正**：现文尾「**本批处置（090 F1／F2①；091 复核判残余不存在）**：下次触发＝同类自指时态再现时」→「**部分处置（090 F1／F2① 已收敛 docstring 与 `[2]` 段注释）**；**残余在册**（`check.sh`／`check_spec.py`／`patch_file.py`／`check_content.py` 内联注释与运行态措辞仍有批次引用——**091 的「残余不存在」判定与实况不符**，本批订正）；本批以 F7 三分处置收口：下次触发＝**新增守卫件时同口径书写**」 | [A] | 规划方实测；成稿审查 F9 事实成立 |
| F9 | `docs/standards/测试与验收标准.md` §4 缺口表（`G25` 行后**追加两整行**） | **补登两条真工具缺陷**（规划方欠账——此前多轮口头称「已登记」，实测四处台账均无）：**`G26`**＝「`scripts/check_spec.py` 的 `BAN_EXCUSE_MARKS` 豁免词表**无「豁免」**，而判一报错文案作「禁改面**未豁免**」（`:1072`）——判据与文案不一致（写「豁免」不被识别；`098`／`099`／本批各白折腾一轮）」；**`G27`**＝「检查 A 对**转义竖线 token**（如 `结局形态\|感情线处理`）按**字面**取数（`:704`）→「现行」计数**恒 0**（工具伪零；`098` 施工期复现）」。**并声明第三条不登**：曾拟登「计数期望易被新引入同串顶红」，经核该情形**已由 `规格写作标准` §5.4「断言与替换文本不得互斥」立法**，属**未执行既有规则**（`施工机制` §六 `#21` 族），**不构成工具缺口** | [A] | 作者 2026-09-16「scripts 都看看」触发；`AGENTS.md` §四「引入任何此类机制须先登记事故出身」；成稿审查 P2-7 |

## 三、验收断言（按 `规格写作标准` §5.9 逐条拆；**编号不入「期望」子句**——成稿审查 P1-4／窄域复核 P1-6）

### 断言 1｜卷数移出（F1）

```bash
grep -c '卷数' skills/abzu-outline/assets/outline-basic-spec-template.md
```

期望 **0**。**预验基线 5 行**（`:22`／`:41`／`:48`／`:62`／`:91`）。

```bash
grep -c '卷数' docs/product/outline/大纲域设计-规格与资料步.md
```

期望 **1**。**预验基线 6 处出现／5 行**（`:36`／`:37`／`:39`／`:115`／`:129`）。**该 1 处系有意保留**（**施工 P0-1 裁定＝路径①**）：它是 `:115` 标尺产物句里的「**可数组**＝①③⑤⑥⑦＋①③换算的派生体量项（**节点数/卷数**）」——**标尺侧的派生项表述**（与 `附录:77` 同构，`附录:77` 亦在保留面），而非规格字段；且 §六 明写「卷数在**分类句**里已写明…不成组、不进组数」，**该措辞正是卷数在标尺侧的落点、必须留**。本批「卷数移出」针对的是**规格字段面**（`assets` 字段行／验收基准／完备校验），**不含**标尺句。**根因**：规划方初稿把期望写作 0，与自家 §六 的保留裁定相抵（「范围与期望同源」失守），施工方按红线 4 **停摆申报而非自改断言**——判断正确。

```bash
grep -c '卷数' skills/abzu-outline/references/outline-stage-spec.md
```

期望 **0**。**预验基线 3 行**（`:41`／`:42`／`:44`）。

```bash
grep -c '分卷数默认取参考标尺' docs/product/outline/大纲域设计-附录.md
```

期望 **1**。**预验基线 0**（现文为「取基本规格**卷数**」）。

```bash
grep -c '卷数按参考标尺卷数定' docs/product/outline/大纲域设计-草稿步.md
```

期望 **1**。**预验基线 0**（现文「**规格卷数**」）。

```bash
grep -c '基本规格.md（卷数与基调的约束源）' docs/product/outline/大纲域设计-草稿步.md
```

期望 **0**。**预验基线 1**。

```bash
grep -rc '四规则' docs/product skills
```

期望 **全 0**。**预验基线 2 处**（`assets:86`／`附录:31`）。

```bash
grep -c '四规则' skills/abzu-outline/assets/outline-basic-spec-template.md
```

期望 **0**。**预验基线 1**（节标题，锚点⑦）。

### 断言 2｜每章字数删除（F2）

```bash
grep -rc '每章字数' docs/product skills
```

期望 **全 0**。**预验基线**：`assets` **3**（`:23`／`:42`／`:63`）／`规格与资料步` **2**（`:36`／`:129`）／`stage-spec` **1**（`:41`）／`附录` **1**（`:31`）＝ **7 行**。

```bash
grep -rc '3000-4000' docs/product skills
```

期望 **全 0**。**预验基线 4 处**（锚点⑥）。

### 断言 3｜草稿步失效括注（F3）

```bash
grep -c '⑬⑭' docs/product/outline/大纲域设计-草稿步.md
```

期望 **0**。**预验基线 4 行**（`:48`／`:86`／`:115`／`:142`）。

```bash
grep -c '⑪⑫决策参照两组装' docs/product/outline/大纲域设计-草稿步.md
```

期望 **4**（F3 只删 ⑬⑭ 半句，其后内容保留）。**预验基线 4**。

### 断言 4｜工具面（F5–F9）

```bash
grep -cF '[3]／[4]／[5] 段需' scripts/check.sh
```

期望 **1**。**预验基线 0**；**现文为 `[3]／[4] 段需`**——**token 含方括号，BRE 下 `[3]` 是字符类，故必须 `-F`**。

```bash
grep -c '加粗密度本批不实现' scripts/check_content.py
```

期望 **0**。**预验基线 1**。

```bash
grep -c '不得按批次叙事改写' scripts/check.sh scripts/check_content.py scripts/check_spec.py scripts/patch_file.py
```

期望 **各 1**。**预验基线各 0**。

```bash
grep -c '残余在册' docs/standards/测试与验收标准.md
```

期望 **1**。**预验基线 0**。

```bash
grep -c '091 复核判残余不存在' docs/standards/测试与验收标准.md
```

期望 **0**。**预验基线 1**。

```bash
grep -c '^| G26 ' docs/standards/测试与验收标准.md
```

期望 **1**。**预验基线 0**。

```bash
grep -c '^| G27 ' docs/standards/测试与验收标准.md
```

期望 **1**。**预验基线 0**。

### 断言 5｜过程产物与门禁

```bash
grep -c '规格字段清账与工具面收口（100）' CHANGELOG.md
```

期望 **1**。**预验基线 0**。

```bash
bash scripts/check.sh
```

期望 **退出 0**。

## 四、规格自审（四条）

1. **锚点现场核过**——§一 十三行于 2026-09-16 重锚，**成稿审查与窄域复核两度逐条复验无偏移**；「某文档 §N」引用核对：`大纲域设计` §四 写法纪律 13（**`:108`–`:118`**——成稿审查 P2-4 订正原记 `:108`–`:116`，少算末段「反机械红线」）。
2. **断言实跑过**——断言 1–5 全部基线已实跑（见各条括注）；**期望子句内不嵌编号/枚举**（防机器取数失配，窄域复核 P1-6）。
3. **禁止事项写明**——见 §五。
4. **范围与期望同源**——各断言期望与基线取自同一件同一命令；断言 2 的「7 行」经逐处实点并附「无一为读者」实证。

**附加项**：清单完备（F1–F9 与两处作者裁定 ＋ 成稿审查 18 条 ＋ 窄域复核 13 条逐条对齐）；**禁改面与改动面对账**——§五 用检查器词表内的「**只改**」形态（**成稿审查 P1-6**：初稿用「豁免」而 `BAN_EXCUSE_MARKS` **无该词**——**正是本批 F9 新登的 `G26` 本身**；现措辞经窄域复核实跑确认候选已消）。

## 五、禁止事项

- **禁改**：`docs/specs/施工机制.md`、`AGENTS.md`、`docs/specs/archive/` 内任何件。
- **禁改**（**只改**所指处）：`scripts/` —— **只改** F5（`check.sh:4`）／F6（`check_content.py:10`）／F7（四件文档头）所指处，其余脚本内容一概不动；`docs/standards/测试与验收标准.md` —— **只改** F8（`G20` 行）／F9（§4 追加两行）所指处；`docs/product/outline/大纲域设计-草稿步.md` —— **只改** `:48`／`:86`／`:115`／`:142`（F3）与 `:114`／`:126`（F1）所指处。
- **禁改**：`skills/abzu-outline/SKILL.md` 与 `skills/abzu-outline/references/outline-workflow.md` 一概不动。
- **禁**：把 `每章字数` **搬到标尺**（裁定＝**删除**）；也**禁**把平台常量留在任何处（正文域建设时重建，见 §八 1）。
- **禁**：**增**标尺组数（本批范围已收窄——**不加 ⑬**、组数仍十二、**命名面不动**；如需同类体量须先定消费点，见 §八 3）。
- **禁**：动子集处的「十组」（量级／判定口径，与 098／099 同规）。
- **禁**：把 F7 的「出处型」与「运行态／示例串」批次引用逐处改写（只加口径句，原文保留）。
- **禁**：联网、采集、跑安装器；禁改任何断言的期望值求绿（红线 4）。

## 六、被否决的替代方案

- **给标尺加 ⑬作品体量（＋ B-2 采「作品体量」）** → **撤销**（窄域复核 P1-5）：该组**无消费点**——唯一候选消费点「开书定位」在**规格阶段（1.1）**，而标尺 **2.3** 才产出，**时间上读不到**；3.5／4.1 的消费清单亦不含它。**恰是本批判据（有没有消费者）的反例**，故不增；留待有消费点（§八 3）。
- **把 `每章字数` 搬进标尺（改实测取）** → 否决（作者 2026-09-16 改判）：判据是「有没有消费者」，而它**大纲／卷纲侧零读点**，真正用户在正文阶段；搬进标尺只是换个地方空转。
- **删 `卷数`（与每章字数同办）** → 否决：卷数**有消费者**（草稿步 3.3 拍三分卷）——同一判据下结论相反。
- **把 `体量`（本书总字数）也移出** → 否决：它是**作者的商业承诺**，且是「总字数→反推卷数」的**推导锚**；主体不同（本书意志 vs 同类画像）。
- **F7 改为「全量改写批次引用为指向式」** → 否决：规划方实测计数不可靠；出处型承载「代码之所以如此」的理由，运行态与示例串更**改述即毁**；「会过时」的风险**由口径句消解**。
- **给 B-3 加「派生·卷数」行** → 否决（成稿审查 P2-3）：B-3 表「组」列现为 ①–⑫，加行会使待建守卫的「组齐」计数与「十二组」相撞；卷数在**分类句**里已写明（「①③换算的派生体量项：节点数/卷数」），据此读得到，**不成组、不进组数**。

## 七、本批核销（来源：`099` §八全部）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `099` §八 1 | 大纲产物模板件（B-4）是否建立 | **仍待** | 触发＝Stage 3（草稿步）建设批 |
| `099` §八 2 | `skill形态标准:113` 的占位符示例 `{书名}` | **仍待** | 本批未触及该件；触发＝下一次触碰该件的占位符语法节 |
| `099` §八 3 | 本批（099）改动面大 | **作废** | 系 `099` 的**自指提示**（非开放项） |
| `099` §八 4 | 「产物对规」的通则面未落（拟落 `产物契约标准`） | **仍待** | 触发＝下一批触及该标准件；关联实例判据＝`产物契约标准` 内出现「产物对规」串 |
| `099` §八 5 | `每章字数`／`体量` 的定位待裁 | **本批处置** | **作者 2026-09-16 两裁**：`每章字数` **删**（F2）／`卷数` **移出规格**（F1）／`体量` **留规格作锚**（不在本批改动面）——与 `099` 原建议「卸掉推导职责」不同，经实践核证后修正 |
| `099` §八 6 | 大纲阶段的 agent 分工（架构级） | **仍待** | 触发＝架构批 |
| `099` §八 7 | 跨批界面：`099` ↔ `100` | **本批处置** | **本批即该界面的后手**：⑬⑭／每章字数的归属以本批为准（**两组均不增**、每章字数删）；`099` F14 ④ 的收窄句已生效 |

- 汇总（恒列五档、无该档计 0）：`已履行 0｜作废 1｜本批处置 2｜仍待 4｜待作者 0 ＝ 7` ✓（**窄域复核 P0-1 订正**：初稿误记「3／3」；**P1-1** 订正：状态列「不适用」→「作废」——**状态取五档之一**）

## 八、开放项

1. **平台每章字数数据待重建**：`起点 3000-4000／番茄 2000-3000` 随 F2 删除；**正文域建设时按需重建**（触发＝正文域建设批）。**注**：规划方 2026-09-15 检索到的实践口径（起点下限 2000、日更 4000 拆 2 章；番茄 1800–2200）与仓里所写**不一致**，而该检索**未返回可引链接**，故不据以改数——重建时**须以平台官方规则复核**。
2. **正文域契约点需同步改述**：`docs/product/书目录契约.md:62`「章节字数落在 `基本规格.md` 声明的**章节字数目标区间**内」——本批删该字段，该机检契约**失去来源**。**关联指针另有两处**（窄域复核 P2-5）：`docs/product/验收场景.md:15`、`docs/product/write/正文域设计.md:16` 亦点名「字数区间」。触发＝**正文域建设批**（均属待设计面，本批不动）。
3. **同类体量／单章字数：待建消费点的可采项**：本批**不增**标尺组（⑬ 因无消费点撤销、⑭ 随每章字数取消）。若日后某阶段**确有消费点**（如卷纲域的每卷字数校准），再按「标尺加组」流程引；**触发＝出现真消费者**。
4. **本批改动面（9 项／11 件）**：若施工期出现 P0 族 ≥2，按 `施工机制` §五 止损条收窄复核面。**件数＝11（含过程产物 `CHANGELOG.md`）／10（不含）**（**施工 P2-1 订正**：初稿记 13／14 与实况不符）。

## 九、审查记录

- **决策沿革（作者与规划方，2026-09-15／09-16）**：① 作者三次追问「字数能不能挪进参考标尺」——规划方前两轮只核「标尺里**有没有**」，未先分「**主体是谁**」，连续两轮答非所问；第三轮补上主体判别后结论翻转。② 作者 09-16 两裁：`卷数` 与 `每章字数` 一并「只住标尺、规格不留字段」→ 规划方核证后**修正为**：`每章字数` **删**（无消费者）、`卷数` **移出**（有消费者）——**同一判据得出两个相反结论**；呈报后作者裁「每章字数删除掉」。③ 作者「**scripts 都看看**」触发面二。**规划方自查（记账）**：①「答『有没有』而不答『该不该有』」；② **口头称「候选已登记」而四处台账均无**——`G26`／`G27` 的补登（F9）即该欠账的清偿；③ **本批在标尺侧加了 ⑬，而它不符合本批自己立的判据**（无消费者）——**窄域复核 P1-5 抓出**，已撤销：**立判据的人在同一个批里违反了自己的判据**。
- **成稿审查·第一轮（18 条）**｜**P0:2 P1:7 P2:9**｜结论 **须修订**。**P0-1 全部锚点为 098/099 落地前旧号**（审查方逐条给出真实行号；根因＝初稿写于 099 落地前、此后未复验）；**P0-2 §七 漏列前一批开放项**（`099` §八 7 条只列 2——**同型缺陷在 `099` 首轮亦被判 P0**）。P1 七条：F2 破三分自洽／F4 列名错／断言 1 不可达／断言 5 违 §5.9／**F9 两分法漏第三类**／**§五「豁免」措辞机器不认——正是本批 F9 新登的 `G26` 本身**／件数 11 实为 13。P2 九条（非文本源口径／枚举段错置／括注体例／主体锚定／变更叙事／引文形态／逐字行文本／F6 子段／契约点）——已逐条并入 F 行或 §八。
- **窄域复核·第二轮（13 条）**｜**P0:1 P1:7 P2:5**｜结论 **第一轮 P0／P1 部分修复**（锚点 13 行**逐条无偏移** ✓／核销表 7 条**列全且来源对得上** ✓／§五 措辞**候选已消** ✓／断言基线**逐条吻合** ✓／F9 三分处置**三处确属第三类** ✓／删每章字数**「无一为读者」实证通过** ✓）。**新 P0-1**：§七 汇总行与表体**不自洽**（表体 2／4／1 而汇总写 3／3／1，且行尾带着 ✓）——**同一错误在 `099` 两轮各犯一次，本批第三次**。**P1 七条**：状态列用「不适用」（非五档词）／**F1 删联动分句后「四规则」两处失准**／**草稿步 `⑬⑭` 残留 4 行指代不存在的组**／类名「体量参照两组」与实际 1 员不符／**⑬ 无消费点（本批判据的反例）**／断言期望句内嵌编号致机器取数仍「未判」／F2 给 `规格与资料步` 列了位置未给目标文本。**P2 五条**（F2 位置列漏真源 `:110`／件数口径／B-3 加行撞组数／写法纪律 13 范围少算末段／契约点另两处指针）——已逐条并入。
- **本轮修订（规划方，2026-09-16）**：**规格第三次重写**（面一按 P1-5 收窄——**撤 ⑬ 与 B-2 作品体量、组数仍十二、命名面不动**；F1 补「四规则」两处与 `assets:91` 联动分句；F3 新增——**删草稿步四处失效的 ⑬⑭ 括注**；§七 汇总订正 2／4／1、状态词「作废」；断言期望子句**去编号**；§八 补至 4 条）＋ **文件改名** `100-…-规格字段清账与工具面收口批.md`（H1 与文件名对齐）。**基线 20 条逐条实跑复核，全部与规格所记一致。**
- **两轮审查上限已用尽**（1 全文审 ＋ 1 窄域复核；窄域复核的修订：`施工机制` §五「第 3 轮起只允许微修已知项 ＋ 呈作者」）——**本规格转入呈作者确认**。
- **施工期偏差与规划方裁定（2026-09-16）**：**P0-1（停摆申报·断言不可达）＝采路径①**——施工方报告断言 1 第 2 条期望 0 而实测 1，根因是 `:115` 同句含两处「卷数」（待删半句 ＋ **保留的标尺侧派生项表述**），**删后者与 §六 裁定相抵且越出 F1 授权**；施工方**未自改断言、未越权删**，判断正确。**裁定：期望改 1**（该处为标尺侧表述、非规格字段；§六 已明写其为卷数在标尺侧的落点），并同步 §一 锚点③ 的基线口径（5 行→**6 处出现／5 行**）。**根因在规划方**：初稿期望与自家 §六 保留裁定互斥（「范围与期望同源」失守）。**P1-1＝采**（`G26`／`G27` 的触发条件列由施工方自拟，两条措辞可，照落）；**P1-2＝采**（F7 口径句落**既有行尾接写、不新增行**——以保 `check_spec.py:1072`／`:704` 与 `check.sh` 的行号不失准；该落法判断好）；**P1-3＝采**（两处 [B] 改述去 `**`、第三处整体加粗，使 token 连续可判——`规格写作标准` §5.4 所指形态）；**P2-1＝采**（§八 4 件数订正 11／10）；**P2-2＝记录**（`CHANGELOG` 工作区 CRLF 系既存状态、非本批引入）。
- **核验三步·第 1／2 步（规划方，2026-09-16）｜独立复跑，非采信施工自述**：**第 1 步·改动面**——`git status` ＝ **11 件 `M`**（10 目标件 ＋ `CHANGELOG`），**与 §八 4 的「11／10」自洽、清单外零文件**；`D`／`??` 两行系规格**改名**（规划方所为、非施工面）；**禁改面零 diff**（`SKILL.md`／`outline-workflow.md`／`施工机制.md`／`AGENTS.md`／`archive/` 均未出现）；`scripts/` 只动四件所指处。**第 2 步·断言独立重跑**（退出码不经管道）——**21 条逐条相符**：1-1＝0／1-2＝**1**（裁定后期望）／1-3＝0／1-4＝1／1-5＝1／1-6＝0／1-7＝0／1-8＝0／2-1＝0／2-2＝0／3-1＝0／3-2＝4／4-1＝1（`-F`）／4-2＝0／4-3＝各 1／4-4＝1／4-5＝0／4-6＝1／4-7＝1／5-1＝1／`check.sh` **退出 0**。
- **`[4]` 段预警逐条判定（规划方，2026-09-16）｜施工后 15 条，无未核项**（`AGENTS.md` §七 信号）：① `自噬·卷数 @ 规格与资料步`（现行 1）＝**属预期**（＝P0-1 裁定保留的那处标尺侧表述）；② `自噬·⑪⑫决策参照两组装 @ 草稿步`（现行 4）＝**属预期**（实测＝期望 4）；③ `自噬·规格字段清账与工具面收口（100）@ CHANGELOG`＝**属预期**（实测＝期望 1）；④–⑧ `目标文本未提及且现行为 0` **5 条**（`基本规格.md（卷数与基调的约束源）`／`每章字数`／`3000-4000`／`^| G26`／`^| G27`（各含尾空格））＝**全属预期**（前三条为**删净型已达成**；后两条 token 含 `^`，检查 A 按字面取数恒 0——**与 `G27` 所记伪零同族**，断言 4 实跑＝各 1）；⑨ `现行有值／目标文本未提及: 5 个`＝**属预期**（5 项逐条实测均达期望）；⑩–⑮ `[A] 未落实或已漂移` **6 条**＝**全属预期**（`联动校验**三规则**`＝规格引文 `**` 形态所致〔P2-3〕／作者原话与现状引文＝**删净型**／`[3]\`／\`[4]`＝`G24` 嵌套引号截断族／F9 依据列的两条**条文名引语**）。
- **产物审查（独立上下文只读 subagent，2026-09-16）｜7 条（P0:0／P1:1／P2:6），结论「须补修（轻）」**：
  - **P1-1（本批核心面）**：**F7 三分处置有一处「状态型」未改述**——施工方在其 F7 范围判据里**明确写了**会把 `scripts/check.sh:157`「本批零 `.js` 目标件」改述为「无 `.js` 目标件」，但**实测该行原样未动**（**报了一处没做的改动**，`施工机制` §六 `#12` 族）；审查方拿同段运行期文本 `:171`（状态无关写法「暂无 `.js` 目标件」）作反证。**裁定：采**——该处在 F7 ① 授权面内，**发补修**。
  - **P2-1**：四件文档头的口径句自称「**分两种**」而 F7 定义为**三分**（缺 ① 状态型），会把 ① 误纳进「运行时措辞」。**裁定：采**（口径句补 ① 类）。
  - **P2-2**：删「参考性字段（字数↔卷数量级）」后，`规格与资料步:37`／`stage-spec:42` 的「联动校验**分档**」只剩一档、**词义成孤**（`参考性字段` 现全仓 0 命中）。**裁定：采**（改述去「分档」层）。
  - **P2-3／P2-4／P2-6＝规划方侧已处置**（规格引文形态订正／`[4]` 段 15 条逐条留痕〔本条即该留痕〕／草稿步正向起首**明示有意保留**）。
  - **P2-5**：`G26`／`G27` 两行**事实经独立实证成立**（审查方用工具自身常量/函数复现），但措辞精度两处不严——`G26` 称「报错文案」实为**候选行**（非阻断）；`G27`「恒 0」未标边界（目标文本含**字面 `\|`** 时除外）。**裁定：采**（台账行微修，不动判据）。
  - **审查方独立复核通过面**：**删净面全部自跑**（`每章字数`／`四规则`／`⑬⑭` 仅余 `CHANGELOG` 本批条目自述，正当；`3000-4000`／`单章字数` 零命中）；**卷数残留逐处判过**（除裁定保留的两处外，其余 6 处均为**量级／派生项语境、非字段**）；**孤句与断链**（`assets` 约束节现读 3 条、标题「三规则」计数自洽；`草稿步:114`／`规格与资料步:36`／`:39`／`:129` 删半句后**句子完整**）；**F7 其余 7 处分类判对**；口径句**不新增行使行号零偏移**（`:1072`／`:704`／`:852`／`:787` 逐行复核）✓；**`G20` 订正与实况相符**；**字段清账设计一致性**（草稿步读卷数 ↔ 标尺侧落点接得上）；**形态维**（四段式与三要素齐、定义表 6 行 ↔ 模板正文 6 行逐项对齐、七字段单元完好、未引入禁形态）；**门禁退出 0**、禁改面守住。
- **补修轮（施工方，2026-09-16）｜认账 ＋ 逐处回读**：**P1-1 施工方认账**——上轮核验请求的 F7 范围判据里写了会改述 `check.sh:157`，**实际未落盘**（首轮只含 `check.sh:2`／`:4`）；根因自述＝「把 F7 的分类枚举当成『已改清单』写入报告、未与逐处回读对账」；**纠正措施＝本轮每处改动以 `python` 直读磁盘逐行回读后才写入报告**。补修 **9 处／7 件**：`check.sh:157` 改述（P1-1）／四件口径句补第 ① 类成**三分**（P2-1）／`规格与资料步:37`＋`stage-spec:42` 去「分档」层（P2-2）／`测试与验收标准:90`（G26「候选文案·非阻断」）＋`:91`（G27 补边界「目标文本含字面 `\|` 时除外」）（P2-5）。
- **补修的独立复核（规划方，2026-09-16）｜4 项全过**：① `check.sh` 内的「本批零 .js 目标件」串**归零**（新文「**零 .js 目标件**」在位）；② 四件文档头的「行内出现的批次号**分三种**」**各 1**、旧「分两种」**全 0**、断言 4-3 的 token 仍**各 1**；③ 旧「联动校验分档」**全仓 0**、新式「联动校验：**承诺性字段**」两处**各 1**；④ `G26` 的「候选文案」与 `G27` 的边界句**各 1**；⑤ `check.sh` **退出 0**。**F7 施工后范围判据复核**：`grep -n '本批' scripts/*` ＝ **7 处**（① 状态型 **0** 处——五处已全改述／② 出处型 3 处保留／③ 运行态与示例串 4 处不动），**三类归零正确**。
- **本批至此全部落定**：F1–F9 ＋ 补修两轮（施工期 P0-1 裁定 ＋ 产物审查 7 条）／两轮规格审查（成稿 18 ＋ 窄域 13）／核验三步（第 1／2 步独立复跑、第 3 步＝双维产物审查 7 条）——**待归档**。
