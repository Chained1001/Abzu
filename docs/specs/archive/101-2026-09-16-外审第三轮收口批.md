# 101-2026-09-16-外审第三轮收口批

> 定位：处置 2026-09-16 会话内外审（四组子代理报告＋两轮补充勘误，经规划方逐条独立复核后裁定）——**19 项已裁 ＋ `#28` 回填 ＋ `命名标准:47` 未授权偏差归位**。覆盖：标准件同句互斥（B9）／预立法标记漏校准（B10）／`git add -N` 授权与红线 1 相抵／轻行措辞与真源分叉／自审四条双载体／缺口表列义侵蚀与乱序／G26・G27 行号引用改函数名／`check_content` 链接扫描不跳代码跨度／`check.sh` 头注检查字母缺失／`.gitignore` 补临时件模式／裸点名 16 处／`#28`（计数三半句）。
> 路径判定：**标准路径**——跨 4 类载体（`施工机制`／五份标准件／`skills/` 四件／`scripts/`＋`.gitignore`），动规则条文与守卫实现。真机走查：本批改 `skills/` 资产（用户可观测），随批尾合并走查。
> 自由度分布：[A]×19 [B]×10
> 必读输入：本规格 §一；`docs/specs/施工机制.md` §二／§三／§六／§八；`docs/standards/skill形态标准.md` §6；`docs/standards/规格写作标准.md` §4；`docs/standards/命名标准.md` §1／§2；`docs/standards/文档写作标准.md` §一.3.8；`docs/standards/测试与验收标准.md` §1.1／§4；`skills/abzu-outline/` 四件；`scripts/check_content.py`；`scripts/check.sh` 头注；`.gitignore`
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-16）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `docs/standards/skill形态标准.md:70` 三要素＝「字段定义、示例行、**约束**三件必须在」（与 `assets` 实件 `### 约束` 节相符）；同件 157 行正例作『字段定义表 + 示例行 + 定位注记约束』——**第三件两名** | 逐行 read |
| ② | `docs/specs/施工机制.md:44` 含「尚无实例，属预立法，随首批实例校准」句（其前带「单维核对」字样）与判据句（某批规格 §九 审查记录内出现该串即校准）；而 `archive/062:225/:230`、`archive/085:4` **均含该串且先于立法日** | 逐行 read＋grep |
| ③ | `施工机制:63` 改动面核对句含「次选 `git add -N <文件>` 后 `git diff`……只由规划方在核验期执行、不得由施工方执行」——**索引写操作**；红线 1 明列索引 ∈ 逐次确认面 | 逐行 read |
| ④ | `施工机制:39` 轻行触发写「单文件且 < 5 行**纯措辞**／格式」；真源 `规格写作标准:23` 写「单文件**错别字**／格式修正（<5 行）」——措辞宽于错别字 | 逐行 read |
| ⑤ | `规格写作标准:46-56` 与 `施工机制:50-55` **双载体**：两处各列自审四条全文；`:48` 自称「与 `施工机制` §二 同源，本件不复制细则（单一真源）」却复制了；且 `规格写作标准:50` 版**多出**「射程含首现术语……」半句（已漂移） | 逐行 read |
| ⑥ | `施工机制:90` 引「§4（**G1–G25**）逐行描述」；`测试与验收标准` §4 表实为 **23 行、序至 G27**，`G19` **排在 G21 之后**，G3/G5/G6/G7 已销号无说明 | grep 序列实测：`G1 G2 G4 G8…G18 G20 G21 G19 G22…G27` |
| ⑦ | `测试与验收标准:90`（G26 行）引 `check_spec.py`「`:1072`」、`:91`（G27 行）引「`:704`」——**均已被检查 H/I（+191 行）推离**（现位 `:1256`／`:719`；写入时点 `3e81056` 处核为准确——漂移系 `d57f98a`/`757221e` 所致） | `git show 3e81056` 与当前 grep 双取证 |
| ⑧ | `测试与验收标准:18` 含「**052 批**的教训」——`archive/` 起于 059，052 为旧仓批次而无「旧仓」限定（同件 `:69/:83` 均带限定） | 逐行 read＋ls |
| ⑨ | `docs/standards/命名标准.md:47` 例句现文「如 `命名标准:63` 开发测试：**类目预留**（`scripts/{skill 名}/scripts/` 尚未启用）＋…」；archive/097 F20 的 [A] 目标为『（`scripts/` 下 `test-*` 类目尚未启用，见该行关联实例判据）』——**未授权改写**（换上的路径串与 63 行位置列 scripts/仓库级、59 行位置列 skills/{skill 名}/scripts/ 均不相符，自成第三形）；63 行现文含真判据串『关联实例判据：`ls scripts/test-*` 命中 ≥1』 | F20 全文逐字取＋对照 |
| ⑩ | 裸点名（口径：13 名含 `AGENTS.md`；豁免＝反引号跨度／链接文字与目标／自身 H1）：`施工机制` **13 处**（`:5/:43/:45/:48×2/:63/:74/:86/:87/:128/:167/:175/:177`）＋ `AGENTS.md` **3 处**（`:3/:60×2`）＝ **16 处** | 逐位置解析实跑 |
| ⑪ | `skills/abzu-outline/assets/outline-basic-spec-template.md:20` 说明列「读者枚举 **＋** 三件（阅读习惯／核心诉求／阅读口味）」——读作 4 项；同件模板正文 3 槽位、附录 31 行『读者枚举（含读者画像三件）』读作 3 项——**三方两读** | 逐行 read |
| ⑫ | `skills/abzu-outline/SKILL.md:18`「先报建设状态行 → 如实说明读不到 → **停下请作者换带文件工具的会话**」——未吸收 `测试与验收标准` §3 已登记前提（有模型自述无文件工具而实际具备→换模型重试） | 逐行 read |
| ⑬ | `skills/abzu-outline/references/outline-stage-spec.md:44/:45`：完备校验句未列「可选项未填」与「④占位」的豁免；`:45` 消歧注引「`3.5`」无未建标注 | 逐行 read |
| ⑭ | `skills/abzu-outline/references/outline-workflow.md:44` 中断契约以三域具名列出，未标依 088 V-05 裁定变体的标记 | 逐行 read |
| ⑮ | `scripts/check_content.py` 的链接扫描对**每行**全文跑 `MD_LINK`，不跳行内代码跨度与围栏——`archive/078:16/:30` 的示例串（在代码跨度内）每批产出 **3 条假候选**；同族实现 `check_spec.check_writing` 已跳 | 实跑门禁：断链候选恰 3 条 |
| ⑯ | `scripts/check.sh:10/:138` 的 [4] 段枚举各项**无检查字母**；`AGENTS.md:70` 已指其为「[4] 段 A-I」真源——真源失守（现有 `[A] 逐字` 系自由度标记，非检查号） | 逐行 read |
| ⑰ | `.gitignore` 现七行，无 `*.patch-tmp`／`*.shapecheck.md`——`patch_file.py` 的同目录临时件异常残留时会成未跟踪垃圾 | cat 实读 |

## 二、文件级改动清单

### 面一·机制与标准件（F1–F15＋F27）–F15）

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F1 | `docs/standards/skill形态标准.md:157` | 「字段定义表 + 示例行 + **定位注记约束**」→「字段定义表 + 示例行 + **约束**节」（与 `:70` 三要素及 `assets` 实件对齐） | [A] | 外审 B9（复核成立，降为一处措辞错） |
| F2 | `docs/specs/施工机制.md:44` | 「『单维核对』一侧**尚无实例，属预立法，随首批实例校准**；关联实例判据：…」→「单维核对的**实例已在册**（`archive/062`／`archive/085` §九/头注）——本条**已非预立法**」；判据句随之删 | [B] | 外审 B10（判据命中未校准，违 `规格写作标准` §8） |
| F3 | `施工机制:63` | 删「；次选 `git add -N <文件>` 后 `git diff`」及「`git add -N` 是索引写操作…不得由施工方执行」整段次选路径——只留首选 `git diff --no-index`（含其退出码注） | [A] | 外审中1（红线 1 明列索引；修法＝删次选而非加确认） |
| F4 | `施工机制:39` | 「单文件且 < 5 行**纯措辞**／格式」→「单文件且 < 5 行**错别字**／格式」 | [A] | 外审中2（真源分叉） |
| F5 | `施工机制:39`（轻行括注内） | 补一句：「涉 `AGENTS.md` 者不得走轻行（该件修改另须作者确认，宪法 §六.1）」 | [A] | 外审中3 |
| F6 | `施工机制` §三（过程产物两项处）＋`:179` | §三 补「git 历史＝提交本身（追溯第三腿，非过程产物）」半句；`:179` 提交信息格式句尾补「主题宜含批号（NNN）」 | [B] | 外审中4 |
| F7 | `施工机制:50-55` ↔ `规格写作标准:46-56` | **自审四条归一**：`规格写作标准` §4 为**真源**（保留全文，含射程半句）；`施工机制` §二 的四条全文改为一行：「规格自审四条（真源＝`规格写作标准` §4；缺任一条不得产出施工交底）：锚点／断言／禁止／范围与期望同源——细则见真源。」 | [B] | 外审 5.1（双载体＋已漂移） |
| F8 | `docs/standards/命名标准.md:54` ↔ `skill形态标准:16` | 两处壳份数措辞对齐为一形（「每 skill（域）一份」），其中一处互指 | [B] | 外审 5.2 |
| F9 | `docs/standards/文档写作标准.md:118` | 「入口与清单型文档不计兜底阈值」→「以表格与编号条目为主要结构者不计兜底阈值（适用范围见 §一.3.8）」 | [A] | 外审 5.4 |
| F10 | `施工机制:90` ＋ `测试与验收标准` §4 表 | `:90`「§4（G1–G25）逐行描述」→「§4 缺口表**逐行**」（去范围引用）；§4 表 **G19 移至 G18 与 G20 之间**；表末补销号注一行：「G3/G5/G6/G7 已销号（065/072 批处置，见各批规格）」 | [B] | 外审 6.1＋100 批漏核回写第五处（本批自查认账） |
| F11 | `测试与验收标准` §4 表 | **四列化**：`编号｜缺口｜状态/处置｜触发条件`——现塞在「触发条件」列的状态与处置结论（G13/G16/G20/G21/G22 等约十行）拆入第三列。**混合格切割判法（成稿审查 P2-2）**：单格内「前半处置记录＋尾部触发条件」者（如实测 G15 行），切割点＝**首次出现触发语**（「触发＝」／「出现…时」）处——前半入状态/处置列、尾部留触发列 | [B] | 外审 6.2（列义侵蚀） |
| F12 | `测试与验收标准:90/:91`（G26/G27 行） | 行号引用改**函数名**：「`:1072`」→「`check_reconcile()` 判一的候选文案行」；「`:704`」→「`check_swallow()` 的现行字面计数（`cur` 统计行）」——**成稿审查 P1-1 订正**：该行在 `check_swallow()` 内（`:719`，`:655` 起）；`_assert_refs()` 只做提取与反转义（G27 行已有的 `_assert_refs()` 指称是 token 反转义口径，正确——**不得**把字面计数也归它，否则同串两现一真一错）；§4 他处引 `check_*.py` 行号者同批改函数名或删。**施工序（成稿审查 P2-1）**：F12 先于 F10（G19 移位使 G20 起整体 -1 行，会推漂 `:90/:91` 字面锚——按「G26/G27 行」语义定位即无虞） | [B] | 外审 6.3；写入时点准确、H/I 推离已双取证（锚点⑦） |
| F13 | `测试与验收标准:18` | 「052 批的教训」→「**旧仓** 052 批的教训」 | [A] | 外审 6.4 |
| F14 | `施工机制` §六 反模式清单（`#27` 行后） | 新增 `#28`：「计数类发现须附**实测命令＋单位**；同一张表的数须**同源同口径**；同一事实换工具／换基线复量不一致时，先判工具口径再下结论」｜正确方式列同 ｜事故出处：「2026-09-16 外审三轮：① UTF-16 码元与字节混标（doc-budget 假触发）；② `Measure-Object -Line` 与 `ReadAllLines` 行数不一致（1401 vs 1512）；③ 换基线 commit 不声明（`578815a` 误作写入时点）——单位／工具／基线三个失效面」 | [A] | 外审两轮自纠＋规划方核证（`#28` 三半句定稿） |
| F15 | `施工机制` 13 处＋`AGENTS.md` 3 处（锚点⑩清单） | 裸点名**逐处加反引号**（16 处；`:5` 旧仓件名同加——同族件「依据」行横比均带反引号，属本件内部不一致而非定义豁免） | [A] | 外审中10（计数三轮收敛：4-6→14→13→**16**，口径已声明） |
| F27 | `docs/standards/测试与验收标准.md` §4 表 **G1 行**（现 `:69`；F10/F11 改表后按「G1 行」定位） | 状态/处置列**两处引述订正**（**F9 的第三载体**——F9 改机检锚点行后，本行的引述与判断随之失真）：① 删半句——现状 `…清单型文档）」，其机检锚点行用「入口与清单型文档」），扩展时须一并立法` → 删后 `…清单型文档）」），扩展时须一并立法`；② 「；入口与清单型文档不计兜底」→「；以表格与编号条目为主要结构者不计兜底」 | [A] | 核验期规划方自查（F9 只改 `:118` 一处，其第三载体漏列——`#2`／`#21` 族：局部改动未查牵连面） |

### 面二·skill 资产（F16–F20）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F16 | `assets/outline-basic-spec-template.md:20` | 「读者枚举 **＋** 三件（…）」→「读者枚举（**含**三件：阅读习惯／核心诉求／阅读口味）——由平台＋题材推导」 | [A] | 外审中6（三方两读，影响产物对规字段数判） |
| F17 | `SKILL.md:18` | 「停下请作者换带文件工具的会话」前补半句：「先换模型重试一次（有模型自述无文件工具而实际具备——已登记前提，见 `测试与验收标准` §3；**维护出处，运行时不必加载**），仍无则停下请作者换带文件工具的会话」 | [B] | 外审中9（假降级） |
| F18 | `references/outline-stage-spec.md:45` | 「与 `3.5` 的『对规格』」→「与 `3.5`（**Stage 3 步号，未建**）的『对规格』」 | [A] | 外审中5（前向引用消歧） |
| F19 | `references/outline-stage-spec.md:44` | 完备校验句补：「（比较想写的可空；**必填性列「可选」者未填不计待定**；④标尺占位见⑤产物对规）」 | [A] | 外审中7 |
| F20 | `references/outline-workflow.md:44` | 「建议使用对应域命令（…）」后补「（**依 088 V-05 裁定**，三域具名列出）」 | [A] | 外审中11（变体标记） |

### 面三·工具与配置（F21–F23＋F26／F28／F29）–F23＋F26／F28）–F23＋F26）–F23）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F21 | `scripts/check_content.py`（链接扫描处） | 扫描**跳过行内代码跨度与围栏块**（对齐 `check_spec.check_writing` 的跨度切分与围栏配对思路——复用其算法形态，不跨件 import）：跨度内与围栏内的 `MD_LINK` 命中不计——`archive/078` 的 3 条示例串候选**归零** | [B] | 外审 7.1（实跑实证：3 条候选全在代码跨度内）；**成稿审查 P1-4**：围栏判据无断言覆盖（3 条候选恰全在跨度内，只实现跳跨度断言 6 亦绿）——**施工交付须附两判据各自生效的自证输出**（行内跨度样例与围栏内样例各一、各归零，夹具落 `.tmp/` 用毕即删） |
| F22 | `.gitignore` | 追加两行（逐字）：「`*.patch-tmp`」「`*.shapecheck.md`」 | [A] | 外审 9.2 |
| F23 | `scripts/check.sh:10`（头注）＋`:138`（echo） | [4] 段各项前加检查号（**用「检查A」…「检查I」全词形**，与自由度 `[A]` 标记消歧）：「检查A 断言自噬／检查B 计数重算／检查C `[A]` 逐字／检查D 写作检查／检查E 三方对账／检查F 禁改面禁词核对／检查G 自由度分布核对／检查H 核销表汇总核对／检查I 锚点对源核对」——两处同步 | [A] | 外审 9.3（AGENTS:70 真源失守） |

### 面四·偏差归位（F24）与过程产物（F25）

| # | 文件:位置 | 改动 | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F24 | `docs/standards/命名标准.md:47`（分野句例句） | 「如 `命名标准:63` 开发测试：**类目预留**（`scripts/{skill 名}/scripts/` 尚未启用）＋ **条文预立法**（该类目下无实例可校准）」→「如本表「开发测试」行：**类目预留**（`scripts/` 下 `test-*` 类目尚未启用，**见该行关联实例判据**）＋ **条文预立法**（该类目下无实例可校准）」——**还原 `097` F20 语义＋去自指行号**（`命名标准:63` 形不在 §5 引用格式表）。**`:63` 不动**（它是指针落点，真判据串在位）。**外审提的「新落点 `skills/{skill 名}/scripts/`」不采**：例句指开发测试行，其位置列＝`scripts/`（仓库级），非 `:59` 运行时脚本行的 `skills/…`——换上即成第四形 | [A] | 红线 4 呈报；作者裁定「还原＋新落点」；字面经 F20 全文逐字核证 |
| F25 | `CHANGELOG.md` | 过程产物一行（条首串「外审第三轮收口（101）」，列 0、以连字符加空格起首、≤400 字符） | [B] | `施工机制` §三 |
| F26 | `scripts/check_spec.py`（`static_report` 的格数校验打印处，约 `:1443`–`:1449`——按内容定位） | **格数校验行计入 findings**：现对 `_cell_mismatch()` 记录**裸 print**（不入发现列表、不入「有预警 N 条」三态计数）——与本件 `:1459`–`:1466` 自家注释（「漏计则『· 有预警 N 条』少计、屏上发现行多于计数行」）相悖、与 078 F1 族的「候选行计入 findings」规相悖。改法：格数行收集进列表、与 `i_lines` 等一并并入 `extra`（计数入三态） | [A] | 外审已裁「采」而规格漏列（规划方自查＋成稿审查 P1-3 双重确认）；属**潜伏缺陷**（101 表格格数全对故未触发） |
| F28 | `scripts/check_spec.py`（`_strip_wrap()` 归一化处，按函数定位） | 归一化增一处：**剥加粗标记**——该函数内补剥标记一环，docstring 补事故出身句，逐字加「另剥加粗标记（规格 A 级行惯以加粗标改动词、实文不含，不剥即恒报未落实假阳性——101 批 F1 行实例）」 | [A] | 核验期规划方自查（**复发族回填**——F1 行系本批实例）；「只修实例不回填族＝本项失守」（`施工机制` §六 反模式清单表头注） |
| F29 | `scripts/check_spec.py`（**7 处**：`:1` 用途句／`:10` 用途枚举末／`:77`／`:78` 呈报守恒段／`:1393`／`:1395`／`:1396` `static_report` docstring） | 「七类」→「九类」共 **7 处**（枚举处并补 H／I）：① 枚举末「——七类一律」补两项后收「——九类一律」；② 三处 `A／B／C／D／E／F／G 七类` 改「A／B／C／D／E／F／G／H／I 九类」；③ 另两处「七类」单字改「九类」。**到位形之一**：「A／B／C／D／E／F／G／H／I 九类」 | [A] | 产物审查观察 3 升格（检查 H／I 添加时未回写计数——`093` F25 同型先例；属 `#2`／`#21` 族） |

## 三、验收断言（基线全部 2026-09-16 实跑）

### 断言 1｜措辞与对齐（F1/F4/F9/F13/F16）

```bash
grep -c '定位注记约束' docs/standards/skill形态标准.md
```

期望 **0**。基线 **1**（`:157`）。

```bash
grep -c '纯措辞' docs/specs/施工机制.md
```

期望 **0**。基线 **1**（`:39`）。

```bash
grep -c '入口与清单型文档' docs/standards/文档写作标准.md
```

期望 **0**。基线 **1**（`:118`）。

```bash
grep -c '旧仓 052 批' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '枚举 ＋ 三件' skills/abzu-outline/assets/outline-basic-spec-template.md
```

期望 **0**。基线 **1**（`:20`）。

### 断言 2｜机制修正（F2/F3/F5/F6/F7/F14）

```bash
grep -c '尚无实例，属预立法' docs/specs/施工机制.md
```

期望 **0**。基线 **1**（`:44`）。

```bash
python -c "print(open('docs/specs/施工机制.md',encoding='utf-8').read().count('git add -N'))"
```

期望 **0**。基线 **2**（`:63` 行内出现 2 次——次选路径句与索引写说明句各 1；**本条计出现次数**，与 F3「删两处出现」语义同源，口径声明见 `规格写作标准` §5.8；反模式 `#8` 系 `git add -A` 不命中）。

```bash
grep -c '涉 `AGENTS.md` 者不得走轻行' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '批号（NNN）' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '不凭对话记忆' docs/specs/施工机制.md
```

期望 **0**（F7 归一后施工机制只留指针）。基线 **1**（`:52`）。

```bash
grep -c '^| 28 |' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 3｜缺口表（F10/F11/F12/F27）

```bash
grep -c 'G1–G25' docs/specs/施工机制.md
```

期望 **0**。基线 **1**（`:90`）。

```bash
python -c "import io,re;L=io.open('docs/standards/测试与验收标准.md',encoding='utf-8').read().split(chr(10));s=[l.split('|')[1].strip() for l in L if re.match(r'^\| G\d+ ',l)];i=s.index('G19');print(s[i-1],s[i],s[i+1])"
```

期望输出 **G18 G19 G20**。基线 **G21 G19 G22**（乱序实证）。

```bash
grep -c 'check_reconcile()' docs/standards/测试与验收标准.md
```

期望 **≥1**。基线 **0**。

```bash
grep -c ':1072\|:704' docs/standards/测试与验收标准.md
```

期望 **0**。基线 **2**（`:90` G26 行含 `:1072`、`:91` G27 行含 `:704`——各占一行；**成稿审查 P1-2 订正**原记「合计 1 行」系误述）。

```bash
grep -c '入口与清单型文档' docs/standards/测试与验收标准.md
```

期望 **0**。基线 **1**（`:69` G1 行）。

```bash
grep -c '其机检锚点行用' docs/standards/测试与验收标准.md
```

期望 **0**。基线 **1**（`:69` 同行）。

### 断言 4｜裸点名归零（F15，口径＝锚点⑩）

```bash
python -c "import io,re;names=['施工机制','测试与验收标准','规格写作标准','命名标准','skill形态标准','文档写作标准','术语标准','触发与交互标准','版本与分发标准','运行环境标准','安全与合规标准','产物契约标准','AGENTS.md'];n=0
for p in ['docs/specs/施工机制.md','AGENTS.md']:
    for ln,raw in enumerate(io.open(p,encoding='utf-8').read().split(chr(10)),1):
        if ln==1: continue
        spans=[];op=None
        for i,ch in enumerate(raw):
            if ch==chr(96):
                if op is None: op=i
                else: spans.append((op,i)); op=None
        lz=[m.span(k) for m in re.finditer(r'\[([^\]]*)\]\(([^)]*)\)',raw) for k in (1,2)]
        for nm in names:
            for m in re.finditer(re.escape(nm),raw):
                if not (any(a<=m.start()<b for a,b in spans) or any(a<=m.start()<b for a,b in lz)): n+=1
print(n)"
```

期望 **0**。基线 **16**（锚点⑩同口径实跑）。

### 断言 5｜skill 资产（F17/F18/F19/F20）

```bash
grep -c '换模型重试' skills/abzu-outline/SKILL.md
```

期望 **1**。基线 **0**。

```bash
grep -c 'Stage 3 步号，未建' skills/abzu-outline/references/outline-stage-spec.md
```

期望 **1**。基线 **0**。

```bash
grep -c '未填不计待定' skills/abzu-outline/references/outline-stage-spec.md
```

期望 **1**。基线 **0**。

```bash
grep -c '依 088 V-05 裁定' skills/abzu-outline/references/outline-workflow.md
```

期望 **1**。基线 **0**。

### 断言 6｜工具与配置（F21/F22/F23/F28）

```bash
bash scripts/check.sh > .tmp/v.txt 2>&1; echo $?; grep '断链: docs/' .tmp/v.txt | grep -vc '@ .tmp/v.txt'; rm -f .tmp/v.txt
```

期望 **退出 0｜断链 0**。基线 **退出 0｜断链 3**（全在 `archive/078` 示例串，均为 `断链: docs/` 形）。**口径（成稿审查 P0-1）**：裸 `断链:` 计数实测 **4**——第 4 条是检查 A 扫**本规格自身**断言命令产出的**自指回声行**（`· 跳过…: 断链: @ .tmp/v.txt`，G27 族）；F21 只消真候选、消不掉回声（施工期本规格不归档）——故本条**排除 `@ .tmp/v.txt` 回声行后**计数真候选（施工期裁定已证 P0-1 的口径假设失效：回声行原样带出 `docs/`，裸计数在制期恒 ≥1；真候选三证归零见 §九）。

```bash
grep -c 'patch-tmp\|shapecheck' .gitignore
```

期望 **2**。基线 **0**。

```bash
grep -c '检查A 断言自噬' scripts/check.sh
```

期望 **2**（头注＋echo 两处——F23 明写「两处同步」，成稿审查 P0-2 订正原期望 1 与之互斥）。基线 **0**。

```bash
grep -c '检查I 锚点对源' scripts/check.sh
```

期望 **≥1**（头注＋echo 两处）。基线 **0**。

```bash
python -c "import sys;sys.path.insert(0,'scripts');import check_spec as c;print(c._strip_wrap('a + **约束**节'))"
```

期望输出 `a + 约束节`。基线 `a + **约束**节`。

```bash
python scripts/check_spec.py --static "docs/specs/101-2026-09-16-外审第三轮收口批.md" | grep -c '^· \[A\] 未落实或已漂移'
```

期望 **3**（`F24`／`F26` 三条引述形态类假阳性，判读见 §九）。基线 **5**（取值时点＝F27 施工前、F28 未落时的本修订起点；**F27 落盘后为 4**——换时点复量差 1 属口径而非错数，`#28` 第二半句）。**口径**：行首锚定 `· [A] 未落实或已漂移`——裸词计数会连带检查 A 中性行内的同名回显（自指回声族，同 P0-1）。

```bash
grep -c '七类' scripts/check_spec.py
```

期望 **0**。基线 **7**（`:1`／`:10`／`:77`／`:78`／`:1393`／`:1395`／`:1396`）。**口径**：`grep -c` **数行**——7 处分布在这 7 行各 1 次，此处行数与处数相等（换 `read().count()` 复量亦为 7 处，同数）。

### 断言 7｜偏差归位（F24）与过程产物

```bash
grep -c '命名标准:63' docs/standards/命名标准.md
```

期望 **0**（自指行号形退出）。基线 **1**（`:47`）。

```bash
grep -c '本表「开发测试」行' docs/standards/命名标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '外审第三轮收口（101）' CHANGELOG.md
```

期望 **1**。基线 **0**。

## 四、规格自审（四条）

1. **锚点现场核过**——§一 十七行 2026-09-16 逐条实跑（read／grep／`git show 3e81056` 双基线取证／逐位置解析）；引文仅在该行现场核过处给出。
2. **断言实跑过**——断言 1–7 的**基线全部本会话实跑**（各条括注）；断言 3 第二条与断言 4 的期望由同口径脚本当场复算。
3. **禁止事项写明**——见 §五。
4. **范围与期望同源**——各断言的基线与期望取自同一命令同一文件；断言 4 的计数口径与锚点⑩逐字同源。

**附加项**：清单完备（F1–F29 与外审三轮裁定逐条对齐——**F26 系成稿审查 P1-3 补的漏项**：外审已裁「采」而初稿漏列，修订轮补入；**F27 系核验期规划方自查补的漏项**：F9 改机检锚点行后，G1 行的引述与判断随之失真，故补入；**F28 系同轮族级回填**——规格 A 级行以 `**` 标改动词、检查 C 不剥即恒报「未落实」（F1 行为实例），落点在工具侧；**F29 系产物审查观察升格**——检查 H／I 添加时未回写「七类」计数（7 处））——19 项已裁＋`#28`＋F24 归位＋F25 过程产物）；**回写清单触发自查**——F21 动 `check_content.py`、F23 动 `check.sh` 头注：四处回写（`测试与验收标准` §1.1／`AGENTS` §七——本批未新增检查、仅加标号，§1.1 不需改；头注自身即回写面）已并入 F23。

## 五、禁止事项

- **禁改**：`AGENTS.md` 除 F15 的 3 处裸点名外一字不动；`docs/specs/archive/` 一概不动。
- **禁改**（只改所指处）：`scripts/` 只改 F21（`check_content.py` 扫描判据）、F23（`check.sh` 两行标号）、F26（`check_spec.py` 格数行入计数）、F28（`_strip_wrap()` 剥加粗标记）与 F29（`check_spec.py` 的「七类」回写为九类）——其余零 diff；`测试与验收标准` §4 表四列化**只拆列不改文**（缺口描述文字原样迁移；**F27 的 G1 行引述订正与 F12 的 G26／G27 行号改函数名除外**）。
- **禁**：F24 不得采用 `skills/{skill 名}/scripts/` 形态（经核与例句所指行不符——见 F24 行）；`:63` 行不得改动。
- **禁**：F2 不得删除「单维核对」路径指引本身（只删预立法标记与假陈述）。
- **禁**：为断言通过而放宽判言 4 的口径（口径＝锚点⑩，已锁死）。
- **禁**：联网、采集、跑安装器；改任何断言期望值求绿（红线 4）。

## 六、被否决的替代方案

- **F24 新落点用 `skills/{skill 名}/scripts/`（外审提议）** → 否决：例句指「开发测试」行（位置列 `scripts/` 仓库级）；换上即成第四形，且与 `:59` 运行时脚本行混淆。
- **G26/G27 行号订正为现值 `:1256`/`:719`** → 否决：行号必随加行再漂（本批 F10/F11/F12 自身就会改 `测试与验收标准` 行序）——改函数名一劳永逸。
- **裸点名以「减法式」（总命中−反引号内）验收** → 否决：本轮三数之差（4-6/14/13/16）已证减法与位置配对各有假阳/假阴——验收用逐位置解析（断言 4）。
- **`.gitignore` 一并加 `.tmp/` 下批 JSON** → 不需：批 JSON 已落 `.tmp/`（红线 2 辖）。
- **外审第 9.1 条（命名标准三条机械化检查）** → 不采（现时）：无事故出身（`AGENTS` §四）；登记 §八 候选，触发＝首个实例。

## 七、本批核销（来源：`100` §八全部）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `100` §八 1 | 平台每章字数数据待重建 | **仍待** | 触发＝正文域建设批 |
| `100` §八 2 | 正文域契约点 4 及两处指针改述 | **仍待** | 触发＝正文域建设批 |
| `100` §八 3 | 同类体量／单章字数待建消费点 | **仍待** | 触发＝出现真消费者 |
| `100` §八 4 | 本批（100）改动面大 | **作废** | 自指提示，非开放项 |

- 汇总（恒列五档、无该档计 0）：`已履行 0｜作废 1｜本批处置 0｜仍待 3｜待作者 0 ＝ 4` ✓

## 八、开放项

1. **命名标准三条机械化检查**（文件名禁字符／路径正斜杠／标准件名与目录一致）——无事故出身，**候选**；触发＝首个实例（外审 9.1 不采的登记位）。
2. **§4 表 G 行射程的行号引用全扫**——F12 只改 G26/G27 与明显者；表内其余引代码行号处（如有）触发＝下次触碰该表时全改函数名。
3. **`施工机制` §二 轻行「复盘驱动治理微调」的豁免条件与 F5 新句的并读复核**——触发＝下次走轻路径时。
4. **本批改动面（29 项／16 件）**：施工期 P0 族 ≥2 时按止损条收窄。
5. **检查 I 的 CJK 引号归一**：`_anchor_norm()` 现剥反引号／`*`／空白，不剥「」『』——目标件文本**自身含**「单维核对」类引号时，去引号的规格引文比对落空（本批锚点②实撞）。触发＝下次出现同类实例时在归一化中剥 CJK 引号（双面剥，降低假阳）。

6. **检查 C 的引述形态类假阳性**（标记后引述被否方案／现状侧、`『』` 记嵌套引号）：本批 3 条实例（`F24`／`F26` 行），属已登记 `G24` 族；触发已现——处置＝下批裁定「核对面只认标记后非引述块」或归一化 `『』`（不扩本批改动面）。

7. **运行时资产的溯源标注体例不统一**——`skills/abzu-outline/references/outline-workflow.md:44` 的 F20 新句挂「依 088 V-05 裁定」而**无**「运行时不必加载」标注，同批 F17（`SKILL.md:18`）则按 `archive/088` §八 13 口径写了「维护出处：…；运行时不必加载」；触发＝下次触碰该件或统一运行时溯源体例时（本批不扩 skills 改动面）。
8. **围栏判据族（潜伏）**——`check_content.FENCE_LINE`（F21 新扩射程）与 `check_spec.FENCE_BLOCK` 同形，均不认 4 反引号围栏与含非字母 info 串（如 `archive/068:113` 的 ```` ```；全仓相对链接条数… ````）→ 围栏态错位可静默吞真候选；产物审查内存夹具实测**现仓影响 0**（被视作围栏内的 1111 行含链接语法 0 处）；触发＝出现吞并实例或下次触碰该判据。
9. **§4 表混合格残余**——G20 状态／处置列以「：」收尾（宾语落在触发列）、G24 未按 G25 之例拆列（修法留在触发列，切割判法未及）；触发＝下次触碰该表（受 §五「只拆列不改文」约束，本批不扩）。

## 九、审查记录

- **外审沿革（2026-09-16，会话内三轮）**：四组子代理报告（约 30 件发现）＋补充勘误两轮。规划方逐条独立复核：**事实错误四处不采**（交底 1/6「冲突」——头注明文已解；「每次施工读全文」——按节点点名机制；G22「移缺口表」——本就在缺口表；doc-budget 假触发——UTF-16 码元/字节混标，17,160 字符 < 2 万阈值）；**评审自撤一条**（`文档写作标准:55`——交叉复核机制生效的正面例）；**评审两轮自纠三笔同族错**（码元/字节、两行数工具、换基线 commit——单位/工具/基线三失效面，直接构成 `#28` 的事故出处）；**评审对我 G26/G27 账的「更正」经 `git show 3e81056` 双取证推翻**（写入时点两行号皆准，漂移系检查 H/I +191 行所致——其算术误以 `578815a`〔H 之后〕为写入基线）；**其「新落点 `skills/…`」提议经 `:63`/`:59` 位置列对照不采**（例句指开发测试行）。
- **裸点名计数收敛过程**（`#28` 的现场演示）：规划方初判 ~4-6（整行无反引号的粗筛）→ 外审 14 → 外审自正 13 → 规划方逐位置解析（口径声明）**16**＝外审 13（施工机制，含 `AGENTS.md` 名）＋规划方 3（AGENTS.md 件内）。三个中间数各差在口径未声明，非错数。
- **外审预核（2026-09-16，批式实跑）**：27 条断言基线逐条复核——**当时**记 26 条相符、1 条口径错已订正；**成稿审查另证 2 条不符**（断链回声行致裸计数基线应为 4——见断言 6 口径注；`:1072`/`:704` 基线应为 2 非原记 1）——预核的「逐条复核」在**自指回声**与**双行计数**两处失察，已同批订正（预核结论的可信度以此两处为限）（`git add -N` 基线写 1、实测出现 2——`grep -c` 数行而 F3 语义是删两处出现，`#28` 第二半句当日即现；已改计出现次数口径，基线 2）。另核：A1 五条基线逐条一致／断言 4 `n=16` 原样复现／`G1–G25` 破折号码点 U+2013 与 grep 模式同字符／G19 乱序实证（`G21 G19 G22`）／F24 证据链（`:63` 位置列＝`scripts/` 仓库级、`:59`＝`skills/…`、现文自成第三形）成立。**送审提示**：F21 的「跳跨度」与「跳围栏」两判据须各自独立核（缺一即断言 6 假绿）。
- **成稿审查·第一轮**（独立上下文只读 subagent，2026-09-16）｜findings 共 **8：P0:2 P1:4 P2:2**｜结论 **须修订→已全部修订**。**P0-1**（断言 6 裸 `断链:` 计数含**自指回声行**——检查 A 扫本规格自身断言命令的跳过行内嵌该 token，F21 消不掉、施工期恒 ≥1）→ 收窄为 `断链: docs/` 形＋口径注；**P0-2**（F23「两处同步」与断言 6 期望 1 互斥）→ 期望改 2；**P1-1**（F12 把 `cur` 行误归 `_assert_refs()`——实在 `check_swallow()` 内）→ 改归属并保 G27 行既有的正确 `_assert_refs()` 指称；**P1-2**（`:1072`/`:704` 基线实 2 非 1＋预核声明失真）→ 基线订正＋§九 预核段补记；**P1-3**（格数校验行漏项——外审已裁「采」而 §二 无 F 项，附加项自述失守）→ **补 F26**；**P1-4**（F21 围栏判据无断言覆盖——3 条候选恰全在跨度内，只跳跨度断言亦绿）→ F21 补施工自证交付；**P2-1**（F12 锚 `:90/:91` 会被 F10 的 G19 移位推漂）→ 补施工序（F12 先于 F10／语义锚）；**P2-2**（F11 混合格切割无判法）→ 补切割点判法。**审查方独立复核通过面**：F24 三形对照成立（含「§5 引用格式表无文件名:行号形」实证）／F2 不伤 §五 指引且 §五 无预立法标记不需同步／F15 逐行一致（n=16 复现）／F10/F11 可执行／互斥无冲突／禁改面与核销逐项对上／基线抽查相符／**交底第 1 点定性获证**（检查 I 零命中＝施工前预期；断链 3＝F21 面——唯回声行漏判已升 P0-1）。
- **施工期裁定（规划方，2026-09-16）**：**断言 6① 未决项＝按 A 判属预期**——施工方按红线 4 停摆申报（未改断言，判断正确）：收窄形 `断链: docs/` 的自指回声行格式为「· 跳过…: 断链: docs/ @ .tmp/v.txt」（回声 token 原样带出 docs/，P0-1 口径注「回声不带 docs/ 形」的假设失效）。**真候选三证归零**（施工方全仓直跑＋check.sh 全输出；规划方复核第三证——直接调 `_check_refs(gov, assets)` 全仓＝**合计 0 候选**，078 的 3 条归零、无新增）。回声随本规格归档自然消失，断言文本不改。
- **核验三步·第 1／2 步（规划方，2026-09-16）｜独立复跑**：第 1 步——git status 16 件与 §二 清单逐名一致、**archive 零改动**、117 增 69 删（施工期当刻；**产物审查修订后现态＝施工面 16 件 127 增／77 删**，另本规格自身在改——`#28` 换时点必同注）；第 2 步——抽全复跑：A1 定位注记约束＝0／A2 `git add -N` 出现次数＝0／A3 旧行号引用＝0／**A4 裸点名逐位置解析＝0**（16 处全归零）／A5 G19 邻序＝G18 G19 G20／A7 检查A 断言自噬＝2／A8 自指行号形＝0／A9 CHANGELOG＝1／A10 反模式 28 行＝1；门禁退出 0（G 不符 0／H 不符 0／I 异常 4 条＝§一 锚点为施工前快照、F2/F24/F16/F17 恰改其引用行——按 F 行语义定位，交底 §7 预授权）。**全部与施工方自述一致**。施工方 8 条 P1 假设申报逐条核过（加粗记法两案／F17 内层不加粗防提前闭合／F18 引号保形／F23 echo 行防命令替换／F11 边界标点消去与空列紧凑形／F7 配套订正 :48 声明句——方向正确、四条全文未动／F6a 落位）——**均采**；P2 两条记录（:65 指针顺延；CRLF 既存）。
- **核验·F27／F28 复跑（规划方直改后，2026-09-16）｜独立复跑**：F27——`grep -c '入口与清单型文档' docs/standards/测试与验收标准.md`＝**0**（基线 1）、`grep -c '其机检锚点行用'` 同件＝**0**（基线 1）；F28——单元探针 `_strip_wrap('a + **约束**节')`＝**a + 约束节**（基线原样返回）、检查 C 的 `· [A] 未落实或已漂移` 行数 **5 → 3**（余三条皆引述形态类假阳性，判读见上）；门禁 `bash scripts/check.sh` 退出 **0**（检查 G 头注↔清单 18／10＝28 行、不符 0；检查 H 不符 0；检查 I 异常 4＝§一 施工前快照）；`git status` **17 件＝施工面 16 件（§二 清单逐名一致）＋本规格自身**（本规格为在制件，不计入施工改动面）。**首跑暴露自指回声**：F27 的新断言以裸词计数得 **4≠3**——检查 A 的中性行内回显同名 token（同 P0-1 族）；改行首锚定 `· [A] 未落实或已漂移` 后复跑得 3。
- **规划方自查（记账）**：落本两行时以双引号 python -c 内嵌反引号文本，bash 命令替换把回声 token 与 git status 全文代入（#18／#20 族——长串含反引号一律走 patch_file.py 或 Edit，双引号 shell 内联是禁径）——已重写，lint 复绿。
- **核验期规划方自查·新增 F27（2026-09-16）**：核验第 2 步后按牵连面复扫（`#2`／`#21` 族纪律）抓到 **F9 的第三载体漏列**——`docs/standards/测试与验收标准.md:69`（G1 行）状态/处置列既引述旧词「入口与清单型文档」，又判断「其机检锚点行用『入口与清单型文档』」；F9 恰把机检锚点行改为「以表格与编号条目为主要结构者」，**该引述与判断双双失效**。已补 **F27**（两处订正）并同步头注自由度／§四 附加项／§八 4／§五 例外；同批订正规格自身两处面节标题范围（面一／面三 在补入 F26／F27／F28 后未同步）。
- **核验期规划方自查·新增 F28（2026-09-16）｜复发族回填**：F27 之外另抓到**族级口径缺口**——规格 A 级行惯以 `**` 标改动词（读者向强调），而检查 C 的 `_strip_wrap()` 只剥外层包裹与转义归一、不剥 `**`，致「目标文本含 `**`、实文不含」恒报「未落实或已漂移」；本批 **F1 行即实例**（实况为无加粗的约束节、规格记加粗约束节；该行目标文本的加粗是规格强调记法、非目标文本）。按「只修实例不回填族＝失守」立 **F28**（工具侧剥加粗标记，与同函数 050 批「转义归一」同型同据），**F1 行不改**。**残余判读**：检查 C 另报 3 条「未落实或已漂移」——`F24` 行标记后引述**被否方案**、`F26` 行以 `『』` 记嵌套引号（实文 `「」`）、`F26` 行引「候选行计入 findings」系对 `078` 规的**指称**——三条皆引述形态类假阳性、非漂移，属已登记 `G24` 族，触发已现、处置入 §八 6。**角色声明**：本两项（F27／F28）的落盘由**规划方直改**（施工方额度耗至 18:36 复位），核验以独立复跑断言＋`patch_file.py` 四段式闸为准；**独立产物审查照旧在额度复位后派出**（重路径不可省）。
- **产物审查·双维（独立上下文只读 subagent，2026-09-16）｜findings 共 13：P0:0 P1:2 P2:11｜结论「修订后通过」**（阻断面为零：门禁退出 0、[3] 候选 0、断言除 6① 外全绿、16 件与 §二 清单逐名一致、archive 与 §五 禁改面零 diff；P0:0 故不触发「修订后重派」条款）。**规划方逐条独立取证后处置**：**P1-1**（断言 6① 期望在制态不可达＋口径注假前提）→ 命令改「排除 `@ .tmp/v.txt` 回声行」后实测 **0**（基线 3 全在 `archive/078`）、口径注改写为施工期裁定的真述；**P1-2**（G15 行四列化时被插字「命令序」→「命令序号」，违 §五 只拆列不改文）→ 回改一字，与 `archive/072:52`／`073:148` 两载体一致；**P2-1**（销号注归给「065／072」）→ 据 `git log -S` 唯一命中（065 批 `3265509`）与 065 规格 F1／F4／F5 订正为「065 批处置」；**P2-2**（F2 出处写「062 §九」）→ 062 全件无 §九（实例在 §八）→ 改「`archive/062` §八／`archive/085` 头注＋§九」；**P2-3**（§五 例外漏 F12）→ 补；**P2-6**（`SKILL.md:18` 同节两引）→ 删首个指针、保 ⑤ 判据所需「维护出处」形；**P2-7**（计数 5→3 未注时点）→ 补时点口径；**P2-8**（`CHANGELOG`「裸点名 13 处」与断言 4 口径 16 不同源）→ 改「16 处（`施工机制` 13＋`AGENTS.md` 3）」；**P2-9**（F22 无逐字载体）→ 补引号块，检查 C 由此可核该 `[A]` 项；**P2-10**（F6 新句与 `AGENTS` §七 成真源环）→ 去指针、改自足句；**观察 3 升格 F29**（`check_spec.py` 的「七类」随检查 H／I 未回写，**7 处**且枚举缺 H／I）→ 全回写为九类；**P2-4／P2-5／P2-11 登记 §八 7／8／9**（列义残余／围栏判据族潜伏／运行时溯源体例）；**观察 1（CRLF）**→ 工作区 6 件为 CRLF 而 HEAD 全 LF，提交落盘恒 LF，归属不明。**审查方独立实证通过面**（未采信规格自述）：断言 1–7 除 6① 外逐条复跑达标／F15 的 16 处口径与归零（HEAD 版复量 13＋3）／F21 两判据（跨度 0／围栏 0、关判据复现 3）／F26 打桩（26→27）／F28 对照（4→3，消掉的恰是 F1 行）／§五 禁改面零 diff／archive 零 diff。
- **本批至此全部落定**：F1／F26 ＋ 核验期补入的 **F27**（F9 的第三载体）／**F28**（`[A]` 加粗记法致检查 C 假阳性的族级回填）／**F29**（「七类」随检查 H／I 未回写）＝ **29 项／16 件**；两轮审查（成稿 **8** 条、产物 **13** 条）／施工（后台 subagent；**F27／F28／F29 由规划方直改**——施工方额度耗尽，核验以独立复跑断言＋`patch_file.py` 四段式闸为准）／核验三步（第 1／2 步独立复跑、第 3 步＝双维产物审查）＋修订轮 —— **待归档**。
