# 107-2026-09-16-归档前经验沉淀闸批

> 定位：补上**归档那一刻缺失的那道工序**。`107` 前置只读审计实测：`docs/specs/archive/` **43 件**、可提取的经验条目 **421 条**，其中**已沉淀 59／埋没 341／已作废 21**——即 **81% 的教训只写在规格里**，而规格一归档就没人再读（`CHANGELOG` 只记沿革、不留判据，不算沉淀）。本批＝**立闸**（新判据 ＋ 新条文）＋ 把审计去重出的 **top10 条反复出现的教训**落进长期台账；余下 20 条列 §八 交 `108`。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**（跨 8 件、新增与修改规范条目、动守卫判据）＋ **双维产物审查**（改标准条文）。真机走查**不适用**——该结论可由 §二 逐行清单直接读出：清单内无 `skills/` 资产正文、无产物契约字段与状态值枚举（非自述）。
> 自由度分布：[A]×14 [B]×4 共 18 行
> 必读输入：`docs/specs/施工机制.md` §三 过程产物两项（`:72`–`:80`）、§四 守卫变更回写清单（`:92`）与规则新增回写清单（`:93`）、§六 反模式表 `#22` 行（`:162`）与表末（`:172`–`:181`）、§七（`:183`–`:191`）、§八「本批核销」节的口径（`:256`–`:263`）；`docs/standards/测试与验收标准.md` §1.1 `[4]` 行（`:26`）、§3 记录约定（`:57`–`:61`）、§4 已知缺口表（`:63`–`:95`）、§三 机检锚点表 `[4]` 行（`:126`）；`docs/standards/命名标准.md` §5 `:105` 与 §三 `:137`；`docs/standards/规格写作标准.md` §3（`:36`–`:44`）、§4（`:46`–`:58`）、§5（`:59`–`:73`）、§三 `:132`；`docs/standards/运行环境标准.md` §一.2（`:16`–`:25`）；`scripts/check_spec.py` 的 `check_nuclear()`（`:1039`–`:1134`）、`check_archive_record()`（`:1385`–`:1458`）、`verify_report()`（`:1811`–`:1900`）（行号按 `107` 批施工终态重算）、模块 docstring `:10` 与 `:49`；`scripts/check.sh` 头注 `:10` 与 `echo` `:138`
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-16）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | **归档动作的全部要求＝「验收通过 ＋ 移动」**——`施工机制` §三 过程产物两项第 1 项现文：「1. 规格——细节落当批规格（含「本批核销」（相关时）与「开放项」节）；验收全部通过后移入 `docs/specs/archive/`，移动不留副本。」**无任何「扫经验、判沉淀」字样** | 逐行 read `:74` |
| ② | 最像「沉淀闸」的一句在 `:78`：「不建全域台账、不建施工记录表。当批欠账当批修完；修不完的写进当批规格的「开放项」节，**或**该主题所属标准的「机检锚点／已知缺口」行——债跟着主题走，不跟着时间走。」——（a）挂在**欠账产生时**而非归档时，（b）「**或**」式二选一、**明文允许只写开放项**，（c）不要求逐条判 | 逐行 read `:78` |
| ③ | `施工机制` §七 `:190` 现文：「登记：本批收尾只核两处落点（§三 过程产物两项）——规格（含本批核销与审查记录）与 `CHANGELOG` 条目；规格随验收移入 `docs/specs/archive/`。不设施工记录表。」——收尾只核两处；而 `CHANGELOG` 只记沿革 | 逐行 read `:190` |
| ④ | `施工机制` §八「本批核销」口径 `:260` 写法行现文末句：「理由写当时判据或落点（无理由的「仍待」等于没核）。」——**理由列是自由文本**，无「须点名台账条号」要求 | 逐行 read `:260` |
| ⑤ | `check_spec.py` 检查 J（`check_archive_record()`，`def` 在 `:1385`）**只判三件**：① §九「审查记录」节存在性（**缺节即整段静默 `return [], []`**，并非出候选）② `findings 共 N` 与各档位之和 ③ 花括号占位符；其 docstring 末句经 `107` F3 改述为「**除开放项台账归宿一项外不读写磁盘**（审查记录核对面取自规格文本本身；开放项台账归宿一项另读五处台账全文）」（逐字）——**原「不读 §八 开放项、不与任何台账比对」在 F3 后不再成立**；本锚点记的是**改动前**状态 | 逐行 read `:1385`–`:1458`（施工终态） |
| ⑥ | `check_spec.py` 检查 H（`check_nuclear()`，`def` 在 `:1039`）只判 §七 核销表**汇总行 ↔ 表体状态列**的内部自洽（五档逐档计数 ＋ 总数 ＋ 五档外状态词）——**不与台账比对**，即「表与账对得上」≠「教训进了台账」 | 逐行 read `:1039`–`:1134`（施工终态） |
| ⑦ | **归档动作本身连机械闸都没有**：`check_spec.py` `:33` 自陈 `--verify` 「**不进 `check.sh` 门禁**」；`命名标准` §三 `:137`「规格验收后移入 `docs/specs/archive/`（移动不留副本）」与 `规格写作标准` §三 `:132`「§2 验收全部通过后归档」两处机检锚点的状态列均＝**待建** | 逐行 read `check_spec.py:33`／`命名标准:137`／`规格写作标准:132` |
| ⑧ | `施工机制` §六 反模式表末行为 **`#32`**（「把『回写面』写进禁止面」，2026-09-16 `106` 批产物审查 P1-1）；同节已知缺口表末行为 **`协2`** | 逐行 read `:172`／`:181` |
| ⑨ | `测试与验收标准` §4 已知缺口表末行为 **`G31`**（`:95`）；`G32`–`G36` **未被占用** | `grep -c` 以「行首 ＋ 竖线 ＋ G3[2-6] ＋ 竖线」为模式（此处不写字面竖线，避 MD056）扫该件，实测 **0** |
| ⑩ | `AGENTS.md` §七 自检信号表第 3 行（信号「工具报得出与应报出一致」）判据列仍写「`[4]` 段 **A-I** 无未核项」，而 `[4]` 段现为 **A–J**（`102` 批加检查 J 时漏回写）——改 `AGENTS.md` 须作者确认（宪法 §六.1），本条**只登记不擅改** | 逐行 read §七 表 |
| ⑪ | `命名标准` §5 `:105` 末句括注现文：「（**批次号型包外标记**——如「随某批未建」——**不属本条**；该类按各批上线时删除的到期制处理）」——而 `skills/` 内「批次号型」实物**已归零**（`089`–`091` 批已收口），存续的是**阶段指向型**标记 ⇒ **类目名与实物脱节** | 逐行 read `:105`；`grep -c '批次号型' docs/standards/命名标准.md` 实测 **1** |
| ⑫ | 两种措辞并存且**指同一件事**：实测「随**对应**阶段批」**8 处**（`outline-emotion-words.md` 4／`outline-workflow.md` 3／`outline-stage-spec.md` 1）vs「随**各自**阶段批」**2 处**（`outline-workflow.md:48`／`outline-stage-spec.md` 1） | `grep -c '随对应阶段批'` 与 `grep -c '随各自阶段批'` 逐件实跑（见 §四.1） |
| ⑬ | `施工机制` §六 `#22` 行（「同载体多副本漏改」，`:162`）事故出处列现含 4 个出处，**未含**「`SKILL.md` 与 workflow 里同一条义务句写了两遍、无单一真源」这一族（审计实测该族命中归档 **3 件**：`094`／`095`／`098`） | 逐行 read `:162`；`grep -rl '义务句' docs/specs/archive/*.md \| wc -l` 实测 **3** |
| ⑭ | 审计给出的 top10「同一教训 ≥3 件复现」里，**1 条已沉淀**（加粗密度兜底与入口型／表格式文档冲突 ⇒ 已在 `文档写作标准` §一.3.8 与 `测试与验收标准` §4 `G1`），其余 9 条**无台账归宿** ⇒ 本批实做 9 条（`F6` 四行对应复现组 1／2／4／5，`F7` 两处对应组 8／9，`F8`／`F9`／`F10` 对应组 10／6／7）；**另夹带 1 条**（`F11`，出自审计埋没清单 #11「义务句写两遍」，不属 top10 复现组） | 逐条比对审计「已沉淀清单」与「埋没清单」 |
| ⑮ | **归档要求的全载体扫描**（P0-A 收口）：活件中述及「规格移入 archive」的共 **4 件 10 处**，逐处裁定——**要求性载体（本批改）4 处**：`施工机制:74`（§三 过程产物第 1 项 ⇒ F1）、`施工机制:190`（§七 登记句 ⇒ F18①）、`施工机制:219`（§八 **规格模板**「过程产物」行 ⇒ F18②）、`规格写作标准:79`（§6 过程产物第 2 项 ⇒ F16）；**描述性／非要求载体（本批不改）6 处**：`命名标准:31`（§1 目录树行内注释）、`命名标准:65`（§2 命名总表「落点」列）、`命名标准:80`（表注·作废批路径）、`命名标准:137`（§三 机检锚点登记）、`规格写作标准:132`（§三 机检锚点登记）、`施工机制:113`（§五 止损第 1 条的**作废批**移入路径——作废批前提已失效，其教训另行按主题处置）。**扫描命令**（Git Bash；归档 43 件按纪律只读、不计入活件面）：`grep -rnF '验收全部通过后移入' --include='*.md' .` → `施工机制:74`／`命名标准:65`／`规格写作标准:79`；`grep -rnF '移动不留副本' --include='*.md' .` → `施工机制:74`／`命名标准:31`／`命名标准:136`／`规格写作标准:79`；逐件 `grep -cF` 以「移入 ＋ 反引号 archive 路径」为模式 → `施工机制` **4**／`命名标准` **3**／`规格写作标准` **1**／`测试与验收标准` **0** | 上列三条命令逐条实跑（2026-09-16）。P0-A 判定「载体面闭合 ＝ **扫过且逐处有裁定**」，不靠沉默跳过 |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md` §三 过程产物两项 第 1 项（`:74`） | 该项现文把归档动作写在验收通过之后；今在其**之前**插入一句硬要求，**逐字**：「**移动前须逐条过该件 §八 开放项与 §九 审查记录中 P0／P1 的建议；凡判为跨批可复用的教训，须先落入 `施工机制` §六 反模式表／`测试与验收标准` §4 G 表／相应标准条文正文／`AGENTS.md` 之一，并在当批规格中逐条写出所落条号；判为「仅本批一次性」者须写明理由——未逐条写出落点者不得移入 archive**。」 | [A] | 锚点①②③；前置审计任务二结论「没有」 |
| F2 | `docs/specs/施工机制.md` §八「本批核销」节的口径（`:260` 写法行） | 在该行句末追加半句，**逐字**：「**理由列写「已沉淀」时须点名台账条号（条号形如反模式 `#N`／缺口 `GN`／标准件 `X` §N）；不给条号即视为未沉淀。**」 | [A] | 锚点④；F1 同源 |
| F3 | `scripts/check_spec.py` 的 `check_archive_record()`（施工终态 `:1385`–`:1458`） | **扩检查 J：加一条「开放项台账归宿」判据**。判据（四步）：① 取该件 §八「开放项」节的**每条行首项**（起始符同时认 `数字.` 与「连字符加空格」两种，至下一个二级标题或节末）——**该节定位模式须写死为含「八、」的标题**（形如 `^##[^#\n]*八[、.]\s*开放项`），**不得**被 §七 标题「本批核销（相关归档规格的开放项）」抢先命中；② 每条取**两个锚点**——**锚点一**＝该条内**第一个反引号跨度**的内容；**若其为纯数字或形如 `NNN` 的批次号，则改取该条内第一个「非批次号」的反引号跨度**（批次号在五处台账中恒命中，不排除则闸门一上线即**恒静默**）；**锚点二**＝该条内**第一个** `§数字`／`G数字`／`#数字` 编号。**取法唯一**，不再有第二读法。二者**皆零命中**才可能出候选；**两个锚点都取不到**（既无任何反引号跨度、亦无编号）者**出候选**、文案以「**开放项无锚点，无法核归宿**」起（**不跳过**——闸门目的正是抓「写不出可查落点」的条目），并在「检查 J 汇总」末尾加一格 `｜无锚点条目 {n} 条`；**结构实测**（用 `check_spec` 自身取词面）：§八 行首项 **7** 条、§七 仍待行 **10** 行——两处的首个反引号 token 多为 `106`／`105` 这类批次号（② 的「批次号不计为锚点 ＋ 改取非批次号跨度」兜底正是为此）。**本规格不再预估候选条数**（预估即会失准，同反模式 `#21`）；首轮全仓实测数按 §八 开放项 5 记入下一批核验结论；③ 该条若含「作废」「已履行」「本批处置」任一，**跳过**；④ 两个锚点在**五处台账**全文均零命中者出候选。**五处**＝① `施工机制` §六 反模式表 ② 同节已知缺口表 ③ `测试与验收标准` §4 G 表 ④ `docs/standards/` 全部标准件正文 ⑤ `AGENTS.md`。候选文案以「开放项无台账归宿」起。**该判据须读盘**（`root` 入参已有）——故同批改述该函数 docstring 的「不读写磁盘」自陈句（改为「除开放项台账归宿一项外不读写磁盘」）。**候选非阻断**（口径同 E–J，退出码不受影响）。**预期收益须下调（如实记）**：五处台账里任一通用 `§N`／`GN`／`#N` 命中即压制报告，故本闸门效力天然弱——它抓的是「**连一个可查条号都写不出**」的开放项，不是「归宿写错」的开放项 | [B] | 锚点⑤；`施工机制` §四 守卫变更回写清单；前置审计任务二建议② |
| F4 | `scripts/check_spec.py` 的 `check_nuclear()`（`:1035`–`:1102`） | **扩检查 H：加一条「仍待项断链」判据**：对 §七 核销表**表体行**中状态列∈{仍待, 待作者} 的行，取其「条目」列的锚点（取法与 F3 ②**同款**，含「批次号不计为锚点 → 改取非批次号跨度」与「两锚点皆取不到即出候选」两条兜底）；该锚点在 F3 ④ 的**五处台账**全文零命中者出候选。候选文案以「仍待项已断链」起。**同批改述 `check_nuclear()` docstring 末句「本检查不读写磁盘。」**（照 F3 对 `check_archive_record()` 的同款处置——F4 亦须读盘）。**候选非阻断** | [B] | 锚点⑥；F3 同源；前置审计任务二建议② |
| F5 | `docs/standards/测试与验收标准.md` §4 表末（`G31` 行 `:95` 之后） | 新增一行 `G32`（4 格；行内表体竖线在规格内写作转义形式，落盘为半角竖线）：第 1 格＝`G32`；第 2 格＝「**归档动作无机械闸**：`命名标准` §三 `:137` 与 `规格写作标准` §三 `:132` 两处归档机检锚点状态均＝待建；`--verify` 只核 `CHANGELOG` 条目与 archive 文件存在，且 `scripts/check_spec.py:33` 自陈**不进 `check.sh` 门禁**——故归档这一步既无强制、也无判据」；第 3 格（状态/处置）＝「**本批处置（`107` F1／F3／F4）**」；第 4 格（触发条件）留空 | [A] | 锚点⑦⑨；`测试与验收标准` §4 表体例（照 `G29`／`G30`／`G31` 现行列位） |
| F6 | `docs/standards/测试与验收标准.md` §4 表末（`G32` 行之后） | 新增**四行** `G33`–`G36`（各 4 格，同上列位；第 3 格＝状态/处置留空、第 4 格＝触发条件）。各行第 2 格逐字：**`G33`**＝「`check_spec.py` **无仓内回归测试件**：判据只由 `.tmp/` 夹具验证、用完即删（红线 2），判据坏了无人知——规划方实测归档件命中 **7 件**（可复算判据：`grep -rl '回归测试件' docs/specs/archive/` 得 7 件，件号＝`077`／`078`／`080`／`083`／`085`／`086`／`087`；前置审计记「5 件」，与本规格实测不符，见 §四.1）」；**`G34`**＝「**检查 A 与检查 C 对「目标文本」是两套取法**（检查 A 取全部引号块、检查 C 取末标记之后的块），两口径并存未统一——同族归档件 `065`／`067`／`068`／`069`／`070`（件号取自前置审计，本规格未逐件复核）」；**`G35`**＝「**`check.sh` 段序／段名三载体漂移**（头注 `:10`、`测试与验收标准` §1.1 表、`AGENTS.md` §七各一份，改一处即漂）——同族归档件 `075`／`078`／`089`／`092`／`095`／`097`（件号取自前置审计，本规格未逐件复核）」；**`G36`**＝「**`check_counts` 类阈值与 `--quiet` 分档长期悬置**——同族归档件 `067`／`072`／`073`／`074`／`075`（件号取自前置审计，本规格未逐件复核）」。各行第 4 格（触发条件）＝「触发＝下次触碰该判据面」 | [A] | 锚点⑭；前置审计「同一教训 ≥3 件复现」第 1／2（检查 A/C）／4／5 组；`#28`（计数须附实测命令） |
| F7 | `docs/standards/命名标准.md` §5 `:105` 末句括注 | **两处**：① **类目名与实物对齐**——删去现括注中的旧类目名，改为，**逐字**：「（**阶段指向型包外标记**——如「随对应阶段批」／「随某批未建」——**不属本条**；该类按各批上线时删除的到期制处理）」；② **两措辞定名**——在同节该段之末追加一句，**逐字**：「**同一到期制标记的措辞统一用「随对应阶段批」**（**规划方实测**：`skills/` 内 8 处 vs「随各自阶段批」2 处；**全仓活件** 8 处 vs 3 处——第三处＝`docs/product/书目录契约.md:22`，非 `skills/` 资产）；既有异形实例（`skills/abzu-outline/references/outline-workflow.md:48`、`skills/abzu-outline/references/outline-stage-spec.md` 各 1 处，及 `docs/product/书目录契约.md:22`）待下次触碰该件时收口，本批均不动。」 | [A] | 锚点⑪⑫；`命名标准` §6「修改命名规则：先全仓 grep 涟漪面」；前置审计埋没清单 #9／#10 |
| F8 | `docs/standards/测试与验收标准.md` §3 走查记录「记录约定」（`:58` 首项）与同件 §二 的走查正例行（`:103`） | **两处**：① `:58` 现文的括注内补必填字段，改为，**逐字**：「走查记录**落当批规格**（日期 ＋ 域 ＋ 通过／失败 ＋ 备注 ＋ **宿主版本／模型／会话形态** 三项必填；未采集者须写明「未记」并说明原因）——不另建台账（平行台账是明确不做项，见 `AGENTS.md`「不做清单」节）。」；② `:103` 的正例行字段枚举同批改齐，**逐字**：「✅ 走查记录落当批规格一行（日期＋域＋通过/失败＋备注＋**宿主版本／模型／会话形态**）。」 | [A] | 前置审计埋没清单 #12（同族归档件实测 `061`／`087`／`088`／`094`／`097`／`098`／`099` ＝ **7 件**）；`094` §八 8 的「未记」实例 |
| F9 | `docs/standards/规格写作标准.md` §5（`:73` 第 13 条之后） | 新增第 14 条，**逐字**：「14. **一行式命令类断言须跨度与围栏感知**：以 `grep -r`／`grep -rl` 之类**一行式**命令扫全仓的断言，其扫描面须**跳过代码跨度与围栏内的引文**（规格里写一个示例链接或示例命令即会被当成真目标）——否则恒假红。与第 11 条（扫描面白名单）互补：第 11 条管**谁在面内**，本条管**面内的什么算数**。实例：`078` §八 5、`080` §八 3 与同族 4 件（`081`–`083`）；`080` §八 6 已把落点指向本件。」 | [A] | 前置审计埋没清单 #3（同族实测 **7 件**：`077`／`078`／`080`／`081`／`082`／`083`／`086`，口径同 `G33` 的可复算判据）；行号按磁盘现文改正——`080` 的 §八 3 在 `:222`、§八 6 在 `:225`（前置审计原记 `:223`／`:224`，差一行，以实测为准） |
| F10 | `docs/standards/运行环境标准.md` §一.2（`:24` 第 7 项之后） | 新增第 8 项，**逐字**：「8. **守卫与断言以 `shell=True` 执行子命令时的跨宿主语义须显式声明**——同一命令串在 Windows 与 bash 下的分词、引号与通配行为不同，跨宿主可复现性不可默认成立；确需 `shell=True` 者，须在脚本头注**显式声明两个宿主——设计宿主与实测宿主**（如「设计按 POSIX `sh`／实测宿主＝Windows `cmd.exe`」），并给出实测命令；**不得只写「已实测」而不点宿主**。**射程面内不满足者（如实记）**：`scripts/check_spec.py` 自身即用 `shell=True`（实测命中 **3 处**：`:25` 模块 docstring 自陈宿主语义、`:414` 与 `:1602` 两处 `subprocess.run`），其头注只陈语义、未按本条声明设计宿主与实测宿主两值 ⇒ 本批**只立条文、不整改该件**（该项转 §八 开放项 7）。**并记前提冲突**：本条落在 §一.2 的「Windows（Git Bash）」约定下，而 `check_spec.py:25` 自陈 Windows 下走 `cmd.exe`——即**本仓实际执行宿主并非 Git Bash**；该冲突本批只如实登记，不裁定。出处：`078` 批头注补述与同族归档 3 件（规划方实测 `shell=True` 命中归档 **6 件**：`075`／`076`／`077`／`078`／`080`／`090`；前置审计记「4 件」，与本规格实测不符，见 §四.1）。」 | [A] | 前置审计埋没清单 #4；`CHANGELOG:67`（`078` 头注补述）是现存的唯一落点 |
| F11 | `docs/specs/施工机制.md` §六 反模式表 `#22` 行（`:162`）的「事故出处」列 | 在现列末追加一个实例，**逐字**：「2026-09-16 `107` 前置审计：`SKILL.md` 与 workflow 里**同一条义务句写了两遍**、未定单一真源（同族归档 3 件：`094`／`095`／`098`）」 | [A] | 锚点⑬；反模式表头注「补实例前先自问是否应新开一行」（本族归 `#22`，不新开行）；前置审计埋没清单 #11 |
| F12 | `CHANGELOG.md`（`## [Unreleased]` 的 `### Added` 小节） | **新增过程产物一行**（列 0、`-` 加空格起首；条首串写死为「归档前经验沉淀闸（107）」；**末尾附成本註记 `（往返 R／T）`**，`R`／`T` 须落实际数值；**连同註记 ≤400 字符**） | [B] | `施工机制` §三 过程产物两项第 2 项；`版本与分发标准` §3 |
| F13 | `scripts/check.sh` 头注 `:10` 与 `echo` `:138`；`scripts/check_spec.py` 模块 docstring `:10`；`docs/standards/测试与验收标准.md` §1.1 `[4]` 行 `:26` 与 §三 机检锚点表 `[4]` 行 `:126` | **守卫变更回写面**（`施工机制` §四 回写清单 ①③④⑤）：上述五处的 `[4]` 段判据面枚举里，**审查记录核对**一项之后补「／**开放项台账归宿核对**」、**核销表汇总核对**一项之后补「／**仍待项断链核对**」（两处旧名按磁盘现文散文指认，**不作引号块**——引号块只留上面两个目标串）——**逐字照该处现文的分隔符与加粗体例**（头注与 `echo` 两处用顿号与斜杠连写；两表行用「／」分隔），**不新增检查号、不改段序段数** | [A] | `施工机制` §四 守卫变更回写清单 ①③④⑤；反模式 `#32`（回写面须显式放行，见 §五） |
| F14 | `scripts/check_spec.py` 的 `check_archive_record()`（同 F3 函数） | **补一条「§九 成本字段」判据**——`105` §八 开放项 6 的触发条件正是「下次触碰 `check_archive_record()`」，F3 已触碰，故**同批处置**：§九 审查记录节内**须有任一行同时含「工具往返数」与「周期时长」两字段、且两值均非占位值**（含「待汇总」「待填」等占位值者**视同缺**）——**按「节」判、不按「行」判**；整节皆无 ⇒ 出**一条**候选，文案以「`§九 含成本字段`」起（全件唯一出口）。**口径改判的实证**：首稿按「行」判（审查位记录行＝整行含「成稿审查／产物审查／单维核对」之一、缺字段即报）在四件在制规格上出 **13 条**候选、逐条复核 **0 条真阳**——皆「成本两字段单列一行」的等效写法，属**过报**；按节判后现行四件 **0 条**。过报实例已记 `测试与验收标准` §4 `G19`。**改口径前的影响面（如实记）**：按「行」判时四件共 **11 行**——`103` **1**／`105` **5**／`106` **3**／`107` **2**（其中 `107` 两条系**判据自引**：该规格正文自身写着三词枚举，反模式 `#24` 的自家实例）。**改按「节」判后现行四件归 0 条**——该判据不再报现行件，`§八 开放项 6` 据此改写。**候选非阻断** | [B] | `105` §八 开放项 6；`施工机制` §八 `:251`（两字段为必填）；`施工机制` §三 `:80`（本批内被处置的开放项须同批改标） |
| F15 | `docs/specs/105-2026-09-16-三处缺口补齐批.md` §八 开放项 3 与 6 | **改标两处 ＋ 订正一处数字**：① `105` §八 3 —— 末句（现文以触发条件作结）**整句替换**为「**本批处置（`107` F3／F4／F14）**」，**且同条正文的件数按实测由 42 订正为 43**（实测 `docs/specs/archive/*.md` ＝ **43** 件，见本规格头注；原「42 件」失实系窄域审观察项④）；② `105` §八 6 —— 末句（现文亦为触发条件式陈述）**整句替换**为「**本批处置（`107` F3／F4／F14）**」。**两处改后整行逐字**：①「3. **已归档规格里的教训是否已回填台账**——已派独立只读审计在查（**43 件**归档），结论待收。**本批处置（`107` F3／F4／F14）**」；②「6. **检查 J 不核「成本字段」**——`check_archive_record()` 只判①审查记录节存在性、②`findings 共 N` 与各档位之和、③占位符，对「工具往返数／周期时长」两字段**无任何判据**，故成本漏记永不会被 `check.sh` 的 `[4]` 段报出（本批即漏：只记了施工段）。**本批处置（`107` F3／F4／F14）**」。**依据**：`107` §七 已把这两条记为「本批处置」，而源件 `105` §八 仍写旧触发 ⇒ 触 `施工机制` §三「本批内被处置的开放项须同批改标」与反模式 `#23` | [A] | `105` §八 3／6；`施工机制` §三；反模式 `#23`；先例＝`106` F5 |
| F16 | `docs/standards/规格写作标准.md` §6 的规格落点句（施工终态 `:79`） | **把归档要求改为指针**（P0-1 的「收口为单一真源」第二处）：该句现文含归档动作与「移动不留副本」，今**逐字**改为：「**验收全部通过后移入 `docs/specs/archive/`（移动不留副本）的动作与前置条件见 [`施工机制`](../specs/施工机制.md) §三 过程产物两项中「移动前须逐条过该件 §八 开放项与 §九 审查记录中 P0／P1 的建议」一句**」——理由：协作／流程真源为 `施工机制`（`AGENTS.md` §六.3 的具体真源指定优先条），与 F1 并存即造第二载体（`#22` 同族）。**`测试与验收标准:37` 的收尾自查指向本件的过程产物两项，指针化后仍可解析 ⇒ 不动 `:37`** | [A] | P0-1 裁定；`AGENTS.md` §六.3；`#22` |
| F17 | `docs/specs/106-2026-09-16-定级守卫与自述即无效批.md` §八 开放项 6 | **改述一处假账**（规划方自查）：该条原写「仓内 `.md` 行尾漂移（LF→CRLF）无机械守卫」，**与仓内既有裁定相抵**——仓根有 `.gitattributes`（`* text=auto eol=lf`，本轮施工期实测），**入库一律归一为 LF**；且 `运行环境标准` §一.3.3 早已裁定「工作树 CRLF 属本地配置差异，不折腾（入库已由 `.gitattributes` 归一）」。故工作树 CRLF 只是**本地噪声**（`git diff` 一行警告），**不是仓内容错、不构成缺口**。本条改为如实记该事实，并把触发条件改为「触发＝作者裁定是否仍要一条**工作树**行尾提醒（**非缺口**）」 | [A] | 规划方自查；`.gitattributes`（`* text=auto eol=lf`）；`运行环境标准` §一.3.3；反模式 `#21`（改一处忘了它使自家结论失效） |
| F18 | `docs/specs/施工机制.md` §七 `:190`（登记句）与 §八 `:219`（**规格模板**「过程产物」行） | **两处均改为指向 §三 闸门的措辞**（P0-A：归档要求的要求性载体共 4 处——`:74` 由 F1 承载、`规格写作标准:79` 由 F16 指针化，本行收口剩下的 `:190` 与 §八 模板两处）：① `:190` 末句改为，**逐字**：「规格随验收移入 `docs/specs/archive/`（**移入前的前置条件见 §三 过程产物两项中「移动前须逐条过该件 §八 开放项与 §九 审查记录中 P0／P1 的建议」一句**）。不设施工记录表。」；② `:219` 改为，**逐字**：「3. 过程产物：验收通过后规格移入 `docs/specs/archive/`（**移入前须逐条过开放项与 P0／P1 建议、判是否沉淀，见 §三**）；`CHANGELOG` 条目已落。」——**② 尤须让后续批一眼看到这一步**（该行是各批规格模板的 `过程产物` 项，会被逐批照抄） | [A] | P0-A 裁定；§一 ⑮；`施工机制` §三（F1 落点）；反模式 `#22`（同载体多副本漏改） |

## 三、验收断言（基线 2026-09-16 实跑）

> 计数口径：以下 `grep -c` 一律**计命中行数**；扫两件以上者已在期望栏注明。

### 断言 1｜F1 与 F2 条文落盘

```bash
grep -c '移动前须逐条过该件' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '不给条号即视为未沉淀' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 1b｜F16 与 F18 条文落盘

```bash
grep -c '移动前逐条判沉淀' docs/standards/规格写作标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '移入前' docs/specs/施工机制.md
```

期望 **2**（两个落点各一处）。基线 **0**。

### 断言 2｜F3 判据**真会报**（夹具正控，防「存在即可」）

```bash
mkdir -p .tmp/107 && printf '# T\n\n## 八、开放项\n\n1. 该条教训已落 `#31`（锚点在台账命中）。\n' > .tmp/107/fx_j_hit.md && python scripts/check_spec.py --static .tmp/107/fx_j_hit.md 2>&1 | grep -c '开放项无台账归宿'
```

期望 **0**（条目锚点 `#31` 在台账命中 ⇒ **不得**报）。基线 **不适用**（本判据新增，改动前该串恒 0）。夹具用完即删（红线 2）。

```bash
mkdir -p .tmp/107 && printf '# T\n\n## 八、开放项\n\n1. 该条教训涉及 `ZZQQXX_TOKEN` 与 `G99`（两锚点均零命中）。\n' > .tmp/107/fx_j_miss.md && python scripts/check_spec.py --static .tmp/107/fx_j_miss.md 2>&1 | grep -c '开放项无台账归宿'
```

期望 **1**（两锚点在五处台账均零命中 ⇒ 必须报）。基线 **不适用**（同上）。
**说明**：本断言是**有效性正控 ＋ 负控对**——只断言候选文案串存在，会被「写了但没接进判据」蒙过（同 `105` 断言 2 的教训）。
**补两条夹具（P1-1 空锚点落点 ／ P1-2 节定位）**：

```bash
mkdir -p .tmp/107 && printf '# T\n\n## 八、开放项\n\n1. 这条既无编号也无反引号跨度，只有一段散文。\n' > .tmp/107/fx_j_noanchor.md && python scripts/check_spec.py --static .tmp/107/fx_j_noanchor.md 2>&1 | grep -c '开放项无台账归宿'
```

期望 **0**（两锚点皆缺 ⇒ 跳过、不出候选）。基线 **不适用**。**并须另核空转明示**：同夹具的 `检查 J 汇总` 行须含 `｜无锚点条目 1 条`。

```bash
mkdir -p .tmp/107 && printf '# T\n\n## 七、本批核销（相关归档规格的开放项）\n\n1. 七节里的假条目 `ZZQQXX_TOKEN`。\n\n## 八、开放项\n\n1. 八节里的真条目已落 `#31`。\n' > .tmp/107/fx_j_sec.md && python scripts/check_spec.py --static .tmp/107/fx_j_sec.md 2>&1 | grep -c '开放项无台账归宿'
```

期望 **0**。**可判定的观测方式**＝先跑上面这条：若得 **0**，说明定位停在了 §八（§八 那条锚点 `#31` 命中台账 ⇒ 不报）；**若得 1，即定位模式被 §七 标题里的「开放项」字样抢先命中**（`ZZQQXX_TOKEN` 零命中而报）。基线 **不适用**。

### 断言 3｜F4 判据**真会报**（夹具正控 ＋ 负控）

```bash
mkdir -p .tmp/107 && printf '# T\n\n## 七、本批核销\n\n| 来源 | 条目 | 状态 | 理由／落点 |\n| --- | --- | --- | --- |\n| a | 该条见 `#31` | 仍待 | x |\n' > .tmp/107/fx_h_hit.md && python scripts/check_spec.py --static .tmp/107/fx_h_hit.md 2>&1 | grep -c '仍待项已断链'
```

期望 **0**（状态仍待但锚点 `#31` 在台账命中 ⇒ 不得报）。基线 **不适用**。

```bash
mkdir -p .tmp/107 && printf '# T\n\n## 七、本批核销\n\n| 来源 | 条目 | 状态 | 理由／落点 |\n| --- | --- | --- | --- |\n| a | 该条见 `ZZQQXX_TOKEN` | 仍待 | x |\n' > .tmp/107/fx_h_miss.md && python scripts/check_spec.py --static .tmp/107/fx_h_miss.md 2>&1 | grep -c '仍待项已断链'
```

期望 **1**（状态仍待且锚点零命中 ⇒ 必须报）。基线 **不适用**。
**说明**：两条夹具均只含最小 §七 表（无汇总行）——`grep` 只匹配本判据候选串，检查 H 既有判据可能另出的候选（汇总行缺失等）**不影响**本断言取值。

### 断言 4｜F5 与 F6 缺口行落盘

```bash
grep -c '^| G32 |' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '^| G3[3-6] |' docs/standards/测试与验收标准.md
```

期望 **4**。基线 **0**。

### 断言 5｜F7 类目名与定名

```bash
grep -c '批次号型' docs/standards/命名标准.md
```

期望 **0**（旧类目名已删净）。基线 **1**。

```bash
grep -c '同一到期制标记的措辞统一用' docs/standards/命名标准.md
```

期望 **1**（新立的定名句）。基线 **0**。

### 断言 6｜F8／F9／F10／F11 四处条文落盘

```bash
grep -c '宿主版本／模型／会话形态' docs/standards/测试与验收标准.md
```

期望 **2**。基线 **0**。**（本项期望随 F8 扩面同批更新**：F8 经窄域审 P1-6 由一处扩为两处（`:58` 与正例 `:103`），两处均含该串，`grep -c` 计行必得 2——施工偏差 **P1-1** 报出、规划方裁定「F8 落两处为是、期望改 2」，处置见 §九。**）**

```bash
grep -c '跨度与围栏感知' docs/standards/规格写作标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c 'shell=True' docs/standards/运行环境标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '同一条义务句写了两遍' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

### 断言 6b｜F17 假账订正落盘

```bash
grep -c '入库一律归一为 LF' docs/specs/106-2026-09-16-定级守卫与自述即无效批.md
```

期望 **1**。基线 **0**。

### 断言 7｜F13 回写面与 F14 成本字段判据

```bash
grep -c '开放项台账归宿核对' scripts/check.sh
```

期望 **2**（头注 `:10` ＋ `echo` `:138` 各一处）。基线 **0**。

```bash
grep -c '开放项台账归宿核对' docs/standards/测试与验收标准.md
```

期望 **2**（§1.1 `:26` ＋ §三 `:126` 各一处）。基线 **0**。

```bash
grep -c '开放项台账归宿核对' scripts/check_spec.py
```

期望 **1**（模块 docstring `:10` 的判据面枚举一处；被改函数的 docstring 只说明「除…外不读写磁盘」、不含该面名）。基线 **0**。

```bash
grep -c '§九 成本字段' scripts/check_spec.py
```

期望 **1**（F14 的候选文案出口）。基线 **0**。

> **附注（F14「上线后影响」的复算判据）**：对 `docs/specs/` 各在制件取 §九 节内以 `-` 起首、**整行**含「成稿审查／产物审查／单维核对」之一、且**不同时**含「工具往返数」与「周期时长」**（或两字段带「待汇总」「待填」等占位值）**的行，逐件计数——**改口径前**规划方实测（按「行」判）：`103`＝**1**／`105`＝**5**／`106`＝**3**／`107`＝**2**，合计 **11 行／4 件**；**改按「节」判后现行四件＝0 条**（判据＝§九 节内须有任一行同时带两字段且两值非占位）。本附注与 §二 F14 判据**同源同口径**。

### 断言 8｜门禁与过程产物

```bash
mkdir -p .tmp/107 && bash scripts/check.sh > .tmp/107/chk.out 2>&1; echo $?; tail -3 .tmp/107/chk.out
```

期望 **退出 0**，末三行含「检查全绿」。基线 **退出 0**（改动前）。

```bash
grep -c '归档前经验沉淀闸（107）' CHANGELOG.md
```

期望 **1**。基线 **0**。

## 四、规格自审（五条）

1. **锚点现场核过**：§一 **十五条**逐条实跑——①–④、⑧、⑪、⑬ 为 `施工机制`／`命名标准` 定点 read；⑤⑥ 为 `check_spec.py` 两函数全文 read；⑦ read 三处机检锚点行；⑨⑭ `grep -c` 以「行首＋竖线＋G3[2-6]＋竖线」为模式扫该件；⑩ read `AGENTS.md` §七 表；⑫ 逐件实跑（结果：`outline-emotion-words.md` 对应 4／各自 0；`outline-workflow.md` 对应 3／各自 1；`outline-stage-spec.md` 对应 1／各自 1）；⑭ 逐条比对审计两清单。**无未预验项**。**与交底不符者两条**（如实记）：① 交底称「`106` §八 开放项 7 条，含第 7 条 `AGENTS.md` §七 A-I 未回写」——实况：`106` §八 第 7 条是「`[A]` 行里的引号块只能放目标侧文本」，而 `AGENTS.md` A-I 那条在 **`106` §七 核销表**（来源＝`105` §八 开放项 7，状态仍待）；② 交底称本批申报**四条**工具缺口 `G33`–`G36`「同族 5／5／6／5 件」——本规格对其中三条**未逐件复核**，已在该三行注明「件号取自审计报告」，并把两条**规划方实测与审计不符**者（`G33` 实为 7 件非 5 件；F10 实为 6 件非 4 件）写进行内。
 **整改轮新增三条实测不符（如实记）**：① 交底称「该件当前 `未落实或已漂移` 4 条、须压到 0」——实测 **12 条**，其中 **4 条属现状侧引号块**（真缺陷，本轮按 P1-7 改散文后应归 0）、**8 条属目标侧引号块**（施工前必报、施工后归 0：`检查 C` 是**施工后**判据，先例＝`106` 件施工后此项为 0），故**建制期压不到 0**；② 交底称 F14 判据取「第 2 个字段位」——该口径实测仅 4 行，复现不出 9 行，本轮改取「整行含三词之一」并可复现 `103:1／105:5／106:3`；③ 交底称「§七 10 个仍待行中 5 行两锚点皆缺、§八 25 个编号项中 6 项两锚点皆缺」——实测 §七 仍待 **10** 行两锚点皆缺 **0**、§八 行首项 **6** 条两锚点皆缺 **0**（因每行首个反引号 token 是 `106`／`105` 这类批次号），故 F3 ② 增「纯数字／批次号不计为锚点」一条。**另记一条实现约束**：`检查 C` 每行**只解析第一个可解析的目标件**（`_row_target_file()`，`scripts/check_spec.py:601`），故 `[A]` 行**不得跨两个不同目标件**——P0-1 的第二处（`规格写作标准` §6）据此拆为独立 **F16**，不并在 `F1` 内。**窄域审轮新增**：**⑮** 为 P0-A 的全载体扫描锚点（4 件 10 处逐处裁定，见该行）；§一 锚点面据此由 14 条增至 **15** 条。
2. **断言实跑过**：§三 **十条**断言的**改前基线**已逐条实跑（F1／F2／F3 候选串／F4 候选串／`^| G32 |`／`^| G3[3-6] |`／`随对应阶段批`／宿主版本串／跨度围栏串／`shell=True`／义务句串／CHANGELOG 串／三处回写串／成本字段串 ＝ **0**；`批次号型` ＝ **1**；**断言 1b 的两条＝ F16 目标串 @ `规格写作标准` 0 ／ F18 目标串 @ `施工机制` 0**；窄域审轮新增断言 1b，故由八条增至 **九条**）。断言 2／3 的正负控夹具文本均在本规格内逐字给出（可照抄实跑）。**未逐条复核项如实标注**：`G34`／`G35`／`G36` 三行的同族件号取自审计报告，本规格未逐件读原件。
3. **禁止事项已写明**：见 §五（含对 `施工机制` §四 回写清单命中面的**显式豁免**，遵反模式 `#32`）。
4. **范围与期望同源**：§三 各断言的期望值均取自 §二 点名文件与节的范围；断言 2／3 的夹具与判据在同一命令内自洽（正控：锚点命中 ⇒ 不得报；负控：锚点零命中 ⇒ 必须报）。
5. **自述有守卫**（`106` 立的第 5 条，本规格头注逐句指认）：①「标准路径」⇒ **指认 `检查 G` 的「路径定级核对」**（`106` F1 加）：该判据只覆盖「自称『快』者」，**不覆盖「自称『标准』」**——故本句仍属**部分无守卫**，按第 5 条照最重路径执行（真做双维产物审查，不放宽）；②「双维产物审查」⇒ 由 `施工机制` §五 条件式覆盖，**无机械检查** ⇒ 同上照最重路径执行；③「真机走查不适用」⇒ **不是自述**：它是 §二 逐行清单的直接推论（清单内无 `skills/` 资产正文、无产物契约字段），判据＝清单本身；④「自由度分布 17 行」⇒ 由检查 G 核（本件为在制规格，`bash scripts/check.sh` 的 `[4]` 段逐批核）。

## 五、禁止事项

- 禁止触碰：`docs/specs/archive/` **全部**（含本批立闸所依据的 43 件审计对象）；`docs/product/` 全部；`skills/` **全部**（F7 只在标准件内定名，**不动资产实例**——留 §八 开放项 2）；`AGENTS.md`（改它须作者确认，宪法 §六.1；本批对回写清单 ② 的处置＝**不擅改**，`107` §七 记仍待）——**显式豁免（遵反模式 `#32`）**：日后若取得作者确认，**只放行 §七 自检信号表第 3 行判据那一格**（把 `[4]` 段的 `A-I` 改为 `A–J`），**不放行整件**；`scripts/check_content.py`；`docs/standards/` 中除 `测试与验收标准`／`命名标准`／`规格写作标准`／`运行环境标准` 外的**七件**。
- **显式豁免（遵反模式 `#32`）**：`scripts/check.sh`（头注 `:10` 与 `echo` `:138`）、`scripts/check_spec.py`（模块 docstring `:10`／`:49`，及各被改函数的 docstring）、`docs/standards/测试与验收标准.md`（§1.1 `:26`／§三 `:126`）**三件均属本批改动面**，理由＝`施工机制` §四 守卫变更回写清单 ①③④⑤ 的**必改面**；豁免**只放行该面，不放行整件**。
- 禁止动作：联网采集；改 `AGENTS.md` §七 的 A-I 行（须作者确认）；改 `skills/` 任何文件（含 F7 点名的异形实例）；把 F3／F4／F14 新判据接成**置红**（候选非阻断，`AGENTS.md` §五.2）；**新增检查号**（不新增 K，不改 `[4]` 段段序与段数）；改既有检查 A–G／I 的判据；把 §八 余下 20 条一次并入本批（见 §六）。

## 六、被否决的替代方案

- **不立闸、只在 `测试与验收标准` §4 登记一行缺口** → 否决：审计实测 341 条埋没的成因正是「只登记、不判」——G 表登记是**缺口留痕**、不产生**动作**，且登记本身也要靠人记得回看（本批 `G32` 只是把该缺口入册，真正堵口的是 F1 的条文与 F3／F4 的判据）。
- **把审计去重出的 30 条埋没项一次吃完** → 否决：30 条横跨四个主题面（工具判据／措辞与多载体／走查与实测／产品层待设计），单批改动面会越过十件，超出「一批＝一次施工＋一次审查」的可审规模（`104` 批即因改动面过大被成稿审查一次报出 5 条 P0）；故本批只吃「**同一教训 ≥3 件复现**」的 top10（其中 1 条已沉淀，实做 9 条），余 20 条列 §八 交 `108`。
- **把 F3／F4 做成置红（不判沉淀即拒绝归档）** → 否决：① `AGENTS.md` §五.2「候选永不拦截」；② 「该条值不值得长期留用」是人判，机械判据只能报候选；③ 置红会逼出「为过检而编条号」的新反模式（同 `#1` 族）。
- **F3 的锚点只取「首个反引号 token」一个** → 否决（部分）：条目常含多个 token 而只有一个入库，单锚点取法假阳率高；本规格取**两个锚点（首个反引号 token ＋ 首个编号）皆零命中才报**，并在 §三 断言 2 用正控夹具压住假阳。

## 七、本批核销（相关归档规格的开放项）

> **必列面**（`施工机制` §八 `:258`–`:259`）：① 前一批（`106`）§八 **全 7 条**；② 本批改动直接触及或使其状态可能变化者＝`105` §八 3／6（本批 F3／F4／F14 触碰其触发面，并由 F15 改标）。**不必重列**（只给索引指针）：`103` §八 全节——已由 `105` §七／`106` §七 两表承载（索引指针＝`105` §七／`106` §七；其中 1／3 的下游＝`105` §八 1／2，本表已列；7 已于该节自标作废）；`105` §八 4／5——`106` §七 已承载并处置（索引指针＝`106` §七）。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `106` §八 开放项 1 | 守卫射程的其余自述句（「轻」行声明／真机走查豁免／「不涉某面」／「断言全部可机械判定」） | 仍待 | 本批只做「归档前沉淀」这一族；该条的射程扩面触发未到 |
| `106` §八 开放项 2 | 「轻」行的定级守卫（前两项可机械核） | 仍待 | 触发＝同开放项 1 |
| `106` §八 开放项 3 | `103` 规格的归档（须先有搬动连带面扫描方法） | 仍待 | 本批不搬动结构、不归档任何件 |
| `106` §八 开放项 4 | 本批「照最重路径执行」的实例留痕 | 仍待 | 触发＝该判定在后续批次被援引或争议时 |
| `106` §八 开放项 5 | 新建 `.md` 不经 `patch_file.py` 形态闸 | 仍待 | **本批自身即第二次实例**（本规格首稿由宿主 `write` 直落、未经闸；以「写完立即单件 lint」兜住，见 §九）；触发＝作者裁定 |
| `106` §八 开放项 6 | 仓内 `.md` 行尾漂移（LF→CRLF）无机械守卫 | 仍待 | 触发＝触碰 `scripts/check.sh` 的 `[1]` 段；本批 F13 只动 `:10`／`:138` 的 `[4]` 枚举，**触发未满** |
| `106` §八 开放项 7 | `[A]` 行里的引号块只能放「目标侧」文本 | 仍待 | 本批 §二 各 `[A]` 行已按该律写（现状侧一律散文描述、引号块只放目标文本）；触发＝该律立成为条文时 |
| `105` §八 开放项 1 | 「移出两块」的连带面扫描方法 | 仍待 | 本批不搬动结构 |
| `105` §八 开放项 2 | 反模式表的「机械判据」列逐行补齐 | 仍待 | 本批 F11 只补 `#22` 一个实例，全表回填触发未到 |
| `105` §八 开放项 3 | 已归档规格里的教训是否已回填台账 | **本批处置** | 前置只读审计已回报（43 件／421 条／已沉淀 59／埋没 341／已作废 21）；本批 F1＋F3＋F4 立闸、F6–F11 落 top10 台账、§八 记余 20 条；并由 **F15** 改标该条末句（含「42 件」→「43 件」的数字订正） |
| `105` §八 开放项 6 | 检查 J 不核「成本字段」（触发＝下次触碰 `check_archive_record()`） | **本批处置** | F3 已触碰该函数 ⇒ 触发已满，同批补判据＝**F14** |
| `105` §八 开放项 7 | `AGENTS.md` §七 仍写「`[4]` 段 A-I」而实为 A–J | 仍待 | 改 `AGENTS.md` 须作者确认（宪法 §六.1）；本批只登记不擅改 |

- 汇总：`按条计 12 条：已履行 0｜作废 0｜本批处置 2｜仍待 10｜待作者 0 ＝ 12`

## 八、开放项

1. **审计余下 20 条埋没项交 `108`**（每条一句话标题，供 `108` 有账可依）：①「输出逐字节不变」作等价性断言时比对语料不得含本批改动件；② `git commit -- <路径>` 对未跟踪文件无效、须先 `git add` 再核再提交；③ `patch_file.py` 三个用法坑未写进工具文档；④「每个产物产出后拿模板做机械对规」通则只落了实例；⑤ 检查 A 有两处静默跳过（位置格只写件数、转义反引号 token）；⑥ `check.sh` 收尾汇总行写死清单、跳过段也说「已检」；⑦ 目标件运行中被删时工具会崩；⑧ `术语标准` §一 有反例没正例；⑨ `~~~` 围栏／`MD_LINK` 带 title／`_dir_split` 计数语义三项「规范放开须同步」无落点；⑩ 底座复述产品层数字会过期（「底座只写指针不写计数」未立法）；⑪ 二级形／三级形指针混用会读到空节；⑫ `施工机制` §八 另两份模板里还混括注式说明；⑬「要不要派产物审查／分流定级」全凭自判、无机器闸；⑭ `AGENTS.md` §三 lint 行要不要补环境前提指针（裁定「不加」但未闭环）；⑮ 归档件实测值随工具演化失效；⑯ 归档规格一多后引用与检索方式未定；⑰ 审查成本策略（纯 P2 是否并入下一批）；⑱ `施工机制:240`「190 KB」口径未核；⑲ `091` 双维审查 findings 没落进 §九（归档件只读、报告无落点）；⑳ 超阈加粗存量件清单漏列。触发＝`108` 立批。
2. **「随各自阶段批」异形实例待收口（`skills/` 内 2 处 ＋ `docs/product/` 1 处）**（`skills/abzu-outline/references/outline-workflow.md:48`、`skills/abzu-outline/references/outline-stage-spec.md` 各 1 处，及 **`docs/product/书目录契约.md:22`** 1 处——后者非 `skills/` 资产，同属 §五 禁改面，一并留此待收口）——本批 F7② 只在 `命名标准` 内定名，**未动资产**。触发＝下次触碰 `skills/` 资产的批次；**并注**：按 `施工机制` §二，触碰 `skills/` 资产的批次＝**重路径**（须产物审查 ＋ 真机走查判据），故该项不可夹带进标准路径批。
3. **审计明细（341 条）的长期落点**——本规格只承载审计的 30 条去重项与 top10 结论；余下明细的落点须**随主题**入 G 表／反模式表／标准条文，**不得新建台账件**（`AGENTS.md` §四 禁平行台账）。触发＝`108` 立批时逐条归主题。
4. **归档动作的机械闸只立了一半**——F1 是条文、F3／F4 是判据，但 `--verify` 仍**未接 `check.sh` 门禁**（`G32` 只登记该缺口）。触发＝评估 `--verify` 接门禁的时机（注意其 `:33` 自陈的「门禁运行点在制规格尚未归档」这一前提）。
5. **F3／F4 上线后的假阳观察**——本判据为语义近似（锚点 token 检索），首轮全仓实跑的候选数须记入下一批规格的核验结论。触发＝`107` 提交后首次 `bash scripts/check.sh` 的 `[4]` 段输出。
6. **F14 改按「节」判后现行件候选＝0**（规划方复核）——**改口径前**按「行」判时四件共 **11 行**（`103` **1**／`105` **5**／`106` **3**／`107` **2**），逐条复核 **0 条真阳**（皆「成本两字段单列一行」的等效写法）；**改按「节」判后现行四件 0 条**。触发＝已消；本条保留作**口径改判的留痕**（过报实例已记 `测试与验收标准` §4 `G19`）。
7. **`shell=True` 的宿主声明整改**——F10 只立条文，**射程面内不满足者**＝`scripts/check_spec.py` 自身（实测 3 处：`:25` docstring、`:418`、`:1792`（行号按施工终态重算）），其头注只陈宿主语义、未按新条声明「设计宿主／实测宿主」两值；且 §一.2 的「Windows（Git Bash）」约定与该件实走 `cmd.exe` 相抵。触发＝下次触碰该件的子进程调用面；裁定项＝该冲突本身（改约定文本，还是改实现）。
8. **`检查 A` 对断言 7 三处报「零命中核对未判（期望子句多值 …）」**——成因：期望解析把断言文里的**行号**（`:10`／`:138`／`:121`）也当成期望值 ⇒ 判「与命令无法对齐」。实测三处：`scripts/check.sh`（期望子句多值 `[2, 10, 138]`）、`docs/standards/测试与验收标准.md`（`[2, 1, 1, 26, 121]`）、`scripts/check_spec.py`（`[1, 10]`）。**本批不为消它而删行号**——行号是判据的可核面，删了反而降低可审计性。触发＝下次触碰该断言面。
9. **`检查 C` 对加粗的归一化不对称（工具面，本批实测新发现）**——`107` 稳态 `未落实或已漂移` 共 **9** 条，逐条核后**至少 8 条的引号块实际已逐字落地**：目标文本含 `**加粗**`，而 `检查 C`（`check_pairs()`）提取的比对串**已去加粗标记**、却与**含加粗的原件**比对 ⇒ **恒判「未落实」**（实例：`宿主版本／模型／会话形态` 正例行、`规格写作标准` 第 14 条、`#22` 义务句实例、`105` 两处改后整行、`106` 触发句）。**后果**：凡 `[A]` 目标文本含加粗者，施工后仍留**永久假阳**，噪声会掩盖真阳性（本批为首次显形）。触发＝作者裁定，或下次触碰 `check_pairs()` 的取词面（修法二择一：比对前**两侧同等**去加粗，或提取时**保留**原标记）。

## 九、审查记录

- 2026-09-16｜**成稿审查（独立只读 subagent）**｜findings 共 18（P0:3 P1:7 P2:8）｜结论**修订后通过**｜工具往返数 57｜周期时长 <1h。**逐条处置**：**P0-1**（F1 只落一个载体，`规格写作标准` §6 仍写归档要求 ⇒ 第二载体）→ 收口为单一真源＝F1（`施工机制` §三 硬要求）＋**新 F16**（`规格写作标准` §6 改指针；因 `检查 C` 每行只解析一个目标件，故拆两行）；**P0-2**（F14 判据自相抵 ＋ 上线影响失实）→ 判据改「整行含三词之一」（可复现 9 行）＋ 认非占位值 ＋ 影响改写为 `103:1／105:5／106:3`；**P0-3**（F7② 实测面漏活件）→ 括注改「`skills` 内 8 vs 2／全仓活件 8 vs 3」并补 `docs/product/书目录契约.md:22`（本批不改，并入 §八 开放项 2 标题）；**P1-1**（空锚点行为未定义）→ 两锚点皆缺 ⇒ 跳过 ＋「检查 J 汇总」加 `｜无锚点条目 {n} 条`，并把实测 `0／0` 写入 F3；**P1-2**（§八 定位未写死）→ 定位模式写死含「八、」、条目起始符并认「连字符加空格」，断言 2 补 §七 抢先命中夹具；**P1-3** → F4 补改述 `check_nuclear()` docstring；**P1-4** → 新增 **F15**（改标 `105` §八 3／6）；**P1-5** → F10 改述为「显式声明设计宿主与实测宿主」＋ 记射程面内 `check_spec.py` 3 处 `shell=True` 与「实际执行宿主非 Git Bash」的前提冲突；**P1-6** → F8 扩两处（`:58` ＋ 正例 `:103`）；**P1-7**（现状侧引号块）→ F13 及 §二 各 `[A]` 行现状侧一律改散文、引号块只留目标串（实测现状侧 4 条；建制期余 **8** 条目标侧属预期）；**P2-1** → F13 依据补 ⑤；**P2-2** → 断言 7 第三项期望写死 1 ＋ 加 F14 影响的复算附注；**P2-3** → 断言 8 先 `mkdir -p`；**P2-4** 并入 P0-2；**P2-5** → §一 ⑤ 按实现改正（缺节**整段静默**，非出候选）；**P2-6** → 全部「审计报告」改「前置只读审计（未落盘；结论摘录见 §一）」＋ `G33` 给可复算判据；**P2-7** → F9 依据行号改 `:222`／`:225`、件数口径统一为 **7 件**；**P2-8** → §七 表前补索引指针行（照 `088`–`092` 诸批先例：**表前引用块**，不入表体、不动汇总算术）。**观察项落地**：① §四.5① 改指认 `检查 G`「路径定级核对」（只覆盖自称「快」者，不覆盖「标准」）；② F3 预期收益已下调（五处台账里任一通用 `§N`／`GN`／`#N` 命中即压制报告）；③ §五 对 `AGENTS.md` 补显式豁免（只放行 §七 第 3 行那一格，不放行整件）。**止损**：本批 P0 ≥ 2，按 `施工机制` §五 止损第 2 条，后续只做**窄域审**（只核本轮修复项 ＋ 其直接影响面，不重开全文审）。
- 2026-09-16｜**整改期三处据实测改判（规划方核定）**｜整改方实跑后与规划方转述冲突三处，均以其实测为准并核定如下：**① 「`未落实或已漂移` 压到 0」在建制期不可达**——原 12 条＝**4 条现状侧**（真缺陷，改散文后消净）＋8 条**目标侧**；`检查 C`（`check_pairs()`）按行切分核对引号块，属**施工后**判据（**实证：已施工的 `106` 件此项＝0**）。故整改后 13 条**全为目标侧**，判为预期；强压 0 只能删目标侧引号块、转而触发「无逐字目标文本」，得不偿失。**② F14 判据口径改判**——规划方转述的「取第 2 个 `｜` 字段」实测**只得 4 行**、复现不出 9 行；改用「**整行含「成稿审查／产物审查／单维核对」之一且缺两字段**」可复现 `103:1／105:5／106:3`＝9 行，**核定采此**；该口径**偏宽（宁多报不漏报）**——「成本记账」行会因「成稿审查段／产物审查段」子串入选，属**可接受的过报**（候选非阻断，影响已记 §八 开放项 6）。**③ 新增「纯数字／批次号不计为锚点」**——整改方实测 §七 `仍待` 行与 §八 条目的首个反引号 token 多为 `106`／`105` 这类**批次号**，而批次号在五处台账中**恒命中** ⇒ 不加此条该闸**一上线即完全失效（恒静默）**；此为保障判据可用的**必要条件**，核定采此（规划方转述的「两锚点皆缺 5 行／6 项」系取词口径不同，实测为 0／0）。**另核定两处结构性调整**：④ P0-1 拆为 **F1（`施工机制`）＋ F16（`规格写作标准`）两行**——因 `检查 C` 每行只解析**第一个**可解析目标件（`_row_target_file()`），`[A]` 行不得跨两个目标件；⑤ §七 的索引指针按既往先例写成**表前引用块**、不入表体，故**汇总算术不变**（`12 条：本批处置 2｜仍待 10`——F15 改的是 `105` 件文本，不改本表档位）。
- 2026-09-16｜**窄域审（`施工机制` §五 止损第 2 条：findings 中 P0 ≥ 2 ⇒ 只核原 P0 修复项与其直接影响面，不重开全文审）**｜findings 共 6（P0:3 P1:2 P2:1）｜结论**修订后通过**｜工具往返数 56｜周期时长 <1h。**逐条处置**：**P0-A**（载体收口未闭合——`施工机制:190`／`:219`（§八 模板行）、`命名标准:31`／`:65`／`:136`、`规格写作标准:131` 同形态并存）→ 新增 **F18**（改 `施工机制:190`／`:219` 为指向 §三 闸门）＋ **§一 新增锚点 ⑮**（全载体扫描 ＋ **逐处裁定**：要求性载体 4 处改、描述性 6 处不改，使载体面「扫过且逐处有裁定」）；**P0-B**（F14 影响数失实）→ 改 **10 行／4 件**（`103`:1／`105`:5／`106`:3／`107`:1），§八 开放项 6 与断言 7 附注同批同步；**P0-C**（F3 的「§八 行首项 6 条」失实）→ 改 **7**（实测 `:251`–`:257`）；**P1-A**（F14 判据两义：字段名存在性 vs 字段值判定）→ 判据与复算附注**同源同口径**，明写两字段**须带非占位值**；**P1-B**（F3 取法与实测数不自洽）→ 取法**写唯一**（锚点一＝首个反引号跨度；为批次号则改取首个「非批次号」跨度；锚点二＝首个 `§N`／`GN`／`#N`；**两锚点皆取不到即出候选**、不跳过），**并取消本规格对候选条数的预估**（改注「不再预估，首轮全仓实测数记入下一批」）；**P2-A**（F16 无 §三 断言）→ §三 新增**断言 1b**（F16／F18 各一条命令一条期望）。**观察项落地**：④ `105` §八 3 正文「（42 件归档）」按实测改「（**43 件**归档）」（并入 F15）；⑤ 断言 7 三处「零命中核对未判」**不追改**，登记 §八 开放项 8（成因＝期望解析把行号当期望值）。**本轮为窄域审，射程＝3 条 P0 修复面 ＋ 三处改判复核，P1／P2 的其余修复面未复核。**
- 2026-09-16｜**窄域审整改期五处报备（规划方核定）**｜① 整改方在裁定外把新增断言 1b 的期望句由「期望 **2**（F18① 与 F18② 各一处）」改为「（**两个落点各一处**）」——原写法给 `检查 A` 造出新一条「零命中核对未判（期望子句多值）」噪声，改写后归零（4→3）、**未删任何行号**、与「不追改观察项⑤」不冲突 ⇒ **核定**。② 「F3 该数改 7」与「取消候选预估」的张力——处置＝「§八 行首项 **7**」作**结构实测**保留，「两锚点皆缺 N 条」作**候选预估**删除 ⇒ **核定**（两者不同族：结构实测可复算、不随判据口径变；候选预估随口径变，故本规格不落）。③ 按写定取法（含「为批次号则改取首个非批次号跨度」兜底）复算，§八 七条**全部取得到**非批次号跨度（`:251`→`git commit -- <路径>`；`:255`／`:256`→`bash scripts/check.sh`）⇒「两锚点皆缺」＝**0 条**，窄域审的「3 条」系**未套兜底**口径；因已取消预估，规格内不落该数 ⇒ **核定**——「**无锚点条目 {n} 条**」是**运行期输出**，不是规格预估数。④ F3 ②／F4 由上一轮的「跳过 ＋ 计数格」改为「**出候选 ＋ 计数格**」并存 ⇒ **核定**：闸门目的正是抓「写不出可查落点」的条目，跳过会让它在最含糊处失明。⑤ 整改方**主动**把 `施工机制:113`（§五 止损第 1 条的**作废批**移入路径）收进 §一 ⑮ 的扫描并给了裁定（作废批前提已失效 ⇒ 本批不改、教训按主题处置）⇒ **核定**：「载体面闭合」的判据是**扫过且逐处有裁定**，不是命中数少。**规划方复核（窄域审射程面，自跑）**：A 段三串载体扫描＝`施工机制` 4／`命名标准` 3／`规格写作标准` 1（另 `106` 在制件 1 处属批内记录、非要求载体；`.zcode/` 下命中共属工具工作件、非仓内件）；B 段复算＝`103`:1／`105`:5／`106`:3／`107`:1＝**10 行/4 件**（与本规格所记一致）；F16／F18 目标串基线均 **0**；lint 0 issue／`未落实或已漂移` 17 条**全为目标侧**／`无逐字目标文本` 0／检查 G「不符 0 项」／检查 H「不符 0 项」／检查 I「异常 0 项」／检查 J「N 与档位不符 0 条」／`check.sh` 退出 0。**收口**：本批共吃 1 轮全文审（18 条）＋ 1 轮窄域审（6 条）＋ 两轮整改；按 `施工机制` §五 止损第 1 条**不再开第三轮**，转入施工。
- 2026-09-16｜**施工（后台 subagent；改动 17 项／9 件）＋ 规划方整改追加 1 项（`106` §八 6 假账订正）＝ 18 项／9 件**｜**§三 十条断言逐条实跑**（其中断言 6 第一项 1 偏差，见下）｜门禁 `bash scripts/check.sh` 退出 0（末行「检查全绿」）。**新判据首轮候选（如实记，§八 开放项 5／6 要求）**：①「开放项无台账归宿」**0** 条；②「开放项无锚点，无法核归宿」**3** 条（`103` §八 1／2、`105` §八 2）；③「仍待项已断链」**16** 条（`103`:3／`105`:**3**／`106`:**4**／`107`:6——分件数已按实测订正，原记 `105`:4／`106`:3 系记反）；④「`§九 成本字段`」**11** 条（`103`:1／`105`:5／`106`:3／`107`:**2**）——**真因订正**：`107` 那 2 条系**记录行自身子串命中**（该行正文含「成稿审查 57」「产物审查」等字样），**不是**「计数面含汇总行与否」（该初判有误）；该判据**已于本批产物审查后改按「节」判，现行四件归 0 条**。**偏差 3 条**：**P1-1**（断言 6 第一项 期望 1／实测 2）→ 判因＝F8 经窄域审扩为两处、该期望未随扩面同批更新；**裁定＝F8 落两处为是、期望改 2**（§三 已改并注明裁定）；**P1-2**（F18① 目标引文未含句末标点 ⇒ 落地时曾得空加粗）→ 施工方补回「。」，**终态 `grep -c '\*\*\*\*'` ＝ 0**，规划方逐行复核 `施工机制:190` 与 `规格写作标准:78` 两处落地文本读得通 ⇒ **已处置**；**P2-1**（`CHANGELOG` 的 R 只填施工段 84）→ **裁定＝按 §八 全批口径订正为 197**（84＋成稿审查 57＋窄域审 56），已在 `CHANGELOG` 落地。**观察项**：A（`107` §二 F15 行的现状侧引号块致 `105` 新增 3 条 `检查 C` 候选）→ 规划方已把 F15 该三处引号块改散文，候选随之消；B（§八 8 的三条「零命中核对未判」施工后自行转形为自噬预警）→ 如实记；C（`命名标准.md` 工作树整件 CRLF）→ **规划方实测：仓根 `.gitattributes` ＝ `* text=auto eol=lf`，入库归一，工作树 CRLF 属本地噪声**；据此**新开 F17 订正 `106` §八 开放项 6 的假账**（该条原称「无机械守卫」与既有裁定相抵）。**本轮为该规格第 1 轮施工，无第二轮。**
- 2026-09-16｜**产物审查（独立只读 subagent）**｜findings 共 11（P0:1 P1:6 P2:4）＋ 观察项 7｜结论**修订后通过**｜工具往返数 73｜周期时长 <1h。**逐条处置**：**P0-1**（`G33` 的「可复算判据」不可复现——写 `grep -rl '回归测试件'` 实测 **0** 件，真串为 `测试件`）→ 已改为 `grep -rl '测试件'`（规划方复跑得 **7** 件，件号与行内所列一致）；**P1-1**（`§九 成本字段` 判据未进判据面枚举）→ `check.sh` `:10`／`:138`、`测试与验收标准` `:26`／`:126` 四处各补「／§九 成本字段核对」，`check_spec.py` 模块 docstring `:10` 同补（兼作断言 7 的唯一出口）；**P1-2**（行号随施工漂移）＋**观察项 4** → 全部按终态重算：`运行环境标准` `:414`→`:418`／`:1602`→`:1792`、`命名标准` `:136`→`:137`、`规格写作标准` `:131`→`:132`、归档句 `:78`→`:79`、`测试与验收标准` §三 `:121`→`:126`；**本批自身的代码改动又使 `check_spec.py` 行号整体位移**，规格内凡引该件行号者（§一 ⑤⑥、F3、必读输入）亦按终态重算（`check_nuclear` `:1039`／`check_archive_record` `:1385`／`verify_report` `:1811`）；**P1-3**（反模式「回填族」失守）→ `施工机制` §六 `#21`／`#24` 两行「事故出处」列各补 `107` 实例；**P1-4**（`CHANGELOG` 的 R 漏产物审查段）→ R 由 197 订正为 **270**（施工 84 ＋ 成稿审查 57 ＋ 窄域审 56 ＋ 产物审查 73）；**P1-5**（`§九 成本字段` 判据首轮 11 条／**0 条真阳**＝过报）→ **改按「节」判**（§九 节内须有任一行同时带两字段且两值非占位；整节皆无才出**一条**候选，文案「`§九 含成本字段`」），现行四件候选 **0** 条；同批改述 §二 F14 判据、§三 断言 7 附注、§八 6，并把过报实例记 `测试与验收标准` §4 `G19`；**P1-6**（`检查 C` 对加粗的归一化不对称）→ 新开 `测试与验收标准` §4 **`G37`** 行；**P2-1**（指针引了不存在的标签）→ `施工机制:190` 与 `规格写作标准:79` 两处改引 §三 实际起句；**P2-2**（`check_archive_record()` docstring 两句自相抵）→ 按实现改正（静默条件＝「无 §九 节**且**各处候选皆空」；无 §九 而有 §八 候选时仍打汇总行）；**P2-3**（`106` §八 6 残留「全仓现为纯 LF」与实测相抵）→ 改述为「**入库面**现为纯 LF——`git ls-files --eol` 实测 87／87 为 `i/lf`；工作树含 CRLF 者 5 件，按 `运行环境标准` §一.3.3 既有裁定不折腾」；**P2-4**（存量计数未随批重测）→ `G31` 的「现存 46 件」改 **47**（archive 43 ＋ 在制 4）、`105` §八 的「反模式表现 30 行」改 **32 行（`#1`–`#32`）**；**观察项 1／2／3** → 本节施工段的分件数按实测订正、并删去错误归因；**观察项 5** → 「断言 23 个取值点」已改述为可复算口径。**另**：本轮另修一处**规格侧残留**——`105` §九 成本记账行原只写「施工段 31」而未点名「工具往返数」，致其仍出 1 条成本候选；已补字段名（该件为本批已动件），四件候选归 **0**。**本轮为产物审查后整改，射程＝11 条 findings ＋ 5 条须同批修的观察项。**
