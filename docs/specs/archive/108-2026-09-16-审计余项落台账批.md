# 108-2026-09-16-审计余项落台账批

> 定位：把 `107` 前置只读审计余下的 **20 条埋没项**逐条实读后**落进长期台账**（G 表／反模式表／标准条文正文），并顺手清一处死代码。`107` 立了「归档前经验沉淀闸」、落了 top10 复现项；本批收口审计的**去重余项**——**不新建台账件**（`AGENTS.md` §四 禁平行台账），一律复用既有载体。
> 路径判定（`施工机制` §二／`规格写作标准` §1 判定表）：**标准路径**（跨 9 件、新增与修改规范条目）＋ **双维产物审查**（改标准条文）。真机走查**不适用**：该结论可由 §二 逐行清单直接读出——清单内无 `skills/` 资产正文、无产物契约字段与状态值枚举。**并注**：本批触碰 `scripts/patch_file.py`／`scripts/check_spec.py` 两个**开发守卫**，按 §二 重路径的三条触发（动 `skills/`／改域内流程与产物契约／架构级）**均不成立**，故仍属标准路径。
> 自由度分布：[A]×19 [B]×2 共 21 行
> 必读输入：`docs/specs/107-2026-09-16-归档前经验沉淀闸批.md` §八 开放项 1（20 条标题）与 §七；`docs/specs/施工机制.md` §五 报告落点（`:107`）、§七 提交与登记（`:183`–`:192`）、§八 规格模板与三份模板（`:197`–`:294`）；`docs/standards/测试与验收标准.md` §4 已知缺口表（`:67`–`:106`，末行 `G42`）；`docs/standards/文档写作标准.md` §3 AI 友好写作（`:36`–`:48`）；`docs/standards/术语标准.md` §二 正例与反例（`:39`–`:43`）；`docs/standards/产物契约标准.md` §2 通用纪律（`:19`–`:26`）；`docs/standards/规格写作标准.md` §5 第 10 条（`:69`）；`scripts/patch_file.py` 头注（`:1`–`:40`）；`scripts/check_spec.py` `:50`–`:54`、`:1266`（现为 `COST_FIELDS`）与 `:1372`
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-16）

> 20 条来自 `107` §八 开放项 1 的一句话标题。**每条均实读原文核过**；「实测」列给命令与结果。审计件本身**未落盘**，故本规格一律不引「审计报告」为仓内物。

| # | 事实（标题摘要 ＋ 实读结论） | 实测 |
| --- | --- | --- |
| ① | 等价性断言的比对语料不得含本批改动件——**已沉淀** | `grep -c '等价性断言的比对语料须排除本批全部改动件' docs/standards/规格写作标准.md` ＝ **1**（§5 第 10 条，`:69`） |
| ② | `git commit -- <路径>` 对未跟踪文件无效、须先 `git add` 再核再提交——**未立** | `grep -c '对未跟踪文件无效' docs/specs/施工机制.md` ＝ **0**；§七 现有相关句只讲「禁 `git add -A` 后直接提交」（`:188`）与反模式 `#8`（`:148`），**未及 pathspec 与未跟踪件的语义** |
| ③ | `patch_file.py` 三个用法坑未写进工具文档——**未立** | `grep -c '锚片段未含到行尾' scripts/patch_file.py` ＝ **0**；头注「用法与参数」段只讲参数语义，三坑中仅「黏连」在 `:225` 的**代码注释**里出现一次（非文档面） |
| ④ | 「每个产物产出后拿模板做机械对规」通则只落了实例、通则本身未进标准件——**未立** | `grep -c '拿模板做机械对规' docs/standards/产物契约标准.md` ＝ **0**；§2 通用纪律五条（`:19`–`:26`）无对规通则 |
| ⑤ | 检查 A 有两处静默跳过（位置格只写件数时命中门失效；转义反引号 token 不解反转义）——**部分已登记** | `grep -c '静默跳过' docs/standards/测试与验收标准.md` ＝ **0**；但同族已有 **`G27`**（`:91`，转义**竖线** token 按字面取数）与 **`G10`**（`:74`，`[A]` 逐字核对载体覆盖缺口）——**「转义反引号 token」一支可并入 `G27` 射程**，「位置格只写件数」一支**无载体** |
| ⑥ | `check.sh` 收尾汇总行写死清单、**跳过段也说「已检」**——**未立**（与 `G18` 互补） | `grep -c '收尾汇总' docs/standards/测试与验收标准.md` ＝ **4**，其中 **`G18`**（`:82`）管的是「段计数对头注形态破坏无容错」；**「跳过段被计入『已检』」无载体** |
| ⑦ | 目标件在运行中被删时工具会崩——**未立** | `grep -c '运行中被删' docs/standards/测试与验收标准.md` ＝ **0** |
| ⑧ | `术语标准` 有反例没正例——**属实（结构已变，实质未变）** | §二「正例与反例」在 `:39`；`:42` 的 ✗ 反例（把 skill 资产与仓库治理文档同等对待）**无配对 ✅ 正例**（`:41` 的 ✅ 讲的是「术语用平台标准词」，与 `:42` 不同题） |
| ⑨ | `~~~` 围栏／`MD_LINK` 带 title／`_dir_split` 计数语义三项「规范放开须同步」无落点——**未立**（三条） | `grep -c '围栏语言' docs/standards/测试与验收标准.md` ＝ **0**；`grep -c 'MD_LINK' …` ＝ **0**；`grep -c '_dir_split' …` ＝ **0** |
| ⑩ | 底座复述产品层数字会过期（「底座只写指针不写计数」未立法）——**未立** | `grep -c '底座只写指针' docs/standards/文档写作标准.md` ＝ **0**；`grep -c '不写计数\|只写指针' docs/standards/命名标准.md` ＝ **0** |
| ⑪ | 二级形／三级形指针混用会读到空节（「引用须写到能唯一落点的那一级」未立）——**未立** | `grep -c '唯一落点的那一级' docs/standards/文档写作标准.md` ＝ **0**；§3 现有第 4 条只讲「引用带节锚点」（`:39`），未及**级深必须唯一落点** |
| ⑫ | `施工机制` §八 另两份模板（参考研究裁定表／核验请求）里还混括注式说明——**属实** | `:269` 模板表体**第 4 格**现文为「采纳→落位到文件节与自由度；已覆盖→指认我方既有对应；拒绝→理由（防未来重复纠结）」——**括注式说明写在占位单元格内**，应移为表注 |
| ⑬ | 「要不要派产物审查／分流定级」全凭自判、无机器闸——**部分已沉淀** | 分流定级一支**已由 `106` F1 立闸**：`grep -c '路径定级' scripts/check_spec.py` ＝ **4**（检查 G 的「路径定级核对」）；**产物审查派发那一支仍无任何机械检查**（`grep -n '产物审查' scripts/check_spec.py` ＝ **8 处**（F16 删 `COST_WORDS` 后由 9 降 8），**全为注释／docstring 的沿革引用，无一是判据**） |
| ⑭ | `AGENTS.md` §三 lint 行要不要补环境前提指针（裁定「不加」但未闭环）——**未闭环** | `AGENTS.md` §三 为「常用命令」节；`sed -n '30,40p' AGENTS.md` 的 lint 行为 `npx markdownlint-cli2 "**/*.md"`，**未标环境前提**；改 `AGENTS.md` 须作者确认（宪法 §六.1） |
| ⑮ | 归档件里的实测值随工具演化会失效——**未立** | `sed -n '183,192p' docs/specs/施工机制.md` 的归档件纪律段 `grep -c '实测值'` ＝ **0**；该段现只有「原则只读 ＋ 两处例外」 |
| ⑯ | 归档规格一多后引用与检索方式未定——**已沉淀** | `grep -c '跨批检索以各批规格的核销表为唯一索引落点' docs/specs/施工机制.md` ＝ **2**（§八「本批核销」口径 `:264` 与其引用） |
| ⑰ | 审查成本策略（纯 P2 是否并入下一批）——**待作者** | 属策略裁定项；`施工机制` §五 现无该口径 |
| ⑱ | `施工机制:240`「190 KB」口径未核——**属实（行号已漂至 `:256`）** | `grep -n '190 KB' docs/specs/施工机制.md` ＝ **`:256`**；现文「088 实测定名十件共 190 KB」，**未给件集判据**（哪十件、按字符还是字节） |
| ⑲ | `091` 双维审查 findings 没落进 §九（归档件只读、报告无落点）——**未立** | `sed -n '95,118p' docs/specs/施工机制.md` 的报告落点段（`:107`）`grep -c '归档后补记'` ＝ **0**；该段现只讲「逐条追加到当批规格的『审查记录』节」 |
| ⑳ | 超阈加粗存量件清单漏列 `施工机制` 等件——**未立** | `grep -c '超阈存量件清单' docs/standards/测试与验收标准.md` ＝ **0**；`G1`（`:69`）现文只讲阈值未立法与「入口型文档」无定义，**未要求列存量件清单** |
| ㉑ | 死代码：`COST_WORDS` 在 `107` 整改期改按「节」判后不再被使用 | `grep -n 'COST_WORDS' scripts/check_spec.py` ＝ **3 处**，全非使用面：`:1266` 常量定义、`:52` 维护入口枚举、`:1373` docstring 的历史陈述 |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制.md` §七（`:188` 的「一域一批」行之后另起一行） | 新增一行，**逐字**：「- **未跟踪件不能被 pathspec 命中**：`git commit -- <路径>` 对未跟踪文件无效（pathspec 不匹配、静默漏掉该件）——未跟踪件须**先 `git add`**、再以 `git diff --cached --name-status` 核暂存面、再提交；提交后以 `git show --stat` 复核该件**确在提交内**（与 `#8`「`git add -A` 后不核对」互补：本条管 add 与 pathspec 的语义，`#8` 管核对面）。」 | [A] | 锚点②；`#8`（`:148`）；反模式 `#12`／`#20`（凭记忆下结论） |
| F2 | `scripts/patch_file.py` 头注「用法与参数」段（`:25` 的 `--no-lint` 行之后） | 新增三行，**逐字**：「  · **坑一：`--set` 只换锚片段**——锚片段未含到行尾时，行尾残文会留在原处（表现为表行多出单元格、句子被截半）。**新文本必须包含从锚片段到行尾的全部内容。**」／「  · **坑二：`--insert-before` 与锚行黏连**——锚行有残余行尾时新文本末行会与它连成一行；插行前先确认锚行无残尾。」／「  · **坑三：回读明细只打印前 120 字符**——真分歧点可能落在截断之外；核验须**再读一次目标件原文**，不得只看回读输出。」 | [A] | 锚点③；`103` §八 5 的三条使用面观察（`107` 前置审计转记） |
| F3 | `docs/standards/产物契约标准.md` §2 通用纪律（`:24` 第 4 条之后） | 新增第 6 条，**逐字**：「6. **每个产物产出后须拿模板做一次机械对规**——把产物与该产物**所属模板**逐段对照（节序、必备字段、状态值枚举）；对规结论落产物状态行或当批核销，**不凭记忆声索「已对规」**。出处＝`archive/099` §八 4（现文逐字：「本批只落 1.1 的实例（F12）；通则（「每个产物产出后须对其模板做机械对规」，拟落 `docs/standards/产物契约标准.md`）**同批未做**」），同族仍待见 `archive/100` §七 核销——本条把该实例升为通则。」 | [A] | 锚点④；`107` §八 开放项 1 之 ④ |
| F4 | `docs/standards/测试与验收标准.md` §4 表末（`G37` 行 `:101` 之后） | **两件事**：① 新增一行 `G38`（4 格，列位＝编号｜缺口｜状态/处置｜触发条件；第 3 格留空）：第 2 格**逐字**「`检查 A` **位置格只写件数**时命中门失效：该行的定位判据不成立，却既不报候选、也不计入「零命中待复核」——**靠人跑才兜住**；与 `G10`（`:74`）同族但不同面（`G10` 管「`[A]` 逐字核对载体覆盖缺口」）」；第 4 格＝「触发＝位置格判据被触碰时」；② **同族另半并入 `G27` 行**（不另开行）：在 `G27` 行末补一句，**逐字**「**并注**：同族＝**转义反引号 token** 亦不解反转义（`107` 前置只读审计埋没项 ⑤ 之半，本批并入本行以便同面触发）」；③ **同表 `G31` 行的在制计数随批重测**：`G31` 现文「现存 **47** 件（archive 43 ＋ 在制 4）」→「现存 **48** 件（archive 43 ＋ 在制 **5**）」（实测 `ls docs/specs/[0-9][0-9][0-9]-*.md \| wc -l` ＝ 5，含本件 `108`）；④ 同批复核该表其他计数句（如 `G19`／`G37` 的件数声称） | [A] | 锚点⑤；同表 `G10`（`:74`）／`G27`（`:91`）体例；反模式 `#4`（同族判据分组不重叠） |
| F5 | 同表 §4 表末（`G38` 行之后） | 新增一行 `G39`（4 格，同上列位；第 3 格留空）：第 2 格**逐字**「**`check.sh` 的全绿收尾行把「跳过」计成「已检」**：该行（`scripts/check.sh` 末行）打的是**写死的段名清单**「已检：真目录／lint／…」，而段级跳过已由 `_seg_summary()` 正确汇总在**上一行**「共 N 段：已跑 a｜跳过 b」——**缺口＝段被跳过时末行仍把该段名列进「已检」**，读者只看末行即被误导；与 `G18`（`:82`，段计数对头注形态破坏无容错）互补：`G18` 管**少计**，本条管**跳过被算作已检**（`测试与验收标准` §一 要求「跳过不得静默」，本条是该要求**在末行上的落实缺口**）」；第 4 格＝「触发＝全绿收尾行的段名清单被触碰时」 | [A] | 锚点⑥；`G18`（`:82`）；`测试与验收标准` `:31`「跳过不得静默」 |
| F6 | 同表 §4 表末（`G39` 行之后） | 新增一行 `G40`（4 格，同上列位；第 3 格留空）：第 2 格**逐字**「**目标件在扫描过程中被删／不可读时工具会崩**：`check_spec.py` 读目标件无「消失即跳过」兜底，异常路径直接抛栈（`075`／`076`／`077` 三批各记一次，裁定「非真实场景」后未补防护）」；第 4 格＝「触发＝该异常再现在门禁或核验中」 | [A] | 锚点⑦；`107` §八 开放项 1 之 ⑦ |
| F7 | 同表 §4 表末（`G40` 行之后） | 新增一行 `G41`（4 格，同上列位；第 3 格留空）：第 2 格**逐字**「**三项「规范放开即须同步」无落点**（合并一条，同族）：① `~~~` 围栏（`extract()` 只认三反引号）② `MD_LINK` 带 title 的链接形态 ③ `_dir_split()` 的计数语义——三者的**规范一旦放开**，须同批同步到实现，但**无任何判据或条文承接**」；第 4 格＝「触发＝上述任一形态的规范被放开时」 | [A] | 锚点⑨；`107` §八 开放项 1 之 ⑨；`#22`（同载体多副本） |
| F8 | `docs/standards/术语标准.md` §二 正例与反例（`:42` 的 ✗ 行之后） | 新增一行正例，**逐字**：「- ✅ **治理文档与 skill 资产分属两轨**：治理文档之间可相对链接，skill 资产禁跨目录链接、对外只留「维护出处」标注。」 | [A] | 锚点⑧；同节体例（该 ✗ 须有配对 ✅）；`命名标准` §5 |
| F9 | `docs/standards/文档写作标准.md` §3 AI 友好写作（`:43` 第 6 条「禁时间敏感表述」之后） | 新增一条，**接在现末条（`9. 箭头`）之后编号为 `10.`**，**逐字**：「10. **底座不写产品层的计数**：治理文档只写指向真源的指针，不复述产品层会变的数字（词条数、字段数、件数、字数）——数字写进底座即**从落地那刻起开始过期**；确需引用时改写为「见 `X` §N」。」 | [A] | 锚点⑩；该节第 6 条同族（同为「防过期」）；`107` §八 开放项 1 之 ⑳。**编号裁定（实测）**：§3 现有编号 `1.`–`9.`（末条＝`9. 箭头 →`，`:46`），既有 `7. 路径用正斜杠`／`8. 加粗`／`9. 箭头` **原样不动**，故新条**接末编号为 `10.`／`11.`**（**不得插 7./8. 重编号**——重编号会使 `§一.3.8` 两处活跃引用失准：`测试与验收标准:69` 与 `文档写作标准:118`，实测 `grep -c '§一.3.8' docs/standards/*.md` ＝ 2 处；本条**不牵动**它们）。依据＝`施工机制` §四 规则新增回写清单（射程面须同批核） |
| F10 | 同节（F9 新增条之后） | 新增一条，**接在 `10.` 之后编号为 `11.`**，**逐字**：「11. **引用须写到能唯一落点的那一级**：写 `X §N` 时，`§N` 须是**该内容实际所在的那一级**（二级形 `§一` 与三级形 `§一.3` 混用会读到**空节**——父节只承载子节标题、正文在子节）；判据＝照该指针去读，**必须直接读到内容**，读到空节即降级或升格重写。」 | [A] | 锚点⑪；`107` §八 开放项 1 之 ㉑→**之 ⑪**；`文档写作标准` §3 第 4 条（`:41`，只讲「带节锚点」）——**行号按实测改**（本条原写 `:39`）。编号同 F9（接末为 `11.`，既有 7./8./9. 不动） |
| F11 | `docs/specs/施工机制.md` §八 参考研究裁定表（`:269` 的模板表体**第 4 格**） | 把该格的**括注式说明移为表注**：**第 4 格（落位或理由）**现文含「采纳→落位到文件节与自由度；已覆盖→指认我方既有对应；拒绝→理由（防未来重复纠结）」→ 改为**占位**「{落位或理由}」；**第 1–3 格原样保留**（实测该行四格＝`['1','{对方怎么做的}','采纳／已覆盖／拒绝','采纳→落位…拒绝→理由（防未来重复纠结）']` ⇒ 括注确在**第 4 格**）。**落点为两处**（施工期实测）：**表头格**（`施工机制:269` 该表列头第 4 格）＋ **表体第 4 格**（`:271` 同格）——两处现均已成占位 `{落位或理由}`，表注在 `:267`；断言 5 的「`{落位或理由}` 命中」即覆盖此两处）；并在表下新增一行表注，**逐字**：「表注（**取值口径**）：**采纳**→落位到文件节与自由度；**已覆盖**→指认我方既有对应；**拒绝**→理由（防未来重复纠结）。」 | [A] | 锚点⑫；`施工机制` §八 的模板区纪律（模板体只放占位、说明移表注——同批 F1 的 `107` 先例） |
| F12 | `docs/specs/施工机制.md` §七 归档件纪律（`:192` 该行句末） | 句末追加半句，**逐字**：「——**并注**：**归档件内的实测值**（计数、行号、字节数等）随工具演化会失效；引用归档件实测值时须**标明工具与基线**，**不得据其直接下结论**（实例：`archive/060` 的「`check.sh` 39 行」至今仍是该件的被报候选）。」 | [A] | 锚点⑮；`#28`（计数类须附实测命令与单位）；`107` §八 开放项 1 之 ⑮ |
| F13 | `docs/specs/施工机制.md` §五 报告落点（`:107` 该行句末） | **按终态改述目标文本**（P2-1 的整改把例外半句移到该 bullet **句末**、并以 `；` 接——**该结构更正确故保留**；本行据此**把目标文本对齐终态**，**不**把落盘挪回句首）：将该项原句末的 `。` 改为 `；`，并追加，**逐字**：「；**例外：归档后补记**——采纳的修订入规格后才交作者确认。」 | [A] | 锚点⑲；`107` §八 开放项 1 之 ⑲；`施工机制` §七 `:191` 归档件纪律（两处例外） |
| F14 | `docs/specs/施工机制.md` `:256`（「088 实测定名十件共 190 KB」） | **只补限定、不改数**（`190 KB` 的件集与量纲本批**不实核**），**逐字**改为：「（088 实测定名十件共 190 KB，**件集与量纲口径待复算**，**实际只用到少数节**）」——「实际只用到少数节」为**现文原句、原样保留**（不得删）。 | [A] | 锚点⑱；`#28`（同一张表的数须同源同口径）；`102` §八 4 与该条同源。**注**：不得写「件集见 088 批规格 §二」——`088` §二 是**文件级改动清单**，**不是装载件集**；件集须自行逐件 `os.path.getsize` 复算 |
| F15 | `docs/standards/测试与验收标准.md` `G1` 行（`:69` 第 2 格句末） | 句末追加半句，**逐字**：「——**并须同批给出『超阈存量件清单』**：凡 `**` 数超阈的**现存件**逐件列出（含治理文档，如 `施工机制`／`测试与验收标准`），使阈值调整时可一次性复测；清单缺件即「靠每批手记」，属本行未闭面。」 | [A] | 锚点⑳；`107` §八 开放项 1 之 ⑳；`G1` 同族自陈「阈值未调整」 |
| F16 | `scripts/check_spec.py` **四处**（**行号＝改动前现状，删除后其后行号 −1**）：`:1266`（常量定义行，**删除后该行消失**）、`:1265`（**同处的内联注释**，即同一表述的第二副本，**删除后仍在 `:1265`**）、`:52`（维护入口枚举）、`:1373`（docstring 历史陈述，**删除后在 `:1372`**） | **删死代码**：删去 `:1266` 的 `COST_WORDS = ('成稿审查', '产物审查', '单维核对')` 整行；`:1265` 的内联注释（现文「…审查位记录行判据＝**整行**含该三词之一；两字段名与占位值。」）**改述为按节口径**（不得仍述旧判据）；`:52` 的维护入口枚举删去 `COST_WORDS／`；`:1373` 的历史陈述把「整行含 `COST_WORDS` 之一」改述为**散文**（「整行含三词之一」），不保留已删常量的名字。**四处须口径一致**（`施工机制` §四 回写清单 ④：docstring／内联注释／实现三者一致）。**不改变任何判据行为**（该常量改按节判后已无使用面，见锚点㉑）。 | [B] | 锚点㉑；`AGENTS.md` §四 不做清单（死代码清理）；`107` 产物审查观察项 |
| F17 | `CHANGELOG.md`（`## [Unreleased]` 的 `### Added` 小节） | **新增过程产物一行**（列 0、`-` 加空格起首；**条首串写死为「审计余项落台账（108）」**；**末尾附成本註记 `（往返 R／T）`**，`R`／`T` 须落实际数值；**连同註记 ≤400 字符**） | [B] | `施工机制` §三 过程产物两项第 2 项；`版本与分发标准` §3 |
| F18 | `docs/standards/测试与验收标准.md` §4 表末（`G41` 行之后） | 新增一行 `G42`（4 格，同上列位；第 3 格留空）：第 2 格**逐字**「**「要不要派产物审查」的派发无机械闸**（⑬ 的**余项**；其「分流定级」一支已由 `106` F1 的「路径定级核对」覆盖）：现全凭规划方读 `施工机制` §五 自判，`scripts/check_spec.py` 内 `产物审查` **8 处**（`108` F16 删 `COST_WORDS` 后由 9 降 8，实测 `grep -c '产物审查' scripts/check_spec.py` ＝ 8）**全为注释／docstring 沿革引用，无一是判据** ⇒ 该步无候选可报。**判据候选**：头注无「路径判定」行、或路径判定行不可解析时报「定级未写」候选（亦即把 `106` F1 的静默面补严）」；第 4 格＝「触发＝下次触碰检查 G 的路径定级判据面」 | [A] | 锚点⑬；`107` §八 开放项 1 的余项处置；`106` F1（路径定级）；`G19`（过报面先例） |
| F19 | `docs/specs/105-2026-09-16-三处缺口补齐批.md` §七 核销表对应两行（`:109` 与 `:112`） | **改标两处**（照 `107` F15 先例）：凡该表载 `103` §八 5 与 `102` §八 4 的两行，状态列改为「**本批处置**」、理由列点名 `108` F2／F14（`施工机制` §三「本批内被处置的开放项须同批改标」）。**实测定位命令**：`grep -n '103.*§八 5\|102.*§八 4' docs/specs/105-2026-09-16-三处缺口补齐批.md`；**若该两行不在 `105` §七 而在他处，按实测落点改，并在本行注明**。**另同步其汇总行**（F19 的连带面，施工期实测到位）：`105:115` 原为 `本批处置 2｜仍待 5` ⇒ 改标后须为 `本批处置 5｜仍待 2`（终态实测；复审期订正——原写 `4｜3`）；**不同步则 `检查 H` 置红**（按 `施工机制` §八 的核销表口径，汇总行须恒列五档且逐格可复核、不得与表列不符）。该连带属**预期**、非偏差（§九 已记）。**另**：本条同批还覆盖 `105:110`（`103` §八 4 行）与 `:115`（汇总行）——故本行**实为三行改标 ＋ 汇总同步**（复审期订正：原只写两行） | [A] | 本批 P1-7 裁定；`施工机制` §三；反模式 `#23`；先例＝`107` F15 |
| F20 | `docs/specs/103-2026-09-16-快路径与成本可见面批.md` §七 `:215` | **改标一行 ＋ 连带同步该表汇总行**（P0-2 的**第二处载体**，由裁定新增）。改后**整行逐字**（行内表体竖线在规格内写作转义形式）：「\| `102` §八 6 \| `git commit -- <pathspec>` 对 untracked 无效 \| **本批处置** \| `108` F1 已触碰 §七（新增「未跟踪件不能被 pathspec 命中」一行） \|」——**现状侧**该行状态列为「仍待」、理由列以触发条件作结（**散文转述，不引旧串**）。**连带面（实测：不同步则 `检查 H` 报「不符 2 项」）**：同表汇总行须同步（该行现文为改标前的旧值，**其最终值以本批 F21 为准**）——「已履行 1｜作废 0｜本批处置 4｜仍待 2｜待作者 0 ＝ 7」（7 条不变）。依据＝本批 F1 已满足该条触发（`施工机制` §七 已落该新增行，见本行目标文本），按 §三 的同批改标要求；该件在制、非归档件 | [A] | P0-2 裁定；`施工机制` §三；反模式 `#23` |
| F21 | `docs/specs/103-2026-09-16-快路径与成本可见面批.md` §七 两行（`:213`／`:216`）、§八 两条（`:225`／`:226`）与 §七 汇总行 | **改标四处 ＋ 同步汇总**（依 `施工机制` §三 的同批改标要求；作者裁定见 §八 8）：① `:213`（`102` §八 4「190 KB」口径）状态列改「**本批处置**」、理由列点名 `108` F14；② `:216`（`102` §八 7「`patch_file.py` 三条使用面观察」）同改、理由列点名 `108` F2；③ **§八 4 与 §八 5 两条同批改标**（其 §七 对应行 `:215`／`:216` 的状态已是「本批处置」而 §八 本体未改，属同型残留一并补齐——**此项系规划方裁定外的实读补足，如实申报**）；④ §七 汇总行由改标前的旧值同步为「**本批处置 4｜仍待 2**」（7 条不变；不同步则 `检查 H` 报告表体与汇总不符）。**四处改后整行逐字**见 §三 断言 9 的五条命中串 | [A] | 作者裁定（§八 8 的处置）；`施工机制` §三；反模式 `#23`；先例＝`108` F19／F20、`107` F15 |

**本批判定「不改」的 5 条**（逐条点名，见 §八 后续处置）：**①已沉淀**（`规格写作标准` §5 第 10 条 `:69`）｜**⑯已沉淀**（`施工机制` §八 `:263`「跨批检索以各批规格的核销表为唯一索引落点」）｜**⑬部分已沉淀**（分流定级＝`106` F1 的「路径定级核对」；**产物审查派发那一支仍无闸**，落 §八 开放项 1）｜**⑭待作者**（`AGENTS.md` 属作者确认面）｜**⑰待作者**（审查成本策略属策略裁定）。

## 三、验收断言（基线 2026-09-16 实跑）

> 计数口径：以下 `grep -c` 一律**计命中行数**；扫两件以上者已在期望栏注明。

### 断言 1｜F1／F2／F3 条文落盘

```bash
grep -c '对未跟踪文件无效' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '锚片段未含到行尾' scripts/patch_file.py
```

期望 **1**。基线 **0**。

```bash
grep -c '拿模板做一次机械对规' docs/standards/产物契约标准.md
```

期望 **1**。基线 **0**。

### 断言 2｜F4–F7 缺口行落盘

```bash
grep -c '^| G38 |' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '^| G4[0-1] |' docs/standards/测试与验收标准.md
```

期望 **2**（`G40` 与 `G41`）。基线 **0**。

```bash
grep -c '^| G39 |' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '^| G42 |' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。**（本批新增缺口行共五行 `G38`–`G42`：`G38`／`G39`／`G42` 各 1，`G40`–`G41` 合计 2——上面三组断言＋本组合计覆盖五行。）**

### 断言 3｜F8 术语正例配对

```bash
grep -c '治理文档与 skill 资产分属两轨' docs/standards/术语标准.md
```

期望 **1**。基线 **0**。

### 断言 4｜F9／F10 写作律落盘

```bash
grep -c '^10\. \*\*底座不写产品层的计数\*\*' docs/standards/文档写作标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '^11\. \*\*引用须写到能唯一落点的那一级\*\*' docs/standards/文档写作标准.md
```

期望 **1**。基线 **0**。

### 断言 5｜F11–F15 五处落盘

```bash
grep -c '取值口径' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '归档件内的实测值' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '归档后补记' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '件集与量纲口径待复算' docs/specs/施工机制.md
```

期望 **1**。基线 **0**。

```bash
grep -c '超阈存量件清单' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -c '| 1 | {对方怎么做的} | 采纳／已覆盖／拒绝 | {落位或理由} |' docs/specs/施工机制.md
```

期望 **1**（**表体**第 4 格＝占位 `{落位或理由}`；旧括注式说明已移出该格）。基线 **0**（改动前该格为括注式说明、无占位）。

```bash
grep -c '| # | 参考条目的主张 | 结论 | 落位或理由 |' docs/specs/施工机制.md
```

期望 **1**（第 4 格已换为占位）。基线 **0**。

```bash
python -c "import pathlib;L=pathlib.Path('docs/specs/施工机制.md').read_text(encoding='utf-8').splitlines();t=[i for i,x in enumerate(L) if x.startswith('| # | 参考条目的主张')][0];n=[i for i,x in enumerate(L) if x.startswith('表注（**取值口径**）')][0];print('below' if n>t else 'above')"
```

期望 **输出 below**（**表注在表下**——F11 的落点要求）。基线 **above**（改动前表注在表头**之上**，见 §九 产物审查 P2-2；本断言为**本轮整改新增**）。

### 断言 6｜F16 死代码清净（行为不变）

```bash
grep -c 'COST_WORDS' scripts/check_spec.py
```

期望 **0**。基线 **3**。

```bash
python -c "import ast,sys; ast.parse(open('scripts/check_spec.py',encoding='utf-8').read()); print('ok')"
```

期望 **输出 ok**（语法不破）。基线 **ok**。

### 断言 7｜门禁与过程产物

```bash
mkdir -p .tmp/108 && bash scripts/check.sh > .tmp/108/chk.out 2>&1; echo $?; tail -3 .tmp/108/chk.out
```

期望 **退出 0**，末三行含「检查全绿」。基线 **退出 0**（改动前）。

```bash
grep -c '审计余项落台账（108）' CHANGELOG.md
```

期望 **1**。基线 **0**。

### 断言 8｜F17 条目限长与成本註记

```bash
python -c "import pathlib;print(len(pathlib.Path('CHANGELOG.md').read_text(encoding='utf-8').splitlines()[8]))"
```

期望 **≤400**（**口径＝字符**，真源＝`版本与分发标准` §3「条目一行结论、≤400 字符」；连同成本註记一并计入）。基线 **实测 292 字符**（本条落实测）。**注意口径**：`awk length` 是**字节**口径（同条目 567 字节）⇒ 不可用；须取字符数（`python` `len()`）。

```bash
grep -c '（往返 [0-9]' CHANGELOG.md
```

期望 **≥1**（该条目末尾带成本註记 （往返 R／T））。基线 **不适用**。

### 断言 9｜F21 改标落盘（五串，逐处覆盖）

```bash
grep -c 'F14 已触碰该行' docs/specs/103-2026-09-16-快路径与成本可见面批.md
```

期望 **1**（§七 `:213` 理由列点名 `108` F14）。基线 **0**。

```bash
grep -c 'F2 已触碰该工具头注' docs/specs/103-2026-09-16-快路径与成本可见面批.md
```

期望 **1**（§七 `:216` 理由列点名 `108` F2）。基线 **0**。

```bash
grep -c '本批处置 4｜仍待 2' docs/specs/103-2026-09-16-快路径与成本可见面批.md
```

期望 **1**（§七 汇总行同步）。基线 **0**。

```bash
grep -c 'F1 已在 §七 补' docs/specs/103-2026-09-16-快路径与成本可见面批.md
```

期望 **1**（§八 4 改标）。基线 **0**。

```bash
grep -c 'F2 已把三坑写入' docs/specs/103-2026-09-16-快路径与成本可见面批.md
```

期望 **1**（§八 5 改标）。基线 **0**。

## 四、规格自审（五条）

1. **锚点现场核过**：§一 二十一条逐条实跑——①–⑳ ＋ ㉑ 的实测列均给命令与结果（`grep -c`／`grep -n`／`sed -n`），**逐条对照 `107` §八 开放项 1 的标题摘要**。**与摘要不符者两条**（如实记）：①标题称「`术语标准` §一 有反例没正例」——实读该节**已重组为 §二「正例与反例」**（`:39`），**反例仍在、仍无配对正例**，故实质成立、节号已变；②标题称「`施工机制:240`『190 KB』」——实读该串**已漂至 `:256`**（本批 F1 与 F14 各自在其上方增行／改述），改按 `:256` 处置。
2. **断言实跑过**：§三 **九组**断言的**改前基线**已逐条实跑（F1／F2／F3 串＝0；`^| G38 |`＝0；`^| G39 |`＝0；`^| G4[0-1] |`＝0；术语正例串＝0；两条写作律串＝0；F11–F15 五串＝0；`COST_WORDS`＝3；CHANGELOG 串＝0；`check.sh` 退出 0）。
3. **禁止事项已写明**：见 §五（含对 `施工机制` §四 回写清单命中面的**显式豁免**，遵反模式 `#32`）。**射程面已核**：F9／F10 给 `文档写作标准` §3 新增两条属「规则新增」，按 `施工机制` §四 规则新增回写清单已核**既有编号 7./8./9. 不动、故 `§一.3.8` 两处活跃引用（`测试与验收标准:69`／`文档写作标准:118`）不牵动**，实测 `grep -c '§一.3.8' docs/standards/*.md` ＝ 2 处（见 §二 F9 依据列）。
4. **范围与期望同源**：§三 各断言的期望值均取自 §二 点名文件与节的范围；`G38`–`G41` 四行的期望取自 `§4 表末` 这一**范围**（故用行首锚 `^| Gxx |`，不按行号）。
5. **自述有守卫**（`106` 立的第 5 条，本件头注逐句指认）：①「标准路径」⇒ 由 `规格写作标准` §1 判定表覆盖（跨 9 件 ＋ 新增与修改规范条目），**无机械检查** ⇒ 按第 5 条**不生效**，本批据此**照最重路径执行**（真做双维产物审查）；②「双维产物审查」⇒ 由 `施工机制` §五 条件式覆盖，**无机械检查** ⇒ 同上照最重路径执行；③「真机走查不适用」⇒ **不是自述**：它是 §二 逐行清单的直接推论（清单内无 `skills/` 资产正文、无产物契约字段与状态值枚举），判据＝清单本身；④「自由度分布 21 行」⇒ 由检查 G 核（本件为在制规格，`bash scripts/check.sh` 的 `[4]` 段逐批核）。

## 五、禁止事项

- 禁止触碰：`docs/specs/archive/` **全部**（含本批落台账所依据的 43 件审计对象——**归档件只读**）；`docs/product/` 全部；`skills/` **全部**；`AGENTS.md`（改它须作者确认，宪法 §六.1——本批对 ⑭ 的处置＝**只登记不擅改**，见 §八 开放项 1）；`scripts/check_content.py`；`docs/standards/` 中**未在 §二 点名者**（点名四件见下「显式豁免」行）。
- **显式豁免（遵反模式 `#32`）**：**六件属本批改动面**——`scripts/patch_file.py`（仅头注「用法与参数」段的**新增三行**）、`scripts/check_spec.py`（仅 **`COST_WORDS` 死代码面四处**：常量定义行、其紧邻内联注释、维护入口枚举、函数 docstring 的历史陈述句——**只删死代码与改述该四处、不改任何判据行为**；该四处**行号随删除位移**（`:1266` 消失、`:1373`→`:1372`），故按「面」表述而不写行号）、`docs/standards/测试与验收标准.md`（仅 §4 表末新增 `G38`–`G42` 五行，与 `G27`／`G31`／`G1` 三处行内补句）、`docs/standards/文档写作标准.md`（仅 §3 末新增 `10.`／`11.` 两条）、`docs/standards/术语标准.md`（仅 §二 新增一行 ✅ 正例）、`docs/standards/产物契约标准.md`（仅 §2 新增第 6 条）；豁免**只放行该面、不放行整件**，各件其余内容一律不碰。
- 禁止动作：联网采集；改 `AGENTS.md`；改 `skills/` 或 `docs/product/` 任何文件；**改任何既有判据的行为或断言**（F16 仅删死代码，须以「行为不变」自证）；把 §二 判「不改」的 5 条**硬凑成改动**；新增检查号（不新增 K，不改 `[4]` 段段序与段数）。

## 六、被否决的替代方案

- **把余下 20 条并入 `107` 一次吃完** → 否决：`107` 已吃 1 轮全文审（18 条）＋1 轮窄域审（6 条）＋1 轮产物审查（11 条）；再并入 17 项会使该批改动面越过 15 件、且**新动机与已审面混装**，违反 `107` §六 已立的「可审规模」判据（`104` 批即因改动面过大被一次报出 5 条 P0）。
- **只登记不落账（把 20 条写进 `108` §八 就完事）** → 否决：这正是 `107` 立闸所要堵的失败模式——审计实测 **341 条埋没**的成因就是「记了、没人判」；且 `107` F1 新立的条文要求「判为跨批可复用的教训**须先落入五处台账之一**」。
- **把 20 条硬凑成 20 项改动** → 否决：其中 ①／⑯ **已沉淀**（点名到条）、⑬ **部分已沉淀**、⑭／⑰ **属作者裁定面**；硬凑会造出「为对齐条数而改」的空改动（同反模式 `#1` 族），并让 `§二` 的「现状 → 目标」失去实义。
- **删除 `COST_WORDS` 同时顺手改 `_cost_field_cands()` 的判据结构** → 否决：`107` 的 `F14` 判据刚经一轮审查定稿（改按「节」判）、现行件候选已归 0；本批**只删死代码、不触判据**，避免在无新证据下重开已定稿的判据面。

## 七、本批核销（相关归档规格的开放项）

> **必列面**（`施工机制` §八 `:259`–`:260`）：① 前一批（`107`）§八 **全 9 条**（下表逐条）。**不必重列、只给索引指针**：`106` §八 全 7 条与 `105` §八 1／2／3／6／7 见 **`107` §七**；`105` §八 4／5 见 **`106` §七**。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `107` §八 1 | 审计余下 20 条埋没项交 `108` | **本批处置** | 逐条实读后落 **15 条**（F1–F15）／判 **5 条不改**（①／⑯ 已沉淀、⑬ 部分已沉淀、⑭／⑰ 待作者），逐条点名见 §二 末与 §八 1 |
| `107` §八 2 | 「随各自阶段批」异形实例待收口（`skills/` 2 处 ＋ `docs/product/` 1 处） | 仍待 | 按 `施工机制` §二，触碰 `skills/` 资产的批次＝**重路径**（须产物审查＋真机走查判据），不可夹带进标准路径批；`docs/product/` 亦属本批禁改面 |
| `107` §八 3 | 审计明细（341 条）的长期落点 | **本批处置** | 20 条去重项已逐条归主题（F1–F15 落 G 表／条文／表注）；余下明细**不新建台账件**（`AGENTS.md` §四 禁平行台账），按主题随批入既有载体——触发＝后续批次遇到同主题时 |
| `107` §八 4 | 归档动作的机械闸只立了一半（`--verify` 未接门禁） | 仍待 | 触发＝评估 `--verify` 接门禁的时机；本批不触该面（其 `:33` 自陈「门禁运行点在制规格尚未归档」这一前提仍成立） |
| `107` §八 5 | F3／F4 上线后的假阳观察（首轮候选数记入下一批） | **本批处置** | 首轮实测（`107` 提交后）：**无台账归宿 0｜无锚点 3｜仍待项断链 16｜§九 成本字段 0**（后者为改按节判后）；已记 §八 3 |
| `107` §八 6 | F14 改按「节」判后现行件候选＝0（留痕） | 作废 | 触发已消（改按节判后现行四件 0 条）；过报实例已留痕于 `测试与验收标准` §4 `G19` |
| `107` §八 7 | `shell=True` 的宿主声明整改 | 仍待 | 触发＝下次触碰 `check_spec.py` 的子进程调用面；本批 F16 只删死代码、**未触该面** |
| `107` §八 8 | `检查 A` 对断言 7 三处报「零命中核对未判（期望子句多值）」 | 仍待 | 触发＝下次触碰该断言面；本批**不为消它删行号**（行号是判据的可核面） |
| `107` §八 9 | `检查 C` 对加粗的归一化不对称 | 仍待 | **登记面已由 `测试与验收标准` §4 `G37`（`:101`）承载**；**修法二择一未做**，触发＝下次触碰 `check_pairs()` 取词面 |
| `103` §八 5 | `patch_file.py` 三条使用面观察（触发＝下次触碰该工具） | **本批处置** | **F2** 已触碰该工具文档（补三坑）；余项「回读明细截断放宽」属代码改动，见 §八 6；`103:216` 与 `103` §八 5 同批改标＝**F21** |
| `102` §八 4 | `施工机制`「190 KB」口径未核（触发＝下次触碰该行） | **本批处置** | **F14** 已触碰该行（补件集与量纲限定；实核留 §八 4）；`103:213` 同批改标＝**F21** |
| `103` §八 4 | `git commit -- <pathspec>` 对 untracked 无效（触发＝下次触碰 §七 时补一行） | **本批处置** | **F1** 已触碰 §七（新增「未跟踪件不能被 pathspec 命中」一行）；源件 `105:110` 同批改标＝**F19**、`103:215` 同批改标＝**F20**、`103` §八 4（`:225`）同批改标＝**F21** |

- 汇总：`按条计 12 条：已履行 0｜作废 1｜本批处置 6｜仍待 5｜待作者 0 ＝ 12`

## 八、开放项

1. **20 条里判「不改」的 5 条的后续去向**——①／⑯ **已沉淀**，无需动作（归宿条号见 §二 末）；⑬ 的**余项**＝「要不要派产物审查」仍无机械闸（分「定级」那一支已由 `106` F1 覆盖）⇒ **已落 `测试与验收标准` §4 新行 `G42`**（见 §二 **F18**；判据候选＝头注无「路径判定」行、或路径判定行不可解析时报「定级未写」候选）；⑭／⑰ **待作者**（`AGENTS.md` 与审查成本策略均属作者裁定面）。
2. **本批可及面已尽，闸门账留各件归档批**——`107` 闸门首轮照出的 **16 条「仍待项已断链」**（分布：`103` 3／`105` 3／`106` 4／`107` 6）与 **3 条「开放项无锚点」**（`103` 2／`105` 1）：按 `107` F1 的条文，闸门要的是**归档那一刻**逐条裁定，而这四件**都还在制**（`docs/specs/` 未归档）⇒ 本批**只处置本批可及者**（即在 20 条范围内已落账的部分），其余**留待各件自身的归档批**逐条裁定。**不得**为把它们清零而提前改标在制件。
3. **`G38`–`G42` 五行为登记项、未实现**——本批只入册（同 `G32`–`G37` 先例），**均未落判据实现**；触发条件已逐行写明，实现归后续批次。
4. **量级声称的件集口径未全线复核**——F14 只校准了 `施工机制:256` 一处「190 KB」；仓内其他「量级／件数」声称（如各标准件的件数、`CHANGELOG` 沿革里的体量句）**未逐处复核**。触发＝下次遇到需要据某量级声称下结论时。
5. **超阈加粗存量件清单本身未产**——F15 只立「须同批给出清单」的要求；**清单尚未逐件测**（需先定阈值口径，见 `G1`）。触发＝阈值立法批。
6. **F2 的出处与余项**——三条坑源自 `103` §八 5 的「使用面观察」；其修法（把三坑写进工具文档）本批已做（F2）；但该条同时提到的**回读明细截断放宽**（现只打印前 120 字符）属**代码改动**、本批未做。触发＝下次触碰 `_readback()`。
7. **`107` §八 3 的档位存疑**——该条标「本批处置」，但审计的 **341 条明细**并未逐条归主题（本批只处置去重后的 20 条）。**建议改「仍待」**，触发＝作者裁定或下批复核；**本批不改 `107`**（非本批改动面），只登记。
8. **（已履行）`103` 同型状态句改标**——作者裁定＝**同批改标**（不是登记不改），依据 `施工机制` §三「本批内被处置的开放项须同批改标」。落点＝**F21**：`103:213`（`102` §八 4「190 KB」口径 ⇒ `108` F14 触碰）、`103:216`（`102` §八 7「`patch_file.py` 三条使用面观察」⇒ `108` F2 触碰），并**一并纳入 `103` §八 4／5**（其 §七 对应行 `:215`／`:216` 上一轮已改标、§八 本体未改，属同型残留）、同步 §七 汇总行为「本批处置 4｜仍待 2」。触发＝无（已履行）；判据＝§三 断言 9 的五条命中串。
9. **F13 落盘与目标文本曾相抵（复审期发现的**唯一**真偏差，已处置）**——`[A]` 目标原要求例外半句在**首**，而 P2-1 的整改把例外半句移到该 bullet **句末**并以 `；` 接 ⇒ 去加粗后仍不命中，属**真偏差**（该 6 条 `未落实` 中唯一非 `G37` 型假阳者）。**处置＝按终态改述 §二 F13 的目标文本**（`施工机制:107` 的终态结构更正确，故**不**把落盘挪回句首），并附可跑对照命令（§九 该条）。触发＝无（已履行）。

## 九、审查记录

- 2026-09-16｜**成稿审查（独立只读 subagent）**｜findings 共 15（P0:5 P1:7 P2:3）｜结论**修订后通过**｜工具往返数 30｜周期时长 <1h。**逐条处置**：**P0-1**（三处行号失实：`文档写作标准` 引用带节锚点 `:39`→**`:41`**、禁时间敏感 `:42`→**`:43`**、`patch_file.py` 的 `--no-lint` `:18`→**`:25`**）→ 已按实测改 F2／F9／F10／§一⑪；**P0-2**（F9／F10 新条与既有 `7./8./9.` 撞号，重编号会牵动 `§一.3.8` 两处活跃引用 ⇒ 施工后 `MD029` 置红）→ **不重编号、接末为 `10.`／`11.`**，既有三条不动，并在 F9／F10 依据列写明「两处引用不牵动」＋实测 2 处；**P0-3**（断言 1 的 token 与 F1 目标文本互斥——目标写作「对**未跟踪**文件**不生效**」而 token 是「对未跟踪文件无效」）→ F1 目标去掉加粗改「**对未跟踪文件无效**」，断言 token 不变；**P0-4**（F11 现状格号错：实测 `:269` 四格中括注在**第 4 格**）→ F11 与 §一⑫ 均改按第 4 格，并补双条件断言（表体第 4 格不含「防未来重复纠结」＝0／已换占位＝1）；**P0-5**（F14 把「未核」写成事实）→ 降为「件集与量纲口径**待复算**」，**保留原句「实际只用到少数节」**，并删去错误指向「件集见 088 批规格 §二」（`088` §二 是文件级改动清单、非装载件集）＋ §八 登记待复算；**P1-1**（F16 回写面被 §五 豁免范围切掉：`:1265` 内联注释仍述旧判据）→ F16 扩为**四处**（`:52`／`:1265`／`:1266`／`:1373`）、§五 豁免扩为**六件**；**P1-2**（F3 实例出处不实）→ 改引 `archive/099` §八 4 **逐字**＋`archive/100` §七；**P1-3**（F5 机制描述失实）→ 改写为「`check.sh` 的**全绿收尾行**打写死段名清单」`_seg_summary()` 已正确计跳过；**P1-4**（`G31` 的「在制 4」因本批失准）→ 随批重测为**在制 5／共 48 件**并写入 §二 F4；**P1-5**（依据列 8 处引「审计埋没清单 #N」不可核）→ 全部改用 `107` §八 开放项 1 的 ①–⑳ 编号；**P1-6**（⑬ 余项该落而漏落）→ 新开 **F18** 落 `G42`，§八 1 改指该行；**P1-7**（§七 漏列两族应销未销：`103` §八 5 与 `102` §八 4 触发已满）→ §七 补两行并重算汇总（9→**11**）＋新开 **F19** 同批改标 `105` §七 对应两行；**P2-1**（§七 索引指针多一跳）→ 改为「`106` §八 全 7 条与 `105` §八 1／2／3／6／7 见 `107` §七；`105` §八 4／5 见 `106` §七」；**P2-2**（`G38` 一行并入两个触发面）→ **拆**：转义反引号那一支**并入 `G27` 行**补一句，`G38` 只留「位置格只写件数」，行数 `G38`–`G42` 五行且断言同步；**P2-3**（§五 用「除外式」写禁改面，触发 4 条「禁改面未豁免」候选）→ 四件标准件自禁改面**移出**、另并在「显式豁免」行（六件，写明只放行 §二 点名面）。**观察项落地**：⑤ F17 断言补长度（`awk` ≤400）与末带成本註记两条；⑥ §四.3 补「F9／F10 射程面已按 §四 规则新增回写清单核过」。**止损**：本批 P0 ≥ 2，按 `施工机制` §五 止损第 2 条，后续只做**窄域审**（只核本轮修复项与其直接影响面，不重开全文审）。
- 2026-09-16｜**施工（后台 subagent；19 项／9 件）**｜门禁 `bash scripts/check.sh` 退出 **0**（末行「检查全绿」）｜工具往返数 **68**。**断言实测 20／22 绿、2 项 FAIL**，两项均属**断言侧失实**（非施工错）：**①** 断言 1 的 token 写作「拿模板做机械对规」，而 F3 落盘文本为「每个产物产出后须拿模板做**一次**机械对规」⇒ **token 是落盘文本的缺字子串、永不可能命中**（`#30` 族、与 P0-3 同型）；**订正 token 为含「一次」的逐字串**（实测新 token ＝ **1**、旧 token ＝ **0**）——**性质＝订正失实断言**（走「呈报→裁定→修订规格」），**不是改断言变绿**；**②** 断言 8 原用 `awk length` ＝ **字节**口径（实测 **567**），而真源 `版本与分发标准` §3 是「≤400 **字符**」⇒ 按字节判**对任何合规条目恒红**；**订正为字符口径**（`python` 取 `len()`，实测 **292** 字符 ≤400）。**另两条连带（均为预期、非偏差）**：**③** F19 的连带面＝`105:115` 汇总行须随改标由 `本批处置 2｜仍待 5` 同步为 `4｜3`（不同步则 `检查 H` 置红），已在 §二 F19 行内显式列出；**④** F11 的落点实为**两处**（表头格 ＋ 表体第 4 格，现均已成 `{落位或理由}`），已在 §二 F11 行内写明。**F16 落地后随批重测（`#21`）**：`grep -c '产物审查' scripts/check_spec.py` 由 **9 → 8** ⇒ §一 ⑬、§二 F18 与 `测试与验收标准` 的 `G42` **三处**「9 处」**同批订正为 8 处**（否则自落地起即过期；§二 F18 那一处系**本轮产物审查 P1-1** 抓出的漏改）。**首轮 `[4]` 段新候选（如实记，附实测命令）**：`python scripts/check_spec.py --static docs/specs/10[3-8]*.md` 报 **6 处** `未落实或已漂移`——**归一化对照（两侧同等去 `**` 与反引号后判子串）后：5 处确为 `G37` 型加粗归一化假阳**（`patch_file.py` 坑一／坑二／坑三、`测试与验收标准` 的「现存 48 件…」、`施工机制` 的「例外：归档后补记」——去加粗后逐字命中目标件），**1 处（F14）为真偏差**（限定语被移出括号、次序颠倒）。**施工段原记「共 5 处／全部为 `G37` 型假阳」失实**——经 `108` 产物审查 **P0-1** 独立复核抓出，本轮已按规格逐字改正（见 §九 末条）。另 `施工机制:256`（F14 那行）出 1 条「开放项无台账归宿」候选，因该行未给台账锚点所致，留 §八。**施工方另申报**：F2 首批漏落、由断言 1 抓回后补落；F11 表头格同改。**本轮为该规格第 1 轮施工。**
- 2026-09-16｜**整改期规划方自查（同型第 6 次）**｜本轮整改首次落笔时在 §二 F19 行的括注里写了**现状侧引号块**（引「汇总行不得与表列不符」并误挂在 `105` §八 名下）——实读证实**该串在 `105` 内根本不存在**（真源是 `施工机制:263` 的核销表口径），`检查 C` 当即按 `[A]` 逐字核对报「未落实」⇒ 若不改即留**永久假阳**。已改为纯散文、**不引任何引号块**（`检查 C` 只把引号块当目标侧核）。**本会话该形态已第 6 次出现**（规划方 2／起草 fork 1／施工方 1／整改方 2）。**律的载体 `规格写作标准` §5 尚未立**（`106` §八 7 是**开放项**，非已立之律——原记「已立律」**失实**）；`G37` 记的是**加粗归一化不对称**、**与本形态非同族**（原记「已记工具面」易误读为同族）。本形态已由本轮**新开反模式 `#33`** 承载（`施工机制` §六 表末），本条如实记频次。
- 2026-09-16｜**产物审查（独立只读 subagent）**｜findings 共 10（P0:2 P1:5 P2:3）｜结论**修订后通过**｜工具往返数 48｜周期时长 <1h。**逐条处置**：**P0-1**（F14 未按 `[A]` 逐字落地——限定语被移出括号、次序颠倒；且施工段「共 5 处／**全部**为 `G37` 型假阳」判属失实）→ `docs/specs/施工机制.md:256` 已按规格**逐字重落**（`…190 KB，**件集与量纲口径待复算**，**实际只用到少数节**）`）。**复审期订正两处**：**①** **真偏差不是 F14 而是 F13**——复审实测 F14 落盘与目标串**原文逐字相等**（属 `G37` 型假阳）；该 6 条 `未落实` ＝ **5 条 `G37` 型假阳 ＋ 1 条真偏差（F13）**；**②** F13 的处置＝**按终态改述 §二 F13 的目标文本**（该行已改）＋补登记（§八 9）。**归一化对照命令（可跑）**：`python -c "import io;n=lambda s:s.replace('*','').replace(chr(96),'');p=n(io.open('docs/specs/施工机制.md',encoding='utf-8').read());print(n('（088 实测定名十件共 190 KB，**件集与量纲口径待复算**，**实际只用到少数节**）') in p)"` → 实测输出 **True**；**P0-2**（应销未销＋核销漏列：F1 已满足 `102` §八 6／`103` §八 4 的触发，而 `105:110`／`103:215` 仍写「仍待」、§七 亦漏列该条）→ `105:110` 改「本批处置」＋`105:115` 汇总改「本批处置 5｜仍待 2」（＝**F19 连带**）；`103:215` 同批改标＝**新 F20**（§二 已补行；头注自由度分布同步为 `[A]×19 [B]×2 共 21 行`（终态实测；复审期订正——原写 `20 行`）；**其汇总行 `:218` 亦连带同步**为「已履行 1｜作废 0｜本批处置 4｜仍待 2｜待作者 0 ＝ 7」（终态实测；复审期订正——原写「已履行 1｜本批处置 2｜仍待 4」）——不同步则 `检查 H` 报「不符 2 项」，实测已消）；§七 补该行、汇总重算为 **12 条：作废 1｜本批处置 6｜仍待 5**；**P1-1**（F18 的 `[A]` 目标仍写「9 处」与落盘 `G42` 的「8 处」相抵）→ 改 8 处；§九 该句「两处」订正为「**三处**」（§一 ⑬／§二 F18／`G42`）；**P1-2**（断言 5 两条命令都只核表头 `:269`、未核表体）→ 两条断言改为**内容锚定**（表体整行＋表头整行各一，不再依赖行号），并**新增**「表注在表下」的 `python` 判据；**P1-3**（「现状侧引号块」形态未回填族，且 §九 依据失实）→ **新开反模式 `#33`**（`施工机制` §六 表末），并订正两处失实表述（`106` §八 7 是**开放项、律未立**；`G37` 是**加粗归一化、与本形态非同族**）；**P1-4**（规格内行号因本批自身改动失准）→ 逐条按**终态**重算（必读输入 5 处、§一 ⑯／⑱、F12／F14／F16、§七 必列面、§八 4）；**P1-5**（`CHANGELOG:9` 的 R 记 **35**，与任何段都不符且漏段）→ 按 §八 口径重取为 **176**（终态；复审期订正——原写 168，见下条成本段）；**P2-1**（`施工机制:107` 的 F13 落点成句中插入、「不另建台账」重复 2 次、句号后悬空 `——`）→ 例外半句移到该 bullet **句末**、删重复、消悬空；**P2-2**（F11 表注置于表**上**且与导语行粘连、表头第 4 格丢列名）→ 表注移到**表下**（上下留空行）、表头恢复列名「落位或理由」（占位只留表体）；**P2-3**（`测试与验收标准:91` 的 `G27` 行第 2 格末多余全角竖线）→ 删除。**观察项落地**：① F12 并注移到该 bullet **句末**、不再打断并列枚举；⑤ §五 豁免面对 `check_spec.py` 改为按「面」表述（行数删除后行号位移，不再写死）。**另登记待裁定一条（§八 8）**：`103:213`／`:216` 与本轮已改标的 `:215` **同型**（其触发亦已由本批满足、且 `105` 对应行已改标），但**本轮裁定只点名 `:215`**，故只登记、不擅自扩大改动面。**止损**：本批 P0＝2 ⇒ 按 `施工机制` §五 止损第 2 条，修订后只核 **2 处 P0 修复项 ＋ 其直接影响面**（不重开全文审）；又按该条**对称条款**（产物审查 P0 ≥ 2 ⇒ 修订后重派产物审查 subagent 复审一次），**建议重派一次产物审查复审**。
- 2026-09-16｜**成本记账（四段 ＋ 一段未自述）**｜成稿审查段 **30**｜施工段 **68**｜产物审查段 **48**｜本轮整改段 **30**｜**全批合计 ≥176**（`CHANGELOG` 成本註记按此记）。**另注**：本批另有**两轮整改**（subagent，**均未自述往返数**）⇒ **176 为下限**（复审期订正——原写「168 为下限」自相抵：168 小于已可核的 176，不能作其下限）。口径＝`施工机制` §八「`R`＝本批规划方会话的工具调用总次数，含施工与审查 subagent 自述数之和」。**并注**：本条系 P1-5 处置——原记「35」与任何段都不符且漏全部审查段。
