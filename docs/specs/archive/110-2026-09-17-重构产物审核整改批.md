# 110-2026-09-17-重构产物审核整改批

> 定位：**T2–T5b 重构产物的首轮审核整改**——对三笔重构提交（`905c3f0`／`a298d3b`／`2ad3c77`）的逐件勘验揪出 3 处残留／漂移，逐条订正；并作为**新流程（一条主线）的首次实跑**，按段记成本。
> 路径判定：**主线**（机械实算见检查 E 判四：动 `docs/standards/` 规范面且改动面 > 3 件）
> 自由度分布：[A]×3 [B]×2 共 5 行
> 必读输入（按「文件:节」点名）：`docs/specs/施工机制.md` §二／§三／§五；`AGENTS.md` §二（含「改某区域前先读什么」表）／§七；`docs/specs/施工机制-附录.md` §一 的 `#2`／`#22`／`#23`／`#33` 四行与 §三（模板全文）；`docs/standards/测试与验收标准.md` §4 的 `G30`／`G42` 两行与表末销号注；`CHANGELOG.md` `## [Unreleased]` → `### Added` 首三行
> 施工形态：subagent（后台 spawn；无 git 写权限）

## 一、现状锚点（逐条现场核过，2026-09-17）

| # | 事实 | 实测 |
| --- | --- | --- |
| ① | `施工机制-附录.md` 的规格模板 §九 行末尾残留「快路径批次」子句（T1 只删了尾半句「＋同样的两个字段」） | `grep -c '快路径批次' docs/specs/施工机制-附录.md` → **1** |
| ② | `测试与验收标准` 的 `G30` 行第 3 格（状态/处置）仍写「本批处置（`106` F1 已加路径定级判据）」，而该判据已于 T1 下线 | `grep -c '已加路径定级判据' docs/standards/测试与验收标准.md` → **1** |
| ③ | 同件 `G42` 行**三处**引用已下线的定级判据：括注「已由 `106` F1 的「路径定级核对」覆盖」／「亦即把 `106` F1 的静默面补严」／第 4 格「触发＝…路径定级判据面」；且同格「`产物审查` **8 处**」实测为 **7 处** | 于该行以 `grep -o` 数 `106` F1 串 → **2** 处；于 `scripts/check_spec.py` 数「产物审查」 → **7** 处 |
| ④ | 同件 `施工机制-附录.md` §三 的 §九 行仍含 `109` F4 落的核验注记「**射程面已核**…产物审查 O1 订正…」（`109` §八 14 要移出者） | `grep -c '射程面已核' docs/specs/施工机制-附录.md` → **1** |
| ⑤ | 勘验其余项全绿：附录 §一 **33 行**无损（`awk` 限域实测）／预算未超（施工机制 11,997 ≤ 13,000；附录 12,636 ≤ 13,000）／`check.sh` 头注 ↔ `echo` 两处同含「档位核对／回写面对账」／`AGENTS.md` §七 为 **A–J**／台账全集含附录／区域表 3 处指针实存／`CHANGELOG` Unreleased `### Added` 前三行 **321／394／298** 字符（均 ≤400） | 见 §九 勘验记录（机械核查 35 项：32 OK／3 PROB，即本表 ①②③） |

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据 |
| --- | --- | --- | --- | --- |
| F1 | `docs/specs/施工机制-附录.md` §三 规格模板的 §九 行 | 该行末尾的「快路径批次」子句（现状侧**散文转述，不引号块化**）整句删去——删去范围为「**快路径批次**该行写「（免审查）」」这一段，**保留该行原有的外层括注收尾**，使该行**以「`>4h`。）」收束**（目标侧不引号块化——原写「…同样的两个字段）」既非现状亦非终态，且被 `检查 C` 判「未落实或已漂移」成永久假阳，产物审查 P1-2 订正） | [A] | 锚点①；附录 `#2`／`#33` |
| F2 | `docs/standards/测试与验收标准.md` §4 的 `G30` 行第 3 格（状态/处置） | 该格现文以「本批处置」记 `106` 已加判据 → 改为，**逐字**：「**已销号（2026-09-17 随「快」行退役）**」 | [A] | 锚点②；`#23` |
| F3 | 同件 `G42` 行（**三处 ＋ 一处计数**） | ① 括注改为，**逐字**：「其「分流定级」一支随「快」行退役已消失（见 `G30` 销号注）」；② 「亦即把 `106` F1 的静默面补严」改为，**逐字**：「亦即把「头注缺分布行」的静默面补严」；③ 第 4 格触发条件改为，**逐字**：「触发＝下次触碰检查 G 的自由度分布判据面」；④ 同格计数「`产物审查` **8 处**」改为，**逐字**：「`产物审查` **7 处**」 | [A] | 锚点③；`#22`／`#23` |
| F4 | 同件 `施工机制-附录.md` §三 的 §九 行内的核验注记 | 把该行内的「**射程面已核**…产物审查 O1 订正…订正如上。」整句移出模板行、落 §三 节末独立散文（同句文字不改），模板行**以原有的括注收尾**（即「…`>4h`。）」）、**不再另加指针**——该行前文已有「（见 §六 `#21` 行）」一处，重复即为噪声（施工期 P2-1 裁定） | [B] | 锚点④；`109` §八 14 |
| F5 | `CHANGELOG.md` 的 `## [Unreleased]` → `### Added` | 新增过程产物一行（列 0、`-` 加空格起首；条首串写死「机制产物审核整改（110）」；末尾成本註记须**分段＋合计**；连同註记 ≤400 字符） | [B] | `施工机制` §三 过程产物两项 |

## 三、验收断言

```bash
grep -c '快路径批次' docs/specs/施工机制-附录.md
```

期望 **0**。基线 **1**（锚点①）。

```bash
sed -n '119p' docs/specs/施工机制-附录.md | grep -c '（免审查）'
```

期望 **0**。基线 **1**（F1 的删法面）。

```bash
grep -c '已加路径定级判据' docs/standards/测试与验收标准.md
```

期望 **0**。基线 **1**（锚点②）。

```bash
grep -c '路径定级' docs/standards/测试与验收标准.md
```

期望 **0**。基线 **2 行**（锚点② 的 `G30` 状态格与锚点③ 的 `G42` 行各一）。

```bash
grep -c '已销号（2026-09-17 随「快」行退役）' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
grep -cF '`产物审查` **7 处**' docs/standards/测试与验收标准.md
```

期望 **1**。基线 **0**。

```bash
sed -n '119p' docs/specs/施工机制-附录.md | grep -c '射程面已核'
```

期望 **0**。基线 **1**（F4：注记移出模板行）。

```bash
grep -c '射程面已核' docs/specs/施工机制-附录.md
```

期望 **1**。基线 **1**（该句仍在件内，只落节末散文——移出而非删除）。

```bash
python scripts/check_spec.py --reconcile docs/specs/110-2026-09-17-重构产物审核整改批.md 2>&1 | grep -c '清单外改动件'
```

期望 **0**（实盘改动件须与 §二 清单一致）。基线 **0**。

```bash
bash scripts/check.sh
```

期望 **退出 0**、末行含「检查全绿」。基线 **退出 0**。

```bash
grep -c '机制产物审核整改（110）' CHANGELOG.md
```

期望 **1**。基线 **0**（**须以 `-F` 固定串匹配**：GNU grep 把 `**` 读作「重复算子」，按字面恒不命中——产物审查 P1-1）。

```bash
sed -n '119p' docs/specs/施工机制-附录.md | grep -o '（见 §六 `#21` 行）' | wc -l
```

期望 **1**。基线 **2**（P2-1：F4 曾另加同串指针，与行内原有者重复）。

## 四、规格自审（五条）

1. **锚点现场核过**：①–④ 为 grep／`sed` 实测（1／1／2 处与 7 处／1）；⑤ 为 35 项机械核查（32 OK）。
2. **断言实跑过**：§三 **十二条**断言的**改前基线**已逐条实跑——依次为 **1／1／1／2／0／0／1／1／0／退出 0／0／2**（末值为 P2-1 的消重面）（第 8 条为 1，系 F4「移出而非删除」面）。
3. **禁止事项已写明**：见 §五（含回写面对账六面「无需」及其理由）。
4. **范围与期望同源**：§三 各断言的期望值均取自 §二 点名件与该节范围。
5. **自述有守卫**：①「主线」⇒ 检查 E 判四机械实算（本件动 `docs/standards/` 规范面 ⇒ 必为「主线」，实测「判四不符 0」）；②「不触检查面」⇒ 判三触发条件核（§二 无 `scripts/check*`／`check.sh`）；③「施工形态 subagent」⇒ 由 §九 记录的实际 spawn 核。

## 五、禁止事项

- 禁止触碰：`docs/specs/archive/` **全部**、`docs/product/` 全部、`skills/` 全部、`AGENTS.md`、`scripts/` **全部**（本批**不改任何守卫** ⇒ 判三不触发）。
- 显式豁免：仅 `docs/specs/施工机制-附录.md`（§三 规格模板的 §九 行）、`docs/standards/测试与验收标准.md`（`G30`／`G42` 两行）、`CHANGELOG.md`（一行）——**只放行上述面，不放行整件**。
- **回写面对账**：① `测试与验收标准` §1.1 —— **无需**（未增删改检查项与判据）；② `AGENTS.md` §七 —— **无需**（检查号仍 A–J）；③ `check.sh` 头注 —— **无需**（段序段名与判据面枚举未变）；④ 工具 docstring —— **无需**（`check_*.py` 未改）；⑤ 各标准件 §三 机检锚点表 —— **无需**（守卫状态未变）；＋缺口表 —— **本批对象**（F2／F3 即改该表两行，属改动面而非回写面）。
- 禁止动作：联网；改断言求绿；把 §二 之外的事并入本批。

## 六、被否决的替代方案

- **走 `小改` 免审查** → 否决：动 `docs/standards/` 规范面正文，`小改` 条件③④不成立；判四实证「不符 0（主线）」。
- **F1 连外层括注收尾一并删** → 否决：会把模板行的括注截断（闸1 P0 指出），故 F1 明确「保留外层收尾」。
- **F4 改为直接删除该注记** → 否决：该注记含「`:75` 同批补分段示例」的有效信息，只是**位置**不当；故「移出而非删除」。
- **只修 A1 不修台账漂移（A2／A3）** → 否决：三处同源（T1 删改未同步副本），按 `#22` 同批并改。

## 七、本批核销（相关归档规格的开放项）

> 来源件：`docs/specs/archive/109-2026-09-16-闸门降噪三事批.md` §八（全 14 条）。本批与该件同主题（机制产物），故**全列**。

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |
| `109` §八 1 | `#33` 的写作律仍未立 | 仍待 | 触发＝触碰 `规格写作标准` §5 时 |
| `109` §八 2 | `G37` 的另一修法未采 | 仍待 | 触发＝该类实例出现 |
| `109` §八 3 | `G38`–`G42` 五行的实现 | 仍待 | 本批只改 `G30`／`G42` 两行文字，未实现判据 |
| `109` §八 4 | `--set ""` 删行不可用 | 仍待 | 触发＝下次触碰 `patch_file.py` |
| `109` §八 5 | F5 判据的数值面归人工 | 仍待 | 触发＝出现「分段齐但合计不符」实例 |
| `109` §八 6 | `R` 的历史值留档 | 仍待 | 无需动作 |
| `109` §八 7 | `检查 C` 只核最后一个方向标记之后的块 | 仍待 | 触发＝下次触碰 `check_pairs()` 取块面 |
| `109` §八 8 | 本件自身候选集无断言护栏 | 仍待 | 触发＝下一批施工期人工过一遍候选 |
| `109` §八 9 | F4／F5／F7 的逐字命中依赖 F1 先落地 | 已履行 | T1 已按序落地（F1 先于其余） |
| `109` §八 10 | F5 插入使行号位移 | 仍待 | 该件头注指针已随 T2 再位移，触发＝下次触碰 |
| `109` §八 11 | 两处已登记的工具面噪声 | 仍待 | 触发＝噪声造成误导时 |
| `109` §八 12 | `检查 C` 成因诊断件集口径的假诊断面 | 仍待 | 触发＝该噪声造成误导时 |
| `109` §八 13 | `103:17` 从属计数未随 F10 重测 | 仍待 | 触发＝取得 `103` 批原判据脚本时 |
| `109` §八 14 | `施工机制:253` 模板行内嵌本批核验注记 | **本批处置** | F4：该模板行已随 T2 迁入附录 §三，本批把注记移出模板行落节末散文 |

- 汇总：`按条计 14 条：已履行 1｜作废 0｜本批处置 1｜仍待 12｜待作者 0 ＝ 14`

## 八、开放项

1. **`check_content.py` 预算注释的历史值**（注释仍写「T2 冷热分离后降至 12,240」，实测已 11,997）。触发＝下次触碰该常量时校准。
2. **`施工机制` 距目标仅余 3 字符**（11,997／12,000；**冻结线 13,000，尚余 1,003**）——后续任何增补都须先减后加。触发＝下次触碰该件时。
3. **第二轮（产物审查）findings 明细留档（已全部处置）**——P1-1 断言 6 的 `**` 须 `-F`（GNU grep 读作重复算子，按字面恒不命中；施工期自跑与闸2 均用 `-F` 才绿，故按字面**不可跑**，已改命令）；P1-2 F1 目标侧串既非现状亦非终态（`git show HEAD` 与现盘均 0 命中）⇒ 已改述为终态「`>4h`。）」；P1-3 §九 超限（单行 646／389 ＞250、节比 18.6% ＞15%）⇒ 本节压缩、明细移此条；P2-1 §四.2 基数「十一条」⇒「十二条」并补基线 2；P2-2 `测试与验收标准:94` 的 `check_spec.py:250` 指针失效（现 `FREEDOM_HEAD` 在 `:256`）；P2-3 同件 `G42` 括注「由 9 降 8」缺 8→7 一环（`108` F16 降 8、T1 判据下线再降为 7）。

## 九、审查记录

- 2026-09-17｜勘验（规划方）｜共 3（P0:0 P1:2 P2:1）｜立批整改｜往返 8｜<1h
- 2026-09-17｜成稿审查（独立只读 subagent）｜共 6（P0:1 P1:5 P2:0）｜修订后通过｜往返 21｜<1h
- 2026-09-17｜施工（后台 subagent）｜共 0｜交付｜往返 51（首轮 33＋微修 18）｜<1h
- 2026-09-17｜核验·闸2（规划方）｜断言 12 逐条（6 条按字面须 `-F`）｜改动面无越界｜reconcile 0｜往返 3
- 2026-09-17｜产物审查（独立只读 subagent，两段制）｜共 6（P0:0 P1:3 P2:3）｜修订后通过｜往返 20｜<1h
- 2026-09-17｜核验·闸3 整改（规划方）｜六条全裁定成立并同批落｜P1-1 断言改 `-F`／P1-2 F1 目标侧订正／P1-3 §九 限长／P2-1 基数／P2-2／P2-3 由施工方续改｜明细见 §八 3
- 2026-09-17｜核验·闸3 后整改＋成本汇总｜六条全裁定成立、产物两处由施工方续改｜`R` 分段：规划段 47｜成稿审查段 21｜产物审查段 20｜施工段 66（33＋18＋15）｜**合计 154**｜各段之和＝合计
- 2026-09-17｜归档前沉淀核对（§三）｜跨批可复用教训＝**成稿审查须逐条按规格字面执行断言、不得改写命令**（`110` 产物审查 P1-1）⇒ 已落 `施工机制` §五 成稿审查行；其余各条判「仅本批一次性」（规格自身的引用错／限长超标属写作面，已由当批规则与断言承接）。
