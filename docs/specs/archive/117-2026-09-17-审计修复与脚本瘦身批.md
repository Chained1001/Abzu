# 117-2026-09-17-审计修复与脚本瘦身批

> 模式：全量

## 一、现状位置（现场核过）

- `AGENTS.md` §四不做清单｜「doc-budget 字数预算」：现文「- doc-budget 字数预算（每批必读文件连两批触行数上限、或单文件 >2 万字符）」；实测计数 1（命令：`grep -c 'doc-budget' AGENTS.md`）。工具面已实现：`scripts/check.sh` [3] 段头注含「每批必读件字数预算」、`scripts/check_content.py` 有 `_check_budget()`——宪法与工具现实冲突。
- `docs/specs/施工机制.md` §九模板 §四留痕表｜「三件现场核过」：表行含「位置（行号与引文）」与判定列预填「通过」；实测计数：「（行号与引文）」1 处（命令：`grep -c '（行号与引文）' docs/specs/施工机制.md`）——与 116 新立「位置不写行号」条文相抵。
- `docs/specs/施工机制.md` §九模板 §三验收断言｜「CHANGELOG 条目已落」：现文「3. 过程产物：规格移入 `archive/`；`CHANGELOG` 条目已落（仅用户可感知变更）。」；实测计数 1——内部治理批按 §五登记条不落 CHANGELOG，该断言无「无则写无」出口、恒不可满足。
- `docs/specs/施工机制.md` §二（审查任务与对象）｜「`109` 批实测」：现文「引 `check_content.py` 既有的 `109` 批实测：点名面上限 ≈10.7 万字符」（命令：`grep -c "109.. 批实测" docs/specs/施工机制.md` → 1）——裸批号（反引号隔断词组，宽 grep 不中），违 §18。同件 §三（审查位）核得「任务书」一词 0 处（`grep -c '任务书' docs/specs/施工机制.md` → 0）——审查派单的文件面／证据口径／输出上限未成文，两轮全靠个人手感。
- `docs/specs/施工问题记录.md` §二表｜「批 116 落地」：现文「形态侧已于批 116 落地（`施工机制` §九 位置形态）」（命令：`grep -c "批 116" docs/specs/施工问题记录.md` → 1）——裸批号，违《文字与命名标准》§18 指认须全路径；「≤30 中文字符（含标点）」1 处——拉丁字符计不计未定。
- `docs/standards/skill资产标准.md` §16.4｜「真机人工试跑」：现文「按[施工机制](../specs/施工机制.md) §二 的『真机人工试跑』条」；实测计数 1——`施工机制` §二 实文为「④ 改用户可观测行为（须真机试跑）」，无该「条」，死链。
- `docs/standards/skill资产标准.md` §17 表｜「三要素 + 规格自审（见」：现文「见[施工机制](../specs/施工机制.md) §二）」；实测计数 1——模板与自审实在 §九，错节号。
- `docs/standards/运行环境标准.md` §7.4｜「裁定表随规格件」：实测计数 1——`施工机制` §九 模板本体仅四节（§一 现状位置／§二 改动清单／§三 验收断言／§四 规格自审），无「裁定表」要素，死内容指针。
- `docs/standards/产物契约标准.md` §2.1｜「分工见」：现文「（分工见[施工机制](../specs/施工机制.md) §四）」；实测计数 1——§四＝Subagent 规则，机器／AI／作者分工实在 AGENTS §五.1。
- `docs/standards/文字与命名标准.md` §13.8 与 §三表｜「行数 ÷ 3」：§13.8 已立保底公式（单文件 `**` 数 ≤ 行数 ÷ 3），§三表写「**不设守卫**（阈值未立法）」、`scripts/check.sh` [3] 头注同写——同一事两读（阈值已立与未立法并存）。
- `scripts/check.sh` [3] 段头注｜「施工机制 §四」：实测计数 1——「余项候选只呈报」之规实在 AGENTS §五.2，反指错节。
- `scripts/check_content.py` 头注｜「仍为全仓」：现文「仍为全仓 `docs/`＋根 `*.md`（含归档、排本批在制规格）」——实测扫描 83 件（治理 78＋skill 资产 5，含 `skills/`）；另句「语法自检用 `ast.parse`」全件无 ast 实现（`grep -c 'ast'` 命中仅该句自身），系自 `check_spec.py` 复制的残句。
- `scripts/check_content.py` `_gov` 段｜「token + '.md' in gov_names」：实测计数 1——死析取（`_gov_names` 已收带扩展名形态，第二析取永冗余）。
- `scripts/check_content.py` 用词段｜「lower()」：实测计数 0——纯子串大小写敏感匹配，英文禁用词大写形态漏报（清单含英文词）。
- `scripts/check_spec.py` check_template_names｜「只核三个有模板对应的正则」：实测计数 1——实核四个（SECTION／CHANGE_SECTION／ANCHOR_SECTION／AUDIT_SECTION），注释陈旧。
- `scripts/check_spec.py` 头注维护入口｜「检查 M 改 check_audit」：现文枚举 A／B／C／D／E／H／I／J／M／L 十项，缺「检查 K」条目（命令：`grep -c '检查 K 改' scripts/check_spec.py` → 0）。
- `scripts/check_spec.py` 核对③文案｜「旧称需同步更新的文件核对」：实测计数 1——与主名同串，读作「X（旧称 X）」，自相矛盾无信息量。
- `scripts/check_spec.py` `_anchor_probe` 与 `_change_position_probe`｜「lines[ln - 1]」：实测计数 2（两腿各一）——`ln=0` 时落到末行参与判定，非法坐标未拦。
- `scripts/check_spec.py` 检查 I 两腿｜「实有」：行数口径＝`len(split('\n'))`（含末空元素）——3 行件报「实有 4 行」，与 `_read_target` docstring「行数＝换行符出现次数（与 `wc -l` 一致）」相抵。
- `scripts/check_spec.py` `_audit_rows` 与 `_change_position_probe`｜「strip('|')」：实测计数 2——首尾多根竖线全剥，空边格行格数位移（`_cells` 只剥一根、认转义）。
- `scripts/check_spec.py` 围栏族｜「FENCE_BLOCK」：严格形态（info-string 只认纯字母）与检查 L 的宽松形态 `FENCE_LOOSE` 并存——「``` python」类围栏在 D／I 下开闭误判、其后内容漏检；`scripts/check_content.py` 另有第三套 `FENCE_LINE` 同病（c++／json5／四反引号栏不识）。
- `scripts/check_spec.py` check_nuclear｜「set(st) <= set('-: ')」：主循环无 `st` 空守卫、非表体计数器有——空状态行被判分隔行静默跳过，两处谓词不一致。
- `scripts/check_spec.py` check_swallow（检查 A）｜「命中 0 条」：实测 115 提取 9 命中 0、116 提取 10 命中 0——现行断言形态（命令 → 期望 N。）不被目标解析，断言自检对在制规格零命中。
- `scripts/check_spec.py` check_template_names｜「_k_reds」：实测计数 1（接收死值）＋`_template_section_names` 同件连读两次。
- `scripts/patch_file.py` `_shape_gate`｜「Linting:」：现守卫只认「0 files」串——审计实测：目标在 `.tmp/`（lint 排除面）时回显「Linting: 1 file」＋0 issues，MD012 违例照写、退出 0 报「全部通过」，排除面失明（与 116 产物审查 P1-① 同构）。
- `scripts/patch_file.py` `_write`｜「except Fail」：实测计数 2——落盘类 `OSError` 不在收编范围，裸 traceback 退 1，头注退出契约（0／2）未列该分支。
- `.gitignore`｜「.mimosa」：现内容 9 行无该串（命令：`grep -c 'mimosa' .gitignore` → 0）——钩子状态目录未忽略，`git add -A` 会带脏数据入库（红线 2 缺口）。
- `docs/specs/` 在制区｜「复盘修复批／自审留痕／位置形态」：实测在制 3 件（114／115／116 均已提交完毕）——一键检查候选（终值实测 47 行/次，全部候选与信息类）：归档前分项 114→26／115→40／116→22／本规格自身 51（其中约 30 行为检查 A「跳过」行，第 23 项修复后转为命中与信息面）；初稿「86／126」两次计数均未含或误含本规格自身，产物审查 P2 纠正后按终值记；归档是在制件的自然出口、候选洪泛的零机制解法。

## 二、改动清单

| # | 文件｜位置 | 改动（现状 → 目标） | 依据／裁定 |
| --- | --- | --- | --- |
| 1 | `AGENTS.md` §四不做清单 与 `CHANGELOG.md` ### Changed｜「doc-budget 字数预算」 | AGENTS 整行删除（解冻成立：工具已实现并跑在 [3] 段）；CHANGELOG 补解冻留痕一行（产物审查 P1：初稿误读「记录已存在」，实为 0——AGENTS §四 的记入义务） | 宪法与工具现实冲突；审计 P0 |
| 2 | `docs/specs/施工机制.md` §九模板 §四留痕表｜「三件现场核过」 | 「位置（行号与引文）」→「位置（位置串与引文）」 | 116 新立位置形态条文的漏网残句 |
| 3 | `docs/specs/施工机制.md` §九模板 §四留痕表｜「通过」 | 判定列预填「通过」→「{判定}」（占位形态，M 拦花括号占位） | 「先写结论后跑」两轮复发两次，机械堵 |
| 4 | `docs/specs/施工机制.md` §九模板 §三验收断言｜「CHANGELOG 条目已落」 | 补出口：「无则写明『无——内部治理批不落 CHANGELOG』」 | 恒不可满足断言，模板设计缺陷 |
| 5 | `docs/specs/施工机制.md` §三审查位｜「成稿审查」 | 补一句：审查任务书三要素——点名文件清单、证据口径、输出上限 | 116 实证收敛式任务书单位发现成本低一个量级 |
| 6 | `docs/specs/施工机制.md` §二 与 `docs/specs/施工问题记录.md` §二表｜「`109` 批实测」「批 116 落地」 | 两处裸批号 → 全路径指认；「≤30 中文字符（含标点）」→ 补「按字符计，拉丁与标点各计 1」 | 《文字与命名标准》§18；计数口径歧义 |
| 7 | `docs/standards/skill资产标准.md` §16.4 与 §17 表 | 「§二 的『真机人工试跑』条」→ 指认 §二 实条「④ 改用户可观测行为（须真机试跑）」；「见施工机制 §二」→「§九」 | 死链与错节号，审计 P1 |
| 8 | `docs/standards/运行环境标准.md` §7.4｜「裁定表随规格件」 | 删「见 `施工机制` §九 模板」死指针，改指批件实际承载节（被否决的替代方案／开放项——模板本体四节无裁定要素） | 死内容指针（模板仅四节） |
| 9 | `docs/standards/产物契约标准.md` §2.1｜「分工见」 | 「施工机制 §四」→「AGENTS §五.1」 | 错节号 |
| 10 | `docs/standards/文字与命名标准.md` §三表、`scripts/check.sh` [3] 头注、`scripts/check_content.py` 三处｜「阈值未立法」 | 四处 →「阈值已立（§13.8 行数 ÷ 3 保底），守卫未设」 | 同一事两读，向实情对齐（成稿审查 P1-4：初稿漏 check_content 三处） |
| 11 | `scripts/check.sh` [3] 段头注｜「施工机制 §四」 | 「（施工机制 §四）」→「（AGENTS §五.2）」——候选只呈报之规的实出处 | 反指错节（审计 P1，§一 已指认、原漏修复项） |
| 12 | `scripts/check_content.py` 头注｜「仍为全仓」 | 扫描面描述改实况（全仓 `.md` 含 `skills/`，排⑦所列）；删「语法自检用 `ast.parse`」残句；依赖清单补 `io`；另 :15「`[4]` 段既有调用惯例」实为 [3] 段，段名订正 | 文档与行为相反 |
| 13 | `scripts/check_content.py` `_gov` 段｜「token + '.md' in gov_names」 | 删死析取 | 死代码 |
| 14 | `scripts/check_content.py` 用词段｜「lower()」 | 匹配双边折叠大小写 | 英文词大写漏报 |
| 15 | `scripts/check_content.py` 注释｜「109 实测」 | 裸批号 →「历史单批实测（见 `docs/specs/archive/`）」 | §18 用词 |
| 16 | `scripts/check_spec.py` 注释与文案批改（七处） | ①「只核三个」→ 实数四个；②维护入口补「检查 K 改 check_template_names() 与 SECTION 族」；③「四项计数」→ 实数；④「（旧称需同步更新的文件核对）」→ 删（与主名同串无信息量）；⑤判二候选前缀误标「核对①」→「核对②」；⑥退出契约补 `--reconcile` git 失败退 2；⑦已知限制 ⑥ 降级改写（实测 0.10 秒／81 次开文件，现规模无瓶颈） | 陈旧自描述＋审计遗漏两条归位 |
| 17 | `scripts/check_spec.py` 两位置腿｜「lines[ln - 1]」 | 补 `ln <= 0` 守卫，报「行号 0 非法坐标」候选 | 非法坐标参与判定 |
| 18 | `scripts/check_spec.py` 检查 I 两腿｜「实有」 | 行数改物理行数（末尾换行不计空行），与 `_read_target` docstring 对齐 | 口径相抵，3 行件误报 4 行 |
| 19 | `scripts/check_spec.py` 两表解析｜「两处 strip 全剥竖线」 | 改单剥首尾各一根（与 `_cells` 同口径） | 空边格行格数位移 |
| 20 | `scripts/check_spec.py` 围栏族 与 `scripts/check_content.py` `FENCE_LINE` | `FENCE_BLOCK` 放宽为宽松语义（info-string 不限字母）、`FENCE_LOOSE` 并入单一常量；`FENCE_LINE` 同型放宽；全规格前后候选对照记录 | 三套口径并存、同输入三种判定 |
| 21 | `scripts/check_spec.py` check_nuclear｜「set(st) <= set('-: ')」 | 主循环补 `st` 空守卫，空状态行改报「五档外状态词（空）」候选 | 两处谓词不一致，静默漏报 |
| 22 | `scripts/check_spec.py` check_template_names｜「_k_reds」 | 死值清理；`_template_section_names` 同件连读合一 | 死代码与微损耗（瘦身项） |
| 23 | `scripts/check_spec.py` check_swallow（检查 A）｜「命中 0 条」 | 目标解析适配现行断言形态（命令 → 期望 N。）——在制规格断言须命中 | 断言自检对现行规格零命中，自审支柱失守【风险项，见 §八】 |
| 24 | `scripts/patch_file.py` `_shape_gate`｜「Linting:」 | 排除面如实化：调用 lint 前自判目标是否命中 `.markdownlint-cli2.jsonc` 的 ignores（`fnmatch`，标准库零依赖）——命中则明示「形态检查未覆盖（lint 排除面）：{路径}」、**不打印「全部通过」**、退出 0（写盘合法，禁的是假报告）；未命中照常 lint，原「0 files」守卫保留 | 排除面失明；成稿审查 P1-6：相对路径方案依赖 lint 对 ignore 的优先级、两读，改自判 |
| 25 | `scripts/patch_file.py` `_write` 与 `_shape_gate`｜「except Fail」 | `OSError` 收进 `Fail`（退 2），头注退出契约补列 | 裸 traceback 破契约 |
| 26 | `.gitignore`｜「.mimosa」 | 追加一行 `.mimosa/` | 红线 2 缺口 |
| 27 | `docs/specs/114-2026-09-17-复盘修复批.md`→`docs/specs/archive/114-2026-09-17-复盘修复批.md`、`docs/specs/115-2026-09-17-自审留痕与位置核批.md`→`docs/specs/archive/115-2026-09-17-自审留痕与位置核批.md`、`docs/specs/116-2026-09-17-位置形态与派生坐标批.md`→`docs/specs/archive/116-2026-09-17-位置形态与派生坐标批.md` | 件级移动（两侧路径齐声明——git 视移动为删＋增）、内容零改动；移动后一键检查候选行数复测记录 | 候选洪泛的零机制解法（否决基线登记机制，见 §七） |
| 28 | 本规格 | 新建（本件） | 文件账本律 |

## 三、验收断言

1. `grep -c 'doc-budget' AGENTS.md` → 期望 0。
2. `grep -c '（行号与引文）' docs/specs/施工机制.md` → 期望 0。
3. `grep -c '{判定}' docs/specs/施工机制.md` → 期望 ≥1。
4. `grep -c '无则写明' docs/specs/施工机制.md` → 期望 1。
5. `grep -c '任务书三要素' docs/specs/施工机制.md` → 期望 1。
6. `grep -c "批 116 落地" docs/specs/施工问题记录.md` 与 `grep -c "109.. 批实测" docs/specs/施工机制.md` → 期望均 0（两处裸批号改全路径指认；`grep -c "docs/specs/116-2026" docs/specs/施工问题记录.md` → 期望 ≥1）。
7. `grep -c '真机人工试跑」条' docs/standards/skill资产标准.md` → 期望 0（判据收窄：死指针形态「§二 的『真机人工试跑』条」须灭；短语本身是正题句合法词——初稿断言写得过宽，施工中订正）。
8. `grep -c '规格自审（见.*§二' docs/standards/skill资产标准.md` → 期望 0（判据收窄：§17 的「见施工机制 §二」错节须灭；:164 的「§二 风险升级信号④」是正确引用——初稿断言写得过宽，施工中订正）。
9. `grep -c '裁定表随规格件' docs/standards/运行环境标准.md` → 期望 0。
10. `grep -c '施工机制.md) §四' docs/standards/产物契约标准.md` → 期望 0。
11. `grep -c '阈值未立法' docs/standards/文字与命名标准.md`、`grep -c '阈值未立法' scripts/check.sh`、`grep -c '阈值未立法' scripts/check_content.py` → 期望均 0（四处改「已立」表述）。
12. `grep -c 'ast.parse' scripts/check_content.py` → 期望 0。
13. `grep -c "token + '.md' in gov_names" scripts/check_content.py` → 期望 0。
14. `grep -c '109 实测' scripts/check_content.py` → 期望 0。
15. 用词扫描大小写折叠（双判）：`grep -c 'lower()' scripts/check_content.py` → 期望 ≥2；行为面（产物审查 P0 补）——夹具 `.tmp/117/` 内一件含大写「Findings」，`python -c "import sys; sys.path.insert(0,'scripts'); import check_content as cc; c,_=cc._check_words(['<夹具相对路径>']); print(c)"` → 期望输出含「用词待改」与「findings×1」且不抛异常（初稿只数 lower() 串数不验行为，old_l 未定义 bug 曾被 0 命中掩住）。
16. `grep -c '只核三个' scripts/check_spec.py` → 期望 0。
17. `grep -c '检查 K 改' scripts/check_spec.py` → 期望 1（维护入口补齐）。
18. `grep -c '旧称需同步更新的文件核对' scripts/check_spec.py` → 期望 0。
19. `grep -c '_k_reds' scripts/check_spec.py` → 期望 0。
20. `grep -c 'FENCE_LOOSE' scripts/check_spec.py` → 期望 0（围栏单一常量）。
21. `grep -c "strip('|')" scripts/check_spec.py` → 期望 0。
22. 行号 0 防护（夹具法）：构造 `.tmp/117/z0.md`（§一 位置条目，内文一行「- 「scripts/check_spec.py」反引号包裹＋冒号 0＋「（行号 0）」——构造用 116 断言 16 的 chr 码单行法，施工时先跑当前基线预期 0）→ `python scripts/check_spec.py --static .tmp/117/z0.md 2>&1 | grep -c '非法坐标'` → 期望 1。
23. 行数口径（夹具法）：构造 3 行目标件 `.tmp/117/zt3.md`（三行各一段文字、末行带换行）与指向它的越界位置条目 → `python scripts/check_spec.py --static <夹具规格> 2>&1 | grep -c '实有 3 行'` → 期望 1（当前基线报「实有 4 行」——即口径 bug 本身）。
24. 围栏统一（夹具法）：夹具 `.tmp/117/fenceA.md`（围栏内仅置坏位置、无栏外对照行——栏外方向已在施工快照双向留痕）→ `python scripts/check_spec.py --static .tmp/117/fenceA.md 2>&1 | grep '^· 检查 I' | grep -cv 汇总` 与 `grep -c '^· 检查 L'` → 期望均 0（判据订正：汇总行恒输出、非候选，初稿未排除属断言笔误；修复前基线＝I 报围栏内越界 ≥1，即 bug 本身）。
25. 检查 A 自食：`python scripts/check_spec.py --static docs/specs/117-2026-09-17-审计修复与脚本瘦身批.md 2>&1 | grep -c '命中 0 条'` → 期望 0（本规格断言须被解析命中）。
26. patch_file 排除面（夹具法）：向 `.tmp/117/` 写入含 MD012 违例（行尾多空格）的目标件 → 输出含「形态检查未覆盖」且不含「全部通过」（退出 0 合法——写盘不禁，禁假报告）。
27. `grep -c 'mimosa' .gitignore` → 期望 1。
28. `ls docs/specs/*.md | wc -l` → 期望 3（施工机制／施工问题记录／本规格）。
29. 一键检查：`bash scripts/check.sh` → 期望退出 0；且 `bash scripts/check.sh 2>&1 | grep -c '^· '` → 期望 ≤50（初稿推算 ≤40 有两处漏计，施工中按实测重定；终值 47 行分类：I 新形态陈旧 16＋截断提示 1＋汇总 1、检查 A 信息面 ≈19（自我匹配预警／零目标达成态／不可判／中性）、J 开放项 4、写作预警 4、其余为各段汇总行；全部为候选/信息类，无判为失败项——分类计数随施工微动 ±3，以「全部候选类＋逐类无失败」为实判据）。
30. 语法：`python -c "import ast,io; [ast.parse(io.open(f,encoding='utf-8').read()) for f in ['scripts/check_spec.py','scripts/check_content.py','scripts/patch_file.py']]"` 与 `bash -n scripts/check.sh` → 期望均退出 0。

## 四、规格自审（五条·留痕表）

| 项 | 核法与命令 | 实测输出摘要 | 判定 |
| --- | --- | --- | --- |
| 1 位置／计数／写入位置三件现场核过 | §一 逐条 grep 实跑＋成稿审查 13 条纠偏后复核 | 初稿 6 条失实已订正（「批 116 落地」实况、「109」反引号形态、模板本体四节、候选 126 行、「可观测」、断言 3 节位）；修订后逐条全真 | 通过 |
| 2 断言实跑过 | §三 断言 1–21／27／28／30 为 grep／语法类，基线值已随 §一 同源核过；22–26 为夹具类，施工后首跑 | 非夹具断言命令可复跑；夹具构造法沿用 116 验证过的 chr 码写法 | 通过 |
| 3 禁改文件已写明 | 逐件指认 | `docs/product/**`、`skills/**`、`README.md`、`CHANGELOG.md`、`.mimosa/**`、`docs/specs/archive/` 既有件（第 26 项三件移动除外） | 通过 |
| 4 范围与期望同源 | 逐条指认 | §二 28 项 ↔ §三 30 条断言：订正类↔grep 断言、防护类（16–25）↔夹具断言、卫生类（26–28）↔文件系统断言；第 20 项附前后对照记录义务 | 通过 |
| 5 自述有守卫 | 逐句指认 | 订正类兜＝grep 断言（1–21）；防护类兜＝夹具断言（22–26）；归档兜＝ls＋候选计数（28／29）；语法兜＝断言 30 | 通过 |

## 五、需同步更新的文件核对

- `scripts/check_content.py` `_check_budget` 注释「依据＝AGENTS §四解冻词」→ 随第 1 项销项改指「CHANGELOG 解冻记录」。
- `scripts/check.sh` [3] 头注「加粗密度不实现——阈值未立法」→ 随第 10 项同步（并入断言 11 判定）。
- CHANGELOG 解冻记录：初稿称「已存在（实测 2）」系**误读**——那 2 是合并式样 `doc-budget\|字数预算` 的合计命中，`grep -c 'doc-budget' CHANGELOG.md` 实为 **0**（产物审查 P1 逮住）。按 AGENTS §四「解冻由作者裁决记入 CHANGELOG」补一行（### Changed 段）；本规格确认即作者裁定凭证。

## 六、禁止事项

- 禁改：`docs/product/**`、`skills/**`、`README.md`、`CHANGELOG.md`（**唯一例外**：§五 所述解冻记录补一行——产物审查 P1 处置）、`.mimosa/**`。
- `docs/standards/**` 四件与 `docs/specs/施工机制.md`、`施工问题记录.md`：仅 §二 点名行可改，余名禁改。
- 归档移动限 114／115／116 三件；`docs/specs/archive/` 既有件不动、不回改。
- 不新增检查项编号、不新增脚本文件、不引外部依赖、不跑联网命令。
- 禁做 §七 否决清单八项。

## 七、被否决的替代方案（过度设计门槛）

1. TOC 锚点 URL 校验——无事故出身（不做清单既有原则：无事故出身不立守卫）。
2. 「交叉引用存活性」机械检查——同上；本轮人工修完即闭环，复发再立。
3. 枚举多处收敛重构（由代码生成 docstring）——两次枚举同步零事故，改动面不抵收益。
4. 节名／词表硬编码抽头——已知限制 ⑦ 已记，现规模收益低。
5. 候选基线登记机制——以「归档」既有机制替代（第 26 项），零新增。
6. 性能优化（缓存／索引）——实测 `--static` 3 规格 0.10 秒／81 次开文件，无瓶颈；仅将已知限制 ⑥ 降级改写（并入第 15 项注释批改）。
7. docstring 结构重构／散文搬迁——无事故驱动，纯搬动。
8. patch_file 跨文件事务——现无跨文件批事故出身。

另：「行数÷3」以「行数 ÷ 3」带空格写法坐实（计入第 10 项）。成稿审查纠初稿一处自误：曾称「109 系审计误报」，实为反引号隔断词组（`grep -c '109 批'` 不中而 `grep -c "109.. 批实测"` 中 1）——裸批号实存，已计入第 6 项。第 7／8 条否决理由「缺事故驱动」未快证，按保守不立。

## 八、开放项

- 第 22 项（检查 A 目标解析适配）为风险项：若施工中发现须重构 `ASSERT_CMD`／`_assert_ok` 解析器主体，停手呈报改轨，不强行塞补丁。
- 归档后 116 件内指向在制路径（`docs/specs/115-…`）的断言属归档件不复跑范畴，不回改。
- 第 20 项守卫补齐后，存量规格若显形「五档外状态词（空）」候选，属既判漏报面显形，逐条人工过（预计 ≤3 条）。

## 九、审查记录

- 2026-09-17｜成稿审查｜发现的问题 共 13：P0:0 P1:7 P2:6｜结论 修订后通过｜工具往返数 19｜周期时长 <1h
  - **P1-①**：§一 引「另立批 116」实况已不存在——116 批施工时已将该行改为「已于批 116 落地」，断言 6 空转 → 引文与断言 6 改按实况。
  - **P1-②**：初稿 §七 称「109 系审计误报」不实——裸批号以反引号形态存在（`` `109` 批实测``，宽 grep 不中）→ 撤回误报句，计入第 6 项。
  - **P1-③**：第 8 项修复指「模板实有节 §七/§八」不成立——**模板本体仅四节**（§一–§四），§五–§九 系批件实际扩展 → 目标改「删死指针、指批件实际承载节」；§一「模板（§一至§九）」同步订正。
  - **P1-④**：断言 11 漏 `check_content.py` 三处「阈值未立法」→ 第 10 项两处扩四处。
  - **P1-⑤**：候选总数 86 夰实（未含本规格自身 ≈39 行，实测全量 126）→ §一 改 126 并给构成；断言 29 上限 ≤30 → ≤40 附推算（检查 A 跳过行修复后消隐）。
  - **P1-⑥**：第 24 项「仓根相对路径」方案两读——lint 对显式传参 vs ignore 的优先级不确定，断言 26 可能恒不满足 → 改自判方案（`fnmatch` 匹配 ignores、如实报「未覆盖」、不打印「全部通过」、退 0），断言 26 判据改输出内容不判退出码。
  - **P1-⑦**：check.sh [3]「施工机制 §四」反指在 §一 已指认但 **§二 漏修复项** → 增补为第 11 项（项号顺延至 28）。
  - P2 六条：⑧断言 3 节位（§五→§九模板 §三）；⑨「可感知」→「可观测」；⑩模板四节描述；⑪第 16 项「四处」→「七处」列明（并归位审计遗漏两条：判二前缀、reconcile 契约）；⑫夹具断言 22–24 补构造口径与当前基线值；⑬§六 两读句改写、CHANGELOG 实测已有解冻记录（2 处）取消条件例外。
  - 观察项：§一 28 行现况 22 条初审即真（计数 25/39/22、mimosa=0、检查 K 缺项、FENCE 三套等复算一致）；patch_file 排除面失明经审查位独立复现（退 0＋「全部通过」）；§七 否决 1–5 成立、7/8 未快证按保守不立；断言 28 基线 6→归档后 3 成立；断言 30 ast＋bash -n 实测过。
- 2026-09-17｜产物审查｜发现的问题 共 6：P0:1 P1:1 P2:4｜结论 修订后通过｜工具往返数 41｜周期时长 <1h
  - **P0**：第 14 项引入 `NameError`（`old_l` 未定义）——用词真命中即崩；仓内恰 0 命中把 bug 掩住，断言 15 只数 `lower()` 串数验不到行为。→ 修为 `low.replace(old.lower(), …)`；补双向行为验证：夹具大写「Findings」实测报「findings×1→发现的问题」不崩、全仓复扫仍 0/0；断言 15 扩为双判。施工自查另记：修复过程中又踩 JSON→heredoc 双层转义（`\u0000` 塌成真 NUL 字节进源码，`ast.parse`「null bytes」拦住）——终以 `chr(92)` 构造绕开，教训与 116 的 `
` 塌陷同族。
  - **P1**：§五「CHANGELOG 解冻记录已存在（→2）」系**误读**——2 是 `doc-budget\|字数预算` 合并计数，`grep -c 'doc-budget' CHANGELOG.md` 实为 0；解冻留痕义务被误免。→ 补 ### Changed 一行（点名裁定出处与本批），§五／§六 同步改按实况。
  - P2 四条：①§一 候选计数（86→126→终值 47）两次初稿值未随修订复算——按终值改写并注明三次计数的错因；②断言 29 内嵌分类数 46 vs 47 漂移——分类计数改「±3 微动、实判据＝全部候选类」并复算终值；③`_lint_ignores` 负向 `!` 串丢弃——docstring 如实化（本仓配置全正向；含负向时保守误报「未覆盖」，不产假「通过」）；④归档 114 工作树现 CRLF（HEAD 为 LF，115/116 未变）——字符级零改动、字节级待 git 提交时按 eol 配置归一（呈报核 rename 识别，不回改文件）。
  - 断言订正正当性核（审查位判定）：7／8／24 **正当**（收窄保原意：杀死指针/错节/围栏内候选，正题合法词与恒输出汇总行不误杀）；29 **存疑轻微**（上限有实测支撑、全候选类；内嵌分类数不可精确复算——已按②改为「实判据」表述）。
  - 已核无问题面：断言 1–21/25/27/28/30 复跑全过、夹具 22/23/24/26 全过；第 23 项两载体（裸无尾／git-grep）回归行为未变；第 16 项七处齐；第 25 项只读目录实测退 2 无裸 traceback；第 3 项五行全改；第 10 项四处清零且与 §13.8 双向自洽；围栏统一后 D/I 在全部在制＋归档规格上零不可解释漂移；检查 A 噪声面仅手动跑归档件显形（不进 check.sh）；归档 115/116 零改动；J/K/TOC/引用闭合无异常；§二 28 项与 diff 逐项相符。
