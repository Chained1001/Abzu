# 规格：批次 B · 扫榜域移植（含 CDP 底座）

## 现状

架构已落盘（architecture.md v1.0）。mo-shu 勘察完成：moshu-scan v1.2.0（SKILL 101 行 + 7 references 948 行 + 6 脚本 2667 行）+ moshu-cdp v1.1.1（SKILL 176 行 + setup-cdp-chrome.js 1008 行）。运行时零 npm 依赖（CDP 走 9222 + agent-browser 全局工具；起点 SSR 纯 Node）。本机前置三项全绿（Node 25 / agent-browser 0.34 / Chrome）。

## 决策

整体移植，单 skill 树落位；内容原样保留（含安全同意流程），只动接口。移植脚本保留原名（scan-analyze.js / cdp-utils.js 等）——内部 require 依赖与上游可对照性优先于命名惯例。

## 文件级改动清单

1. `scripts/` 移植 7 个脚本（scan-analyze / cdp-utils / 4 平台抓取器 / setup-cdp-chrome），原样
2. `references/scan/` 移植 7 份 references，原样（互相为同目录链接，符合引用分区规则）
3. 新写 `references/scan/workflow-scan.md`（自 moshu-scan/SKILL.md 改写：头部三行声明；/moshu-cdp 改指 cdp-base.md；流程衔接改"analyze/outline 域未建设"前向占位）
4. 新写 `references/scan/cdp-base.md`（自 moshu-cdp/SKILL.md 改写为底座文档：去 frontmatter，保留同意流程/启动/操作/清理/超时/FAQ 全部操作内容）
5. 重写 `.claude/skills/abzu/SKILL.md` 正文首版：总控（角色/会话恢复协议/域路由表/门控原则/语言规则），scan 路由激活，五域标未建设；description 补扫榜触发词；删除对仓库 docs 的引用（运行时自包含）
6. `CHANGELOG.md` 记录（含来源标注：移植自 mo-shu v2.6.1）

## 验收标准

- [x] `agentskills validate .claude/skills/abzu` 通过；SKILL.md <300 行
- [x] scripts/ 7 个脚本存在，require 关系完整（cdp-utils 被抓取器引用）
- [x] references/scan/ 9 份文件（7 移植 + workflow-scan + cdp-base）
- [x] 移植文件内无 moshu 残留指称（来源标注除外）；无对仓库 docs/ 的引用
- [x] 安全内容保真：CDP 杀 Chrome 同意流程（NEEDS_CONSENT / --yes）原文保留
- [x] `npx markdownlint-cli2` 0 违例
- [x] SKILL.md description 含扫榜触发词

> 验收完成：2026-09-05，全部通过。moshu 残留 2 处为来源标注中的上游文件路径名（验收条款豁免）。
