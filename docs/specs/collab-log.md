# 协作日志（collab-log）

> **定位**：规划方/施工方分工协作的逐次登记与模板四件套——数据化调优协作流程（模型稳定性/粒度性价比/任务适配）。
> **纪律**：每次规格施工完成（核验通过或整改）追加一条；只记录事实与数字，不写过程叙事。

## 模板四件套

### 施工提示词

```
你是施工方。任务：按规格施工 {规格文件}。你没有 git 写权限，也无需尝试提交；完成后只输出核验请求。

1. 先读 AGENTS.md（宪法）与规格全文，再动手。
2. 严格按规格「文件级改动清单」施工——每条带自由度标记：[A] 逐字执行、[B] 按方向自定实现（不得改变行为）。
3. 偏差分级：P0 阻塞型（规格与现状冲突，继续会做错）→ 立即停下，输出全部改为偏差报告；P1 观察型（不影响施工）→ 继续施工，记入核验请求偏差记录。
4. 规格「禁止事项」为硬边界：禁止触碰清单外文件。
5. 全部完成后输出【核验请求】——**整段包在单个代码块里**，便于作者原样回传规划方：改动清单逐文件对照规格、验收标准逐条自跑结果、偏差记录（P1）、未决问题。
```

### 核验请求（施工方产出）

```
【核验请求】
- 规格文件：{规格路径}
- 改动清单：逐文件对照规格改动清单
- 自核验结果：验收标准逐条 + 实际执行命令与输出摘要
- 偏差记录：P1 观察型偏差（如有）
- 未决问题：需规划方裁决的点（如有）
```

### 整改提示词（核验未通过时）

```
你是施工方。上一轮施工未通过核验。
- 核验结论：{逐条列出问题}
- 整改范围：{只允许改这些文件/这些点，其余维持原状}
- 整改后重新输出【核验请求】
- 禁止扩大整改范围，禁止"顺手"改其他东西
```

### 裁决请求（呈作者拍板）

```
【裁决请求】
- 背景：{一段话说明分歧/冲突}
- 选项 A：{描述 + 代价 + 收益}
- 选项 B：{描述 + 代价 + 收益}
- 我的建议：{选 X，理由}
- 请你拍板
```

## 核验协议（规划方执行）

> 判定标准来源：具体断言以各规格「验收标准」节为准（立法在先）；本协议定义核验动作与判定细则。

### 第一步 · 改动面核对（最强一道闸）

- 文件白名单：`git status --short` 必须且仅显示规格清单允许的文件——多出任何一个 = P0，整份打回
- 改动类型：`git diff` 逐 hunk 审查，只允许规格「文件级改动清单」声明的改动类型；任何逻辑行变动（函数/URL/字符串拼接/流程）= 打回
- [A] 级条目：与规格给出文本 diff 为零，一字之差即偏差
- 未动文件：规格声明"不得触碰"的文件必须零 diff

### 第二步 · 验收命令独立重跑（不信施工方报告）

- 规格验收标准的每条命令用规划方自己的会话重跑；施工方自跑结果只作参考
- 退出码不经管道吞掉（宪法反模式 #6）

### 第三步 · 内容双轨抽查（§3.6）

- 结构轨：由 grep 类断言覆盖
- 内容轨（人工语义）：通读 diff 上下文——注释块插入位置合理（shebang/JSDoc 事故防范）、改后文案语义通顺不误导、施工方报的 P1 偏差属实、未决问题是否升级为裁决请求

### 审查维度七分（内容轨的完整检查矩阵）

| 维度 | 审什么 | 检测手段 | 执行者 |
| --- | --- | --- | --- |
| ① 结构合规 | 命名/位置/层级/序号——对照 file-conventions | lint + grep | 命令 |
| ② 格式合规 | 排版/引号/加粗密度/占位符——对照 markdown-style | lint + grep | 命令 |
| ③a 内容残留 | 上游残留/批次号/旧概念词/过时指向 | grep + 通读 | 规划方 |
| ③b 内容质量 | 语义通顺/逻辑自洽/数字一致/事实相符/无断裂截断 | **仅 LLM 语义审**（不可机械自动化） | 分身或规划方通读 |
| ④ 跨文件一致 | 同概念不同文件表述一致（术语/数字/路径） | 通读 + 对照 | 规划方 |
| ⑤ 完整性 | 必备节序齐全/无孤儿字段/引用闭合 | 逐节核对 | 规划方 |
| ⑥ 规范健康度 | 规范条文本身是否被现实违反/是否有条文过时 | **仅 fresh 分身能做**（规划方有确认偏袒） | 分身 |

**③a/③b 分界**：残留是"有没有不该在的"（模式可 grep）；质量是"在的东西对不对、通不通"（需语义理解）。两类检测手段、执行者、可靠度完全不同，必须分开定义。

**③b 不可机械自动化**：语义断裂、逻辑矛盾、数字与规则打架——均需 LLM 语义理解才能发现，grep/lint 无法覆盖。这是 subagent 审查的独占价值所在。

**维度⑥ 仅 fresh 分身可做**：规划方知道自己"为什么这么写"，看不见自己规范里的过时与矛盾（实证：中央路由退役后 skill-writing 理念层残留——规划方三轮审计未发现，fresh 分身首跑即报）。

### 结论三态

- 全绿 → 报告通过，请求作者确认提交（pre-commit hook 双保险）
- 有问题 → 出【整改提示词】（问题逐条 + 整改范围 + 禁止扩大）
- 规格自身缺陷 → 【裁决请求】呈作者

### 边界声明

核验通过 = 按规格改了、语法没坏、文档合规、没越界；**不等于运行时行为验证**——需要联网/真机验证的，归实测阶段（由作者执行）。

### 独立审查分身（可选增强，重大规格/全域审计时启用）

规划方可 spawn 独立审查分身（fresh context，不经规划方意图污染）执行内容轨审查——依据 agent-design §二 审查隔离判据（评审者与创作者分身，防自审偏袒）。分身只回报 findings；断言类检查仍由命令执行。分身报告须经规划方复核后采纳（分身也会误报）。本机制为开发工具链用法，不属 T1 运行时物化（不违 agent-design 的 T1 禁令）。

## 施工记录

| # | 日期 | 规格 | 施工方模型 | 自由度分布 | 偏差（P0/P1） | 整改轮数 | 结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-09-06 | 012-2026-09-06-爬虫补强.md | flash（施工方窗口） | [A]×5 [B]×1 | P1×4（D1 现状事实出入/D2 双分支报错/D3 目录同步/D4 措辞近似——均规划方裁定接受） | 0 | 核验通过，已入库（c5f867b），待真机实测 |
| 2 | 2026-09-06 | 013-2026-09-06-多技能骨架切换.md | 规划方自施工（骨架切换为结构性迁移，未走外部分工） | [A]×6 [B]×1 | P1×3（D1 锚点行号过时/D2 验收算术/D3 旧路由残留——D3 规划方微修）＋独立分身审查 17 条（2 P0/6 P1/9 P2）全数核实修复 | 0 | 核验通过（含分身审查首跑实证），已入库，待真机实测 |
