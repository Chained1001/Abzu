# 挂账台账（open-items）

> 定位：全部未办事项的单点来源
> 纪律：每批核验收尾核对；销账须记销账依据（批号或 commit）；本文件只记**未办事项与触发条件**，不记过程叙事——AGENTS §6 禁者为过程叙事台账（施工日志／审核记录），本件性质为欠账清单。**旧源收口**：自本批起不再新增第二来源——README Roadmap／collab-log 记录行／规格开放项新产生的挂账一律登记入本文件；存量是否回填另行评估。

## 挂账清单

### 等裁决

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| S1 | 043/044 审查记录欠账是否回补 | 待另行评估（历史数据修复，非流程优化） | 待办 |
| S5 | 退出码是否承载预验器崩溃信号 | 作者裁定（须同批改三处载体） | 待办 |
| A6 | glossary 英文对应列语义弱化（model-invoked / user-invoked） | 后续批评议 | 待办 |
| A10 | 038 第四章【待作者校准】标记 | 作者校准 | 待办 |
| A13 | 044 四项规划方判读（阈值系数 1/3、免跑档含 3.5、配档依据披露、1.1 规格收集档） | 作者确认或改档 | 待办 |
| R6 | marketplace.json 元数据内容标准立法 | 发布前置 | 待办 |
| N20 | scan 域两脚本退出码语义与 `file-conventions.md` §脚本编写规范不一致：`setup-cdp-chrome.js` 参数缺失/非法与端口非法走 `exit 1` 且无用法说明（规范要求 2＋用法）、环境错误走 `1`（规范 `1` 为违规）、用户拒绝走 `2`（规范 `2` 为参数或环境错误）；`cdp-utils.js` 以 `exitCode = 2` 表 partial。**方向待裁决**：收口脚本 or 放宽规范 | 作者裁定方向后另批（域脚本改动，052 批禁动 `skills/`） | 待办 |

### 等域建设

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| R1 | 卷纲／正文／文风三域建设 | 各域立项 | 待办 |
| R2 | test-prompts 八场景全绿（050 批随 README 同步订正；原写「六场景」） | 各域建设后 | 待办 |
| R10 | 书目录共享语言文件立法（术语/设定/不变量三段式） | write 域真消费方检验 | 待办 |
| R11 | 正文域会话交接契约立法 | write 域建设时 | 待办 |
| R4 | skill 调用契约立法（输入校验/输出格式/失败返回） | 拆书域已裁定搁置，无期 | 待办 |
| G3 | outline 域 project-structure 规范 | outline 域建设（命名权威现旁落至设计文档 §5.3） | 待办 |
| G10 | test-prompts 场景 1-5 待启用契约 | 对应域建设后生效 | 待办 |
| A8 | 书项目脚手架生产能力重建 | 大纲域新设计落地 | 待办 |

### 等触发条件

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| R3 | v1.x 写作项目内 AGENTS.md 生成、示例项目 | v1.x 阶段 | 待办 |
| R5 | 系统性依赖声明清单（Node/pip/全局 CLI） | 任一域引入外部依赖时 | 待办 |
| R8 | junction 免手动部署；域级命令分发方案 | 多域上线后裁定 | 待办 |
| R12 | 发布前置四项（演示示例／最小 CI／插件市场提交／同类项目对比） | 开源发布时 | 待办 |
| G1 | 命令薄壳启用（`.claude/commands/abzu-{路由键}.md`） | 多域上线、分发方案裁定后 | 待办 |
| G2 | agent 文件启用（`.claude/agents/abzu-{角色名}.md`） | T1 触发 | 待办 |
| G6 | agent-design 骨架立法填充（`[T1-TODO]` 实测 2 处，**按内容指认**：命名与文件节的「目录」格、T1 触发时的填充清单节标题——051 批插入新节后原行号指针已漂移，故改按内容） | T1 触发 | 待办 |
| G7 | 安装预检（依赖版本前置报错） | 脚本依赖复杂化 | 待办 |
| G8 | architecture §七 四项挂账（粒度五次法则／季度评审节律／reflect 机制／精读 document-skills） | 经验积累或节律触发 | 待办 |
| G9 | 采风助手 T1 候选物化为实体 agent（051 批随术语单点同步：原写「采风员 … agent」） | T1 触发 | 待办 |
| G14 | 多人协作治理件（CONTRIBUTING / CODE_OF_CONDUCT / OWNERS） | 收到外部 PR 时 | 待办 |

### 等另批

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| S4 | 施工记录表拆列／hook 提示语／markdownlint 依赖 | 后续批次 | 待办 |
| S7 | 形态条补「后台 spawn」 | → 批 3（协议收口） | 待办 |
| C5 | 复审轮次与「整改轮数」列口径＋静态输出骨架与 `--verify` 契约上收协议条 | → 批 3（协议收口） | 待办 |
| C6 | 文件白名单与多批共存口径缺口（含 049 批的豁免①② 与非阻断级别——核验协议第一步「多出任何一个 = P0」字面与新工具口径并存） | → 批 3（协议收口） | 待办 |
| R9 | 内容轨加粗密度检查扩展至治理文档 | 阈值另行立法 | 待办 |
| A1 | 三评委 spawn 打磨循环（五期） | 分期施工 | 待办 |
| A2 | 方法论实萃取（挂载点占位） | 萃取批 | 待办 |
| A3 | 卷纲／细纲 schema | 二三期规格 | 待办 |
| A4 | referFile 七书路径现场核定 | 萃取批 | 待办 |
| A14 | 参考标尺「脚本数事实」实现 | skill 建设批 | 待办 |
| A15 | 045 三项候选（追踪档版本指针根治／`.gitignore` 补 `__pycache__`／预验器提取规则） | → 批 2（提取规则）+ 后续 | 待办 |
| N2 | file-conventions 规格行膨胀（四条规则同格，同列速查可比性下降）——下沉为表注 | → 批 4d（索引与词条补缺） | 待办 |
| N4 | 检查 A 的预测引入替换模拟（现存加法预测不计删除，整句替换型会高估） | 待出现误报实例后评估 | 待办 |
| N5 | 检查 B 的量词扩展（`条`／`项`／`处` 类计数因定义随文件类型而异未覆盖） | 待出现漏报后按类扩 | 待办 |
| N6 | `check_spec_assertions.py` 更名（现名只覆盖「断言」，静态阶段已超其字面） | 另案评估（现名被协议条与多份归档规格引用） | 待办 |
| N7 | 检查 B 分级只有「是否本批改动清单件」一维——他批未提交改动件被在制规格引用尺寸时会误判「计数不符」而阻断提交（与 C6 多批共存口径同族） | → 批 2（与 C6 同批处置） | 待办 |
| N8 | 检查 A 对「到位列与现状列同值」的反例哨兵型断言固定输出预警行（稀释信噪比）——判据加「到位＝现状则跳过」可消 | → 批 3（与 N11 同属表格感知族） | 待办 |
| N9 | 脚本头注「本工具退出码恒 0，仅呈报」主语为全工具，而工具现可退 1（静态计数不符、`--verify` 阻断）与 2（参数错误）——建议加限定词 | → 批 3（协议收口，与 S5 一并议） | 待办 |
| N11 | `[A]` 级「现文／替换为」成对逐字核对（需成对结构感知） | → 批 3（与 N8 同族） | 待办 |
| N19 | 检查 B 的第三个假阳性：把**行项编号**（如「MD034 行」「G1 行」）读成文件的「N 行」尺寸声称，并把该编号错配给同段出现的其它文件路径（052 批实测两例的源头均为在制规格自身的措辞，随该规格归档即消失——故是「在制期常驻」而非永驻；预警行内嵌的行数会随件增长失真，故本条目不记具体数字） | → 批 3（与 N8／N11 同属表格感知族） | 待办 |
| N14 | `release-and-versioning` 的版本兼容机制待新书项目设计落地后重写（现为退役声明） | 新书项目设计批 | 待办 |
| N15 | `architecture.md:15` 的 `tracking`／`手稿` 两个旧载体名待新设计对齐确认（050 批只改了 `project.md` 一处的承接名） | 新书项目设计批 | 待办 |
| N16 | 清单判定用子串匹配偏松——`check_counts()` 的「本批改动件」判定与 `--verify` 的「声明集」均把**仅被提及**的文件计入（050 批施工实测：声明集含 AGENTS.md、docs/product 等未改文件） | → 批 3（协议收口，与 C6 同族） | 待办 |
| N12 | 预验器脚本的文档与健壮性补遗（转义引号残差注记／依赖与前置段补不可审计类／check_content 非仓根调用） | → 批 3 | 待办 |
| N13 | 核验比对与静态输出的边界补遗（待验规格自身归档移动的自指噪声／「无发现」剩余可达情形改述） | → 批 3 | 待办 |
| N18 | `docs/product/大纲阶段设计文档.md` 的术语对照表（「采风员 → 采风助手」）左列语义失效——051 批把「采风助手」升为单点权威名后，左列仍标「内部术语」，读者会读成「内部术语叫采风员」 | 设计文档下次修订批（该件属大纲域真源，非本仓规范件） | 待办 |

### 等在途收尾

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| C1 | 031 场景 1 真机走查 | 随新版大纲场景走查 | 待办 |
| G11 | 真机走查存量两笔（029 拆书真测、031 场景 1） | 下次使用／走查时 | 待办 |
| S2 | 045 §二.8 锚点重锚 | 045 施工时（046 已作废，045 单独施工或并入后续） | 待办 |

### 纯遗漏

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| C8 | 施工记录满 30 条的 P1 成因回顾 | 已到期 → 补做产出落点待定（默认入 collab-log 头注） | 待办 |
| A9 | test-prompts 场景 1 重写（触发条件已达成；场景 1-5 断言另含 `project.md` 旧产物名，同属重写范围） | → 批 4d（索引与词条补缺） | 待办 |
| G4 | 未来守卫待办：check.sh 增 references 相对链接扫描 | 无归属 → 待认领 | 待办 |
| G5 | 产物模板三要素「现行正例待新批模板产生」（实况已有 5 件合格模板） | 表述与实况相反 → 随批 4 修正 | 待办 |

### 已销

| 编号 | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| b0-1 | collab-log 施工记录表重复 `#31` 行（序号 31→32→31 逆序重复） | —（已办结） | 已销（批 0，2026-09-12 删除） |
| b0-2 | 046 规格（前提全面失效：行号 +2 漂移、替换文本会回退 045 修复、A7 基线不可复现、crashes 期望 6/8 自相矛盾） | —（已办结） | 已销（批 0 作废归档为 `archive/046-…-已作废.md`；有效内容转批 2／批 3） |
| b0-3 | 施工记录表 12 行失效提交状态（行 21–24「组队提交计划待提交」、行 25–31「待作者提交确认」、行 32 红基线措辞） | —（已办结） | 已销（批 0 改记事实：8fe8ff3／7c57d25／各库 sha；与 C7 双行并存） |
| b0-4 | check.sh 红基线（046 两处 MD038） | —（已办结） | 已销（批 0 随 046 归档复绿，退出 0） |
| A11 | 039 施工后「未预验·核验时实跑」标记未销 | —（已办结） | 已销（该批已核验通过，collab-log 行 26） |
| A16 | 030 旧设计文档两项开放问题（断点契约、数据回流） | —（已办结） | 已销（承载件已随 036 批退役归档，问题随件失效） |
| C2 | 组队提交计划（032 → 豁免批 → 035 → 033 → 034） | —（已办结） | 已销（入 8fe8ff3／7c57d25） |
| C7 | 施工记录「待作者提交确认」存量 | 行 32（045）仍待提交 | 部分已销（行 25–31 已改记库 sha；余项与 S2 同批处置；与 `b0-3` 双行并存） |
| G13 | doc-budget 体系（登记表＋守卫） | —（已办结） | 已销（047 批处置：改条目纪律不建体系，依据见 AGENTS §6 doc-budget 条） |
| S3 | 预验器 GBK 解码参数修复 | —（已办结） | 已销（已随 045 落盘，`check_spec_assertions.py` 含 encoding/errors 与 stdout 重配置） |
| S8 | 046 待送审与产物审查派发 | —（已办结） | 已销（046 已作废，随件销账） |
| S9 | 046 内 crashes 期望值 6 与 8 不一致 | —（已办结） | 已销（046 已作废，随件销账） |
| C3 | 预验工具输出区分「命令不可用／退出非零」 | —（已办结） | 已销（049 批：返回码 126/127 逐条标注「命令不可用」，见 A11） |
| N1 | 条目 ≤ 400 字符 条的机械化守卫缺失（判据纯算术） | —（已办结） | 已销（049 批：check_content.py 新增第五段 + 计数基线常量） |
| N3 | `readonly()` 放行面收窄（bash／sh／node 与 `python -c` 现被判可执行） | —（已办结） | 已销（049 批：bash／sh／node／npx 改判不可审计，见 A5） |
| N10 | 静态阶段「无发现」判据收紧（有自噬预警时不得打「无发现」） | —（已办结） | 已销（049 批 §二.1.B 直接实施） |
| R7 | release-and-versioning §二「四处一致」补限定词 | —（已办结） | 已销（051 批：§二首句改「套件发布版本号…（不含治理文档头注 vX.Y）」） |
| C4 | 直引号成片存量处置 | —（已办结） | 已销（052 批：markdown-style §二.6 追认直引号为本仓常态、规范侧收口；存量不做批量改写——方向为「改规范侧追认实况」） |
