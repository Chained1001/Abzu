# 守卫缺口清单

> 定位：脚本检查／工具／守卫类（G 表）与协议／协作类（协表）已知缺口的唯一清单——债跟着主题走，不跟着时间走。**冷件**：只在涉及守卫（`scripts/check*.py`／`check.sh`）或复盘时读，不进每批必读件。
> 依据：原《测试与验收标准》§4 的 G 表与原《施工机制-附录》§二 的协表整块迁入（2026-09-17 开发侧重构）；**条目编号保留原号**——在制规格的条目编号引用与频次记录挂在原号上，不得重编。
> 清单归属：检查 H／J 的「清单全文」面含本件（`scripts/check_spec.py` 的 `LEDGER_FILES`）。行内旧出处（如《测试与验收标准》§N）按历史文本读——所引件已随重构溶解或合并，沿革见 `CHANGELOG`。

## 一、守卫与工具类缺口（G 表）

| 编号 | 缺口 | 状态/处置 | 触发条件 |
| --- | --- | --- | --- |
| G1 | 内容轨加粗密度检查扩展至治理文档（旧仓只扫 `skills/`，阈值须另行立法） | 计算方式已由原《文档写作标准》§一.3.8（现《文字与命名标准》§13）明确（按 `**` 标记出现次数；以表格与编号条目为主要结构者不计保底做法）；**阈值（行数 ÷ 3）未调整**——扩展至治理文档前须先决定阈值。**本批不实现**（093：`[3]` 段未含加粗密度——阈值未立法且仓内零**适用**目标件）；「入口型文档」无独立定义，扩展时须一并立法——并须同批给出『超阈存量件清单』：凡 `**` 数超阈的**现存件**逐件列出，使阈值调整时可一次性复测；清单缺件即「靠每批手记」，属本行未闭面。 | 阈值另行立法 |
| G2 | 计数重算的量词面：`行`／`字符` 已覆盖；`处` 于 072 批按类扩，计算方式限死为加粗标记计数（紧邻段须含「加粗」；数字前紧邻「加」／「增」的增量式不判）；`条`／`项`／`个` 仍未覆盖（计算方式随文件类型而异，不自动重算）；不言明加粗的「N 处 `**`」类措辞不判 | 072 批已按此触发扩第一类 | 待出现该类漏报后按类扩 |
| G4 | 计数重算分级只有「是否本批改动清单件」一维——他批未提交改动件被在制规格引用尺寸时会被算作「计数不符」（065 批后该类只呈报，不再阻断提交） | 无排期（065 批保留该一维：`check_counts()` 仍按「目标件是否出现在本批改动清单节」二分，本批不改） | |
| G8 | 清单判定用子串匹配偏松——「本批改动件」判定与核验比对的「声明集」均把仅被提及的文件计入 | 维持现状＋理由（065 批不动：「本批改动件」二分所依的子串匹配是本批规格明定维持的既有计算方式；核验比对的声明集本批改取改动清单表路径列 token，偏松面收窄一档） | |
| G9 | 脚本用法文本多载体同步风险（头注参数表／`usage()` 输出／域文档选项表）——属可机械化同步点 | | 出现同类脚本时 |
| G10 | `[A]` 逐字核对载体覆盖缺口（065 批后只核表格 `[A]` 行的引号块，无引号块的行降为候选呈报；内联载体不核）与归档件历史噪声 | | 扩展覆盖前 |
| G11 | 规格写作机械检查在非规格件上误报（该项已由 [4] 段枚举计算方式与工具内置的跳过名单覆盖）；同一行的嵌套反引号构造产出同文重复预警（检查 D 本批照搬，未动） | 维持现状＋理由（065 批：前半已覆盖——枚举计算方式排除 `施工机制.md`，工具再兜一道跳过名单；后半属检查 D 既有行为，本批不扩改动文件） | |
| G12 | 开发脚本无体积上限（体积纪律只约束 `SKILL.md`／references）——预算体系是否扩展到开发脚本 | | 若体积预算范围扩展 |
| G13 | 未来守卫待办：`check.sh` 增 references 相对链接扫描 | **仍待（本批不认领）**：G13 是**禁止型**扫描（不许出现相对 `.md` 链接），与实存型闭合扫描**异题** | |
| G14 | F2（零命中待复核）判定标准只认字面 token，路径型（含 `.`）／标点型 token 一律跳过该判 | | 觉漏报过度时评估「按载体形态分流」 |
| G15 | F2 的聚合期望句：072 批起子句数字个数 K 与断言数 M 相等时按命令序号配对取数（须每个数字各对一条命令）；K≠M 时分配型句不判、其余全同取值，而子句多值时不再静默——产出「零命中核对未判」候选行；残留＝数字序与命令序写反即静默误配（缓解＝断言写法第 9 条，见 `施工机制-附录` §三） | 加「数字序与命令序可疑不一致」提示（072 批已按原触发响应） | 出现误配实例时 |
| G16 | 工具头注未枚举不可审计类（判定标准只在内联 docstring） | **本批处置（075 F1⑤／076 F12 补枚举）**：头注已枚举六类；原触发条件已由 076 F12 履行 | |
| G17 | 规格内「某文档 §N」引用的节号未自动化核对（现只人工判断轨） | **候选扩展位，本批不实现**（准入依据见 `AGENTS.md` §四：事故出身 ＋ 可机械化为零外部依赖断言）；2026-09-17 重构批已实撞多起悬空 §引用（§八 模板类），事故出身已足 | 触发＝「引用闭合」类检查器扩展时 |
| G18 | `check.sh` 收尾汇总的段计数对头注段行形态破坏无容错（某行 `# [N]` 被误改即该段静默少计、`共 N 段` 无信号——078 批 F5 的已知潜伏面，产物审查 P2-3 登记） | **候选扩展位，本批不实现** | 触发＝头注段行被破坏致「共 N 段」少计的实撞出现时 |
| G19 | 检查 F 的取词面＝顿号分隔中文词组，**通用词**（断言／数字／位置类）易误报 | 按类收窄取集源（**`107` 批同族实例**：`§九 成本字段` 判定标准按「行」判出 13 条候选、逐条复核 0 条真阳——已改按「节」判，现行四件 0 条） | 触发＝上线后出现大批「人工逐条判为预期」的候选时 |
| G20 | 守卫与工具文档的**批次叙事**未收敛（内联注释与运行态措辞仍有批次引用，与 `CHANGELOG`／规格形成第二叙事源） | **部分处置（090 已收敛 docstring 与 `[2]` 段注释）**；残余在册 | 下次触发＝新增守卫件时同计算方式书写 |
| G21 | 标准件的**数字来源与依据强度**（限速值未标〔自定〕；以「本机实测」为据的条款）（外部评审 P2-6／P2-7） | **本批处置（090 F3／F4）**：残余＝无 | 下次触发＝出现同类「数字未标来源」缺口时 |
| G22 | 规格头注「可自定程度分布」与 §二 改动清单的**可自定程度列**不自洽（本仓**第四次**复发）——该族**可机械化**（两侧都是可数结构） | **本批处置（F22）**（第五次复发已到：本批自查即其一）（093 F22 已实现；095 F4 同步） | |
| G23 | 内容轨「维护出处」标注检查的判定标准依赖正则近似（「**见／按／引／依照／据** ＋ 反引号治理件名」形态），无标注的等价写法（如裸文件名字符串）漏报 | 按类扩取集 | 出现漏报实例时 |
| G24 | 检查 C 的「嵌套引号取目标文本」近似：`_row_target_text()` 被内层引号截断（实测 `F17` → `维护出处`、`F13` → `改了什么 ＋ 核验结论`），致检查 A 的中性汇总误计、检查 C 核错文本 | | 修 `_quoted_blocks()` 的嵌套切分，或出现误报实例 |
| G25 | 检查 G 的两型误报：**非表格形态**改动清单（无别名列，实测 `archive/085`／`086` 头注分布正确却被判「清单 0 行」）与**头注「混合×k」写在续行**（11 条候选中已确证 2 条误报）；其 docstring 亦把该情形断言为「真不自洽」，须同改 | 按「降级为「不可判」明示行」改造 | 同族误报再现时 |
| G26 | `scripts/check_spec.py` 的 `BAN_EXCUSE_MARKS` 豁免词表**无「豁免」**，而判一的**候选文案**作「禁改文件**未豁免**」——判定标准与文案不一致（写「豁免」不被识别；`098`／`099`／本批各白折腾一轮） | | 触发＝下次涉及 `BAN_EXCUSE_MARKS` 判定标准面时，或「按禁改文件写法豁免却不被识别」再现时 |
| G27 | 检查 A 对**转义竖线 token**（如 `结局形态⏐感情线处理`）按**字面**取数（`check_swallow()` 的现行字面计数）→「现行」计数**恒 0**（目标文本含字面 `⏐` 时除外；工具伪零；`098` 施工期复现）。**并注**：同族＝**转义反引号 token** 亦不解反转义 | | 触发＝下次涉及 `_assert_refs()` 的 token 反转义计算方式时，或该伪零致漏判的实例出现时 |
| G28 | 在制规格的「九、审查记录」节存在性（缺失或留占位符即报候选） | | 触发＝检查 J 上线后首个漏检实例 |
| G29 | 检查 G（可自定程度分布核对）**不认 `[C]` 档**：`fmt()` 只统计 `[A]`／`[B]`／混合三值，头注写 `[C]×n` 时既不计数也不报不符——`[C]` 已定义，故该档在分布核对里是盲区（`102` 批实测） | **本批处置（`105` F1 已补 `[C]` 计数与不符判定标准）** | |
| G30 | 检查 G **不核头注的路径自述**（头注自称「小改流程」而 §二 含 `[B]` 行时无任何检查报） | **已标记已解决（2026-09-17 随「快」行退役）** | |
| G31 | 检查 G 的 `[C]` 计数对**组合标记行**不对称：`[A]`＋`[C]`／`[B]`＋`[C]` 行按分支序只计前者；头注**缺写** `[C]` 时该档不参与核对（现存 48 件无一命中，属潜在面） | | 触发＝首个组合标记行实例出现，或作者裁定补组合行头注申报计算方式 |
| G32 | **归档动作无机械检查关**：各标准件归档脚本检查点状态均＝待建；`--verify` 只核 `CHANGELOG` 条目与 archive 文件存在，且 `check_spec.py` 自陈不进 `check.sh` 提交前自动检查——故归档这一步既无强制、也无判定标准 | **本批处置（`107` F1／F3／F4）** | |
| G33 | `check_spec.py` **无仓内回归测试件**：判定标准只由 `.tmp/` 测试样例验证、用完即删（红线 2），判定标准坏了无人知——规划方实测归档件命中 7 件（件号＝`077`／`078`／`080`／`083`／`085`／`086`／`087`） | | 触发＝下次涉及该判定标准面 |
| G34 | **检查 A 与检查 C 对「目标文本」是两套取法**（检查 A 取全部引号块、检查 C 取末标记之后的块），两计算方式并存未统一 | | 触发＝下次涉及该判定标准面 |
| G35 | **`check.sh` 段序／段名三载体不符**（头注、原《测试与验收标准》§1.1 表、`AGENTS.md` §七各一份，改一处即漂） | **重构批收敛（2026-09-17）**：原《测试与验收标准》§1.1 现状表已随该件溶解删除——段序与段现状唯一出处＝`check.sh` 头注，`AGENTS.md` §七 只留粗粒度指认；本行留作频次记录 | 触发＝下次涉及该判定标准面 |
| G36 | **`check_counts` 类阈值与 `--quiet` 分档长期悬置** | | 触发＝下次涉及该判定标准面 |
| G37 | 检查 C（`check_pairs()`）对**加粗的归一化不对称**：`[A]` 目标文本含 `**加粗**` 时，提取的比对串已去加粗标记、却与含加粗的原件比对 ⇒ 恒判「未落实或已不符」——`107` 批实测稳态 9 条中至少 8 条内容已逐字落地，属**永久假阳** | **本批处置（`109` F1）** | 触发＝下次涉及 `check_pairs()` 取词面时择一修 |
| G38 | 检查 A **位置格只写件数**时命中门失效：该行的定位判定标准不成立，却既不报候选、也不计入「零命中待复核」——靠人跑才兜住；与 `G10` 同族但不同面 | | 触发＝位置格判定标准被涉及时 |
| G39 | **`check.sh` 的全绿收尾行把「跳过」计成「已检」**：末行打的是写死的段名清单，而段级跳过已由 `_seg_summary()` 正确汇总在上一行——读者只看末行即被误导；与 `G18` 互补：`G18` 管少计，本条管跳过被算作已检 | | 触发＝全绿收尾行的段名清单被涉及时 |
| G40 | **目标件在扫描过程中被删／不可读时工具会崩**：`check_spec.py` 读目标件无「消失即跳过」保底做法，异常路径直接抛栈（`075`／`076`／`077` 三批各记一次，裁定「非真实场景」后未补防护） | **本批处置（2026-09-17 开发侧重构）**：`_read_target()` 已加 OSError 保底做法（返回空串、断言降为候选面），重构批量删除标准件后该场景真实出现 | |
| G41 | **三项「规范放开即须同步」无写入位置**（合并一条，同族）：① `~~~` 围栏（`extract()` 只认三反引号）② `MD_LINK` 带 title 的链接形态 ③ `_dir_split()` 的计数语义 | | 触发＝上述任一形态的规范被放开时 |
| G42 | **「要不要派产物审查」的派发无机械检查关**：现全凭规划方读 `施工机制` §五 自判。**判定标准候选**：头注无「路径判定」行、或路径判定行不可解析时报「判定流程未写」候选 | | 触发＝下次涉及检查 G 的可自定程度分布判定标准面 |
| G43 | **§九 成本行的分段标识无机械判定标准**：`108` 批实测出现过与任何段都不符的单一合计数；本会话 `R` 记错 4 例（四例四错、逐例可核）⇒ 靠人记必错 | **条文已立（`109` F4）／判定标准仍待** | |
| G44 | 用词检查只网清单已列词形；单字缩写家族（用词规则第 3 条）与清单外同义写法无判定标准 | | |
| G45 | **机架通用性无检查**：开发侧定位为通用机架，机架件混入未标注「（本项目＝…）」的产品专有事实时无判据（2026-09-17 解耦批靠人工扫描完成） | 候选扩展位，本批不实现——判据候选＝机架件出现标注之外的产品专名（以产品层《术语表》词条为词表，扫 AGENTS／标准件／施工机制／scripts） | 触发＝首次把机架复用于第二个项目时 |

> G3/G5/G6/G7 已标记已解决（065 批处置，见该批规格 F1／F4／F5）。**G30 于 2026-09-17 标记已解决**——其判定标准面「头注路径自述」随「快」行退役而消失（机制成本研究：0/14 批使用）。

## 二、协议与协作类缺口（协表）

| 编号 | 缺口 | 触发条件 |
| --- | --- | --- |
| 协1 | 施工开工说明不设模板（附录只给必带四项，见 `施工机制-附录` §三「施工开工说明结构」），旧仓「提示词一律从模板派生」条随之失效——是否补模板待定 | 出现开工说明漏项事故时 |
| 协2 | 归档件的审查记录节标题编号不统一（**9/41 件**不在 §九：`059`／`060` 编 §七、`061`–`065` 编 §八、`071` 编 §十）——**归档只读，不回溯改**；F4① 已立规，新批按规落 | 出现第三处编号形态时评估是否加机械检查关 |
