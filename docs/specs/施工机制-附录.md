# 施工机制 · 附录（冷路径）

> 定位：**冷路径附录**——`施工机制.md` 的搬迁区：反模式清单全文／缺口表／三份模板全文／立例依据。**规则真源仍是 `施工机制.md`**；本件按需读（写规格、复盘时），不进热路径。
> 台账归属：`检查 H／J` 的「台账全文」面含本件（`scripts/check_spec.py` 的 `LEDGER_FILES`）。

## 一、反模式清单（全文，事故回填）

> 条号与 `施工机制` §六 的一行式索引一一对应；「事故出处」列的数字即频次。

| # | 错误做法 | 正确方式 | 事故出处 |
| --- | --- | --- | --- |
| 1 | 为过检改断言／改守卫数字 | 先判因；文档数字与实测对齐 | 继承 |
| 2 | 删改机制留文档残留（或反之） | 当次全仓 grep 清理全部引用；grep 词须含概念近义词，不止原句 | 继承（旧仓 032 立法只搜原句，姊妹文档「触发关键词」措辞漏网至次日审计） |
| 3 | 模板／文档写「格式同上、参照上文」式互引 | 显式内嵌完整内容或指向真实路径 | 继承 |
| 4 | 同一规范复制多份维护 | 单一真源 + 引用，发现第二来源即收口 | 继承 |
| 5 | CI／守卫里跑 LLM 或联网 | 场景走查人工判断 + 脚本断言；守卫零外部依赖 | 继承 |
| 6 | 验收命令的退出码经管道被吞（`cmd \| tail` 后取 `$?` 是 `tail` 的） | 退出码不经管道直接取；必须接管道时取 `PIPESTATUS[0]` | 2026-09-05 lint 体检首轮误判违例数 |
| 7 | 给带 shebang 的脚本插入头部注释（把 `#!` 挤离首行） | 注释插在 shebang 之后；脚本改动后必跑语法检查（`node --check`） | 2026-09-05 移植审计发现 6 脚本语法全坏 |
| 8 | `git add -A` 后直接提交，不核对暂存内容——提交标签与实际内容不符 | 提交前必看 `git status`／`git diff --stat` 对照预期改动清单 | 2026-09-06 核验协议提交扫入施工方未验收改动 |
| 9 | 纪律靠自觉不靠机制——写完纪律条文就以为会自动执行 | 可机械化的纪律固化为工具（`check.sh`／hook／脚本），不可机械化的写明触发条件 | 2026-09-06 全天多次「该查没查／该走规格没走」（shebang、治理同步、外审未转规格）**＋ 2026-09-15 两笔自有事故**：`规格写作标准` §8 新规未回扫存量预立法标记（8 处＝`095` 口径含定义句；`096` 订正为 **7 处标记**）、`施工机制` §八 交底装载路径补丁未回填族——**均为 `#9` 的实例化** |
| 10 | 在源仓库内运行分发／安装类工具（安装器把 `skills/` 符号链接化） | 分发工具只在目标项目目录运行；仓库加真目录守卫（`check.sh` [0]）拦截 | 2026-09-07 首次真机安装实测（旧仓规格 027） |
| 11 | 把施工／整改指令发给错误对象（未核对目标 id 的角色与可续性） | 发送前核对目标 id 的角色与可续性（§一 subagent 纪律） | 2026-09-11 旧仓 044 批整改指令误发复审 subagent（即时 interrupt + 撤回，该 subagent 回报未改动、零损失） |
| 12 | 以为改了、实际没改——修订脚本失败未检查，且未回读核验文件实际状态 | 修订类脚本三段式（全量校验 → 统一写盘 → 回读核验），改后逐条 `grep`／read 核验目标文本确在文件中 | 2026-09-14 076 窄域复核：两次 `.tmp` 脚本末尾报错中止在写盘之前，12 处改动零落地，审查方逐字读文件发现 |
| 13 | 在反引号片段内写首尾带空格的串 | 改述为文字（如「井号加一空格」），或用无边缘空格的等价写法；**且经 `scripts/patch_file.py` 落盘的 `.md` 由形态闸在写盘前拦截**（2026-09-16 起，该工具四段式第②段——`md` 目标件对**待写入全文**跑 lint，不过即**零写盘**） | 2026-09-15 088–089 批（本会话同型六次，MD038）；**2026-09-16 `100` 批规格内同型三次**（每次皆因「写完隔若干步才跑门禁」而检测过晚——直接促成 `patch_file.py` 加形态闸） |
| 14 | 在 markdown 表格单元内写裸竖线（含反引号内；含把整行表格塞进单元） | 单元内竖线改用全角占位，或改述为文字（如「半角竖线」） | 2026-09-15 089 批两次（MD060） |
| 15 | 以「锚点行前插」的方式增补有序列表项（新项落到锚点项之前，序号顶翻） | 锚点取**插入点之前**的那一项，或插入后逐项核对序号 | 2026-09-15 088 §八 15／16 与 089 §八 6／7（MD029，两次同型） |
| 16 | 在表格单元内写内联 HTML（如换行标签） | 改述为分行列表或表后独立段落 | 2026-09-15 088 批（F1／F2 单元格各一次，MD033） |
| 17 | 处理中文时用非仓内临时路径或按字节切的工具（`/tmp` 在 Git Bash 与 Windows python 之间不互通；`cut -c`／`rev` 对中文按字节切） | 临时件一律落**仓内** `.tmp/`；中文一律用 python 处理 | 2026-09-15 089 批（各一次：读输出文件失败、控制台乱码） |
| 18 | 凭记忆写 Edit／replace 的匹配键（未先取原文、批量替换不带计数断言） | 改前先 `grep` 取原文；批量脚本**每个替换带计数断言**（不为 1 即中止） | 2026-09-15 088–089 批（本会话 ≥4 次；089 一次匹配到 2 处，靠计数断言写盘前中止、零误改） |
| 19 | 用截断输出或**放宽后的判据**下结论／登记「属预期」 | 结论基于全量输出；登记「属预期」须附**原样**取证（不得先放宽判据再取证） | 085 窄域复审（截断输出判「无标尺字样」而实含）＋ 2026-09-15 089 批（V-01 伴生：真因被放宽取证盖住） |
| **20** | 以「手写临时脚本 ＋ 凭记忆写的匹配键」改仓内文本（中止信息只有计数、须再猜一轮） | 改用 `scripts/patch_file.py`：锚点由磁盘校验、命中 0 或 >1 即中止并打印实际命中行（**本行取代 #18 的手写脚本路径**；#18 保留为事故记录——防两行并置产生相反指引） | 2026-09-15 089–090 批：同型中止三次（089 内该串命中 2 行；090 两次 `count=0`） |
| 21 | 改一处、忘了它使自家断言／数字失效 | 改动落地后逐条重算受影响的断言与计数——含本批新增的样本串与新增链接数；**同一事实的计数写法常不一（`件`／`份`／`张`／`行`）**，重测时按**数字**检索全部词形、不止已想到的那一种，并**优先去掉数字**（断根）而非改数 | `092` 窄域 N-1、`092` 窄域复审·补修面 P1-1 链接数 175→176、`094` 成稿 P0-1 断言 4-1 期望 4→5——**三次同族**；**＋ `G22` 族第五次复发**（`测试与验收标准` §4 `G22`：规格头注「自由度分布」与 §二 清单不自洽；`068`／`069`／`090`／`093` 已有记录；**本批无 G22 实例**——检查 G 汇总实测「不符 0 项」✓）——**本族无独立反模式行，并入本行**；**`107` 批施工期**：窄域审把 F8 由一处扩为两处、断言 6 的期望未同批同步（1→2），施工偏差 P1-1 报出后方订正；**`109` 批产物审查 P1-①**：`#33` 行频次由 6 改 7 时**未同批重算共变面**——描述列分项「规划方 2」应改 3（须＝合计 7）、事故出处列区间末点应改 `109`，两处构成句与频次自相抵（`#23` 同面；已订正并加断言 5f／5g）；**`111` 闸3 P1-1**：新建第 12 件标准后 `文档写作标准:54`／`:57` 的「11 张机检锚点表」漏改——规划方只 grep 了 `11 件标准`／`11 份标准` 两种词形，同事实的第三种量词漏网（已改「各标准件 §三 的机检锚点表（每件 1 张）」断根）；**＋ `111` 闸2**：计数类断言以 `check.sh` 输出为计数对象时，`[4]` 段会把规格里的断言字面**回显**成诊断行，`grep -c` 不锚定行首即**自计一次**（`111` 断言 18／19 首跑恒得 2） |
| 22 | 同载体多副本漏改 | 改一处枚举／判据前先 `grep` 该形态的**全部载体**，同批并改 | `089` F9、`093` P1-3 九行状态收口、`095` 成稿 P1-1 同件第二处枚举／P1-2 改动清单漏 F 行、2026-09-16 `107` 前置审计：`SKILL.md` 与 workflow 里**同一条义务句写了两遍**、未定单一真源（同族归档 3 件：`094`／`095`／`098`） |
| 23 | 文档里的状态句与计数句随实现漂移 | 状态句**指真源**（不写死「待建／已实现」）、计数句随批重测 | `093` P1-5「20 件全部超阈」实为 31 件／`095` P1-1「`[3]` 待建」而 `[3]` 已实现 |
| 24 | 工具判据遇自家产物即失效 | 判据的样本来源与判据实现分离——转义行、括注复述旧值、嵌套引号都会让判据读错自家文本 | `093` 的 `\|` 盲区（检查 C 静默跳过含转义竖线的自家样本行）、`093`／`094` **批头注**括注复述旧值被检查 G 读作声明值；**`107` 批 F14 判据自引**：§九 记录行含「成稿审查／产物审查」字样枚举 ⇒ 被自家判据命中（该批已改按节判、并把过报实例记 `测试与验收标准` §4 `G19`）；**`109` 批整改期新实例**：`检查 J` 的档位解析以（档位词 ＋ 至多 6 个非数字字符 ＋ 数字）取数，§九 散文里的 `P0-1` 因此被读成档位值，同句引用的被删占位行**裸计数声明**亦被扫作规格声明 ⇒ 报 `N 与档位不符 1 条`、`有预警` 由 19 涨至 20。**处置**＝写作侧改散文转述（`109` 整改期已落、复跑不符 0）；工具侧口径未改 |
| 25 | 自检与自评同源 | **不得以自己写的检查断言自己的成品并据此宣布「无问题」**——汇报全绿须**声明已检范围**（既有条文明文，`施工机制` §四 边界声明），用语止于「已检 X／未检 Y」，不出现「没问题」类总评 | `092`–`095` 四批的独立上下文审查各挑出语义级问题（逐批可核） |
| 26 | 拿世界知识的浅层直觉当设计判据（如见作者同选「轻松」与「暗黑」即判「两者矛盾」并据此劝删） | 先查该张力在**既有设计里有无承载层**（分层归属：基调的底色交织层 ＋ 卷纲域的分卷情绪弧线）；有承载层则张力是常态、判据不成立，**不得**据「矛盾」劝改 | 2026-09-15 真机走查 1.1 段三 基调（实机与规划方同型误判；作者原话〔逐字〕：「llm自认为悲壮／史诗厚重／暗黑和爽快、解压、搞笑沙雕天生矛盾，这就是很浅显的理解，长篇作品往往是各种基调和底色交织的」（出处＝2026-09-16 会话；**本条曾把转述当引文写入，经 2026-09-16 审查发现后改为逐字引**）） |
| 27 | 代码跨度内容以反斜杠收尾——收尾反引号被转义，跨度误配对、吞掉后文 | 跨度内写路径时**不留尾反斜杠**，或改述为文字；含路径的长跨度写完即跑 `bash scripts/check.sh` 核对 `[4]` 段（**判据已双重机械化**：① 检查 D ② 会报「代码跨度边缘空格」——**限于在制规格**；② `patch_file.py` 的形态闸在**写盘前**拦任意 `.md`（2026-09-16 加，见 `#13`）） | 2026-09-15 `094` 走查记录回填：Windows 路径尾反斜杠令检查 D ② 报 3 条预警、后文反引号全错配（已修实例；同族见 `#13`） |
| 28 | 计数类发现不附实测命令与单位；同一张表的数不同源同口径；同一事实换工具／换基线复量不一致即下结论 | 计数类发现须附**实测命令＋单位**；同一张表的数须**同源同口径**；同一事实换工具／换基线复量不一致时，先判工具口径再下结论；**凡称『常驻面／归档件／守卫面』者，须同时给件集判据（glob 或清单），否则量出的数不可比**；**因果claim须先做变量分离**——同一现象有两个候选成因时（如「加行后 lint 红」＝新行格数错 **与** 相邻行畸形两种可能），须**逐一改单一变量实跑对照**再下结论，禁把相关当因果（2026-09-16 `102` 施工期两度误诊：先误判 G27 为唯一真因、后误判「与 G27 无关」，正解＝两条件同时满足才触发）。 | 2026-09-16 外审三轮：① UTF-16 码元与字节混标（doc-budget 假触发）；② `Measure-Object -Line` 与 `ReadAllLines` 行数不一致（1401 vs 1512）；③ 换基线 commit 不声明（`578815a` 误作写入时点）——单位／工具／基线三个失效面；**＋ 2026-09-16 外审三轮报告复核**：④『常驻面』件集口径未声明（13 件 79,601 字符 vs 误含归档 42 件的 187,385 字符）；⑤ `git add -N` 基线用 `grep -c` 行数口径记作 1、实为出现 2 次。 |
| 29 | 规格头注「自由度分布」凭**记忆**填（按自己以为写了几条算），未按 §二 改动清单**逐行实算** | 头注分布须**由清单实跑得出**：写完改动清单后，先跑 `check_spec.py --static` 的检查 G，再据其「清单」侧计数填头注；改清单后同批重算**（已机械化：跑 `bash scripts/check.sh`，`检查 G 汇总` 报「不符 N 项」且 N≥1 即命中本行——该汇总只对在制规格打印，口径同 `#27`）** | 2026-09-16 同族三次（`102` 批两次：头注 12 与实际 12／13 往复；`103` 批一次：头注写 15、清单 13） —— **三次均由检查 G 抓出** |
| 30 | **只读了一小段代码就断言「此处缺某检查」**——把**已修的缺陷当成未修**，据此立了一个**无标的的改动项**（`104` F8 声称 `static_report()` 的格数不符行「只 print、未并入 `extra`」） | 断言「某处缺 X」之前，**先全件 grep 该形态**（此处 grep `extra +=` 即得 `:1504`／`:1526` 两处，后者正是该检查）并**读相邻注释**（`:1517-1519` 自陈「`101` 批 F26 已修……修前系裸 print……本批订正」）；判据来源须是**全件检索**，不是局部阅读 | 2026-09-16 `104` 批成稿审查 P0-3（锚点⑧失实、F8 无标的：所声称的缺陷已由 `101` 批 F26 修毕并留注释）；同族＝`#19`（放宽判据下结论）／`#25`（自检同源） |
| 31 | **靠头注自述取得流程豁免**——把「本批走快路径（免成稿审查与产物审查）」写在规格头注，而 §二 里明明留着 `[B]`（按方向自定实现）项；豁免条件写的是「改动面全部是逐字替换」，**这句话由规划方自己写、无人核** | 头注每句自述**须指认核对它的机械检查**；**路径自述由检查 E 判四核**（按 §二 清单**实算档位**并与头注「路径判定」行比对，不符即出候选——`106` 立守卫、2026-09-17 改为**机器实算**）；指认不出守卫的自述，照主线执行；**结构性跳过同理**——守卫因载有清单／真源而**必须跳过某件**（如用词检查跳过载清单的 `用词标准`）时，该件正文即成盲区：须在件内声明「本件正文不在自检面，由人工审查兜」，并在其 §三 机检锚点表登记该行的状态为「无脚本检查」 | 2026-09-16 `105` 批（自称「快路径」而 F1 标 `[B]`，两轮审查被免、一路绿灯至提交，规划方自查时才发现；见该批 §九）；同族＝`#9`（纪律靠自觉不靠机制）／`#19`（放宽判据下结论）；**`111` 闸3 P1-2**：`用词标准` **自身正文**违自列清单 **12 行／13 处**（守卫按设计跳过该件 ⇒ 永不报出）——**唯一出处件恰是唯一不可见件**；已逐处改齐并在 §一.3 声明「模板固定节名除外」，模板节名的统一改名归标准件迁移批 |
| 32 | **把「回写面」写进禁止面**——规格 §五 禁改某件，而 `施工机制` §四 的守卫变更回写清单恰要求同批回写该件（`106` 实测：§五 列禁改 `scripts/check.sh`，回写清单 ③ 正是它 ⇒ 施工方照禁改面守法、回写必然漏；产物审查 P1-1 抓出） | 写禁止面之前**先对 `施工机制` §四 回写清单反查一遍**；命中回写面者**须显式豁免并写明范围**（只放行该面，不放行整件） | 2026-09-16 `106` 批产物审查 P1-1（已按 F9 补回写、F13 解除禁改）；同族＝`#22`（同载体多副本漏改）／`#24`（判据遇自家产物失效）——本批 P1-2／P1-3 各犯一次；**`109` 批三实例**：F3 判据面（`_out_of_scope_*`）／`施工机制:75` 载体／模块 docstring「维护入口」段——三者均系 §四 回写清单所要求却落在 §五 放行面之外，经产物审查 P2-② 与核验期 O1 抓出后各扩豁免面（现放行六处） |
| 33 | **把「现状侧」旧文写成引号块**——`[A]`／`[B]` 行的「改动（现状 → 目标）」列里，把**现状侧**的旧文用引号块写出（而非散文转述）。后果：`检查 A`／`检查 C` 把该引号块当**目标侧**逐字核对，旧文一经替换即**恒报「未落实或已漂移」**，成为**永久假阳**（本会话同型 **7 次**：规划方 **3**／起草 fork 1／施工方 1／整改方 2；`107` 批实测一次即留下 3 条不会消的候选） | `[A]`／`[B]` 行的引号块**只放目标侧**文本；现状侧旧文一律**散文转述**，**引号块内不写旧值**（「现文以…作结」「现文只讲…」式描述可用，**不得**把旧句整段括起） | 2026-09-16 本会话同型 **7 次**（首见 `105` 批 §九 整改、末见 `109` 批 F4 行）。**与 `#24`／`#30` 不同族**：本条是**写作侧把两侧混写**，`#24` 是**判据读自家产物**、`#30` 是**只读一小段即断言**；律的载体＝`规格写作标准` §5（**尚未立**；`106` §八 7 是**开放项**，非已立之律）。**已部分机械化**：`检查 C` 对未命中目标件的引号块到改动面外件集检索，命中者文案追加**疑现状侧引文**成因括注（`109` F3）；**律本身仍未立**（`规格写作标准` §5）；**`109` 批第 7 次**：规划方 §二 F4 行把现状侧旧文与目标句写进同一对引号块（施工期由 `检查 C` 残余行抓出、本批已改述） |

## 二、已知缺口（协议／协作类，全文）

### 已知缺口（协议／协作类）

> 落点分工：机检／工具类缺口落 `测试与验收标准` §4，规范文本类落各标准件的待收口注（现无存量），协议／协作类落本节。触发条件到来时须核对并更新（当批开放项核对，见 §三）。

| 编号 | 缺口 | 触发条件 |
| --- | --- | --- |
| 协1 | 施工交底不设模板（本文件只给必带七项，见 §八），旧仓「提示词一律从模板派生」条随之失效——是否补模板待定 | 出现交底漏项事故时 |
| 协2 | 归档件的审查记录节标题编号不统一（**9/41 件**不在 §九：`059`／`060` 编 §七、`061`–`065` 编 §八、`071` 编 §十）——**归档只读，不回溯改**；F4① 已立规，新批按规落 | 出现第三处编号形态时评估是否加机械闸 |

## 三、模板全文（规格／参考研究裁定／核验请求）

## 八、模板（规格／核验请求／参考研究裁定，三份）

**可选 tasks 层 `edits.json`（T5b）**：格式即 `scripts/patch_file.py --batch` 的入参数组——`[{"path": "docs/x.md", "at": "锚片段", "set": "新文本"}, …]`；施工方一条命令落盘（四段式内置）。规格 §二 只写「改哪件、改什么方向」，逐字文本住 JSON。
规格模板（逐字可复用）：

```markdown
# {NNN}-{YYYY-MM-DD}-{中文主题}

> 定位：{一句话说本批解决什么}
> 路径判定：{主线／小改}（机械实算见检查 E 判四）
> 自由度分布：{如 [A]×3 [B]×1}
> 必读输入：{本批需要读的既有文件与节；不涉业务写「无」}
> 施工形态：{subagent／外部窗口}（{模型标识}）

## 一、现状锚点（现场核过）

- {文件}:{行}：现文「{原文}」；实测计数 {N}（命令：{grep -c 类命令}）。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| 1 | {路径}:{行} | {现状} → {目标} | [A] | {理由／来源} |

## 三、验收断言

1. 命令型：`{可直接执行的命令}` → 期望 {值}。
2. 走查型：{做法} → 通过判据 {判据}。
3. 过程产物：验收通过后规格移入 `docs/specs/archive/`（**移入前须逐条过开放项与 P0／P1 建议、判是否沉淀，见 §三**）；`CHANGELOG` 条目已落。

## 四、规格自审（五条）

1. 锚点现场核过：{命令与输出摘要}
2. 断言实跑过（与「替换为」侧文本不互斥）：{命令与输出摘要}
3. 禁止事项已写明：见 §五
4. **范围与期望同源**——断言若建立在某段范围（文件行段／节／清单）之上，其期望值须由**该范围**实跑得出（范围与期望取自不同侧即失守）。实例：088 成稿审查 F-02——迁移范围写 `:14`–`:52`（不含父标题 `:12`），而期望 4 是按**含** `:12` 的源件算的，施工后必红。
5. **自述有守卫**——头注每句自述指认核对它的机械检查；指认不出者不生效、照主线执行（**射程**：只管本该有机械检查而没写的自述；真源明文交给作者或人判轨的豁免不在此列）。{逐句指认结果}

## 五、禁止事项

- 禁止触碰：{文件／目录清单}
- 禁止动作：{如联网采集、改动面外文件}

## 六、被否决的替代方案（可选）

- {方案} → {否决原因：约束／成本／失败模式}（无方案比较则整节省略，不留空节）

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |

- 汇总：`按条计 n 条：已履行 a｜作废 b｜本批处置 c｜仍待 d｜待作者 e ＝ n`

## 八、开放项

- {当批修不完的欠账 → 落点}

## 九、审查记录

- {日期}｜{成稿审查／产物审查}｜findings 共 {N}：P0:{a} P1:{b} P2:{c}｜结论 {通过／修订后通过／裁决建议}｜工具往返数 {R}｜周期时长 {T}（`R`＝本批规划方会话的工具调用总次数，含施工与审查 subagent 自述数之和，核验期由规划方汇总，**须分段列**（形如 `施工段 {n}｜{审查名}段 {n}｜…｜合计 {N}`）——**只写单一合计＝失据**；数值自洽与全批汇总仍由规划方人工核（见 §六 `#21` 行）。`T`＝本批首条改动至提交的墙钟时长，人工填写、量级即可：`<1h`／`1-4h`／`>4h`。）
```

规格模板头注五行：定位／路径判定／自由度分布／必读输入／施工形态；施工形态字段由规划方在写规格时填默认形态（subagent）——外部窗口形态下模型标识由作者提供，实际变更同批订正。**必读输入按「文件:节」点名**（不得只写整件）；交底与施工默认**只读点名节**，未点名的节不读——**整件点名（如 `AGENTS.md`）即读整件**；装载量是每个 subagent 的成本主项（**2026-09-17 复算，字符口径**：以 `109` 为样本，交底点名面**上限 ≈ 10.7 万字符/实例**——规格全文 3.2 万＋`施工机制` 四节 1.6 万＋`测试与验收标准` 两节 1.2 万＋`check_spec.py` 五处面 2.0 万＋`AGENTS.md` 0.3 万；原「088 实测 190 KB」系**字节**口径、不可比，故订正）。**本条同辖审查与核验位**——成稿审查／产物审查／单维核对的交底同按『文件:节』点名，未点名的节不读；审查方产出须含『**所读文件清单**』（读了哪些件与节），供规划方核装载面与成本。**规格自身亦按节点名**：施工默认读 `§二`／`§三`／`§五`，`§一` 按需、`§九` 不读；`AGENTS.md` 与「规格路径」本身给全。

### 规格模板「本批核销」节的口径

- 触发：本批改动面与某归档规格**有交集**（**同一文件／同一主题／同一产物**，三者任一）时，此节必写；无相关归档规格则整节省略，不留空节。
- 核销面：**必列**——① 前一批规格的全部开放项；② 本批改动直接触及或使其状态可能变化的条目（含前批应销未销者）。**不必重列**——已由既往核销表承载、且本批未使其状态变化者，本节只给该表的索引指针（避免两份并行口径）。
- 写法：**只核销、不复述原文**——一行一条，状态取五档之一（已履行／作废／本批处置／仍待／待作者），理由写当时判据或落点（无理由的「仍待」等于没核）。**T4（2026-09-17）**：理由列**只写「五档 ＋ 落点」**（点名条号／件节），**长论述移入该件 §八 开放项**——§七 是索引不是论说文（`109` 实测 §九 占规格 30%，同源教训）。**理由列写「已沉淀」时须点名台账条号（条号形如反模式 `#N`／缺口 `GN`／标准件 `X` §N）；不给条号即视为未沉淀。**
- 表：列名固定为「来源｜条目｜状态｜理由／落点」，末附汇总行。
- 汇总行尾注：**恒列五档、无该档计 0**，且逐格可复核、不得与表列不符。
- 落点纪律：核销落**本批规格**，不另建台账、不建索引件（`AGENTS.md` §四 平行台账不做）；跨批检索以各批规格的核销表为唯一索引落点。

参考研究裁定表（点名研读仓库／规范时作为规格附件，逐条填写）：

| # | 参考条目的主张 | 结论 | 落位或理由 |
| --- | --- | --- | --- |
| 1 | {对方怎么做的} | 采纳／已覆盖／拒绝 | {落位或理由} |

表注（**取值口径**）：**采纳**→落位到文件节与自由度；**已覆盖**→指认我方既有对应；**拒绝**→理由（防未来重复纠结）。

### 审查报告骨架（固定四段，**总字符 ≤ 2,000**；2026-09-17 T3 立）

1. **结论行**：通过／修订后通过／裁决建议 ＋ `findings 共 N：P0:a P1:b P2:c`（**N ＝ 各档之和**，观察项不计入 N）。
2. **逐条**：档位｜对象（`文件:行`／节）｜问题｜**实测命令与输出**｜建议修法——**不复制原文**（长引文只给指针）。
3. **观察项**（不计入 N）＋ **未核项**（无则写「无」）。
4. **所读文件清单**（件:节）＋ **工具往返数**。

> 超此长即由规划方**退回重写**；报告越长，下一轮施工的注意力预算被挤掉越多。

核验请求模板（施工方产出，逐字可复用）：

```
【核验请求】
- 摘要：{改动 N 项｜验收 a/b 绿｜P1×n}
- 规格文件：{规格路径}
- 改动清单：逐文件对照规格改动清单
- 自核验结果：验收断言逐条 + 实际执行命令与输出摘要
- 本次工具往返数：{N}（自述；核验期由规划方核对，**非置红项**）
- 偏差记录：P1 观察型偏差（如有）
- 未决问题：需规划方裁决的点（如有）
```

施工交底骨架（**固定四项**，规划方照填）：

1. **规格路径** ＋ 必读「文件:节」清单——施工默认 `§二`（改动清单）／`§三`（验收断言）／`§五`（禁止事项与豁免）；`§一` 按需、`§九`（审查记录）不读。
2. **身份与权限 ＋ 自由度语义**：施工 subagent／**无 git 写权限**；`[A]` 逐字／`[B]` 按方向／`[C]` 按验收断言。
3. **纪律**：P0 停摆申报（禁改断言、禁改期望）｜P1 继续并写进核验请求「偏差记录」｜实跑**批式**（三列：命令｜实测｜期望，一次输出）。
4. **回报格式**：按下文【核验请求】模板，**整段包在单个代码块**，含本轮工具往返数与分段。

> **其余全部不复述**：渲染／装载面／审查位点名以 `施工机制` 正文与 `规格写作标准` 为准（2026-09-17 T2-3：原「七项」与本节其余说明并入上列四项或删除）。
外部窗口备选形态：作者在另一窗口施工时，首句身份改为「你是施工方」，其余不变。

**射程面已核**：`施工机制:75`（`CHANGELOG` 成本註记真源）与本模板行同为 `R` 的载体 ⇒ 该处**同批补分段示例**（`:75` 的示例一并改为含「分段 ＋ 合计」形态）；**产物审查 O1 订正**：原句称该处「已覆盖、不再另改」，而 `:75` 示例仍为单一值、两载体口径不一，订正如上。

## 四、立例依据

立例依据：旧仓 039 批六轮 58 findings、046 批四稿、043／044 一审 23 findings 后整稿重写；047 批窄域复审 8 次工具调用即抓出 2 处新问题——窄域审判别力已实证。

**史例（核销档位算错）**：`088` 批 §八 10／11／12 三处「已履行」却仍写「触发＝下批」，致下一批核销档位算错——故立「本批内被处置的开放项须同批改标」。
