# 施工机制

> 定位：开发时用的流程纪律——角色、规格流程、核验、审查、止损与模板；协作与流程类规则的单点权威。
> 适用范围：**开发侧**——读者是开发者与开发用的 AI 工具，本件不随产品安装分发；产品运行在 Claude Code 上（`/{skill 名}`），与本文无关（开发／运行分野见根 [`AGENTS.md`](../../AGENTS.md) 头注）。
> 依据：旧仓 AGENTS.md §3（开发节奏与分工协作）与协作模板全套条文（来源件已随 rebuild 作废，条文逐条迁移）；来源件为多域时期内容，已通用化改写，协作模板收敛为三份。

## 目录

- 一、角色与职责
- 二、规格流程（判定表与三条路径分流）
- 三、施工与验收（核验三步／实跑纪律／过程产物两项）
- 四、机器闸（段序真源／守卫纪律）
- 五、审查（成稿审查／产物审查／止损）
- 六、偏差与止损（P0／P1、停摆呈报、反模式清单）
- 七、提交与登记
- 八、模板（规格／核验请求／参考研究裁定，三份）

## 一、角色与职责

协作三角色。触发条件：任何非平凡改动（§二 分流表的「标准」「重」两行）都按本节配角色。

- **规划方**（持全局上下文）：勘察现状、写规格、派生施工交底、spawn 审查 subagent、执行核验三步、统一提交。对规格初稿质量负首要责任——审查是兜底不是替代。
- **施工方**：只按规格施工；**无 git 写权限**，也无需尝试提交。默认形态＝规划方 spawn 的施工 subagent；备选形态＝作者在外部窗口施工（此时作者即施工执行者）。交付物只有一份【核验请求】（§八 模板）。
- **作者**：纯裁决位——规格确认、提交确认两处常规出手；另有**治理微调的会话内逐项确认**（§二 轻行）与**重大分歧裁决**（§六）两类例外。

subagent 纪律（触发条件：规划方 spawn 任一 subagent 时逐条核对）：

- 规格获作者确认后由规划方主动 spawn 施工 subagent，不退回作者粘贴中转、默认流程不经外部窗口；**作者主动选择 `§八` 外部窗口备选形态时除外**。
- **施工 subagent 一律后台 spawn**——前台结束后不可续、整改只能重新 spawn；后台 spawn 不阻塞且可续（补修／整改直接对同一 agent 续发指令，无需重派）；**规划方须在交底约定回报点、完成后主动取回报**（不阻塞不等于不管）。**对外部窗口形态**（§八 备选形态）：无 subagent，本条不适用。
- 审查 subagent 一律只读，不得承接整改或施工。
- 发送指令前核对目标 id 的角色与可续性（反模式 #11：旧仓 044 批整改指令误发复审 subagent）。

## 二、规格流程（判定表与三条路径分流）

三条路径分流表。触发条件：动手改任何文件前先定路径；不确定属哪条时取较重的一条（**同时命中两条 ≠ 「不确定属哪条」**——前者按「轻」行的优先规则定（「标准 × 重」由「重」行流程含标准路径判定），后者才取较重的一条）。

| 路径 | 触发条件 | 流程 |
| --- | --- | --- |
| 轻 | 单文件且 < 5 行纯措辞／格式；或复盘驱动的治理微调 ≤ 3 文件（后者豁免条件缺一不可、另需作者会话内逐项确认、**当批 `CHANGELOG` 留痕不计入**；与其他行命中时——**若同时命中「重」行触发条件，以「重」为准**；否则以本行为准（具体优于一般）） | 直接改 → `bash scripts/check.sh` 全绿 → `CHANGELOG` 一行 |
| 标准 | 跨文件、或增删文件、或改规则 | 勘验现状 → 写规格 → 独立审查（成稿）→ 施工 → 核验 → **（触发产物审查条件时）产物审查** → 请求提交 |
| 重 | 动 `skills/`、改域内流程／规则／产物契约、架构级 | 标准路径 + 产物审查 + 真机走查（真机走查仅当本批改动用户可观测行为时必跑；纯治理／底座改动不跑，豁免理由写进规格） |

- 判据来源是规格写作标准 §1 判定表：「标准」行即该表 ✅ 各行（新增／删除／重命名文件、跨文件改动 ≥ 2 文件、新增或修改规范条目、架构／流程／规则变更、外审与外部参考研究驱动的修改）；「轻」行即该表 ❌ 各行（单文件错别字／格式修正 < 5 行、复盘驱动的治理微调 ≤ 3 文件，后者豁免条件缺一不可，且另需作者会话内逐项确认——归入「轻」不免除该确认）。（轻行细则以 `规格写作标准` §1 判定表原文为准——含「`CHANGELOG` 留痕不计入」与冲突优先规则）。
- **产品层纯文档内容批**（写／改 `docs/product/` 的叙述与设计正文，不动契约字段与规则）按**标准路径**走，产物审查降为**单维核对**（见 §五）——定级判据是「改的是**叙述**还是**规则**」，不是「改了几个文件」。（实例：059／060／061 三批均为改标准／规则／结构 → 双维全审；「单维核对」一侧尚无实例，属预立法，随首批实例校准；**关联实例判据：出现首批产品层纯文档内容批**。）
- 重路径的真机走查做法与记录落点见测试与验收标准 §3，走查记录落当批规格。
- **小改动攒批**：发现的零散欠账与同族残留，优先**夹带进邻近批次**（写进该批改动清单与核销表），不零散开批——一批＝一次施工＋一次审查；只有触发条件独立且紧迫时才单独立批。实例：088 夹带 087 三条账与审查标出的三处同族残留。

规格三要素（缺一不成规格）：现状锚点／文件级改动清单／验收断言。命名与归档位置见命名标准 §2；自由度与深度分级见规格写作标准 §3。

规格自审四条（规格「规格自审」节按此写；缺任一条不得产出施工交底）：

1. 锚点现场核过——规格引用的行号、原文、计数逐条现场 `grep`／read 确认，不凭对话记忆。（反例：旧仓 014 批 3 处现状事实过时，施工时才暴露。）
2. 断言实跑过——每条验收断言写进规格前实际跑一遍（计数用 `wc -l`／`grep -c` 实数，禁目视估计），并核对断言 token 与自家「替换为」侧文本不互斥。（反例：旧仓 020／021／022 三发连击——断言吞替换文本、断言对象与计数错、零命中吞立例句字样。）
3. 禁止事项写明——逐条列出本批禁止触碰的文件与禁止动作（联网、采集、改动面外文件）。
4. **范围与期望同源**——断言若建立在某段范围（文件行段／节／清单）之上，其期望值须由**该范围**实跑得出（范围与期望取自不同侧即失守）。实例：088 成稿审查 F-02——迁移范围写 `:14`–`:52`（不含父标题 `:12`），而期望 4 是按**含** `:12` 的源件算的，施工后必红。

自由度标记：`[A]` 逐字执行（一字之差即偏差）／`[B]` 按方向自定实现（不得改变行为）／`[C]` 按验收标准执行。

## 三、施工与验收（核验三步／实跑纪律／过程产物两项）

核验三步（规划方执行；施工方的自跑结果只作参考）。触发条件：每批施工交付后、请求提交前。

1. 改动面核对（最强一道闸）：`git status --short` 与 `git diff` 逐 hunk 对照规格文件级改动清单——出现清单外文件、或未声明的逻辑行变动＝P0 打回；`[A]` 项与规格给出文本 diff 为零；规格声明不得触碰的文件零 diff。副作用口径：`.tmp/` 等被忽略的临时产物不计（AGENTS.md 红线 2 已辖）；untracked 且未忽略的新文件须对应规格预期新增（首选 `git diff --no-index /dev/null <文件>`——不需写索引；次选 `git add -N <文件>` 后 `git diff`。`--no-index` 有差异时**退出码＝1 属预期**（勿按失败读）；`git add -N` 是索引写操作，只由规划方在核验期执行、**不得由施工方执行**），否则仍 P0。
2. 断言独立重跑：规格验收断言的每条命令用规划方自己的会话重跑；退出码不经管道吞掉（反模式 #6）。
3. 内容抽查：机检覆盖不到的内容质量与规范健康度按 §五 分工走——**触发产物审查条件的批次（改规则／结构／契约，含重路径与动标准条文的批次）派产物审查 subagent**（轻路径批次除外——不派审查 subagent，改后由规划方自查）；产品层纯文档内容批走**单维核对**；**其余批次（不含轻路径——轻路径不写规格、不走核验三步）由规划方通读抽查**。

断言预验：规格验收断言必须在写进规格前实跑（§二 自审条 2）；无法预验的标注「未预验」，交成稿审查 subagent 重点核验。

**实跑纪律（批式）**：断言与门禁的实跑一律**批式**——断言用一个脚本一次跑完并一次输出（每行三列：命令／实测／期望）；门禁命令取尾（`bash scripts/check.sh > .tmp/<任务>/chk.out 2>&1; tail -5`）；大输出先 `head`／`tail` 收敛。**理由**：每次工具往返都要重发整段上下文，逐条单跑是批内成本的主要来源（088 实测 205 次往返／~23.5 M token）。**适用范围**：施工方自跑、规划方核验重跑、审查方抽样实跑三处同一口径。

过程产物两项（非平凡改动必落；无落点即核验未完成）：

1. 规格——细节落当批规格（含「本批核销」（相关时）与「开放项」节）；验收全部通过后移入 `docs/specs/archive/`，移动不留副本。
2. `CHANGELOG`——一行结论，只写「改了什么 + 核验结论」，细节留在规格；**条目限长的规范真源**见 `版本与分发标准` §3。落 `[Unreleased]` 对应小节，版本段定稿见版本与分发标准 §3。

不建全域台账、不建施工记录表。当批欠账当批修完；修不完的写进当批规格的「开放项」节，或该主题所属标准的「机检锚点／已知缺口」行——债跟着主题走，不跟着时间走。

**开放项核销机制**：新批规格须核对**相关归档规格的开放项**，在本批规格内逐条标五档之一（已履行／作废／本批处置／仍待／待作者，含理由）；核销落本批规格的「本批核销」节（模板见 §八），**不另建台账、不建索引件**（`AGENTS.md` §四 平行台账不做）。**本批内被处置的开放项须同批改标**（不得留到下一批核销时才改——088 批 §八 10／11／12 曾三处已履行却仍写「触发＝下批」，致下一批核销档位算错）。触发条件：**本批改动面与某归档规格有交集**（**同一文件／同一主题／同一产物**，三者任一）（**不设时间触发**）。

**修订工具律**：仓内文本修订一律经 `scripts/patch_file.py`（或其等价的三段式实现）——**禁手写长串匹配的临时脚本**；该工具把反模式 #12 的三段式固化为机制（锚点由**磁盘**校验、命中 0 或 >1 即中止并打印实际命中行、统一写盘、回读核验）。触发条件：**以脚本／命令行方式做批量或长串匹配的仓内文本改动**时本条适用；**宿主自带的一次一处、要求唯一匹配的编辑工具（Edit）不受本条约束**，但改前须 `grep` 取原文（#18）。

修复后自核验四条（谁修谁核）：① 修改已落实到位（**逐条 `grep`／read 核验目标文本确在文件中，不得凭脚本输出判「已改」**） ② 无遗漏点 ③ 无衍生新问题 ④ **断言对账**：每轮修订后逐条核断言 token／期望与正文一致（**能机械化者由 `check.sh` 的规格静态自检段报——段序真源见该脚本头注**（各段现状见 `测试与验收标准` §1.1）；报不到者以此条兜）——四项核完才算修复完成。（旧仓教训：修复引入双空格与 `CHANGELOG` 游离行，靠 lint 与事后复核才抓回。）

## 四、机器闸（段序真源／守卫纪律）

- 段序真源＝`scripts/check.sh` 头注（唯一真源，本文件不复制段序列表）；新增检查项按头注的维护入口追加一段并同步头注。守卫全景、各段现状与已知缺口见测试与验收标准 §1／§4。
- 何时必须跑：改任何文件后 `bash scripts/check.sh` 必须全绿（pre-commit 自动执行）；提交前必跑（§七）。有红先判因再改，呈报裁决前不在红基线上叠改动（AGENTS.md 红线 6）。
- 守卫纪律：守卫零外部依赖、不跑 LLM、不联网；失败先判因——禁止改断言、改数字、删检查来变绿。
- 边界声明：核验通过／全绿不等于运行时行为已验证——仅覆盖已检项与已知模式（扫描只能证伪不能证真）；汇报全绿时须声明已检范围（哪几段已实现、哪几段待建）。
- **守卫变更回写清单**：改 `check.sh` 段或 `check_*.py` 的检查项时，须**同批核四处**——① `测试与验收标准` §1.1 表（该段的「管什么」与「现状」——**本项的义务真源是该表 `:18` 的既有口径注「段序变更时须同步本表」，本条扩充其触发面为「检查项变更」并引之，不另立第二载体**）② `AGENTS.md` §七 自检信号（凡述及该守卫状态者）③ **`check.sh` 头注的段行与段名**（段序真源）④ 工具自身的**模块 docstring**（与同件内联注释、正则实现三者一致）。触发条件：**任何检查项的新增／删除／改判据**。本清单不设守卫（回写面含语义判断，机械化成本高于收益）；漏回写按 P1 观察型处置（§六 分级）。
- **规则新增回写清单**：新增／修改任一**硬约束类条文**（`须`／`不得`／`禁止`级）时，须同批核**该条文的射程面**（**由条文自身描述限定**——如新增反模式行须核该族全部入口是否已回填；新增标准件条文须核该标准件内射程面；新增术语规则须核术语引用面）。**发现射程面内有不满足者，同批补齐**；**本句为回写纪律的最后一层**——不得再为「回写纪律本身」立回写规则（续立即违本句），其失效由**独立上下文审查**发现。（**正例**：本条自身即 F5 `#9` 事故的直接产物；**反例**：`095` 批 `规格写作标准` §8 立规未回扫存量——见 `施工机制` §六 `#9` 行事故出处）。

## 五、审查（成稿审查／产物审查／止损）

两个独立上下文 subagent 位。判据核心：评审者与创作者必须为不同上下文（防意图污染与自审偏袒）——规划方知道自己为什么这么写，看不见自己规范里的过时与矛盾（旧仓实证：某机制退役后理念层残留，规划方三轮自审未发现，独立上下文 subagent 首跑即报）。

| 位置 | 何时派 | 审什么 |
| --- | --- | --- |
| 成稿审查（规格侧） | 标准与重路径：规格成稿后、交作者确认前 | 规格本身四查 ＋ 报告纪律——锚点真实性、改动清单完备可执行、验收断言可判定（含断言与 `[A]` 文本不互斥、断言对象实存、计数正确）、内部矛盾（含与既有条文冲突）；**报告纪律**见表格下方的纪律行 |
| 产物审查（产物侧） | **改规则／结构／契约**的批次（含重路径与动标准条文的批次；轻路径批次除外，见 §三 第 3 步）：施工完成且核验第 1／2 步走完后。**产品层纯文档内容批**改派**单维核对**（**单维核对由独立上下文的只读 subagent 执行**——判据同 §五 判据核心句：评审者与创作者必须为不同上下文；覆盖面见本行既列三面，不逐段通读；并引本条「subagent 不复用」条） | 两个维度——内容质量（语义通顺／逻辑自洽／数字一致／无断裂截断，重点看替换边界与新旧文衔接处）；规范健康度（本次改动是否使既有条文被现实违反或过时）。**单维核对**只覆盖其中的引用／数字／冲突面 |

- **机器已覆盖面不重复**：派审查 subagent 的交底须附『本批 `[4]` 段已覆盖的面』（**面清单不复述，口径见 `测试与验收标准` §1.1**）；审查方**跳过该面**、只做机器做不到的语义面（锚点真实性／改动清单可执行性／内部矛盾／内容质量）。**不删审查面，只删重复**。
- **报告纪律**：**计数／行数／条数类发现须附实测命令与输出**（`wc -l`／`grep -c` 等）对**成稿审查／产物审查／单维核对三处审查位通用**，未附者标注「估计」且不计入 findings（只能作观察项）。
- subagent 不复用：同一批的成稿审查与产物审查（含单维核对）必须是两个独立上下文 subagent（对象不同、时点不同）。复用会把施工期上下文带进产物审查。
- 报告落点：审查报告经规划方复核（subagent 也会误报）后转述入当批核验结论，并逐条追加到当批规格的「审查记录」节——不另建台账；采纳的修订入规格后才交作者确认。
- 无裁量：标准与重路径**一律送审**，无「感觉很小」式逃逸（旧仓 033／034 未送审即呈作者系旧病复发）；**送审深度**按 §二 分级——改规则／结构／契约批双维，产品层纯文档内容批单维核对。

止损三条（审查螺旋）。触发条件：任一审查位进入第 2 轮及以上时对照本节。

1. 全文审硬上限 2 轮：同一规格全文审最多 2 轮（初稿一轮、修订后一轮）；第 3 轮起只允许微修已知项 + 呈作者，不得再开全文审。规格前提失效时走作废重出——移入 `docs/specs/archive/` 并标废（文件名追加 `-已作废`），有效内容另批重新锚定后重出，不在失效前提上反复续修。
2. 重审只核修复面：findings 中 P0 ≥ 2 时，修订后只核原 P0 修复项 + 其直接影响面，不重开全文审；窄域审须逐条实跑自证（附命令输出），结论与全文审等效。对称条款：产物审查 findings 中 P0 ≥ 2 时，修订后重派产物审查 subagent 复审一次（单维核对同此）。
3. 本条 1／2 的触发条件由原「同类精度 P0（计数／断言互斥／范围失控三类）」放宽为任意 P0 族。

立例依据：旧仓 039 批六轮 58 findings、046 批四稿、043／044 一审 23 findings 后整稿重写；047 批窄域复审 8 次工具调用即抓出 2 处新问题——窄域审判别力已实证。

## 六、偏差与止损（P0／P1、停摆呈报、反模式清单）

偏差分级（施工方与审查 subagent 同一口径）：

| 级别 | 判定 | 处置 |
| --- | --- | --- |
| P0 阻塞型 | 规格与现状冲突、或违反红线，继续会做错 | 施工方立即停摆，全部输出改为偏差申报（禁止顺手按自己理解改断言或改期望）；规划方裁定后重落 |
| P1 观察型 | 规格含糊、边界模糊，不影响施工 | 按最合理理解执行 + 标注假设 + 核验请求偏差记录中申报；规划方裁定，作者可推翻 |
| P2 建议 | 风格／优化，不影响正确性 | 记录，不阻塞提交 |

- 停摆呈报：施工方遇 P0 一律停摆——旧仓 044 批施工方按纪律停摆申报（规格两节计数互斥），规划方裁定后产物侧零改动。
- 分歧升级：规划方与施工方对同一 finding 的性质或修法有分歧时，升级为裁决请求呈作者。禁止规划方压制施工方的 P0 报告，或施工方擅自把 P0 降为 P1。
- 失败回滚：验收不通过不得改断言求绿；规格前提失效走作废重出（§五 止损条）；已发布版本发现严重缺陷用 bump patch 重新发布，不删除已发布 tag（版本与分发标准 §6）。

### 反模式清单（事故回填）

> 格式：「错误做法 → 正确方式 + 事故出处」。继承自旧仓的通则出处标注「继承」；本项目事故发生后按此格式回填，并同步评估是否值得引入对应守卫。
> **每批复盘 findings 时须判「本批发现是否构成新族」**——构成即按本表格式回填（错误做法 → 正确方式 ＋ 事故出处），并同步评估可否机械化为零外部依赖断言（`AGENTS.md` §四 解冻句）；**只修实例不回填族＝本项失守**。

| # | 错误做法 | 正确方式 | 事故出处 |
| --- | --- | --- | --- |
| 1 | 为过检改断言／改守卫数字 | 先判因；文档数字与实测对齐 | 继承 |
| 2 | 删改机制留文档残留（或反之） | 当次全仓 grep 清理全部引用；grep 词须含概念近义词，不止原句 | 继承（旧仓 032 立法只搜原句，姊妹文档「触发关键词」措辞漏网至次日审计） |
| 3 | 模板／文档写「格式同上、参照上文」式互引 | 显式内嵌完整内容或指向真实路径 | 继承 |
| 4 | 同一规范复制多份维护 | 单一真源 + 引用，发现第二来源即收口 | 继承 |
| 5 | CI／守卫里跑 LLM 或联网 | 场景走查人工判断 + 脚本断言；守卫零外部依赖 | 继承 |
| 6 | 验收命令的退出码经管道被吞（`cmd \| tail` 后取 `$?` 是 `tail` 的） | 退出码不经管道直接取；必须接管道时取 `PIPESTATUS[0]` | 2026-09-05 lint 体检首轮误判违例数 |
| 7 | 给带 shebang 的脚本插入头部注释（把 `#!` 挤离首行） | 注释插在 shebang 之后；脚本改动后必跑语法检查（`node --check`） | 2026-09-05 移植审计发现 6 脚本语法全坏 |
| 8 | `git add -A` 后直接提交，不核对暂存内容——提交标签与实际内容不符 | 提交前必看 `git status`／`git diff --stat` 对照预期改动清单 | 2026-09-06 核验协议提交扫入施工方未验收改动 |
| 9 | 纪律靠自觉不靠机制——写完纪律条文就以为会自动执行 | 可机械化的纪律固化为工具（`check.sh`／hook／脚本），不可机械化的写明触发条件 | 2026-09-06 全天多次「该查没查／该走规格没走」（shebang、治理同步、外审未转规格）**＋ 2026-09-15 两笔自有事故**：`规格写作标准` §8 新规未回扫存量预立法标记（8 处＝`095` 口径含定义句；`096` 订正为 **7 处标记**）、`施工机制` §八 交底装载路径补丁未回填族——**均为 `#9` 的实例化** |
| 10 | 在源仓库内运行分发／安装类工具（安装器把 `skills/` 符号链接化） | 分发工具只在目标项目目录运行；仓库加真目录守卫（`check.sh` [0]）拦截 | 2026-09-07 首次真机安装实测（旧仓规格 027） |
| 11 | 把施工／整改指令发给错误对象（未核对目标 id 的角色与可续性） | 发送前核对目标 id 的角色与可续性（§一 subagent 纪律） | 2026-09-11 旧仓 044 批整改指令误发复审 subagent（即时 interrupt + 撤回，该 subagent 回报未改动、零损失） |
| 12 | 以为改了、实际没改——修订脚本失败未检查，且未回读核验文件实际状态 | 修订类脚本三段式（全量校验 → 统一写盘 → 回读核验），改后逐条 `grep`／read 核验目标文本确在文件中 | 2026-09-14 076 窄域复核：两次 `.tmp` 脚本末尾报错中止在写盘之前，12 处改动零落地，审查方逐字读文件发现 |
| 13 | 在反引号片段内写首尾带空格的串 | 改述为文字（如「井号加一空格」），或用无边缘空格的等价写法 | 2026-09-15 088–089 批（本会话同型六次，MD038） |
| 14 | 在 markdown 表格单元内写裸竖线（含反引号内；含把整行表格塞进单元） | 单元内竖线改用全角占位，或改述为文字（如「半角竖线」） | 2026-09-15 089 批两次（MD060） |
| 15 | 以「锚点行前插」的方式增补有序列表项（新项落到锚点项之前，序号顶翻） | 锚点取**插入点之前**的那一项，或插入后逐项核对序号 | 2026-09-15 088 §八 15／16 与 089 §八 6／7（MD029，两次同型） |
| 16 | 在表格单元内写内联 HTML（如换行标签） | 改述为分行列表或表后独立段落 | 2026-09-15 088 批（F1／F2 单元格各一次，MD033） |
| 17 | 处理中文时用非仓内临时路径或按字节切的工具（`/tmp` 在 Git Bash 与 Windows python 之间不互通；`cut -c`／`rev` 对中文按字节切） | 临时件一律落**仓内** `.tmp/`；中文一律用 python 处理 | 2026-09-15 089 批（各一次：读输出文件失败、控制台乱码） |
| 18 | 凭记忆写 Edit／replace 的匹配键（未先取原文、批量替换不带计数断言） | 改前先 `grep` 取原文；批量脚本**每个替换带计数断言**（不为 1 即中止） | 2026-09-15 088–089 批（本会话 ≥4 次；089 一次匹配到 2 处，靠计数断言写盘前中止、零误改） |
| 19 | 用截断输出或**放宽后的判据**下结论／登记「属预期」 | 结论基于全量输出；登记「属预期」须附**原样**取证（不得先放宽判据再取证） | 085 窄域复审（截断输出判「无标尺字样」而实含）＋ 2026-09-15 089 批（V-01 伴生：真因被放宽取证盖住） |
| **20** | 以「手写临时脚本 ＋ 凭记忆写的匹配键」改仓内文本（中止信息只有计数、须再猜一轮） | 改用 `scripts/patch_file.py`：锚点由磁盘校验、命中 0 或 >1 即中止并打印实际命中行（**本行取代 #18 的手写脚本路径**；#18 保留为事故记录——防两行并置产生相反指引） | 2026-09-15 089–090 批：同型中止三次（089 内该串命中 2 行；090 两次 `count=0`） |
| 21 | 改一处、忘了它使自家断言／数字失效 | 改动落地后逐条重算受影响的断言与计数——含本批新增的样本串与新增链接数 | `092` 窄域 N-1、`092` 窄域复审·补修面 P1-1 链接数 175→176、`094` 成稿 P0-1 断言 4-1 期望 4→5——**三次同族** |
| 22 | 同载体多副本漏改 | 改一处枚举／判据前先 `grep` 该形态的**全部载体**，同批并改 | `089` F9、`093` P1-3 九行状态收口、`095` 成稿 P1-1 同件第二处枚举／P1-2 改动清单漏 F 行 |
| 23 | 文档里的状态句与计数句随实现漂移 | 状态句**指真源**（不写死「待建／已实现」）、计数句随批重测 | `093` P1-5「20 件全部超阈」实为 31 件／`095` P1-1「`[3]` 待建」而 `[3]` 已实现 |
| 24 | 工具判据遇自家产物即失效 | 判据的样本来源与判据实现分离——转义行、括注复述旧值、嵌套引号都会让判据读错自家文本 | `093` 的 `\|` 盲区（检查 C 静默跳过含转义竖线的自家样本行）、`093`／`094` **批头注**括注复述旧值被检查 G 读作声明值 |
| 25 | 自检与自评同源 | **不得以自己写的检查断言自己的成品并据此宣布「无问题」**——汇报全绿须**声明已检范围**（既有条文明文，`施工机制` §四 边界声明），用语止于「已检 X／未检 Y」，不出现「没问题」类总评 | `092`–`095` 四批的独立上下文审查各挑出语义级问题（逐批可核） |

### 已知缺口（协议／协作类）

> 落点分工：机检／工具类缺口落测试与验收标准 §4，规范文本类落各标准件的待收口注（现无存量），协议／协作类落本节。触发条件到来时须核对并更新（当批开放项核对，见 §三）。

| 编号 | 缺口 | 触发条件 |
| --- | --- | --- |
| 协1 | 施工交底不设模板（本文件只给必带七项，见 §八），旧仓「提示词一律从模板派生」条随之失效——是否补模板待定 | 出现交底漏项事故时 |

## 七、提交与登记

- 提交需作者明确确认：**凡改变仓库状态的 git 命令**（commit／push／tag／分支等，含只改 `.git/` 者）每次单独取得作者确认，作者对其他操作的授权不外延（AGENTS.md 红线 1）。AI 会话不自建分支——批次为原子单元直推当前工作分支（`git branch --show-current` 返回值；作者明确要求除外）。
- 提交前必跑 `bash scripts/check.sh` 且退出 0（pre-commit 自动执行，见 §四）。
- 提交前自检四项：改动未越出规格范围（**轻路径无规格，该项改按「改动未越出轻路径触发条件」自检**）；验收断言全绿；术语与[术语表](../product/术语表.md) 一致；新增文件符合命名标准。
- 一域一批：一次提交只承载一个域的批次改动，跨域改动拆批。多批在制时提交前逐路径对照 `git status`，禁 `git add -A` 后直接提交（反模式 #8）。
- 提交信息用 Conventional Commits：`type(scope): 主题`；type ∈ {feat, fix, docs, chore, test}，scope 取改动作用域——skill 资产＝`skill`，单域建设＝域名（见 `docs/product/架构设计.md` §一 域清单，如 `abzu-outline`），治理文档＝`governance`，标准件＝`standards`，规格＝`specs`，**开发守卫＝`scripts`**（`check.sh`／`check_spec.py` 等；其改动为主作用域的批次取 `scripts`；074／075 两批按历史用 `standards`／`governance`，不复改）。
- 登记：本批收尾只核两处落点（§三 过程产物两项）——规格（含本批核销与审查记录）与 `CHANGELOG` 条目；规格随验收移入 `docs/specs/archive/`。不设施工记录表。
- 归档件纪律：`docs/specs/archive/` **原则只读**；**两处例外**：① 移入时的作废头注；② **归档位移造成的机械断链可只改路径**——被修件为归档 070 件的那条相对链接，经作者追认；对照先例见 071 §九 第 3 条（同型处置：归档位移断链只改路径）与 `064` §七 第 5 条（归档件历史记录不改）——两处并列引用，非传递链；除上述两处外，归档后不再修改；**作废批移入时即带 `-已作废` 后缀，归档后不再重命名**；跨批检索以各批规格的核销表为唯一索引落点。

## 八、模板（规格／核验请求／参考研究裁定，三份）

规格模板（逐字可复用）：

```markdown
# {NNN}-{YYYY-MM-DD}-{中文主题}

> 定位：{一句话说本批解决什么}
> 自由度分布：{如 [A]×3 [B]×1}
> 必读输入：{本批需要读的既有文件与节；不涉业务写「无」}
> 施工形态：{subagent／外部窗口}（{模型标识}）

## 一、现状锚点（现场核过）

- {文件}:{行}：现文「{原文}」；实测计数 {N}（命令：{grep -c 类命令}）。

## 二、文件级改动清单

| # | 文件:位置 | 改动（现状 → 目标） | 自由度 | 依据／裁定 |
| --- | --- | --- | --- | --- |
| 1 | {路径}:{行} | {现状} → {目标} | [A] | {理由／来源} |

## 三、验收断言

1. 命令型：`{可直接执行的命令}` → 期望 {值}。
2. 走查型：{做法} → 通过判据 {判据}。
3. 过程产物：验收通过后规格移入 `docs/specs/archive/`；`CHANGELOG` 条目已落。

## 四、规格自审（四条）

1. 锚点现场核过：{命令与输出摘要}
2. 断言实跑过（与「替换为」侧文本不互斥）：{命令与输出摘要}
3. 禁止事项已写明：见 §五
4. **范围与期望同源**——断言若建立在某段范围（文件行段／节／清单）之上，其期望值须由**该范围**实跑得出（范围与期望取自不同侧即失守）。实例：088 成稿审查 F-02——迁移范围写 `:14`–`:52`（不含父标题 `:12`），而期望 4 是按**含** `:12` 的源件算的，施工后必红。

## 五、禁止事项

- 禁止触碰：{文件／目录清单}
- 禁止动作：{如联网采集、改动面外文件}

## 六、被否决的替代方案（可选）

- {方案} → {否决原因：约束／成本／失败模式}（无方案比较则整节省略，不留空节）

## 七、本批核销（相关归档规格的开放项）

| 来源 | 条目 | 状态 | 理由／落点 |
| --- | --- | --- | --- |

- 汇总：`按条计 n 条：已履行 a｜作废 b｜本批处置 c｜仍待 d｜待作者 e ＝ n`

## 八、开放项

- {当批修不完的欠账 → 落点}

## 九、审查记录

- {日期}｜{成稿审查／产物审查}｜findings 共 {N}：P0:{a} P1:{b} P2:{c}｜结论 {通过／修订后通过／裁决建议}
```

规格模板头注四行：定位／自由度分布／必读输入／施工形态；施工形态字段由规划方在写规格时填默认形态（subagent）——外部窗口形态下模型标识由作者提供，实际变更同批订正。**必读输入按「文件:节」点名**（不得只写整件）；交底与施工默认**只读点名节**，未点名的节不读——**整件点名（如 `AGENTS.md`）即读整件**；装载量是每个 subagent 的成本主项（088 实测定名十件共 190 KB，实际只用到少数节）。

### 规格模板「本批核销」节的口径

- 触发：本批改动面与某归档规格**有交集**（**同一文件／同一主题／同一产物**，三者任一）时，此节必写；无相关归档规格则整节省略，不留空节。
- 核销面：**必列**——① 前一批规格的全部开放项；② 本批改动直接触及或使其状态可能变化的条目（含前批应销未销者）。**不必重列**——已由既往核销表承载、且本批未使其状态变化者，本节只给该表的索引指针（避免两份并行口径）。
- 写法：**只核销、不复述原文**——一行一条，状态取五档之一（已履行／作废／本批处置／仍待／待作者），理由写当时判据或落点（无理由的「仍待」等于没核）。
- 表：列名固定为「来源｜条目｜状态｜理由／落点」，末附汇总行。
- 汇总行尾注：**恒列五档、无该档计 0**，且逐格可复核、不得与表列不符。
- 落点纪律：核销落**本批规格**，不另建台账、不建索引件（`AGENTS.md` §四 平行台账不做）；跨批检索以各批规格的核销表为唯一索引落点。

参考研究裁定表（点名研读仓库／规范时作为规格附件，逐条填写）：

| # | 参考条目的主张 | 结论 | 落位或理由 |
| --- | --- | --- | --- |
| 1 | {对方怎么做的} | 采纳／已覆盖／拒绝 | 采纳→落位到文件节与自由度；已覆盖→指认我方既有对应；拒绝→理由（防未来重复纠结） |

核验请求模板（施工方产出，逐字可复用）：

```
【核验请求】
- 摘要：{改动 N 项｜验收 a/b 绿｜P1×n}
- 规格文件：{规格路径}
- 改动清单：逐文件对照规格改动清单
- 自核验结果：验收断言逐条 + 实际执行命令与输出摘要
- 偏差记录：P1 观察型偏差（如有）
- 未决问题：需规划方裁决的点（如有）
```

施工交底必带七项（交底不设模板，规划方按本节当面撰写）：

1. 规格路径，且要求先读规格全文与 `AGENTS.md` 再动手；
2. 身份与权限：施工 subagent／施工方，无 git 写权限，也无需尝试提交；
3. 自由度标记语义（`[A]` 逐字／`[B]` 按方向／`[C]` 按验收断言）；
4. 偏差分级与停摆条（P0 停摆申报、禁止改断言；P1 继续并在核验请求中申报）；
5. 输出要求：完成后按上模板输出【核验请求】全文，整段包在单个代码块里；
6. 必读输入清单（逐条列本批该读的文件与节）——**凡本批触及 `skills/` 资产正文（改其 `[A]`／`[B]` 落点）者，默认必带 `skill形态标准` §6（**结构真源**：每类文件必备节序——workflow 四节、方法论底线与两子类、产物模板四段式与三要素、阶段文件七字段单元）与 §13（**文件类别 × 内容真源 × 头部规范 × 风格依据 × 机检** 的类别索引表）**——那是「**写这类文件之前该知道什么**」的真源，交底**不得只凭规划方复述**（复述有损：本文所指的规范此前从未被点进任何交底）；**按节点名规则见 §八 头注说明**（本节原有指针，补回）。
7. **实跑须批式**（规则见 §三 实跑纪律）；报告计数类发现时附实测命令与输出（未测者标「估计」）。

外部窗口备选形态：作者在另一窗口施工时，首句身份改为「你是施工方」，其余不变。
